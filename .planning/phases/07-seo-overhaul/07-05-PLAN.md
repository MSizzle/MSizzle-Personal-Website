---
phase: 07-seo-overhaul
plan: 05
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/app/prometheus/page.tsx
autonomous: true
requirements: []
decisions_addressed:
  - D-07
  - D-08
  - D-09
  - D-10
  - D-14
  - D-15
  - D-16
  - D-22

must_haves:
  truths:
    - "src/app/prometheus/page.tsx exists"
    - "Page renders FAQPage JSON-LD with the four exact D-14 Q&As via buildFaqPageSchema"
    - "Page renders Breadcrumbs with items [{name:'Home',href:'/'}, {name:'Prometheus'}]"
    - "Page contains headline 'Prometheus' and subhead 'AI Integrations and Education'"
    - "Page includes both anonymized case studies (orthodontic PDF pipeline, hospitality research tool)"
    - "Page CTA links to https://prometheus.today"
    - "Metadata title is 'Prometheus | AI Integrations and Education | Monty Singer'"
  artifacts:
    - path: "src/app/prometheus/page.tsx"
      provides: "New Prometheus route with FAQ schema"
      contains: "buildFaqPageSchema"

threat_model:
  summary: "New static server-rendered page. No user input, no forms, no external data."
  threats: []
---

<objective>
Ship `/prometheus` per D-22 with the exact structure and copy from PRD §4 + the four-question FAQPage JSON-LD from D-14.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-seo-overhaul/07-CONTEXT.md
@.planning/phases/07-seo-overhaul/07-01-PLAN.md
@claude-code-prompt.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create /prometheus page</name>
  <files>src/app/prometheus/page.tsx</files>
  <read_first>
    - src/app/about/page.tsx (reference after Plan 04 for structure, Breadcrumbs usage)
    - src/lib/seo/schemas.ts (buildFaqPageSchema)
    - src/components/seo/json-ld.tsx (JsonLd)
    - src/components/seo/breadcrumbs.tsx (Breadcrumbs)
    - .planning/phases/07-seo-overhaul/07-CONTEXT.md (D-14, D-22)
  </read_first>
  <action>
Create `src/app/prometheus/page.tsx`:

```tsx
import type { Metadata } from 'next'
import { Breadcrumbs } from '@/components/seo/breadcrumbs'
import { JsonLd } from '@/components/seo/json-ld'
import { buildFaqPageSchema } from '@/lib/seo/schemas'

export const metadata: Metadata = {
  title: 'Prometheus | AI Integrations and Education | Monty Singer',
  description:
    'Prometheus is an AI integrations and education company founded by Monty Singer. Custom automation, AI implementation, and training for businesses.',
  alternates: { canonical: '/prometheus' },
  openGraph: {
    title: 'Prometheus | AI Integrations and Education | Monty Singer',
    description:
      'Prometheus is an AI integrations and education company founded by Monty Singer. Custom automation, AI implementation, and training for businesses.',
    url: '/prometheus',
    type: 'website',
  },
}

const FAQS = [
  {
    question: 'What does Prometheus do?',
    answer:
      'Prometheus helps businesses implement AI into their workflows through custom automation pipelines, AI tool integration, and hands-on education and training.',
  },
  {
    question: 'Who is Prometheus for?',
    answer:
      'Prometheus works with small and mid-size businesses, professional services firms, and healthcare practices looking to integrate AI tools and automate repetitive workflows.',
  },
  {
    question: 'What kind of AI solutions does Prometheus build?',
    answer:
      'Prometheus builds custom automation pipelines, AI-powered document processing tools, email and communication agents, workflow automation, and provides AI education and training for teams.',
  },
  {
    question: 'Who founded Prometheus?',
    answer: 'Prometheus was founded by Monty Singer in 2026.',
  },
]

export default function PrometheusPage() {
  return (
    <>
      <Breadcrumbs items={[{ name: 'Home', href: '/' }, { name: 'Prometheus' }]} />
      <JsonLd data={buildFaqPageSchema(FAQS)} />

      <article className="mx-auto max-w-[66ch] px-6 pt-8 pb-16 md:px-0">
        <h1 className="text-2xl font-normal tracking-tight sm:text-3xl">Prometheus</h1>
        <p className="mt-2 text-sm uppercase tracking-widest opacity-50">
          AI Integrations and Education
        </p>

        <div className="prose mt-8 max-w-none">
          <p>
            Prometheus helps businesses implement AI into their workflows. From custom
            automation pipelines to hands-on AI education, Prometheus bridges the gap
            between what AI can do and what businesses actually need. Founded by Monty
            Singer.
          </p>

          <h2>What We Do</h2>
          <ul>
            <li>Custom AI automation pipelines</li>
            <li>AI tool implementation and integration</li>
            <li>AI education and training for teams</li>
            <li>Workflow optimization with AI</li>
          </ul>

          <h2>Case Studies</h2>
          <p>
            <strong>An orthodontic practice.</strong> Built an automated pipeline that
            converts patient PDF reports into formatted PowerPoint presentations,
            eliminating hours of manual work per week. HIPAA-compliant local architecture.
          </p>
          <p>
            <strong>A boutique hospitality company.</strong> Scoped and designed a custom
            research tool and website for a property rental business, including automated
            content workflows.
          </p>

          <h2>Work with Prometheus</h2>
          <p>
            Visit{' '}
            <a href="https://prometheus.today" target="_blank" rel="noopener noreferrer">
              prometheus.today
            </a>{' '}
            to learn more, or{' '}
            <a href="mailto:mds345@georgetown.edu">get in touch</a>{' '}
            to discuss a project.
          </p>
        </div>
      </article>
    </>
  )
}
```

All copy verbatim from PRD §4 and §11 except unavoidable reformatting (paragraph breaks between case studies for readability).
  </action>
  <verify>
    <automated>test -f src/app/prometheus/page.tsx && grep -q "buildFaqPageSchema" src/app/prometheus/page.tsx && grep -q "orthodontic" src/app/prometheus/page.tsx && grep -q "boutique hospitality" src/app/prometheus/page.tsx && grep -q "prometheus.today" src/app/prometheus/page.tsx && grep -q "Prometheus | AI Integrations and Education | Monty Singer" src/app/prometheus/page.tsx && ! grep -q "&mdash;\|—" src/app/prometheus/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - src/app/prometheus/page.tsx exists
    - Contains `buildFaqPageSchema` import and call
    - Contains `<Breadcrumbs` component
    - Contains the four D-14 answer strings (case-sensitive substring match on "Prometheus helps businesses implement AI", "small and mid-size businesses", "custom automation pipelines, AI-powered", "Prometheus was founded by Monty Singer in 2026")
    - Contains the two case-study markers: "orthodontic practice" and "boutique hospitality"
    - Contains `https://prometheus.today`
    - Metadata title exactly `'Prometheus | AI Integrations and Education | Monty Singer'`
    - No em dashes
  </acceptance_criteria>
  <done>/prometheus live with Breadcrumbs + FAQPage JSON-LD + CTA.</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds and the route `/prometheus` is emitted.
- Manual: visit `/prometheus` — breadcrumb shows Home / Prometheus, headline and subhead visible, four Q&As in page body match D-14, CTA link works.
- View source: two `application/ld+json` blocks (BreadcrumbList + FAQPage).
</verification>

<rollback>`rm -rf src/app/prometheus`</rollback>