---
phase: 20-mono-token-foundation
plan: 02
type: execute
wave: 2
depends_on: ["20-01"]
files_modified: [src/app/globals.css]
autonomous: true
requirements: [MO-02, MO-03]

must_haves:
  truths:
    - "Every hover/emphasis rule in globals.css that used to read var(--accent) now either inverts to black (Tier 1), keeps its ink color with a text-decoration/opacity-only reveal (Tier 2), or fills its outline stroke solid (Tier 3) — never a hue"
    - "grep for var(--accent) or var(--color-accent) anywhere in globals.css returns zero hits"
  artifacts:
    - path: "src/app/globals.css"
      provides: "fully accent-free rule set with the three-tier D-09 hover language applied"
      contains: "var(--color-invert)"
  key_links:
    - from: "globals.css Tier 1 fills (e.g. .pb-note, .mm-sub, .nav-cell::before)"
      to: "--color-invert / --color-text-inverse tokens defined in 20-01"
      via: "CSS var() reference"
      pattern: "background: var\\(--color-invert\\)"
---

<objective>
Convert every remaining `var(--accent)` / `var(--color-accent)` reference in `globals.css` — the ~35
sites the branch/token/hardcoded-hex work in 20-01 didn't already touch — to the three-tier D-09 mono
hover language: block surfaces invert, inline text links keep their ink color and reveal via
underline/opacity, oversized display type fills its outline stroke solid.

Purpose: This is the bulk of MO-03 ("emphasis and interaction states are expressed by inversion and
type weight, never by hue") for the shared stylesheet layer. Every component in 20-03/20-04 renders
through these rules.
Output: `globals.css` with zero remaining `var(--accent)`/`var(--color-accent)` references.

Runs on the `v4-mono` branch created in 20-01 (D-12/D-13) — do not create a new branch or worktree for
this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/20-mono-token-foundation/20-CONTEXT.md
@.planning/phases/20-mono-token-foundation/20-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Convert Tier 1 (block-surface) accent fills and shadows</name>
  <files>src/app/globals.css</files>
  <read_first>
    src/app/globals.css (lines 233-264, 677-740, 822-902, 960-1024, 1060-1079, 1320-1368, 1443-1490)
    .planning/phases/20-mono-token-foundation/20-CONTEXT.md (D-09 Tier 1, D-05 token names)
    .planning/phases/20-mono-token-foundation/20-PATTERNS.md (Part 1 globals.css rows; Part 3 Tier 1 list)
  </read_first>
  <action>
    Convert every remaining Tier 1 (block-surface) `var(--accent)`/`var(--color-accent)` reference.
    Do not touch lines already fixed by 20-01 (618, 674, 1285, 1289, 1485). For drop-shadow uses of
    accent (not fills), use `var(--color-text)` instead of `var(--color-invert)` — both resolve to
    `#000000` in mono, but `--color-text` is the file's existing hard-offset-shadow token (e.g. the
    `.pb-frame` hover shadow already uses `var(--color-text)`), so match that convention rather than
    introducing a second black token for the same visual role.

    FILLS → `var(--color-invert)`:
    - Line 258 `.marker, h1.sig .hw`: `background: var(--accent);` → `background: var(--color-invert);`
    - Line 467 `.statustag .dot`: `background: var(--accent);` → `background: var(--color-invert);`
    - Line 682 `.hero-band`: `background: var(--accent);` → `background: var(--color-invert);`
    - Line 827 `.mm-sub`: `background: var(--accent);` → `background: var(--color-invert);`
    - Lines 881-882 `.mm-btn:hover`: `background: var(--accent); border-color: var(--accent);` →
      `background: var(--color-invert); border-color: var(--color-invert);`
    - Line 898 `.mm-dots span.on`: `background: var(--accent);` → `background: var(--color-invert);`
      (this is a background fill on a decorator dot, not text — reclassified from PATTERNS.md's
      "Inline Text" label to Tier 1 here, consistent with the sibling `.avail`/`.statustag` dots which
      are Tier 1 fills too)
    - Line 982 `.nav-cell::before`: `background: var(--accent);` → `background: var(--color-invert);`
    - Line 1015 `.stickynav .cta`: `background: var(--accent);` → `background: var(--color-invert);`
    - Line 1076 `.avail .dot`: `background: var(--accent);` → `background: var(--color-invert);`
    - Line 1326 `.pb-play > span`: `background: var(--accent);` → `background: var(--color-invert);`
    - Line 1355 `.pb-note`: `background: var(--accent); color: var(--color-bg);` → `background:
      var(--color-invert); color: var(--color-text-inverse);` (sketch 015: "the note panel is black now")
    - Line 1457 `.pb-btn:hover`: currently text-only `color: var(--accent);`. Reclassify to Tier 1 — D-09
      names buttons as block surfaces, and `.pb-btn` already has a border/box-shadow chrome like a real
      button (unlike a plain inline link), matching the sibling `button.tsx` non-accent variant's
      existing `hover:bg-text hover:text-bg` pattern. Replace with: `background: var(--color-invert);
      color: var(--color-text-inverse); border-color: var(--color-invert);`
    - Lines 1459-1460 `.pb-btn--go`, `.pb-btn--go:hover`: `background: var(--accent); color:
      var(--color-bg);` and `:hover { color: var(--color-bg); }` → `background: var(--color-invert);
      color: var(--color-text-inverse);` and `:hover { color: var(--color-text-inverse); }`

    SHADOWS → `var(--color-text)`:
    - Line 733 `.card-grid > a:hover`: `box-shadow: 8px 8px 0 var(--color-accent);` → `box-shadow: 8px
      8px 0 var(--color-text);`
    - Line 1361 `.pb-card.is-open .pb-frame`: `box-shadow: 10px 10px 0 var(--accent);` → `box-shadow:
      10px 10px 0 var(--color-text);`
    - Line 1464 `.pb-btn--stop`: `box-shadow: 4px 4px 0 var(--accent);` → `box-shadow: 4px 4px 0
      var(--color-text);`
    - Line 1489 `.pb-card.pb-drawn .pb-frame`: `box-shadow: 12px 12px 0 var(--accent);` → `box-shadow:
      12px 12px 0 var(--color-text);`

    PAIRED TEXT-ON-INVERTED-FILL TOKEN FIX (repoint `var(--color-bg)` to `var(--color-text-inverse)`
    wherever it means "light text/icon sitting on a fill this task just converted to invert", so the
    token stays correct if a future phase changes `--color-bg` away from white — this is the Phase 24
    dark-mode seam CONTEXT.md asks this phase to name):
    - Line 1005 `.nav-cell:hover > span, .nav-cell:focus-visible > span`: `color: var(--color-bg);` →
      `color: var(--color-text-inverse);`
    - Line 1328 `.pb-play > span::after`: `border-left: 14px solid var(--color-bg);` → `border-left:
      14px solid var(--color-text-inverse);`
    - Line 1363 `.pb-open`: `color: var(--color-bg);` → `color: var(--color-text-inverse);`
    - Line 1366 `.pb-close`: `color: var(--color-bg);` → `color: var(--color-text-inverse);`

    Do not alter any existing `transition-duration` value on these rules — this is a token
    substitution pass only, not a motion-timing normalization pass (motion work belongs to Phase 21's
    MS-01/MS-02).
  </action>
  <acceptance_criteria>
    `grep -n "background: var(--accent)\|border-color: var(--accent)\|box-shadow:.*var(--accent)\|box-shadow:.*var(--color-accent)" src/app/globals.css`
    returns 0 hits.
    `grep -c "var(--color-invert)" src/app/globals.css` is at least 15.
    `grep -n "\.pb-note {" -A2 src/app/globals.css | grep -q "background: var(--color-invert)"` succeeds.
    `grep -n "color: var(--color-bg)" src/app/globals.css | grep -c "1005\|1328\|1363\|1366"` — manually
    confirm lines 1005, 1328, 1363, 1366 no longer show `var(--color-bg)` (now `var(--color-text-inverse)`).
  </acceptance_criteria>
  <verify>
    <automated>grep -c "background: var(--accent)" "src/app/globals.css"</automated>
  </verify>
  <done>Every Tier 1 fill/shadow site in globals.css uses mono invert/text tokens, zero accent left in fills.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Convert Tier 2 (inline text) and Tier 3 (oversized display) accent references</name>
  <files>src/app/globals.css</files>
  <read_first>
    src/app/globals.css (lines 70-82, 130-170, 357-410, 495-560, 780-820, 1080-1220)
    .planning/phases/20-mono-token-foundation/20-CONTEXT.md (D-09 Tier 2, Tier 3)
    .planning/phases/20-mono-token-foundation/20-PATTERNS.md (Part 1 globals.css rows; Part 3 Tier 2/3 lists)
  </read_first>
  <action>
    Convert every remaining Tier 2 and Tier 3 `var(--accent)` reference. Tier 2 rule (D-09): text keeps
    its ink color at rest and on hover — no color change ever; the "reveal" is either an
    already-permanent underline/border-bottom device (leave it, just retint the color) or, where no
    such device exists, add `text-decoration: underline` on hover instead of a color swap. Tier 3 rule
    (D-09): outline stroke uses the invert token instead of accent, nothing else changes.

    TIER 3 (1 site in this file — component-level hover-fill logic lives in 20-03, not here):
    - Line 79 `.sig-out`: `-webkit-text-stroke: 2px var(--accent);` → `-webkit-text-stroke: 2px
      var(--color-invert);`

    TIER 2 — retint only, no hover color change, existing underline/border-bottom device kept:
    - Line 158 `.prose a`: `color: var(--accent);` → `color: var(--color-text);` (underline is already
      present on the same rule via `text-decoration: underline`)
    - Line 164 `.prose blockquote`: `border-left: 2px solid var(--accent);` → `border-left: 2px solid
      var(--color-border-strong);`
    - Line 374 `.wayin a`: `border-bottom: 2px solid var(--accent);` → `border-bottom: 2px solid
      var(--color-text);`
    - Line 378 `.wayin a:hover { color: var(--accent); }`: delete this rule entirely — the border-bottom
      already provides the permanent reveal; D-09 forbids an additional color change on hover
    - Line 405 `.photo .icon`: `color: var(--accent);` → `color: var(--color-text);` (existing opacity
      0.55 stays, giving a muted icon)
    - Line 436 `.rail-box .num`: `color: var(--accent);` → `color: var(--color-text-inverse);` (the
      number sits on the block's own `background: var(--color-text)` ink field — needs the light token
      to stay legible, matching `.rail-box .lbl` on the same block which already uses `var(--color-bg)`)
    - Line 518 `.hero-ticker .tick-link:hover { opacity: 1; color: var(--accent); }`: delete `color:
      var(--accent);`, keep `opacity: 1;` as the sole hover reveal
    - Line 529 `.hero-ticker .tick-sep`: `color: var(--accent);` → `color: var(--color-text-inverse);`
      (the ticker strip has its own literal dark background `#141416` with light text `#f5f3ee`; the
      separator needs a light token to stay visible against it, not an ink token)
    - Line 554 `.inline`: `border-bottom: 2px solid var(--accent);` → `border-bottom: 2px solid
      var(--color-text);`
    - Line 558 `.inline:hover { color: var(--accent); }`: delete this rule entirely (same reasoning as
      `.wayin a:hover`)
    - Lines 568-570 `.prometheus-link`: already restructured by 20-01 Task 3 (D-10) — verify no
      `var(--accent)` remains here and skip if already clean.
    - Line 794 `.mm-issue`: `color: var(--accent);` → `color: var(--color-text-dim);`
    - Line 818 `.mm-read`: `border-bottom: 2px solid var(--accent);` → `border-bottom: 2px solid
      var(--color-text);` (already has `text-decoration: none` and `color: var(--color-text)`, so the
      border-bottom remains the permanent underline device)
    - Line 851 `.mm-sub .btn`: `color: var(--accent);` → `color: var(--color-text);` (button background
      stays literal `#fff`, so black text on a white pill)
    - Line 1105 `.foot-col a:hover { color: var(--accent); }`: `.foot-col a` has no permanent underline
      at rest, so replace with `text-decoration: underline; text-underline-offset: 3px;` — adds the
      Tier 2 reveal instead of a color change
    - Line 1218 `.selective b`: `color: var(--accent);` → `color: var(--color-text);` (already bold;
      ink alone carries the emphasis)

    After these edits, run `grep -n 'var(--accent)\|var(--color-accent)' src/app/globals.css` — it must
    return zero results anywhere in the file. Combined with Task 1 and 20-01, this closes out every
    accent reference in globals.css.
  </action>
  <acceptance_criteria>
    `grep -c "var(--accent)\|var(--color-accent)" src/app/globals.css` returns 0.
    `grep -n "\.wayin a:hover" src/app/globals.css` returns 0 hits (rule deleted).
    `grep -n "\.inline:hover" src/app/globals.css` returns 0 hits (rule deleted).
    `grep -n "\.foot-col a:hover" -A1 src/app/globals.css | grep -q "text-decoration: underline"` succeeds.
    `grep -n "sig-out {" -A1 src/app/globals.css | grep -q "var(--color-invert)"` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "var(--accent)\|var(--color-accent)" "src/app/globals.css"</automated>
  </verify>
  <done>Zero var(--accent)/var(--color-accent) references remain anywhere in globals.css.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| none new | pure CSS token substitution, no new input surface or trust boundary introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-03 | Tampering | globals.css rule deletion (`.wayin a:hover`, `.inline:hover`) | accept | Deleting a hover color-change rule cannot introduce a vulnerability; worst case is a missed visual reveal, caught by the Plan 20-05 build/visual check |
| T-20-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
Combined with 20-01, `grep -c "var(--accent)\|var(--color-accent)\|accent" src/app/globals.css` (minus
comment-only false positives already handled by the rewrite) should be 0 after both tasks in this plan.
</verification>

<success_criteria>
- Zero `var(--accent)`/`var(--color-accent)` references remain in globals.css.
- Every converted Tier 1 site uses `--color-invert`/`--color-text-inverse`; every converted shadow uses
  `--color-text`; every converted Tier 2 site keeps its ink color and either an existing or newly-added
  underline reveal; the one Tier 3 site uses `--color-invert` for its stroke.
</success_criteria>

<output>
Create `.planning/phases/20-mono-token-foundation/20-02-SUMMARY.md` when done
</output>
