---
phase: 20-mono-token-foundation
plan: 04
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - src/components/layout/site-footer.tsx
  - src/components/about/timeline.tsx
  - src/app/contact/page.tsx
  - src/app/writing/page.tsx
  - src/app/blog/[slug]/page.tsx
  - src/app/building/[slug]/page.tsx
autonomous: true
requirements: [MO-02, MO-03]

must_haves:
  truths:
    - "The footer's offset rule and both nav-link hover groups carry no hue"
    - "timeline.tsx's icon-dot colors resolve to real, defined mono tokens even though the component is currently unused"
    - "Every interior route named in D-02's call-site list (contact, writing, blog/[slug], building/[slug]) is accent-free"
  artifacts:
    - path: "src/components/layout/site-footer.tsx"
      provides: "accent-free footer with the D-11 invert offset rule"
      contains: "bg-[var(--color-invert)]"
    - path: "src/components/about/timeline.tsx"
      provides: "dead-code component with real mono token references instead of undefined --accent-warm/--gold"
  key_links:
    - from: "site-footer.tsx bg-[var(--color-invert)]"
      to: "globals.css --color-invert token (20-01)"
      via: "CSS var() reference"
      pattern: "bg-\\[var\\(--color-invert\\)\\]"
---

<objective>
Convert every `accent` reference in the site-wide footer, the (currently unused) about-timeline
component, and the four interior routes named in D-02's call-site list.

Purpose: closes out the last group of the ~17 known `accent`-referencing files under `src/`, so the
D-03 zero-hits grep in 20-05 has nothing left to find outside the spec doc and the OG-image routes.
Output: six accent-free files.

Runs on the `v4-mono` branch created in 20-01 (D-12/D-13) — do not create a new branch or worktree for
this plan. Depends only on 20-01's token definitions; does not touch `globals.css` or `src/components/
v3/`, so it can run in the same wave as 20-02 and 20-03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-mono-token-foundation/20-CONTEXT.md
@.planning/phases/20-mono-token-foundation/20-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Convert site-footer.tsx (D-11) and timeline.tsx</name>
  <files>src/components/layout/site-footer.tsx, src/components/about/timeline.tsx</files>
  <read_first>
    src/components/layout/site-footer.tsx (full file)
    src/components/about/timeline.tsx (full file)
    .planning/phases/20-mono-token-foundation/20-CONTEXT.md (D-11, D-02 timeline.tsx guidance)
    .planning/phases/20-mono-token-foundation/20-PATTERNS.md (Part 1 Layout and About/Timeline rows)
  </read_first>
  <action>
    site-footer.tsx line 37: `<div className="h-2 bg-[var(--accent)]" aria-hidden="true" />` →
    `<div className="h-2 bg-[var(--color-invert)]" aria-hidden="true" />` (D-11 — stays a solid offset
    shape, only the hue dies). Update the line-36 comment ("Vermilion accent rule") to describe it as
    the invert rule. Lines 65 and 87 (`hover:text-[var(--accent)]` inside the Explore and Elsewhere nav
    link `className`s): neither footer link group has a permanent underline at rest, so replace
    `hover:text-[var(--accent)]` with `hover:underline hover:underline-offset-4` at both locations
    (Tier 2 reveal, no color change).

    timeline.tsx (confirmed unused — no importer anywhere in `src/`; verify with `grep -rn "from
    .*about/timeline\|<Timeline" src/` before editing, expect zero results): this component references
    two tokens that do not exist anywhere in the codebase (`--accent-warm`, `--gold`) alongside the real
    `--accent` token. Since it is dead code, do not invent new tokens for `--accent-warm`/`--gold` —
    repoint all three to existing mono tokens so the file is grep-clean and would render sensibly if
    ever wired up again: line 27 `'bg-[var(--accent)]'` → `'bg-[var(--color-invert)]'`; line 28
    `'bg-[var(--accent-warm)]'` → `'bg-[var(--color-text-dim)]'`; line 29 `'bg-[var(--gold)]'` →
    `'bg-[var(--color-border-strong)]'`; line 30 `'bg-[var(--accent)]'` (the `award` icon also reused
    `--accent`) → `'bg-[var(--color-invert)]'`; line 68 `group-hover:text-[var(--accent)]` →
    `group-hover:text-[var(--color-invert)]`; line 123 `text-[var(--accent)]` →
    `text-[var(--color-invert)]`. Add a one-line comment above `ICON_COLOR` noting the component is
    currently unimported/unused. Leave `--fg-muted`, `--border`, `--bg` untouched — those are the
    pre-v3 back-compat aliases already defined in `globals.css` (lines 116-119, from 20-01) and already
    resolve to non-accent tokens.
  </action>
  <acceptance_criteria>
    `grep -n "accent" src/components/layout/site-footer.tsx src/components/about/timeline.tsx` returns
    0 hits.
    `grep -n "gold\|accent-warm" src/components/about/timeline.tsx` returns 0 hits.
    `grep -n "bg-\[var(--color-invert)\]" src/components/layout/site-footer.tsx` returns 1 hit.
    `grep -n "hover:underline" src/components/layout/site-footer.tsx` returns 2 hits.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "accent" src/components/layout/site-footer.tsx src/components/about/timeline.tsx | awk -F: '{s+=$2} END {print s}'</automated>
  </verify>
  <done>site-footer.tsx and timeline.tsx are accent-free; timeline.tsx's dead-code tokens resolve to real mono values.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Convert contact, writing, blog/[slug], and building/[slug] routes</name>
  <files>src/app/contact/page.tsx, src/app/writing/page.tsx, src/app/blog/[slug]/page.tsx, src/app/building/[slug]/page.tsx</files>
  <read_first>
    src/app/contact/page.tsx (lines 60-80)
    src/app/writing/page.tsx (lines 165-185)
    src/app/blog/[slug]/page.tsx (lines 80-100)
    src/app/building/[slug]/page.tsx (lines 75-100)
    src/components/v3/contact-row.tsx (full file — verify zero accent references)
    .planning/phases/20-mono-token-foundation/20-CONTEXT.md (D-09 Tier 2)
    .planning/phases/20-mono-token-foundation/20-PATTERNS.md (Part 1 Routes rows; Part 3 Ambiguous table,
      building/[slug]/page.tsx entry)
  </read_first>
  <action>
    contact/page.tsx line 71: doc-comment `Brand rules: Vermilion accent, no gradients, no em dashes.`
    → `Brand rules: pure black/white, zero accent, no gradients, no em dashes.` No functional code in
    this file references accent. `ContactRow` in `src/components/v3/contact-row.tsx` already has zero
    accent references — confirm with the read_first grep, no change needed there.

    writing/page.tsx line 177: `"font-mono text-sm uppercase tracking-[0.12em] text-accent mb-[18px]"`
    → `"font-mono text-sm uppercase tracking-[0.12em] text-text-dim mb-[18px]"` (static Monty Monthly
    section subheading, Tier 2, no hover state).

    blog/[slug]/page.tsx lines 90 and 96: `<span className="text-[var(--accent)]">.</span>` (metadata
    separator dots) → `<span className="text-[var(--color-text-muted)]">.</span>` at both locations
    (static separator glyphs, muted ink).

    building/[slug]/page.tsx line 91: `className="inline-block mt-4 px-0 text-[var(--accent)]
    underline hover:text-[var(--accent-hover)]"` — this link already carries a permanent `underline`,
    so per D-09 Tier 2 the hover state must not change color at all. Resolve PATTERNS.md's flagged
    ambiguity by dropping the hover variant entirely: → `className="inline-block mt-4 px-0 text-text
    underline"` (rest color becomes ink; no hover class needed since the underline is already permanent
    and `--accent-hover` no longer exists after 20-01).
  </action>
  <acceptance_criteria>
    `grep -n "accent" src/app/contact/page.tsx src/app/writing/page.tsx "src/app/blog/[slug]/page.tsx"
    "src/app/building/[slug]/page.tsx" src/components/v3/contact-row.tsx` returns 0 hits.
    `grep -n "hover:text-\[var(--accent-hover)\]" "src/app/building/[slug]/page.tsx"` returns 0 hits.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "accent" src/app/contact/page.tsx src/app/writing/page.tsx "src/app/blog/[slug]/page.tsx" "src/app/building/[slug]/page.tsx" | awk -F: '{s+=$2} END {print s}'</automated>
  </verify>
  <done>All four routes and the ContactRow component are accent-free.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| none new | pure JSX className/comment token substitution, no new input surface introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-05 | Tampering | building/[slug]/page.tsx external link | accept | `target="_blank" rel="noopener noreferrer"` on the anchor is untouched by this edit — only the className string changes |
| T-20-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
`grep -rn "accent" src/components/layout/site-footer.tsx src/components/about/timeline.tsx
src/app/contact/page.tsx src/app/writing/page.tsx "src/app/blog/[slug]/page.tsx"
"src/app/building/[slug]/page.tsx"` returns 0 hits after this plan.
</verification>

<success_criteria>
- Zero `accent` references remain in any of the six files this plan owns.
- site-footer.tsx's offset rule uses `--color-invert`; both nav-link groups reveal via underline on hover.
- timeline.tsx's icon-dot map resolves entirely to real, existing mono tokens.
</success_criteria>

<output>
Create `.planning/phases/20-mono-token-foundation/20-04-SUMMARY.md` when done
</output>
