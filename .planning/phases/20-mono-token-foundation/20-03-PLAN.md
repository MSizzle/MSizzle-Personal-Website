---
phase: 20-mono-token-foundation
plan: 03
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - src/components/v3/button.tsx
  - src/components/v3/video-card.tsx
  - src/components/v3/newsletter-carousel.tsx
  - src/components/v3/big-list.tsx
  - src/components/v3/marquee.tsx
  - src/components/v3/page-hero.tsx
  - src/components/v3/section-label.tsx
  - src/components/v3/card.tsx
  - src/components/v3/uses-list.tsx
autonomous: true
requirements: [MO-02, MO-03]

must_haves:
  truths:
    - "None of the 9 files in src/components/v3/ render an accent Tailwind utility class or reference var(--accent)"
    - "button.tsx's boolean variant prop is named invert, not accent, with zero call sites broken (component confirmed unused site-wide)"
  artifacts:
    - path: "src/components/v3/button.tsx"
      provides: "accent-free Button with an invert variant prop"
      contains: "invert?: boolean"
    - path: "src/components/v3/big-list.tsx"
      provides: "accent-free Tier 3 outline-stroke hover fill"
      contains: "hover:text-invert"
  key_links:
    - from: "button.tsx bg-invert / text-invert / border-invert classes"
      to: "globals.css --color-invert token (20-01)"
      via: "Tailwind v4 utility emission"
      pattern: "bg-invert|text-invert|border-invert"
---

<objective>
Convert every `accent` reference across the nine `src/components/v3/` shared-layer components (D-02's
named call-site list) to the mono tier tokens established in 20-01.

Purpose: this is the shared component layer every route imports from — converting it here means the
interior-route sweep in Phase 23 is a visual audit, not a second conversion pass.
Output: nine accent-free component files, tier-classified per D-09.

Runs on the `v4-mono` branch created in 20-01 (D-12/D-13) — do not create a new branch or worktree for
this plan. Depends only on 20-01's token definitions; does not touch `globals.css` and has no file
overlap with 20-02 or 20-04, so it can run in the same wave as those.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-mono-token-foundation/20-CONTEXT.md
@.planning/phases/20-mono-token-foundation/20-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Convert Tier 1 (block-surface) components — button, video-card, newsletter-carousel</name>
  <files>src/components/v3/button.tsx, src/components/v3/video-card.tsx, src/components/v3/newsletter-carousel.tsx</files>
  <read_first>
    src/components/v3/button.tsx (full file)
    src/components/v3/video-card.tsx (full file)
    src/components/v3/newsletter-carousel.tsx (lines 1-45)
    .planning/phases/20-mono-token-foundation/20-CONTEXT.md (D-02, D-08, D-09 Tier 1)
    .planning/phases/20-mono-token-foundation/20-PATTERNS.md (Part 1 Components table; Part 3 Tier 1 list)
  </read_first>
  <action>
    button.tsx (confirmed unused — no importer anywhere in `src/`; verify with `grep -rn "from .*v3/button\|<Button" src/` before editing, expect zero results): rename the `accent?: boolean` prop
    to `invert?: boolean` throughout (type declaration, destructure, and the ternary), and rewrite the
    top doc-comment (lines 1-6) to drop "vermilion"/"crimson" language, describing the invert variant
    instead. Replace the ternary body: `accent ? "bg-accent border-accent text-bg hover:bg-transparent
    hover:text-accent" : "border-border-strong text-text hover:bg-text hover:text-bg"` becomes `invert
    ? "bg-invert border-invert text-text-inverse hover:bg-transparent hover:text-invert" :
    "border-border-strong text-text hover:bg-text hover:text-bg"` — the non-invert branch is untouched,
    it never referenced accent.

    video-card.tsx line 37: `bg-surface` here means "dark placeholder panel awaiting a thumbnail". Per
    D-08, `bg-surface` no longer means dark (it is now white in 20-01's token block), so leaving it
    would silently turn this placeholder white. Converting straight to `bg-invert` would also break the
    hover feedback (rest and hover would both render solid black). Replace `bg-surface` with the
    neutral placeholder fill `bg-[rgba(0,0,0,0.08)]` (matches the equivalent `.pb-media` placeholder fix
    in `globals.css` 20-01), and replace `group-hover:bg-accent` with `group-hover:bg-invert` — this
    preserves a light-to-black hover invert, matching D-09 Tier 1's canonical direction. Line 52:
    `border-l-accent` → `border-l-invert`. Line 55: `group-hover:border-l-bg` → `group-hover:border-l-
    [color:var(--color-text-inverse)]` (the play-triangle edge needs the light/inverse token once its
    container becomes solid black on hover, not `--color-bg`, matching the seam-naming fix applied to
    the equivalent pattern in 20-01/20-02). Update the line-36 comment to drop "accent".

    newsletter-carousel.tsx line 30: same `bg-surface`-as-placeholder issue — replace `bg-surface` with
    `bg-[rgba(0,0,0,0.08)]`, and `border-accent` with `border-invert` (this is a static bottom-border
    edge, not hover-triggered, so no hover class is involved here).
  </action>
  <acceptance_criteria>
    `grep -n "accent" src/components/v3/button.tsx src/components/v3/video-card.tsx
    src/components/v3/newsletter-carousel.tsx` returns 0 hits.
    `grep -n "accent?:" src/components/v3/button.tsx` returns 0 hits; `grep -n "invert?:"
    src/components/v3/button.tsx` returns 1 hit.
    `grep -n "bg-surface" src/components/v3/video-card.tsx src/components/v3/newsletter-carousel.tsx`
    returns 0 hits.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "accent" src/components/v3/button.tsx src/components/v3/video-card.tsx src/components/v3/newsletter-carousel.tsx | awk -F: '{s+=$2} END {print s}'</automated>
  </verify>
  <done>All three Tier 1 v3 components are accent-free; hover feedback on video-card is preserved via a neutral rest state.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Convert Tier 3 (oversized display) components — big-list, marquee, page-hero</name>
  <files>src/components/v3/big-list.tsx, src/components/v3/marquee.tsx, src/components/v3/page-hero.tsx</files>
  <read_first>
    src/components/v3/big-list.tsx (full file)
    src/components/v3/marquee.tsx (full file)
    src/components/v3/page-hero.tsx (lines 1-45)
    .planning/phases/20-mono-token-foundation/20-CONTEXT.md (D-09 Tier 3)
    .planning/phases/20-mono-token-foundation/20-PATTERNS.md (Part 3 Tier 3 list)
  </read_first>
  <action>
    big-list.tsx: lines 4 and 6 doc-comment — drop "accent" wording. Line 45: `"hover:text-accent
    hover:[text-shadow:none]"` → `"hover:text-invert hover:[text-shadow:none]"`. Line 46:
    `item.outline && "hover:[-webkit-text-stroke-color:var(--accent)]"` → `item.outline &&
    "hover:[-webkit-text-stroke-color:var(--color-invert)]"`. Line 55: `"group-hover:text-accent"` →
    `"group-hover:text-invert"` (Tier 3 — the `.tag` element inside an outline `sig-out` item fills
    solid on hover, per PATTERNS.md's classification).

    marquee.tsx: line 6 doc-comment — drop "accent" wording. Line 50: `"[color:transparent]
    [-webkit-text-stroke:1.5px_var(--accent)]"` → `"[color:transparent]
    [-webkit-text-stroke:1.5px_var(--color-invert)]"`.

    page-hero.tsx: line 6 doc-comment references "accent" in prose only (`h1 .out {
    -webkit-text-stroke:2px accent; ... }`) — update the comment text to say "invert" instead. There is
    no functional code to change in this file; the actual stroke color lives in `globals.css`
    `.sig-out`, already converted in 20-01/20-02.
  </action>
  <acceptance_criteria>
    `grep -n "accent" src/components/v3/big-list.tsx src/components/v3/marquee.tsx
    src/components/v3/page-hero.tsx` returns 0 hits.
    `grep -n "hover:text-invert" src/components/v3/big-list.tsx` returns 2 hits (lines 45 and 55 equivalents).
  </acceptance_criteria>
  <verify>
    <automated>grep -c "accent" src/components/v3/big-list.tsx src/components/v3/marquee.tsx src/components/v3/page-hero.tsx | awk -F: '{s+=$2} END {print s}'</automated>
  </verify>
  <done>All three Tier 3 v3 components use --color-invert for their stroke/fill, zero accent left.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Convert Tier 2 (inline text) components — section-label, card, uses-list</name>
  <files>src/components/v3/section-label.tsx, src/components/v3/card.tsx, src/components/v3/uses-list.tsx</files>
  <read_first>
    src/components/v3/section-label.tsx (full file)
    src/components/v3/card.tsx (lines 95-115)
    src/components/v3/uses-list.tsx (lines 20-40)
    .planning/phases/20-mono-token-foundation/20-CONTEXT.md (D-09 Tier 2)
    .planning/phases/20-mono-token-foundation/20-PATTERNS.md (Part 1 Components table)
  </read_first>
  <action>
    section-label.tsx line 18: `"font-mono text-sm uppercase tracking-[0.12em] text-accent block
    mb-8"` → `"font-mono text-sm uppercase tracking-[0.12em] text-text-dim block mb-8"` (static mono
    label, no hover state defined; D-09 Tier 2 sites with no interaction just need an ink-family
    color). Update the line-4 doc-comment to drop "accent".

    card.tsx line 106: `"font-mono text-xs text-accent block mb-[14px]"` → `"font-mono text-xs
    text-text-dim block mb-[14px]"` (kicker label, same static treatment).

    uses-list.tsx line 30: `"font-mono text-sm uppercase tracking-[0.12em] text-accent mb-[18px]"` →
    `"font-mono text-sm uppercase tracking-[0.12em] text-text-dim mb-[18px]"` (group heading, same
    static treatment).
  </action>
  <acceptance_criteria>
    `grep -n "accent" src/components/v3/section-label.tsx src/components/v3/card.tsx
    src/components/v3/uses-list.tsx` returns 0 hits.
    `grep -n "text-text-dim" src/components/v3/section-label.tsx src/components/v3/card.tsx
    src/components/v3/uses-list.tsx` returns 3 hits total (one per file).
  </acceptance_criteria>
  <verify>
    <automated>grep -c "accent" src/components/v3/section-label.tsx src/components/v3/card.tsx src/components/v3/uses-list.tsx | awk -F: '{s+=$2} END {print s}'</automated>
  </verify>
  <done>All three Tier 2 v3 components are accent-free, static ink color applied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| none new | pure JSX className token substitution, no new input surface introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-04 | Tampering | button.tsx prop rename (accent → invert) | mitigate | Verified zero call sites reference the old prop name before renaming (`grep -rn "<Button" src/` returns nothing); no consumer can break |
| T-20-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
`grep -rn "accent" src/components/v3/*.tsx` returns 0 hits across all nine files after this plan.
</verification>

<success_criteria>
- Zero `accent` references remain in any of the nine `src/components/v3/` files.
- button.tsx's variant prop is `invert`, not `accent`.
- video-card.tsx and newsletter-carousel.tsx no longer use `bg-surface` as a placeholder fill.
</success_criteria>

<output>
Create `.planning/phases/20-mono-token-foundation/20-03-SUMMARY.md` when done
</output>
