---
phase: 20-mono-token-foundation
plan: 05
type: execute
wave: 3
depends_on: ["20-02", "20-03", "20-04"]
files_modified: []
autonomous: false
requirements: [MO-01, MO-02, MO-03, MO-05, DQ-01]

must_haves:
  truths:
    - "grep -rn accent|hex-list src/ returns zero hits outside montysinger-v2-spec.md and the three opengraph-image.tsx routes"
    - "npm run build exits 0 and vitest shows no failures beyond the three documented pre-existing ones"
    - "v4-mono is pushed to origin with a Ready Vercel branch preview rendering the mono system"
    - "montysinger.com still serves the v3 design, unchanged"
    - "D-03: repo-wide grep audit returns zero accent/hex hits outside montysinger-v2-spec.md and the opengraph-image.tsx routes"
    - "D-14: preview is the automatic v4-mono branch preview, no manual deploy command, no production alias swap"
    - "D-15: DQ-01 met when the preview renders mono while montysinger.com serves v3, both verified same session"
  artifacts: []
  key_links:
    - from: "git push -u origin v4-mono"
      to: "Vercel branch-preview deployment (GitHub integration, project m-sizzle-personal-website)"
      via: "automatic branch preview, no manual vercel deploy command"
      pattern: "gitBranch=v4-mono"
---

<objective>
Prove the whole phase is done: run the definitive repo-wide D-03 grep audit, confirm the build and test
suite are clean, push `v4-mono` to trigger the automatic Vercel branch preview, and get Monty's visual
sign-off that the preview renders mono while production is untouched (DQ-01/D-15).

Purpose: this is the goal-backward closing plan — every truth in the phase's must_haves gets verified
here, not assumed.
Output: a pushed, previewed, human-approved `v4-mono` branch. No source files are expected to change in
this plan; if the audit finds a residual accent reference, fix it directly here rather than deferring.

Runs on the `v4-mono` branch created in 20-01. Do not merge to `main` and do not run any production
alias/promotion command — that is explicitly Phase 25's job (D-14, DQ-04). Never use `vercel deploy
--prebuilt --prod` on this project (known project gotcha).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/20-mono-token-foundation/20-CONTEXT.md
@.planning/phases/20-mono-token-foundation/20-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Full-repo D-03 audit, build, and vitest smoke check</name>
  <files></files>
  <read_first>
    .planning/phases/20-mono-token-foundation/20-CONTEXT.md (D-03, D-07, D-04 audit flags)
    .planning/phases/20-mono-token-foundation/20-PATTERNS.md (Planner Notes)
    .planning/STATE.md (Known Pre-Existing Test Failures section)
    package.json (scripts)
  </read_first>
  <action>
    Run the definitive D-03 grep across the whole repo: `grep -rn "accent\|e5411f\|c8381a\|a52d13\|
    f4ecdd\|faf9f7" src/`. Every hit must be inside `src/app/montysinger-v2-spec.md` or one of the
    three `opengraph-image.tsx` routes (`src/app/opengraph-image.tsx`, `src/app/blog/[slug]/
    opengraph-image.tsx`, `src/app/building/[slug]/opengraph-image.tsx`) — those are explicitly Phase
    23 per D-03. Any hit outside those four files means a conversion task in 20-01 through 20-04 was
    missed or incomplete; fix it directly in this task (do not defer to a new plan), then re-run the
    grep until it is clean.

    Verify the two audit-flagged blue-tinted greys and the D-07 contrast override landed: `grep -n
    "dbe2ee" src/app/globals.css` returns 0; `grep -n "color-text-muted: rgba(0,0,0,0.60)"
    src/app/globals.css` returns 1 hit (not 0.46).

    Verify MO-05 preservation — nothing in this phase should have touched these: `grep -n
    "radius-sm:\s*0\|radius-md:\s*0" src/app/globals.css` returns 2 hits; `grep -n "font-hanken"
    src/app/layout.tsx` returns at least 1 hit; `grep -in "gradient" src/app/globals.css` shows only
    pre-existing "no gradient(s)" comment lines, never a `linear-gradient(`/`radial-gradient(`
    declaration.

    Run `npm run build` from the repo root — must exit 0. Run the vitest suite (`npx vitest run`) — the
    only acceptable failures are the three pre-existing ones documented in STATE.md (`section-building`
    HD-04, `explorative-homepage` TD-03/HD-05); any other failure is a Phase 20 regression and must be
    fixed in this task before proceeding.
  </action>
  <acceptance_criteria>
    `grep -rn "accent\|e5411f\|c8381a\|a52d13\|f4ecdd\|faf9f7" src/ | grep -v
    "montysinger-v2-spec.md" | grep -v "opengraph-image.tsx"` returns empty.
    `npm run build` exits 0.
    `npx vitest run` reports no failures beyond the three named pre-existing ones.
  </acceptance_criteria>
  <verify>
    <automated>grep -rn "accent\|e5411f\|c8381a\|a52d13\|f4ecdd\|faf9f7" src/ | grep -v "montysinger-v2-spec.md" | grep -v "opengraph-image.tsx" | wc -l | grep -qx 0</automated>
  </verify>
  <done>Zero unaccounted-for accent/hex hits in src/; build and vitest are clean modulo the three known pre-existing failures.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Push v4-mono and verify the Vercel branch preview</name>
  <files></files>
  <read_first>
    .planning/phases/20-mono-token-foundation/20-CONTEXT.md (D-14, D-15)
    .vercel/project.json
  </read_first>
  <action>
    Commit any remaining uncommitted changes on `v4-mono` (a safety net — every prior plan in this
    phase should already be committed by execute-phase). Push the branch: `git push -u origin
    v4-mono`. Discover the resulting Vercel branch-preview deployment: run `vercel ls
    m-sizzle-personal-website` (project name from `.vercel/project.json`) and find the newest
    deployment whose metadata shows `gitBranch=v4-mono`; if it is not there yet, poll until one appears
    and reaches `● Ready`. Once ready, `curl -s <preview-url>` and grep the returned HTML for the
    forbidden hex strings (`e5411f`, `f4ecdd`, `faf9f7`) — 0 hits confirms the deployed preview itself
    (not just source) is accent-free. Separately `curl -s -o /dev/null -w "%{http_code}"
    https://montysinger.com` and confirm it still returns 200 — do not alias or promote anything, the
    production swap is explicitly Phase 25 (DQ-04), out of scope here. Record the preview URL for the
    next task.
  </action>
  <acceptance_criteria>
    `git log origin/v4-mono -1 --format=%H` equals `git log v4-mono -1 --format=%H`.
    `vercel ls m-sizzle-personal-website` shows a `Ready` deployment with `gitBranch=v4-mono`.
    curl of the preview URL returns HTTP 200 with 0 grep hits for `e5411f`, `f4ecdd`, `faf9f7`.
    curl of `https://montysinger.com` returns HTTP 200.
  </acceptance_criteria>
  <verify>
    <automated>git log origin/v4-mono -1 --format=%H 2>/dev/null | grep -q .</automated>
  </verify>
  <done>v4-mono is pushed, has a Ready preview deployment, and the preview HTML is confirmed accent-free.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human sign-off — preview shows mono, production is untouched</name>
  <files></files>
  <read_first>
    .planning/phases/20-mono-token-foundation/20-CONTEXT.md (D-15)
  </read_first>
  <action>
    No file changes — present the Task 2 preview URL and the production URL to Monty for visual
    confirmation per DQ-01/D-15. See how-to-verify below for the exact steps.
  </action>
  <what-built>
    The full Phase 20 mono token retheme is on the v4-mono branch, pushed, and live on its own Vercel
    preview URL (printed by Task 2). Production (montysinger.com) has not been touched.
  </what-built>
  <how-to-verify>
    1. Open the preview URL from Task 2. Confirm every surface is white/black with zero color anywhere
       — headline, buttons, hover states, the pinboard note panel, the footer rule. Hover a Building
       row and a footer link; both should react without any hue appearing.
    2. Open https://montysinger.com in a separate tab and confirm it still shows the current v3 design
       (Vermilion accents, cream hero) completely unchanged.
    3. Note that the page LAYOUT (hero photo carousel, marquee, pulsing status dot) will still look
       like v3 structurally on the preview — only the COLOR system changed in this phase; the layout
       rebuild is Phase 21. That is expected, not a bug.
  </how-to-verify>
  <resume-signal>
    Reply "approved" to close out Phase 20, or describe anything that still shows color/hue so it can
    be patched before Phase 21 starts.
  </resume-signal>
  <acceptance_criteria>
    Monty replies "approved", or any described issue is patched in this plan and re-verified before
    approval.
  </acceptance_criteria>
  <verify>
    <human-check>Monty opens both URLs and replies with the resume-signal above.</human-check>
  </verify>
  <done>Monty has approved the preview, or all described issues are patched and re-approved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| local machine → GitHub → Vercel | pushing v4-mono must not touch main or trigger a production alias swap |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-06 | Tampering | production alias/promotion | mitigate | Task explicitly scoped to `git push -u origin v4-mono` only; no `vercel deploy`/alias command is issued anywhere in this plan; DQ-04's alias swap is Phase 25 |
| T-20-07 | Repudiation | preview verification | mitigate | Task 2's curl checks give an automated, reproducible record before the human checkpoint, rather than relying on human memory alone |
| T-20-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
Tasks 1 and 2's acceptance_criteria are the phase-closing verification; Task 3 is the human-observable
confirmation of DQ-01/D-15 that automated grep/curl checks cannot fully replace (visual "does it look
mono" judgment).
</verification>

<success_criteria>
- `grep -rn` for the full forbidden-string list across `src/` returns zero unaccounted hits.
- `npm run build` exits 0; vitest has no new failures.
- v4-mono is pushed with a Ready Vercel preview confirmed accent-free by curl.
- montysinger.com is confirmed unchanged.
- Monty has approved the preview via the checkpoint.
</success_criteria>

<output>
Create `.planning/phases/20-mono-token-foundation/20-05-SUMMARY.md` when done
</output>
