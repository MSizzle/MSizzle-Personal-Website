---
phase: 19-project-cards-covers-redesign
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/og-fonts/hanken-grotesk-800.woff
  - src/app/og-fonts/jetbrains-mono-400.woff
  - src/app/opengraph-image.tsx
  - src/app/blog/[slug]/opengraph-image.tsx
  - src/app/projects/[slug]/opengraph-image.tsx
  - src/app/og-photo.jpg
  - src/__tests__/pages/og-image.test.tsx
autonomous: true
requirements: [SC-5]
must_haves:
  truths:
    - "All three OG images render as paper/ink/vermilion title-cards; the navy #1a1a2e scheme and every linear-gradient are gone from all opengraph-image.tsx sources"
    - "OG titles render in Hanken Grotesk 800 (real font data embedded), kickers and marks in JetBrains Mono; depth comes from a hard offset solid box shadow, never a gradient"
    - "Blog and project OG images show the content title with a content-type kicker (ESSAY / PROJECT) and a montysinger.com mark"
    - "Every OG route exports alt text with no em dash and no double hyphen"
  artifacts:
    - path: "src/app/og-fonts/hanken-grotesk-800.woff"
      provides: "Hanken Grotesk ExtraBold static font for satori"
    - path: "src/app/og-fonts/jetbrains-mono-400.woff"
      provides: "JetBrains Mono Regular static font for satori"
    - path: "src/app/opengraph-image.tsx"
      provides: "Root OG title-card (Monty Singer)"
      contains: "#e5411f"
    - path: "src/app/blog/[slug]/opengraph-image.tsx"
      provides: "Essay OG title-card"
      contains: "#e5411f"
    - path: "src/app/projects/[slug]/opengraph-image.tsx"
      provides: "Project OG title-card"
      contains: "#e5411f"
  key_links:
    - from: "src/app/opengraph-image.tsx"
      to: "src/app/og-fonts/hanken-grotesk-800.woff"
      via: "readFileSync(join(process.cwd(), 'src/app/og-fonts/hanken-grotesk-800.woff'))"
      pattern: "og-fonts/hanken-grotesk-800"
    - from: "src/app/blog/[slug]/opengraph-image.tsx"
      to: "src/app/og-fonts/hanken-grotesk-800.woff"
      via: "readFileSync + ImageResponse fonts option"
      pattern: "og-fonts/hanken-grotesk-800"
    - from: "src/app/projects/[slug]/opengraph-image.tsx"
      to: "src/app/og-fonts/hanken-grotesk-800.woff"
      via: "readFileSync + ImageResponse fonts option"
      pattern: "og-fonts/hanken-grotesk-800"
---

<objective>
Rebuild all three opengraph-image.tsx routes (root, blog/[slug], projects/[slug]) as vermilion/ink/paper typographic title-cards (SC-5): Hanken Grotesk 800 title on a white block with a hard offset ink shadow, mono kicker chip on vermilion, montysinger.com mark. The navy #1a1a2e scheme and the "OG gradient exception" are retired: no gradients in OG images either.

Purpose: OG images are the first-impression surface on X/LinkedIn and are currently the last off-brand navy surface on the site.
Output: Two committed woff font assets, three rebuilt OG routes, real tests replacing the three it.todo stubs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-project-cards-covers-redesign-typographic-title-card-covers-/19-CONTEXT.md
@src/app/opengraph-image.tsx
@src/app/blog/[slug]/opengraph-image.tsx
@src/app/projects/[slug]/opengraph-image.tsx
@src/__tests__/pages/og-image.test.tsx

<interfaces>
From src/lib/notion.ts: `getPostBySlug(slug)` returns a post with `title`, `date`, `description`. From src/lib/notion-projects.ts: `getProjectBySlug(slug)` returns a project with `title`, `description`, `tags`.

Existing root route pattern (src/app/opengraph-image.tsx lines 9-14): module-scope `readFileSync(join(process.cwd(), 'src/app/og-photo.jpg'))` — reuse this exact fs pattern for the font files. The root route has NO runtime export (Node runtime, statically prerendered). The two slug routes currently export `runtime = 'edge'`; that line must be REMOVED so readFileSync works (Next 16 fully supports ImageResponse in the Node runtime; the Notion SDK already runs in Node elsewhere in this repo).

ImageResponse fonts option shape: `new ImageResponse(jsx, { ...size, fonts: [{ name: 'Hanken Grotesk', data: hankenFont, weight: 800, style: 'normal' }, { name: 'JetBrains Mono', data: monoFont, weight: 400, style: 'normal' }] })` where data is the Buffer from readFileSync. Satori supports WOFF (not WOFF2); the @fontsource files below are WOFF. Satori supports the CSS `boxShadow` property for the hard offset solid.

Brand values (hardcode as string literals; OG routes cannot read CSS variables): paper `#faf9f7`, ink `#171717`, vermilion `#e5411f`, white `#ffffff`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commit font assets and rebuild the root OG image as a title-card</name>
  <files>src/app/og-fonts/hanken-grotesk-800.woff, src/app/og-fonts/jetbrains-mono-400.woff, src/app/opengraph-image.tsx, src/app/og-photo.jpg</files>
  <read_first>src/app/opengraph-image.tsx (entire file, 110 lines), .planning/phases/19-project-cards-covers-redesign-typographic-title-card-covers-/19-CONTEXT.md (OG images + specifics sections)</read_first>
  <action>
    1. Create `src/app/og-fonts/` and download the two static WOFF fonts (these are the official Google-Fonts-derived @fontsource package files served by jsdelivr; this is an asset download, not a package install):
       - `curl -fL -o src/app/og-fonts/hanken-grotesk-800.woff "https://cdn.jsdelivr.net/npm/@fontsource/hanken-grotesk@5/files/hanken-grotesk-latin-800-normal.woff"`
       - `curl -fL -o src/app/og-fonts/jetbrains-mono-400.woff "https://cdn.jsdelivr.net/npm/@fontsource/jetbrains-mono@5/files/jetbrains-mono-latin-400-normal.woff"`
       If jsdelivr 404s, retry with host `https://unpkg.com/` and the same package paths. Sanity-check both files: each must be larger than 15000 bytes and `file <path>` must identify a Web Open Font Format file. If neither host serves the files, STOP and report; do not substitute a different font source.

    2. Rewrite `src/app/opengraph-image.tsx` as a static title-card:
       - Keep exports: `alt` (keep the existing string), `size = { width: 1200, height: 630 }`, `contentType = 'image/png'`. Keep readFileSync/join imports; DELETE the og-photo.jpg read and the `<img>`/gradient JSX entirely.
       - Module scope: `const hankenFont = readFileSync(join(process.cwd(), 'src/app/og-fonts/hanken-grotesk-800.woff'))` and `const monoFont = readFileSync(join(process.cwd(), 'src/app/og-fonts/jetbrains-mono-400.woff'))`.
       - JSX layout (all values literal): outer div, width and height '100%', display 'flex', flexDirection 'column', justifyContent 'space-between', alignItems 'flex-start', background '#faf9f7', padding 72.
         - Kicker chip (top-left): div, display 'flex', fontFamily 'JetBrains Mono', fontSize 24, letterSpacing 4, textTransform 'uppercase', background '#e5411f', color '#ffffff', padding '10px 20px', text "MONTYSINGER.COM".
         - Title block (middle-left): div, display 'flex', background '#ffffff', color '#171717', fontFamily 'Hanken Grotesk', fontWeight 800, fontSize 108, lineHeight 1, letterSpacing -3, padding '28px 44px', boxShadow '16px 16px 0 #171717', text "Monty Singer".
         - Bottom line: div, display 'flex', fontFamily 'JetBrains Mono', fontSize 26, color '#171717', opacity 0.75, text "Founder of Prometheus. Builder, writer, and perpetual tinkerer."
       - Options: `{ ...size, fonts: [{ name: 'Hanken Grotesk', data: hankenFont, weight: 800, style: 'normal' }, { name: 'JetBrains Mono', data: monoFont, weight: 400, style: 'normal' }] }`.
       - File header comment: Phase 19 SC-5 title-card OG; no gradients (site-wide rule, the old OG gradient exception is void); fonts read via fs at module scope so the route statically prerenders.

    3. Delete `src/app/og-photo.jpg` (its only reference was the JSX removed above; confirm with `grep -rn "og-photo" src/` before deleting; if any other reference exists, leave the file and report).
  </action>
  <verify>
    <automated>test $(wc -c < src/app/og-fonts/hanken-grotesk-800.woff) -gt 15000 && test $(wc -c < src/app/og-fonts/jetbrains-mono-400.woff) -gt 15000 && ! grep -qE 'linear-gradient|1a1a2e|og-photo' src/app/opengraph-image.tsx && grep -q 'e5411f' src/app/opengraph-image.tsx && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - Both woff files exist, each >15000 bytes, identified as WOFF by `file`
    - src/app/opengraph-image.tsx contains '#faf9f7', '#171717', '#e5411f', "boxShadow: '16px 16px 0 #171717'", the fonts option with weight 800, and NO occurrence of 'linear-gradient', '1a1a2e', '0a0a0a', or 'og-photo'
    - src/app/og-photo.jpg no longer exists and `grep -rn 'og-photo' src/` returns nothing
    - No em dash and no "--" in any string literal in the rewritten file
    - `npx tsc --noEmit` (or the project's typecheck invocation) reports no errors in src/app/opengraph-image.tsx
  </acceptance_criteria>
  <done>Font assets are committed repo files; the root OG image is a static paper/ink/vermilion title-card with real Hanken 800 and JetBrains Mono data and zero gradients; og-photo.jpg is gone.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rebuild blog and project slug OG routes and write real OG tests</name>
  <files>src/app/blog/[slug]/opengraph-image.tsx, src/app/projects/[slug]/opengraph-image.tsx, src/__tests__/pages/og-image.test.tsx</files>
  <read_first>src/app/blog/[slug]/opengraph-image.tsx (60 lines), src/app/projects/[slug]/opengraph-image.tsx (54 lines), src/app/opengraph-image.tsx (rebuilt in Task 1; copy its font-loading and title-block styling exactly), src/__tests__/pages/og-image.test.tsx (3 it.todo stubs to replace)</read_first>
  <behavior>
    - Test 1: root OG module exports size {width: 1200, height: 630}, contentType 'image/png', and a non-empty alt string containing no em dash
    - Test 2: blog slug OG module exports size {1200x630}, contentType 'image/png', a non-empty alt string, and its default export is a function; the module does NOT export runtime
    - Test 3: project slug OG module: same assertions as Test 2
    - (Tests import the modules and assert exports only; they must NOT call the default export, so no Notion mocking or satori rendering happens in vitest)
  </behavior>
  <action>
    1. Rewrite `src/app/blog/[slug]/opengraph-image.tsx`:
       - DELETE the `export const runtime = 'edge'` line (Node runtime required for readFileSync). Keep `size`, `contentType`, the Props interface, the `await params` destructure, and the try/catch getPostBySlug data fetch with fallback title 'Writing'.
       - ADD `export const alt = 'Essay by Monty Singer'`.
       - ADD module-scope font reads identical to the root route: readFileSync(join(process.cwd(), 'src/app/og-fonts/hanken-grotesk-800.woff')) and the mono equivalent, importing readFileSync from 'node:fs' and join from 'node:path'.
       - Title safety: `const displayTitle = title.length > 90 ? title.slice(0, 87).trimEnd() + '…' : title` (horizontal ellipsis, not hyphens).
       - JSX layout (literals): outer div width/height '100%', display 'flex', flexDirection 'column', justifyContent 'space-between', alignItems 'flex-start', background '#faf9f7', padding 64.
         - Kicker chip: same styling as the root route chip (JetBrains Mono 24, letterSpacing 4, uppercase, background '#e5411f', color '#ffffff', padding '10px 20px'), text "ESSAY".
         - Title block: div display 'flex', background '#ffffff', color '#171717', fontFamily 'Hanken Grotesk', fontWeight 800, fontSize 68, lineHeight 1.05, letterSpacing -1.5, padding '24px 36px', boxShadow '14px 14px 0 #171717', maxWidth 1000, rendering {displayTitle}.
         - Footer row: div display 'flex', width '100%', justifyContent 'space-between', fontFamily 'JetBrains Mono', fontSize 24, color '#171717'; left span "montysinger.com" at opacity 0.75; right span rendering {date} (may be empty).
       - Same `fonts` option as the root route. File header comment: Phase 19 SC-5, Node runtime for fs font loading, no gradients.
    2. Rewrite `src/app/projects/[slug]/opengraph-image.tsx` identically in structure, with these differences: fallback title 'Project'; ADD `export const alt = 'Project by Monty Singer'`; kicker text "PROJECT"; below the title block render the description when non-empty as a div, fontFamily 'JetBrains Mono', fontSize 26, color '#171717', opacity 0.75, maxWidth 960, lineHeight 1.4, truncated with the same slice pattern at 140 chars; footer left span "montysinger.com" (no date on projects).
    3. Replace the three it.todo stubs in `src/__tests__/pages/og-image.test.tsx` with the real tests from the behavior block. Import modules via `await import('@/app/opengraph-image')`, `await import('@/app/blog/[slug]/opengraph-image')`, `await import('@/app/projects/[slug]/opengraph-image')`. Assert `('runtime' in module) === false` for the two slug modules. Assert alt strings do not match /—|--/ . Do not invoke the default exports.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/pages/og-image.test.tsx && ! grep -qE "linear-gradient|1a1a2e|0a0a0a|runtime = 'edge'" "src/app/blog/[slug]/opengraph-image.tsx" "src/app/projects/[slug]/opengraph-image.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - Neither slug route contains `runtime = 'edge'`, 'linear-gradient', '1a1a2e', or '0a0a0a'; both contain '#faf9f7', '#e5411f', 'Hanken Grotesk', fontWeight 800, a boxShadow of '14px 14px 0 #171717', and an `export const alt`
    - Blog route kicker text is "ESSAY"; project route kicker text is "PROJECT"; both footer marks read "montysinger.com"
    - src/__tests__/pages/og-image.test.tsx contains zero `it.todo` and `npx vitest run src/__tests__/pages/og-image.test.tsx` exits 0
    - No em dash and no "--" in string literals across both routes (ellipsis is …)
    - `npx tsc --noEmit` reports no errors in either route
  </acceptance_criteria>
  <done>Both dynamic OG routes render vermilion/ink/paper title-cards with real font data on the Node runtime, export alt text, and the OG test file has three real passing tests instead of todos.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL slug param -> OG route | Arbitrary slugs reach getPostBySlug / getProjectBySlug |
| jsdelivr CDN -> committed font asset | One-time asset download during execution |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-05 | Injection | slug param in blog/projects OG routes | mitigate | Slug is passed only to the existing Notion SDK filter query (pattern unchanged from current code); rendered output goes through satori text nodes (no HTML interpretation); unknown slugs fall back to generic titles via the existing try/catch |
| T-19-06 | Tampering (supply chain) | Downloaded woff font binaries | mitigate | Fetched from the official @fontsource package path on jsdelivr (unpkg fallback), size- and filetype-checked, then committed as static repo assets; no runtime fetch and no npm install, so no new package-manager dependency (T-19-SC not triggered) |
| T-19-07 | DoS | Per-request OG render on slug routes | accept | Same request surface as the current routes; Vercel function limits and Notion try/catch already bound the cost; runtime change edge->node does not widen exposure |
</threat_model>

<verification>
- `npx vitest run src/__tests__/pages/og-image.test.tsx` exits 0 with 3 real tests.
- `grep -rEn 'linear-gradient|1a1a2e|0a0a0a' src/app/opengraph-image.tsx 'src/app/blog/[slug]/opengraph-image.tsx' 'src/app/projects/[slug]/opengraph-image.tsx'` returns nothing.
- `npx vitest run` shows no new failing test files beyond the pre-existing explorative-homepage/section-building failures.
- Human check (non-blocking, for verify-work later): `npm run dev`, then open http://localhost:3000/opengraph-image, a real /blog/[slug]/opengraph-image, and a real /projects/[slug]/opengraph-image in a browser; each renders a paper-field card with a white Hanken-800 title block, hard ink offset shadow, and vermilion kicker chip.
</verification>

<success_criteria>
- All three OG routes are on-brand title-cards: paper #faf9f7 field, white title block with hard ink offset shadow, vermilion kicker chip, mono montysinger.com mark.
- Navy scheme and gradients fully retired from OG sources.
- Real Hanken Grotesk 800 and JetBrains Mono font data embedded via committed woff assets.
- og-image.test.tsx has three real passing tests; no new suite failures.
</success_criteria>

<output>
Create `.planning/phases/19-project-cards-covers-redesign-typographic-title-card-covers-/19-03-SUMMARY.md` when done.
</output>
