---
phase: 19-project-cards-covers-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/v3/title-card.tsx
  - src/components/v3/card-cover.tsx
  - src/components/v3/card.tsx
  - src/app/globals.css
  - src/__tests__/components/title-card.test.tsx
  - src/__tests__/components/card.test.tsx
autonomous: true
requirements: [SC-1]
must_haves:
  truths:
    - "A Card with no coverSrc renders a typographic title-card face (mono kicker chip, Hanken 800 title) instead of nothing or a gray box"
    - "A Card whose cover image fires an error event swaps to the title-card face; the gray broken-image state is unreachable"
    - "Title-card field color (paper vs ink) is controlled by an explicit prop so callers can alternate deterministically by index; no randomness at render time"
    - "Card can render a reading time line with the exact text pattern 'N min read'"
  artifacts:
    - path: "src/components/v3/title-card.tsx"
      provides: "TitleCard component, the typographic card face"
      exports: ["TitleCard"]
      min_lines: 30
    - path: "src/components/v3/card-cover.tsx"
      provides: "Client-side cover image with onError fallback swap"
      exports: ["CardCover"]
      contains: "use client"
    - path: "src/components/v3/card.tsx"
      provides: "Card extended with readingTime, titleCardField, and TitleCard fallback face"
      contains: "TitleCard"
    - path: "src/app/globals.css"
      provides: ".title-card styles, .card-grid offset-shadow grid styles, .shadowed .title-card homepage hooks"
      contains: ".title-card"
    - path: "src/__tests__/components/title-card.test.tsx"
      provides: "TitleCard render tests"
      min_lines: 30
  key_links:
    - from: "src/components/v3/card.tsx"
      to: "src/components/v3/title-card.tsx"
      via: "import { TitleCard }"
      pattern: "from \"@/components/v3/title-card\"|from './title-card'"
    - from: "src/components/v3/card.tsx"
      to: "src/components/v3/card-cover.tsx"
      via: "import { CardCover }"
      pattern: "CardCover"
    - from: "src/components/v3/title-card.tsx"
      to: "src/app/globals.css"
      via: "className title-card / title-card--ink"
      pattern: "title-card"
---

<objective>
Build the typographic title-card system (SC-1): a TitleCard component in the hero "Create Order" DNA (Hanken Grotesk 800 title, mono kicker chip on vermilion, paper #faf9f7 or ink #17171a field, hard corners, no gradients), a client-side cover-error fallback, Card integration so the title-card is the automatic face when a cover is missing or fails, and the authored CSS (.title-card, .card-grid, .shadowed .title-card) that Plans 02 consumes.

Purpose: Retire the gray broken-image placeholder and bare cropped-logo faces everywhere. This plan is the foundation; Plan 02 wires it into the homepage, /writing, and /projects.
Output: TitleCard + CardCover components, extended Card, globals.css additions, passing vitest files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-project-cards-covers-redesign-typographic-title-card-covers-/19-CONTEXT.md
@src/app/globals.css
@src/components/v3/card.tsx
@src/__tests__/components/card.test.tsx

<interfaces>
Current Card props (src/components/v3/card.tsx lines 5-16):

```typescript
type Props = {
  kicker?: string;
  title: ReactNode;
  blurb?: ReactNode;
  href?: string;
  coverSrc?: string;
  coverAlt?: string;
};
```

Design tokens (src/app/globals.css @theme + :root): `--color-bg: #faf9f7` (paper), `--color-text: #171717` (ink text), `--color-accent: #e5411f` (vermilion), `--font-display: var(--hanken-grotesk)` via `--font-display`, `--font-mono`, `--sig-weight: 800`, `--ease-out: cubic-bezier(0.22, 1, 0.36, 1)`. Offset-solid signature: `box-shadow: 12px 12px 0 var(--color-text)` (h1.sig, globals.css line 99). Homepage shadow hooks: `.shadowed .photo` (line ~933), `.shadowed.in .photo` (line ~937), `body.no-motion .shadowed .photo` (line ~964), and `.shadowed .photo` inside the `@media (prefers-reduced-motion: reduce)` block (line ~982).

Card is consumed ONLY by src/app/writing/page.tsx and src/app/projects/page.tsx (verified by grep). Changing the no-cover fallback from "nothing" to TitleCard affects only those two surfaces, both of which want it.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create TitleCard component and its authored CSS (title-card, card-grid, shadowed hooks)</name>
  <files>src/components/v3/title-card.tsx, src/app/globals.css, src/__tests__/components/title-card.test.tsx</files>
  <read_first>src/app/globals.css (the @theme block at top, h1.sig at lines 85-100, .marker at lines 255-263, .work-grid at line 580, .shadowed rules at lines 925-995), src/components/v3/card.tsx (import/JSDoc conventions), src/__tests__/components/card.test.tsx (test file conventions: vi.mock of next/image and next/link, cleanup in afterEach)</read_first>
  <behavior>
    - Test 1: renders the title text passed as `title`
    - Test 2: renders the kicker text when `kicker` is provided; kicker element has class `title-card-kicker`
    - Test 3: renders the dek text when `dek` is provided; omits the dek element when `dek` is undefined
    - Test 4: root element has class `title-card`; when `field="ink"` it ALSO has class `title-card--ink`; when `field` is omitted it does NOT have `title-card--ink`
    - Test 5: when `aspectRatio="3/2.2"` is passed, the root element's inline style contains aspect-ratio 3/2.2
    - Test 6: renders NO img element (pure typographic face)
  </behavior>
  <action>
    Create `src/components/v3/title-card.tsx`, a server component (no "use client") exporting `export function TitleCard(props)` with props type: `{ title: ReactNode; kicker?: string; dek?: string; field?: "paper" | "ink"; aspectRatio?: string; className?: string }` where `field` defaults to `"paper"`. Import `type { ReactNode }` from "react" and `cn` from "@/utils/cn". Render a single root div: `className={cn("title-card", field === "ink" && "title-card--ink", className)}` and `style={aspectRatio ? { aspectRatio } : undefined}`. Children in order: (1) if kicker, `<span className="title-card-kicker">{kicker}</span>`; (2) a bottom group div containing `<div className="title-card-title">{title}</div>` and, if dek, `<p className="title-card-dek">{dek}</p>`. Add a JSDoc header noting this is the Phase 19 typographic card face (SC-1), that field alternation MUST be driven by callers from list index (deterministic, SSG/ISR safe, per CONTEXT decision "Variation between adjacent title-cards must be deterministic"), and that no user-visible string may contain an em dash.

    In `src/app/globals.css`, add a new authored-CSS section titled `/* ── Phase 19: typographic title-card + offset-shadow card grid ── */` placed directly after the `.work-grid` rule (line ~586). Add these rules with these exact declarations:
    - `.title-card`: position relative; overflow hidden; display flex; flex-direction column; justify-content space-between; gap 14px; padding clamp(16px, 2vw, 24px); background #faf9f7; color #171717; border 1px solid var(--color-border); border-radius 0.
    - `.title-card--ink`: background #17171a; color #faf9f7. (Field colors are hardcoded per CONTEXT: paper #faf9f7 or ink #17171a; they must NOT invert with .band-dark, the face is self-colored.)
    - `.title-card-kicker`: font-family var(--font-mono); font-size 11px; letter-spacing 0.12em; text-transform uppercase; background #e5411f; color #ffffff; padding 5px 9px; align-self flex-start. (This is the marker/chip vermilion accent, DNA from .marker.)
    - `.title-card-title`: font-family var(--font-display); font-weight 800; line-height 1.02; letter-spacing -0.02em; font-size clamp(1.3rem, 2.4vw, 1.9rem); display -webkit-box; -webkit-box-orient vertical; -webkit-line-clamp 3; overflow hidden. (Clamp handles long titles.)
    - `.title-card-dek`: font-size 0.85rem; line-height 1.4; opacity 0.72; margin 6px 0 0; display -webkit-box; -webkit-box-orient vertical; -webkit-line-clamp 2; overflow hidden.
    - `.card-grid`: display grid; grid-template-columns repeat(auto-fill, minmax(280px, 1fr)); gap clamp(20px, 2.4vw, 32px); padding 2px 18px 22px 2px. (Right/bottom padding gives the offset shadows room; consumed by Plan 02 on /writing and /projects.)
    - `.card-grid > a, .card-grid > div`: border 1px solid var(--color-border-strong); box-shadow 8px 8px 0 var(--color-text); transition box-shadow 0.18s var(--ease-out), background-color 0.18s var(--ease-out).
    - `.card-grid > a:hover`: box-shadow 8px 8px 0 var(--color-accent). (Vermilion hover per CONTEXT grid-treatment decision. No gradients anywhere.)

    Then extend the four existing homepage shadow selectors so TitleCard works inside `.shadowed .slide` cells (consumed by Plan 02 in section-work): change `.shadowed .photo` (line ~933) to `.shadowed .photo, .shadowed .title-card`; change `.shadowed.in .photo` (line ~937) to `.shadowed.in .photo, .shadowed.in .title-card`; change `body.no-motion .shadowed .photo` (line ~964) to `body.no-motion .shadowed .photo, body.no-motion .shadowed .title-card`; change the `.shadowed .photo` selector inside the `@media (prefers-reduced-motion: reduce)` block (line ~982) to `.shadowed .photo, .shadowed .title-card`. Do not change any declaration values in those rules.

    Create `src/__tests__/components/title-card.test.tsx` implementing the six behavior tests above, following the conventions of card.test.tsx (React.createElement, @testing-library/react render/screen/cleanup, afterEach cleanup). No next/image or next/link mocks are needed (TitleCard uses neither).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/components/title-card.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - src/components/v3/title-card.tsx exists, exports TitleCard, contains no "use client" directive, no next/image import, and no em dash character anywhere in the file
    - src/app/globals.css contains selectors .title-card, .title-card--ink, .title-card-kicker, .title-card-title, .title-card-dek, .card-grid, and the string "8px 8px 0 var(--color-accent)"
    - `grep -c 'shadowed .title-card' src/app/globals.css` outputs 4
    - `grep -c 'linear-gradient' src/components/v3/title-card.tsx` outputs 0 and the new CSS section contains no linear-gradient
    - `npx vitest run src/__tests__/components/title-card.test.tsx` exits 0 with 6 passing tests
  </acceptance_criteria>
  <done>TitleCard renders kicker chip + Hanken-800 title + optional dek on paper or ink field with deterministic prop-driven alternation; all supporting CSS (title-card, card-grid, shadowed hooks) exists in globals.css; 6 new tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CardCover error-fallback client component and Card integration</name>
  <files>src/components/v3/card-cover.tsx, src/components/v3/card.tsx, src/__tests__/components/card.test.tsx</files>
  <read_first>src/components/v3/card.tsx (entire file, 73 lines), src/components/v3/title-card.tsx (created in Task 1), src/__tests__/components/card.test.tsx (existing 9 tests; all must keep passing)</read_first>
  <behavior>
    - Test 1 (new): Card with no coverSrc renders a title-card face: container has an element with class `title-card` containing the title text
    - Test 2 (new): Card with `titleCardField="ink"` and no coverSrc renders an element with class `title-card--ink`
    - Test 3 (new): Card with `readingTime={4}` renders the exact text "4 min read"
    - Test 4 (new): Card with coverSrc renders an img; after `fireEvent.error(img)` the img is gone and an element with class `title-card` is present (fallback swap)
    - Existing tests: all 9 current card.test.tsx tests continue to pass unchanged, including "does NOT render an img element when coverSrc is absent" (TitleCard contains no img) and "renders kicker text when provided" (kicker now appears on the title-card face)
  </behavior>
  <action>
    Create `src/components/v3/card-cover.tsx` as a client component: first line exactly `"use client";`. Exports `export function CardCover(props)` with props type `{ src: string; alt: string; sizes?: string; fallback: ReactNode }`. Uses `useState(false)` for a `failed` flag. When `failed` is true, return `<>{fallback}</>`. Otherwise return the current cover slot markup moved from card.tsx: a div `className="relative w-full aspect-[4/3] overflow-hidden"` containing next/image `<Image src={src} alt={alt} fill sizes={sizes ?? "(max-width: 768px) 100vw, (max-width: 1200px) 50vw, 33vw"} className="object-cover" onError={() => setFailed(true)} />`. JSDoc: retires the gray broken-image placeholder (Phase 19 SC-1); a cover that 404s or errors swaps to the TitleCard fallback client-side.

    Modify `src/components/v3/card.tsx`:
    1. Extend Props with `readingTime?: number;` (minutes, rendered as "N min read", /writing only) and `titleCardField?: "paper" | "ink";` (deterministic field alternation, callers pass from list index).
    2. Import TitleCard from "@/components/v3/title-card" and CardCover from "@/components/v3/card-cover".
    3. Build the fallback face once: `const titleCardFace = <TitleCard title={title} kicker={kicker} field={titleCardField} aspectRatio="4/3" />`.
    4. Replace the coverSlot logic: when coverSrc is provided, render `<CardCover src={coverSrc} alt={coverAlt ?? ""} fallback={titleCardFace} />`; when coverSrc is absent, render `titleCardFace` directly (this replaces the old `null`).
    5. Restructure the text block: when coverSrc is PRESENT (image face), keep the existing padded text block exactly as today (kicker span, h3 title, blurb) and append after blurb, when readingTime is defined: `<p className="font-mono text-xs text-[var(--color-text-muted)] mt-2">{readingTime} min read</p>`. When coverSrc is ABSENT (title-card face), the title and kicker live on the face, so the padded text block must NOT repeat them: render the `p-[26px]` div only if blurb or readingTime is defined, containing only the blurb paragraph and the readingTime paragraph. (Known accepted edge: if a cover errors at runtime, the client fallback shows the title on the face while the text block below still shows kicker/title; rare failure state, still legible, do not add client state to Card itself.)
    6. Update the component JSDoc: title-card fallback per Phase 19 SC-1, deterministic field via titleCardField, no em dashes in user-visible strings.

    Update `src/__tests__/components/card.test.tsx`: keep all 9 existing tests untouched; add the four new tests from the behavior block in a new describe block "Card title-card fallback (Phase 19 / 19-01)". For Test 4 import `fireEvent` from @testing-library/react; the existing next/image mock passes onError through to the rendered img, so `fireEvent.error(container.querySelector("img")!)` triggers the swap. Note the existing mock passes non-DOM props (fill, sizes) to the img; that already produces React warnings today and is acceptable.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/components/card.test.tsx src/__tests__/components/title-card.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - src/components/v3/card-cover.tsx exists, first line is `"use client";`, exports CardCover, wires onError to a state flag that swaps to the fallback ReactNode
    - src/components/v3/card.tsx imports TitleCard and CardCover; the string `null` no longer appears as the no-cover coverSlot value; Props include readingTime and titleCardField
    - Card renders the exact string pattern `{readingTime} min read` (space before "min", no punctuation)
    - `npx vitest run src/__tests__/components/card.test.tsx` exits 0 with 13 passing tests (9 existing + 4 new)
    - No em dash character in card.tsx or card-cover.tsx
  </acceptance_criteria>
  <done>Card automatically shows a TitleCard face when a cover is missing, swaps to it client-side when a cover errors, and supports readingTime and deterministic titleCardField; 13 card tests + 6 title-card tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Notion content -> rendered JSX | Card/TitleCard render titles, kickers, deks sourced from Monty's Notion workspace (semi-trusted authoring surface) |
| Cover image URL -> client img | CardCover loads /api/notion-cover proxy URLs; error events drive client state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Injection (XSS) | TitleCard/Card rendering Notion strings | mitigate | Render only via JSX text interpolation; no dangerouslySetInnerHTML in any file this plan touches (acceptance-checked by review of new files) |
| T-19-02 | DoS | CardCover onError handler | accept | onError sets a boolean once and removes the img; no retry loop, no network amplification |
| T-19-SC | Tampering | npm/pip/cargo installs | accept | This plan installs no packages; no new dependencies added to package.json |
</threat_model>

<verification>
- `npx vitest run src/__tests__/components/` exits with card.test.tsx and title-card.test.tsx green.
- `npx vitest run` produces NO new failing test files; the only failing files remain the pre-existing src/__tests__/home/explorative-homepage.test.tsx and src/__tests__/home/section-building.test.tsx (3 pre-existing failures; do not attribute them to this plan and do not fix them here).
- `grep -n 'linear-gradient' src/components/v3/title-card.tsx src/components/v3/card-cover.tsx src/components/v3/card.tsx` returns nothing (no-gradients rule).
- `grep -c 'shadowed .title-card' src/app/globals.css` outputs 4.
</verification>

<success_criteria>
- TitleCard component exists with kicker chip, Hanken 800 clamped title, optional dek, paper/ink field prop, aspectRatio prop.
- Card falls back to TitleCard when coverSrc is absent and when the cover image errors (client swap via CardCover).
- .card-grid and .shadowed .title-card CSS exists for Plan 02 to consume.
- 19 component tests pass (13 card + 6 title-card); no new suite-level failures.
</success_criteria>

<output>
Create `.planning/phases/19-project-cards-covers-redesign-typographic-title-card-covers-/19-01-SUMMARY.md` when done.
</output>
