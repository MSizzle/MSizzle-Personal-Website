---
phase: 19-project-cards-covers-redesign
plan: 02
type: execute
wave: 2
depends_on: [19-01]
files_modified:
  - src/components/home/section-work.tsx
  - src/app/writing/page.tsx
  - src/app/projects/page.tsx
  - src/__tests__/home/section-work.test.tsx
  - src/__tests__/pages/writing.test.tsx
  - src/__tests__/pages/projects.test.tsx
autonomous: true
requirements: [SC-2, SC-3, SC-4]
must_haves:
  truths:
    - "Every homepage Work grid cell shows a typographic title-card with a visible project title; no cropped-logo cover images and no gray broken-image tiles remain (audit failure cases 'MAHea Scann', 'Goalter', 'Insider Tra', 'Web C' are impossible)"
    - "Every /projects index card face is a title-card (projects never use Notion covers as card faces); adjacent cards alternate paper/ink deterministically by index"
    - "/writing cards keep real Notion covers when present and fall back to title-cards otherwise; each card shows a one-line dek when the Notion description exists and a reading time computed from post content"
    - "/writing and /projects grids use the offset-solid shadow treatment (8px ink shadow, radius 0) with a vermilion hover, matching the homepage energy"
  artifacts:
    - path: "src/components/home/section-work.tsx"
      provides: "Work grid cells rendering TitleCard faces with title, kicker, dek"
      contains: "TitleCard"
    - path: "src/app/writing/page.tsx"
      provides: "card-grid treatment, readingTime via getBlocks + calculateReadingTime, titleCardField alternation"
      contains: "calculateReadingTime"
    - path: "src/app/projects/page.tsx"
      provides: "card-grid treatment, always-title-card faces (no coverSrc), titleCardField alternation"
      contains: "card-grid"
  key_links:
    - from: "src/components/home/section-work.tsx"
      to: "src/components/v3/title-card.tsx"
      via: "import { TitleCard }"
      pattern: "from \"@/components/v3/title-card\""
    - from: "src/app/writing/page.tsx"
      to: "src/utils/reading-time.ts"
      via: "calculateReadingTime(blocks) per post"
      pattern: "calculateReadingTime"
    - from: "src/app/writing/page.tsx"
      to: "src/lib/notion.ts"
      via: "getBlocks(post.id)"
      pattern: "getBlocks"
    - from: "src/app/writing/page.tsx"
      to: "src/app/globals.css .card-grid"
      via: "grid container className"
      pattern: "card-grid"
    - from: "src/app/projects/page.tsx"
      to: "src/app/globals.css .card-grid"
      via: "grid container className"
      pattern: "card-grid"
---

<objective>
Wire the Plan 01 title-card system into the three broken surfaces: the homepage "Some of the work I am proudest of" grid gets typographic title-card faces with visible titles and deks (SC-2); /projects index cards become always-title-card faces; /writing cards gain reading time and deks (SC-4); both index grids adopt the brutalist offset-shadow treatment with vermilion hover (SC-3).

Purpose: Kill the audit failure cases (cropped logo wordmarks, gray broken-image tiles, untitled cards, flat white interior grids).
Output: Modified section-work.tsx, writing/page.tsx, projects/page.tsx with updated tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-project-cards-covers-redesign-typographic-title-card-covers-/19-CONTEXT.md
@.planning/phases/19-project-cards-covers-redesign-typographic-title-card-covers-/19-01-SUMMARY.md
@src/components/home/section-work.tsx
@src/app/writing/page.tsx
@src/app/projects/page.tsx
@src/components/v3/title-card.tsx
@src/components/v3/card.tsx

<interfaces>
From src/components/v3/title-card.tsx (created by Plan 19-01):

```typescript
export function TitleCard(props: {
  title: ReactNode;
  kicker?: string;
  dek?: string;
  field?: "paper" | "ink";   // default "paper"; callers alternate by index
  aspectRatio?: string;       // e.g. "3/2.2"
  className?: string;
}): JSX.Element
```

From src/components/v3/card.tsx (extended by Plan 19-01): existing props plus `readingTime?: number` and `titleCardField?: "paper" | "ink"`. When coverSrc is undefined the Card face is automatically a TitleCard.

From src/lib/notion.ts: `getPublishedPosts(): Promise<BlogPost[]>`, `getBlocks(blockId: string): Promise<BlockObjectResponse[]>`. BlogPost has `id, slug, title, description, date, tags, cover`.

From src/utils/reading-time.ts: `calculateReadingTime(blocks: BlockObjectResponse[]): number` (exact, from content blocks; returns at least 1). Do NOT use `estimateReadingTime(post.description)` for the card reading time: a one-line description always yields "1 min read", which reads as fake. CONTEXT requires reading time computed from content length; getBlocks + calculateReadingTime is the reuse path.

From src/lib/notion-projects.ts: `Project` has `id, slug, title, description, tags, image, cover, lastEdited`. Homepage passes `getFeaturedProjects()` results into SectionWork via props.

From src/app/globals.css (Plan 19-01): `.card-grid` grid container class (offset shadows + vermilion hover applied to direct children), and `.shadowed .title-card` slide-in shadow hooks mirroring `.shadowed .photo`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Homepage Work grid renders TitleCard faces with visible titles and deks</name>
  <files>src/components/home/section-work.tsx, src/__tests__/home/section-work.test.tsx</files>
  <read_first>src/components/home/section-work.tsx (entire file, 95 lines), src/components/v3/title-card.tsx (props from Plan 01), src/__tests__/home/section-work.test.tsx (existing 5 tests, all must keep passing)</read_first>
  <behavior>
    - Test 1 (new): given `projects` containing two projects with titles "AI Assistant" and "Design Tool", both title strings are visible in the rendered output
    - Test 2 (new): given a project with `description: "An AI product"`, that dek text is visible
    - Test 3 (new): no img element whose src contains "notion-cover" renders, even when a project has a non-null `cover` (projects never show cover images per CONTEXT decision)
    - Test 4 (new): with no projects prop, four elements with class `title-card` still render (placeholder cells keep the 2x2 grid shape)
    - Existing: all 5 current tests (link to /projects, no /portfolio link, "Projects" text, "SELECTED" kicker, Prometheus rel attributes) keep passing
  </behavior>
  <action>
    Modify `src/components/home/section-work.tsx` (per CONTEXT decisions: projects DEFAULT to typographic title-cards; homepage Work cards get a visible title and one-line dek if available):
    1. Remove the `Photo` import; import `TitleCard` from "@/components/v3/title-card".
    2. Replace `captionFor` with `kickerFor(project, index)`: if no project, return `0${index + 1}`; otherwise compute `year` from `project.lastEdited` UTC full year (as today) and return `[project.tags?.[0], year].filter(Boolean).join(" · ")` falling back to `0${index + 1}` when that join is empty. The middle-dot separator " · " is allowed; em dashes and "--" are not.
    3. Inside each `.shadowed .slide` cell, replace the `<Photo ...>` element with `<TitleCard aspectRatio="3/2.2" field={i % 2 === 0 ? "paper" : "ink"} title={project?.title ?? "Selected work"} kicker={kickerFor(project, i)} dek={project?.description || undefined} />`. Field alternation by index is the deterministic variation required by CONTEXT (never random). Do NOT pass any cover/src; project covers are retired as homepage card faces.
    4. Keep everything else byte-identical in behavior: the `cells` Array.from(4) fill, the `.shadowed slide from-left/from-right` wrapper classes and key, RailBox, the h2, the /projects link with "SELECTED" kicker, and the Prometheus anchor with rel="noopener noreferrer".
    5. Update the component JSDoc: D-07 grid now renders typographic TitleCard faces (Phase 19 SC-2); cover images are no longer used as project card faces; placeholders are TitleCards with index kickers.

    Update `src/__tests__/home/section-work.test.tsx`: remove the now-unused `@/components/home/photo` mock; keep all other mocks and the 5 existing tests unchanged; add the four new tests from the behavior block in a describe block "SectionWork title-card faces (Phase 19 / 19-02)". Build mock projects inline with the full Project shape used in src/__tests__/pages/projects.test.tsx (id, slug, title, description, cover, image, emoji, externalUrl, tags, featured, published, lastEdited). Let TitleCard render for real (it is a pure server component; no mock needed).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/home/section-work.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - src/components/home/section-work.tsx imports TitleCard and no longer imports Photo; `grep -c 'notion-cover' src/components/home/section-work.tsx` outputs 0
    - Field alternation is exactly `i % 2 === 0 ? "paper" : "ink"` (deterministic by index)
    - The /projects link, "SELECTED" kicker, and Prometheus rel="noopener noreferrer" anchor are unchanged
    - `npx vitest run src/__tests__/home/section-work.test.tsx` exits 0 with 9 passing tests (5 existing + 4 new)
    - No em dash and no "--" in any string literal added to section-work.tsx
  </acceptance_criteria>
  <done>Homepage Work grid shows 4 typographic title-cards with visible titles (and deks when available), deterministic paper/ink alternation, slide-in offset shadows intact, and zero cover images; 9 tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: /writing gets reading time, deks, and the offset-shadow card grid</name>
  <files>src/app/writing/page.tsx, src/__tests__/pages/writing.test.tsx</files>
  <read_first>src/app/writing/page.tsx (entire file, 155 lines), src/utils/reading-time.ts, src/lib/notion.ts (getBlocks signature at line 160), src/__tests__/pages/writing.test.tsx (note the vi.mock("@/lib/notion") factory at lines 45-47 exports ONLY getPublishedPosts; it must gain getBlocks or the page import will crash in tests)</read_first>
  <behavior>
    - Test 1 (new): given one post whose mocked getBlocks resolves to paragraph blocks totaling ~450 words, the card shows "3 min read" (Math.ceil(450/200) = 3); simpler alternative accepted: mock getBlocks to reject and assert no "min read" text, plus a second post asserting the happy path with a small block set showing "1 min read"
    - Test 2 (new): when getBlocks rejects for a post, the page still renders that post's card with its title and no "min read" line (graceful omission, no crash)
    - Test 3 (new): the grid container element has class "card-grid" and no longer has class "gap-px"
    - Existing: all current writing.test.tsx tests keep passing
  </behavior>
  <action>
    Modify `src/app/writing/page.tsx`:
    1. Add imports: `getBlocks` to the existing `@/lib/notion` import, and `import { calculateReadingTime } from "@/utils/reading-time";`.
    2. After the posts fetch, compute per-post reading times without failing the page: `const readingTimeEntries = await Promise.all(posts.map(async (post) => [post.id, await getBlocks(post.id).then(calculateReadingTime).catch(() => undefined)] as const)); const readingTimes = new Map(readingTimeEntries);`. Each post's failure degrades to an omitted reading-time line (CONTEXT: fall back gracefully, never fabricate). ISR revalidate 1800 stays unchanged and caps Notion call frequency.
    3. In the yearPosts.map card render, change the callback to `(post, i)` and pass three new props to Card: `readingTime={readingTimes.get(post.id)}`, `kicker={post.tags?.[0] ?? "Essay"}` (replacing the current kicker line so fallback title-cards always carry a kicker chip), and `titleCardField={i % 2 === 0 ? "paper" : "ink"}` (deterministic alternation within each year grid). Keep coverSrc/coverAlt logic exactly as-is (essays KEEP real covers when present; the title-card is only the fallback). Keep `blurb={post.description}` as the dek (empty string already renders nothing).
    4. Replace the grid container className string `grid [grid-template-columns:repeat(auto-fill,minmax(260px,1fr))] gap-px bg-[var(--color-border)] border border-[var(--color-border)]` with `card-grid` (authored CSS from Plan 19-01: real gaps, 8px offset ink shadows on children, vermilion hover, radius 0). Keep the surrounding `-mx-6 md:-mx-40` wrapper and YearBlock structure untouched (YearBlock supplies its own padding).
    5. Update the page JSDoc comment block: reading time per D (Phase 19 SC-4) computed from content blocks via getBlocks + calculateReadingTime with per-post catch; grid uses the Phase 19 card-grid offset-shadow treatment (SC-3).

    Update `src/__tests__/pages/writing.test.tsx`:
    1. Extend the `vi.mock("@/lib/notion", ...)` factory to `{ getPublishedPosts: vi.fn(), getBlocks: vi.fn().mockResolvedValue([]) }` so existing tests keep working (empty blocks -> calculateReadingTime returns 1).
    2. NOTE: with that default mock, posts in EXISTING tests will now render a "1 min read" line; existing assertions query specific text/roles so they are unaffected, but verify none assert exact card text counts.
    3. Add the new tests from the behavior block in a describe block "/writing reading time + card-grid (Phase 19 / 19-02)". For block mocks, a paragraph block shaped like `{ type: "paragraph", paragraph: { rich_text: [{ plain_text: "word ".repeat(450) }] } }` is enough IF extractTextFromBlock reads plain_text; read src/utils/notion-text.ts first and shape the mock to whatever it actually reads. If shaping real blocks is brittle, use the accepted simpler alternative in Test 1.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/pages/writing.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - src/app/writing/page.tsx contains `getBlocks`, `calculateReadingTime`, `readingTimes.get(post.id)`, `titleCardField={i % 2 === 0 ? "paper" : "ink"}`, and the grid container className is exactly `card-grid`
    - `grep -c 'gap-px' src/app/writing/page.tsx` outputs 0
    - `grep -c 'estimateReadingTime' src/app/writing/page.tsx` outputs 0 (reading time comes from content blocks, not the one-line description)
    - A getBlocks rejection for one post does not remove that post's card and does not crash the page render (covered by a test)
    - `npx vitest run src/__tests__/pages/writing.test.tsx` exits 0
    - No em dash and no "--" in any string literal added to writing/page.tsx (kicker fallback is "Essay")
  </acceptance_criteria>
  <done>/writing cards show content-derived reading time and deks, keep real covers with title-card fallback, alternate fallback fields deterministically, and sit in the offset-shadow card-grid; tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: /projects goes always-title-card with the offset-shadow grid</name>
  <files>src/app/projects/page.tsx, src/__tests__/pages/projects.test.tsx</files>
  <read_first>src/app/projects/page.tsx (entire file, 115 lines), src/__tests__/pages/projects.test.tsx (lines 100-212: the index-page tests; lines 161-185 assert a cover img renders for project.image non-null, which this task must REWRITE; lines 214+ are detail-page tests and must NOT be touched)</read_first>
  <behavior>
    - Test 1 (rewritten from "renders cover image when project.image is non-null"): given a project WITH a non-null image, NO img with src containing "notion-cover" renders, and an element with class `title-card` containing the project title renders instead (projects never use covers as card faces per CONTEXT)
    - Test 2 (existing, still passes): project with null image renders no notion-cover img
    - Test 3 (new): the grid container element has class "card-grid"
    - Test 4 (new): with two projects, the first card face has class `title-card` without `title-card--ink` and the second has `title-card--ink` (deterministic index alternation)
    - Existing: empty-state, PageHero, and card-link tests keep passing; detail-page tests untouched
  </behavior>
  <action>
    Modify `src/app/projects/page.tsx`:
    1. In the yearProjects.map card render, change the callback to `(project, i)` and REMOVE the `coverSrc` and `coverAlt` props entirely: per the locked CONTEXT decision, logo-lockup Notion covers must no longer be the card face for projects, so every /projects card face is the Card's automatic TitleCard fallback. Do not add any "v1/for now" language; this is the intended end state until real per-project art exists (explicitly deferred content work).
    2. Add `kicker={project.tags?.[0] ?? "Project"}` (replacing the current kicker line) and `titleCardField={i % 2 === 0 ? "paper" : "ink"}`.
    3. Replace the grid container className string `grid [grid-template-columns:repeat(auto-fill,minmax(260px,1fr))] gap-px bg-[var(--color-border)] border border-[var(--color-border)]` with `card-grid`. Keep the `-mx-6 md:-mx-40` wrapper and YearBlock structure untouched.
    4. Update the page JSDoc: Phase 19 SC-2/SC-3, always-title-card faces with deterministic paper/ink alternation, offset-shadow card-grid, covers retired as card faces (project.image remains in the data layer for the detail page).

    Update `src/__tests__/pages/projects.test.tsx` (index-page describe only):
    1. Rewrite the test at lines 161-185 ("renders cover image when project.image is non-null") to the Behavior Test 1 assertion; rename it "renders a title-card face instead of a cover image when project.image is non-null (Phase 19)".
    2. Keep "does NOT render cover image when project.image is null" as-is (still true).
    3. Add Behavior Tests 3 and 4 in a describe block "/projects title-card grid (Phase 19 / 19-02)".
    4. Do not modify anything in the detail-page section (line 214 onward).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/pages/projects.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'coverSrc' src/app/projects/page.tsx` outputs 0 and `grep -c 'gap-px' src/app/projects/page.tsx` outputs 0
    - src/app/projects/page.tsx contains `card-grid`, `titleCardField={i % 2 === 0 ? "paper" : "ink"}`, and the kicker fallback string "Project"
    - `npx vitest run src/__tests__/pages/projects.test.tsx` exits 0, including the rewritten no-cover-face test and untouched detail-page tests
    - No em dash and no "--" in any string literal added to projects/page.tsx
  </acceptance_criteria>
  <done>/projects renders every card as a typographic title-card with deterministic field alternation inside the offset-shadow grid; no notion-cover imgs on the index; tests pass with the cover-face test rewritten.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Notion content -> rendered JSX | Titles, tags, descriptions from Notion render on three surfaces |
| /writing render -> Notion API | getBlocks called once per published post at ISR revalidation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-03 | Injection (XSS) | section-work/writing/projects rendering Notion strings | mitigate | JSX text interpolation only; no dangerouslySetInnerHTML in touched files |
| T-19-04 | DoS | per-post getBlocks fan-out on /writing | mitigate | Per-post .catch(() => undefined) so one failure never breaks the page; ISR revalidate=1800 caps Notion call frequency to one fan-out per 30 min; failure degrades to omitting the reading-time line |
| T-19-SC | Tampering | npm/pip/cargo installs | accept | No packages installed by this plan |
</threat_model>

<verification>
- `npx vitest run src/__tests__/home/section-work.test.tsx src/__tests__/pages/writing.test.tsx src/__tests__/pages/projects.test.tsx` exits 0.
- `npx vitest run` shows NO new failing test files; only the pre-existing explorative-homepage.test.tsx and section-building.test.tsx failures remain (3 pre-existing failures; not this phase's).
- `grep -rn 'notion-cover' src/components/home/section-work.tsx src/app/projects/page.tsx` returns nothing; `grep -n 'notion-cover' src/app/writing/page.tsx` still returns the essay cover line (essays keep covers).
- Human check (non-blocking, for verify-work later): on `npm run dev`, the homepage Work grid shows 4 titled title-cards (no cropped logos, no gray tiles); /projects shows alternating paper/ink title-cards with 8px offset shadows warming to vermilion on hover; /writing shows covers where present, "N min read" lines, and deks.
</verification>

<success_criteria>
- Zero cropped-logo card faces and zero gray broken-image placeholders on homepage Work grid and /projects (the five audit failure cases cannot render).
- Every card on all three surfaces shows a visible title.
- /writing shows content-derived reading time and deks with graceful omission on failure.
- Both index grids carry the offset-solid + vermilion-hover treatment, radius 0, no gradients.
</success_criteria>

<output>
Create `.planning/phases/19-project-cards-covers-redesign-typographic-title-card-covers-/19-02-SUMMARY.md` when done.
</output>
