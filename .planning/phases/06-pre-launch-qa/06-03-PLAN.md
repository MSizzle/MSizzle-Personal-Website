---
phase: 06-pre-launch-qa
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - .planning/phases/06-pre-launch-qa/06-UAT.md
  - .planning/phases/06-pre-launch-qa/evidence/requirements/
autonomous: false
requirements:
  - HOME-01
  - HOME-02
  - HOME-03
  - HOME-04
  - PORT-01
  - PORT-02
  - PORT-03
  - BLOG-01
  - BLOG-02
  - BLOG-03
  - BLOG-04
  - BLOG-05
  - ABOUT-01
  - ANLY-01
  - ANLY-02
  - ANLY-03
  - ANLY-04
  - ANLY-05
  - ANLY-06
  - DSGN-01
  - DSGN-02
  - DSGN-03
  - DSGN-04
  - DSGN-05
  - DSGN-06
  - SOC-01
  - SOC-02
  - SOC-03
  - SOC-04
  - SOC-05
decisions_addressed:
  - D-01
  - D-06
  - D-07
  - D-09
  - D-10

must_haves:
  truths:
    - "Every one of the 28 v1 requirement IDs has been walked on the live Vercel production URL"
    - "Each requirement has at least one desktop screenshot and one mobile screenshot committed under evidence/requirements/"
    - "06-UAT.md exists and lists every requirement ID with status pass | fail | skipped | blocked"
    - "Every `fail` row has a corresponding FIX sub-plan file or an entry on the blocking-issues list"
    - "Every `skipped` or `blocked` row has a one-line reason documented"
    - "The UAT was run against the same commit SHA captured by 06-02 (Lighthouse)"
  artifacts:
    - path: ".planning/phases/06-pre-launch-qa/06-UAT.md"
      provides: "Phase 6 primary audit artifact per D-07 standard GSD UAT format"
      contains: "| Requirement | Status |"
    - path: ".planning/phases/06-pre-launch-qa/evidence/requirements/{ID}-desktop.png"
      provides: "Desktop screenshot evidence per requirement ID"
    - path: ".planning/phases/06-pre-launch-qa/evidence/requirements/{ID}-mobile.png"
      provides: "Mobile screenshot evidence per requirement ID"
  key_links:
    - from: "06-03 06-UAT.md"
      to: "06-06 GO-NO-GO report"
      via: "06-06 aggregates UAT pass/fail counts into final launch recommendation"
      pattern: "pass | fail | skipped | blocked counts per category"
    - from: "06-03 06-UAT.md"
      to: "reconciled REQUIREMENTS.md (from 06-01)"
      via: "UAT reads the reconciled list, then updates each row with pass/fail"
      pattern: "requirement IDs match 1:1"

threat_model:
  summary: "Read-only human walk of the live site with screenshotting. No code changes. Only risk is committing a screenshot that inadvertently captures PII or session data from the operator's browser."
  threats:
    - id: T-06-03-1
      category: Information Disclosure
      description: "Screenshots could capture browser UI containing operator's bookmarks, extensions, or autofill data"
      severity: low
      mitigation: "Use an incognito window for all screenshot captures. Crop screenshots to page content area only (hide browser chrome where practical)."
      verified_by: "Operator visual check before commit"
---

<objective>
Walk all 28 v1 requirements on the live Vercel production URL, capture a desktop + mobile screenshot for each, and produce `06-UAT.md` in the standard GSD UAT format (`pass | fail | skipped | blocked` per requirement).

Per D-06, this is eyeball + screenshot, not Playwright (D-06 deferred Playwright to post-launch). Per D-07, the UAT file is the primary audit artifact for Phase 6 completion. Per D-01 / D-02, the walk targets the live production URL at the commit SHA recorded by 06-02.

Plan `autonomous: false` because this is entirely human verification against a rendered browser. The operator runs through a scripted checklist and records outcomes.

Scope bounded by D-06 (method), D-07 (UAT file format), D-09 / D-10 (blocking failures open FIX sub-plans; non-blocking go to `/gsd-add-todo`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/06-pre-launch-qa/06-CONTEXT.md
@.planning/phases/06-pre-launch-qa/06-01-PLAN.md
@.planning/phases/04-animation-polish/04-VERIFICATION.md
@.planning/phases/04-animation-polish/04-HUMAN-UAT.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Scaffold 06-UAT.md with all 28 requirement rows</name>
  <files>.planning/phases/06-pre-launch-qa/06-UAT.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (reconciled by 06-01 Task 1, the 28 v1 IDs live here)
    - .planning/phases/04-animation-polish/04-HUMAN-UAT.md (format precedent for GSD UAT files)
  </read_first>
  <action>
Create `06-UAT.md` with the standard GSD frontmatter and a row per requirement. Status starts as `pending`; Task 2 flips each to `pass` / `fail` / `skipped` / `blocked`.

Use the requirement groupings from REQUIREMENTS.md so the walk order is natural (Homepage, Portfolio, Blog, About, Analytics, Design, Social).

```markdown
---
status: partial
phase: 06-pre-launch-qa
source: [REQUIREMENTS.md (reconciled 06-01), ROADMAP.md Phase 6, 06-CONTEXT.md]
audit_url: https://montysinger.com
commit_sha: <populate from 06-02 SUMMARY.md>
started: <UTC timestamp>
updated: <UTC timestamp>
---

# Phase 6 User Acceptance Test

**Method:** Eyeball + screenshot against live Vercel production URL (D-06).
**Audit URL:** https://montysinger.com
**Commit SHA:** <copy from .planning/phases/06-pre-launch-qa/evidence/lighthouse/SUMMARY.md>
**Screenshot evidence:** `.planning/phases/06-pre-launch-qa/evidence/requirements/<ID>-desktop.png` + `<ID>-mobile.png`

Legend: `pass` · `fail` (opens FIX sub-plan per D-10) · `skipped` (intentionally deferred, must note reason) · `blocked` (dependency outside Phase 6, e.g., DSGN-06 Super-to-Vercel cutover per D-15).

## Homepage

| ID       | Requirement (abbrev)                    | Expected behavior on live URL                                                                             | Status   | Evidence                            | Notes |
|----------|-----------------------------------------|-----------------------------------------------------------------------------------------------------------|----------|-------------------------------------|-------|
| HOME-01  | Hero: photo, name, tagline, CTA          | `/` renders photo carousel + name + 2-3 sentence intro paragraph (D-19, NOT the old rotating carousel)    | pending  | HOME-01-desktop.png, HOME-01-mobile.png |       |
| HOME-02  | Scroll narrative sections                | About snapshot, Works list, Writings list, Events section visible as user scrolls down `/`                | pending  | HOME-02-desktop.png, HOME-02-mobile.png |       |
| HOME-03  | GSAP scroll-triggered animations         | Hero elements stagger in on page load/scroll (see 04-VERIFICATION truth #7 for reference behavior)         | pending  | HOME-03-desktop.png                 |       |
| HOME-04  | Lenis smooth scrolling site-wide         | Scrolling on `/` shows momentum/easing (test Chrome + Safari per 04-HUMAN-UAT #1)                         | pending  | HOME-04-desktop.png                 |       |

## Portfolio

| ID       | Requirement                                | Expected on live                                                                                | Status   | Evidence                                  | Notes |
|----------|--------------------------------------------|-------------------------------------------------------------------------------------------------|----------|-------------------------------------------|-------|
| PORT-01  | Project cards (title, desc, image, links)  | `/projects` shows 7 cards (MAHealth, Goaltender, Insider Trader, CRM Bot, Weather Bot, AI Fashion, Two Phones per D-34)                                           | pending  | PORT-01-desktop.png, PORT-01-mobile.png   |       |
| PORT-02  | Hover-reveal on project cards              | Desktop hover = overlay slide-up with "View Project" CTA. Mobile tap toggles. Per 04-HUMAN-UAT #4. | pending  | PORT-02-desktop.png, PORT-02-mobile.png   |       |
| PORT-03  | Case study deep-dive pages                 | Click a card, navigate to `/projects/{slug}`, page shows rich media content                       | pending  | PORT-03-desktop.png                       |       |

## Blog

| ID       | Requirement                                | Expected on live                                                                                  | Status   | Evidence                                      | Notes |
|----------|--------------------------------------------|---------------------------------------------------------------------------------------------------|----------|-----------------------------------------------|-------|
| BLOG-01  | Notion-powered posts with custom rendering | `/blog/{slug}` renders Notion content with custom block renderer (not react-notion-x)             | pending  | BLOG-01-desktop.png                           |       |
| BLOG-02  | @tailwindcss/typography prose              | Body copy uses prose styling with readable line-length, heading scale, list styling                | pending  | BLOG-02-desktop.png                           |       |
| BLOG-03  | Reading time on posts                      | Post detail + blog index show "X min read" per D-39                                                | pending  | BLOG-03-desktop.png                           |       |
| BLOG-04  | Tag filtering on blog index                | `/blog` filters posts by tag without full reload                                                    | pending  | BLOG-04-desktop.png                           |       |
| BLOG-05  | Rich Notion blocks (callouts, toggles, code, embeds) | At least one post renders each block type without placeholder/blank                       | pending  | BLOG-05-desktop.png                           |       |

## About

| ID       | Requirement                          | Expected on live                                                              | Status   | Evidence           | Notes |
|----------|--------------------------------------|-------------------------------------------------------------------------------|----------|--------------------|-------|
| ABOUT-01 | Bio with Georgetown + prose skills   | `/about` shows D-21 rewrite: Prometheus intro, Writing, Education (Georgetown University only). No NYC, no past jobs, no Leerink. | pending | ABOUT-01-desktop.png, ABOUT-01-mobile.png | |

## Analytics (Umami)

| ID       | Requirement                     | Expected (verified in Umami dashboard, NOT just on-site)                         | Status   | Evidence                       | Notes |
|----------|---------------------------------|----------------------------------------------------------------------------------|----------|--------------------------------|-------|
| ANLY-01  | Self-hosted Umami dashboard      | Dashboard reachable at https://analytics.montysinger.com, login works           | pending  | ANLY-01-umami.png              |       |
| ANLY-02  | Real-time visitor tracking       | Visit `/` in new incognito, dashboard pageview count increments within 30s      | pending  | ANLY-02-umami.png              |       |
| ANLY-03  | Traffic source breakdown         | Dashboard "Sources" tab shows direct/referrer/UTM split                         | pending  | ANLY-03-umami.png              |       |
| ANLY-04  | Page popularity (views per page)| Dashboard shows per-page view counts                                             | pending  | ANLY-04-umami.png              |       |
| ANLY-05  | Geo + device/browser breakdown  | Dashboard shows country + device tables                                          | pending  | ANLY-05-umami.png              |       |
| ANLY-06  | Geo map of visitor locations     | Dashboard "World map" or "Countries" visual                                     | pending  | ANLY-06-umami.png              |       |

## Design system

| ID       | Requirement                          | Expected on live                                                                       | Status   | Evidence                    | Notes |
|----------|--------------------------------------|----------------------------------------------------------------------------------------|----------|-----------------------------|-------|
| DSGN-01  | Dark/light mode, system + toggle, no FOUC | Incognito system=dark renders dark immediately (no flash). Manual toggle switches. Persists. **Cross-check: Plan 06-04 runs the deep FOUC test.** | pending | DSGN-01-desktop.png | |
| DSGN-02  | 375px mobile responsive              | DevTools 375px viewport on every page, no horizontal scroll, no overlapping elements   | pending  | DSGN-02-mobile-home.png, DSGN-02-mobile-blog.png | |
| DSGN-03  | Animated page transitions             | Nav between Home/Projects/Blog/About/Links shows fade+slide per 04-HUMAN-UAT #1         | pending  | DSGN-03-desktop.png          |       |
| DSGN-04  | Scroll reveal animations              | Sections animate in on scroll across home/projects/blog/about/links                    | pending  | DSGN-04-desktop.png          |       |
| DSGN-05  | Consistent design tokens (Tailwind v4)| Spacing/color/typography feel consistent across all pages (spot-check)                  | pending  | DSGN-05-desktop.png          |       |
| DSGN-06  | Custom domain to Vercel               | `https://montysinger.com` resolves to Vercel app with valid HTTPS. (Final Super-to-Vercel cutover is blocked per D-15, mark `partial`.)   | pending  | DSGN-06-curl.txt             | partial per D-15 |

## Social / SEO

| ID      | Requirement                                     | Expected on live                                                                                          | Status   | Evidence                       | Notes |
|---------|--------------------------------------------------|-----------------------------------------------------------------------------------------------------------|----------|--------------------------------|-------|
| SOC-01  | Social links in footer + /links page             | Footer visible on every page with GitHub, X, LinkedIn. `/links` page exists with same links.              | pending  | SOC-01-desktop.png              |       |
| SOC-02  | OG meta + dynamic OG images                      | `curl -sL https://montysinger.com/blog/{slug}` HTML includes `og:image` pointing to `/opengraph-image`. Twitter Card validator renders it. | pending | SOC-02-validator.png           |       |
| SOC-03  | Auto sitemap at /sitemap.xml                     | `curl https://montysinger.com/sitemap.xml` returns XML with homepage, about, prometheus, newsletter, uses, blog, projects, each post, each project | pending | SOC-03-sitemap.txt            |       |
| SOC-04  | robots.txt + Person JSON-LD                      | `curl https://montysinger.com/robots.txt` returns allow-all + sitemap ref. Homepage HTML contains Person schema matching D-13. Schema.org validator reports zero errors. | pending | SOC-04-robots.txt, SOC-04-validator.png | |
| SOC-05  | Newsletter CTA inline at end of blog posts       | At least one blog post shows newsletter/subscribe CTA near the end                                        | pending  | SOC-05-desktop.png              |       |

## Summary

total: 28
pass: 0
fail: 0
skipped: 0
blocked: 0
pending: 28

## Blocking issues (open FIX sub-plans per D-10)

(Populated as Task 2 encounters failures.)

## Non-blocking issues (routed to /gsd-add-todo per D-09/D-10)

(Populated as Task 2 encounters nits.)
```

Populate the commit-SHA line by copying from `.planning/phases/06-pre-launch-qa/evidence/lighthouse/SUMMARY.md` so both UAT and Lighthouse audit the same commit.
  </action>
  <verify>
    <automated>test -f .planning/phases/06-pre-launch-qa/06-UAT.md && for id in HOME-01 HOME-02 HOME-03 HOME-04 PORT-01 PORT-02 PORT-03 BLOG-01 BLOG-02 BLOG-03 BLOG-04 BLOG-05 ABOUT-01 ANLY-01 ANLY-02 ANLY-03 ANLY-04 ANLY-05 ANLY-06 DSGN-01 DSGN-02 DSGN-03 DSGN-04 DSGN-05 DSGN-06 SOC-01 SOC-02 SOC-03 SOC-04 SOC-05; do grep -qE "^\| $id\b" .planning/phases/06-pre-launch-qa/06-UAT.md || { echo "Missing $id"; exit 1; }; done</automated>
  </verify>
  <acceptance_criteria>
    - 06-UAT.md exists with standard frontmatter (status, phase, source, audit_url, commit_sha, started, updated)
    - All 28 v1 requirement IDs are present as table rows
    - Every row starts with `pending` status
    - Commit SHA is populated (not `<populate...>`) from 06-02 SUMMARY.md
    - Summary counter reads `pending: 28`
  </acceptance_criteria>
  <done>UAT skeleton ready. Task 2 walks the live site to fill it in.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Walk the live site, screenshot each requirement, flip each UAT row</name>
  <what-built>
    Task 1 produced an empty UAT skeleton with 28 rows. Task 2 fills it in.
  </what-built>
  <how-to-verify>
For each of the 28 requirement rows in `06-UAT.md`:

1. Open `https://montysinger.com` in a fresh incognito window (Chrome).
2. Navigate to the route(s) specified in that row's "Expected on live" column.
3. Confirm the behavior matches the expectation. For animation-heavy rows (HOME-03, HOME-04, PORT-02, DSGN-03, DSGN-04), cross-reference the exact behavior described in `04-HUMAN-UAT.md` — those expectations were validated for Phase 4 and still apply.
4. Capture screenshots:
   - Desktop (1440px wide viewport): save as `.planning/phases/06-pre-launch-qa/evidence/requirements/{ID}-desktop.png`.
   - Mobile (DevTools at 375px, iPhone SE profile): save as `.planning/phases/06-pre-launch-qa/evidence/requirements/{ID}-mobile.png`.
   - Per D-06, one desktop + one mobile per requirement is the minimum. Requirements that are inherently desktop-only (e.g., hover interactions) can skip mobile with a note.
5. Update the row in `06-UAT.md`:
   - `pass` — behavior matches expectation, screenshots captured.
   - `fail` — behavior does not match. Write a one-line summary in the Notes column, create `06-0X-FIX-{req-id}.md` skeleton, and add a row to "Blocking issues" list at bottom.
   - `skipped` — requirement intentionally out of scope for this run (rare; note reason).
   - `blocked` — external dependency prevents verification (e.g., DSGN-06 with its partial status per D-15 — the main site IS live at montysinger.com so mark `pass` for resolution + HTTPS but add a note pointing to the deferred Super cutover).
6. For the 6 Analytics requirements (ANLY-01..06), screenshot the Umami dashboard at https://analytics.montysinger.com showing the relevant panel for each, instead of the main site.
7. For animation rows, include a note that GSAP/Lenis visual quality was spot-checked; these cannot be proven from a still screenshot. If concerns arise, defer to Plan 06-04's Safari cross-browser check.

Special handling per D-09 (blocking vs non-blocking):

**Blocking failures** (stop launch): broken Notion images, `NOTION_TOKEN` leak (already cleared by 06-01), dark-mode FOUC (re-checked by 06-04), Safari page-transition breakage (re-checked by 06-04), any v1 requirement failing. Each blocking failure = one `06-0X-FIX-<req>.md` sub-plan.

**Non-blocking** (record for post-launch): cosmetic nits, minor copy polish, non-critical DX warnings. Route via `/gsd-add-todo` after Task 2 completes; list in the UAT's "Non-blocking issues" section.

After all 28 rows are resolved:

```bash
# Regenerate the Summary counters
PASS=$(grep -cE '\| pass ' .planning/phases/06-pre-launch-qa/06-UAT.md)
FAIL=$(grep -cE '\| fail ' .planning/phases/06-pre-launch-qa/06-UAT.md)
SKIP=$(grep -cE '\| skipped ' .planning/phases/06-pre-launch-qa/06-UAT.md)
BLOCK=$(grep -cE '\| blocked ' .planning/phases/06-pre-launch-qa/06-UAT.md)
PEND=$(grep -cE '\| pending ' .planning/phases/06-pre-launch-qa/06-UAT.md)
echo "pass=$PASS fail=$FAIL skipped=$SKIP blocked=$BLOCK pending=$PEND"
```

Update the `## Summary` counters and the frontmatter `status:` field to `complete` (all pass), `blocked` (any blocking fails), or `partial` (only non-blocking items outstanding).
  </how-to-verify>
  <resume-signal>Type "approved" when every one of the 28 rows has a final status (no `pending`), every screenshot exists, the Summary counters match reality, and any FIX sub-plans have been created. Describe any blocking failures encountered.</resume-signal>
</task>

</tasks>

<verification>

Checklist for the human QA operator:

- [ ] 06-UAT.md frontmatter has `status`, `commit_sha`, and `audit_url` populated.
- [ ] Every one of the 28 v1 requirement IDs has a row with status != `pending`.
- [ ] Every `pass`/`fail`/`blocked` row references at least one screenshot filename that exists under `evidence/requirements/`.
- [ ] Every `fail` row has a corresponding `06-0X-FIX-<id>.md` file AND is listed in "Blocking issues".
- [ ] Every `skipped` or `blocked` row has a reason in Notes.
- [ ] Summary counters match the actual row distribution.
- [ ] The UAT commit SHA matches the SHA in 06-02 Lighthouse SUMMARY.md (prevents cross-audit drift).

</verification>

<rollback>
No code changes to roll back. If the UAT identifies a regression severe enough to warrant re-deploying a prior production build: `vercel rollback` to the last green deployment, then re-run 06-02 and 06-03 against the rolled-back commit.
</rollback>
