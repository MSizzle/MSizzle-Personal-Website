---
phase: 08-motion-subtractions
plan: 02
type: execute
wave: 1
depends_on:
  - 08-01
files_modified:
  - src/app/page.tsx
  - src/components/home/rotating-tagline.tsx
autonomous: true
requirements:
  - MOTION-02
must_haves:
  truths:
    - "Honors CONTEXT.md decisions: D-01, D-02, D-10, D-12, D-13"
    - "Loading `/` in production shows no rotating tagline below the Hero intro paragraph"
    - "No `setInterval(3500)` timer for tagline rotation exists in the bundle"
    - "`vercel build --prod` exits 0 after MOTION-02"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Homepage Hero without RotatingTagline import or JSX block"
      contains: "export default async function Home"
  key_links:
    - from: "src/app/page.tsx"
      to: "(none — RotatingTagline call site removed)"
      via: "deletion"
      pattern: "RotatingTagline"
---

<objective>
Delete the rotating tagline (MOTION-02). Remove the component file `src/components/home/rotating-tagline.tsx`, its import in `src/app/page.tsx`, and the `<div className="mt-4"><RotatingTagline /></div>` block at lines 76–78 (post-Plan-01 line numbers shift; identifier-anchored edits keep this stable).

Purpose: The 3.5s `setInterval`-driven rotating tagline violates the v2.0 motion budget (zero looping animations). Phase 10 replaces this slot with a static letter-style intro paragraph (HOME-V2-05).

Output: `src/components/home/rotating-tagline.tsx` deleted; `src/app/page.tsx` import line + `<RotatingTagline />` JSX block removed (including wrapping `<div className="mt-4">`). `npm run build` exits 0.

Depends on Plan-01 (same file: `src/app/page.tsx`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/08-motion-subtractions/08-CONTEXT.md
@.planning/phases/08-motion-subtractions/08-RESEARCH.md
@.planning/phases/08-motion-subtractions/08-VALIDATION.md
@.planning/phases/08-motion-subtractions/08-01-SUMMARY.md

<interfaces>
<!-- Anchor-based edits (line numbers shift after Plan-01; the identifiers below remain stable). -->

Pre-edit anchors (from src/app/page.tsx as of phase start — verified 2026-05-20):
```typescript
import { RotatingTagline } from "@/components/home/rotating-tagline";  // import line — DELETE
```

JSX block to remove (Hero section, post-intro paragraph):
```jsx
<div className="mt-4">
  <RotatingTagline />
</div>
```

The surrounding Hero section structure that remains:
```jsx
<section className="px-6 pt-6 pb-20 md:px-24">
  <div className="mx-auto max-w-[66ch]">
    <h1>Monty Singer</h1>
    <p className="mt-6 text-lg leading-relaxed opacity-80">I'm Monty Singer, founder of Prometheus...</p>
    {/* <div className="mt-4"><RotatingTagline /></div> — DELETE this block */}
    <div className="mt-8 flex items-center gap-6">{/* Prometheus / More About Me / Get in Touch links — KEEP */}</div>
  </div>
</section>
```

Verified via `rg "RotatingTagline|rotating-tagline"`: 2 hits in `src/app/page.tsx` (import + JSX block), plus the component file. No tests reference it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete RotatingTagline component file and homepage call site</name>
  <files>src/app/page.tsx, src/components/home/rotating-tagline.tsx</files>
  <read_first>
    - src/app/page.tsx (current state — post-Plan-01)
    - src/components/home/rotating-tagline.tsx (component being deleted)
  </read_first>
  <action>
    1. Delete file `src/components/home/rotating-tagline.tsx` with `rm` (Bash).
    2. Edit `src/app/page.tsx`:
       - Remove the import line `import { RotatingTagline } from "@/components/home/rotating-tagline";` (search by identifier — line number depends on Plan-01's state).
       - Remove the entire JSX block:
         ```
         <div className="mt-4">
           <RotatingTagline />
         </div>
         ```
         This block sits between the Hero `<p>` (the "I'm Monty Singer, founder of Prometheus..." paragraph ending with "interesting.") and the `<div className="mt-8 flex items-center gap-6">` (the Prometheus/More About Me/Get in Touch nav links). Use the Edit tool with full surrounding context to avoid ambiguity.
    3. Do NOT touch the surrounding Hero section: the `<h1>Monty Singer</h1>`, the intro `<p>`, and the `mt-8` action-link `<div>` all stay.
    4. Do NOT touch any other import (WritingsCarousel — Plan-04, WorksCarousel — Plan-03), JSX block, or the event-cards block.
    5. Per CONTEXT.md D-12: do NOT touch `src/components/animations/scroll-reveal.tsx`, `src/components/providers/lenis-provider.tsx`, or `src/app/template.tsx`.
  </action>
  <verify>
    <automated>cd "/Users/Montster/MSizzle Personal Website" && rg "RotatingTagline|rotating-tagline" -g '!.claude/**' -g '!node_modules/**' -g '!.next/**' -g '!.planning/**' . | (! grep -q .) && (! ls src/components/home/rotating-tagline.tsx 2>/dev/null) && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `rg "RotatingTagline|rotating-tagline" -g '!.claude/**' -g '!node_modules/**' -g '!.next/**' -g '!.planning/**'` returns 0 hits in source
    - `ls src/components/home/rotating-tagline.tsx` returns "No such file" (exit 1)
    - The Hero section's `<h1>Monty Singer</h1>` and the action-link `<div className="mt-8 flex items-center gap-6">` block both remain present in `src/app/page.tsx`
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>RotatingTagline deleted from disk; src/app/page.tsx no longer imports or renders RotatingTagline; Hero structure otherwise unchanged; npm run build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Pure deletion. The deleted `RotatingTagline` was a client component running `setInterval(3500)` over a hardcoded string array — no input, no auth, no network. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02 | Tampering | `src/app/page.tsx` Hero section | mitigate | `npm run build` (D-10) catches any structural regression (e.g., a stray closing `</div>` left behind). |

ASVS V1–V13: N/A. V14 Configuration: production build covers regression risk.
</threat_model>

<verification>
- `npm run build` exits 0
- Grep absence: `RotatingTagline|rotating-tagline` zero hits in source
- File deletion: `src/components/home/rotating-tagline.tsx` does not exist
- Hero structure intact: `<h1>`, intro `<p>`, and `<div className="mt-8 flex items-center gap-6">` still present
</verification>

<success_criteria>
- MOTION-02 requirement satisfied: rotating tagline and all references removed
- Validation map task 8-02-V passes
- Hero section renders cleanly: h1 → intro paragraph → action links (no rotating sub-tagline between them)
</success_criteria>

<output>
Create `.planning/phases/08-motion-subtractions/08-02-SUMMARY.md` when done. Reference task ID `8-02-V` from the validation map.
</output>

---

## Decisions Honored

This plan implements CONTEXT.md decisions: D-01, D-02, D-10, D-12, D-13.

See `08-CONTEXT.md` `<decisions>` block for the full text of each.
