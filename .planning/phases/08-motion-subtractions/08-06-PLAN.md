---
phase: 08-motion-subtractions
plan: 06
type: execute
wave: 1
depends_on:
  - 08-05
files_modified:
  - src/app/page.tsx
  - src/app/events/page.tsx
  - src/components/events/event-cards.tsx
autonomous: true
requirements:
  - MOTION-06
must_haves:
  truths:
    - "Honors CONTEXT.md decisions: D-01, D-03, D-04, D-05, D-10, D-12, D-13"
    - "No accumulator-style `delay={N + i * M}` pattern exists anywhere in `src/`"
    - "`UpcomingMini` and `PastEventCard` accept only `event` as a prop — `delay` is removed from both signatures"
    - "Event lists on `/` and `/events` fade in together (or per-item without cascade)"
    - "`vercel build --prod` exits 0 after MOTION-06 (TS catches any stale `delay=` prop usage)"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Homepage event lists with no cascade-delay props"
      contains: "UpcomingMini"
    - path: "src/app/events/page.tsx"
      provides: "/events page with no cascade-delay props"
      contains: "UpcomingMini"
    - path: "src/components/events/event-cards.tsx"
      provides: "UpcomingMini and PastEventCard signatures without `delay` prop"
      contains: "function UpcomingMini"
  key_links:
    - from: "src/app/page.tsx + src/app/events/page.tsx"
      to: "UpcomingMini / PastEventCard"
      via: "JSX without delay prop"
      pattern: "UpcomingMini[^>]*\\}"
---

<objective>
Flatten cascading ScrollReveal delays (MOTION-06). Per CONTEXT.md D-04: drop the `delay` prop entirely from every call site of `UpcomingMini` and `PastEventCard` AND from each component's prop signature.

Per RESEARCH.md F1 + Pitfall 1: there are EXACTLY four accumulator call sites site-wide — `src/app/page.tsx:175,192` and `src/app/events/page.tsx:66,85`. RESEARCH.md verified no other `i * 0.N` patterns on `/blog`, `/projects`, or `/links`.

Per RESEARCH.md F1 + Pitfall 2: `ScrollReveal` is currently a no-op `<div>` wrapper — the `delay` prop is type-accepted but never used. This is a TypeScript signature + JSX cleanup, not a runtime behavior change. D-12 forbids touching `src/components/animations/scroll-reveal.tsx`.

Per RESEARCH.md Open Question 2 + Recommendation: keep the `<ScrollReveal>` wrappers around the inner JSX inside `UpcomingMini` / `PastEventCard`, but drop the `delay={delay}` argument (or pass nothing — `ScrollReveal`'s `delay` prop is optional).

For `FeaturedUpcoming`: it has an optional `delay?: number` defaulting to `0.15`. Drop the `delay` prop from its signature and from the `<ScrollReveal delay={delay}>` inside its body. Keep the `priority` prop intact.

Output: 4 JSX `delay={...}` props removed, 3 prop signatures cleaned (`UpcomingMini`, `PastEventCard`, `FeaturedUpcoming`), 3 internal `<ScrollReveal delay={delay}>` arguments dropped. `npm run build` exits 0 (TypeScript verifies no stale `delay=` consumer remains).

Depends on Plan-05 (same file: `src/components/events/event-cards.tsx`) and indirectly on Plans 01–04 (same file: `src/app/page.tsx`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/08-motion-subtractions/08-CONTEXT.md
@.planning/phases/08-motion-subtractions/08-RESEARCH.md
@.planning/phases/08-motion-subtractions/08-VALIDATION.md
@.planning/phases/08-motion-subtractions/08-05-SUMMARY.md

<interfaces>
<!-- All four call sites + three component signatures. Verified 2026-05-20. -->

CALL SITE 1 — src/app/page.tsx (Also Coming Up section):
```jsx
// BEFORE
<UpcomingMini
  key={event.id}
  event={event}
  delay={0.2 + i * 0.05}
/>
// AFTER
<UpcomingMini
  key={event.id}
  event={event}
/>
```

CALL SITE 2 — src/app/page.tsx (Past section):
```jsx
// BEFORE
<PastEventCard
  key={event.id}
  event={event}
  delay={0.25 + i * 0.03}
/>
// AFTER
<PastEventCard
  key={event.id}
  event={event}
/>
```

CALL SITE 3 — src/app/events/page.tsx (Also Coming Up):
```jsx
// BEFORE
<UpcomingMini
  key={event.id}
  event={event}
  delay={0.25 + i * 0.05}
/>
// AFTER
<UpcomingMini
  key={event.id}
  event={event}
/>
```

CALL SITE 4 — src/app/events/page.tsx (Past):
```jsx
// BEFORE
<PastEventCard
  key={event.id}
  event={event}
  delay={0.35 + i * 0.03}
/>
// AFTER
<PastEventCard
  key={event.id}
  event={event}
/>
```

COMPONENT SIGNATURE 1 — src/components/events/event-cards.tsx UpcomingMini:
```typescript
// BEFORE
export function UpcomingMini({
  event,
  delay,
}: {
  event: EventItem;
  delay: number;
}) { ... }

// AFTER
export function UpcomingMini({
  event,
}: {
  event: EventItem;
}) { ... }
```
And inside the body: `<ScrollReveal delay={delay}>` becomes `<ScrollReveal>` (`delay` is optional on `ScrollReveal`).

COMPONENT SIGNATURE 2 — src/components/events/event-cards.tsx PastEventCard:
```typescript
// BEFORE
export function PastEventCard({
  event,
  delay,
}: {
  event: EventItem;
  delay: number;
}) { ... }

// AFTER
export function PastEventCard({
  event,
}: {
  event: EventItem;
}) { ... }
```
Inside body: `<ScrollReveal delay={delay}>` becomes `<ScrollReveal>`.

COMPONENT SIGNATURE 3 — src/components/events/event-cards.tsx FeaturedUpcoming:
```typescript
// BEFORE
export function FeaturedUpcoming({
  event,
  delay = 0.15,
  priority = false,
}: {
  event: EventItem;
  delay?: number;
  priority?: boolean;
}) { ... }

// AFTER
export function FeaturedUpcoming({
  event,
  priority = false,
}: {
  event: EventItem;
  priority?: boolean;
}) { ... }
```
Inside body: `<ScrollReveal delay={delay}>` becomes `<ScrollReveal>`.

Other `ScrollReveal` call sites in `src/app/events/page.tsx` (lines 40, 47, 56, 75) pass fixed `delay={0}`, `delay={0.15}`, `delay={0.2}`, `delay={0.3}` — NOT accumulator-style. RESEARCH.md scopes Plan-06 strictly to the four `i *` accumulator sites + the three event-card signatures. The fixed-delay `ScrollReveal` wrappers stay as-is (they're not cascade-by-index).

D-12: do NOT touch `src/components/animations/scroll-reveal.tsx` itself. The component file is a 9-line no-op `<div>` wrapper; leave it.

D-05 contract: post-edit, `rg "delay=\{?\d?\.\d+\s*\+\s*i\s*\*"` returns 0 hits anywhere in src/.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Drop delay props from 4 call sites and clean 3 component signatures</name>
  <files>src/app/page.tsx, src/app/events/page.tsx, src/components/events/event-cards.tsx</files>
  <read_first>
    - src/app/page.tsx (post-Plan-04 current state; identify the two `delay={0.2 + i * 0.05}` and `delay={0.25 + i * 0.03}` patterns inside the Events section)
    - src/app/events/page.tsx (current state; identify the two `delay={0.25 + i * 0.05}` and `delay={0.35 + i * 0.03}` patterns)
    - src/components/events/event-cards.tsx (post-Plan-05 current state)
    - src/components/animations/scroll-reveal.tsx (read once to confirm `delay` is optional on its prop interface — DO NOT EDIT)
    - .planning/phases/08-motion-subtractions/08-RESEARCH.md §"Pitfall 1: Missing the second call site" and §"Pitfall 2: ScrollReveal no-op confusion" and §"Pitfall 3: Assuming /blog, /projects, /links cascade"
  </read_first>
  <action>
    1. Edit `src/app/page.tsx`:
       - Find the `<UpcomingMini ... delay={0.2 + i * 0.05} />` JSX inside the "Also Coming Up" block. Remove the `delay={0.2 + i * 0.05}` line (the prop on its own line). The result: `<UpcomingMini key={event.id} event={event} />`.
       - Find the `<PastEventCard ... delay={0.25 + i * 0.03} />` JSX inside the "Past" block. Remove the `delay={0.25 + i * 0.03}` line. Result: `<PastEventCard key={event.id} event={event} />`.
    2. Edit `src/app/events/page.tsx`:
       - Find `<UpcomingMini ... delay={0.25 + i * 0.05} />` inside the "Also Coming Up" block. Remove the `delay={0.25 + i * 0.05}` line. Result: `<UpcomingMini key={event.id} event={event} />`.
       - Find `<PastEventCard ... delay={0.35 + i * 0.03} />` inside the "Past" block. Remove the `delay={0.35 + i * 0.03}` line. Result: `<PastEventCard key={event.id} event={event} />`.
       - Do NOT touch the fixed-delay `ScrollReveal` wrappers (`<ScrollReveal delay={0}>`, `delay={0.15}`, `delay={0.2}`, `delay={0.3}`) — those are NOT accumulator patterns and are out of scope per RESEARCH.md Pitfall 3.
    3. Edit `src/components/events/event-cards.tsx`:
       - In `UpcomingMini`: change the destructured props from `{ event, delay }` to `{ event }` and the type from `{ event: EventItem; delay: number; }` to `{ event: EventItem; }`. Inside the function body, change `<ScrollReveal delay={delay}>` to `<ScrollReveal>`.
       - In `PastEventCard`: same pattern — remove `delay` from destructure and from type, change `<ScrollReveal delay={delay}>` to `<ScrollReveal>`.
       - In `FeaturedUpcoming`: remove `delay = 0.15,` from the destructured props and `delay?: number;` from the type. KEEP `priority = false,` and `priority?: boolean;`. Inside the function body, change `<ScrollReveal delay={delay}>` to `<ScrollReveal>`.
       - Do NOT touch the `formatLongDate`, `formatShortDate` helpers or the imports (`Image`, `EventItem`, `ScrollReveal`).
    4. Verify no other consumers of the dropped `delay` prop exist:
       - `rg "<UpcomingMini[^/>]*delay" -g '!.planning/**'` returns 0 hits
       - `rg "<PastEventCard[^/>]*delay" -g '!.planning/**'` returns 0 hits
       - `rg "<FeaturedUpcoming[^/>]*delay" -g '!.planning/**'` returns 0 hits
    5. Run final accumulator-pattern sweep: `rg "delay=\{?\d?\.\d+\s*\+\s*i\s*\*" -g '!.claude/**' -g '!node_modules/**' -g '!.next/**' -g '!.planning/**' src/` — must return 0 hits.
    6. Per CONTEXT.md D-12: do NOT touch `src/components/animations/scroll-reveal.tsx`. The `delay?` prop in its interface stays; we're stopping the *consumers* from passing it.
    7. Do NOT touch `/blog`, `/projects`, `/links` route files — RESEARCH.md Pitfall 3 confirms they don't cascade.
  </action>
  <verify>
    <automated>cd "/Users/Montster/MSizzle Personal Website" && rg "delay=\\{?\\d?\\.\\d+\\s*\\+\\s*i\\s*\\*" -g '!.claude/**' -g '!node_modules/**' -g '!.next/**' -g '!.planning/**' src/ | (! grep -q .) && rg "<UpcomingMini[^/>]*delay|<PastEventCard[^/>]*delay|<FeaturedUpcoming[^/>]*delay" -g '!.planning/**' src/ | (! grep -q .) && rg -q 'function UpcomingMini' src/components/events/event-cards.tsx && rg -q 'function PastEventCard' src/components/events/event-cards.tsx && rg -q 'function FeaturedUpcoming' src/components/events/event-cards.tsx && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `rg "delay=\\{?\\d?\\.\\d+\\s*\\+\\s*i\\s*\\*" src/` returns 0 hits (D-05 contract)
    - `rg "<UpcomingMini[^/>]*delay" src/` returns 0 hits
    - `rg "<PastEventCard[^/>]*delay" src/` returns 0 hits
    - `rg "<FeaturedUpcoming[^/>]*delay" src/` returns 0 hits
    - `src/components/events/event-cards.tsx` still exports `UpcomingMini`, `PastEventCard`, `FeaturedUpcoming` (verified by function-name grep)
    - `src/components/animations/scroll-reveal.tsx` is byte-identical to pre-plan state (D-12)
    - `src/app/blog/page.tsx`, `src/app/projects/page.tsx`, `src/app/links/page.tsx` are byte-identical to pre-plan state (RESEARCH.md Pitfall 3)
    - `npm run build` exits 0 (TS would catch any leftover `<UpcomingMini ... delay={...} />` because the prop no longer exists in the type)
  </acceptance_criteria>
  <done>4 accumulator-style delay props removed from JSX (2 in src/app/page.tsx, 2 in src/app/events/page.tsx); 3 component prop signatures cleaned (UpcomingMini, PastEventCard, FeaturedUpcoming) with internal `<ScrollReveal delay={delay}>` → `<ScrollReveal>`; ScrollReveal component itself untouched; `/blog`, `/projects`, `/links` untouched; npm run build green with no TS errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | TypeScript signature + JSX prop deletion. No input, no auth, no network, no state. The `delay` prop carried a number used only inside a no-op wrapper. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-06 | Tampering | `event-cards.tsx` exported function signatures | mitigate | TypeScript itself is the contract verifier — any leftover `<UpcomingMini ... delay={...} />` consumer would fail `npm run build` with a "Property 'delay' does not exist on type" error. D-10 build gate enforces. |

ASVS V1–V13: N/A. V14 Configuration: production build catches stale consumers.
</threat_model>

<verification>
- `npm run build` exits 0 (TypeScript verifies no orphaned `delay=` JSX prop usage)
- Grep absence: `delay=\{?\d?\.\d+\s*\+\s*i\s*\*` zero hits in src/
- Grep absence: `<UpcomingMini[^/>]*delay`, `<PastEventCard[^/>]*delay`, `<FeaturedUpcoming[^/>]*delay` all zero hits
- `src/components/animations/scroll-reveal.tsx` unchanged (D-12 enforcement)
- Component exports preserved: `function UpcomingMini`, `function PastEventCard`, `function FeaturedUpcoming` all still found
</verification>

<success_criteria>
- MOTION-06 requirement satisfied: cascading ScrollReveal delays flattened on both `/` and `/events`; component prop signatures cleaned
- Validation map task 8-06-V passes
- Event lists reveal together (same-time fade via the no-op ScrollReveal wrapper) — no visible cascade-by-index
- TypeScript guarantees no stale `delay=` consumer survived the deletion
</success_criteria>

<output>
Create `.planning/phases/08-motion-subtractions/08-06-SUMMARY.md` when done. Reference task ID `8-06-V` from the validation map. Note: this plan completes ALL motion-deletion work; Plan-07 verifies preservation.
</output>

---

## Decisions Honored

This plan implements CONTEXT.md decisions: D-01, D-03, D-04, D-05, D-10, D-12, D-13.

See `08-CONTEXT.md` `<decisions>` block for the full text of each.
