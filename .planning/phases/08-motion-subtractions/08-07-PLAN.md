---
phase: 08-motion-subtractions
plan: 07
type: execute
wave: 2
depends_on:
  - 08-01
  - 08-02
  - 08-03
  - 08-04
  - 08-05
  - 08-06
files_modified: []
autonomous: false
requirements:
  - MOTION-08
must_haves:
  truths:
    - "Honors CONTEXT.md decisions: D-01, D-11, D-12, D-13"
    - "`src/components/providers/lenis-provider.tsx` is byte-identical to pre-phase HEAD"
    - "`src/app/template.tsx` is byte-identical to pre-phase HEAD"
    - "`src/components/animations/scroll-reveal.tsx` is byte-identical to pre-phase HEAD"
    - "All three preservation-target Vitest tests pass: template, scroll-reveal, lenis-provider"
    - "`vercel build --prod` exits 0 (phase gate per D-11)"
    - "Manual smoke test on `npm run dev /`: Lenis still smooth-scrolls and the 200-300ms page-load fade still fires on internal navigation"
  artifacts:
    - path: ".planning/phases/08-motion-subtractions/08-07-SUMMARY.md"
      provides: "MOTION-08 preservation verification log"
      contains: "MOTION-08"
  key_links:
    - from: "Plans 01-06 (deletion plans)"
      to: "Preserved motion primitives (Lenis, template fade, ScrollReveal)"
      via: "git diff verification"
      pattern: "lenis-provider|template|scroll-reveal"
---

<objective>
Verify MOTION-08 — Lenis smooth scroll and the 200–300ms page-load fade survived Phase 8 unchanged. This is a verification-only plan with no file modifications.

Per CONTEXT.md D-11: phase verification runs `vercel build --prod` as the production-readiness gate (matches Phase 13's gate and v1.0 retrospective lesson #2 — production-build-as-truth, not local dev/lint).

Per CONTEXT.md D-12: the three preserved files must be byte-identical to their pre-phase HEAD state. The phase started from the `main` branch (per STATE.md), so `git diff main -- <files>` is the canonical preservation check.

Per RESEARCH.md F-Wave: Plan-07 runs in Wave 2 AFTER all deletion plans (01-06) complete. The preservation guards depend on knowing nothing in plans 01-06 mutated the target files.

This plan has one `checkpoint:human-verify` task at the end — the production `npm run dev` smoke test cannot be automated (executor cannot view a rendered browser).

Output: `08-07-SUMMARY.md` with git-diff confirmation, Vitest pass log, `vercel build --prod` exit-0 evidence, and a human-confirmed smoke test verdict. Phase 8 ready for `/gsd:verify-work`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/08-motion-subtractions/08-CONTEXT.md
@.planning/phases/08-motion-subtractions/08-RESEARCH.md
@.planning/phases/08-motion-subtractions/08-VALIDATION.md
@.planning/phases/08-motion-subtractions/08-06-SUMMARY.md
@.planning/RETROSPECTIVE.md

<interfaces>
<!-- Preserved-files contract per CONTEXT.md D-12 -->

Files that MUST be byte-identical to pre-phase HEAD (i.e., to `main`):
1. `src/components/providers/lenis-provider.tsx` — Lenis smooth-scroll wiring with GSAP ticker. Mounted between `ThemeProvider` and `MotionProvider` in `src/app/layout.tsx`.
2. `src/app/template.tsx` — App Router template re-mounts on every route change. Hosts the `<m.div>` with `AnimatePresence mode="wait"`, 300ms `easeOut` opacity 0→1 + y:20→0 enter / 10px-y exit, with reduced-motion fallback to 150ms opacity-only.
3. `src/components/animations/scroll-reveal.tsx` — currently a 9-line no-op `<div>` wrapper that the v2.0 design contract preserves (Phase 9/10 may swap for a real impl). Plan-06 removed `delay=` *consumers*, not the component.

Tests that MUST pass:
- `src/__tests__/animations/template.test.tsx` — covers MOTION-08 page-fade preservation
- `src/__tests__/animations/scroll-reveal.test.tsx` — covers `ScrollReveal` (no-op `<div>` wrapper)
- `src/__tests__/providers/lenis-provider.test.tsx` — covers MOTION-08 Lenis preservation

The canonical Vitest invocation per RESEARCH.md (no `test` script in package.json):
```bash
npx vitest run src/__tests__/animations/template.test.tsx src/__tests__/animations/scroll-reveal.test.tsx src/__tests__/providers/lenis-provider.test.tsx
```

Phase gate command (CONTEXT.md D-11):
```bash
vercel build --prod
```

The `vercel` binary is available at `/opt/homebrew/bin/vercel` v54.2.0 per RESEARCH.md §"Environment Availability".

Manual smoke check (CONTEXT.md D-12 / RESEARCH.md Open Question 3):
- `npm run dev`
- In Chrome, open `http://localhost:3000/`
- Confirm Lenis-smoothed scroll feels intact (try mouse wheel + trackpad gesture)
- Navigate to `/about` then back to `/` and confirm the 200–300ms page-load fade fires
- Confirm no console errors related to motion, Lenis, or providers
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify three preserved files have byte-identical state to main, run preservation tests, and run production build gate</name>
  <files></files>
  <read_first>
    - .planning/phases/08-motion-subtractions/08-CONTEXT.md §"Preservation Guarantees (MOTION-08)" (D-12)
    - .planning/phases/08-motion-subtractions/08-VALIDATION.md (task 8-07-V row)
    - .planning/RETROSPECTIVE.md §"What Worked" — `vercel build --prod` gate rationale
  </read_first>
  <action>
    1. Run `git diff main -- src/components/providers/lenis-provider.tsx src/app/template.tsx src/components/animations/scroll-reveal.tsx` and capture the output.
       - Expected: empty diff (no changes to any of the three files). If diff is non-empty, this plan FAILS — record the divergence in the SUMMARY and stop. A non-empty diff means one of Plans 01–06 touched a preserved file, which is a D-12 violation and must be reverted before this plan can complete.
    2. Run `npx vitest run src/__tests__/animations/template.test.tsx src/__tests__/animations/scroll-reveal.test.tsx src/__tests__/providers/lenis-provider.test.tsx` and capture exit code + test output.
       - Expected: all three test files report passing (no failures, no errors).
    3. Run `vercel build --prod` from project root and capture exit code + the final summary lines.
       - Expected: exit code 0. Any non-zero exit means the phase gate failed (D-11).
    4. Record all three results in `.planning/phases/08-motion-subtractions/08-07-SUMMARY.md` under explicit headers:
       - `## Preserved Files (git diff)` — paste the empty diff confirmation
       - `## Preservation Tests (vitest)` — paste the pass count
       - `## Phase Gate (vercel build --prod)` — paste the build success line + exit code
    5. Do NOT modify any source file. This task is verification only.
    6. If ANY of the three checks fails, do NOT proceed to Task 2 (the human-verify checkpoint). Instead, file the failure in the SUMMARY and return control to the orchestrator.
  </action>
  <verify>
    <automated>cd "/Users/Montster/MSizzle Personal Website" && git diff main -- src/components/providers/lenis-provider.tsx src/app/template.tsx src/components/animations/scroll-reveal.tsx | (! grep -q .) && npx vitest run src/__tests__/animations/template.test.tsx src/__tests__/animations/scroll-reveal.test.tsx src/__tests__/providers/lenis-provider.test.tsx && vercel build --prod</automated>
  </verify>
  <acceptance_criteria>
    - `git diff main -- src/components/providers/lenis-provider.tsx src/app/template.tsx src/components/animations/scroll-reveal.tsx` produces NO output (empty diff)
    - `npx vitest run` over the three target test files exits 0 with all tests passing
    - `vercel build --prod` exits 0
    - `.planning/phases/08-motion-subtractions/08-07-SUMMARY.md` exists with three result-recording sections populated
  </acceptance_criteria>
  <done>All three preserved files unchanged; preservation Vitest suite green; `vercel build --prod` green; SUMMARY records all three results with timestamps.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human smoke test — Lenis + page-load fade on production dev server</name>
  <what-built>
    Phase 8 deletion plans 01–06 plus the automated preservation checks in Task 1 (git diff + Vitest + `vercel build --prod`). The only remaining MOTION-08 evidence is a perceptual check that Lenis still smooths scrolling and the 200–300ms route-change fade still fires — neither of which an automated check can observe.
  </what-built>
  <how-to-verify>
    1. In a fresh terminal at the project root: `npm run dev`
    2. Wait for Next.js to report "Ready" at `http://localhost:3000`
    3. Open Chrome (or Chromium) in a normal (not incognito) window. Disable any reduced-motion OS setting for this test.
    4. Visit `http://localhost:3000/`
    5. Use the mouse wheel and the trackpad two-finger gesture to scroll the homepage top → bottom and back. Confirm scrolling feels smoothed (Lenis interpolated) — not the OS default "snappy" wheel scroll.
    6. Click the `More About Me` link in the Hero section (navigates to `/about`). Confirm that during the route change, the new page fades in over ~200–300ms with a slight upward translate. Confirm there is NO visible carousel, NO rotating tagline, NO pulsing dot anywhere.
    7. Click the browser back button to return to `/`. Confirm the fade fires again on `/`.
    8. Open Chrome DevTools console. Confirm no red errors mentioning `lenis`, `motion`, `template`, `provider`, or `framer`.
    9. (Optional but recommended) Repeat steps 4–7 at a 390px viewport via DevTools device toolbar to confirm mobile-pass parity.
    10. Stop the dev server (Ctrl+C).

    Then update `.planning/phases/08-motion-subtractions/08-07-SUMMARY.md` with:
    - `## Manual Smoke (human-verify)` section
    - PASS / FAIL verdict
    - Any observations (e.g., "smooth scroll confirmed", "fade fires on /about → / both directions", "no console errors")
  </how-to-verify>
  <resume-signal>
    Type `approved` if all 9 steps pass and the SUMMARY is updated with the PASS verdict.
    Type `failed: <description>` if any step regresses (e.g., scroll feels native instead of smoothed, fade doesn't fire, console error appears). On failure, the orchestrator will treat MOTION-08 as a gap and trigger gap-closure planning.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build pipeline → production artifacts | `vercel build --prod` is the production-readiness gate per v1.0 retrospective lesson #2. It catches env-var mismatches and chunk-split regressions that local `next build` misses. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-07 | Tampering | Preserved-file integrity | mitigate | `git diff main` byte-equality check (Task 1) is the durable preservation contract. Any unintended edit to Lenis / template / ScrollReveal flags here. |
| T-08-07b | Tampering | Production build artifacts | mitigate | `vercel build --prod` exits 0 (D-11) confirms no build regression introduced across Phase 8. |

ASVS V1–V13: N/A — verification-only plan. V14 Configuration: `vercel build --prod` is the explicit V14 control per CONTEXT.md D-11 and v1.0 retrospective.
</threat_model>

<verification>
- `git diff main -- <three preservation files>` empty
- `npx vitest run <three test files>` exits 0
- `vercel build --prod` exits 0
- Human smoke test PASS with SUMMARY notation
- No console errors related to motion, Lenis, or providers in the manual run
</verification>

<success_criteria>
- MOTION-08 requirement satisfied: Lenis smooth scroll preserved, template page-load fade preserved, ScrollReveal component preserved
- Validation map task 8-07-V passes (file-hash + unit tests + production build + human smoke)
- Phase gate (vercel build --prod) green — phase ready for `/gsd:verify-work` and SUMMARY rollup
</success_criteria>

<output>
Create `.planning/phases/08-motion-subtractions/08-07-SUMMARY.md` with four sections:
1. `## Preserved Files (git diff)` — empty-diff confirmation
2. `## Preservation Tests (vitest)` — pass log
3. `## Phase Gate (vercel build --prod)` — exit-0 confirmation
4. `## Manual Smoke (human-verify)` — human PASS verdict + observations

Reference task ID `8-07-V` from the validation map. Note in the SUMMARY: this is the phase-closing artifact for Phase 8; Plans 01–06 ship code changes, Plan-07 confirms what survived.
</output>

---

## Decisions Honored

This plan implements CONTEXT.md decisions: D-01, D-11, D-12, D-13.

See `08-CONTEXT.md` `<decisions>` block for the full text of each.
