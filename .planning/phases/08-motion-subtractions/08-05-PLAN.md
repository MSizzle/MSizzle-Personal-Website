---
phase: 08-motion-subtractions
plan: 05
type: execute
wave: 1
depends_on:
  - 08-04
files_modified:
  - src/components/events/event-cards.tsx
autonomous: true
requirements:
  - MOTION-05
must_haves:
  truths:
    - "Honors CONTEXT.md decisions: D-01, D-03, D-06, D-07, D-08, D-10, D-12, D-13"
    - "The featured event card on `/` and `/events` renders with a static red dot — no pulsing ring"
    - "No `animate-ping`, `animate-pulse`, `animate-bounce`, or `animate-spin` utility class exists anywhere in `src/`"
    - "`vercel build --prod` exits 0 after MOTION-05"
  artifacts:
    - path: "src/components/events/event-cards.tsx"
      provides: "FeaturedUpcoming with static-dot Upcoming label (no pulsing ring)"
      contains: "function FeaturedUpcoming"
  key_links:
    - from: "src/components/events/event-cards.tsx (FeaturedUpcoming)"
      to: "(none — pulsing animation removed)"
      via: "static dot only"
      pattern: "bg-red-500"
---

<objective>
Remove the `animate-ping` pulsing indicator on the featured event card (MOTION-05) and sweep for any other always-on Tailwind animation utility class site-wide.

Per CONTEXT.md D-06/D-07: in `src/components/events/event-cards.tsx` (`FeaturedUpcoming` component, lines 51–56 as of phase start), delete the inner `<span className="animate-ping ...">` wrapper (line 53) but KEEP the static red dot inner `<span>` (line 54) and the outer `<span className="relative flex h-2 w-2 shrink-0">` container (line 52). Then run a four-class sweep (`animate-ping`, `animate-pulse`, `animate-bounce`, `animate-spin`) — RESEARCH.md verified today only `animate-ping` is in use, but D-06 makes the sweep the contract, not just today's grep.

Output: `src/components/events/event-cards.tsx` `FeaturedUpcoming` renders with a single static red dot inside the flex wrapper. `npm run build` exits 0.

Depends on Plan-04 (last `src/app/page.tsx` plan ships first to keep file-conflict serialization clean even though Plan-05 doesn't touch `src/app/page.tsx`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/08-motion-subtractions/08-CONTEXT.md
@.planning/phases/08-motion-subtractions/08-RESEARCH.md
@.planning/phases/08-motion-subtractions/08-VALIDATION.md
@.planning/phases/08-motion-subtractions/08-04-SUMMARY.md

<interfaces>
<!-- The exact JSX block to edit, verified 2026-05-20 in src/components/events/event-cards.tsx:51–57 -->

BEFORE:
```jsx
<h3 className="flex items-center gap-2 text-xs font-normal uppercase tracking-widest">
  <span className="relative flex h-2 w-2 shrink-0">
    <span className="animate-ping absolute inline-flex h-full w-full rounded-full bg-red-500 opacity-60" />
    <span className="relative inline-flex h-2 w-2 rounded-full bg-red-500" />
  </span>
  Upcoming
</h3>
```

AFTER:
```jsx
<h3 className="flex items-center gap-2 text-xs font-normal uppercase tracking-widest">
  <span className="relative flex h-2 w-2 shrink-0">
    <span className="relative inline-flex h-2 w-2 rounded-full bg-red-500" />
  </span>
  Upcoming
</h3>
```

The outer `<span className="relative flex h-2 w-2 shrink-0">` stays (needed as flex parent). The inner static `<span ... bg-red-500 />` stays. The middle `<span className="animate-ping ...">` is removed entirely (not just the class name — the whole element, per CONTEXT.md D-07 and RESEARCH.md Pitfall 6).

Verified counts at phase start:
- `rg "animate-ping" -g '!.claude/**' -g '!node_modules/**' -g '!.planning/**' src/` = 1 hit (`event-cards.tsx:53`)
- `rg "animate-pulse|animate-bounce|animate-spin" -g '!.claude/**' -g '!node_modules/**' -g '!.planning/**' src/` = 0 hits

Plan-05 does NOT change `UpcomingMini` or `PastEventCard` (Plan-06 owns those). The `delay` prop signatures on `UpcomingMini`/`PastEventCard` stay intact for now — Plan-06 will drop them.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove animate-ping wrapper from FeaturedUpcoming + sweep for other always-on animation classes</name>
  <files>src/components/events/event-cards.tsx</files>
  <read_first>
    - src/components/events/event-cards.tsx (current state — focus on lines 51–57 inside `FeaturedUpcoming`)
    - .planning/phases/08-motion-subtractions/08-RESEARCH.md §"Pitfall 6: Removing animate-ping but leaving the outer span wrapper" + §"Example 3"
  </read_first>
  <action>
    1. Edit `src/components/events/event-cards.tsx`:
       - Inside the `FeaturedUpcoming` function body, locate the `<h3 className="flex items-center gap-2 text-xs font-normal uppercase tracking-widest">` element.
       - Inside that `<h3>`, locate the `<span className="relative flex h-2 w-2 shrink-0">` wrapper which contains TWO inner `<span>` children.
       - DELETE the first inner span: `<span className="animate-ping absolute inline-flex h-full w-full rounded-full bg-red-500 opacity-60" />`.
       - KEEP the second inner span: `<span className="relative inline-flex h-2 w-2 rounded-full bg-red-500" />`.
       - KEEP the outer wrapper `<span className="relative flex h-2 w-2 shrink-0">` and the trailing literal text `Upcoming` inside the `<h3>`.
    2. Do NOT change the `<h3>`'s classes, the surrounding `<article>`, the Image, the title `<h4>`, the date `<p>`, the description, or the Register link.
    3. Do NOT touch `UpcomingMini` or `PastEventCard` — Plan-06 owns those.
    4. Do NOT touch the `FeaturedUpcoming` prop signature (`event`, `delay = 0.15`, `priority = false`) — Plan-06 may revisit `delay`, but THIS plan leaves the signature unchanged.
    5. Run a final site-wide sweep:
       - `rg "animate-(ping|pulse|bounce|spin)" -g '!.claude/**' -g '!node_modules/**' -g '!.next/**' -g '!.planning/**' src/`
       - Expected: 0 hits. If any additional hit surfaces (post-v1.0 code that wasn't in research's grep), remove it in this same plan/commit before declaring done.
    6. Per CONTEXT.md D-12: do NOT touch `src/components/animations/scroll-reveal.tsx`, `src/components/providers/lenis-provider.tsx`, or `src/app/template.tsx`.
  </action>
  <verify>
    <automated>cd "/Users/Montster/MSizzle Personal Website" && rg "animate-(ping|pulse|bounce|spin)" -g '!.claude/**' -g '!node_modules/**' -g '!.next/**' -g '!.planning/**' src/ | (! grep -q .) && rg -q 'Upcoming' src/components/events/event-cards.tsx && rg -q 'rounded-full bg-red-500' src/components/events/event-cards.tsx && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `rg "animate-(ping|pulse|bounce|spin)" -g '!.claude/**' -g '!node_modules/**' -g '!.next/**' -g '!.planning/**' src/` returns 0 hits
    - `src/components/events/event-cards.tsx` still contains the literal `Upcoming` text and one `rounded-full bg-red-500` static dot inside `FeaturedUpcoming`
    - The `<span className="relative flex h-2 w-2 shrink-0">` wrapper still has exactly ONE child `<span>` after the edit (the static dot)
    - `UpcomingMini` and `PastEventCard` are byte-identical to pre-plan state (Plan-06 territory)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>animate-ping wrapper span removed from FeaturedUpcoming; static red dot remains; no other always-on animation utility class exists anywhere in src/; UpcomingMini and PastEventCard untouched; npm run build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Pure deletion of a presentational `<span>` element. No input, no auth, no network, no state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05 | Tampering | `FeaturedUpcoming` JSX structure | mitigate | `npm run build` + existing `event-cards` import sites (`src/app/page.tsx`, `src/app/events/page.tsx`) compile against the unchanged prop signature. |

ASVS V1–V13: N/A — pure JSX deletion. V14 Configuration: production build catches any structural regression (stray closing tag, missing brace).
</threat_model>

<verification>
- `npm run build` exits 0
- Grep absence: `animate-(ping|pulse|bounce|spin)` zero hits in `src/`
- DOM check via diff: `FeaturedUpcoming`'s `<h3>` block now has 1 inner `<span>` inside the flex wrapper (was 2 before)
- `UpcomingMini` + `PastEventCard` unchanged (validated by git diff)
</verification>

<success_criteria>
- MOTION-05 requirement satisfied: `animate-ping` removed; full four-class sweep confirms no other always-on Tailwind animation utilities in the codebase
- Validation map task 8-05-V passes
- Featured event card renders with the static red dot prefix — no pulse, no ring
</success_criteria>

<output>
Create `.planning/phases/08-motion-subtractions/08-05-SUMMARY.md` when done. Reference task ID `8-05-V` from the validation map. Note: confirms `animate-pulse`, `animate-bounce`, `animate-spin` were all already zero — only `animate-ping` removal required.
</output>

---

## Decisions Honored

This plan implements CONTEXT.md decisions: D-01, D-03, D-06, D-07, D-08, D-10, D-12, D-13.

See `08-CONTEXT.md` `<decisions>` block for the full text of each.
