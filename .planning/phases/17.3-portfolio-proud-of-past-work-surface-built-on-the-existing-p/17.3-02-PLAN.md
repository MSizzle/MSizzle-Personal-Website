---
phase: "17.3"
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/home/section-work.tsx
  - src/__tests__/home/section-work.test.tsx
autonomous: true
requirements:
  - SC-2

must_haves:
  truths:
    - "The homepage 'Selected Work' section contains a link to /portfolio (not /projects)"
    - "The portfolio link in SectionWork is labeled 'Portfolio' with the mono kicker 'SELECTED'"
    - "The 'All work' Button in SectionWork links to /portfolio"
    - "The Prometheus external link retains rel=noopener noreferrer target=_blank"
  artifacts:
    - path: "src/components/home/section-work.tsx"
      provides: "Updated homepage section with /portfolio Link and Button (D-01)"
      contains: "/portfolio"
    - path: "src/__tests__/home/section-work.test.tsx"
      provides: "Vitest unit tests verifying SC-2 link target and D-01 link strategy"
      contains: "/portfolio"
  key_links:
    - from: "src/components/home/section-work.tsx"
      to: "/portfolio"
      via: "Link href + Button href both updated from /projects to /portfolio"
      pattern: "href=\"/portfolio\""
---

<objective>
Update the homepage SectionWork component so the "Selected Work" section links into the portfolio (not the full archive), and write automated tests that assert the new link target.

Purpose: SC-2 -- the homepage "selected work" section links into the portfolio.
Output: Updated src/components/home/section-work.tsx with /portfolio link; new src/__tests__/home/section-work.test.tsx with SC-2 assertions.

Decision locked:
D-01 (homepage link strategy): Portfolio-only. The big-type Link and the Button both target /portfolio. The /projects archive remains accessible via the site nav. Rationale: the "Selected Work" section is semantically about curated work; dual-linking would blur the distinction between portfolio (curated) and archive (all published).
D-02 (route slug): /portfolio (per 17.3-01-PLAN.md). section-work.tsx links to the same slug.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update section-work.tsx -- /portfolio link (D-01)</name>
  <files>src/components/home/section-work.tsx</files>

  <read_first>
    - src/components/home/section-work.tsx (the file being modified -- current state at lines 51-75)
    - src/__tests__/home/section-building.test.tsx (analog for homepage section test patterns)
  </read_first>

  <action>
    Make four targeted changes to src/components/home/section-work.tsx. Touch nothing else -- no className, no structure, no SectionLabel, no Prometheus link.

    Change 1: Line 53 -- href="/projects" on the big-type Link element: change to href="/portfolio" (per D-01).

    Change 2: Line 64 -- inner <span>Projects</span>: change text content to Portfolio.

    Change 3: Line 65-67 -- the mono kicker span containing ARCHIVE: change to SELECTED (the span element itself is unchanged; only the text content changes).

    Change 4: Line 72 -- <Button href="/projects">All work</Button>: change href to /portfolio. The button text "All work" stays the same.

    Update the doc comment at the top of the function from "D-11: no em dashes; Prometheus link uses rel=noopener noreferrer per T-17.1-02." -- add on the same line or the next: "D-01 (17.3): big-type Link and Button both target /portfolio (curated portfolio, not the full archive)."

    The Prometheus external link block (lines 30-48) must NOT be modified. rel="noopener noreferrer" and target="_blank" on the Prometheus link are preserved exactly.
  </action>

  <verify>
    <automated>grep -c 'href="/portfolio"' src/components/home/section-work.tsx</automated>
  </verify>

  <acceptance_criteria>
    - grep -c 'href="/portfolio"' src/components/home/section-work.tsx outputs 2 (both the Link and the Button)
    - grep -n 'href="/projects"' src/components/home/section-work.tsx has no output (no remaining /projects hrefs)
    - src/components/home/section-work.tsx contains the string SELECTED
    - src/components/home/section-work.tsx contains the string Portfolio (as span text)
    - src/components/home/section-work.tsx still contains rel="noopener noreferrer" on the Prometheus link
  </acceptance_criteria>

  <done>section-work.tsx updated: big-type Link and Button both target /portfolio; link text is "Portfolio" with kicker "SELECTED"; Prometheus external link is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Create section-work.test.tsx -- SC-2 assertions</name>
  <files>src/__tests__/home/section-work.test.tsx</files>

  <read_first>
    - src/__tests__/home/section-building.test.tsx (closest analog for homepage section test pattern -- mock setup style)
    - src/components/home/section-work.tsx (just modified -- import path and component name to use)
  </read_first>

  <action>
    Create src/__tests__/home/section-work.test.tsx using the same vitest + React Testing Library setup as section-building.test.tsx.

    Mock layer:
    - vi.mock("next/link", ...) renders children inside an <a> with the href prop forwarded: ({ children, href, ...props }) => React.createElement("a", { href, ...props }, children)
    - vi.mock("@/components/v3/section-label", ...) renders children in a <div data-testid="section-label">
    - vi.mock("@/components/v3/button", ...) renders children inside an <a> with the href prop forwarded: ({ children, href, ...props }) => React.createElement("a", { href, ...props }, children) -- this allows asserting the Button href via getAllByRole("link")
    - vi.mock("@/utils/cn", ...) returns its first argument unchanged: (...args) => args.filter(Boolean).join(" ")

    afterEach: cleanup() only (no vi.clearAllMocks() needed since no vi.fn() mocks).

    Test suite: describe("SectionWork (Phase 17.3 SC-2)") with these it() cases:

    Case 1 (SC-2.1): "renders a link to /portfolio" -- render React.createElement(SectionWork), get allLinks = screen.getAllByRole("link"), assert allLinks.some(l => l.getAttribute("href") === "/portfolio") is true.

    Case 2 (D-01): "does NOT render any link to /projects" -- render SectionWork, get allLinks = screen.getAllByRole("link"), assert allLinks.every(l => l.getAttribute("href") !== "/projects") is true.

    Case 3 (SC-2.2): "portfolio link displays text Portfolio" -- render SectionWork, find the link whose href is /portfolio: const portfolioLink = screen.getAllByRole("link").find(l => l.getAttribute("href") === "/portfolio"), assert portfolioLink textContent includes "Portfolio".

    Case 4 (SC-2.2): "portfolio section contains kicker SELECTED" -- render SectionWork, assert screen.getByText(/SELECTED/i) is defined.

    Case 5 (T-17.1-02 preservation): "Prometheus external link retains rel=noopener noreferrer" -- render SectionWork, find the link with href="https://prometheus.today": const prometheusLink = screen.getAllByRole("link").find(l => l.getAttribute("href") === "https://prometheus.today"), assert prometheusLink.getAttribute("rel") contains "noopener", assert prometheusLink.getAttribute("rel") contains "noreferrer".

    Dynamic import for the component: use `const { SectionWork } = await import("@/components/home/section-work")` inside each it() or at the describe level after the mocks.
  </action>

  <verify>
    <automated>npx vitest run src/__tests__/home/section-work.test.tsx --run</automated>
  </verify>

  <acceptance_criteria>
    - `npx vitest run src/__tests__/home/section-work.test.tsx --run` exits 0 with 5 passing tests
    - src/__tests__/home/section-work.test.tsx contains assertion for href="/portfolio"
    - src/__tests__/home/section-work.test.tsx contains assertion that href="/projects" does not exist
    - src/__tests__/home/section-work.test.tsx contains assertion for "SELECTED"
    - src/__tests__/home/section-work.test.tsx contains assertion for "noopener" on Prometheus link
  </acceptance_criteria>

  <done>section-work.test.tsx created with 5 passing tests verifying SC-2 link target, D-01 portfolio-only strategy, label text, kicker, and Prometheus rel preservation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SectionWork -> Browser | Static JSX component; no Notion fetch, no user input, no secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17.3-04 | Tampering | section-work.tsx Prometheus external link | accept (no change) | rel="noopener noreferrer" already present on the Prometheus link per T-17.1-02; this plan explicitly preserves it; test Case 5 asserts it |
| T-17.3-05 | Information Disclosure | section-work.tsx | accept | Component contains no secrets, no env vars, no Notion tokens; purely static JSX |
| T-17.3-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
After Task 2 completes:
- `npx vitest run src/__tests__/home/section-work.test.tsx --run` exits 0 (5 tests)
- grep confirms href="/portfolio" appears twice in section-work.tsx (Link + Button)
- grep confirms href="/projects" does not appear in section-work.tsx
</verification>

<success_criteria>
- src/components/home/section-work.tsx contains href="/portfolio" (2 occurrences: Link + Button)
- src/components/home/section-work.tsx contains no href="/projects"
- src/components/home/section-work.tsx contains "Portfolio" and "SELECTED"
- src/__tests__/home/section-work.test.tsx exists with 5 passing tests
- npx vitest run src/__tests__/home/section-work.test.tsx --run exits 0
</success_criteria>

<output>
Create `.planning/phases/17.3-portfolio-proud-of-past-work-surface-built-on-the-existing-p/17.3-02-SUMMARY.md` when done
</output>
