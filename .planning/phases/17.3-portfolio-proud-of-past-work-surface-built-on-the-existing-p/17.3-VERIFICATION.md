---
phase: "17.3"
verified: 2026-07-02T23:48:00Z
status: passed
score: 8/8 must-haves verified
overrides_applied: 0
re_verification: false
---

# Phase 17.3: Portfolio Surface + Homepage Link Verification Report

**Phase Goal:** A real portfolio surface for proud past work exists, built on the existing /projects (Notion) pipeline in the current aesthetic, and linked from the homepage "selected work" section.

**Verified:** 2026-07-02T23:48:00Z
**Status:** PASSED
**Score:** 8/8 must-haves verified

## Summary

Phase 17.3 successfully delivers two key outcomes:

1. **SC-1 — Portfolio Surface:** A curated `/portfolio` route displays Featured projects from Notion in a year-grouped Card grid matching the v3 aesthetic. The route uses `getFeaturedProjects()` (server-side Notion query filtering by both published=true AND featured=true), implements ISR 1800 for cache invalidation, and includes a defensive empty-state ("No featured projects yet. Check back soon.") that renders gracefully if Notion fails or returns zero featured items.

2. **SC-2 — Homepage Link:** The homepage "Selected Work" section (`SectionWork` component) now links to `/portfolio` instead of `/projects` via both the big-type Link element and the "All work" Button. The external Prometheus link is preserved with `rel="noopener noreferrer"`. The link text is "Portfolio" with mono kicker "SELECTED" (previously was "Projects" / "ARCHIVE").

All 8 must-haves from the plans are satisfied. All 13 tests pass (5 portfolio + 5 section-work + 3 sitemap). No regressions to 17.3-specific code.

## Observable Truths Verification

| # | Truth | Status | Evidence |
| --- | --- | --- | --- |
| 1 | A visitor navigating to /portfolio sees featured projects from Notion, year-grouped in the existing Card grid aesthetic | ✓ VERIFIED | `src/app/portfolio/page.tsx:48` calls `getFeaturedProjects()`, renders year-grouped Card grid via `groupProjectsByYear()` at lines 51-102. Card hrefs point to `/projects/[slug]` (detail route unchanged). `src/__tests__/pages/portfolio.test.tsx` test case 3 asserts two featured projects render as Cards with correct hrefs. |
| 2 | If Notion returns zero featured projects or the API fails, /portfolio renders the empty-state message 'No featured projects yet. Check back soon.' and does not crash | ✓ VERIFIED | `src/app/portfolio/page.tsx:47-49` has defensive try/catch with empty catch body. Line 69-72 renders empty-state message when `yearEntries.length === 0`. Test case 1 (DEF-1) mocks `getFeaturedProjects()` returning `[]` and asserts `screen.getByText(/No featured projects yet/i)` is defined. |
| 3 | /portfolio appears in sitemap.ts as a static route with priority 0.9 and changeFrequency weekly | ✓ VERIFIED | `src/app/sitemap.ts:24` shows `{ url: \`${SITE_URL}/portfolio\`, lastModified: new Date(), changeFrequency: 'weekly', priority: 0.9 }`. Comment on line 18 updated to "= 7" (now includes /portfolio). `src/__tests__/seo/sitemap.test.ts` line 33-39 has new test case asserting portfolio entry with priority 0.9 and changeFrequency 'weekly'. |
| 4 | The homepage 'Selected Work' section contains a link to /portfolio (not /projects) | ✓ VERIFIED | `src/components/home/section-work.tsx:54` has `href="/portfolio"` on big-type Link; line 73 has Button with `href="/portfolio"`. Grep: `grep -c 'href="/portfolio"' src/components/home/section-work.tsx` = 2 (both Link + Button). Grep: `grep -n 'href="/projects"' src/components/home/section-work.tsx` = (no matches). Component is imported into ExplorativeHomepage (line 1 of `src/components/home/explorative-homepage.tsx`), which is used in `src/app/page.tsx`. Test case 1 (SC-2.1) asserts `/portfolio` link exists. Test case 2 (D-01) asserts no `/projects` link. |
| 5 | The portfolio link in SectionWork is labeled 'Portfolio' with the mono kicker 'SELECTED' | ✓ VERIFIED | `src/components/home/section-work.tsx:65` contains `<span>Portfolio</span>` (link text); line 67 contains `SELECTED` (kicker text). Grep: `grep "Portfolio" src/components/home/section-work.tsx` = match on line 65. Test case 3 (SC-2.2) asserts portfolio link text contains "Portfolio"; test case 4 (SC-2.2) asserts `screen.getByText("SELECTED")` is defined. |
| 6 | The 'All work' Button in SectionWork links to /portfolio | ✓ VERIFIED | `src/components/home/section-work.tsx:73` shows `<Button href="/portfolio">All work</Button>`. Grep: `grep -c 'href="/portfolio"' src/components/home/section-work.tsx` = 2 (includes Button). Test case 1 (SC-2.1) passes, asserting at least one `/portfolio` link exists (the Button). |
| 7 | The Prometheus external link retains rel=noopener noreferrer target=_blank | ✓ VERIFIED | `src/components/home/section-work.tsx:34` shows `rel="noopener noreferrer"` on the external Prometheus link (line 32-50 is the untouched Prometheus block). Grep: `grep 'rel="noopener noreferrer"' src/components/home/section-work.tsx` = match. Test case 5 (T-17.1-02 preservation) asserts `prometheusLink.getAttribute("rel")` contains both "noopener" and "noreferrer". |
| 8 | Tests pass: portfolio.test.tsx (5 cases) + section-work.test.tsx (5 cases) + sitemap.test.ts (/portfolio assertion + updated count) | ✓ VERIFIED | `npx vitest run src/__tests__/pages/portfolio.test.tsx src/__tests__/home/section-work.test.tsx src/__tests__/seo/sitemap.test.ts --run` output: Test Files 3 passed (3), Tests 13 passed (13). All exit code 0. No failures. |

**Score:** 8/8 truths verified

## Required Artifacts

| Artifact | Expected | Status | Details |
| --- | --- | --- | --- |
| `src/app/portfolio/page.tsx` | Server Component, ISR 1800, getFeaturedProjects, defensive empty-state, year-grouped Card grid | ✓ VERIFIED | File exists. Line 11: `export const revalidate = 1800`. Line 5: `import { getFeaturedProjects, type Project } from "@/lib/notion-projects"`. Line 48: `projects = await getFeaturedProjects()`. Lines 47-49: try/catch defensive. Lines 69-72: empty-state "No featured projects yet". Lines 75-99: year-grouped Card grid. Metadata (lines 13-25): title, description, canonical, OG all correct. Doc comment (lines 1-2) references Phase 17.3 SC-1. |
| `src/app/sitemap.ts` | Static routes array includes /portfolio entry (priority 0.9, weekly) | ✓ VERIFIED | File exists. Line 24: portfolio static route. Line 18 comment: "= 7". Grep: `grep -c "portfolio" src/app/sitemap.ts` = 2 (one in comment, one in array). |
| `src/__tests__/pages/portfolio.test.tsx` | 5 test cases (DEF-1, SC-1.3, SC-1.1/SC-1.4, cover-present, cover-absent) | ✓ VERIFIED | File exists, 229 lines. Lines 61-66: DEF-1 empty-state test. Lines 68-74: SC-1.3 PageHero title test. Lines 76-115: SC-1.1/SC-1.4 featured projects render test. Lines 117-141: cover-present test. Lines 143-167: cover-absent test. All 5 pass. Grep: `grep -c "getFeaturedProjects" src/__tests__/pages/portfolio.test.tsx` = 3 (imports + 2 mocks in tests). |
| `src/__tests__/home/section-work.test.tsx` | 5 test cases (SC-2.1, D-01, SC-2.2 link text, SC-2.2 kicker, T-17.1-02 Prometheus) | ✓ VERIFIED | File exists, 102 lines. Lines 59-64: SC-2.1 portfolio link test. Lines 66-71: D-01 no /projects test. Lines 73-81: SC-2.2 portfolio link text test. Lines 83-88: SC-2.2 kicker SELECTED test. Lines 90-100: T-17.1-02 Prometheus rel preservation test. All 5 pass. Grep: `grep "SELECTED" src/__tests__/home/section-work.test.tsx` = 1 match (line 87). |
| `src/__tests__/seo/sitemap.test.ts` | Updated static-count test + new /portfolio assertion | ✓ VERIFIED | File exists. Lines 22-31: static-count test bumped from `>= 6` to `>= 7` on line 30 and comment on line 24. Lines 33-39: new /portfolio entry test asserting priority 0.9 and changeFrequency 'weekly'. All 3 sitemap tests pass. |
| `src/components/home/section-work.tsx` | Two href="/portfolio" (Link + Button), "Portfolio" text, "SELECTED" kicker, rel=noopener noreferrer preserved | ✓ VERIFIED | File exists. Line 54: Link href="/portfolio". Line 73: Button href="/portfolio". Line 65: `<span>Portfolio</span>`. Line 67: `SELECTED`. Line 34: `rel="noopener noreferrer"`. Doc comment (lines 7-11) updated to include D-01 (17.3). Prometheus block (lines 30-50) untouched. |

## Key Link Verification

| From | To | Via | Status | Details |
| --- | --- | --- | --- | --- |
| `src/app/portfolio/page.tsx` | `src/lib/notion-projects.ts::getFeaturedProjects()` | import + call | ✓ WIRED | Line 5: `import { getFeaturedProjects, type Project }`. Line 48: `projects = await getFeaturedProjects()`. getFeaturedProjects exists in notion-projects.ts lines 182-219, properly filters by published=true AND featured=true, handles pagination, returns empty array on error. |
| `src/app/portfolio/page.tsx` | `/api/notion-cover` | coverSrc conditional | ✓ WIRED | Lines 87-91: `coverSrc={ project.image ? `/api/notion-cover?pageId=${project.id}` : undefined }`. Conditional correctly includes pageId parameter when image exists. Matches /projects pattern exactly. Test case 4 (cover-present) mocks a project with image and asserts img src includes "notion-cover?pageId=". |
| `src/app/sitemap.ts` | `/portfolio` route | static routes array | ✓ WIRED | Line 24 adds /portfolio entry to staticRoutes array with correct priority (0.9) and changeFrequency ('weekly'). Sitemap function (lines 6-44) exports this array. |
| `src/components/home/section-work.tsx` | `/portfolio` route | Link href + Button href | ✓ WIRED | Line 54: Link href="/portfolio". Line 73: Button href="/portfolio". SectionWork is exported (line 16) and imported into ExplorativeHomepage (src/components/home/explorative-homepage.tsx line 1), which is used in src/app/page.tsx. Chain: page.tsx → ExplorativeHomepage → SectionWork → /portfolio links. |
| `src/components/home/section-work.tsx` | `https://prometheus.today` | external Link | ✓ WIRED | Lines 31-50: External link to Prometheus remains untouched with `href="https://prometheus.today"`, `target="_blank"`, `rel="noopener noreferrer"`. Test case 5 asserts rel attributes are present. |

## Test Verification

| Test File | Test Count | Status | Details |
| --- | --- | --- | --- |
| `src/__tests__/pages/portfolio.test.tsx` | 5 | ✓ PASS | All 5 pass: DEF-1 (empty-state), SC-1.3 (PageHero), SC-1.1/SC-1.4 (featured projects), cover-present, cover-absent. Command: `npx vitest run src/__tests__/pages/portfolio.test.tsx --run` = "5 passed". |
| `src/__tests__/home/section-work.test.tsx` | 5 | ✓ PASS | All 5 pass: SC-2.1 (portfolio link), D-01 (no /projects), SC-2.2 (Portfolio text), SC-2.2 (SELECTED kicker), T-17.1-02 (Prometheus rel). Command: `npx vitest run src/__tests__/home/section-work.test.tsx --run` = "5 passed". |
| `src/__tests__/seo/sitemap.test.ts` | 3 | ✓ PASS | All 3 pass: /uses entry check, static-count (bumped to >= 7), /portfolio entry with priority 0.9 and weekly frequency. Command: `npx vitest run src/__tests__/seo/sitemap.test.ts --run` = "3 passed". |
| **Phase 17.3 Total** | **13** | ✓ PASS | 100% pass rate. No failures, no skips. |

**Full Test Suite Status:** 132 passed, 4 pre-existing failures (section-building.test.tsx HD-04, explorative-homepage.test.tsx TD-03/HD-05) unrelated to 17.3. These failures predate this phase per project memory.

## Requirements Coverage

Phase 17.3 has no formal REQ-IDs (as instructed). Verification against the two Success Criteria given:

| Criterion | Addressed By | Status | Evidence |
| --- | --- | --- | --- |
| SC-1: Portfolio surface exists, built on /projects pipeline, in v3 aesthetic, linked from homepage | Plan 01 (portfolio route) + Plan 02 (homepage link) | ✓ VERIFIED | src/app/portfolio/page.tsx (SC-1 surface), src/lib/notion-projects.ts::getFeaturedProjects (existing /projects pipeline), src/components/home/section-work.tsx:54,73 (homepage links). Year-grouped Cards, v3 metadata, ISR pattern match existing /projects route exactly. |
| SC-2: Homepage "selected work" section links to /portfolio | Plan 02 (homepage update) | ✓ VERIFIED | src/components/home/section-work.tsx:54 (big-type Link href="/portfolio"), line 73 (Button href="/portfolio"). No /projects references remain. SectionWork used in ExplorativeHomepage (homepage). Tests: section-work.test.tsx cases 1-2 assert portfolio links exist and /projects links do not. |

## Anti-Patterns & Code Quality

| File | Scan Result | Severity | Status |
| --- | --- | --- | --- |
| `src/app/portfolio/page.tsx` | No TODO, FIXME, XXX, TBD markers. No hardcoded empty data (empty array only returned defensively on error). Defensive try/catch pattern correct. | ℹ️ Info (clean) | ✓ PASS |
| `src/components/home/section-work.tsx` | No TODO, FIXME, XXX, TBD markers. Doc comment updated with D-01 citation. Prometheus link rel attributes preserved. No JSX changes beyond href updates and text. | ℹ️ Info (clean) | ✓ PASS |
| `src/__tests__/pages/portfolio.test.tsx` | No debt markers. Mocks properly set up (getFeaturedProjects, next/image, next/link, next/navigation, NotionRenderer). afterEach cleanup correct. | ℹ️ Info (clean) | ✓ PASS |
| `src/__tests__/home/section-work.test.tsx` | No debt markers. Mocks set up correctly (next/link, SectionLabel, Button, cn). afterEach cleanup correct. Exact string match ("SELECTED") used to avoid regex collision with SectionLabel text ("Selected Work"). | ℹ️ Info (clean) | ✓ PASS |
| `src/app/sitemap.ts` | No modifications beyond adding /portfolio line. Comment count updated correctly. No new dependencies. | ℹ️ Info (clean) | ✓ PASS |
| `src/__tests__/seo/sitemap.test.ts` | Static-count test bumped from >= 6 to >= 7. New /portfolio assertion added. No other changes. | ℹ️ Info (clean) | ✓ PASS |

**Anti-Pattern Summary:** No debt markers, no stubs, no hardcoded placeholder data beyond intentional empty-state message. All code follows existing patterns (portfolio mirrors projects route, tests mirror existing test structure).

## Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| --- | --- | --- | --- |
| Portfolio page exports async function and metadata | `grep -E "^export default async|^export const metadata" src/app/portfolio/page.tsx` | Matches on lines 45 (function) and 13 (metadata). | ✓ PASS |
| getFeaturedProjects is imported and called | `grep -c "getFeaturedProjects" src/app/portfolio/page.tsx` | 3 (import line 5, call line 48, and one additional reference). | ✓ PASS |
| ISR revalidate is set to 1800 | `grep "revalidate = 1800" src/app/portfolio/page.tsx` | Match on line 11. | ✓ PASS |
| Empty-state message is exact text | `grep "No featured projects yet" src/app/portfolio/page.tsx` | Match on line 71. | ✓ PASS |
| Portfolio link appears in sitemap | `grep "portfolio" src/app/sitemap.ts` | 2 matches (comment and array entry). | ✓ PASS |
| Section-work links to portfolio (not projects) | `grep 'href="/portfolio"' src/components/home/section-work.tsx` | 2 matches (Link and Button). `grep 'href="/projects"' src/components/home/section-work.tsx` = no matches. | ✓ PASS |
| Prometheus rel attributes preserved | `grep 'rel="noopener noreferrer"' src/components/home/section-work.tsx` | 1 match (Prometheus link line 34). | ✓ PASS |

## Wiring Summary

The implementation creates a complete wiring chain:

1. **Notion → Portfolio Page:** getFeaturedProjects (server-only, filters by featured + published) → PortfolioPage (Server Component, defensive try/catch) → rendered HTML with year-grouped Cards.

2. **Portfolio Page → Image Proxy:** Card coverSrc conditional constructs `/api/notion-cover?pageId=${project.id}` URLs for images. Matches /projects pattern exactly.

3. **Portfolio Page → Sitemap:** src/app/sitemap.ts includes /portfolio entry with priority 0.9 and weekly frequency.

4. **Homepage → Portfolio:** src/app/page.tsx → ExplorativeHomepage → SectionWork → two /portfolio links (big-type Link + Button). No /projects references remain in SectionWork.

5. **External Links Preserved:** Prometheus external link in SectionWork retains target="_blank" and rel="noopener noreferrer" per T-17.1-02.

All wiring confirmed by code inspection and test assertions.

## Conclusion

**Phase Goal Achievement:** VERIFIED

- SC-1 (portfolio surface) is fully achieved: /portfolio route exists, uses getFeaturedProjects, renders year-grouped Cards in v3 aesthetic, includes sitemap entry, and has defensive empty-state.
- SC-2 (homepage link) is fully achieved: SectionWork links to /portfolio via both Link and Button, text is "Portfolio" with "SELECTED" kicker, /projects reference removed, Prometheus link preserved.

All 8 must-haves from the two plans are satisfied. All 13 tests pass (100% green for phase-specific tests). No regressions to 17.3 code. No debt markers, stubs, or incomplete implementations.

**Status: PASSED**

---

_Verified: 2026-07-02T23:48:00Z_
_Verifier: Claude (gsd-verifier)_
