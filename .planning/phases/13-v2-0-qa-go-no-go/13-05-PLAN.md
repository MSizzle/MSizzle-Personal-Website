---
phase: 13-v2-0-qa-go-no-go
plan: 05
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - .planning/phases/13-v2-0-qa-go-no-go/13-05-EVIDENCE.md
  - .planning/phases/13-v2-0-qa-go-no-go/13-05-SUMMARY.md
autonomous: true
requirements:
  - QA-V2-05
  - QA-V2-06

must_haves:
  truths:
    - "D-14 secret scan returns zero matches in client chunk directories"
    - "process.env.NOTION_TOKEN only appears in server-only modules (src/lib/notion*.ts), never in client chunks"
    - "Dark-mode decision explicitly recorded: v2.0 ships light-only; dark dropped to future requirement"
    - "Both build trees scanned: .next/static/chunks/ AND .vercel/output/static/_next/static/chunks/"
  artifacts:
    - path: ".planning/phases/13-v2-0-qa-go-no-go/13-05-EVIDENCE.md"
      provides: "Secret scan output + dark-mode decision record"
      contains: "NOTION_TOKEN"
    - path: ".planning/phases/13-v2-0-qa-go-no-go/13-05-SUMMARY.md"
      provides: "Per-plan SUMMARY for GSD tracking"
  key_links:
    - from: "rg"
      to: ".next/static/chunks/"
      via: "D-14 pattern inherited from Phase 6"
      pattern: "zero matches"
    - from: "rg"
      to: ".vercel/output/static/_next/static/chunks/"
      via: "D-14 dual-tree requirement"
      pattern: "zero matches"
---

<objective>
Run the D-14 client-bundle secret scan against both build output trees to verify zero literal-secret leaks post-Phase-12 restyle. Separately, record the explicit v2.0 light-only dark-mode decision per D-05.

Purpose: QA-V2-06 requires the D-14 secret scan to pass. QA-V2-05 requires the dark-mode FOUC status to be documented. Both are resolved by this plan: the secret scan is automated and should return zero hits (Phase 12 introduced no new env-var patterns); the dark-mode decision is a documentation step, not a code test, because D-05 pre-resolved it as "light-only ship."

Output: `13-05-EVIDENCE.md` with grep output + dark-mode decision block. `13-05-SUMMARY.md` for GSD tracking.
</objective>

<execution_context>
@/Users/Montster/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Montster/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-v2-0-qa-go-no-go/13-CONTEXT.md
@.planning/phases/13-v2-0-qa-go-no-go/13-RESEARCH.md
@.planning/phases/13-v2-0-qa-go-no-go/13-01-SUMMARY.md

Key decisions:
- D-05: v2.0 ships light-only. Dark mode dropped to future requirement. QA-V2-05 satisfied by explicit recording of this decision; no FOUC test required.
- D-06: Reuse Phase 6 D-14 grep pattern verbatim. Scan BOTH `.next/static/chunks/` AND `.vercel/output/static/_next/static/chunks/`. Any client-chunk hit = blocking fail.
- D-07: Per-plan SUMMARY.md + EVIDENCE.md
- D-09: Phase 12 WR-01/02/03 warnings (missing dateTime, img dimensions, Umami tracking) are non-blocking per D-09 — they are NOT secret-leak patterns and will not appear in the D-14 grep.

From RESEARCH.md §"Secret Scan Command (D-06)":
- Scan patterns: `NOTION_TOKEN|secret_`
- D-14 clarification (from STATE.md): "process.env reads in server Edge functions are INFO, not LEAK"
- Local test confirmed 2026-05-21: zero hits in .next/static/chunks/ — Phase 12 restyle clean

Gotcha from RESEARCH.md §13-05:
- If `.vercel/output/` directory was NOT created by `vercel build --prod` (per assumption A1 in RESEARCH.md), note it in EVIDENCE.md and mark the .vercel/output/ tree check as "N/A — directory not generated locally". The `.next/static/chunks/` scan is always authoritative.
- The `.vercel/output/` tree is generated by Vercel CI during deploy, not necessarily by local `vercel build --prod`. Plan 13-01 EVIDENCE.md confirms whether it was created.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run D-14 secret scan on both build output trees</name>
  <files>.planning/phases/13-v2-0-qa-go-no-go/13-05-EVIDENCE.md</files>
  <read_first>
    - `.planning/phases/13-v2-0-qa-go-no-go/13-CONTEXT.md` §D-06
    - `.planning/phases/13-v2-0-qa-go-no-go/13-RESEARCH.md` §"Secret Scan Command (D-06)" and §"Pitfall: Secret scan misses .vercel/output/"
    - `.planning/phases/13-v2-0-qa-go-no-go/13-01-EVIDENCE.md` — check whether .vercel/output/ was generated
    - `.planning/milestones/v1.0-phases/06-pre-launch-qa/06-GO-NO-GO.md` §"Security Gate (D-14)" — exemplar output format
  </read_first>
  <action>
Run the D-14 secret scan pattern inherited verbatim from Phase 6. Execute each command and capture output:

**Scan 1: .next/static/chunks/ (always present after vercel build --prod)**
```
rg "NOTION_TOKEN|secret_" .next/static/chunks/ 2>/dev/null && echo "HITS FOUND — FAIL" || echo "✓ Zero matches — .next/static/chunks/ PASS"
```

**Scan 2: .vercel/output/ tree (present if vercel build generated it; check 13-01-EVIDENCE.md first)**
```
# Check if .vercel/output/ exists
ls .vercel/output/static/_next/static/chunks/ 2>/dev/null && \
  (rg "NOTION_TOKEN|secret_" .vercel/output/static/_next/static/chunks/ 2>/dev/null && echo "HITS FOUND — FAIL" || echo "✓ Zero matches — .vercel/output/ PASS") || \
  echo "N/A — .vercel/output/static/ not present locally (generated by Vercel CI)"
```

**Scan 3: Broader .vercel/output/ sweep (if directory exists)**
```
ls .vercel/output/ 2>/dev/null && \
  (rg "NOTION_TOKEN|secret_" .vercel/output/ 2>/dev/null | grep -v "process\.env\.NOTION_TOKEN" | head -20 || echo "✓ Zero literal-secret matches in .vercel/output/") || \
  echo "N/A — .vercel/output/ directory not present"
```

Note: `process.env.NOTION_TOKEN` variable-name references in server Edge functions are expected INFO, not a value leak. The grep `-v "process\.env\.NOTION_TOKEN"` filter excludes these per D-06 and the STATE.md D-14 clarification.

**Scan 4: Confirm server-only usage (positive check)**
```
rg "process\.env\.NOTION_TOKEN" src/lib/notion*.ts 2>/dev/null && echo "✓ NOTION_TOKEN found in server-only modules (expected)" || echo "WARNING: NOTION_TOKEN not found in src/lib/notion*.ts — check if env var is used"
```

Write the first section of `13-05-EVIDENCE.md` with:
1. Date/time of scan
2. Git commit SHA (`git rev-parse --short HEAD`)
3. Results of all 4 scans (copy the exact output)
4. Table (match Phase 6 format):

```
| Check | Result |
|-------|--------|
| NOTION_TOKEN in .next/static/chunks/ | Zero hits |
| NOTION_TOKEN in .vercel/output/static/_next/static/chunks/ | Zero hits / N/A |
| secret_ literal in client chunks | Zero hits |
| Broader .vercel/output/ sweep | Zero literal-secret hits (INFO refs filtered) |
| process.env.NOTION_TOKEN in src/lib/notion*.ts | Present (server-only) |
```

5. Verdict: PASS if all client-chunk scans return zero hits, FAIL if any hit found.
  </action>
  <verify>
    <automated>test -f .planning/phases/13-v2-0-qa-go-no-go/13-05-EVIDENCE.md && grep -v "^#" .planning/phases/13-v2-0-qa-go-no-go/13-05-EVIDENCE.md | grep -c "Zero matches\|Zero hits\|N/A"</automated>
  </verify>
  <acceptance_criteria>
    - `rg "NOTION_TOKEN|secret_" .next/static/chunks/` returns zero matches (no client-chunk leaks)
    - Either `.vercel/output/` scan returns zero matches, OR directory absent is documented as N/A
    - `process.env.NOTION_TOKEN` confirmed to appear only in `src/lib/notion*.ts` server modules
    - `13-05-EVIDENCE.md` contains full grep output + table with PASS/FAIL per check
    - No hits from Phase 12 restyle (`secret_` scan returns zero — confirmed by RESEARCH.md local test 2026-05-21)
  </acceptance_criteria>
  <done>D-14 secret scan complete; zero client-chunk leaks; QA-V2-06 PASS recorded in EVIDENCE.md</done>
</task>

<task type="auto">
  <name>Task 2: Record dark-mode light-only decision and write SUMMARY</name>
  <files>
    .planning/phases/13-v2-0-qa-go-no-go/13-05-EVIDENCE.md
    .planning/phases/13-v2-0-qa-go-no-go/13-05-SUMMARY.md
  </files>
  <read_first>
    - `.planning/phases/13-v2-0-qa-go-no-go/13-CONTEXT.md` §D-05
    - `src/app/globals.css` — verify warm-paper palette is light-only (no `.dark` block, no `prefers-color-scheme: dark` block in @theme)
    - `.planning/phases/13-v2-0-qa-go-no-go/13-RESEARCH.md` §"Locked Decisions" D-05
  </read_first>
  <action>
Verify the light-only decision in code:

```
# Confirm no dark-mode CSS block in globals.css
grep -n "\.dark\|prefers-color-scheme.*dark\|@theme.*dark" src/app/globals.css && echo "DARK TOKENS FOUND — review" || echo "✓ No dark-mode tokens — confirms light-only ship"
```

```
# Confirm ThemeProvider was removed in Phase 9 (09-01)
grep -rn "ThemeProvider\|next-themes" src/app/layout.tsx 2>/dev/null && echo "ThemeProvider still present" || echo "✓ ThemeProvider removed — Phase 9 D-04 confirmed"
```

Append to `13-05-EVIDENCE.md` the dark-mode decision block:

```
## Dark-Mode Decision (QA-V2-05)

**Decision:** v2.0 ships LIGHT-ONLY.

**Rationale:** No dark editorial palette was built in Phases 8–12. The warm-paper palette
(#F4F2EC paper / #0E0E0C ink) is a single-mode design system. Phase 9 plan 09-01 explicitly
deleted the `.dark` CSS block and removed `<ThemeProvider>` from `layout.tsx` (per D-04).

**Code verification:**
- `globals.css`: zero `.dark` or `prefers-color-scheme: dark` blocks found
- `layout.tsx`: ThemeProvider removed (Phase 9 D-04 complete)

**Future requirement:** Design a dark variant of the warm-paper palette. Tracked as
deferred post-v2.0 requirement in REQUIREMENTS.md §"Future Requirements (Deferred Beyond v2.0)".

**FOUC test:** Not required. Per QA-V2-05 and ROADMAP §Phase 13 risk note: "if no dark-mode
editorial palette has been built by this phase, the GO doc must explicitly record
'light-only ship, dark dropped to future requirement' rather than silently fail the FOUC test."

**QA-V2-05 verdict: PASS (explicit decision recorded).**
```

Write `13-05-SUMMARY.md` with:
- QA-V2-06 verdict (secret scan): PASS/FAIL
- QA-V2-05 verdict (dark-mode decision): PASS (light-only ship, decision recorded)
- Reference to `13-05-EVIDENCE.md`
  </action>
  <verify>
    <automated>grep -c "light-only\|QA-V2-05" .planning/phases/13-v2-0-qa-go-no-go/13-05-EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>
    - `13-05-EVIDENCE.md` contains dark-mode decision block stating light-only ship
    - Code verification confirms no dark-mode CSS tokens in globals.css
    - ThemeProvider removal confirmed
    - `13-05-SUMMARY.md` states both QA-V2-06 (secret scan) and QA-V2-05 (dark-mode) verdicts
  </acceptance_criteria>
  <done>Dark-mode light-only decision recorded with code verification; QA-V2-05 PASS; SUMMARY.md written with both verdicts</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server module → client bundle | NOTION_TOKEN must not cross this boundary (primary D-14 concern) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-05 | Information Disclosure | client bundle (.next/static/chunks/) | mitigate | This plan IS the mitigation — D-14 secret scan verifies zero literal-secret leaks; any hit is a blocking FAIL |
| T-13-06 | Tampering | dark-mode CSS (none) | accept | No dark-mode tokens defined; v2.0 ships light-only per D-05; no FOUC vector exists |
| T-13-SC | Tampering | no new packages | accept | No npm installs in this plan; rg and grep are system tools |

Phase 13 is a verification phase. T-13-05 is the primary security gate for this phase — mitigated by this plan's execution. All threats are inherited/low severity.
</threat_model>

<verification>
QA-V2-05 and QA-V2-06 both complete when:
- D-14 secret scan returns zero hits in .next/static/chunks/ (always present)
- D-14 secret scan returns zero hits in .vercel/output/ (if present) or N/A documented
- process.env.NOTION_TOKEN confirmed server-only
- Dark-mode light-only decision recorded with code verification
- `13-05-EVIDENCE.md` contains both sections (scan output + dark-mode decision)
- `13-05-SUMMARY.md` states both QA-V2-05 and QA-V2-06 verdicts
</verification>

<success_criteria>
Secret scan returns zero client-chunk leaks (QA-V2-06 PASS). Light-only ship decision documented with code verification (QA-V2-05 PASS). Both verdicts in EVIDENCE.md and SUMMARY.md.
</success_criteria>

<output>
Create `.planning/phases/13-v2-0-qa-go-no-go/13-05-SUMMARY.md` and `.planning/phases/13-v2-0-qa-go-no-go/13-05-EVIDENCE.md` when done.
</output>
