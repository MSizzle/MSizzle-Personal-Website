---
phase: 13-v2-0-qa-go-no-go
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/13-v2-0-qa-go-no-go/13-01-EVIDENCE.md
  - .planning/phases/13-v2-0-qa-go-no-go/13-01-SUMMARY.md
autonomous: true
requirements:
  - QA-V2-01

must_haves:
  truths:
    - "`vercel build --prod` exits with code 0"
    - "Zero TypeScript errors appear in build output"
    - "Zero ESLint errors appear in build output"
    - "Zero 429 (Notion rate-limit) errors appear in build output"
    - "Build log captured and committed to 13-01-EVIDENCE.md"
  artifacts:
    - path: ".planning/phases/13-v2-0-qa-go-no-go/13-01-EVIDENCE.md"
      provides: "Build gate evidence — exit code, error summary, page count"
      contains: "exit 0"
    - path: ".planning/phases/13-v2-0-qa-go-no-go/13-01-SUMMARY.md"
      provides: "Per-plan SUMMARY for GSD tool tracking"
  key_links:
    - from: "vercel build --prod"
      to: "13-01-EVIDENCE.md"
      via: "captured stdout/stderr"
      pattern: "exit 0"
---

<objective>
Run `vercel build --prod` against the v2.0 codebase and capture the exit code, error output, and page count as the canonical production-readiness gate for Phase 13.

Purpose: QA-V2-01 requires `vercel build --prod` to exit 0 with zero TS/ESLint/429 errors before any performance or visual QA runs. This is the foundation gate — if the build is broken, all downstream QA is invalidated.

Output: `13-01-EVIDENCE.md` containing the full build log summary, exit code, and any errors found. `13-01-SUMMARY.md` for GSD tracking.
</objective>

<execution_context>
@/Users/Montster/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Montster/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-v2-0-qa-go-no-go/13-CONTEXT.md
@.planning/phases/13-v2-0-qa-go-no-go/13-RESEARCH.md
@.planning/ROADMAP.md
@.planning/STATE.md

Key decisions:
- D-01: Run `vercel build --prod` locally (mirrors v1.0 Phase 6 precedent)
- D-07: Per-plan SUMMARY.md AND EVIDENCE.md required for every plan
- D-10: Known vitest infra failure (`@rolldown/binding-darwin-arm64`) is out of scope — this gate is build-only

Gotcha from RESEARCH.md §13-01:
- `vercel build --prod` requires valid env vars in `.env.local` or `.env.production`
- Missing `NOTION_TOKEN` will cause 429 errors in the build output; confirm env var is set before running
- A1 assumption: `vercel build --prod` locally generates `.vercel/output/` tree — confirm in EVIDENCE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify env vars and run vercel build --prod</name>
  <files>.planning/phases/13-v2-0-qa-go-no-go/13-01-EVIDENCE.md</files>
  <read_first>
    - `.planning/phases/13-v2-0-qa-go-no-go/13-CONTEXT.md` §D-01 and §D-07
    - `.planning/phases/13-v2-0-qa-go-no-go/13-RESEARCH.md` §"Tooling Confirmation" and §"Plan-by-Plan Gotchas: 13-01"
    - `.env.local` or `.env.production` — confirm NOTION_TOKEN is set before running build
  </read_first>
  <action>
Pre-flight: confirm env vars are available. Run:

```
grep -l "NOTION_TOKEN" .env.local .env.production 2>/dev/null || echo "WARNING: NOTION_TOKEN not found in local env files — build may 429"
```

If NOTION_TOKEN is missing, stop and surface the error to the user before continuing.

Run the build gate (capture full output — both stdout and stderr):

```
vercel build --prod 2>&1 | tee /tmp/13-01-build-log.txt; echo "EXIT_CODE: $?" | tee -a /tmp/13-01-build-log.txt
```

After the build completes, extract key stats from the log:
- Total pages compiled (look for "Compiled X pages" or "Generated static pages")
- Any TypeScript errors (look for "Type error:" or "TS error")
- Any ESLint errors (look for "ESLint:" or "error  @")
- Any 429 errors (look for "429" or "NOTION rate limit")
- Whether `.vercel/output/` directory was created (confirms assumption A1)
- Exit code (0 = pass, non-zero = fail)

Check `.vercel/output/` was created:
```
ls .vercel/output/static/_next/static/chunks/ 2>/dev/null | head -5 || echo "WARNING: .vercel/output/ not created by this build"
```

Write `13-01-EVIDENCE.md` with:
1. Build date/time and git commit SHA (`git rev-parse --short HEAD`)
2. Full exit code result (PASS: exit 0 / FAIL: exit N)
3. Page count from build output
4. Any errors found (TS, ESLint, 429) — list each or state "zero errors"
5. Whether `.vercel/output/` tree was generated (for D-06 secret scan in 13-05)
6. First 50 lines of captured build log (to document the gate run)
  </action>
  <verify>
    <automated>grep -c "exit 0\|EXIT_CODE: 0" /tmp/13-01-build-log.txt</automated>
  </verify>
  <acceptance_criteria>
    - `vercel build --prod` exit code == 0
    - Build log contains zero lines matching "Type error:" or "ESLint error" or "429"
    - `13-01-EVIDENCE.md` exists in `.planning/phases/13-v2-0-qa-go-no-go/` with exit code documented
    - EVIDENCE.md records whether `.vercel/output/` was created (required by 13-05 secret scan)
  </acceptance_criteria>
  <done>Build gate passes: exit 0, zero TS/ESLint/429 errors, evidence committed</done>
</task>

<task type="auto">
  <name>Task 2: Write 13-01-SUMMARY.md</name>
  <files>.planning/phases/13-v2-0-qa-go-no-go/13-01-SUMMARY.md</files>
  <read_first>
    - `.planning/phases/13-v2-0-qa-go-no-go/13-01-EVIDENCE.md` (just written in Task 1)
    - `.planning/milestones/v1.0-phases/06-pre-launch-qa/06-GO-NO-GO.md` §"Success Criteria Status" — exemplar PASS notation
  </read_first>
  <action>
Write a concise per-plan SUMMARY following the GSD summary template. The SUMMARY is a single-page record for tooling; the EVIDENCE.md is the human-readable output.

Required content:
- Plan: 13-01
- Gate: QA-V2-01 (vercel build --prod)
- Result: PASS or FAIL (from EVIDENCE.md exit code)
- Evidence: reference to `13-01-EVIDENCE.md`
- Commit SHA at time of build
- Page count from build output
- One sentence: "Build gate passed/failed; detailed log in 13-01-EVIDENCE.md"

Do NOT re-copy the full build log into SUMMARY — just the verdict and the evidence reference.
  </action>
  <verify>
    <automated>test -f .planning/phases/13-v2-0-qa-go-no-go/13-01-SUMMARY.md && grep -c "QA-V2-01" .planning/phases/13-v2-0-qa-go-no-go/13-01-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - `13-01-SUMMARY.md` exists with QA-V2-01 verdict (PASS/FAIL)
    - References `13-01-EVIDENCE.md` by path
    - Contains commit SHA
  </acceptance_criteria>
  <done>SUMMARY.md written with QA-V2-01 PASS/FAIL verdict and evidence reference</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local env → vercel build | NOTION_TOKEN loaded from .env.local; must not leak into client chunks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Information Disclosure | client bundle (.next/static/chunks/) | mitigate | D-14 secret scan in plan 13-05 verifies zero literal-secret leaks; this plan's build gate is the precondition for 13-05 to run |
| T-13-SC | Tampering | npm installs | accept | No new packages installed in Phase 13; verification-only phase inherits Phase 8–12 supply chain posture |

Phase 13 is a verification phase. No new attack surface is introduced. Threats are inherited from Phases 8–12 and the production deploy boundary. T-13-01 "leaked secret in new client bundle from Phase 12 restyle" is mitigated by QA-V2-06's D-14 secret scan (covered by plan 13-05). All threats are inherited/low severity.
</threat_model>

<verification>
QA-V2-01 complete when:
- `vercel build --prod` exits 0
- Build log shows zero TypeScript errors
- Build log shows zero ESLint errors
- Build log shows zero 429 (Notion rate-limit) errors
- `13-01-EVIDENCE.md` exists with the evidence
- `13-01-SUMMARY.md` exists with PASS verdict
</verification>

<success_criteria>
Production build gate passes: `vercel build --prod` exits 0 with zero errors. Evidence committed. QA-V2-01 satisfied.
</success_criteria>

<output>
Create `.planning/phases/13-v2-0-qa-go-no-go/13-01-SUMMARY.md` and `.planning/phases/13-v2-0-qa-go-no-go/13-01-EVIDENCE.md` when done.
</output>
