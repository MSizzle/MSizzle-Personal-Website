---
phase: 10-editorial-homepage
plan: 03
type: execute
wave: 1
depends_on:
  - 10-02
files_modified:
  - src/lib/dates.ts
  - src/app/page.tsx
autonomous: true
requirements:
  - HOME-V2-07
  - HOME-V2-08

must_haves:
  truths:
    - "Per D-21a: src/lib/dates.ts exists and exports two pure functions: formatMonthYear(iso) returning 'MAY 2026' style and formatMonthDay(iso) returning 'JUN 12' style"
    - "Per D-17 REVISED: WRITING renders 3 latest posts as <ListRow big> using post.description (NOT excerpt/subtitle) for extra, formatMonthYear(post.date) for meta, and /blog/${post.slug} for href"
    - "Per D-18: WRITING empty state renders 'More essays coming soon.' in text-caption text-muted when posts.length === 0"
    - "Per D-17: WRITING ends with <AllLink href=\"/blog\">All writing →</AllLink>"
    - "Per D-13/D-14: WRITING uses canonical pattern with SectionLabel children='Writing' / numeral='02 — Library'"
    - "Per D-19 REVISED: EVENTS featured event uses event.name (NOT event.title), event.link (NOT event.rsvpUrl), event.description, event.location, and event.date ISO via formatMonthDay(event.date)"
    - "Per D-19: EVENTS featured event renders as 3-column grid (180px date / 1fr content / auto CTA) with text-event-title for the name, AllLink href={event.link || '/events'} for RSVP"
    - "Per D-21 REVISED: EVENTS renders 2 secondary <ListRow> rows (non-big) from upcomingEvents.slice(1, 3) with title=event.name, extra=event.description, meta=formatMonthYear(event.date)"
    - "Per D-22: EVENTS empty state renders 'No upcoming events.' in text-muted (does NOT ship handoff sample copy)"
    - "Per D-20: NO animate-ping in EVENTS section (carryforward MOTION-05)"
    - "Per D-13/D-14: EVENTS uses canonical pattern with SectionLabel children='Events' / numeral='03 — Calendar' + <AllLink href=\"/events\">All events →</AllLink>"
    - "Per D-39: npm run build exits 0 after every task commit"
    - "The PLAN-10-03 WRITING + EVENTS placeholder is removed; PLAN-10-04 and PLAN-10-05 placeholders remain"
  artifacts:
    - path: src/lib/dates.ts
      provides: "Pure date-formatting helpers (formatMonthYear + formatMonthDay)"
      exports: ["formatMonthYear", "formatMonthDay"]
    - path: src/app/page.tsx
      provides: "WRITING + EVENTS sections wired to Notion data"
      contains: "post.description"
      min_lines: 200
  key_links:
    - from: src/app/page.tsx
      to: src/lib/dates.ts
      via: "import { formatMonthYear, formatMonthDay }"
      pattern: "from \"@/lib/dates\""
    - from: src/app/page.tsx
      to: src/components/editorial/list-row.tsx
      via: import ListRow
      pattern: "ListRow"
    - from: src/app/page.tsx
      to: src/lib/notion.ts
      via: posts already fetched in Plan 10-01
      pattern: "posts\\.slice"
    - from: src/app/page.tsx
      to: src/lib/notion-events.ts
      via: upcomingEvents already fetched in Plan 10-01
      pattern: "upcomingEvents"
---

<objective>
Create `src/lib/dates.ts` (per D-21a) and wire the WRITING (HOME-V2-07) + EVENTS (HOME-V2-08) sections to live Notion data using the verified field names from 10-RESEARCH.md (`post.description`, `event.name`, `event.link`, `event.date`).

Purpose: Two of the largest dynamic sections. Replaces the `PLAN-10-03 WRITING + EVENTS` placeholder from Plan 10-01. WRITING uses `<ListRow big>`; EVENTS uses an inline featured-event treatment + 2 secondary `<ListRow>` rows. The new `src/lib/dates.ts` module gives Phase 11 (archive pages) the same helpers for free.

Output: 1 NEW file (`src/lib/dates.ts`); 1 modified file (`src/app/page.tsx`); `npm run build` exits 0; visiting `/` shows real Notion-sourced essay titles + dates and the next upcoming event (or graceful empty states).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/10-editorial-homepage/10-CONTEXT.md
@.planning/phases/10-editorial-homepage/10-RESEARCH.md
@.planning/phases/10-editorial-homepage/10-VALIDATION.md
@.planning/phases/10-editorial-homepage/10-02-SUMMARY.md
@.planning/research/editorial-redesign-handoff/README.md
@src/app/page.tsx
@src/lib/notion.ts
@src/lib/notion-events.ts
@src/components/editorial/list-row.tsx
@src/components/editorial/section-label.tsx
@src/components/editorial/all-link.tsx
@src/components/editorial/rule-strong.tsx
@src/components/events/event-cards.tsx

<interfaces>
From src/lib/notion.ts (10-RESEARCH §"Notion Data Shapes"):
- interface BlogPost: id, slug, title, description, published, date (string), tags, cover, emoji, lastEdited
- post.description is the field (NOT excerpt, NOT subtitle).

From src/lib/notion-events.ts (10-RESEARCH §"Notion Data Shapes"):
- interface EventItem: id, name, date (string|null), endDate, location, link (string|null), description, emoji, image, published
- event.name (NOT event.title); event.link (NOT event.rsvpUrl); event.date is an ISO timestamp.

From src/components/editorial/list-row.tsx:
- type Props = { title; href; extra?; meta?; big? }
- Non-big: py-5, text-list-title-home (20px). Big: py-7, text-list-title (28px).
- extra renders below title in text-caption text-muted; meta renders right in text-meta uppercase text-muted.

Date helper API to create (per D-21a):
- formatMonthYear(iso: string | null): string returning "MAY 2026"
- formatMonthDay(iso: string | null): string returning "JUN 12"
Both accept null and return "" when null.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create src/lib/dates.ts (D-21a)</name>
  <files>src/lib/dates.ts</files>
  <read_first>
    - .planning/phases/10-editorial-homepage/10-CONTEXT.md §D-21a (helper contract)
    - .planning/phases/10-editorial-homepage/10-RESEARCH.md §"Date Formatting Helpers" (verbatim implementation pattern)
    - src/components/events/event-cards.tsx (existing formatLongDate/formatShortDate for in-file pattern reference; do NOT extract these — write fresh helpers in src/lib/dates.ts)
  </read_first>
  <action>
    Create a new file src/lib/dates.ts with exactly two exported pure functions.

    The file consists of:
    - A one-line module-level comment explaining purpose ("Date formatting helpers for the editorial homepage and archive pages. Pure functions; safe to call from both Server and Client Components.")
    - export function formatMonthYear with signature `(iso: string | null): string`. Body: if iso falsy return "". Otherwise call `new Date(iso).toLocaleDateString("en-US", { month: "short", year: "numeric" }).toUpperCase()` and return — produces "MAY 2026" form.
    - export function formatMonthDay with signature `(iso: string | null): string`. Body: if iso falsy return "". Otherwise call `new Date(iso).toLocaleDateString("en-US", { month: "short", day: "numeric" }).toUpperCase()` and return — produces "JUN 12" form.

    Do NOT add a third helper for now (RESEARCH suggested formatEventTime but D-21a only locks the two month-based helpers; v2.0 homepage per D-19 displays only month + day). If Plan 10-03 Task 3 decides it needs time formatting, extend this file at that point.

    The file MUST be free of imports — both functions use only the built-in Date + toLocaleDateString. Pure module.

    Commit message: feat(10-03): add src/lib/dates.ts date helpers (D-21a).
  </action>
  <verify>
    <automated>test -f src/lib/dates.ts && rg "export function formatMonthYear" src/lib/dates.ts >/dev/null && rg "export function formatMonthDay" src/lib/dates.ts >/dev/null && rg "toLocaleDateString" src/lib/dates.ts | grep -v "^#" | wc -l | awk '{ if ($1 >= 2) exit 0; else exit 1 }' && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/dates.ts exists.
    - Exports formatMonthYear AND formatMonthDay (both named exports).
    - Both functions accept `string | null` and return "" for null input.
    - Both use `toLocaleDateString("en-US", ...)` followed by `.toUpperCase()`.
    - No import statements in the file (pure module).
    - npm run build exits 0.
  </acceptance_criteria>
  <done>The dates helper module is live and ready for import by the WRITING + EVENTS tasks.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add WRITING section with 3 latest essays as ListRow big (HOME-V2-07)</name>
  <files>src/app/page.tsx</files>
  <read_first>
    - src/app/page.tsx (output of Plan 10-02 — locate the PLAN-10-03 WRITING + EVENTS placeholder comment)
    - src/components/editorial/list-row.tsx (signature: title, href, extra, meta, big? — big variant uses py-7 + text-list-title 28px)
    - src/lib/notion.ts (BlogPost interface — description field, NOT excerpt)
    - src/lib/dates.ts (created in Task 1 — formatMonthYear)
    - .planning/phases/10-editorial-homepage/10-CONTEXT.md §D-13, §D-14 (WRITING label "Writing" / numeral "02 — Library"), §D-17 REVISED (post.description), §D-18 (empty state)
    - .planning/research/editorial-redesign-handoff/README.md §"Pages" → §"1. Homepage — Desktop" → §"WRITING — 02 / Library"
  </read_first>
  <action>
    Add two imports to page.tsx: `import { ListRow } from "@/components/editorial/list-row";` and `import { formatMonthYear } from "@/lib/dates";`.

    Locate the PLAN-10-03 WRITING + EVENTS placeholder JSX comment. KEEP the comment in place for now (Task 3 deletes it after adding EVENTS). Insert the WRITING section BEFORE that placeholder, BELOW the BUILDING section that Plan 10-02 added.

    WRITING markup uses the canonical D-13 pattern:
    - Open with `<RuleStrong />`.
    - Then a `<section>` with classes `px-6 pt-[120px] pb-[120px] md:px-40` (120px top + bottom per D-13; px-6 mobile / md:px-40 desktop = 160px).
    - Inside: `<SectionLabel numeral="02 — Library">Writing</SectionLabel>` per D-14.
    - 72px below the label: a `<div className="mt-[72px]">` container.
    - Inside that container, branch on `posts.length`:
      - Empty branch: `<p className="text-caption text-muted">More essays coming soon.</p>` (per D-18).
      - Populated branch: a `<div>` wrapping `posts.slice(0, 3).map((post) => (...))`. Each iteration returns a `<ListRow>` with: key={post.id}, big (boolean attribute), href={`/blog/${post.slug}`}, title={post.title}, extra={post.description}, meta={formatMonthYear(post.date)}.
    - Below the branch, a `<div className="mt-12"><AllLink href="/blog">All writing →</AllLink></div>` — renders unconditionally so even an empty post list still shows the All-link.

    Hard rules per D-17 REVISED: ListRow's `extra` prop MUST be `post.description` — NOT `post.excerpt` or `post.subtitle` (those fields do not exist on BlogPost). The `meta` prop receives the string returned by formatMonthYear (e.g., "MAY 2026"); ListRow's meta slot already applies text-meta uppercase text-muted styling, so do NOT add Tailwind classes inline.

    Commit message: feat(10-03): WRITING section — 3 latest essays as ListRow big (HOME-V2-07).
  </action>
  <verify>
    <automated>npm run build && rg "ListRow" src/app/page.tsx | grep -v "^#" | wc -l | awk '{ if ($1 >= 2) exit 0; else exit 1 }' && rg "post\.description" src/app/page.tsx >/dev/null && rg "formatMonthYear" src/app/page.tsx >/dev/null && rg "SectionLabel numeral=" src/app/page.tsx | rg "02 — Library" >/dev/null && rg "All writing" src/app/page.tsx >/dev/null && rg "More essays coming soon" src/app/page.tsx >/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - ListRow import added; formatMonthYear import added.
    - One `<SectionLabel numeral="02 — Library">Writing</SectionLabel>` usage.
    - At least one ListRow usage (inside posts.slice map), with extra={post.description} and meta={formatMonthYear(post.date)}.
    - Empty branch literal "More essays coming soon." present.
    - One `<AllLink href="/blog">All writing →</AllLink>` usage.
    - The PLAN-10-03 placeholder comment still present (will be removed in Task 3).
    - npm run build exits 0.
  </acceptance_criteria>
  <done>WRITING section renders below BUILDING with 3 ListRow big rows (or empty fallback) and the All-writing link.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Add EVENTS section with inline featured event + 2 secondary ListRows (HOME-V2-08)</name>
  <files>src/app/page.tsx</files>
  <read_first>
    - src/app/page.tsx (output of Task 2 — locate the PLAN-10-03 WRITING + EVENTS placeholder and the WRITING section above it)
    - src/lib/notion-events.ts (EventItem interface — name, link, description, location, date — NOT title/rsvpUrl)
    - src/lib/dates.ts (formatMonthYear AND formatMonthDay from Task 1)
    - src/components/editorial/list-row.tsx, src/components/editorial/section-label.tsx, src/components/editorial/all-link.tsx, src/components/editorial/rule-strong.tsx (signatures)
    - .planning/phases/10-editorial-homepage/10-CONTEXT.md §D-13, §D-14 (EVENTS label "Events" / numeral "03 — Calendar"), §D-19 REVISED (featured event field names), §D-20 (no animate-ping), §D-21 REVISED (2 secondary ListRows), §D-22 (empty state)
    - .planning/phases/10-editorial-homepage/10-RESEARCH.md §"Notion Data Shapes" → EventItem mismatch flags for D-19 / D-21
    - .planning/research/editorial-redesign-handoff/README.md §"Pages" → §"1. Homepage — Desktop" → §"EVENTS — 03 / Calendar"
  </read_first>
  <action>
    Extend the formatMonthYear import line in page.tsx to also import formatMonthDay: change `import { formatMonthYear } from "@/lib/dates";` to `import { formatMonthYear, formatMonthDay } from "@/lib/dates";`.

    Locate the PLAN-10-03 WRITING + EVENTS placeholder comment. DELETE the comment line entirely. In its place, insert the EVENTS section between the WRITING section (added in Task 2) and the next placeholder comment (`PLAN-10-04 PHOTOGRAPHS`).

    EVENTS markup:
    - Open with `<RuleStrong />`.
    - Then a `<section>` with classes `px-6 pt-[120px] pb-[120px] md:px-40`.
    - Inside: `<SectionLabel numeral="03 — Calendar">Events</SectionLabel>` per D-14.
    - `<div className="mt-[72px]">` container.

    Inside the container, derive a local variable at the top: `const featuredEvent = upcomingEvents[0];` and `const secondaryEvents = upcomingEvents.slice(1, 3);`. Then branch on whether upcomingEvents is empty:

    - Empty branch (`upcomingEvents.length === 0`): render `<p className="text-caption text-muted">No upcoming events.</p>` per D-22. Do NOT ship the handoff sample copy "AI for Small Biz, Vol. II..." as a hardcoded placeholder.

    - Populated branch: render the featured event inline as a 3-column grid (180px date / 1fr content / auto CTA per D-19):
      - Use `<div className="grid grid-cols-1 gap-6 py-9 md:grid-cols-[180px_1fr_auto] md:gap-12">`.
      - Date column: `<div>` with two stacked spans. Top: `<div className="text-meta uppercase text-ink">NEXT · {formatMonthDay(featuredEvent.date)}</div>`. Below: `<div className="mt-1 text-meta uppercase text-muted">{featuredEvent.location}</div>`. (Do NOT attempt to format event time — D-21a's helpers don't include time, and per RESEARCH §"Notion Data Shapes" mismatch flag, time is embedded in `event.date` ISO but not separately reliable.)
      - Content column: `<div>` with two children. Top: `<div className="text-event-title text-ink">{featuredEvent.name}</div>` (per D-19 — `event.name`, NOT `event.title`; uses the text-event-title token at 36px / 1.1 / -0.02em / 700 from Phase 9). Below: `<div className="mt-4 max-w-[34rem] text-body text-muted">{featuredEvent.description}</div>` (max-w-[34rem] = 540px per D-19).
      - CTA column: `<div className="md:self-start"><AllLink href={featuredEvent.link || "/events"}>RSVP →</AllLink></div>` (per D-19 — `event.link`, NOT `event.rsvpUrl`; falls back to `/events` when link is null).

    - Then 2 secondary ListRow rows (per D-21 REVISED). Render `<div className="mt-12">` wrapping a `.map` over `secondaryEvents` — each iteration: `<ListRow key={event.id} href={event.link || "/events"} title={event.name} extra={event.description} meta={formatMonthYear(event.date)} />`. NOTE: no `big` prop — secondary rows use the non-big variant (py-5 / 20px text-list-title-home).

    Finally, OUTSIDE the empty/populated branch but INSIDE the `mt-[72px]` container, add the AllLink: `<div className="mt-12"><AllLink href="/events">All events →</AllLink></div>` — renders unconditionally per D-21 (even an empty event list shows the link).

    Hard rules:
    - DO NOT add `animate-ping` anywhere (per D-20 + Phase 8 MOTION-05). The "NEXT" label is a static text marker.
    - DO NOT use `event.title` or `event.rsvpUrl` — those fields do not exist (RESEARCH §"Notion Data Shapes" verified).
    - DO NOT extract `<HomeFeaturedEvent>` to a separate component — D-CONTEXT defers extraction to Phase 11 (inline-then-extract pattern).

    Commit message: feat(10-03): EVENTS section — featured event + 2 secondary ListRows (HOME-V2-08).
  </action>
  <verify>
    <automated>npm run build && rg "event\.name" src/app/page.tsx >/dev/null && rg "event\.link" src/app/page.tsx >/dev/null && rg "formatMonthDay" src/app/page.tsx >/dev/null && rg "text-event-title" src/app/page.tsx >/dev/null && rg "SectionLabel numeral=" src/app/page.tsx | rg "03 — Calendar" >/dev/null && rg "All events" src/app/page.tsx >/dev/null && rg "No upcoming events" src/app/page.tsx >/dev/null && rg "animate-ping" src/app/page.tsx | grep -v "^#" | wc -l | awk '{ if ($1 == 0) exit 0; else exit 1 }' && rg "PLAN-10-03" src/app/page.tsx | grep -v "^#" | wc -l | awk '{ if ($1 == 0) exit 0; else exit 1 }'</automated>
  </verify>
  <acceptance_criteria>
    - formatMonthDay added to the dates import.
    - The PLAN-10-03 WRITING + EVENTS placeholder comment is REMOVED from page.tsx.
    - One `<SectionLabel numeral="03 — Calendar">Events</SectionLabel>` usage.
    - At least one `event.name` reference (featured event content column).
    - At least one `event.link || "/events"` pattern (RSVP AllLink + secondary ListRow hrefs).
    - `text-event-title` class used on the featured event name.
    - Empty branch literal "No upcoming events." present.
    - One `<AllLink href="/events">All events →</AllLink>` usage.
    - ZERO `animate-ping` occurrences in page.tsx.
    - At least one `formatMonthDay` invocation (featured event date column).
    - At least one `formatMonthYear(event.date)` invocation (secondary ListRow meta).
    - npm run build exits 0.
    - 10-VALIDATION row 10-03-V grep assertions pass (≥ 3 ListRow hits across WRITING + EVENTS; post.description hit; event.name hit; test -f src/lib/dates.ts).
  </acceptance_criteria>
  <done>EVENTS section renders the next upcoming event inline (date / content / RSVP) + 2 secondary ListRows + All-events link, or graceful "No upcoming events." empty state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Phase 10 introduces no new attack surface. EVENTS section consumes data fetched by Plan 10-01's try/catch wrapper around getUpcomingEvents() (server-side, ISR-cached). Empty fallbacks render when the fetcher returns []. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03-CONF | Configuration | npm/Next.js build pipeline | mitigate | npm run build exit 0 after each task; Vercel preview build at phase gate. |
| T-10-03-DATE | Information Disclosure | new src/lib/dates.ts | accept | Pure formatting functions accepting ISO strings. No PII, no env reads. Reviewed at planner. |

ASVS V1–V13 not applicable — no auth/session/input handling. V14 Configuration covered by per-task build gate.
</threat_model>

<verification>
- npm run build exits 0 after each of Task 1, Task 2, Task 3.
- Visual smoke at `/`: WRITING section renders 3 essay rows (or empty fallback) below BUILDING. EVENTS section renders the next upcoming event inline (date / content / RSVP) + 2 secondary rows below it.
- With Notion env missing (empty fetcher results): WRITING shows "More essays coming soon."; EVENTS shows "No upcoming events." — page does not crash.
- No `animate-ping` regression (zero occurrences anywhere in page.tsx).
- 10-VALIDATION row 10-03-V passes.
</verification>

<success_criteria>
After this plan ships:
- src/lib/dates.ts is a 2-export pure module ready for reuse in Phase 11 archive pages.
- 8 of 13 homepage requirements are now complete (Plans 10-01 + 10-02 + 10-03).
- Two placeholder comments still remain in page.tsx: PLAN-10-04 PHOTOGRAPHS, PLAN-10-05 PERSONAL + FOOTER.
- WRITING and EVENTS sections use real Notion data via post.description, event.name, event.link, event.date — never hardcoded titles.
</success_criteria>

<output>
Create `.planning/phases/10-editorial-homepage/10-03-SUMMARY.md` when done. Capture:
- The new src/lib/dates.ts module (formatMonthYear + formatMonthDay).
- The verified Notion field names actually used (post.description, event.name, event.link).
- The empty-state fallback strings used (D-18 + D-22).
- Confirmation that no animate-ping was reintroduced.
- Build green confirmation.
</output>
