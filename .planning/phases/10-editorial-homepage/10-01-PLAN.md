---
phase: 10-editorial-homepage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/nav/navigation.tsx
  - src/components/footer.tsx
  - src/app/layout.tsx
  - src/app/page.tsx
autonomous: true
requirements:
  - HOME-V2-01
  - HOME-V2-02
  - HOME-V2-03
  - HOME-V2-04

must_haves:
  truths:
    - "Per D-42: visiting `/` does NOT double-render the v1.0 `<Navigation />` or `<Footer />` chrome (both early-return null when `pathname === '/'`)"
    - "Per D-42a: visiting `/` does NOT inherit the v1.0 `<main className=\"pt-16\">` 64px offset (layout.tsx gates pt-16 on non-home routes)"
    - "Per D-38: the legacy v1.0 page.tsx scaffold (Hero / Writings / Works / Events sections + FeaturedUpcoming/UpcomingMini/PastEventCard imports) is deleted wholesale"
    - "Per D-07/D-08 (HOME-V2-01): page.tsx renders a `<header>` with 'Monty Singer' name + 5-link nav (Building → /projects, Writing → /blog, Events → /events, About → /about, Links → /links)"
    - "Per D-01/D-02 (HOME-V2-02): page.tsx renders the manifesto as `<h1 className=\"text-display uppercase text-ink\">` with two `<span className=\"block whitespace-nowrap\">` lines: 'BRING FIRE' / 'TO HUMANITY.' (static markup — Plan 10-07 wraps with <ManifestoReveal>)"
    - "Per D-06 (HOME-V2-03): page.tsx renders a meta row with 32px hairline (`inline-block h-px w-8 bg-ink`) + 'EST. 2026 · WASHINGTON, D.C.' in text-meta uppercase text-muted, 56px below the manifesto"
    - "Per D-09/D-10 (HOME-V2-04): page.tsx renders a `<figure>` containing `next/image` priority epigraph (PHOTOS[0] = /MSizzle-website-photos/000092530012.jpeg, 1120×540, object-cover) + figcaption row ('Plate I — A year in motion · 2025–26' / 'Photographed on film')"
    - "Per D-36/D-37: src/app/template.tsx, src/components/animations/scroll-reveal.tsx, src/components/providers/lenis-provider.tsx, and /newsletter carousel remain UNTOUCHED"
    - "Per D-39: `npm run build` exits 0 after every task commit"
    - "Per D-13: TODO comments / empty placeholders mark BUILDING / WRITING / EVENTS / PHOTOGRAPHS / PERSONAL / footer positions so Plans 10-02 through 10-05 can drop in section markup without restructuring"
    - "JsonLd + buildPersonSchema preserved at the top of the new page.tsx"
    - "All 4 try/catch Notion getters (posts, projects, upcomingEvents) preserved as `let` declarations so downstream plans (10-02, 10-03) inherit data fetches; getPastEvents dropped per RESEARCH (not used in v2.0 homepage)"
  artifacts:
    - path: src/components/nav/navigation.tsx
      provides: "v1.0 Navigation with pathname === '/' early-return gate"
      contains: "usePathname"
    - path: src/components/footer.tsx
      provides: "v1.0 Footer converted to 'use client' with pathname === '/' early-return gate"
      contains: "usePathname"
    - path: src/app/layout.tsx
      provides: "RootLayout with conditional pt-16 main offset (no offset on `/`)"
    - path: src/app/page.tsx
      provides: "New v2.0 homepage scaffold — header + manifesto + meta row + epigraph + section placeholders"
      contains: "text-display"
      min_lines: 80
  key_links:
    - from: src/components/nav/navigation.tsx
      to: next/navigation
      via: usePathname()
      pattern: "usePathname"
    - from: src/components/footer.tsx
      to: next/navigation
      via: usePathname()
      pattern: "usePathname"
    - from: src/app/page.tsx
      to: /MSizzle-website-photos/000092530012.jpeg
      via: next/image priority
      pattern: "000092530012"
---

<objective>
Rebuild the homepage skeleton from scratch with the global-chrome conflict resolved up front (D-42 / D-42a). After this plan ships:

- The v1.0 `<Navigation />` and `<Footer />` no longer render on `/` (they continue to render on every other route until Phase 12).
- `src/app/page.tsx` is a clean v2.0 server component containing the JsonLd schema, all three Notion try/catch getters (posts, projects, upcomingEvents), an editorial `<header>`, the static manifesto `<h1>` (no animation — Plan 10-07 adds it), the meta row, and the priority epigraph image.
- Comment placeholders mark the BUILDING / WRITING / EVENTS / PHOTOGRAPHS / PERSONAL / footer slots so Plans 10-02 through 10-05 can extend without restructuring.

Purpose: D-42 must be fixed BEFORE the new editorial header lands or `/` renders two stacked navs. Building the scaffold in one plan gives Plans 02–05 a stable canvas to extend.

Output: 4 modified files; `npm run build` exits 0; visiting `/` shows the v2.0 hero (no v1.0 chrome above it, no v1.0 footer below).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/10-editorial-homepage/10-CONTEXT.md
@.planning/phases/10-editorial-homepage/10-RESEARCH.md
@.planning/phases/10-editorial-homepage/10-VALIDATION.md
@.planning/research/editorial-redesign-handoff/README.md
@src/app/page.tsx
@src/app/layout.tsx
@src/components/nav/navigation.tsx
@src/components/footer.tsx
@src/components/seo/json-ld.tsx
@src/lib/seo/schemas.ts
@src/lib/notion.ts
@src/lib/notion-projects.ts
@src/lib/notion-events.ts

<interfaces>
<!-- Verified primitive signatures from src/components/editorial/*.tsx -->
<!-- (Plan 10-01 doesn't import primitives yet beyond inline static markup,
     but downstream plans need these — included once for the whole phase.) -->

Notion data shapes (verified in 10-RESEARCH.md §"Notion Data Shapes"):

From src/lib/notion.ts:
- export interface BlogPost { id; slug; title; description; published; date; tags; cover; emoji; lastEdited }
- export async function getPublishedPosts(): Promise<BlogPost[]>

From src/lib/notion-projects.ts:
- export interface Project { id; slug; title; description; image; emoji; externalUrl; tags; featured; published; lastEdited }
- export async function getFeaturedProjects(): Promise<Project[]>

From src/lib/notion-events.ts:
- export interface EventItem { id; name; date; endDate; location; link; description; emoji; image; published }
  (Note: NOT `title`/`rsvpUrl` — the actual fields are `name` and `link`. Plan 10-03 uses these.)
- export async function getUpcomingEvents(): Promise<EventItem[]>

From src/components/seo/json-ld.tsx + src/lib/seo/schemas.ts:
- export function JsonLd({ data }: { data: unknown }): JSX.Element
- export function buildPersonSchema(): SchemaPersonObject

LazyMotion strict mode (10-RESEARCH.md §"motion/react 12.38 API Surface"):
- `<MotionProvider>` in layout.tsx wraps in <LazyMotion features={domAnimation} strict>.
- ANY motion component inside the tree MUST use `m.*` (NOT `motion.*`) — Plan 10-07 obeys this.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 0: Gate the v1.0 global chrome to non-home routes only (D-42 + D-42a)</name>
  <files>src/components/nav/navigation.tsx, src/components/footer.tsx, src/app/layout.tsx</files>
  <read_first>
    - src/components/nav/navigation.tsx (already 'use client', already imports usePathname — only needs early-return)
    - src/components/footer.tsx (currently a server component, no 'use client' directive — must be converted)
    - src/app/layout.tsx (lines 60-82 wrap children in `<main className="pt-16">{children}</main>` between Navigation and Footer)
    - .planning/phases/10-editorial-homepage/10-CONTEXT.md §D-42, §D-42a (exact gate strategy)
    - .planning/phases/10-editorial-homepage/10-RESEARCH.md §"Critical Gap: Global Chrome Conflict" (rationale)
    - .planning/phases/10-editorial-homepage/10-VALIDATION.md row 10-01-V0 (acceptance command)
  </read_first>
  <action>
    Implement the D-42 chrome gate in 3 surgical edits.

    Edit 1 — `src/components/nav/navigation.tsx`: the file is already `'use client'` and already imports `usePathname` (line 5). Add a single early-return BEFORE the `useState` hook on a separate line: `if (pathname === '/') return null;`. Insert the line AFTER `const pathname = usePathname()` (currently line 15) — i.e. between the `usePathname()` call and the `return (` on line 17. Do NOT remove or restructure any other code. The two existing nav links (About, #contact) and the mobile drawer remain intact for non-home routes.

    Edit 2 — `src/components/footer.tsx`: this file is currently a server component (no `'use client'`). Convert to a client component:
      (a) Add `'use client'` as the first line of the file (before the existing `import Link from 'next/link'`).
      (b) Add `import { usePathname } from 'next/navigation'` directly after the `Link` import.
      (c) Inside `export function Footer() {`, add `const pathname = usePathname();` as the first statement, then `if (pathname === '/') return null;` immediately after.
      (d) Leave the rest of the JSX (Contact / Links / Socials grid) untouched — sub-pages still render the v1.0 footer.

    Edit 3 — `src/app/layout.tsx`: the existing layout wraps children in `<main className="pt-16">`. The v2.0 homepage needs `pt-9` (36px per D-07), NOT `pt-16` (64px). Two options per D-42a; use option (a) — conditional className via pathname. Layout.tsx is currently a server component. To use `usePathname`, extract the pt-16 main into a tiny client subcomponent OR convert layout.tsx to read the pathname server-side via `headers()`. Recommended approach: create a small inline client component or use a CSS-only solution by relying on `:root[data-path]`. Simplest path: convert layout.tsx to a client component is invalid (RootLayout must be a server component to export metadata). Instead, create `src/components/main-offset.tsx`:
      ```
      'use client';
      import { usePathname } from 'next/navigation';
      import type { ReactNode } from 'react';
      export function MainOffset({ children }: { children: ReactNode }) {
        const pathname = usePathname();
        const isHome = pathname === '/';
        return <main className={isHome ? '' : 'pt-16'}>{children}</main>;
      }
      ```
    Then in `src/app/layout.tsx`, replace the literal `<main className="pt-16">{children}</main>` with `<MainOffset>{children}</MainOffset>`, and add the import `import { MainOffset } from "@/components/main-offset";` near the other component imports. The layout itself stays a server component (metadata export preserved).

    DO NOT touch: `src/app/template.tsx`, `src/components/animations/scroll-reveal.tsx`, `src/components/providers/lenis-provider.tsx`, `src/components/providers/motion-provider.tsx` (per D-36).

    Commit after this task with message `chore(10-01): gate v1.0 chrome on non-home routes (D-42/D-42a)`.
  </action>
  <verify>
    <automated>rg "usePathname" src/components/nav/navigation.tsx src/components/footer.tsx | grep -v "^#" | wc -l | awk '{ if ($1 >= 2) exit 0; else exit 1 }' && rg "if \(pathname === '/'\) return null" src/components/nav/navigation.tsx src/components/footer.tsx | grep -v "^#" | wc -l | awk '{ if ($1 >= 2) exit 0; else exit 1 }' && test -f src/components/main-offset.tsx && rg "MainOffset" src/app/layout.tsx >/dev/null && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/nav/navigation.tsx` contains both `usePathname` (already present) AND a new `if (pathname === '/') return null;` early-return BEFORE the existing return JSX.
    - `src/components/footer.tsx` line 1 is `'use client'`, includes `import { usePathname } from 'next/navigation'`, and has `if (pathname === '/') return null;` as the first statement inside `Footer()`.
    - `src/components/main-offset.tsx` exists, is `'use client'`, uses `usePathname()`, and conditionally renders `<main className="pt-16">` only when pathname !== '/'.
    - `src/app/layout.tsx` imports `MainOffset` and uses `<MainOffset>{children}</MainOffset>` in place of the previous `<main className="pt-16">{children}</main>`.
    - `npm run build` exits 0.
    - 10-VALIDATION.md row 10-01-V0 grep command (`rg "usePathname" src/components/nav/navigation.tsx src/components/footer.tsx` returns ≥ 2 hits) passes.
  </acceptance_criteria>
  <done>The v1.0 chrome gate is live; visiting `/` shows no v1.0 Navigation header or v1.0 Footer, and `<main>` no longer applies pt-16 to the homepage. All other routes are unchanged.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 1: Rewrite src/app/page.tsx as the v2.0 scaffold (HOME-V2-01..04)</name>
  <files>src/app/page.tsx</files>
  <read_first>
    - src/app/page.tsx (current 191-line v1.0 scaffold — DELETED wholesale per D-38; only the Notion getters + JsonLd are preserved)
    - .planning/phases/10-editorial-homepage/10-CONTEXT.md §D-07 (header layout), §D-08 (nav links), §D-01/D-02 (manifesto), §D-06 (meta row), §D-09/D-10 (epigraph + figcaption), §D-13 (section pattern marker placeholders)
    - .planning/phases/10-editorial-homepage/10-RESEARCH.md §"Code Examples" → "Static manifesto markup", "Meta row", "Epigraph" (verbatim JSX skeletons)
    - .planning/phases/10-editorial-homepage/10-RESEARCH.md §"Current page.tsx Inventory" (what to delete, what to keep)
    - .planning/phases/10-editorial-homepage/10-VALIDATION.md row 10-01-V1 (acceptance command)
    - .planning/research/editorial-redesign-handoff/README.md §"Pages" → §"1. Homepage — Desktop (1440 reference width)" (top-to-bottom layout spec)
    - src/lib/seo/schemas.ts + src/components/seo/json-ld.tsx (signature: `<JsonLd data={buildPersonSchema()} />` preserved)
  </read_first>
  <action>
    Rewrite `src/app/page.tsx` from scratch. The new file has this exact structure (every identifier and class name listed below is final — do not paraphrase tokens or rearrange):

    Imports block:
      - `import Link from "next/link";`
      - `import Image from "next/image";`
      - `import { getPublishedPosts } from "@/lib/notion";`
      - `import { getFeaturedProjects } from "@/lib/notion-projects";`
      - `import { getUpcomingEvents } from "@/lib/notion-events";`
      - `import { JsonLd } from "@/components/seo/json-ld";`
      - `import { buildPersonSchema } from "@/lib/seo/schemas";`
      DROP: `getPastEvents` and all three `event-cards` named imports (`FeaturedUpcoming`, `UpcomingMini`, `PastEventCard`) — they are not used by v2.0 homepage. The file `src/components/events/event-cards.tsx` STAYS in the codebase for the /events route until Phase 11 — only the imports from page.tsx are deleted.

    Module-level: `export const revalidate = 1800;`

    Default export: `export default async function Home() { ... }` (async server component). Body:
      1. Three `let` declarations with type inference from the fetcher return types (mirror the existing pattern at the top of the v1.0 file): `let posts`, `let projects`, `let upcomingEvents`. DO NOT add `pastEvents`.
      2. Three try/catch blocks calling the corresponding fetchers — same defensive style as the current file. Catch is empty `catch {}` (existing pattern).
      3. Return a fragment containing, in this exact order:
         a. `<JsonLd data={buildPersonSchema()} />` (preserved verbatim from current page.tsx line 36).
         b. `<header>` per D-07: `<header className="flex items-baseline justify-between px-6 pt-7 md:px-40 md:pt-9">`. Left child: `<Link href="/" className="text-[15px] font-bold tracking-tight">Monty Singer</Link>`. Right child: `<nav>` containing an inline list of 5 `<Link>` elements with classes `text-nav text-ink` and `gap-8` spacing. Routes per D-08: Building → /projects; Writing → /blog; Events → /events; About → /about; Links → /links. Use a single flex container `<ul className="flex gap-8 list-none">` with `<li>` wrappers (or directly `<div className="flex gap-8">` if avoiding semantic list). Pick `<ul>` for accessibility.
         c. Hero `<section>` containing the static manifesto (no animation yet):
            ```
            <section className="px-6 pt-16 md:px-40 md:pt-24">
              <h1 className="text-display uppercase text-ink">
                <span className="block whitespace-nowrap">BRING FIRE</span>
                <span className="block whitespace-nowrap">TO HUMANITY.</span>
              </h1>
              {/* Plan 10-07 wraps this h1 with <ManifestoReveal lines={...} /> */}

              {/* Meta row — D-06 */}
              <div className="mt-14 flex items-center gap-3">
                <span aria-hidden="true" className="inline-block h-px w-8 bg-ink" />
                <span className="text-meta uppercase text-muted">EST. 2026 · WASHINGTON, D.C.</span>
              </div>

              {/* Epigraph — D-09 + D-10 */}
              <figure className="mt-20">
                <Image
                  src="/MSizzle-website-photos/000092530012.jpeg"
                  alt="A year in motion, on film"
                  width={1120}
                  height={540}
                  priority
                  sizes="(max-width: 768px) 100vw, 1120px"
                  className="aspect-[1120/540] w-full object-cover"
                />
                <figcaption className="mt-4 flex justify-between text-meta uppercase text-muted">
                  <span>Plate I — A year in motion · 2025–26</span>
                  <span>Photographed on film</span>
                </figcaption>
              </figure>
            </section>
            ```
         d. Section placeholders — for each downstream plan, insert a JSX comment that marks where its section will land. Use the exact form below so subsequent plans can find their slots via grep:
            ```
            {/* PLAN-10-02 INTRO + BUILDING */}
            {/* PLAN-10-03 WRITING + EVENTS */}
            {/* PLAN-10-04 PHOTOGRAPHS */}
            {/* PLAN-10-05 PERSONAL + FOOTER */}
            ```
            (Do NOT render any text content for these placeholders — comment-only so the page is buildable and the placeholder shows nothing in the rendered output.)
         e. Suppress unused-variable warnings: `posts`, `projects`, `upcomingEvents` are not consumed by Plan 10-01's JSX. ESLint will warn. Acceptable approaches: (1) leave them — Next.js build allows unused locals; (2) reference them in a JSX comment like `{/* upcomingEvents.length === ${upcomingEvents.length}: consumed in Plan 10-03 */}` — but that emits a value-bearing expression. Recommended: leave as-is; ESLint `no-unused-vars` is configured to "warn" not "error" in next.config; if the build fails, add `// eslint-disable-next-line @typescript-eslint/no-unused-vars` above each `let` declaration. Run `npm run build` to confirm.

    DO NOT introduce any Phase 9 primitive imports yet — Plans 10-02 through 10-05 add them as they're needed. Plan 10-01 ships with only `Link` + `Image` from next.

    Commit after this task with message `feat(10-01): rewrite homepage scaffold — header + manifesto + epigraph (HOME-V2-01..04)`.
  </action>
  <verify>
    <automated>npm run build && rg "text-display" src/app/page.tsx | grep -v "^#" | wc -l | awk '{ if ($1 >= 1) exit 0; else exit 1 }' && rg "BRING FIRE" src/app/page.tsx | grep -v "^#" | wc -l | awk '{ if ($1 >= 1) exit 0; else exit 1 }' && rg "Photographed on film" src/app/page.tsx | grep -v "^#" | wc -l | awk '{ if ($1 >= 1) exit 0; else exit 1 }' && rg "000092530012" src/app/page.tsx >/dev/null && rg "PLAN-10-0[2-5]" src/app/page.tsx | grep -v "^#" | wc -l | awk '{ if ($1 >= 4) exit 0; else exit 1 }'</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/page.tsx` is rewritten; no longer imports `FeaturedUpcoming`, `UpcomingMini`, `PastEventCard`, or `getPastEvents`.
    - File contains the `<JsonLd data={buildPersonSchema()} />` line.
    - File contains exactly two manifesto `<span>` lines: `BRING FIRE` and `TO HUMANITY.`, wrapped in an `<h1 className="text-display uppercase text-ink">`.
    - File contains `next/image` `priority` epigraph pointing to `/MSizzle-website-photos/000092530012.jpeg` with width 1120 and height 540.
    - File contains `figcaption` with "Plate I — A year in motion · 2025–26" and "Photographed on film".
    - File contains four JSX placeholder comments matching `PLAN-10-02`, `PLAN-10-03`, `PLAN-10-04`, `PLAN-10-05`.
    - Header renders 5 `<Link>` nav items with the routes from D-08.
    - `npm run build` exits 0.
    - VALIDATION row 10-01-V1 grep assertions all return ≥ 1 hit.
  </acceptance_criteria>
  <done>The v2.0 homepage scaffold renders header → static manifesto → meta row → epigraph image at `/`; subsequent plans extend by replacing the four placeholder comments. Visual smoke at /: no double chrome; manifesto reads at 124px; epigraph fills the column.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Phase 10 introduces no new auth, no new input handling, no new network endpoints. Only static markup + existing Notion server-side getters (wrapped in try/catch, same as v1.0). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01-CONF | Configuration | npm/Node + Vercel preview build | mitigate | `npm run build` per task (D-39) + Vercel preview build at phase gate (D-40); ASVS V14 covered. |

ASVS V1–V13 not applicable — homepage rebuild introduces no new attack surface (no auth, no input handlers, no new network calls, no new client-side state beyond UI animation in Plan 10-07). V14 Configuration: covered by per-task `npm run build` exit-0 gate + Vercel preview deploy at phase gate.
</threat_model>

<verification>
- `npm run build` exits 0 after Task 0 AND after Task 1.
- Visual smoke at `/`: no v1.0 fixed-position header overlays the editorial header; no v1.0 paper footer below the (still-empty) section placeholders.
- Visual smoke at `/about`, `/blog`, `/events`, `/projects`, `/links`: v1.0 Navigation + Footer STILL render (the gate is path-specific to `/`).
- VALIDATION rows 10-01-V0 + 10-01-V1 both pass.
</verification>

<success_criteria>
After this plan ships:
- Visiting `/` renders exactly one editorial `<header>` (the new one) + the static 124px manifesto + meta row + 1120×540 epigraph image, in handoff order.
- Visiting any other route still renders the v1.0 Navigation header (fixed top, 64px) and the v1.0 Footer (Contact + Links/Socials grid).
- `src/app/page.tsx` is < 100 lines and contains 4 placeholder JSX comments marking section slots for Plans 10-02 through 10-05.
- No v1.0 utility class regressions introduced — only v2.0 tokens (text-display, text-ink, text-muted, text-nav, text-meta, bg-ink, bg-paper from the alias bridge default).
</success_criteria>

<output>
Create `.planning/phases/10-editorial-homepage/10-01-SUMMARY.md` when done. Capture:
- Chrome-gate strategy (option (a) per D-42 — pathname conditional in both Nav and Footer).
- Decision to extract `MainOffset` client component vs converting layout.tsx (chose extraction to preserve layout.tsx's metadata export as a server component).
- Confirmation that no v1.0 alias-bridge utility (`bg-background`, `text-foreground`, etc.) appears in the new page.tsx.
- Build green confirmation.
</output>
