---
phase: 10-editorial-homepage
plan: 05
type: execute
wave: 1
depends_on:
  - 10-04
files_modified:
  - src/app/page.tsx
autonomous: true
requirements:
  - HOME-V2-10
  - HOME-V2-11

must_haves:
  truths:
    - "Per D-13/D-14: PERSONAL section uses canonical pattern with SectionLabel children='Personal' / numeral='05 — Person'"
    - "Per D-27/D-28: PERSONAL renders a 3-column card grid (single column mobile, 3-col desktop) with 3 cards: 'Photo Archive' → /photos, 'Links & Elsewhere' → /links, 'About' → /about"
    - "Per D-27: Each card has top 1px ink border (border-t border-ink), pt-8 padding, a 20px bold title in text-list-title-home (or analogous), a description in text-caption text-muted, and a tracked 'Enter →' AllLink CTA"
    - "Per D-29: Footer is <footer> with bg-footer-bg + text-footer-fg + padding py-20 px-40 desktop / py-14 px-7 mobile"
    - "Per D-29/D-30: Footer 4-column grid (single column mobile, 4-col desktop) with 4 cols: colophon + 3 FooterCol (Studio / Library / Person... wait D-30 names them Studio/Library/About but the section heading per D-13/D-14 was 'Personal' — D-30 footer uses 'About' as the third column title per the handoff)"
    - "Per D-30: Col 1 colophon = 'MONTY SINGER' text-label uppercase text-footer-mute, then <h2 className='text-section-feature mt-6 max-w-[20rem]'>A calling card, not a billboard.</h2> in text-footer-fg"
    - "Per D-30: Cols 2-4 use <FooterCol> with the exact verbatim link arrays from D-30 (Studio: Prometheus/Selected Works/Process Notes; Library: Monty Monthly/Essays/Reading List; About: About/Photo Archive/Contact mailto)"
    - "Per D-31: Bottom row below the 4-column grid with mt-24 pt-7 border-t border-footer-rule; left=copyright text-meta uppercase; right=4 social links (Twitter, GitHub, LinkedIn, Email) as inline <a> text-meta uppercase"
    - "Per D-39: npm run build exits 0 after every task commit"
    - "The PLAN-10-05 placeholder is removed — page.tsx has zero placeholder comments after this plan"
  artifacts:
    - path: src/app/page.tsx
      provides: "PERSONAL 3-card grid + inverted ink footer with 4 cols + bottom row"
      contains: "bg-footer-bg"
      min_lines: 290
  key_links:
    - from: src/app/page.tsx
      to: src/components/editorial/footer-col.tsx
      via: import FooterCol
      pattern: "FooterCol"
    - from: src/app/page.tsx
      to: mailto:montydsinger@gmail.com
      via: contact mailto link in footer
      pattern: "mailto:montydsinger"
---

<objective>
Add the PERSONAL section (HOME-V2-10) — a 3-column card grid — and the inverted ink footer (HOME-V2-11) with 4 columns + bottom row containing copyright and socials. This is the last plan that builds out the desktop layout. After this plan, all 11 homepage section requirements have markup in place (HOME-V2-01..11); Plan 10-06 handles mobile parity (HOME-V2-12); Plan 10-07 wires the manifesto stagger (MOTION-07).

Purpose: Closes the page's bottom half with the PERSONAL grid and the editorial inverted-ink footer. The footer replaces the v1.0 chrome that Plan 10-01 already gated off `/` — this footer is page-local, not global.

Output: 1 modified file (`src/app/page.tsx`); npm run build exits 0; visiting `/` shows the PERSONAL 3-card grid + the inverted ink footer with copyright and 4 social links.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/10-editorial-homepage/10-CONTEXT.md
@.planning/phases/10-editorial-homepage/10-RESEARCH.md
@.planning/phases/10-editorial-homepage/10-VALIDATION.md
@.planning/phases/10-editorial-homepage/10-04-SUMMARY.md
@.planning/research/editorial-redesign-handoff/README.md
@src/app/page.tsx
@src/components/editorial/footer-col.tsx
@src/components/editorial/section-label.tsx
@src/components/editorial/all-link.tsx
@src/components/editorial/rule-strong.tsx

<interfaces>
From src/components/editorial/footer-col.tsx (verified):
- type FooterLink = { label: string; href: string; sub?: string }
- type Props = { title: string; links: FooterLink[] }
- Renders title in text-label uppercase text-footer-mute, then a vertical link list with each link's label in text-footer-fg + optional sub line in text-caption text-footer-mute.

Footer token names (Phase 9 D-01):
- bg-footer-bg: inverted ink background
- text-footer-fg: warm-paper foreground
- text-footer-mute: muted grey for less-prominent text
- border-footer-rule: 1px rgba ink divider for bottom row

Verbatim footer link arrays (D-30):

Studio:
- Prometheus → https://prometheus.today
- Selected Works → /projects
- Process Notes → /blog

Library:
- Monty Monthly → /newsletter
- Essays → /blog
- Reading List → /links

About (col 4):
- About → /about
- Photo Archive → /photos
- Contact → mailto:montydsinger@gmail.com

Social row (D-31):
- Twitter → https://x.com/thefullmonty0
- GitHub → https://github.com/MSizzle
- LinkedIn → https://linkedin.com/in/monty-singer
- Email → mailto:montydsinger@gmail.com
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add PERSONAL section with 3-card grid (HOME-V2-10)</name>
  <files>src/app/page.tsx</files>
  <read_first>
    - src/app/page.tsx (output of Plan 10-04 — locate the PLAN-10-05 PERSONAL + FOOTER placeholder comment, positioned after the PHOTOGRAPHS section)
    - .planning/phases/10-editorial-homepage/10-CONTEXT.md §D-13, §D-14 (label 'Personal' / numeral '05 — Person'), §D-27 (card structure: top border, pt-8, 20px title, muted description, AllLink Enter →), §D-28 (3 cards verbatim)
    - .planning/research/editorial-redesign-handoff/README.md §"Pages" → §"1. Homepage — Desktop" → §"PERSONAL — 05 / Person"
  </read_first>
  <action>
    No new imports — Plan 10-02 already imported RuleStrong, SectionLabel, AllLink.

    Define a card-data constant at module scope (above export default, after HOME_PHOTOS from Plan 10-04):
    ```
    const PERSONAL_CARDS = [
      { title: "Photo Archive",        description: "A film-led survey of the year.", href: "/photos" },
      { title: "Links & Elsewhere",    description: "Where I show up online.",        href: "/links"  },
      { title: "About",                description: "The longer version.",            href: "/about"  },
    ] as const;
    ```

    Locate the `PLAN-10-05 PERSONAL + FOOTER` placeholder comment. KEEP the comment for now (Task 2 deletes it after adding the footer). Insert the PERSONAL section BEFORE that comment, BELOW the PHOTOGRAPHS section.

    PERSONAL markup:
    - `<RuleStrong />`
    - `<section className="px-6 pt-[120px] pb-[120px] md:px-40">`
    - Inside: `<SectionLabel numeral="05 — Person">Personal</SectionLabel>`
    - `<div className="mt-[72px] grid grid-cols-1 gap-10 md:grid-cols-3 md:gap-12">`
    - Inside the grid: `PERSONAL_CARDS.map((card) => (...))` — each iteration returns a `<div className="border-t border-ink pt-8" key={card.href}>` containing:
      - `<h3 className="text-list-title-home text-ink">{card.title}</h3>` (20px bold per D-27; text-list-title-home is the Phase 9 token at 20px / 1.2 / -0.015em / 700)
      - `<p className="mt-3 text-caption text-muted">{card.description}</p>`
      - `<div className="mt-6"><AllLink href={card.href}>Enter →</AllLink></div>`

    Hard rules:
    - The card "Photo Archive" → /photos card is intentional — /photos is a 404 until Phase 11 (acceptable per CONTEXT deferred items + Risk 3 in 10-RESEARCH).
    - Use `border-t border-ink` (NOT border-rule or border-rule-strong) per D-27 — the 1px solid ink line is the strongest of the three rule weights.
    - DO NOT add card-level images per D-27 ("no images").

    Commit message: feat(10-05): PERSONAL section — 3-card grid (HOME-V2-10).
  </action>
  <verify>
    <automated>npm run build && rg "SectionLabel numeral=" src/app/page.tsx | rg "05 — Person" >/dev/null && rg "PERSONAL_CARDS" src/app/page.tsx >/dev/null && rg "text-list-title-home" src/app/page.tsx | grep -v "^#" | wc -l | awk '{ if ($1 >= 1) exit 0; else exit 1 }' && rg "Enter →" src/app/page.tsx >/dev/null && rg "Photo Archive" src/app/page.tsx | grep -v "^#" | wc -l | awk '{ if ($1 >= 2) exit 0; else exit 1 }'</automated>
  </verify>
  <acceptance_criteria>
    - PERSONAL_CARDS constant defined at module scope with exactly 3 entries.
    - One `<SectionLabel numeral="05 — Person">Personal</SectionLabel>` usage.
    - At least one `text-list-title-home` usage in the cards.
    - At least one `<AllLink>Enter →</AllLink>` usage (likely 3, one per card).
    - "Photo Archive" appears in page.tsx at least twice (once from Plan 10-04's AllLink and once from this section's card).
    - `border-t border-ink` class pair appears at least once in the cards.
    - npm run build exits 0.
  </acceptance_criteria>
  <done>PERSONAL section renders 3 cards (Photo Archive / Links & Elsewhere / About) with top ink border and Enter → CTA.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add inverted ink footer with 4 columns + bottom row (HOME-V2-11)</name>
  <files>src/app/page.tsx</files>
  <read_first>
    - src/app/page.tsx (output of Task 1 — locate the PLAN-10-05 PERSONAL + FOOTER placeholder comment, now positioned after the PERSONAL section)
    - src/components/editorial/footer-col.tsx (signature: title + links array with label/href/optional sub)
    - .planning/phases/10-editorial-homepage/10-CONTEXT.md §D-29 (footer container styling), §D-30 (verbatim 4 column content), §D-31 (bottom row: copyright + 4 socials)
    - .planning/phases/10-editorial-homepage/10-RESEARCH.md §"Code Examples" → "Inverted footer" (verbatim JSX skeleton)
    - .planning/research/editorial-redesign-handoff/README.md §"Pages" → §"1. Homepage — Desktop" → §"INVERTED INK FOOTER"
  </read_first>
  <action>
    Add the FooterCol import: `import { FooterCol } from "@/components/editorial/footer-col";`.

    Locate the `PLAN-10-05 PERSONAL + FOOTER` placeholder comment. DELETE the comment entirely.

    Insert the `<footer>` element at the very bottom of the JSX return (still INSIDE the wrapping fragment that holds JsonLd, header, hero section, BUILDING section, WRITING, EVENTS, PHOTOGRAPHS, PERSONAL):

    Footer outer container per D-29:
    `<footer className="bg-footer-bg text-footer-fg px-7 py-14 md:px-40 md:py-20">` (mobile = px-7 py-14 / desktop = px-40 py-20 = 160px sides, 80px top, 56px bottom per the spec; we collapse "py-20" to a single 80px-equivalent value via py-20 = 80px in Tailwind v4 default scale).

    Inside: a 4-column grid wrapper.
    `<div className="grid grid-cols-1 gap-10 md:grid-cols-4 md:gap-12">`

    Column 1 — colophon (D-30):
    ```
    <div>
      <div className="text-label uppercase text-footer-mute">MONTY SINGER</div>
      <h2 className="text-section-feature mt-6 max-w-[20rem] text-footer-fg">
        A calling card, not a billboard.
      </h2>
    </div>
    ```
    Note: D-30 references the token `text-section-feature` — if this token does not exist in Phase 9 (the Phase 9 token list has text-feature 44px but `text-section-feature` may be a synonym), fall back to `text-feature` per the Phase 9 token list verified in CONTEXT.md §code_context. Run `npm run build` after adding — if it errors on `text-section-feature`, change to `text-feature`.

    Column 2 — Studio (D-30):
    ```
    <FooterCol
      title="Studio"
      links={[
        { label: "Prometheus",      href: "https://prometheus.today" },
        { label: "Selected Works",  href: "/projects" },
        { label: "Process Notes",   href: "/blog" },
      ]}
    />
    ```

    Column 3 — Library (D-30):
    ```
    <FooterCol
      title="Library"
      links={[
        { label: "Monty Monthly",   href: "/newsletter" },
        { label: "Essays",          href: "/blog" },
        { label: "Reading List",    href: "/links" },
      ]}
    />
    ```

    Column 4 — About (D-30) — note title is "About" per D-30 even though the PERSONAL section header above was "Personal" / "05 — Person":
    ```
    <FooterCol
      title="About"
      links={[
        { label: "About",         href: "/about" },
        { label: "Photo Archive", href: "/photos" },
        { label: "Contact",       href: "mailto:montydsinger@gmail.com" },
      ]}
    />
    ```

    Close the 4-column grid `</div>`.

    Bottom row (D-31), AFTER the 4-column grid but INSIDE the `<footer>`:
    ```
    <div className="mt-24 pt-7 border-t border-footer-rule flex flex-col gap-6 md:flex-row md:items-baseline md:justify-between">
      <span className="text-meta uppercase text-footer-fg">
        © 2026 Monty Singer · Washington, D.C.
      </span>
      <div className="flex flex-wrap gap-6">
        <a href="https://x.com/thefullmonty0"          className="text-meta uppercase text-footer-fg hover:text-footer-fg/70">Twitter</a>
        <a href="https://github.com/MSizzle"           className="text-meta uppercase text-footer-fg hover:text-footer-fg/70">GitHub</a>
        <a href="https://linkedin.com/in/monty-singer" className="text-meta uppercase text-footer-fg hover:text-footer-fg/70">LinkedIn</a>
        <a href="mailto:montydsinger@gmail.com"        className="text-meta uppercase text-footer-fg hover:text-footer-fg/70">Email</a>
      </div>
    </div>
    ```

    Close the `</footer>`.

    Hard rules:
    - The Email mailto address per D-30 and the user profile (`montydsinger@gmail.com`) — NOT `mds345@georgetown.edu` (the v1.0 footer used the Georgetown email; v2.0 uses the personal email per CONTEXT decisions).
    - The bottom row uses `mt-24 pt-7 border-t border-footer-rule` — 96px above, 28px below the rule (D-31).
    - The "© 2026" year is hardcoded per D-31; this is acceptable for a one-year manifesto site. A future plan can replace with `{new Date().getFullYear()}` if desired.

    Commit message: feat(10-05): inverted ink footer (HOME-V2-11).
  </action>
  <verify>
    <automated>npm run build && rg "FooterCol" src/app/page.tsx | grep -v "^#" | wc -l | awk '{ if ($1 >= 3) exit 0; else exit 1 }' && rg "A calling card, not a billboard" src/app/page.tsx >/dev/null && rg "bg-footer-bg" src/app/page.tsx >/dev/null && rg "mailto:montydsinger@gmail.com" src/app/page.tsx | grep -v "^#" | wc -l | awk '{ if ($1 >= 2) exit 0; else exit 1 }' && rg "border-footer-rule" src/app/page.tsx >/dev/null && rg "© 2026 Monty Singer" src/app/page.tsx >/dev/null && rg "PLAN-10-05" src/app/page.tsx | grep -v "^#" | wc -l | awk '{ if ($1 == 0) exit 0; else exit 1 }'</automated>
  </verify>
  <acceptance_criteria>
    - FooterCol import present.
    - 3 FooterCol usages (Studio / Library / About).
    - Colophon `<h2>` contains the literal "A calling card, not a billboard.".
    - `bg-footer-bg` class present on the `<footer>` element.
    - `mailto:montydsinger@gmail.com` appears at least twice (once in FooterCol About → Contact, once in the bottom-row Email link).
    - `border-footer-rule` present in the bottom row.
    - Literal "© 2026 Monty Singer · Washington, D.C." present.
    - 4 social links (Twitter, GitHub, LinkedIn, Email) in the bottom row.
    - The PLAN-10-05 placeholder comment is REMOVED.
    - 10-VALIDATION row 10-05-V grep assertions all pass (≥ 3 FooterCol; "A calling card, not a billboard" present; bg-footer-bg present).
    - npm run build exits 0.
  </acceptance_criteria>
  <done>The inverted ink footer renders below PERSONAL with 4 columns (colophon + Studio/Library/About) and a bottom row with copyright + 4 social links.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Static markup — link arrays are constants; mailto: protocol is browser-native; external x.com/github/linkedin URLs target trusted domains. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-05-CONF | Configuration | npm/Next.js build pipeline | mitigate | npm run build exit 0 after each task; Vercel preview build at phase gate. |
| T-10-05-LINK | Tampering | External social <a> tags | accept | Outbound links to trusted domains (x.com, github.com, linkedin.com). Email link is mailto: native protocol. No third-party JS injected. |

ASVS V1–V13 not applicable — no auth/session/input handling. V14 Configuration covered by per-task build gate.
</threat_model>

<verification>
- npm run build exits 0 after each of Task 1, Task 2.
- Visual smoke at `/`: PERSONAL renders 3 cards with top ink border below PHOTOGRAPHS; footer renders below PERSONAL with bg-footer-bg ink background, 4 columns of links, and a bottom row showing copyright (left) + 4 socials (right).
- The page.tsx no longer contains ANY `PLAN-10-NN` placeholder comments.
- VALIDATION row 10-05-V passes.
</verification>

<success_criteria>
After this plan ships:
- 11 of 13 homepage requirements complete (Plans 10-01 through 10-05).
- src/app/page.tsx is the full editorial homepage in static form (manifesto NOT animated yet; mobile parity NOT swept yet).
- Zero placeholder comments remain in page.tsx.
- The footer is page-local (not global) — the v1.0 Footer is still gated off `/` by Plan 10-01.
</success_criteria>

<output>
Create `.planning/phases/10-editorial-homepage/10-05-SUMMARY.md` when done. Capture:
- The PERSONAL_CARDS constant content.
- Whether `text-section-feature` exists in the Phase 9 token set (build error catches drift to text-feature).
- Confirmation of the 4-column footer + bottom-row hierarchy.
- Build green confirmation.
- A reminder that mobile parity sweep is Plan 10-06 (not handled here).
</output>
