---
phase: 10-editorial-homepage
plan: 02
type: execute
wave: 1
depends_on:
  - 10-01
files_modified:
  - src/app/page.tsx
autonomous: true
requirements:
  - HOME-V2-05
  - HOME-V2-06

must_haves:
  truths:
    - "Per D-11/D-12 (HOME-V2-05): page.tsx renders the letter-style intro paragraph (max-w 720px, text-body-lead) with three `<IntroLink>` components: 'Prometheus' → https://prometheus.today, 'Monty Monthly' → /newsletter, 'essays' → /blog"
    - "Per D-15 (HOME-V2-06): page.tsx renders BUILDING section with `<RuleStrong />` divider, `<SectionLabel numeral=\"01 — Studio\">Building</SectionLabel>`, and a 3-column grid (180px tag / 1fr title / 1fr blurb)"
    - "Per D-15: BUILDING row 1 = Prometheus (tag 'Active · AI Studio', title 'Prometheus' in text-feature, blurb with inline AllLink prometheus.today →)"
    - "Per D-15/D-16: BUILDING row 2 = Selected Works (tag `Archive · ${projects.length} projects`, title 'Selected Works' in text-feature, blurb = comma-joined first 8 project titles + ' +N more' suffix when projects.length > 8 + AllLink 'View all works →')"
    - "Per D-15 + 10-RESEARCH Risk 1: BUILDING empty state when projects.length === 0 renders 'Recent work coming soon.' in text-muted (no NaN suffix)"
    - "Per D-16: the two BUILDING rows are separated by `<Rule />` (1px hairline), each row gets py-9 vertical padding"
    - "Per D-13: section uses the canonical pattern (RuleStrong → 120px → SectionLabel → 72px → content → optional AllLink → 120px below before next RuleStrong)"
    - "Per D-36: src/app/template.tsx, lenis-provider.tsx, scroll-reveal.tsx remain UNTOUCHED"
    - "Per D-39: `npm run build` exits 0 after every task commit"
    - "The `PLAN-10-02 INTRO + BUILDING` JSX comment marker placed by Plan 10-01 is replaced with the intro + BUILDING sections (other markers remain for downstream plans)"
  artifacts:
    - path: src/app/page.tsx
      provides: "Letter-style intro paragraph + BUILDING section (Prometheus + Selected Works)"
      contains: "IntroLink"
      min_lines: 130
  key_links:
    - from: src/app/page.tsx
      to: src/components/editorial/intro-link.tsx
      via: import IntroLink
      pattern: "IntroLink"
    - from: src/app/page.tsx
      to: src/components/editorial/section-label.tsx
      via: import SectionLabel
      pattern: "SectionLabel"
    - from: src/app/page.tsx
      to: src/lib/notion-projects.ts
      via: getFeaturedProjects() data already fetched in Plan 10-01
      pattern: "projects\\.slice"
---

<objective>
Add the letter-style intro paragraph (HOME-V2-05) and the BUILDING section with Prometheus + Selected Works rows (HOME-V2-06). Both sections render BELOW the epigraph image and ABOVE the section placeholders for Plans 10-03 through 10-05.

Purpose: Closes 2 of the 13 homepage requirements; introduces the canonical D-13 section pattern (RuleStrong → SectionLabel → content → optional AllLink → next RuleStrong) for the first time. Subsequent plans (10-03, 10-04, 10-05) reuse this pattern verbatim.

Output: 1 modified file (page.tsx); `npm run build` exits 0; visiting `/` shows the intro paragraph with 3 underlined links + the BUILDING section with two rows separated by a hairline.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/10-editorial-homepage/10-CONTEXT.md
@.planning/phases/10-editorial-homepage/10-RESEARCH.md
@.planning/phases/10-editorial-homepage/10-VALIDATION.md
@.planning/phases/10-editorial-homepage/10-01-SUMMARY.md
@.planning/research/editorial-redesign-handoff/README.md
@src/app/page.tsx
@src/components/editorial/intro-link.tsx
@src/components/editorial/rule-strong.tsx
@src/components/editorial/rule.tsx
@src/components/editorial/section-label.tsx
@src/components/editorial/all-link.tsx
@src/lib/notion-projects.ts

<interfaces>
Phase 9 primitives consumed by this plan (verified signatures from 10-RESEARCH.md §"Phase 9 Primitive Prop Signatures"):

From src/components/editorial/intro-link.tsx:
- type Props = { children: ReactNode; href: string }
- Renders <Link href={href} className="border-b border-ink">{children}</Link>

From src/components/editorial/rule-strong.tsx:
- export function RuleStrong(): JSX.Element  // self-closing, no props

From src/components/editorial/rule.tsx:
- export function Rule(): JSX.Element  // self-closing, no props

From src/components/editorial/section-label.tsx:
- type Props = { children: ReactNode; numeral?: string }
- Renders flex baseline div with text-label uppercase text-ink + optional numeral in text-muted right.

From src/components/editorial/all-link.tsx:
- type Props = { children: ReactNode; href: string }
- Renders <Link> with `inline-block border-b border-ink pb-1 text-label uppercase text-ink`

From src/lib/notion-projects.ts (10-RESEARCH §"Notion Data Shapes"):
- interface Project { id, slug, title, description, image, emoji, externalUrl, tags, featured, published, lastEdited }
- getFeaturedProjects(): Promise<Project[]> — already fetched in Plan 10-01 as `projects` local
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add the letter-style intro paragraph (HOME-V2-05)</name>
  <files>src/app/page.tsx</files>
  <read_first>
    - src/app/page.tsx (output of Plan 10-01 — locate the `{/* PLAN-10-02 INTRO + BUILDING */}` marker)
    - src/components/editorial/intro-link.tsx (signature confirms `children` + `href` props, inline span with `border-b border-ink`)
    - .planning/phases/10-editorial-homepage/10-CONTEXT.md §D-11, §D-12 (verbatim copy)
    - .planning/research/editorial-redesign-handoff/README.md §"Pages" → §"1. Homepage — Desktop" → §"Letter-style intro" (handoff verbatim copy)
  </read_first>
  <action>
    Add the `IntroLink` import to the imports block: `import { IntroLink } from "@/components/editorial/intro-link";`.

    In the JSX, locate the `{/* PLAN-10-02 INTRO + BUILDING */}` comment placed by Plan 10-01. Replace it (keep the comment for now, but ADD content above the BUILDING section — see Task 2 for BUILDING). For this task, insert the letter-style intro paragraph as a `<section>` immediately after the epigraph `<figure>`:

    ```
    {/* Letter-style intro — HOME-V2-05 / D-11 + D-12 */}
    <section className="px-6 pt-20 md:px-40 md:pt-24">
      <p className="max-w-[45rem] text-body-lead text-ink">
        I&rsquo;m Monty — a builder and a writer. I run{" "}
        <IntroLink href="https://prometheus.today">Prometheus</IntroLink>, a studio that designs custom AI pipelines for businesses that have outgrown off-the-shelf tools. Outside the studio I publish{" "}
        <IntroLink href="/newsletter">Monty Monthly</IntroLink>, a newsletter of long-form{" "}
        <IntroLink href="/blog">essays</IntroLink> on philosophy, technology, and the texture of an attentive life. Most of what I make is an attempt to slow something down enough to see it clearly.
      </p>
    </section>
    ```

    Copy is VERBATIM from D-12 — do not paraphrase. Three `<IntroLink>` components in the order: Prometheus → https://prometheus.today; Monty Monthly → /newsletter; essays → /blog.

    `max-w-[45rem]` = 720px (45 × 16px). `text-body-lead` is the 22px / 1.55 / -0.005em token established in Phase 9.

    Commit with message `feat(10-02): letter-style intro paragraph (HOME-V2-05)`.
  </action>
  <verify>
    <automated>npm run build && rg "IntroLink" src/app/page.tsx | grep -v "^#" | wc -l | awk '{ if ($1 >= 4) exit 0; else exit 1 }' && rg "max-w-\[45rem\]" src/app/page.tsx >/dev/null && rg "text-body-lead" src/app/page.tsx >/dev/null && rg "Most of what I make is an attempt to slow something down" src/app/page.tsx >/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `IntroLink` import added to page.tsx (1 hit total in the import block + 3 usages = `rg "IntroLink"` returns ≥ 4 hits).
    - Three `<IntroLink>` usages with hrefs: `https://prometheus.today`, `/newsletter`, `/blog`.
    - Paragraph wraps in `<p className="max-w-[45rem] text-body-lead text-ink">`.
    - Copy matches D-12 verbatim (last sentence about "attentive life" + "slow something down enough to see it clearly" is grep-confirmable).
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>Visiting `/` shows the letter-style intro paragraph below the epigraph with three underlined inline links rendering correctly.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add the BUILDING section with Prometheus + Selected Works rows (HOME-V2-06)</name>
  <files>src/app/page.tsx</files>
  <read_first>
    - src/app/page.tsx (output of Task 1 — locate the now-orphaned `{/* PLAN-10-02 INTRO + BUILDING */}` comment, plus the intro section just added)
    - src/components/editorial/rule-strong.tsx, src/components/editorial/rule.tsx, src/components/editorial/section-label.tsx, src/components/editorial/all-link.tsx (verified signatures)
    - .planning/phases/10-editorial-homepage/10-CONTEXT.md §D-13 (canonical section pattern), §D-14 (BUILDING label = "Building" / numeral "01 — Studio"), §D-15 (3-col grid spec, Prometheus + Selected Works content), §D-16 (Rule separator + py-9)
    - .planning/phases/10-editorial-homepage/10-RESEARCH.md §"Architecture Patterns" → "Pattern 1: Section Composition" (verbatim skeleton), §"Risks Surfaced" → Risk 1 (Selected Works empty-state fallback)
    - .planning/research/editorial-redesign-handoff/README.md §"Pages" → §"1. Homepage — Desktop" → §"BUILDING — 01 / Studio" (handoff copy)
  </read_first>
  <action>
    Add imports to the existing import block: `import { RuleStrong } from "@/components/editorial/rule-strong";`, `import { Rule } from "@/components/editorial/rule";`, `import { SectionLabel } from "@/components/editorial/section-label";`, `import { AllLink } from "@/components/editorial/all-link";`.

    Locate the `{/* PLAN-10-02 INTRO + BUILDING */}` comment marker. DELETE this comment (its job is done after Task 1 + Task 2). Replace it with the BUILDING section, immediately AFTER the intro `<section>` and BEFORE the remaining placeholder comments.

    BUILDING markup per D-13 + D-14 + D-15 + D-16:

    ```
    {/* BUILDING — HOME-V2-06 / D-13–D-16 */}
    <RuleStrong />
    <section className="px-6 pt-[120px] pb-[120px] md:px-40">
      <SectionLabel numeral="01 — Studio">Building</SectionLabel>

      <div className="mt-[72px]">
        {/* Row 1 — Prometheus */}
        <div className="grid grid-cols-1 gap-6 py-9 md:grid-cols-[180px_1fr_1fr] md:gap-12">
          <div className="text-meta uppercase text-muted">Active · AI Studio</div>
          <div className="text-feature text-ink">Prometheus</div>
          <div className="text-body text-ink">
            <p className="text-ink">Recent work: orthodontic + hospitality clients running custom AI pipelines designed to outlive the next platform shift.</p>
            <div className="mt-4">
              <AllLink href="https://prometheus.today">prometheus.today →</AllLink>
            </div>
          </div>
        </div>

        <Rule />

        {/* Row 2 — Selected Works */}
        <div className="grid grid-cols-1 gap-6 py-9 md:grid-cols-[180px_1fr_1fr] md:gap-12">
          <div className="text-meta uppercase text-muted">Archive · {projects.length} projects</div>
          <div className="text-feature text-ink">Selected Works</div>
          <div className="text-body text-ink">
            {projects.length === 0 ? (
              <p className="text-muted">Recent work coming soon.</p>
            ) : (
              <p className="text-ink">
                {projects.slice(0, 8).map((p) => p.title).join(", ")}
                {projects.length > 8 ? ` +${projects.length - 8} more` : ""}
              </p>
            )}
            <div className="mt-4">
              <AllLink href="/projects">View all works →</AllLink>
            </div>
          </div>
        </div>
      </div>
    </section>
    ```

    Notes per Risk 1 (10-RESEARCH): the empty-state fallback ("Recent work coming soon.") prevents an awkward " +-N more" string when the Notion fetcher returns []. The `+N more` suffix only renders when length > 8.

    Per D-14: SectionLabel children = "Building" (capital B, NOT uppercase — the primitive itself applies `uppercase` via the `text-label` token), numeral = "01 — Studio".

    Per the handoff: the Prometheus blurb copy is "Recent work: orthodontic + hospitality clients..." — keep this verbatim from the handoff (the README §"BUILDING — 01 / Studio" block). The inline link `prometheus.today →` uses `<AllLink>` per D-15.

    Spacing: `pt-[120px]` and `pb-[120px]` for the 120px above/below per D-13. Tailwind v4 supports arbitrary `[120px]` values; do NOT rely on `pt-30` (not in default scale).

    Commit with message `feat(10-02): BUILDING section — Prometheus + Selected Works (HOME-V2-06)`.
  </action>
  <verify>
    <automated>npm run build && rg "SectionLabel numeral=\"01 — Studio\">Building" src/app/page.tsx >/dev/null && rg "text-feature" src/app/page.tsx | grep -v "^#" | wc -l | awk '{ if ($1 >= 2) exit 0; else exit 1 }' && rg "View all works" src/app/page.tsx >/dev/null && rg "projects\.slice\(0, 8\)" src/app/page.tsx >/dev/null && rg "Recent work coming soon" src/app/page.tsx >/dev/null && rg "PLAN-10-02" src/app/page.tsx | grep -v "^#" | wc -l | awk '{ if ($1 == 0) exit 0; else exit 1 }'</automated>
  </verify>
  <acceptance_criteria>
    - `RuleStrong`, `Rule`, `SectionLabel`, `AllLink` imports all present in page.tsx.
    - One `<SectionLabel numeral="01 — Studio">Building</SectionLabel>` usage.
    - Two `text-feature` usages (Prometheus title + Selected Works title).
    - One `<AllLink href="https://prometheus.today">` + one `<AllLink href="/projects">` (View all works →).
    - Empty-state branch: rendered string literal "Recent work coming soon." present.
    - Suffix logic `projects.length > 8 ? \` +${projects.length - 8} more\` : ""` present.
    - The `{/* PLAN-10-02 INTRO + BUILDING */}` placeholder comment is REMOVED.
    - `npm run build` exits 0 with live Notion env OR empty `projects` (defensive try/catch in Plan 10-01 ensures empty branch renders cleanly).
    - 10-VALIDATION row 10-02-V grep assertions (≥ 3 IntroLink hits, ≥ 1 Prometheus hit) pass.
  </acceptance_criteria>
  <done>The BUILDING section renders below the intro paragraph with a strong rule above it, Section Label "Building / 01 — Studio", two 3-column rows (Prometheus + Selected Works) separated by a hairline, and "View all works →" link below. Empty-state safe.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | No new auth, no new input handling, no new network calls. The Selected Works row consumes data already fetched by the Plan 10-01 try/catch (server-side, ISR-cached). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-02-CONF | Configuration | npm/Next.js build pipeline | mitigate | `npm run build` exit 0 after each task; Vercel preview build at phase gate. |

ASVS V1–V13 not applicable — no auth/session/input handling. V14 Configuration covered by per-task build gate.
</threat_model>

<verification>
- `npm run build` exits 0 after Task 1 AND after Task 2.
- Visual smoke at `/`: intro paragraph reads with 3 underlined inline links; BUILDING section appears below with strong-rule top border, "Building / 01 — Studio" label, two rows separated by hairline, and the "View all works →" CTA.
- Smoke at `/` when Notion env is missing (or DB returns 0 projects): BUILDING row 2 shows "Recent work coming soon." in text-muted instead of an empty join.
- VALIDATION row 10-02-V passes.
</verification>

<success_criteria>
After this plan ships:
- `src/app/page.tsx` is < 200 lines and renders 4 of the 13 homepage requirements (HOME-V2-01..04 from Plan 10-01 + HOME-V2-05..06 from this plan).
- The canonical D-13 section pattern is in place — Plans 10-03/04/05 will reuse `<RuleStrong /> → mt-[72px] section content → <AllLink>` verbatim.
- Three placeholder comments still remain: PLAN-10-03, PLAN-10-04, PLAN-10-05.
- No new external dependencies; no new files; only `src/app/page.tsx` modified.
</success_criteria>

<output>
Create `.planning/phases/10-editorial-homepage/10-02-SUMMARY.md` when done. Capture:
- The 4 Phase 9 primitives now imported in page.tsx (IntroLink, RuleStrong, Rule, SectionLabel, AllLink — 5 total).
- The empty-state branch for Selected Works (Risk 1 mitigation).
- Confirmation of D-12 verbatim copy.
- Build green confirmation.
</output>
