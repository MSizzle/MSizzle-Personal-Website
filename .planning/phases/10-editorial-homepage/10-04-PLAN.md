---
phase: 10-editorial-homepage
plan: 04
type: execute
wave: 1
depends_on:
  - 10-03
files_modified:
  - src/app/page.tsx
autonomous: true
requirements:
  - HOME-V2-09

must_haves:
  truths:
    - "Per D-13/D-14: PHOTOGRAPHS section uses canonical pattern with SectionLabel children='Photographs' / numeral='04 — Archive'"
    - "Per D-23: PHOTOGRAPHS renders a 12-column asymmetric grid with grid-cols-12 grid-rows-[180px] gap-3 + 6 plates with the exact col/row spans (Plate A=col-span-7 row-span-3, B=col-span-5 row-span-2, C=col-span-3 row-span-1, D=col-span-2 row-span-1, E=col-span-5 row-span-2, F=col-span-7 row-span-2)"
    - "Per D-23: 6 plates map to PHOTOS[0..5] = 000092530012.jpeg, 20230928%20MSB_0114.jpg (URL-encoded space), IMG_0028.jpeg, IMG_1075.JPG, IMG_2129.jpeg, Patricof09.jpg"
    - "Per D-24: each plate has an absolutely-positioned caption with classes 'absolute left-3.5 bottom-3 text-[10px] uppercase tracking-[0.2em] font-bold text-paper mix-blend-difference' (acceptable Phase 9 D-09 exception — documented in SUMMARY)"
    - "Per D-24: captions render 'No. 01' through 'No. 06'"
    - "Per D-25: each Image uses next/image fill + object-cover + saturate-[0.92], NO priority (only the epigraph from Plan 10-01 carries priority per LCP guidance in 10-RESEARCH)"
    - "Per D-26: PHOTOGRAPHS section ends with <AllLink href=\"/photos\">Photo Archive →</AllLink> (404 until Phase 11 ships /photos)"
    - "Per D-39: npm run build exits 0"
    - "The PLAN-10-04 PHOTOGRAPHS placeholder is removed; PLAN-10-05 placeholder remains"
  artifacts:
    - path: src/app/page.tsx
      provides: "PHOTOGRAPHS section with 12-col asymmetric grid + 6 plates + mix-blend captions"
      contains: "col-span-7"
      min_lines: 240
  key_links:
    - from: src/app/page.tsx
      to: /public/MSizzle-website-photos/
      via: next/image src paths
      pattern: "MSizzle-website-photos"
    - from: src/app/page.tsx
      to: src/components/editorial/all-link.tsx
      via: import AllLink (already imported in Plan 10-02)
      pattern: "Photo Archive"
---

<objective>
Add the PHOTOGRAPHS section (HOME-V2-09): a 12-column asymmetric CSS grid with 6 plates, mix-blend-difference caption overlays, and an "All photo archive" AllLink to `/photos` (which is a 404 until Phase 11 ships).

Purpose: One of the only visual sections — exact handoff fidelity matters. The 12-col asymmetric grid is the signature visual hook of the homepage besides the manifesto. Mobile breakpoint sweep (2×2 grid) is handled in Plan 10-06, NOT this plan.

Output: 1 modified file (`src/app/page.tsx`); npm run build exits 0; visiting `/` shows a 6-plate grid below the EVENTS section with captions that remain legible over any image background (mix-blend-difference).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/10-editorial-homepage/10-CONTEXT.md
@.planning/phases/10-editorial-homepage/10-RESEARCH.md
@.planning/phases/10-editorial-homepage/10-VALIDATION.md
@.planning/phases/10-editorial-homepage/10-03-SUMMARY.md
@.planning/research/editorial-redesign-handoff/README.md
@src/app/page.tsx
@src/components/editorial/section-label.tsx
@src/components/editorial/all-link.tsx
@src/components/editorial/rule-strong.tsx

<interfaces>
Photo file paths (verified via ls in 10-RESEARCH §"Asset Verification"):
- PHOTOS[0] = /MSizzle-website-photos/000092530012.jpeg (also used as epigraph in Plan 10-01 — duplicate use is intentional per D-23 Plate A)
- PHOTOS[1] = /MSizzle-website-photos/20230928%20MSB_0114.jpg  ← URL-encode the literal space as %20
- PHOTOS[2] = /MSizzle-website-photos/IMG_0028.jpeg
- PHOTOS[3] = /MSizzle-website-photos/IMG_1075.JPG  (uppercase .JPG extension — works as-is in next/image)
- PHOTOS[4] = /MSizzle-website-photos/IMG_2129.jpeg
- PHOTOS[5] = /MSizzle-website-photos/Patricof09.jpg

Grid spans (per D-23):
| Plate | col-span | row-span | photo |
|-------|----------|----------|-------|
| A | 7 | 3 | PHOTOS[0] |
| B | 5 | 2 | PHOTOS[1] |
| C | 3 | 1 | PHOTOS[2] |
| D | 2 | 1 | PHOTOS[3] |
| E | 5 | 2 | PHOTOS[4] |
| F | 7 | 2 | PHOTOS[5] |

next/image guidance (per 10-RESEARCH §"Standard Stack" → "Supporting"):
- Use `fill` (NOT explicit width/height) so the parent grid cell controls dimensions.
- Use `object-cover` to fill without distortion.
- Use `saturate-[0.92]` for the slight desaturation per D-25.
- DO NOT add `priority` — these 6 plates are below-fold; priority would defeat LCP heuristics by competing with the epigraph (PHOTOS[0] in Plan 10-01).
- `sizes` attribute: a reasonable default is "(max-width: 768px) 50vw, 50vw" — the actual responsive sizing in mobile (Plan 10-06) tightens this.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add PHOTOGRAPHS section with 12-col asymmetric grid + 6 plates (HOME-V2-09)</name>
  <files>src/app/page.tsx</files>
  <read_first>
    - src/app/page.tsx (output of Plan 10-03 — locate the PLAN-10-04 PHOTOGRAPHS placeholder comment, positioned after the EVENTS section)
    - .planning/phases/10-editorial-homepage/10-CONTEXT.md §D-13, §D-14 (label "Photographs" / numeral "04 — Archive"), §D-23 (grid + plate spans), §D-24 (caption overlay tokens + Phase 9 D-09 exception note), §D-25 (next/image config), §D-26 (AllLink target)
    - .planning/phases/10-editorial-homepage/10-RESEARCH.md §"Code Examples" → "Photographs 12-col grid" (verbatim JSX skeleton)
    - .planning/phases/10-editorial-homepage/10-RESEARCH.md §"Asset Verification" (URL-encoding for PHOTOS[1])
    - .planning/research/editorial-redesign-handoff/README.md §"Pages" → §"1. Homepage — Desktop" → §"PHOTOGRAPHS — 04 / Archive"
  </read_first>
  <action>
    Imports: the page already has Image and AllLink imported (from Plans 10-01 and 10-02). Nothing new to import.

    Define a module-level constant ABOVE the default export (between the import block and `export const revalidate = 1800;`):
    ```
    const HOME_PHOTOS = [
      { src: "/MSizzle-website-photos/000092530012.jpeg",       no: "01", span: "col-span-7 row-span-3" },
      { src: "/MSizzle-website-photos/20230928%20MSB_0114.jpg",  no: "02", span: "col-span-5 row-span-2" },
      { src: "/MSizzle-website-photos/IMG_0028.jpeg",           no: "03", span: "col-span-3 row-span-1" },
      { src: "/MSizzle-website-photos/IMG_1075.JPG",            no: "04", span: "col-span-2 row-span-1" },
      { src: "/MSizzle-website-photos/IMG_2129.jpeg",           no: "05", span: "col-span-5 row-span-2" },
      { src: "/MSizzle-website-photos/Patricof09.jpg",          no: "06", span: "col-span-7 row-span-2" },
    ] as const;
    ```
    Note PHOTOS[1] uses the URL-encoded `%20` for the literal space in `20230928 MSB_0114.jpg` — this is the verified working path per 10-RESEARCH.

    Locate the `PLAN-10-04 PHOTOGRAPHS` placeholder comment. DELETE the comment. Insert the PHOTOGRAPHS section in its place (after EVENTS, before the PLAN-10-05 placeholder):

    Structure:
    - `<RuleStrong />`
    - `<section className="px-6 pt-[120px] pb-[120px] md:px-40">`
    - Inside: `<SectionLabel numeral="04 — Archive">Photographs</SectionLabel>`
    - `<div className="mt-[72px]">` container
    - Inside: `<div className="grid grid-cols-12 grid-rows-[180px] gap-3">` (the 12-col asymmetric grid per D-23; mobile breakpoint handled in Plan 10-06).
    - Inside the grid: `HOME_PHOTOS.map((p, i) => (...))` — each iteration returns a `<div className={\`relative ${p.span}\`}>` containing:
      - `<Image src={p.src} alt="" fill className="object-cover saturate-[0.92]" sizes="(max-width: 768px) 50vw, 50vw" />`
      - `<span className="absolute left-3.5 bottom-3 text-[10px] uppercase tracking-[0.2em] font-bold text-paper mix-blend-difference">No. {p.no}</span>` (caption overlay per D-24).
    - Key prop on each iteration: `key={p.no}` (or `key={i}` — both stable since the array is module-level constant).
    - Outside the grid but inside the mt-[72px] container, the AllLink: `<div className="mt-12"><AllLink href="/photos">Photo Archive →</AllLink></div>` per D-26.

    Hard rules:
    - alt="" is intentional — these are decorative editorial plates per the handoff (the caption "No. NN" provides the semantic label; the figure has no per-image alt-text in the handoff). Captions render visually via mix-blend-difference.
    - DO NOT add `priority` to any of the 6 plates (LCP regression risk per 10-RESEARCH §"Anti-Patterns to Avoid").
    - DO NOT replace `<Image>` with `<img>` — the optimization is intentional even on below-fold images.
    - The `text-[10px] tracking-[0.2em]` arbitrary values are an acceptable Phase 9 D-09 exception per D-24 — record the exception in 10-04-SUMMARY for Phase 13 QA audit.

    Add a TODO comment near the AllLink:
    `{/* TODO: /photos route lands in Phase 11 (ARCH-03) — current target is 404 until then. */}`

    Commit message: feat(10-04): PHOTOGRAPHS section — 12-col asymmetric grid + 6 plates (HOME-V2-09).
  </action>
  <verify>
    <automated>npm run build && rg "col-span-7" src/app/page.tsx | grep -v "^#" | wc -l | awk '{ if ($1 >= 2) exit 0; else exit 1 }' && rg "mix-blend-difference" src/app/page.tsx | grep -v "^#" | wc -l | awk '{ if ($1 >= 1) exit 0; else exit 1 }' && rg "000092530012" src/app/page.tsx | grep -v "^#" | wc -l | awk '{ if ($1 >= 2) exit 0; else exit 1 }' && rg "20230928%20MSB_0114" src/app/page.tsx >/dev/null && rg "Patricof09" src/app/page.tsx >/dev/null && rg "Photo Archive" src/app/page.tsx >/dev/null && rg "SectionLabel numeral=" src/app/page.tsx | rg "04 — Archive" >/dev/null && rg "PLAN-10-04" src/app/page.tsx | grep -v "^#" | wc -l | awk '{ if ($1 == 0) exit 0; else exit 1 }'</automated>
  </verify>
  <acceptance_criteria>
    - HOME_PHOTOS constant defined at module scope with 6 entries.
    - PLAN-10-04 placeholder comment removed.
    - One `<SectionLabel numeral="04 — Archive">Photographs</SectionLabel>` usage.
    - `grid-cols-12 grid-rows-[180px] gap-3` present.
    - At least 2 occurrences of `col-span-7` (Plate A AND Plate F per D-23).
    - At least 1 occurrence of `mix-blend-difference`.
    - URL-encoded `20230928%20MSB_0114.jpg` path present.
    - PHOTOS[0] (000092530012.jpeg) appears in page.tsx in BOTH the epigraph (Plan 10-01) AND the photographs grid Plate A — so the grep returns ≥ 2 hits.
    - One `<AllLink href="/photos">Photo Archive →</AllLink>` usage.
    - npm run build exits 0.
    - 10-VALIDATION row 10-04-V passes.
  </acceptance_criteria>
  <done>PHOTOGRAPHS section renders a 6-plate asymmetric grid below EVENTS with mix-blend captions and a Photo Archive AllLink.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Static `<Image>` src paths to /public assets. No user input, no auth, no network call beyond next/image optimization (built-in CDN). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04-CONF | Configuration | npm/Next.js build pipeline | mitigate | npm run build exit 0 after task; Vercel preview build at phase gate. |

ASVS V1–V13 not applicable — no auth/session/input handling. V14 Configuration covered by per-task build gate.
</threat_model>

<verification>
- npm run build exits 0 after the task.
- Visual smoke at `/`: a 6-plate asymmetric grid renders below the EVENTS section. Each plate has a "No. NN" caption that inverts color over its image (mix-blend-difference legibility test).
- Click "Photo Archive →" → navigates to /photos (currently 404 until Phase 11 — acceptable per Risk 3 in 10-RESEARCH).
- Mobile (390px) is intentionally NOT addressed here — Plan 10-06 collapses this grid to 2×2.
- 10-VALIDATION row 10-04-V passes.
</verification>

<success_criteria>
After this plan ships:
- 9 of 13 homepage requirements complete (Plans 10-01 + 10-02 + 10-03 + 10-04).
- One placeholder remaining in page.tsx: PLAN-10-05 PERSONAL + FOOTER.
- PHOTOGRAPHS grid matches handoff layout exactly at desktop (Plates A–F with their D-23 col/row spans).
- The mix-blend-difference caption pattern is established (Phase 13 QA validates legibility over the actual photos).
</success_criteria>

<output>
Create `.planning/phases/10-editorial-homepage/10-04-SUMMARY.md` when done. Capture:
- The HOME_PHOTOS constant structure (6 plates with verified file paths).
- Documentation of the Phase 9 D-09 exception for `text-[10px] tracking-[0.2em]` arbitrary values in the caption overlays (per D-24).
- Confirmation no `priority` was added to any of the 6 plates.
- Confirmation PHOTOS[1] URL-encoded `%20` is present.
- Build green confirmation.
</output>
