---
phase: 09-design-tokens-editorial-primitives
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/editorial/section-label.tsx
autonomous: true
requirements: [PRIM-03]
tags: [primitive, editorial, server-component]

must_haves:
  truths:
    - "src/components/editorial/section-label.tsx exists and exports SectionLabel as a named function (D-08)"
    - "SectionLabel renders an 11px tracked uppercase section heading using text-label + text-ink tokens (D-09)"
    - "Optional `num` prop renders right-aligned numeral (e.g., '01' or '02 — Library') in muted color"
    - "Layout uses flex justify-between with baseline alignment when num is provided"
    - "Component is a Server Component (no 'use client' directive) since it has no interactivity (D-11)"
    - "Zero arbitrary values — uses text-label, text-ink, text-muted, uppercase tokens only (D-09)"
    - "Preservation contract from Phase 8 untouched (D-15, D-16)"
    - "Honors CONTEXT.md decisions: D-08, D-09, D-11, D-15, D-16"
  artifacts:
    - path: "src/components/editorial/section-label.tsx"
      provides: "PRIM-03 — 11px tracked uppercase section heading with optional right-aligned numeral"
      exports: ["SectionLabel"]
  key_links:
    - from: "src/components/editorial/section-label.tsx"
      to: "@theme inline --text-label + --color-ink"
      via: "Tailwind utilities text-label + text-ink + uppercase"
      pattern: "text-label.*text-ink"
---

<objective>
Create the `SectionLabel` primitive — the 11px tracked uppercase heading that labels every section on the homepage and archive pages (e.g., "BUILDING", "WRITING", "EVENTS"). Optional right-aligned numeral renders the editorial section index (e.g., "02 — Library" on `/writing`).

Purpose: Five labeled sections on the Phase 10 homepage (Building / Writing / Events / Photographs / Personal) all share this component. Phase 11 archive title blocks also use the labeled-section + numeral pattern.
Output: `src/components/editorial/section-label.tsx` exporting a `SectionLabel` Server Component with `{ children, num? }` props.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-design-tokens-editorial-primitives/09-CONTEXT.md
@.planning/phases/09-design-tokens-editorial-primitives/09-RESEARCH.md
@.planning/phases/09-design-tokens-editorial-primitives/09-VALIDATION.md
@.planning/research/editorial-redesign-handoff/README.md
@CLAUDE.md
</context>

<interfaces>
SectionLabel prop interface (this plan defines):
```typescript
type Props = {
  children: React.ReactNode;
  num?: string;  // optional right-aligned numeral (e.g., "01" or "02 — Library")
};

export function SectionLabel(props: Props): JSX.Element;
```

Token references (defined by Plan 09-01):
- `text-label` → 11px / line-height 1 / letter-spacing 0.2em / font-weight 700
- `text-ink` → color #0E0E0C (primary heading color)
- `text-muted` → color #9A9690 (secondary numeral color per handoff)
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create src/components/editorial/section-label.tsx — SectionLabel primitive (PRIM-03)</name>
  <files>src/components/editorial/section-label.tsx</files>
  <read_first>
    - .planning/phases/09-design-tokens-editorial-primitives/09-CONTEXT.md §"Primitive Components" (D-08, D-09, D-11)
    - .planning/phases/09-design-tokens-editorial-primitives/09-RESEARCH.md §"Server Component primitive with optional prop (SectionLabel — PRIM-03)" (lines 598-615)
    - .planning/research/editorial-redesign-handoff/README.md §"Components Catalog" — SectionLabel ("11px tracked uppercase section heading with optional right-aligned numeral")
    - .planning/research/editorial-redesign-handoff/README.md §"Typography" — Label (tracked) 11px / 700 / 0.18em–0.22em
    - .planning/phases/09-design-tokens-editorial-primitives/09-VALIDATION.md task 9-04-V
  </read_first>
  <action>
Create `src/components/editorial/section-label.tsx`:

- No 'use client' directive (D-11 — Server Component).
- Define `type Props = { children: React.ReactNode; num?: string }`.
- Named export `SectionLabel({ children, num }: Props): JSX.Element`.
- Render a `<div>` with classes `flex items-baseline justify-between text-label uppercase text-ink`. Inside:
  - `<span>{children}</span>` — the section label text (e.g., "BUILDING")
  - Conditionally `{num && <span className="font-normal text-muted">{num}</span>}` — right-aligned numeral with muted color and 400 weight to visually subordinate it to the label (handoff Typography table: meta is 400 weight, label is 700)

`text-label` resolves to the bundled `font-size: 11px; line-height: 1; letter-spacing: 0.2em; font-weight: 700` from Plan 09-01's @theme block (D-06 + D-07). Adding `uppercase` is an explicit utility — Tailwind does not infer text-transform from the @theme typography token. `text-ink` applies the primary ink color.

No `cn()` needed since className composition is unconditional. Reference example from RESEARCH.md §"Server Component primitive with optional prop":

  export function SectionLabel({ children, num }: Props) {
    return (
      <div className="flex items-baseline justify-between text-label uppercase text-ink">
        <span>{children}</span>
        {num && <span className="font-normal text-muted">{num}</span>}
      </div>
    )
  }

Zero arbitrary values (D-09).
  </action>
  <verify>
    <automated>test -f src/components/editorial/section-label.tsx && rg "export function SectionLabel" src/components/editorial/section-label.tsx -c | grep -qv "^0$" && rg "text-label" src/components/editorial/section-label.tsx -c | grep -qv "^0$" && rg "uppercase" src/components/editorial/section-label.tsx -c | grep -qv "^0$" && rg "text-ink" src/components/editorial/section-label.tsx -c | grep -qv "^0$" && rg "use client" src/components/editorial/section-label.tsx -c 2>/dev/null; test $? -eq 1 && rg "text-\[|tracking-\[|font-\[" src/components/editorial/section-label.tsx -c 2>/dev/null; test $? -eq 1 && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/components/editorial/section-label.tsx` exits 0
    - `rg "export function SectionLabel" src/components/editorial/section-label.tsx` returns ≥1 hit
    - `rg "text-label" src/components/editorial/section-label.tsx` returns ≥1 hit (D-06 token)
    - `rg "uppercase" src/components/editorial/section-label.tsx` returns ≥1 hit (text-transform)
    - `rg "text-ink" src/components/editorial/section-label.tsx` returns ≥1 hit
    - `rg "num\?:\s*string" src/components/editorial/section-label.tsx` returns ≥1 hit (optional prop signature)
    - `rg "use client" src/components/editorial/section-label.tsx` returns 0 hits (Server Component — D-11)
    - `rg "text-\[|tracking-\[|font-\[" src/components/editorial/section-label.tsx` returns 0 hits (no arbitrary values — D-09)
    - `npm run build` exits 0 (D-18)
  </acceptance_criteria>
  <done>section-label.tsx created; exports SectionLabel; uses text-label + text-ink + uppercase + flex justify-between; optional num prop wired; build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Foundation phase — no trust boundaries crossed. Presentational primitive only.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04-01 | Tampering (V14) | section-label.tsx token reference | mitigate | `npm run build` catches typo'd token names; TypeScript catches missing prop interface |
| T-09-04-SC | Tampering | npm/pip/cargo installs | accept | No new packages added |
</threat_model>

<verification>
- VALIDATION.md task 9-04-V automated checks pass
- npm run build exits 0
</verification>

<success_criteria>
- PRIM-03: `SectionLabel` component exists, accepts `children` + optional `num` prop, renders 11px tracked uppercase via `text-label` + `text-ink` tokens, right-aligns numeral in muted color when present, zero arbitrary values
- `npm run build` exits 0
</success_criteria>

<output>
Create `.planning/phases/09-design-tokens-editorial-primitives/09-04-SUMMARY.md` when done.
</output>
