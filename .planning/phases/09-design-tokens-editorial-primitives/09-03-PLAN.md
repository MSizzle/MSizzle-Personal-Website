---
phase: 09-design-tokens-editorial-primitives
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/editorial/rule-strong.tsx
autonomous: true
requirements: [PRIM-02]
tags: [primitive, editorial, server-component]

must_haves:
  truths:
    - "src/components/editorial/rule-strong.tsx exists and exports RuleStrong as a named function (D-08)"
    - "RuleStrong renders a 1px bold horizontal section divider using the bg-rule-strong token (D-09)"
    - "Component is a Server Component (no 'use client' directive) since it has no interactivity (D-11)"
    - "Zero arbitrary values — no border-[1px], no bg-[#1A1A18] (D-09)"
    - "Preservation contract from Phase 8 untouched (D-15, D-16)"
    - "Honors CONTEXT.md decisions: D-08, D-09, D-11, D-15, D-16"
  artifacts:
    - path: "src/components/editorial/rule-strong.tsx"
      provides: "PRIM-02 — 1px bold horizontal section divider"
      exports: ["RuleStrong"]
  key_links:
    - from: "src/components/editorial/rule-strong.tsx"
      to: "@theme inline --color-rule-strong"
      via: "Tailwind utility bg-rule-strong"
      pattern: "bg-rule-strong"
---

<objective>
Create the `RuleStrong` primitive — a 1px bold horizontal section divider that visually anchors major section boundaries (e.g., between manifesto and Building section on the homepage).

Purpose: Editorial pages use `Rule` (hairline #E5E2D9) for sub-section breaks and `RuleStrong` (ink #1A1A18) for major section anchors. Phase 10 manifesto-to-Building transition depends on this.
Output: `src/components/editorial/rule-strong.tsx` exporting a `RuleStrong` Server Component.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-design-tokens-editorial-primitives/09-CONTEXT.md
@.planning/phases/09-design-tokens-editorial-primitives/09-RESEARCH.md
@.planning/phases/09-design-tokens-editorial-primitives/09-VALIDATION.md
@.planning/research/editorial-redesign-handoff/README.md
@CLAUDE.md
</context>

<interfaces>
RuleStrong prop interface (this plan defines):
```typescript
type Props = {
  className?: string;  // optional override for layout (margin) only — color/thickness fixed
};

export function RuleStrong(props?: Props): JSX.Element;
```
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create src/components/editorial/rule-strong.tsx — RuleStrong primitive (PRIM-02)</name>
  <files>src/components/editorial/rule-strong.tsx</files>
  <read_first>
    - .planning/phases/09-design-tokens-editorial-primitives/09-CONTEXT.md §"Primitive Components" (D-08, D-09, D-11)
    - .planning/phases/09-design-tokens-editorial-primitives/09-RESEARCH.md §"Pattern 3: Server Component primitive" and §"Server Component primitive (Rule — PRIM-01)" (lines 586-596 — same pattern, different token)
    - .planning/research/editorial-redesign-handoff/README.md §"Components Catalog" — `Rule strong` ("1px bold horizontal section divider")
    - .planning/research/editorial-redesign-handoff/README.md §"Color" — `rule-strong #1A1A18` Bold horizontal section dividers (1px)
    - .planning/phases/09-design-tokens-editorial-primitives/09-VALIDATION.md task 9-03-V
  </read_first>
  <action>
Create `src/components/editorial/rule-strong.tsx` mirroring the pattern in `rule.tsx` but using the `bg-rule-strong` token instead of `bg-rule`:

- No 'use client' directive (D-11 — Server Component).
- Named export `RuleStrong` typed as `(props?: { className?: string }) => JSX.Element`.
- Render `<hr aria-hidden="true" className={cn("h-px w-full border-0 bg-rule-strong", className)} />`.
- Import `cn` from `@/utils/cn`.
- Token-driven only: `bg-rule-strong` resolves to `--color-rule-strong: #1A1A18` defined by Plan 09-01 (D-01).
- Zero arbitrary values (D-09).

Note: visual thickness is the same 1px as `Rule` (both use `h-px`). "Strong" refers to the darker ink color (#1A1A18 vs. #E5E2D9), not the thickness. This matches the handoff §"Color" table.
  </action>
  <verify>
    <automated>test -f src/components/editorial/rule-strong.tsx && rg "export function RuleStrong" src/components/editorial/rule-strong.tsx -c | grep -qv "^0$" && rg "bg-rule-strong" src/components/editorial/rule-strong.tsx -c | grep -qv "^0$" && rg "use client" src/components/editorial/rule-strong.tsx -c 2>/dev/null; test $? -eq 1 && rg "border-\[|bg-\[|text-\[|tracking-\[" src/components/editorial/rule-strong.tsx -c 2>/dev/null; test $? -eq 1 && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/components/editorial/rule-strong.tsx` exits 0
    - `rg "export function RuleStrong" src/components/editorial/rule-strong.tsx` returns ≥1 hit
    - `rg "bg-rule-strong" src/components/editorial/rule-strong.tsx` returns ≥1 hit (token-driven — D-09)
    - `rg "use client" src/components/editorial/rule-strong.tsx` returns 0 hits (Server Component — D-11)
    - `rg "border-\[|bg-\[|text-\[|tracking-\[" src/components/editorial/rule-strong.tsx` returns 0 hits (no arbitrary values — D-09)
    - `rg 'aria-hidden="true"' src/components/editorial/rule-strong.tsx` returns ≥1 hit
    - `npm run build` exits 0 (D-18)
  </acceptance_criteria>
  <done>rule-strong.tsx created; exports RuleStrong; uses bg-rule-strong token; build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Foundation phase — no trust boundaries crossed. Presentational primitive only.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03-01 | Tampering (V14) | rule-strong.tsx token reference | mitigate | `npm run build` catches typo'd token names |
| T-09-03-SC | Tampering | npm/pip/cargo installs | accept | No new packages added (RESEARCH.md §"Package Legitimacy Audit") |
</threat_model>

<verification>
- VALIDATION.md task 9-03-V automated checks pass
- npm run build exits 0
</verification>

<success_criteria>
- PRIM-02: `RuleStrong` component exists in `src/components/editorial/rule-strong.tsx`, exports a Server Component using `bg-rule-strong` token, zero arbitrary values
- `npm run build` exits 0
</success_criteria>

<output>
Create `.planning/phases/09-design-tokens-editorial-primitives/09-03-SUMMARY.md` when done.
</output>
