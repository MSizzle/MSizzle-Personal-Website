---
phase: 09-design-tokens-editorial-primitives
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/app/layout.tsx
  - src/components/providers/theme-provider.tsx
  - src/components/theme-toggle.tsx
autonomous: true
requirements: [TOKEN-01, TOKEN-02, TOKEN-03]
tags: [tailwind-v4, design-tokens, typography, theme-provider]

must_haves:
  truths:
    - "globals.css `@theme inline` block contains all 10 warm-paper palette tokens (D-01)"
    - "globals.css `@theme inline` block contains all 10 typography roles including --text-caption (D-06 + D-14a)"
    - "globals.css `.dark { ... }` block is fully removed (D-04)"
    - "v1.0 compat aliases (--bg, --fg, --bg-secondary, --fg-muted, --border, --accent, --accent-warm, --gold) remain in :root pointing at v2.0 hex values so v1.0 pages do not break during Phase 9 (D-02)"
    - "@theme inline keyword preserved (D-03)"
    - "src/app/layout.tsx still loads Inter via next/font/google with weights [\"400\", \"700\"] and CSS variable --font-inter (D-17)"
    - "src/app/layout.tsx no longer wraps children in <ThemeProvider> (D-04)"
    - "suppressHydrationWarning on <html> preserved (D-05)"
    - "src/components/providers/theme-provider.tsx deleted (D-04)"
    - "src/components/theme-toggle.tsx deleted (D-04)"
    - "next-themes package remains in package.json (D-04 — zero-cost retention)"
    - "npm run build exits 0 after rewrite (D-18)"
    - "Honors CONTEXT.md decisions: D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-14a, D-17, D-18"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Tailwind v4 @theme inline with v2.0 palette + type scale + v1.0 alias bridge"
      contains: "--color-paper, --color-ink, --color-muted, --color-rule, --color-rule-strong, --color-footer-bg, --color-footer-fg, --color-footer-mute, --color-footer-rule, --text-display, --text-page-title, --text-feature, --text-event-title, --text-section-feature, --text-list-title, --text-list-title-home, --text-body-lead, --text-caption, --text-nav, --text-label, --text-meta"
    - path: "src/app/layout.tsx"
      provides: "Root layout without ThemeProvider wrap; Inter font loader preserved"
  key_links:
    - from: "src/app/globals.css :root"
      to: "src/app/globals.css @theme inline aliases"
      via: "v1.0 token name → v2.0 hex value bridge"
      pattern: "--bg:\\s*#F4F2EC"
    - from: "src/app/layout.tsx body"
      to: "Tailwind v4 utility classes (bg-paper, text-ink)"
      via: "@theme tokens generate utilities"
      pattern: "bg-background|text-foreground"
---

<objective>
Establish the v2.0 design-token foundation in `src/app/globals.css` and drop dark-mode wiring from `src/app/layout.tsx`.

Purpose: Every downstream phase (10 homepage, 11 archives, 12 sub-page restyle) composes against these tokens. Without this plan, no `text-display`, no `bg-paper`, no `border-rule` utility exists.
Output: A rewritten `@theme inline` block with 10 palette + 10 typography tokens + v1.0 compat aliases; deleted `.dark` block; unwired `<ThemeProvider>`; deleted theme-provider + theme-toggle files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-design-tokens-editorial-primitives/09-CONTEXT.md
@.planning/phases/09-design-tokens-editorial-primitives/09-RESEARCH.md
@.planning/phases/09-design-tokens-editorial-primitives/09-VALIDATION.md
@.planning/research/editorial-redesign-handoff/README.md
@CLAUDE.md
@src/app/globals.css
@src/app/layout.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rewrite globals.css @theme inline block with v2.0 palette + type scale + v1.0 alias bridge</name>
  <files>src/app/globals.css</files>
  <read_first>
    - src/app/globals.css (current 114-line file)
    - .planning/phases/09-design-tokens-editorial-primitives/09-CONTEXT.md §"Token Replacement Strategy" (D-01, D-02, D-03) and §"Type Scale Authoring" (D-06, D-07)
    - .planning/phases/09-design-tokens-editorial-primitives/09-CONTEXT.md D-14a (--text-caption: 13px addition)
    - .planning/research/editorial-redesign-handoff/README.md §"Design Tokens" §"Color" and §"Typography" (canonical hex + size + tracking + weight values)
    - .planning/phases/09-design-tokens-editorial-primitives/09-RESEARCH.md §"Pattern 1: Tailwind v4 @theme inline with var() indirection" and §"Pattern 2: Tailwind v4 typography utility with bundled properties"
  </read_first>
  <action>
Replace the entire `@theme inline { ... }` block (current lines 3–13) with a new block that contains, in order: (a) the 10 v2.0 palette tokens, (b) the 10 typography roles with their --line-height / --letter-spacing / --font-weight modifiers, (c) the font-family declarations preserved from current (--font-sans, --font-mono), (d) the v1.0 compat color aliases pointing at the new tokens via `var()` so old code keeps rendering during the v1.0→v2.0 cutover (D-02 + D-03).

Then replace the `:root { ... }` block (current lines 15–24) so the v1.0 token names hold v2.0 hex values directly (so old `var(--bg)` consumers paint v2.0 colors).

Then DELETE the `.dark { ... }` block (current lines 26–35) entirely (D-04 — no dark mode for v2.0).

Then preserve unchanged: `html { scroll-behavior: auto; }`, `body { ... }`, the `a` rule, the `.prose` rules, and the `.section-inverted` rule. Inside `.section-inverted`, rewrite the hex values from `#2d1724` / `#fffbfc` to the v2.0 ink/paper pair (`#0E0E0C` for bg, `#F4F2EC` for fg) so the inverted band uses the v2.0 palette.

DELETE the `.dark .section-inverted { ... }` rule (current lines 102–113) — dark mode is dropped (D-04).

The exact new `@theme inline` block contents (verbatim from D-01 + D-06 + D-14a):

Palette tokens (10):
  --color-paper: #F4F2EC;
  --color-ink: #0E0E0C;
  --color-muted: #9A9690;
  --color-faint: #C7C3BA;
  --color-rule: #E5E2D9;
  --color-rule-strong: #1A1A18;
  --color-footer-bg: #0E0E0C;
  --color-footer-fg: #F4F2EC;
  --color-footer-mute: #7A7770;
  --color-footer-rule: rgba(244, 242, 236, 0.18);

Typography tokens (10 — display, page-title, feature, event-title, section-feature, list-title, list-title-home, body-lead, caption, nav, label, meta — D-06 + D-14a):
  --text-display: 124px; --text-display--line-height: 0.96; --text-display--letter-spacing: -0.045em; --text-display--font-weight: 700;
  --text-page-title: 120px; --text-page-title--line-height: 0.95; --text-page-title--letter-spacing: -0.045em; --text-page-title--font-weight: 700;
  --text-feature: 44px; --text-feature--line-height: 1.05; --text-feature--letter-spacing: -0.03em; --text-feature--font-weight: 700;
  --text-event-title: 36px; --text-event-title--line-height: 1.1; --text-event-title--letter-spacing: -0.02em; --text-event-title--font-weight: 700;
  --text-section-feature: 28px; --text-section-feature--line-height: 1.15; --text-section-feature--letter-spacing: -0.025em; --text-section-feature--font-weight: 700;
  --text-list-title: 28px; --text-list-title--line-height: 1.2; --text-list-title--letter-spacing: -0.01em; --text-list-title--font-weight: 400;
  --text-list-title-home: 20px; --text-list-title-home--line-height: 1.4; --text-list-title-home--letter-spacing: -0.005em; --text-list-title-home--font-weight: 400;
  --text-body-lead: 22px; --text-body-lead--line-height: 1.55; --text-body-lead--letter-spacing: -0.005em; --text-body-lead--font-weight: 400;
  --text-caption: 13px; --text-caption--line-height: 1.5; --text-caption--font-weight: 400;   /* D-14a */
  --text-nav: 13px; --text-nav--line-height: 1.4; --text-nav--letter-spacing: 0.02em; --text-nav--font-weight: 400;
  --text-label: 11px; --text-label--line-height: 1; --text-label--letter-spacing: 0.2em; --text-label--font-weight: 700;   /* D-07 mid-range */
  --text-meta: 11px; --text-meta--line-height: 1; --text-meta--letter-spacing: 0.16em; --text-meta--font-weight: 400;   /* D-07 mid-range */

Font family (preserved):
  --font-sans: var(--font-inter);
  --font-mono: var(--font-geist-mono);

v1.0 compat aliases (D-02 — point old names at new tokens via var()):
  --color-background: var(--color-paper);
  --color-foreground: var(--color-ink);
  --color-bg-secondary: var(--color-paper);
  --color-fg-muted: var(--color-muted);
  --color-border: var(--color-rule-strong);
  --color-accent: var(--color-ink);
  --color-accent-warm: var(--color-ink);

New :root block (v1.0 token names now hold v2.0 hex values directly so unmigrated code paints v2.0):
  --bg: #F4F2EC;
  --fg: #0E0E0C;
  --bg-secondary: #F4F2EC;
  --fg-muted: #9A9690;
  --border: #1A1A18;
  --accent: #0E0E0C;
  --accent-warm: #0E0E0C;
  --gold: #0E0E0C;

Use the Edit tool (not Write) to preserve byte-equality of unchanged regions (html/body/a/.prose). Do NOT introduce arbitrary values or extra tokens beyond what's listed. Do NOT touch any other files in this task.
  </action>
  <verify>
    <automated>rg "@theme inline" src/app/globals.css | wc -l | grep -q "1" && rg -v '^\s*/\*' src/app/globals.css | rg "\.dark \{" -c 2>/dev/null; test $? -eq 1 && rg -c "color-paper|color-ink|color-rule-strong|color-footer-bg" src/app/globals.css | grep -qv "^0$" && rg -c "text-display|text-page-title|text-label|text-meta|text-caption" src/app/globals.css | grep -qv "^0$" && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `rg "@theme inline" src/app/globals.css` returns 1 hit (D-03 inline keyword preserved)
    - `rg "\.dark \{" src/app/globals.css` returns 0 hits (D-04 dark block deleted)
    - `rg "--color-paper:\s*#F4F2EC" src/app/globals.css` returns ≥1 hit
    - `rg "--color-ink:\s*#0E0E0C" src/app/globals.css` returns ≥1 hit
    - `rg "--color-rule-strong:\s*#1A1A18" src/app/globals.css` returns ≥1 hit
    - `rg "--text-display:\s*124px" src/app/globals.css` returns ≥1 hit
    - `rg "--text-label" src/app/globals.css` returns ≥4 hits (base + 3 modifiers)
    - `rg "--text-caption:\s*13px" src/app/globals.css` returns ≥1 hit (D-14a)
    - `rg "var\(--color-paper\)" src/app/globals.css` returns ≥1 hit (compat alias bridge — D-02)
    - `rg "^\s*--bg:\s*#F4F2EC" src/app/globals.css` returns ≥1 hit (v1.0 alias holds v2.0 value — D-02)
    - `npm run build` exits 0 (D-18)
  </acceptance_criteria>
  <done>globals.css has v2.0 @theme inline block + .dark block removed + alias bridge intact + npm run build green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Remove ThemeProvider wrap from layout.tsx (preserve Inter font loader + suppressHydrationWarning)</name>
  <files>src/app/layout.tsx</files>
  <read_first>
    - src/app/layout.tsx (current 86-line file; ThemeProvider import line 3, wrap lines 68 + 76)
    - .planning/phases/09-design-tokens-editorial-primitives/09-CONTEXT.md §"Dark Mode Disposition" (D-04, D-05) and §"Existing Code" (D-17)
    - .planning/phases/09-design-tokens-editorial-primitives/09-RESEARCH.md §"Layout removal sweep" diagram (lines 228-238)
  </read_first>
  <action>
Edit `src/app/layout.tsx` to remove the `<ThemeProvider>` wrap entirely (D-04). Three edits:

1. Delete line 3: `import { ThemeProvider } from "@/components/providers/theme-provider";`
2. Delete line 68: `        <ThemeProvider>` (the opening tag — note 8-space indent inside `<body>`)
3. Delete line 76: `        </ThemeProvider>` (the closing tag — same indent)

After the edits, the provider chain inside `<body>` reads:
  <LenisProvider>
    <MotionProvider>
      <Navigation />
      <main className="pt-16">{children}</main>
      <Footer />
    </MotionProvider>
  </LenisProvider>
  <UmamiAnalytics />
  <VisitSurvey />
  <span className="fixed right-3 bottom-3 z-50 text-xs opacity-20 select-none">Prometheus</span>

DO NOT touch the Inter font loader at lines 13–17 — TOKEN-03 requires `weight: ["400", "700"]` to remain (D-17). DO NOT touch `suppressHydrationWarning` on `<html>` at line 65 (D-05). DO NOT touch the Geist_Mono loader, the metadata export, the SITE_URL / SITE_TITLE constants, or the LenisProvider / MotionProvider / Navigation / Footer / UmamiAnalytics / VisitSurvey imports.

Re-indent the remaining `<LenisProvider>` and contents so they sit at 8-space indent (one level shallower than they were under ThemeProvider) to keep the file readable.
  </action>
  <verify>
    <automated>rg "ThemeProvider" src/app/layout.tsx | wc -l | grep -q "^0$" && rg 'weight:\s*\["400",\s*"700"\]' src/app/layout.tsx -c | grep -qv "^0$" && rg "suppressHydrationWarning" src/app/layout.tsx -c | grep -qv "^0$" && rg "LenisProvider|MotionProvider|Navigation|Footer|UmamiAnalytics|VisitSurvey" src/app/layout.tsx -c | grep -qv "^0$" && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `rg "ThemeProvider" src/app/layout.tsx` returns 0 hits (no import, no wrap, no closing tag)
    - `rg 'weight:\s*\["400",\s*"700"\]' src/app/layout.tsx` returns 1 hit (Inter 400/700 preserved — TOKEN-03 / D-17)
    - `rg "suppressHydrationWarning" src/app/layout.tsx` returns 1 hit (D-05)
    - `rg "LenisProvider" src/app/layout.tsx` returns ≥2 hits (import + JSX — preserved per D-15)
    - `rg "MotionProvider" src/app/layout.tsx` returns ≥2 hits (preserved)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>layout.tsx wraps children in LenisProvider > MotionProvider only; ThemeProvider gone; Inter 400/700 still loaded; build green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Delete theme-provider.tsx and theme-toggle.tsx (zero consumers verified)</name>
  <files>src/components/providers/theme-provider.tsx, src/components/theme-toggle.tsx</files>
  <read_first>
    - .planning/phases/09-design-tokens-editorial-primitives/09-CONTEXT.md §"Dark Mode Disposition" D-04 ("Delete src/components/providers/theme-provider.tsx and src/components/theme-toggle.tsx")
    - .planning/phases/09-design-tokens-editorial-primitives/09-CONTEXT.md §"Canonical References" — "Researcher verified: ZERO consumer call sites — `rg \"ThemeToggle|theme-toggle\" src/` returns only the definition file. No nav-component sweep needed"
  </read_first>
  <action>
First, verify the no-consumer claim is still true at execution time. Run:
  rg "ThemeToggle|theme-toggle" src/ --type ts --type tsx
The only hit must be the definition file `src/components/theme-toggle.tsx` itself. Run:
  rg "from .*theme-provider|next-themes" src/ --type ts --type tsx
Only consumer should be `src/components/providers/theme-provider.tsx` (the file being deleted). If either check returns an unexpected consumer, STOP and surface the unexpected reference; do not proceed.

Then delete both files:
  rm src/components/providers/theme-provider.tsx
  rm src/components/theme-toggle.tsx

Do NOT remove `next-themes` from package.json — D-04 explicitly keeps the package installed for potential reuse if a future milestone restores dark mode (zero cost to keep).

Re-run the searches to confirm:
  rg "ThemeProvider|ThemeToggle" src/  → returns 0 hits
  rg "theme-toggle|theme-provider" src/  → returns 0 hits
  </action>
  <verify>
    <automated>test ! -f src/components/providers/theme-provider.tsx && test ! -f src/components/theme-toggle.tsx && rg "ThemeProvider|ThemeToggle" src/ -c 2>/dev/null; test $? -eq 1 && rg "theme-toggle|theme-provider" src/ -c 2>/dev/null; test $? -eq 1 && rg '"next-themes"' package.json -c | grep -qv "^0$" && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/components/providers/theme-provider.tsx` returns non-zero (file deleted)
    - `test -f src/components/theme-toggle.tsx` returns non-zero (file deleted)
    - `rg "ThemeProvider|ThemeToggle" src/` returns 0 hits site-wide
    - `rg "theme-toggle|theme-provider" src/` returns 0 hits site-wide
    - `rg '"next-themes"' package.json` returns ≥1 hit (package retained per D-04)
    - `npm run build` exits 0 (no orphaned imports)
  </acceptance_criteria>
  <done>theme-provider.tsx and theme-toggle.tsx removed; zero references remain; next-themes still in deps; build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build → bundle | Tailwind v4 token generation crosses from CSS source into client bundle |

## STRIDE Threat Register

Foundation phase — no new auth, no new input handling, no new network calls, no new state. ASVS V1–V13: N/A. V14 Configuration is the only relevant category and is covered by the per-task `npm run build` gate.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01-01 | Tampering (V14) | globals.css @theme block | mitigate | `npm run build` exits 0 catches malformed CSS or undefined token references before commit (D-18) |
| T-09-01-02 | Information Disclosure (V14) | theme-provider deletion sweep | accept | Researcher verified zero consumer call sites; deletion is safe. Build gate catches any missed import. |
| T-09-01-SC | Tampering | npm/pip/cargo installs | accept | No new packages added. RESEARCH.md §"Package Legitimacy Audit" confirms zero installs in Phase 9. |
</threat_model>

<verification>
- `npm run build` exits 0 after all three tasks
- `rg "@theme inline" src/app/globals.css` returns 1 hit
- `rg "\.dark \{" src/app/globals.css` returns 0 hits
- `rg "ThemeProvider|ThemeToggle" src/` returns 0 hits
- Inter 400/700 still loaded in layout.tsx
- next-themes still in package.json
- Per VALIDATION.md task 9-01-V: all checks green
</verification>

<success_criteria>
- TOKEN-01: 10 warm-paper palette tokens defined in `@theme inline` block of globals.css with the exact hex values from D-01
- TOKEN-02: 10 typography roles (display, page-title, feature, event-title, section-feature, list-title, list-title-home, body-lead, caption, nav, label, meta) defined as Tailwind v4 utilities with bundled size/line-height/letter-spacing/weight per D-06 + D-14a
- TOKEN-03: Inter at weights 400/700 verified loaded in `src/app/layout.tsx` (already in place; this plan does not change the font loader)
- Dark mode dropped per D-04: `.dark` block removed, `<ThemeProvider>` unwired, theme-provider + theme-toggle files deleted
- v1.0 alias bridge present (D-02) so v1.0 pages do not break during Phase 9
- `npm run build` exits 0
</success_criteria>

<output>
Create `.planning/phases/09-design-tokens-editorial-primitives/09-01-SUMMARY.md` when done.
</output>
