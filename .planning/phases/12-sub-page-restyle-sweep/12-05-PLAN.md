---
phase: 12-sub-page-restyle-sweep
plan: 05
type: execute
wave: 2
depends_on: ['12-01']
files_modified:
  - src/app/links/page.tsx
autonomous: true
requirements: ['RESTYLE-04']
must_haves:
  truths:
    - "Visiting /links renders no rounded corners, no shadows, no v1.0 CSS-var aliases in src/app/links/page.tsx"
    - "The h1 'Links' uses text-label uppercase text-muted"
    - "The link list items use text-body-lead (22px) instead of the v1.0 text-3xl sm:text-lg pattern; hover opacity is preserved"
    - "The Newsletter link href points to /newsletter (not /blog — the LINKS array has a stale /blog reference)"
    - "npm run build exits 0"
  artifacts:
    - path: "src/app/links/page.tsx"
      provides: "/links route — RESTYLE-04"
      contains: "text-label"
      min_lines: 40
  key_links:
    - from: "src/app/links/page.tsx"
      to: "LINKS array — outbound hrefs"
      via: "map over LINKS constant"
      pattern: "LINKS"
---

<objective>
Apply the v2.0 warm-paper restyle to `/links` per **RESTYLE-04**.

This is the simplest route in Phase 12 — one file, a few className swaps.

Changes:
1. h1 `text-sm font-normal uppercase tracking-widest` → `text-label uppercase text-muted`
2. Link list anchor `text-3xl underline transition-opacity hover:opacity-60 sm:text-lg` → `text-body-lead text-ink underline transition-opacity hover:opacity-60` (v2.0 type-scale token, consistent size at all breakpoints)
3. Fix the stale LINKS entry: `{ href: '/blog', label: 'Newsletter' }` → `{ href: '/newsletter', label: 'Newsletter' }` — this is a data correction, not a structural change; /blog is the wrong destination for a "Newsletter" link

No structural changes. Breadcrumbs, ScrollReveal, outbound link logic (target/rel/data-umami-event), and page layout preserved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-sub-page-restyle-sweep/12-CONTEXT.md
@.planning/phases/12-sub-page-restyle-sweep/12-RECIPE.md
@src/app/links/page.tsx

<interfaces>
<!-- Current file state (52 lines, already in context) -->

Current h1: className="text-sm font-normal uppercase tracking-widest"
Recipe §2 → className="text-label uppercase text-muted"

Current link anchor: className="text-3xl underline transition-opacity hover:opacity-60 sm:text-lg"
Recipe §1 → className="text-body-lead text-ink underline transition-opacity hover:opacity-60"
(text-3xl was 30px mobile / sm:text-lg was 18px desktop — inconsistent sizing. text-body-lead is 22px consistent.)

Current LINKS array Newsletter entry:
{ href: '/blog', label: 'Newsletter' }
Fix: { href: '/newsletter', label: 'Newsletter' }
(/blog is the writing archive; /newsletter is the Monty Monthly page. The label "Newsletter" maps to /newsletter.)

<!-- ScrollReveal — preserve, only change inner classNames -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle /links — h1 token, link text size, Newsletter href fix</name>
  <files>src/app/links/page.tsx</files>
  <action>
Read `src/app/links/page.tsx` (64 lines, already in context). Apply three changes:

**Change 1 — h1 className** (line 33):
- From: `className="text-sm font-normal uppercase tracking-widest"`
- To: `className="text-label uppercase text-muted"`

**Change 2 — link anchor className** (line 51):
- From: `className="text-3xl underline transition-opacity hover:opacity-60 sm:text-lg"`
- To: `className="text-body-lead text-ink underline transition-opacity hover:opacity-60"`

**Change 3 — LINKS array Newsletter href** (line 24):
- From: `{ href: '/blog', label: 'Newsletter' },`
- To: `{ href: '/newsletter', label: 'Newsletter' },`

No other changes. Breadcrumbs, ScrollReveal, `isHttp`/`isMailto` logic, `data-umami-event` attributes, `target="_blank"`, `rel="noopener noreferrer"` — all stay exactly as-is.

**Run rg gates:**

Negative (expect 0):
```
rg -c 'rounded-(?!full)' src/app/links/page.tsx
rg -c 'shadow-' src/app/links/page.tsx
rg -c 'var\(--bg\)\|var\(--border\)\|var\(--accent\)\|var\(--foreground\)' src/app/links/page.tsx
rg -c 'text-3xl' src/app/links/page.tsx
rg -c 'text-sm font-normal uppercase tracking-widest' src/app/links/page.tsx
```

Positive (expect >= 1):
```
rg -c 'text-label' src/app/links/page.tsx
rg -c 'text-body-lead' src/app/links/page.tsx
rg -c 'text-ink' src/app/links/page.tsx
```

Also verify Newsletter href fix:
```
rg -c "href: '/newsletter'" src/app/links/page.tsx
```
(expect 1; and `rg -c "href: '/blog', label: 'Newsletter'"` should be 0)

Run `npm run build` — expect exit 0.
  </action>
  <verify>
    <automated>rg -c 'text-label' src/app/links/page.tsx &amp;&amp; rg -c 'text-body-lead' src/app/links/page.tsx &amp;&amp; rg -c "href: '/newsletter'" src/app/links/page.tsx &amp;&amp; npm run build</automated>
  </verify>
  <done>
    /links h1 uses text-label uppercase text-muted. Link list anchors use text-body-lead text-ink (22px, consistent breakpoints). Newsletter LINKS entry points to /newsletter. No rounded/shadow/v1.0 CSS-var aliases in the file. npm run build exits 0. RESTYLE-04 met.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static page | /links has no Notion calls; all data is hardcoded in LINKS const |
| Outbound links | Static hrefs to mailto, x.com, linkedin.com, github.com, /newsletter — no user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-05-01 | Tampering (tabnabbing) | Outbound `<a target="_blank">` for external links | accept | Existing `rel="noopener noreferrer"` on isHttp links preserved |
| T-12-05-SC | Tampering | npm/pip/cargo installs | accept | Zero new package installs; styling-only changes |
</threat_model>

<verification>
- `rg -c 'text-label' src/app/links/page.tsx` >= 1
- `rg -c 'text-body-lead' src/app/links/page.tsx` >= 1
- `rg -c "href: '/newsletter'" src/app/links/page.tsx` >= 1 (Newsletter link corrected)
- `rg -c 'text-3xl' src/app/links/page.tsx` = 0 (old arbitrary size gone)
- `rg -c 'rounded-(?!full)' src/app/links/page.tsx` = 0
- `rg -c 'shadow-' src/app/links/page.tsx` = 0
- `rg -c 'var\(--bg\)\|var\(--border\)\|var\(--accent\)' src/app/links/page.tsx` = 0
- `rg -c 'bg-paper\|text-ink\|text-muted\|border-rule' src/app/links/page.tsx` >= 1
- `npm run build` exits 0
</verification>

<success_criteria>
- /links h1 renders in 11px tracked uppercase muted (text-label)
- All link list items render at 22px body-lead size, consistent mobile and desktop
- Newsletter link correctly routes to /newsletter (not /blog)
- Hover opacity effect preserved
- No v1.0 chrome in the file
- RESTYLE-04 requirement met
- Build green
</success_criteria>

<output>
Create `.planning/phases/12-sub-page-restyle-sweep/12-05-SUMMARY.md` when done. Include: the three changes made, rg gate outputs, build result, and a note about the Newsletter href correction.
</output>
