---
phase: 12-sub-page-restyle-sweep
plan: 07
type: execute
wave: 2
depends_on: ['12-01']
files_modified:
  - src/app/newsletter/page.tsx
  - src/components/newsletter/newsletter-carousel.tsx
autonomous: true
requirements: ['RESTYLE-06']
must_haves:
  truths:
    - "Visiting /newsletter renders no rounded corners, no shadows, no v1.0 CSS-var aliases in src/app/newsletter/page.tsx"
    - "The NewsletterCarousel component is no longer imported or used by /newsletter; the issue gallery is a server-rendered grid directly in newsletter/page.tsx"
    - "The issue gallery breaks out of the 66ch reading column to full editorial page width (px-6 md:px-40) and renders a 2-col mobile / 3-col desktop grid showing all issues at once (no horizontal scroll, no arrow buttons)"
    - "The Subscribe CTA button in the intro column uses the v2.0 flat outlined spec (border border-ink px-7 py-3 text-label uppercase) matching the /writing footer"
    - "fetchMontyMonthlyIssues is called with limit 20 (bumped from 10)"
    - "npm run build exits 0 after deletion of carousel + grid implementation"
    - "rg 'NewsletterCarousel' src/ returns 0 hits (no other file imports the component)"
  artifacts:
    - path: "src/app/newsletter/page.tsx"
      provides: "/newsletter route — RESTYLE-06 + D-NEWSLETTER-REDESIGN"
      contains: "grid-cols-2"
      min_lines: 60
    - path: "src/components/newsletter/newsletter-carousel.tsx"
      provides: "Component emptied/deleted — no longer imported"
      contains: "NewsletterCarousel"
      min_lines: 1
  key_links:
    - from: "src/app/newsletter/page.tsx"
      to: "src/lib/rss/substack.ts → fetchMontyMonthlyIssues"
      via: "await fetchMontyMonthlyIssues(20)"
      pattern: "fetchMontyMonthlyIssues"
    - from: "src/app/newsletter/page.tsx"
      to: "https://montymonthly.substack.com"
      via: "Subscribe CTA anchor + issue card links"
      pattern: "montymonthly\\.substack\\.com"
---

<objective>
Apply the v2.0 restyle AND structural redesign to `/newsletter` per **RESTYLE-06 + D-NEWSLETTER-REDESIGN** (operator feedback: "Make it feel full of all the different Monty Monthlys").

This is the heaviest Phase 12 plan — it combines palette/typography restyle with a layout change:

**Task 1 — Safety check:** Before touching any file, run `rg "NewsletterCarousel" src/` to confirm the component is only imported in `newsletter/page.tsx`. If any other file imports it, stop and surface that gap.

**Task 2 — Rebuild `newsletter/page.tsx`:** Replace the carousel-based layout with:
- Intro column (stays `max-w-[66ch]`): restyle h1, prose, Subscribe CTA
- Issue grid (full editorial width, outside the reading column): server-rendered 2-col/3-col grid, all issues visible, v2.0 card chrome

**Task 3 — Delete/tombstone `newsletter-carousel.tsx`:** Once the new page ships and build is green, the carousel component is dead code. Either delete it or replace its body with a comment-only tombstone indicating it was superseded by Plan 12-07. Prefer delete — `rg` confirms no other imports.

Output: updated `src/app/newsletter/page.tsx` + deleted/tombstoned `src/components/newsletter/newsletter-carousel.tsx`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-sub-page-restyle-sweep/12-CONTEXT.md
@.planning/phases/12-sub-page-restyle-sweep/12-RECIPE.md
@.planning/todos/pending/2026-05-21-newsletter-page-issues-prominence-redesign.md
@src/app/newsletter/page.tsx
@src/components/newsletter/newsletter-carousel.tsx
@src/app/writing/page.tsx
@src/app/globals.css

<interfaces>
<!-- Current newsletter/page.tsx — 74 lines in context -->
Current h1: className="text-2xl font-normal tracking-tight sm:text-3xl"
Recipe §2 → className="text-section-feature text-ink"

Current Subscribe CTA (lines 38-45):
  className="mt-6 inline-block rounded-lg border border-[var(--accent)] bg-[var(--accent)] px-6 py-3 text-base font-normal text-white no-underline shadow-md transition-all hover:shadow-lg hover:brightness-110"
Recipe §4 → className="mt-6 inline-block border border-ink px-7 py-3 text-label uppercase text-ink transition-opacity hover:opacity-80 no-underline"

Current section label "Recent Issues" (line 50):
  className="text-sm font-normal uppercase tracking-widest"
Recipe §1 → className="text-label uppercase text-muted"

Current carousel call: fetchMontyMonthlyIssues(10) → bump to fetchMontyMonthlyIssues(20)

<!-- MontyMonthlyIssue type — from src/lib/rss/substack.ts (RESEARCH-verified) -->
interface MontyMonthlyIssue {
  title: string        // "Issue #42: Building in the Void"
  link: string         // "https://montymonthly.substack.com/p/42-..."
  pubDate: string      // RFC822 date string
  thumbnail: string | null
}

<!-- Grid spec from 12-RECIPE.md §5 -->
Issue gallery: breaks outside max-w-[66ch] reading column.
Layout: full-width section with px-6 md:px-40 padding.
Grid: grid grid-cols-2 md:grid-cols-3 gap-4 md:gap-6
Issue card:
  <a href={issue.link} target="_blank" rel="noopener noreferrer"
     className="group block bg-paper border border-rule">
    {/* thumbnail or fallback */}
    <div className="p-4">
      <h3 className="text-list-title text-ink">{issue.title}</h3>
      <time className="mt-2 block text-meta uppercase text-muted">{formatted date}</time>
    </div>
  </a>
Thumbnail: <div className="relative aspect-[4/5] md:aspect-[16/9] overflow-hidden bg-muted">
           <Image src={issue.thumbnail} alt={issue.title} fill className="object-cover saturate-[0.92]" />
           </div>
No-thumbnail fallback: <div className="aspect-[4/5] md:aspect-[16/9] bg-muted" aria-hidden />

<!-- /writing Substack CTA for visual reference (line 167) -->
className="inline-block border border-footer-fg/40 px-7 py-3 text-label uppercase text-footer-fg transition-opacity hover:opacity-80"
Note: /writing uses footer-fg (inverted, dark bg); /newsletter uses ink (light bg). The spec is border/padding/text-label/uppercase — color differs by context.

<!-- Image component required import -->
import Image from 'next/image'
(Already imported via existing newsletter-carousel.tsx; must add to newsletter/page.tsx directly since carousel is deleted)

<!-- Breadcrumbs — sr-only, no change; keep as-is -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Safety check — verify NewsletterCarousel has no other consumers</name>
  <files>src/components/newsletter/newsletter-carousel.tsx</files>
  <action>
Run: `rg "NewsletterCarousel" src/`

Expected result: exactly 2 hits — one in `src/app/newsletter/page.tsx` (import line) and one in `src/components/newsletter/newsletter-carousel.tsx` (export declaration). If any additional file imports `NewsletterCarousel`, stop and report the file path — do not proceed with deletion until that import is resolved.

Also check: `rg "newsletter-carousel" src/` (lowercase dash form — catches any path-based imports).

If both checks return only the expected 2 hits (or 1 hit each), proceed to Task 2.
  </action>
  <verify>
    <automated>rg "NewsletterCarousel" src/ | wc -l</automated>
  </verify>
  <done>
    Confirmed: NewsletterCarousel is imported only by src/app/newsletter/page.tsx (2 total hits across both files). Safe to proceed with deletion.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rebuild newsletter/page.tsx — restyle + grid redesign; delete carousel</name>
  <files>src/app/newsletter/page.tsx, src/components/newsletter/newsletter-carousel.tsx</files>
  <action>
Rewrite `src/app/newsletter/page.tsx` entirely. The page is 74 lines — a full rewrite is cleaner than surgical edits given the structural change (moving the issue gallery outside the reading column).

New file structure:

```
import type { Metadata } from 'next'
import Image from 'next/image'
import { Breadcrumbs } from '@/components/seo/breadcrumbs'
import { fetchMontyMonthlyIssues } from '@/lib/rss/substack'

export const revalidate = 86400 // 24h — unchanged

export const metadata: Metadata = {
  // unchanged from current file — copy verbatim
}

export default async function NewsletterPage() {
  const issues = await fetchMontyMonthlyIssues(20)  // bumped from 10

  return (
    <>
      <Breadcrumbs items={[{ name: 'Home', href: '/' }, { name: 'Monty Monthly' }]} />

      {/* Intro column — stays in max-w-[66ch] reading column */}
      <div className="mx-auto max-w-[66ch] px-6 pt-8 pb-8 md:px-0">
        <h1 className="text-section-feature text-ink uppercase">Monty Monthly</h1>

        <div className="prose mt-8 max-w-none">
          <p>
            Monty Monthly is a monthly newsletter covering what I&rsquo;m building,
            learning, and thinking about. Essays on AI, entrepreneurship, philosophy,
            and life.
          </p>
        </div>

        <a
          href="https://montymonthly.substack.com"
          target="_blank"
          rel="noopener noreferrer"
          className="mt-6 inline-block border border-ink px-7 py-3 text-label uppercase text-ink transition-opacity hover:opacity-80 no-underline"
        >
          Subscribe on Substack &rarr;
        </a>
      </div>

      {/* Issue gallery — breaks out to full editorial page width */}
      {issues.length > 0 ? (
        <section className="px-6 pb-16 md:px-40">
          <div className="text-label uppercase text-muted mb-4">Recent Issues</div>
          <div className="grid grid-cols-2 md:grid-cols-3 gap-4 md:gap-6">
            {issues.map((issue) => (
              <a
                key={issue.link}
                href={issue.link}
                target="_blank"
                rel="noopener noreferrer"
                className="group block bg-paper border border-rule no-underline"
              >
                {issue.thumbnail ? (
                  <div className="relative aspect-[4/5] md:aspect-[16/9] overflow-hidden bg-muted">
                    <Image
                      src={issue.thumbnail}
                      alt={issue.title}
                      fill
                      sizes="(max-width: 768px) 50vw, 33vw"
                      className="object-cover saturate-[0.92]"
                    />
                  </div>
                ) : (
                  <div className="aspect-[4/5] md:aspect-[16/9] bg-muted" aria-hidden />
                )}
                <div className="p-4">
                  <h3 className="text-list-title text-ink">{issue.title}</h3>
                  <time className="mt-2 block text-meta uppercase text-muted">
                    {new Date(issue.pubDate).toLocaleDateString('en-US', {
                      year: 'numeric',
                      month: 'long',
                      day: 'numeric',
                      timeZone: 'UTC',
                    })}
                  </time>
                </div>
              </a>
            ))}
          </div>
        </section>
      ) : (
        <section className="px-6 pb-16 md:px-40">
          <p className="text-muted">
            Recent issues coming soon. In the meantime,{' '}
            <a
              href="https://montymonthly.substack.com"
              target="_blank"
              rel="noopener noreferrer"
              className="underline transition-opacity hover:opacity-60"
            >
              subscribe on Substack
            </a>{' '}
            to catch the next one.
          </p>
        </section>
      )}
    </>
  )
}
```

Key structural decisions:
- `NewsletterCarousel` import is REMOVED (no longer in import list)
- `Image` imported directly from `next/image` in the page (since carousel is deleted)
- Issue gallery is a `<section>` at page level, OUTSIDE the `max-w-[66ch]` intro div
- Full-width via `px-6 md:px-40` (matching Phase 11 archive pages)
- Issue card: `bg-paper border border-rule` (flat, no rounded, no shadow)
- Thumbnail: `saturate-[0.92]` filter per D-07
- Subscribe CTA: matches recipe §4 spec (border-ink, flat outlined, text-label uppercase)

After writing newsletter/page.tsx, delete `src/components/newsletter/newsletter-carousel.tsx` entirely (confirmed safe in Task 1 — no other consumers). Use the file system to delete (or replace file contents with a one-line comment indicating it was deleted in Plan 12-07 if file deletion is not available).

**Run rg gates after both file operations:**

Negative (expect 0 in newsletter/page.tsx):
```
rg -c 'rounded-(?!full)' src/app/newsletter/page.tsx
rg -c 'shadow-' src/app/newsletter/page.tsx
rg -c 'var\(--bg\)\|var\(--border\)\|var\(--accent\)\|var\(--foreground\)' src/app/newsletter/page.tsx
rg -c 'NewsletterCarousel' src/app/newsletter/page.tsx
```

Negative (confirm carousel has no consumers):
```
rg 'NewsletterCarousel' src/
```
(expect 0 hits after deletion; or 1 hit if tombstoned with a comment in the carousel file itself)

Positive (expect >= 1 in newsletter/page.tsx):
```
rg -c 'grid-cols-2' src/app/newsletter/page.tsx
rg -c 'text-list-title' src/app/newsletter/page.tsx
rg -c 'text-meta' src/app/newsletter/page.tsx
rg -c 'border-rule' src/app/newsletter/page.tsx
rg -c 'border border-ink' src/app/newsletter/page.tsx
rg -c 'fetchMontyMonthlyIssues(20)' src/app/newsletter/page.tsx
```

Run `npm run build` — expect exit 0. The carousel component deletion must not cause a build error.
  </action>
  <verify>
    <automated>rg -c 'grid-cols-2' src/app/newsletter/page.tsx &amp;&amp; rg -c 'border-rule' src/app/newsletter/page.tsx &amp;&amp; rg -c 'border border-ink' src/app/newsletter/page.tsx &amp;&amp; ! rg -q 'NewsletterCarousel' src/app/newsletter/page.tsx &amp;&amp; npm run build</automated>
  </verify>
  <done>
    newsletter/page.tsx rebuilt: server-rendered issue grid (2-col/3-col), full editorial page width, flat v2.0 card chrome, bumped to fetchMontyMonthlyIssues(20). Subscribe CTA uses flat outlined spec. NewsletterCarousel removed from import and component deleted. No v1.0 vocabulary in the page file. npm run build exits 0. RESTYLE-06 + D-NEWSLETTER-REDESIGN met.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server → Substack RSS | `fetchMontyMonthlyIssues(20)` — server-side RSS fetch; no user input; limit bump from 10 to 20 is bounded |
| Issue card hrefs | `issue.link` comes from RSS feed controlled by Monty's Substack; not user-supplied |
| Issue thumbnails | `issue.thumbnail` comes from RSS enclosure or img extraction; rendered via next/image with fill + sizes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-07-01 | Tampering (tabnabbing) | Issue card `<a target="_blank">` links | mitigate | `rel="noopener noreferrer"` on all outbound links (Substack hrefs) |
| T-12-07-02 | Tampering (open redirect) | `issue.link` used directly as href | accept | link is sourced from Substack RSS (author-controlled feed); not user input; no sanitization needed beyond what Next.js provides |
| T-12-07-03 | DoS (RSS timeout) | `fetchMontyMonthlyIssues(20)` — increased limit | accept | Substack RSS is a public feed; fetch timeout handled by existing fetcher; if slow, ISR revalidate=86400 means failures are rare and cached |
| T-12-07-04 | Tampering (XSS via issue.title) | `{issue.title}` in h3 | accept | React auto-escapes string children; no dangerouslySetInnerHTML |
| T-12-07-SC | Tampering | npm/pip/cargo installs | accept | Zero new package installs — Image already in next/image (project dep); no new deps |
</threat_model>

<verification>
- `rg -c 'grid-cols-2' src/app/newsletter/page.tsx` >= 1 (grid present)
- `rg -c 'md:grid-cols-3' src/app/newsletter/page.tsx` >= 1 (3-col desktop)
- `rg -c 'border-rule' src/app/newsletter/page.tsx` >= 1 (flat card border)
- `rg -c 'border border-ink' src/app/newsletter/page.tsx` >= 1 (Subscribe CTA spec)
- `rg -c 'text-list-title' src/app/newsletter/page.tsx` >= 1 (card issue title)
- `rg -c 'text-meta' src/app/newsletter/page.tsx` >= 1 (card date)
- `rg -c 'fetchMontyMonthlyIssues' src/app/newsletter/page.tsx` >= 1 (fetch call exists)
- `rg -c 'rounded-(?!full)' src/app/newsletter/page.tsx` = 0
- `rg -c 'shadow-' src/app/newsletter/page.tsx` = 0
- `rg -c 'var\(--bg\)\|var\(--border\)\|var\(--accent\)' src/app/newsletter/page.tsx` = 0
- `rg -c 'NewsletterCarousel' src/app/newsletter/page.tsx` = 0 (import removed)
- `rg 'NewsletterCarousel' src/` = 0 hits (component deleted or tombstoned)
- `rg -c 'bg-paper\|text-ink\|text-muted\|border-rule' src/app/newsletter/page.tsx` >= 1
- `npm run build` exits 0
</verification>

<success_criteria>
- /newsletter intro column: h1 renders at text-section-feature uppercase; Subscribe CTA is flat outlined (border-ink, no fill, no rounded, no shadow)
- Issue gallery: full-width section (outside 66ch column), 2-col mobile / 3-col desktop grid, all issues visible at once (no scroll, no arrows)
- Issue cards: flat bg-paper + border-rule, poster thumbnails (4/5 mobile / 16/9 desktop aspect), title in text-list-title, date in text-meta uppercase
- Photo treatment: saturate-[0.92] on thumbnails (D-07)
- NewsletterCarousel completely removed — no import, no reference, component file deleted
- fetchMontyMonthlyIssues called with limit 20
- No v1.0 chrome in newsletter/page.tsx
- RESTYLE-06 requirement met + D-NEWSLETTER-REDESIGN operator feedback satisfied
- Build green
- Todo file `.planning/todos/pending/2026-05-21-newsletter-page-issues-prominence-redesign.md` can be moved to `.planning/todos/completed/` after SUMMARY is written
</success_criteria>

<output>
Create `.planning/phases/12-sub-page-restyle-sweep/12-07-SUMMARY.md` when done. Include:
- Confirmation that NewsletterCarousel rg check returned only expected 2 hits before deletion
- Full list of structural changes made (page layout, grid spec, Subscribe CTA)
- rg gate outputs (positive + negative)
- Build result
- Confirmation newsletter-carousel.tsx was deleted (or tombstoned if deletion not possible)
- Move `.planning/todos/pending/2026-05-21-newsletter-page-issues-prominence-redesign.md` to `.planning/todos/completed/2026-05-21-newsletter-page-issues-prominence-redesign.md`
</output>
