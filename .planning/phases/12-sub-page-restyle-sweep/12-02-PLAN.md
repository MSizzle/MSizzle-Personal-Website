---
phase: 12-sub-page-restyle-sweep
plan: 02
type: execute
wave: 2
depends_on: ['12-01']
files_modified:
  - src/app/about/page.tsx
autonomous: true
requirements: ['RESTYLE-01']
must_haves:
  truths:
    - "Visiting /about renders the page with no rounded corners, no shadows, no v1.0 CSS-var aliases (var(--bg), var(--border), var(--accent)) in its source file"
    - "The h1 'About' uses text-label uppercase text-muted (11px tracked), not the v1.0 text-sm font-normal uppercase tracking-widest pattern"
    - "The prose block renders correctly in ink color via the global .prose override — confirmed via manual dev server smoke test"
    - "npm run build exits 0 after the restyle"
  artifacts:
    - path: "src/app/about/page.tsx"
      provides: "/about route — RESTYLE-01"
      contains: "text-label"
      min_lines: 30
  key_links:
    - from: "src/app/about/page.tsx"
      to: "src/app/globals.css .prose block"
      via: "className='prose mt-8 max-w-none'"
      pattern: "prose"
---

<objective>
Apply the v2.0 warm-paper restyle to `/about` per **RESTYLE-01**.

This is a palette + typography pass only — the existing layout (Breadcrumbs + h1 + prose) is preserved per CONTEXT.md D-06 and the RESTYLE-01 success criterion. No structural changes.

Concretely:
1. h1 `text-sm font-normal uppercase tracking-widest` → `text-label uppercase text-muted` (per 12-RECIPE.md §2)
2. Prose block already renders correctly via global `.prose` override in globals.css — verify on dev server, no class changes needed
3. No v1.0 vocabulary (`rounded-*`, `shadow-*`, `bg-[var(--bg)]`, `text-[var(--accent)]`) exists in the current file — confirm via rg gates, then confirm the h1 swap is the sole change
4. The `/blog` href in the prose body (`<a href="/blog">essays...`) — leave as-is per D-04 (URL preservation, no redirect scope in Phase 12)

Output: updated `src/app/about/page.tsx`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-sub-page-restyle-sweep/12-CONTEXT.md
@.planning/phases/12-sub-page-restyle-sweep/12-RECIPE.md
@src/app/about/page.tsx
@src/app/globals.css

<interfaces>
<!-- Phase 9 prose override — already applied globally; no per-file action needed -->
From src/app/globals.css lines 118-130:
.prose block sets --tw-prose-body, --tw-prose-headings, --tw-prose-links to var(--fg) = #0E0E0C (ink).
h2 in prose: font-weight 400, uppercase, 1rem, letter-spacing 0.05em.
No additional prose className needed in the route file.

<!-- Current h1 pattern being replaced -->
Current: className="text-sm font-normal uppercase tracking-widest"
Recipe replacement (§2): className="text-label uppercase text-muted"

<!-- Breadcrumbs — sr-only, no visual change -->
<Breadcrumbs> renders as sr-only per src/components/seo/breadcrumbs.tsx. No className change per recipe §6.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle /about — swap h1 to v2.0 token; verify rg gates</name>
  <files>src/app/about/page.tsx</files>
  <action>
Read `src/app/about/page.tsx` (already in context above — 52 lines). Apply the following single change:

**Change h1 className** (line 24):

From:
```
className="text-sm font-normal uppercase tracking-widest"
```
To:
```
className="text-label uppercase text-muted"
```

No other changes to the file. The prose block (`className="prose mt-8 max-w-none"`) is correct as-is — the global `.prose` override in `src/app/globals.css` already maps all prose colors to ink via `--tw-prose-*: var(--fg)`. The `/blog` href in the prose body stays (D-04 URL preservation).

**Verify via rg gates before committing:**

Negative gates (expect 0):
- `rg -c 'rounded-(?!full)' src/app/about/page.tsx` → 0 (file never had rounded corners; confirm)
- `rg -c 'shadow-' src/app/about/page.tsx` → 0
- `rg -c 'var\(--bg\)\|var\(--border\)\|var\(--accent\)\|var\(--foreground\)' src/app/about/page.tsx` → 0
- `rg -c 'text-sm font-normal uppercase tracking-widest' src/app/about/page.tsx` → 0 (old h1 gone)

Positive gate (expect >= 1):
- `rg -c 'text-label\|text-muted\|text-ink\|bg-paper\|border-rule' src/app/about/page.tsx` → >= 1

If any negative gate returns > 0, fix the residual v1.0 class before proceeding.

Run `npm run build` — expect exit 0.
  </action>
  <verify>
    <automated>rg -c 'text-label' src/app/about/page.tsx &amp;&amp; rg -v 'rounded-(?!full)' src/app/about/page.tsx | rg -c 'rounded-' || true &amp;&amp; npm run build</automated>
  </verify>
  <done>
    `src/app/about/page.tsx` h1 uses `text-label uppercase text-muted`. No rounded-*, shadow-*, or v1.0 CSS-var aliases in the file. npm run build exits 0. Prose block renders in ink color (manual dev server smoke confirms). RESTYLE-01 success criteria met.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server → Notion (none) | /about is static — no Notion API calls |
| Prose outbound links | External links (prometheus.today, montymonthly.substack.com) are static hrefs, no user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-02-01 | Tampering (tabnabbing) | Outbound `<a target="_blank">` in prose | accept | Already has `rel="noopener noreferrer"` per existing code; no change |
| T-12-02-SC | Tampering | npm/pip/cargo installs | accept | Zero new package installs; styling-only change |
</threat_model>

<verification>
- `rg -c 'text-label' src/app/about/page.tsx` >= 1 (v2.0 h1 token present)
- `rg -c 'text-sm font-normal uppercase tracking-widest' src/app/about/page.tsx` = 0 (v1.0 h1 gone)
- `rg -c 'rounded-(?!full)' src/app/about/page.tsx` = 0
- `rg -c 'shadow-' src/app/about/page.tsx` = 0
- `rg -c 'var\(--bg\)\|var\(--border\)\|var\(--accent\)\|var\(--foreground\)' src/app/about/page.tsx` = 0
- `rg -c 'bg-paper\|text-ink\|text-muted\|border-rule' src/app/about/page.tsx` >= 1
- `npm run build` exits 0
</verification>

<success_criteria>
- /about h1 visually renders in 11px tracked uppercase muted color (text-label)
- Prose body text, h2s, and links render in ink (#0E0E0C) via global .prose override
- No v1.0 visual chrome on the page (no rounded, no shadow, no accent color)
- RESTYLE-01 requirement met
- Build green
</success_criteria>

<output>
Create `.planning/phases/12-sub-page-restyle-sweep/12-02-SUMMARY.md` when done. Include: the single className change made, rg gate outputs, build result, and manual smoke test note.
</output>
