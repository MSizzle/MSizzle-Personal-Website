---
phase: 12-sub-page-restyle-sweep
plan: 06
type: execute
wave: 2
depends_on: ['12-01']
files_modified:
  - src/app/prometheus/page.tsx
autonomous: true
requirements: ['RESTYLE-05']
must_haves:
  truths:
    - "Visiting /prometheus renders no rounded corners, no shadows, no v1.0 CSS-var aliases in src/app/prometheus/page.tsx"
    - "The h1 'Prometheus' uses text-section-feature text-ink (28px bold tracked)"
    - "The subtitle 'AI Integrations and Education' uses text-label uppercase text-muted"
    - "The prose block renders correctly in ink color via global .prose override; no per-file prose overrides needed"
    - "npm run build exits 0; JsonLd and FAQ schema generation untouched"
  artifacts:
    - path: "src/app/prometheus/page.tsx"
      provides: "/prometheus route — RESTYLE-05"
      contains: "text-section-feature"
      min_lines: 60
  key_links:
    - from: "src/app/prometheus/page.tsx"
      to: "src/app/globals.css .prose block"
      via: "className='prose mt-8 max-w-none'"
      pattern: "prose"
    - from: "src/app/prometheus/page.tsx"
      to: "src/lib/seo/schemas.ts → buildFaqPageSchema"
      via: "JsonLd + FAQS array"
      pattern: "JsonLd"
---

<objective>
Apply the v2.0 warm-paper restyle to `/prometheus` per **RESTYLE-05**.

One file, targeted typography swaps on h1 and subtitle. The prose body, FAQ JSON-LD, and all layout structure are preserved.

Changes:
1. h1 `text-2xl font-normal tracking-tight sm:text-3xl` → `text-section-feature text-ink` (per recipe §2)
2. Subtitle p `text-sm uppercase tracking-widest opacity-75` → `text-label uppercase text-muted` (per recipe §1)
3. The `prose mt-8 max-w-none` prose block is already correct — global `.prose` override in globals.css maps all prose colors to ink. No per-file changes needed.

No structural changes. Breadcrumbs, JsonLd, FAQS array, external links (prometheus.today, mailto), and article layout preserved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-sub-page-restyle-sweep/12-CONTEXT.md
@.planning/phases/12-sub-page-restyle-sweep/12-RECIPE.md
@src/app/prometheus/page.tsx
@src/app/globals.css

<interfaces>
<!-- Current file state (96 lines, in context) -->

Current h1: className="text-2xl font-normal tracking-tight sm:text-3xl"
Recipe §2 → className="text-section-feature text-ink"

Current subtitle p: className="mt-2 text-sm uppercase tracking-widest opacity-75"
Recipe §1 → className="mt-2 text-label uppercase text-muted"

<!-- Prose block — already correct; no change -->
className="prose mt-8 max-w-none"
Global .prose override in globals.css maps --tw-prose-body, --tw-prose-headings, --tw-prose-links to var(--fg) = ink.
Manual verify required: open dev server, confirm h2, p, ul, a render in #0E0E0C (ink).

<!-- JsonLd + buildFaqPageSchema — DO NOT touch -->
FAQ schema is SEO infrastructure. No changes to FAQS array, JsonLd, or src/lib/seo/schemas.ts.

<!-- External links in prose — already compliant -->
prometheus.today and mailto links have rel="noopener noreferrer" already. No changes needed.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle /prometheus — h1 and subtitle token swap</name>
  <files>src/app/prometheus/page.tsx</files>
  <action>
Read `src/app/prometheus/page.tsx` (96 lines, already in context). Apply two targeted edits:

**Change 1 — h1 className** (line 49):
- From: `className="text-2xl font-normal tracking-tight sm:text-3xl"`
- To: `className="text-section-feature text-ink"`

**Change 2 — subtitle p className** (line 50-51):
- From: `className="mt-2 text-sm uppercase tracking-widest opacity-75"`
- To: `className="mt-2 text-label uppercase text-muted"`

No other changes. Article wrapper, Breadcrumbs, JsonLd, FAQS const, prose block, internal/external links, metadata — all stay exactly as-is.

**Run rg gates:**

Negative (expect 0):
```
rg -c 'rounded-(?!full)' src/app/prometheus/page.tsx
rg -c 'shadow-' src/app/prometheus/page.tsx
rg -c 'var\(--bg\)\|var\(--border\)\|var\(--accent\)\|var\(--foreground\)' src/app/prometheus/page.tsx
rg -c 'text-2xl font-normal tracking-tight' src/app/prometheus/page.tsx
rg -c 'text-sm uppercase tracking-widest' src/app/prometheus/page.tsx
```

Positive (expect >= 1):
```
rg -c 'text-section-feature' src/app/prometheus/page.tsx
rg -c 'text-label' src/app/prometheus/page.tsx
rg -c 'text-muted' src/app/prometheus/page.tsx
rg -c 'text-ink' src/app/prometheus/page.tsx
```

Run `npm run build` — expect exit 0.
  </action>
  <verify>
    <automated>rg -c 'text-section-feature' src/app/prometheus/page.tsx &amp;&amp; rg -c 'text-label' src/app/prometheus/page.tsx &amp;&amp; npm run build</automated>
  </verify>
  <done>
    /prometheus h1 uses text-section-feature text-ink (28px bold). Subtitle uses text-label uppercase text-muted (11px tracked). No rounded/shadow/v1.0 CSS-var aliases in the file. JsonLd and FAQ schema untouched. npm run build exits 0. Prose body renders in ink via global override (manual dev server confirm). RESTYLE-05 met.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static page | /prometheus has no Notion API calls; all content is hardcoded |
| Outbound links | prometheus.today, mailto — static literals, no user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-06-01 | Tampering (tabnabbing) | prometheus.today `<a target="_blank">` in prose | accept | Existing `rel="noopener noreferrer"` preserved |
| T-12-06-02 | Information disclosure | JsonLd FAQ schema in page output | accept | FAQS is static author-controlled content; no user data in schema |
| T-12-06-SC | Tampering | npm/pip/cargo installs | accept | Zero new package installs; styling-only changes |
</threat_model>

<verification>
- `rg -c 'text-section-feature' src/app/prometheus/page.tsx` >= 1
- `rg -c 'text-label' src/app/prometheus/page.tsx` >= 1
- `rg -c 'text-muted' src/app/prometheus/page.tsx` >= 1
- `rg -c 'text-2xl font-normal tracking-tight' src/app/prometheus/page.tsx` = 0
- `rg -c 'text-sm uppercase tracking-widest' src/app/prometheus/page.tsx` = 0
- `rg -c 'rounded-(?!full)' src/app/prometheus/page.tsx` = 0
- `rg -c 'shadow-' src/app/prometheus/page.tsx` = 0
- `rg -c 'var\(--bg\)\|var\(--border\)\|var\(--accent\)' src/app/prometheus/page.tsx` = 0
- `rg -c 'bg-paper\|text-ink\|text-muted\|border-rule' src/app/prometheus/page.tsx` >= 1
- `npm run build` exits 0
</verification>

<success_criteria>
- /prometheus h1 renders at 28px bold tracked (text-section-feature text-ink)
- Subtitle renders at 11px tracked uppercase muted (text-label)
- Prose body, h2s, lists, links render in ink via global .prose override
- JsonLd FAQ schema unchanged (no SEO regression)
- No v1.0 chrome in the file
- RESTYLE-05 requirement met
- Build green
</success_criteria>

<output>
Create `.planning/phases/12-sub-page-restyle-sweep/12-06-SUMMARY.md` when done. Include: the two className changes made, rg gate outputs, build result, and manual dev server smoke test confirmation for prose rendering.
</output>
