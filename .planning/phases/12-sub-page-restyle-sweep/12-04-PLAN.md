---
phase: 12-sub-page-restyle-sweep
plan: 04
type: execute
wave: 2
depends_on: ['12-01']
files_modified:
  - src/app/blog/page.tsx
  - src/app/blog/[slug]/page.tsx
autonomous: true
requirements: ['RESTYLE-03']
must_haves:
  truths:
    - "Visiting /blog renders no rounded corners, no shadows, no v1.0 CSS-var aliases in src/app/blog/page.tsx; h1 uses text-label uppercase text-muted"
    - "Visiting /blog/[slug] renders the post title with text-section-feature text-ink; date/meta row uses text-muted instead of opacity-75; prose body renders in ink via global override"
    - "TagFilter, NewsletterCta, and RelatedEssays components are NOT modified — only the page-level className patterns in the two route files are changed"
    - "npm run build exits 0; /blog/[slug] post permalinks are unchanged (D-04)"
  artifacts:
    - path: "src/app/blog/page.tsx"
      provides: "/blog index — RESTYLE-03 (index)"
      contains: "text-label"
      min_lines: 50
    - path: "src/app/blog/[slug]/page.tsx"
      provides: "/blog/[slug] post — RESTYLE-03 (detail)"
      contains: "text-section-feature"
      min_lines: 80
  key_links:
    - from: "src/app/blog/[slug]/page.tsx"
      to: "src/app/globals.css .prose block"
      via: "className='prose max-w-none'"
      pattern: "prose"
    - from: "src/app/blog/page.tsx"
      to: "src/components/blog/tag-filter.tsx"
      via: "import { TagFilter }"
      pattern: "TagFilter"
---

<objective>
Apply the v2.0 warm-paper restyle to `/blog` (index + `[slug]`) per **RESTYLE-03**.

Two files touched; shared components (TagFilter, NewsletterCta, RelatedEssays) are intentionally NOT modified.

Concretely:

**`src/app/blog/page.tsx` (index):**
- h1 `text-sm font-normal uppercase tracking-widest` → `text-label uppercase text-muted`
- Container `max-w-3xl` → `max-w-[66ch]` (alignment with other sub-pages; was divergent)
- pt-24 → pt-8 (matches /about, /projects, /links consistent top padding)
- No changes to TagFilter wrapper or post logic

**`src/app/blog/[slug]/page.tsx` (detail):**
- h1 `text-2xl font-normal tracking-tight sm:text-3xl` → `text-section-feature text-ink`
- Meta row `text-sm opacity-75` → `text-sm text-muted` (replace opacity hack with token)
- Description p `text-lg opacity-80` → `text-muted` (replace opacity with token)
- Tag spans `text-sm` → `text-sm text-muted`
- `pt-24` on article wrapper → `pt-8` (consistent with other sub-pages; was inherited)
- Prose block `className="prose max-w-none"` — correct as-is, global override handles color

No changes to: Breadcrumbs, permalink shapes, feed.xml, tag filter, RSS, OG image, `generateStaticParams`, NotionRenderer, NewsletterCta, RelatedEssays. D-04 preserved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-sub-page-restyle-sweep/12-CONTEXT.md
@.planning/phases/12-sub-page-restyle-sweep/12-RECIPE.md
@src/app/blog/page.tsx
@src/app/blog/[slug]/page.tsx
@src/app/globals.css

<interfaces>
<!-- /blog index current patterns -->
Current h1: className="text-sm font-normal uppercase tracking-widest"
Recipe §2 → className="text-label uppercase text-muted"

Current container: className="mx-auto max-w-3xl px-6 pb-16 pt-24 md:px-0"
Replace with: className="mx-auto max-w-[66ch] px-6 pb-16 pt-8 md:px-0"
(max-w-3xl → max-w-[66ch] for consistency with other sub-pages; pt-24 → pt-8)

<!-- /blog/[slug] current patterns -->
Current h1: className="text-2xl font-normal tracking-tight sm:text-3xl"
Recipe §2 → className="text-section-feature text-ink"

Current article wrapper: className="mx-auto max-w-[66ch] px-6 pb-16 pt-24 md:px-0"
Change pt-24 → pt-8 (consistent with other sub-pages)

Current meta row: className="mt-4 flex items-center gap-4 text-sm opacity-75"
Change opacity-75 → text-muted: className="mt-4 flex items-center gap-4 text-sm text-muted"

Current description p: className="mt-4 text-lg opacity-80"
Change: className="mt-4 text-lg text-muted"

Current tag span: className="text-sm"
Change: className="text-sm text-muted"

<!-- TagFilter, NewsletterCta, RelatedEssays — DO NOT modify -->
These components live in src/components/blog/ — they are NOT in files_modified and MUST NOT be touched.
If restyle is needed inside those components, that is a separate gap item for Phase 12 follow-up.

<!-- Prose on /blog/[slug] — global .prose override handles it -->
className="prose max-w-none" — correct as-is; no changes
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle /blog index and /blog/[slug] — typography + token swap</name>
  <files>src/app/blog/page.tsx, src/app/blog/[slug]/page.tsx</files>
  <action>
Apply targeted edits to the two route files. Do NOT touch any files in `src/components/blog/`.

**File A — `src/app/blog/page.tsx`**

A.1. Change h1 className (inside the first `<ScrollReveal>`, line 54):
- From: `className="text-sm font-normal uppercase tracking-widest"`
- To: `className="text-label uppercase text-muted"`

A.2. Change container div className (line 52):
- From: `className="mx-auto max-w-3xl px-6 pb-16 pt-24 md:px-0"`
- To: `className="mx-auto max-w-[66ch] px-6 pb-16 pt-8 md:px-0"`

No other changes. TagFilter, Suspense wrapper, and Breadcrumbs stay untouched.

**File B — `src/app/blog/[slug]/page.tsx`**

B.1. Change article wrapper className (line 57):
- From: `className="mx-auto max-w-[66ch] px-6 pb-16 pt-24 md:px-0"`
- To: `className="mx-auto max-w-[66ch] px-6 pb-16 pt-8 md:px-0"`

B.2. Change h1 className (line 59):
- From: `className="text-2xl font-normal tracking-tight sm:text-3xl"`
- To: `className="text-section-feature text-ink"`

B.3. Change meta row div className (line 62):
- From: `className="mt-4 flex items-center gap-4 text-sm opacity-75"`
- To: `className="mt-4 flex items-center gap-4 text-sm text-muted"`

B.4. Change description p className (line 88):
- From: `className="mt-4 text-lg opacity-80"`
- To: `className="mt-4 text-lg text-muted"`

B.5. Change tag span className (line 79):
- From: `className="text-sm"`
- To: `className="text-sm text-muted"`

No other changes. Breadcrumbs, `generateStaticParams`, `generateMetadata`, NotionRenderer, prose block, `NewsletterCta`, `RelatedEssays` — all stay exactly as-is.

**Run rg gates after both edits:**

Negative (expect 0):
```
rg -c 'rounded-(?!full)' src/app/blog/page.tsx "src/app/blog/[slug]/page.tsx"
rg -c 'shadow-' src/app/blog/page.tsx "src/app/blog/[slug]/page.tsx"
rg -c 'var\(--bg\)\|var\(--border\)\|var\(--accent\)\|var\(--foreground\)' src/app/blog/page.tsx "src/app/blog/[slug]/page.tsx"
rg -c 'text-sm font-normal uppercase tracking-widest' src/app/blog/page.tsx
rg -c 'text-2xl font-normal tracking-tight' "src/app/blog/[slug]/page.tsx"
```

Positive (expect >= 1):
```
rg -c 'text-label' src/app/blog/page.tsx
rg -c 'text-section-feature' "src/app/blog/[slug]/page.tsx"
rg -c 'text-muted' "src/app/blog/[slug]/page.tsx"
```

Run `npm run build` — expect exit 0.
  </action>
  <verify>
    <automated>rg -c 'text-label' src/app/blog/page.tsx &amp;&amp; rg -c 'text-section-feature' "src/app/blog/[slug]/page.tsx" &amp;&amp; rg -c 'text-muted' "src/app/blog/[slug]/page.tsx" &amp;&amp; npm run build</automated>
  </verify>
  <done>
    /blog index h1 uses text-label uppercase text-muted; container uses max-w-[66ch] with pt-8. /blog/[slug] h1 uses text-section-feature text-ink; meta/description/tags use text-muted. No v1.0 CSS-var aliases or rounded/shadow in either route file. npm run build exits 0. TagFilter, NewsletterCta, RelatedEssays untouched. RESTYLE-03 met.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server → Notion API | `getPublishedPosts()`, `getPostBySlug()`, `getBlocks()` — server-side only |
| Slug param → URL | `notFound()` guard; `generateStaticParams` pre-generates known slugs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-04-01 | Tampering (XSS) | post.title, post.description, post.tags as React children | accept | React auto-escapes; no dangerouslySetInnerHTML in route files |
| T-12-04-SC | Tampering | npm/pip/cargo installs | accept | Zero new package installs; styling-only changes |
</threat_model>

<verification>
- `rg -c 'text-label' src/app/blog/page.tsx` >= 1
- `rg -c 'max-w-\[66ch\]' src/app/blog/page.tsx` >= 1 (was max-w-3xl)
- `rg -c 'text-section-feature' "src/app/blog/[slug]/page.tsx"` >= 1
- `rg -c 'text-muted' "src/app/blog/[slug]/page.tsx"` >= 1
- `rg -c 'rounded-(?!full)' src/app/blog/page.tsx "src/app/blog/[slug]/page.tsx"` = 0
- `rg -c 'shadow-' src/app/blog/page.tsx "src/app/blog/[slug]/page.tsx"` = 0
- `rg -c 'var\(--bg\)\|var\(--border\)\|var\(--accent\)' src/app/blog/page.tsx "src/app/blog/[slug]/page.tsx"` = 0
- `rg -c 'bg-paper\|text-ink\|text-muted\|border-rule' "src/app/blog/[slug]/page.tsx"` >= 1
- `npm run build` exits 0
</verification>

<success_criteria>
- /blog index: h1 renders in 11px tracked uppercase muted; container width matches other sub-pages (66ch)
- /blog/[slug]: post title renders at 28px bold tracked (text-section-feature); meta row, description, tags render in muted token
- Prose body renders in ink color (global .prose override)
- TagFilter, NewsletterCta, RelatedEssays untouched
- Feed.xml and /blog/[slug] permalinks unchanged (D-04)
- RESTYLE-03 requirement met
- Build green
</success_criteria>

<output>
Create `.planning/phases/12-sub-page-restyle-sweep/12-04-SUMMARY.md` when done. Include: all className changes made in each file, rg gate outputs, build result, and confirmation that component files in src/components/blog/ were not modified.
</output>
