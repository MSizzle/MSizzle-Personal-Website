---
phase: 12-sub-page-restyle-sweep
plan: 03
type: execute
wave: 2
depends_on: ['12-01']
files_modified:
  - src/app/projects/page.tsx
  - src/app/projects/[slug]/page.tsx
  - src/components/projects/project-card.tsx
autonomous: true
requirements: ['RESTYLE-02']
must_haves:
  truths:
    - "Visiting /projects renders no rounded corners, no shadows, no v1.0 CSS-var aliases in src/app/projects/page.tsx or src/components/projects/project-card.tsx"
    - "ProjectCard uses border-rule (hairline) instead of border-[var(--border)] for row dividers"
    - "Visiting /projects/[slug] renders the hero image without rounded-lg wrapper; h1 uses text-section-feature text-ink"
    - "npm run build exits 0 after the restyle"
  artifacts:
    - path: "src/app/projects/page.tsx"
      provides: "/projects index — RESTYLE-02 (index)"
      contains: "text-label"
      min_lines: 30
    - path: "src/app/projects/[slug]/page.tsx"
      provides: "/projects/[slug] detail — RESTYLE-02 (detail)"
      contains: "text-section-feature"
      min_lines: 60
    - path: "src/components/projects/project-card.tsx"
      provides: "ProjectCard component — border-rule hairline divider"
      contains: "border-rule"
      min_lines: 15
  key_links:
    - from: "src/app/projects/page.tsx"
      to: "src/components/projects/project-card.tsx"
      via: "import { ProjectCard }"
      pattern: "ProjectCard"
    - from: "src/app/projects/[slug]/page.tsx"
      to: "src/components/notion/notion-renderer.tsx"
      via: "prose block + NotionRenderer"
      pattern: "prose"
---

<objective>
Apply the v2.0 warm-paper restyle to `/projects` (index + `[slug]`) per **RESTYLE-02**.

Three files touched:

1. **`src/app/projects/page.tsx`** — h1 `text-sm font-normal uppercase tracking-widest` → `text-label uppercase text-muted`
2. **`src/components/projects/project-card.tsx`** — `border-[var(--border)]` → `border-rule` (hairline divider); hover stays `hover:opacity-60` (already v2.0 compliant)
3. **`src/app/projects/[slug]/page.tsx`** — h1 `text-2xl font-normal tracking-tight sm:text-3xl` → `text-section-feature text-ink`; hero image wrapper `rounded-lg bg-[var(--muted)]` → no rounded, `bg-muted`

Layout structure is preserved. OG image generation path (`/api/notion-cover`) untouched (D-04). ProjectCard row layout unchanged (title + description).

Note: `/projects` index h1 uses `text-label` (functional label style) rather than `text-section-feature` per recipe §2 — the page itself is a list, not a featured page; the card titles carry the visual weight. `/projects/[slug]` h1 uses `text-section-feature` (28px) since the project name deserves prominence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-sub-page-restyle-sweep/12-CONTEXT.md
@.planning/phases/12-sub-page-restyle-sweep/12-RECIPE.md
@src/app/projects/page.tsx
@src/app/projects/[slug]/page.tsx
@src/components/projects/project-card.tsx

<interfaces>
<!-- Current ProjectCard — the border-[var(--border)] is the sole v1.0 pattern -->
From src/components/projects/project-card.tsx:
export function ProjectCard({ project }: { project: Project })
Current className on Link: "group block border-b border-[var(--border)] py-4 transition-opacity hover:opacity-60"
Recipe replacement: "group block border-b border-rule py-4 transition-opacity hover:opacity-60"
hover:opacity-60 is already v2.0 compliant — keep.

<!-- Current /projects index h1 -->
Current: className="text-sm font-normal uppercase tracking-widest"
Recipe §2 replacement: className="text-label uppercase text-muted"

<!-- Current /projects/[slug] h1 -->
Current: className="text-2xl font-normal tracking-tight sm:text-3xl"
Recipe §2 replacement: className="text-section-feature text-ink"

<!-- Current /projects/[slug] hero image wrapper -->
Current: className="w-full overflow-hidden rounded-lg bg-[var(--muted)]"
Recipe §1 replacement: className="w-full overflow-hidden bg-muted"
(drop rounded-lg; bg-[var(--muted)] → bg-muted)

<!-- Prose on /projects/[slug] — global .prose override handles it; no change needed -->
className="prose mt-12 max-w-none" — correct as-is

<!-- ScrollReveal — preserve wrapper structure, only swap inner classes -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle /projects index, ProjectCard, and /projects/[slug]</name>
  <files>src/app/projects/page.tsx, src/components/projects/project-card.tsx, src/app/projects/[slug]/page.tsx</files>
  <action>
Apply three targeted edits. Read each file first to confirm current line content matches expectations below.

**Edit A — `src/app/projects/page.tsx`**

Change h1 className (line 31-33):
- From: `className="text-sm font-normal uppercase tracking-widest"`
- To: `className="text-label uppercase text-muted"`

No other changes. ScrollReveal wrappers stay. Empty-state paragraph stays. Breadcrumbs stay.

**Edit B — `src/components/projects/project-card.tsx`**

Change Link className on the root Link element (line 6):
- From: `"group block border-b border-[var(--border)] py-4 transition-opacity hover:opacity-60"`
- To: `"group block border-b border-rule py-4 transition-opacity hover:opacity-60"`

The description `p` tag has `opacity-75` — this is not a v1.0 token, it's a transparent value. Per recipe §1, prefer `text-muted` for muted text. Change the description className:
- From: `className="mt-1 text-sm opacity-75 line-clamp-2"`
- To: `className="mt-1 text-sm text-muted line-clamp-2"`

**Edit C — `src/app/projects/[slug]/page.tsx`**

Three sub-edits:

C.1. Hero image wrapper className (line 50):
- From: `className="w-full overflow-hidden rounded-lg bg-[var(--muted)]"`
- To: `className="w-full overflow-hidden bg-muted"`

C.2. h1 className (line 63):
- From: `className="text-2xl font-normal tracking-tight sm:text-3xl"`
- To: `className="text-section-feature text-ink"`

C.3. Description p tag `opacity-80` (line 69):
- From: `className="mt-3 opacity-80"`
- To: `className="mt-3 text-muted"`

C.4. Tag span `opacity-75` (line 79):
- From: `className="text-sm opacity-75"`
- To: `className="text-sm text-muted"`

No changes to: Breadcrumbs, OG image fetch path (`/api/notion-cover`), `revalidate`, metadata, `generateStaticParams`, NotionRenderer, prose block, external link pattern. Prose block `className="prose mt-12 max-w-none"` is correct.

**Run rg gates after all three edits:**

Negative (expect 0):
```
rg -c 'rounded-(?!full)' src/app/projects/\[slug\]/page.tsx
rg -c 'shadow-' src/app/projects/page.tsx src/app/projects/\[slug\]/page.tsx src/components/projects/project-card.tsx
rg -c 'var\(--bg\)\|var\(--border\)\|var\(--accent\)\|var\(--muted\)' src/app/projects/page.tsx src/app/projects/\[slug\]/page.tsx src/components/projects/project-card.tsx
```

Positive (expect >= 1):
```
rg -c 'border-rule' src/components/projects/project-card.tsx
rg -c 'text-section-feature' src/app/projects/\[slug\]/page.tsx
rg -c 'text-label\|text-muted' src/app/projects/page.tsx
```

Run `npm run build` — expect exit 0.
  </action>
  <verify>
    <automated>rg -c 'border-rule' src/components/projects/project-card.tsx &amp;&amp; rg -c 'text-section-feature' "src/app/projects/[slug]/page.tsx" &amp;&amp; rg -c 'text-label' src/app/projects/page.tsx &amp;&amp; npm run build</automated>
  </verify>
  <done>
    ProjectCard uses border-rule hairline. /projects index h1 uses text-label uppercase text-muted. /projects/[slug] h1 uses text-section-feature text-ink. Hero image wrapper has no rounded-lg. No v1.0 CSS-var aliases in any of the three touched files. npm run build exits 0. RESTYLE-02 met.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server → Notion API | `getPublishedProjects()` + `getProjectBySlug()` — server-side only; slug from URL params |
| Slug param → URL | `notFound()` guard prevents invalid slugs from rendering |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-03-01 | Tampering (open redirect) | `project.externalUrl` in `<a href={project.externalUrl}>` | accept | URL is a Notion-controlled field from the site author, not user input; existing `rel="noopener noreferrer"` stays |
| T-12-03-02 | Tampering (XSS) | `project.title`, `project.description` React children | accept | React auto-escapes string children; no dangerouslySetInnerHTML |
| T-12-03-SC | Tampering | npm/pip/cargo installs | accept | Zero new package installs; styling-only changes |
</threat_model>

<verification>
- `rg -c 'border-rule' src/components/projects/project-card.tsx` >= 1
- `rg -c 'border-\[var\(--border\)\]' src/components/projects/project-card.tsx` = 0
- `rg -c 'text-section-feature' "src/app/projects/[slug]/page.tsx"` >= 1
- `rg -c 'text-label' src/app/projects/page.tsx` >= 1
- `rg -c 'rounded-(?!full)' "src/app/projects/[slug]/page.tsx"` = 0
- `rg -c 'shadow-' src/app/projects/page.tsx "src/app/projects/[slug]/page.tsx" src/components/projects/project-card.tsx` = 0
- `rg -c 'var\(--bg\)\|var\(--border\)\|var\(--accent\)\|var\(--muted\)' src/app/projects/page.tsx "src/app/projects/[slug]/page.tsx" src/components/projects/project-card.tsx` = 0
- `rg -c 'bg-paper\|text-ink\|text-muted\|border-rule' "src/app/projects/[slug]/page.tsx"` >= 1
- `npm run build` exits 0
</verification>

<success_criteria>
- /projects index: h1 renders in 11px tracked uppercase muted (text-label)
- ProjectCard rows separated by hairline border-rule (no border-[var(--border)])
- /projects/[slug]: h1 renders at 28px bold tracked (text-section-feature); hero image has no rounded corners or v1.0 muted bg
- Prose body on project detail renders in ink color (global .prose override)
- No v1.0 chrome anywhere in the three touched files
- RESTYLE-02 requirement met
- Build green
</success_criteria>

<output>
Create `.planning/phases/12-sub-page-restyle-sweep/12-03-SUMMARY.md` when done. Include: all className changes made, rg gate outputs, build result, and confirmation that no file outside the three listed was touched.
</output>
