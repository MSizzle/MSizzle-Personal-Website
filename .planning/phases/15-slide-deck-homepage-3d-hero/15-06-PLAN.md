---
phase: 15-slide-deck-homepage-3d-hero
plan: 06
type: execute
wave: 3
depends_on: [15-05]
files_modified:
  - public/hero-blob-poster.webp
  - .planning/phases/15-slide-deck-homepage-3d-hero/15-VALIDATION.md
autonomous: false
requirements: [HD-05, TD-03]

must_haves:
  truths:
    - "hero-blob-poster.webp exists at /public/hero-blob-poster.webp and is a valid WebP image"
    - "The poster shows the elevated blob at a cinematic moment (clearcoat shine visible, crimson rim visible)"
    - "FallbackPoster renders correctly in the object slot (no 404) on first load and under no-WebGL / reduced-motion"
    - "VALIDATION.md wave_0_complete: true and nyquist_compliant: true are set in frontmatter"
    - "Manual: visual inspection confirms the deck homepage meets the phase success criteria"
  artifacts:
    - path: public/hero-blob-poster.webp
      provides: Static poster for fallback (D-08)
  key_links:
    - from: src/components/home-deck/fallback-poster.tsx
      to: /public/hero-blob-poster.webp
      via: next/image src="/hero-blob-poster.webp"
      pattern: "hero-blob-poster"
---

<objective>
Produce and commit the pre-rendered blob poster image (D-08); run the manual visual verification pass against the phase success criteria; close out validation tracking. This is the only plan with a checkpoint — the poster requires human capture from the running dev server.

Purpose: Completes the fallback path (D-08/D-09/HD-05/TD-03). The poster must exist before the homepage can be fully QA'd. Manual verification confirms the deck feel, blob quality, and reduced-motion fallback before the phase is closed.
Output: hero-blob-poster.webp committed to /public; all 5 phase success criteria verified; VALIDATION.md closed.
</objective>

<execution_context>
@/Users/Montster/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Montster/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/15-slide-deck-homepage-3d-hero/15-VALIDATION.md
@.planning/phases/15-slide-deck-homepage-3d-hero/15-CONTEXT.md
@.planning/phases/15-slide-deck-homepage-3d-hero/15-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify full test suite GREEN and build passes</name>
  <files></files>
  <read_first>
    - .planning/phases/15-slide-deck-homepage-3d-hero/15-VALIDATION.md (final checklist before manual verification)
    - .planning/phases/15-slide-deck-homepage-3d-hero/15-05-SUMMARY.md (confirm all Wave 2 tasks completed)
  </read_first>
  <action>
    Run the full automated validation gate:
    1. npx vitest run — must exit 0 (all 8 home-deck tests GREEN, no other regressions)
    2. npx next build — must pass cleanly (no "ssr: false in Server Component", no transpilePackages failures, no TypeScript build errors)

    If either fails, diagnose and fix before proceeding to the checkpoint task.

    After green gates, update VALIDATION.md frontmatter:
    - nyquist_compliant: true
    - wave_0_complete: true
    - status: "ready-for-manual-verification"

    Also update the Per-Task Verification Map table in VALIDATION.md — change all 8 status fields from "⬜ pending" to "✅ green".
  </action>
  <verify>
    <automated>npx vitest run 2>&1 | tail -5 && npx next build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - npx vitest run exits 0
    - npx next build exits 0 (no build errors)
    - VALIDATION.md frontmatter has nyquist_compliant: true, wave_0_complete: true
    - All 8 rows in VALIDATION.md Per-Task Verification Map show "✅ green"
  </acceptance_criteria>
  <done>All automated gates green; VALIDATION.md updated.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    The deck homepage is fully implemented:
    - CHOMP-style wheel deck with fresh-gesture detection, 820ms lock, 800ms easeInOutCubic
    - 3D morphing blob with MeshPhysicalMaterial (clearcoat=0.9), RoomEnvironment IBL, crimson rim light
    - 5 static slides: hero, BigList index, Prometheus, Newsletter, footer-as-slide-5
    - Progress dots, keyboard nav (arrows/space/Home/End), touch swipe (>28px), scroll resync
    - Lenis stopped on homepage mount; native-scroll fallback for touch + reduced-motion
    - FallbackPoster rendered in the object slot (currently 404s — poster.webp not created yet)
    - InkFooter suppressed on "/"
  </what-built>
  <how-to-verify>
    Run: npm run dev

    Then visit http://localhost:3000 and verify each success criterion:

    1. SUCCESS CRITERION 1 (HD-01/HD-02): On a trackpad, flick the scroll wheel once downward → exactly one slide advances, not more. Then flick in the opposite direction immediately → moves back without waiting for a lock to expire. Confirm momentum from a fast flick does NOT skip multiple slides.

    2. SUCCESS CRITERION 2 (HD-03): Press ArrowDown repeatedly → advances one slide each press. Press Home → jumps to slide 1. Press End → jumps to slide 5. Confirm the background (atmosphere/objstage) stays fixed while only the slide content scrolls.

    3. SUCCESS CRITERION 3 (HD-04): Slide 2 shows "What I'm" header and three large items: Building → /projects, Writing → /writing, Doing → prometheus.today. Confirm they are styled as BigList brutalist type.

    4. SUCCESS CRITERION 4 (HD-05): Open DevTools → toggle device toolbar to a phone size (or set pointer:coarse via emulation) → reload. Confirm the homepage shows normal vertical scroll (no deck controller active, no fixed scroller), poster image or placeholder in the object slot.

    5. SUCCESS CRITERION 5 (TD-01/TD-02/TD-03): Back on desktop, confirm the 3D object is visible in the right portion of the screen. It should be a dark glossy blob with a crimson/orange rim light. On each slide change, the object resets to the right and flies in from the left over ~1 second. Confirm it is NOT part of the LCP — open DevTools → Performance → run a trace and confirm the LCP element is text or the poster, not the WebGL canvas.

    CAPTURE POSTER IMAGE (D-08):
    While the 3D blob is running on the homepage (good lighting moment — clearcoat shine visible, crimson rim visible):
    - Open Chrome DevTools → Elements → select the canvas element inside #objstage
    - Right-click the canvas → "Capture screenshot" (or use: DevTools → more options → Capture node screenshot)
    - Save as hero-blob-poster.webp (convert to WebP if captured as PNG: use squoosh.app or cwebp)
    - Place the file at: /public/hero-blob-poster.webp (in the project root public/ directory)

    After placing the poster, reload the homepage and confirm:
    - The poster image renders correctly in the object slot (no 404)
    - On reduced-motion OR phone emulation: poster is visible in the object slot

    Report:
    - Which success criteria PASS
    - Which (if any) need fixes — describe the specific issue
    - Confirm poster is committed to /public/hero-blob-poster.webp
  </how-to-verify>
  <resume-signal>Type "approved" if all 5 success criteria pass and poster is committed. Or describe which criteria fail and what the specific issue is so fixes can be planned.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Commit poster + update ROADMAP phase status</name>
  <files>
    public/hero-blob-poster.webp,
    .planning/ROADMAP.md,
    .planning/phases/15-slide-deck-homepage-3d-hero/15-VALIDATION.md
  </files>
  <read_first>
    - .planning/ROADMAP.md (read the Phase 15 section — update Plans count and plan list)
    - .planning/phases/15-slide-deck-homepage-3d-hero/15-VALIDATION.md (read and confirm all fields are final)
  </read_first>
  <action>
    This task runs after the human checkpoint resumes with "approved".

    1. Confirm public/hero-blob-poster.webp exists and is a valid image (non-zero bytes).

    2. Update .planning/ROADMAP.md Phase 15 section:
       - Change "**Plans:** TBD" to "**Plans:** 6 plans"
       - Add plan list:
         Plans:
         - [x] 15-01-PLAN.md — Deps install + transpilePackages + 6 failing test stubs (Wave 0)
         - [x] 15-02-PLAN.md — Lenis context + CHOMP deck controller hook (Wave 1)
         - [x] 15-03-PLAN.md — R3F HeroBlob + HeroBlobCanvas (Wave 1)
         - [x] 15-04-PLAN.md — 5 slide components + fallback poster (Wave 1)
         - [x] 15-05-PLAN.md — DeckHomepage orchestrator + page.tsx replacement (Wave 2)
         - [x] 15-06-PLAN.md — Poster production + manual verification + phase closeout (Wave 3)

    3. Update VALIDATION.md:
       - status: "complete"
       - Update manual verification rows from "pending" to "✅ passed" for all items that were confirmed in the checkpoint

    4. Verify the full test suite is still green after the poster was added: npx vitest run
  </action>
  <verify>
    <automated>ls -la public/hero-blob-poster.webp && npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - public/hero-blob-poster.webp exists and file size is > 0 bytes
    - ROADMAP.md Phase 15 section shows "6 plans" and all 6 checkboxes marked [x]
    - VALIDATION.md status: complete
    - npx vitest run exits 0
  </acceptance_criteria>
  <done>Poster committed; ROADMAP updated; VALIDATION.md closed; full suite green. Phase 15 complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No server-side trust boundaries. The poster image is a one-time static asset committed to /public.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-06 | Information Disclosure | hero-blob-poster.webp (static asset) | accept | A still-frame of the 3D object is intentionally public — it is the visual fallback shown to all users. No private data is rendered in the scene. |

No ASVS categories apply — presentation-only phase, no auth, no untrusted input (RESEARCH.md §Security Domain).
</threat_model>

<verification>
Manual: all 5 phase success criteria confirmed by user in checkpoint.
Automated: npx vitest run exits 0; ls public/hero-blob-poster.webp shows non-zero file.
</verification>

<success_criteria>
- hero-blob-poster.webp committed to /public
- FallbackPoster renders correctly (no 404) on touch/reduced-motion
- All 5 phase success criteria from ROADMAP.md §Phase 15 confirmed by manual review
- VALIDATION.md status: complete, nyquist_compliant: true
- ROADMAP.md Phase 15 updated with 6 plans all checked [x]
- Full vitest suite green
</success_criteria>

<output>
Create `.planning/phases/15-slide-deck-homepage-3d-hero/15-06-SUMMARY.md` when done.
</output>
