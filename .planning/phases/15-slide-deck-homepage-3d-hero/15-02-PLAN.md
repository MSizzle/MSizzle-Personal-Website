---
phase: 15-slide-deck-homepage-3d-hero
plan: 02
type: execute
wave: 1
depends_on: [15-01]
files_modified:
  - src/components/providers/lenis-provider.tsx
  - src/components/home-deck/deck-controller.ts
  - src/__tests__/home-deck/deck-controller.test.ts
  - src/__tests__/home-deck/use-deck-mode.test.ts
autonomous: true
requirements: [HD-01, HD-02, HD-03, HD-05]

must_haves:
  truths:
    - "LenisProvider exposes useLenisControl() hook returning { stop, start }"
    - "useDeckController hook implements fresh-gesture detection: pause>110ms OR direction change OR adel>wAbs*1.25+2"
    - "820ms lock + 800ms easeInOutCubic tween on scrollTop (not CSS scroll-behavior)"
    - "Direction reversal bypasses the lock so one push in the opposite direction moves immediately"
    - "Keyboard: ArrowDown/Space/PageDown→+1, ArrowUp/PageUp→-1, Home→0, End→last slide"
    - "Touch: >28px swipe on the scroller triggers step; <28px is ignored"
    - "Scroll re-sync: after user drags scrollbar, nearest slide snaps after 90ms idle"
  artifacts:
    - path: src/components/providers/lenis-provider.tsx
      provides: LenisControlContext + useLenisControl export
      exports: [useLenisControl]
    - path: src/components/home-deck/deck-controller.ts
      provides: useDeckController hook
      exports: [useDeckController]
  key_links:
    - from: src/components/home-deck/deck-controller.ts
      to: src/components/providers/lenis-provider.tsx
      via: useLenisControl() called in deck-homepage (Wave 2)
      pattern: "useLenisControl"
    - from: src/components/home-deck/deck-controller.ts
      to: ".deck-slide"
      via: querySelectorAll('.deck-slide') on scrollerRef
      pattern: "deck-slide"
---

<objective>
Augment LenisProvider to expose stop()/start() via context; implement the CHOMP deck controller as a "use client" ref-only hook. Both are pure logic — no UI, no R3F, no slides yet. This unblocks the Wave 2 orchestrator and turns the deck-controller and use-deck-mode test stubs GREEN.

Purpose: The controller is the highest-risk behavioral logic in this phase. Isolating it in its own plan lets it be tested and verified before the UI is assembled.
Output: lenis-provider.tsx augmented with LenisControlContext; deck-controller.ts and its objEnter helper; use-deck-mode detection utility; HD-01/HD-02/HD-03/HD-05 tests GREEN.
</objective>

<execution_context>
@/Users/Montster/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Montster/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/15-slide-deck-homepage-3d-hero/15-RESEARCH.md
@.planning/phases/15-slide-deck-homepage-3d-hero/15-PATTERNS.md
@.planning/phases/15-slide-deck-homepage-3d-hero/15-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts for this plan. No codebase exploration needed. -->

From src/components/providers/lenis-provider.tsx (current shape, lines 1-7):
```typescript
"use client";
import { useEffect, useRef } from "react";
import { useReducedMotion } from "motion/react";

export function LenisProvider({ children }: { children: React.ReactNode }) {
  const lenisRef = useRef<InstanceType<typeof import("lenis").default> | null>(null);
```
lenisRef exists but is private. The Lenis instance has .stop() and .start() methods.
The GSAP ticker wiring at lines 30-36 must NOT be changed — stop()/start() only pause the Lenis RAF loop.

From .planning/sketches/002-full-site-model/assets/site.js lines 110-169:
Key deckInit() constants: pause threshold 110ms, lock 820ms, tween 800ms, touch threshold 28px, adel formula adel>wAbs*1.25+2, scroll resync timeout 90ms, minimum deltaY 4.

objEnter() call site (site.js line 134): called immediately on goTo, before animateTo.
objEnter() transform values (RESEARCH.md Pattern 5): initial translateX(20vw), opacity 0 → settled translateX(38vw), opacity 1, transition: 'transform 1s cubic-bezier(.16,1,.3,1), opacity .55s ease'.

From .planning/phases/15-slide-deck-homepage-3d-hero/15-PATTERNS.md §deck-controller.ts:
All controller state in refs: idxRef, lockRef, rafRef, wTRef, wDirRef, wAbsRef, stepDirRef.
easeInOutCubic: x < 0.5 ? 4*x*x*x : 1 - Math.pow(-2*x+2,3)/2
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Augment LenisProvider with useLenisControl() context</name>
  <files>src/components/providers/lenis-provider.tsx</files>
  <read_first>
    - src/components/providers/lenis-provider.tsx (full file — MUST read first; contains the lenisRef and GSAP ticker integration that must not be disturbed)
    - src/components/providers/motion-provider.tsx (context shape pattern — createContext + useContext export)
    - .planning/phases/15-slide-deck-homepage-3d-hero/15-PATTERNS.md §lenis-provider.tsx (exact augmentation code: LenisControlContext, useLenisControl export, wrap return with provider)
    - .planning/phases/15-slide-deck-homepage-3d-hero/15-RESEARCH.md §Pattern 6 (Lenis Conflict Resolution)
  </read_first>
  <action>
    Edit src/components/providers/lenis-provider.tsx:

    1. Add imports at the top (after existing imports): createContext, useContext from "react"

    2. Before the LenisProvider function, add:
       const LenisControlContext = createContext<{ stop: () => void; start: () => void } | null>(null);
       export function useLenisControl() { return useContext(LenisControlContext); }

    3. Change the return statement from `return <>{children}</>` to:
       return (
         <LenisControlContext.Provider value={{
           stop:  () => lenisRef.current?.stop(),
           start: () => lenisRef.current?.start(),
         }}>
           {children}
         </LenisControlContext.Provider>
       );

    DO NOT change anything else — the useEffect, GSAP ticker wiring (lines that call gsap.ticker.add/remove), or lenisRef assignment must remain exactly as they are.

    Rationale (RESEARCH.md Pitfall 1): Lenis intercepts raw deltaY before the deck's wheel handler. Calling lenis.stop() before the wheel listener is attached ensures the deck receives raw browser deltaY values, making the adel>wAbs*1.25+2 fresh-gesture detection reliable. lenis.start() is called on homepage unmount.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep "lenis-provider" | head -5; echo "lenis-provider tsc check done"</automated>
  </verify>
  <acceptance_criteria>
    - src/components/providers/lenis-provider.tsx exports useLenisControl function
    - The function returns { stop, start } from a React context
    - LenisProvider wraps children in LenisControlContext.Provider
    - The GSAP ticker add/remove lines are unchanged
    - npx tsc --noEmit passes with no errors in lenis-provider.tsx
    - Existing lenis-provider tests still pass: npx vitest run src/__tests__/providers/lenis-provider.test.tsx exits 0
  </acceptance_criteria>
  <done>useLenisControl() exported; existing tests still green.</done>
</task>

<task type="auto">
  <name>Task 2: Implement deck-controller.ts hook + update test stubs to GREEN</name>
  <files>
    src/components/home-deck/deck-controller.ts,
    src/__tests__/home-deck/deck-controller.test.ts,
    src/__tests__/home-deck/use-deck-mode.test.ts
  </files>
  <read_first>
    - .planning/sketches/002-full-site-model/assets/site.js lines 110-169 (canonical deckInit() + objEnter() source — port from here verbatim for logic constants)
    - .planning/phases/15-slide-deck-homepage-3d-hero/15-RESEARCH.md §Pattern 4 (complete TypeScript translation of useDeckController) and §Pattern 5 (objEnter function)
    - .planning/phases/15-slide-deck-homepage-3d-hero/15-PATTERNS.md §deck-controller.ts (ref-only pattern; easeInOutCubic; event listener cleanup)
    - src/components/home-v2/cycling-photo.tsx (useEffect + addEventListener + cleanup pattern — structural analog)
    - src/__tests__/home-deck/deck-controller.test.ts (the stub file created in Wave 0 — update this to real tests)
    - src/__tests__/home-deck/use-deck-mode.test.ts (the stub file created in Wave 0 — update this to real tests)
    - src/__tests__/providers/lenis-provider.test.tsx (vi.mock pattern to follow)
  </read_first>
  <action>
    CREATE src/components/home-deck/deck-controller.ts:
    "use client" directive at top.

    Export useDeckController({ scrollerRef, slideCount, onSlideChange }) hook:
    - All state in refs: idxRef=0, lockRef=0, rafRef=0, wTRef=0, wDirRef=0, wAbsRef=0, stepDirRef=0
    - easeInOutCubic: x < 0.5 ? 4*x*x*x : 1 - Math.pow(-2*x+2,3)/2
    - getSlides(): querySelectorAll('.deck-slide') on scrollerRef.current → Array<HTMLElement>
    - animateTo(top, dur): cancelAnimationFrame(rafRef.current); rAF loop using easeInOutCubic driving sc.scrollTop
    - goTo(i): clamp to [0, slideCount-1]; skip if i===idxRef.current; set idxRef.current=i; lockRef.current=Date.now()+820; call onSlideChange(i); animateTo(slides[i].offsetTop, 800)
    - step(dir): if locked (Date.now()<lockRef.current) AND dir===stepDirRef.current → return; else stepDirRef.current=dir; goTo(idxRef.current+dir)
    - Wheel handler: e.preventDefault(); adel=Math.abs(e.deltaY); if adel<4 return; dir=e.deltaY>0?1:-1; now=Date.now(); fresh = now-wTRef.current>110 || dir!==wDirRef.current || adel>wAbsRef.current*1.25+2; update wTRef, wDirRef, wAbsRef; if(!fresh)return; step(dir)
    - Touch: onTouchStart stores tY; onTouchMove calls e.preventDefault(); onTouchEnd: if |dy|>28 call step(dy>0?1:-1)
    - Keyboard (window keydown): ArrowDown/Space/PageDown → step(1); ArrowUp/PageUp → step(-1); Home → goTo(0); End → goTo(slideCount-1); skip if target is input/textarea/select
    - Scroll resync (passive): debounced 90ms; if unlocked, find nearest .deck-slide by offsetTop+offsetHeight/2 vs sc.scrollTop+clientHeight/2; if near!==idxRef.current update idxRef and call onSlideChange
    - Resize handler: scroll scroller to slides[idxRef.current].offsetTop immediately
    - Cleanup: removeEventListener for all; cancelAnimationFrame
    - Returns { idxRef }

    Export objEnter(objWrapRef: React.RefObject<HTMLElement | null>):
    - el=objWrapRef.current; if !el return
    - vw=window.innerWidth/100
    - el.style.transition='none'; el.style.transform=`translateX(${(20*vw).toFixed(1)}px)`; el.style.opacity='0'
    - requestAnimationFrame(()=>requestAnimationFrame(()=>{ el.style.transition='transform 1s cubic-bezier(.16,1,.3,1), opacity .55s ease'; el.style.transform=`translateX(${(38*vw).toFixed(1)}px)`; el.style.opacity='1' }))

    Import useEffect, useRef, useCallback from "react". No other imports.

    UPDATE src/__tests__/home-deck/deck-controller.test.ts (replace stubs with real tests):
    - Create a fake scroller div (document.createElement('div')) with 5 fake .deck-slide children
    - Call useDeckController via renderHook from @testing-library/react
    - HD-01: dispatch a 'wheel' event with deltaY=120 → check onSlideChange called with idx=1
    - HD-02 (momentum): dispatch two wheel events 20ms apart, same direction, same delta → second call should not trigger onSlideChange again (not fresh)
    - HD-02 (reversal bypass): after goTo(1), immediately dispatch wheel with opposite direction → should move to idx=0 despite lock
    - HD-03: dispatch keydown 'ArrowDown' → onSlideChange called; 'Home' → idx=0; 'End' → idx=4

    UPDATE src/__tests__/home-deck/use-deck-mode.test.ts (replace stubs with real tests testing the detection logic):
    Note: The detection logic lives inside deck-homepage.tsx (Wave 2) but the logic can be extracted to a tiny utility or tested via the hook. For now, test the detection logic as an inline function:
    - Export a detectDeckMode() utility from deck-controller.ts (or a separate use-deck-mode.ts) that takes { matchesCoarse: boolean, innerWidth: number } and returns { showDeck: boolean }
    - Test: matchesCoarse=true → showDeck=false; innerWidth=500 → showDeck=false; both false → showDeck=true
    - If detectDeckMode is not a separate export, stub the test with a pure function test that documents the logic: isTouchOrSmall = coarse || width < 760

    Pattern: follow lenis-provider.test.tsx mock structure (vi.mock at top, dynamic import after mocks, describe/it/expect).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/home-deck/deck-controller.test.ts src/__tests__/home-deck/use-deck-mode.test.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - src/components/home-deck/deck-controller.ts exists and exports useDeckController and objEnter
    - useDeckController uses only useRef + useCallback + useEffect — zero useState
    - Fresh-gesture condition is: now-wTRef.current>110 || dir!==wDirRef.current || adel>wAbsRef.current*1.25+2 (exact values per D-06 / site.js line 147)
    - Lock duration is 820ms; tween duration is 800ms (site.js line 142)
    - Touch threshold is 28px (site.js — matches RESEARCH.md HD-05 spec)
    - objEnter uses cubic-bezier(.16,1,.3,1) for transform and 0.55s ease for opacity (RESEARCH.md Pattern 5)
    - npx vitest run src/__tests__/home-deck/deck-controller.test.ts exits 0 (GREEN)
    - npx vitest run src/__tests__/home-deck/use-deck-mode.test.ts exits 0 (GREEN)
    - npx vitest run exits 0 (full suite — no regressions)
  </acceptance_criteria>
  <done>deck-controller.ts and objEnter implemented; HD-01/HD-02/HD-03/HD-05 tests GREEN; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser wheel/keyboard/touch → deck controller | Event handler receives untrusted input from user devices |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Denial of Service | wheel event handler | accept | adel<4 guard prevents micro-vibration spam; lock prevents rapid slide advancement. No server involved; worst case is a client-side UI glitch, not data loss. |
| T-15-02 | Tampering | keyboard onKeyDown | accept | Input field guard (skip if target is input/textarea/select) prevents navigation shortcuts from firing during text input. No PII or server mutation involved. |

No ASVS categories apply — presentation-only phase, no auth, no sessions, no user input sent to any server (RESEARCH.md §Security Domain).
</threat_model>

<verification>
npx vitest run src/__tests__/home-deck/deck-controller.test.ts src/__tests__/home-deck/use-deck-mode.test.ts exits 0.
npx vitest run (full suite) exits 0.
Grep: grep -c "useDeckController" src/components/home-deck/deck-controller.ts returns 1.
Grep: grep -c "useLenisControl" src/components/providers/lenis-provider.tsx returns at least 1.
</verification>

<success_criteria>
- LenisProvider exposes useLenisControl() returning { stop, start }
- useDeckController hook implements all CHOMP mechanics (fresh-gesture, 820ms lock, 800ms tween, keyboard, touch 28px, scroll resync)
- objEnter sets initial translateX(20vw)/opacity:0 → settled translateX(38vw)/opacity:1 with cubic-bezier(.16,1,.3,1)
- HD-01, HD-02, HD-03, HD-05 test stubs turned GREEN
- Full vitest suite exits 0
</success_criteria>

<output>
Create `.planning/phases/15-slide-deck-homepage-3d-hero/15-02-SUMMARY.md` when done.
</output>
