---
phase: 15
plan: 02
type: execute
wave: 2
depends_on:
  - 15-01
files_modified:
  - src/components/home/hero-blob.tsx
  - src/components/home/hero-blob-canvas.tsx
  - src/components/home/hero-podium.tsx
autonomous: true
requirements:
  - TD-01
  - TD-02

must_haves:
  truths:
    - "hero-blob.tsx uses CustomShaderMaterial (CSM) from three-custom-shader-material/vanilla — NO per-frame JS morph loop"
    - "IcosahedronGeometry in hero-blob.tsx calls geometry.computeTangents() immediately after creation"
    - "hero-blob.tsx vertex shader string contains csm_Position and csm_Normal output assignments"
    - "hero-blob-canvas.tsx imports EffectComposer and Bloom from @react-three/postprocessing"
    - "hero-blob-canvas.tsx includes a crimson directional light (color 0xe23838)"
    - "hero-podium.tsx is a thin CylinderGeometry disc (MeshStandardMaterial, crimson emissive)"
    - "npx vitest run src/__tests__/home/hero-blob.test.tsx exits 0"
  artifacts:
    - path: "src/components/home/hero-blob.tsx"
      provides: "GPU vertex-shader displaced PBR blob"
      contains: "computeTangents"
    - path: "src/components/home/hero-blob-canvas.tsx"
      provides: "R3F Canvas with postprocessing + podium"
      contains: "EffectComposer"
    - path: "src/components/home/hero-podium.tsx"
      provides: "thin reflective disc under blob"
      contains: "CylinderGeometry"
  key_links:
    - from: "hero-blob.tsx"
      to: "three-custom-shader-material/vanilla"
      via: "useMemo(() => new CustomShaderMaterial({baseMaterial: THREE.MeshPhysicalMaterial, ...}))"
      pattern: "CustomShaderMaterial"
    - from: "hero-blob-canvas.tsx"
      to: "@react-three/postprocessing"
      via: "EffectComposer + Bloom JSX inside Canvas"
      pattern: "EffectComposer"
    - from: "hero-blob-canvas.tsx"
      to: "hero-podium.tsx"
      via: "HeroPodium JSX child of R3F Canvas"
      pattern: "HeroPodium"
---

<objective>
Build the GPU-powered HeroBlob and its containing Canvas: migrate the existing JS morph loop to a GPU vertex shader via three-custom-shader-material, add a thin podium disc, and wire in postprocessing bloom. This is the core WebGL hero object — the centerpiece of the homepage.

Purpose: The existing HeroBlob uses a per-frame JavaScript computeVertexNormals loop that tanks mobile FPS (measured 5.4s scripting time in spike 001). Moving displacement and normal recalculation to the GPU vertex shader is a hard requirement (D-03). Postprocessing (bloom) is required by D-02.

Output: Three files in src/components/home/ — hero-blob.tsx (GPU morph), hero-blob-canvas.tsx (Canvas + EffectComposer), hero-podium.tsx (simple podium disc). New npm packages @react-three/postprocessing and three-custom-shader-material installed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-slide-deck-homepage-3d-hero/15-RESEARCH.md
@.planning/phases/15-slide-deck-homepage-3d-hero/15-PATTERNS.md

Prior plan context needed:
@.planning/phases/15-slide-deck-homepage-3d-hero/15-01-SUMMARY.md
</context>

<interfaces>
<!-- Key patterns the executor needs. Extracted from analog files. -->

From src/components/home-deck/hero-blob.tsx (analog — full migration source):
The existing file uses useMemo for blobGeo (IcosahedronGeometry(1.3, 12)), IBL setup via RoomEnvironment + PMREMGenerator in a second useMemo, then meshPhysicalMaterial JSX with per-frame JS morph. The morph loop (lines 35-72) and computeVertexNormals call are REPLACED. IBL setup (lines 26-32) and rotation-only useFrame carry forward.

From src/components/home-deck/hero-blob-canvas.tsx (analog):
Canvas props: dpr={[1, 2]}, camera={{ fov: 42, position: [0, 0, 4.4], near: 0.1, far: 100 }}, gl={{ antialias: true, alpha: true }}, style={{ position: "absolute", inset: 0 }}. Light rig: directionalLight white at [3,4,5] intensity 1.9, directionalLight orange-ish at [-4,-2,-4] intensity 1.7 (this one becomes crimson 0xe23838), directionalLight white at [-3,2,3] intensity 0.45, ambientLight dark at intensity 0.5. All these carry forward; the [-4,-2,-4] light becomes crimson.

CSM GPU morph GLSL (from RESEARCH.md Pattern 2 + PATTERNS.md hero-blob.tsx section):
Import: CustomShaderMaterial from "three-custom-shader-material/vanilla"
useMemo material: new CustomShaderMaterial({ baseMaterial: THREE.MeshPhysicalMaterial, vertexShader: BLOB_VERT, uniforms: { uTime: { value: 0 } }, color: 0x140805, metalness: 0.6, roughness: 0.18, clearcoat: 0.9, clearcoatRoughness: 0.1, envMapIntensity: 1.2 })
useFrame: mat.uniforms.uTime.value = clock.getElapsedTime(); + rotation.y += 0.0035; + rotation.x = Math.sin(t * 0.3) * 0.22
JSX: mesh ref={meshRef} geometry={blobGeo} material={mat}
</interfaces>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 0: Install new npm packages (human-verify package legitimacy)</name>

  <read_first>
    - .planning/phases/15-slide-deck-homepage-3d-hero/15-RESEARCH.md section "Package Legitimacy Audit" (both packages are Approved per manual npm registry verification)
    - .planning/phases/15-slide-deck-homepage-3d-hero/15-RESEARCH.md section "New Dependencies Required" (version pins and peer dep compatibility)
    - package.json (confirm three@0.184 and @react-three/fiber@9.6 are installed — peer dep anchors)
  </read_first>

  <what-built>
    The RESEARCH.md Package Legitimacy Audit verified both packages manually against npm registry and GitHub:
    - @react-three/postprocessing@3.0.4 — pmndrs official org, 5+ years, very high downloads, github.com/pmndrs/react-postprocessing. Peer deps satisfied: @react-three/fiber ^9, three >= 0.156.0, react ^19.
    - three-custom-shader-material@6.4.0 — FarazzShaikh/THREE-CustomShaderMaterial, 3+ years, TypeScript, widely referenced. Peer deps: three >= 0.159, react >= 18, @react-three/fiber >= 8.

    Because slopcheck could not run in the research session, both are tagged [ASSUMED: npm registry + GitHub].
  </what-built>

  <how-to-verify>
    Before approving, please check in your browser:
    1. Visit https://www.npmjs.com/package/@react-three/postprocessing — confirm it is the pmndrs package (author: pmndrs), published within the last 2 years, weekly downloads in the millions.
    2. Visit https://www.npmjs.com/package/three-custom-shader-material — confirm it is by FarazzShaikh, published within the last 2 years, and links to the correct GitHub repo (FarazzShaikh/THREE-CustomShaderMaterial).
    3. Confirm neither package was recently transferred to a new owner with no prior history.

    If both check out: type "approved"
    If either is suspicious: type "reject: [reason]" and do not proceed with install.
  </how-to-verify>

  <resume-signal>Type "approved" to proceed with npm install, or "reject: [reason]" to abort</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Install deps + create hero-blob.tsx with GPU vertex-shader morph</name>

  <read_first>
    - src/components/home-deck/hero-blob.tsx (full analog — 92 lines; read entirely; this is the file being migrated)
    - .planning/phases/15-slide-deck-homepage-3d-hero/15-PATTERNS.md section "src/components/home/hero-blob.tsx" (exact diff: what to remove, what to add, GLSL string, JSX pattern)
    - .planning/phases/15-slide-deck-homepage-3d-hero/15-RESEARCH.md section "Pattern 2: GPU Vertex-Shader Displacement Morph" and "Code Examples — GPU Morph Full HeroBlob migration skeleton"
    - src/__tests__/home/hero-blob.test.tsx (the test scaffold from 15-01; update its vi.todo stub to a real assertion once the real component file exists)
  </read_first>

  <files>
    src/components/home/hero-blob.tsx,
    src/__tests__/home/hero-blob.test.tsx
  </files>

  <action>
    First, run: npm install @react-three/postprocessing three-custom-shader-material

    Then create src/components/home/hero-blob.tsx as a migration of src/components/home-deck/hero-blob.tsx with these precise changes:

    KEEP (verbatim carry-forward):
    - "use client" directive at top
    - Imports for useRef, useMemo from "react"; useFrame, useThree from "@react-three/fiber"; THREE from "three"; RoomEnvironment from "three/examples/jsm/environments/RoomEnvironment.js"
    - IBL setup useMemo block (RoomEnvironment + PMREMGenerator) — verbatim from analog lines 26-32
    - The rotation-only portion of useFrame: meshRef.current.rotation.y += 0.0035 and the sin-based rotation.x

    ADD:
    - Import: CustomShaderMaterial from "three-custom-shader-material/vanilla"
    - A BLOB_VERT GLSL string constant (declared as const BLOB_VERT = /* glsl */`...`) containing the sineDisplace function and main() with csm_Position and csm_Normal assignments. The exact GLSL is in RESEARCH.md Pattern 2 and PATTERNS.md hero-blob.tsx section. Key identifiers: sineDisplace(vec3 p), csm_Position, csm_Normal, tangent.xyz, biTangent = cross(normal, tangent.xyz), shift = 0.001.
    - blobGeo useMemo: const g = new THREE.IcosahedronGeometry(1.3, 12); g.computeTangents(); return g; — the computeTangents() call is mandatory for the tangent attribute in the vertex shader.
    - mat useMemo: new CustomShaderMaterial({ baseMaterial: THREE.MeshPhysicalMaterial, vertexShader: BLOB_VERT, uniforms: { uTime: { value: 0 } }, color: 0x140805, metalness: 0.6, roughness: 0.18, clearcoat: 0.9, clearcoatRoughness: 0.1, envMapIntensity: 1.2 })
    - In useFrame: mat.uniforms.uTime.value = clock.getElapsedTime() (update the uniform per frame)
    - A matRef: useRef (for test introspection — optional but matches test scaffold)

    REMOVE (do NOT carry forward from analog):
    - The tRef = useRef(0) scroll tracker
    - The entire per-frame JS morph loop (for loop over position buffer, sin calculations, pos.needsUpdate = true)
    - The blobGeo.computeVertexNormals() call in useFrame (this is the expensive call being removed)
    - The wireGeo and wireRef (optional wire overlay — omit for v1 per PATTERNS.md)
    - The meshPhysicalMaterial JSX child — material is now passed as the mat prop directly to mesh

    JSX return: a single mesh element: mesh ref={meshRef} geometry={blobGeo} material={mat} — no children (material is passed as prop, not JSX child).

    After creating hero-blob.tsx, update src/__tests__/home/hero-blob.test.tsx: replace the vi.todo stub with a real test. Remove the mock of @/components/home/hero-blob (import the real component). The test uses @react-three/test-renderer create() to render HeroBlob and asserts renderer.scene.children.length > 0. Keep all three vi.mock blocks (RoomEnvironment, PMREMGenerator stub in three mock, CustomShaderMaterial stub).
  </action>

  <verify>
    <automated>npx vitest run src/__tests__/home/hero-blob.test.tsx --reporter=verbose 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - src/components/home/hero-blob.tsx exists
    - hero-blob.tsx contains the string "computeTangents()" (required for tangent attribute)
    - hero-blob.tsx contains "csm_Position" (GPU output variable — confirms CSM shader is present)
    - hero-blob.tsx contains "csm_Normal" (GPU normal recalculation — confirms normals are in shader)
    - hero-blob.tsx contains "CustomShaderMaterial" import from "three-custom-shader-material/vanilla"
    - hero-blob.tsx does NOT contain "computeVertexNormals" (the JS morph loop call must be gone)
    - hero-blob.tsx does NOT contain "needsUpdate" (the JS morph loop pos.needsUpdate must be gone)
    - npx vitest run src/__tests__/home/hero-blob.test.tsx exits 0 with the TD-01 test passing (not todo)
    - package.json devDependencies or dependencies now includes "@react-three/postprocessing" and "three-custom-shader-material"
    - npm install exits without peer dependency conflict errors
  </acceptance_criteria>

  <done>GPU morph HeroBlob exists in src/components/home/; no JS morph loop; test passes; deps installed.</done>
</task>

<task type="auto">
  <name>Task 2: Create hero-podium.tsx + hero-blob-canvas.tsx with bloom</name>

  <read_first>
    - src/components/home-deck/hero-blob-canvas.tsx (full analog — 21 lines; Canvas props, light positions, exact JSX structure to carry forward)
    - .planning/phases/15-slide-deck-homepage-3d-hero/15-PATTERNS.md section "src/components/home/hero-blob-canvas.tsx" (exact additions: EffectComposer, crimson rim light, HeroPodium)
    - .planning/phases/15-slide-deck-homepage-3d-hero/15-PATTERNS.md section "src/components/home/hero-podium.tsx" (CylinderGeometry spec: radius 1.6/1.6, height 0.06, segments 64, position [0,-1.55,0], MeshStandardMaterial with metalness 0.8, roughness 0.1, emissive 0xe23838, emissiveIntensity 0.04)
    - .planning/phases/15-slide-deck-homepage-3d-hero/15-RESEARCH.md section "Pattern 5: EffectComposer + Bloom" (Bloom props: luminanceThreshold 0.85, luminanceSmoothing 0.9, intensity 0.6, radius 0.4)
  </read_first>

  <files>
    src/components/home/hero-podium.tsx,
    src/components/home/hero-blob-canvas.tsx
  </files>

  <action>
    Create src/components/home/hero-podium.tsx first (it is imported by hero-blob-canvas.tsx). The podium is a thin disc representing the platform the blob stands on per D-08. Use CylinderGeometry(1.6, 1.6, 0.06, 64) positioned at [0, -1.55, 0]. Material: MeshStandardMaterial with color 0x141414, metalness 0.8, roughness 0.1, emissive 0xe23838, emissiveIntensity 0.04. The low emissiveIntensity means the crimson glow is subtle — it catches Bloom from the crimson rim light without competing with the blob. Add "use client" directive and export the HeroPodium function. No useFrame or animation needed — it is a static mesh.

    Create src/components/home/hero-blob-canvas.tsx as a migration of src/components/home-deck/hero-blob-canvas.tsx with these additions:

    KEEP from analog:
    - "use client" directive (already in analog — the Canvas component requires client)
    - Canvas props: dpr={[1, 2]}, camera={{ fov: 42, position: [0, 0, 4.4] as [number, number, number], near: 0.1, far: 100 }}, style={{ position: "absolute", inset: 0 }}, gl={{ antialias: true, alpha: true }}
    - directionalLight white at position [3, 4, 5] intensity 1.9 (key light)
    - directionalLight white at position [-3, 2, 3] intensity 0.45 (fill light)
    - ambientLight color 0x1a0a06 intensity 0.5
    - Import HeroBlob from "./hero-blob" (updated path)

    CHANGE from analog:
    - The directionalLight at position [-4, -2, -4] with color 0xff6a3a intensity 1.7 becomes: color={0xe23838} intensity={2.2} (crimson rim — per PATTERNS.md; intensity 2.2 pushes luminance above Bloom's 0.85 threshold)

    ADD:
    - Import HeroPodium from "./hero-podium"
    - Import EffectComposer, Bloom from "@react-three/postprocessing"
    - After HeroBlob in Canvas JSX: HeroPodium element
    - After HeroPodium: EffectComposer with Bloom child (props: luminanceThreshold={0.85} luminanceSmoothing={0.9} intensity={0.6} radius={0.4})

    The file is a default export function named HeroBlobCanvas (matches the dynamic import target in canvas-loader.tsx which will be built in Plan 03).

    Verify that the import of HeroPodium works (the file was just created). Verify EffectComposer and Bloom import from "@react-three/postprocessing" — this package was installed in Task 1 of this plan.
  </action>

  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "home/hero-(blob-canvas|podium)" | head -10; echo "TypeScript check done"</automated>
  </verify>

  <acceptance_criteria>
    - src/components/home/hero-podium.tsx exists and contains "CylinderGeometry" and "emissive"
    - src/components/home/hero-blob-canvas.tsx exists and contains "EffectComposer" and "Bloom"
    - hero-blob-canvas.tsx contains a directionalLight with color={0xe23838} (the crimson rim)
    - hero-blob-canvas.tsx imports HeroPodium from "./hero-podium"
    - hero-blob-canvas.tsx is a default export (not named export) so dynamic import works without destructuring
    - npx tsc --noEmit reports no errors in the three new home/ component files (hero-blob, hero-blob-canvas, hero-podium)
    - hero-podium.tsx position prop is [0, -1.55, 0] (positions the disc below the blob's geometry center)
  </acceptance_criteria>

  <done>hero-podium.tsx and hero-blob-canvas.tsx exist; Canvas has crimson bloom and podium; TypeScript clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm install | Two new third-party packages enter the dependency tree for the first time |
| GLSL shader string | Inline GLSL executes on the GPU — no user input reaches the shader |
| Canvas / WebGL context | Created only on desktop/fine-pointer devices; mobile never creates it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-04-SC | Tampering | npm install @react-three/postprocessing three-custom-shader-material | mitigate | Human-verify checkpoint (Task 0) gates install on user confirmation of npm registry legitimacy; both packages pre-verified in RESEARCH.md Package Legitimacy Audit |
| T-15-05 | Denial of Service | EffectComposer + Bloom on low-end GPU causing browser tab crash or jank | mitigate | Canvas only mounts on desktop/pointer:fine/webglOk path (gate in Plan 03); Bloom intensity conservatively set to 0.6; perf trim order from spike 001 (reduce intensity before removing) |
| T-15-06 | Information Disclosure | GPU fingerprinting via WebGL context attributes | mitigate | failIfMajorPerformanceCaveat: true already in gate (carried from deck-homepage.tsx); prevents software renderer and fingerprinting via performance characteristics |
| T-15-07 | Tampering | Malicious GLSL injection — N/A; shader is a static string literal | accept | No user input reaches the vertex shader; BLOB_VERT is a hardcoded string constant in the source file |
</threat_model>

<verification>
Run after all tasks complete:
1. Dep install: cat package.json | grep "@react-three/postprocessing\|three-custom-shader" → both present
2. CSM GPU morph: grep -c 'computeTangents' src/components/home/hero-blob.tsx → returns 1
3. No JS morph: grep -c 'computeVertexNormals' src/components/home/hero-blob.tsx → returns 0
4. Bloom wired: grep -c 'EffectComposer' src/components/home/hero-blob-canvas.tsx → returns 1
5. Tests: npx vitest run src/__tests__/home/hero-blob.test.tsx → exits 0
6. TypeScript: npx tsc --noEmit → no errors in home/ component files
</verification>

<success_criteria>
- @react-three/postprocessing and three-custom-shader-material are in package.json
- HeroBlob uses GPU vertex shader with csm_Position/csm_Normal — no JS morph loop
- geometry.computeTangents() is called after IcosahedronGeometry creation
- HeroBlobCanvas has crimson rim light (0xe23838) and EffectComposer with Bloom
- HeroPodium is a thin reflective disc with faint crimson emissive
- TD-01 test passes in vitest
- TypeScript reports no errors on the three new files
</success_criteria>

<output>
Create .planning/phases/15-slide-deck-homepage-3d-hero/15-02-SUMMARY.md when done
</output>
