---
phase: 15
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - public/hero-blob-poster.webp
  - src/__tests__/home/hero-blob.test.tsx
  - src/__tests__/home/canvas-loader.test.tsx
  - src/__tests__/home/explorative-homepage.test.tsx
  - src/__tests__/home/section-building.test.tsx
autonomous: true
requirements:
  - TD-01
  - TD-02
  - TD-03
  - HD-04
  - HD-05

must_haves:
  truths:
    - "globals.css @theme inline block uses near-black #0a0a0a for --color-bg, NOT #d93c1e"
    - "All new-palette tokens (--color-bg-2, --color-surface, --blob-core, --blob-rim, --accent-hover, --accent-deep, --accent-glow) are present"
    - ":root sig vars use off-white fill and crimson-deep shadow, NOT the Crimson Poster values"
    - "The Phase 15 Deck Styles block (lines 134-257) is removed from globals.css"
    - "public/hero-blob-poster.webp exists (1x1 dark placeholder) so FallbackPoster does not 404"
    - "Four test scaffold files exist in src/__tests__/home/ with correct imports and vi.mock stubs"
    - "npx vitest run src/__tests__/home/ exits 0 (scaffolds pass or skip — no build failures)"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Crimson Line palette tokens + stripped deck styles"
      contains: "--color-bg:             #0a0a0a"
    - path: "public/hero-blob-poster.webp"
      provides: "1x1 dark placeholder so FallbackPoster image tag does not 404"
    - path: "src/__tests__/home/hero-blob.test.tsx"
      provides: "TD-01 test scaffold with CSM mock"
    - path: "src/__tests__/home/canvas-loader.test.tsx"
      provides: "TD-02 timing test scaffold"
    - path: "src/__tests__/home/explorative-homepage.test.tsx"
      provides: "TD-03 + HD-05 gate test scaffold"
    - path: "src/__tests__/home/section-building.test.tsx"
      provides: "HD-04 BigList render scaffold"
  key_links:
    - from: "globals.css @theme inline"
      to: "every v3 component that uses text-text, bg-bg, border-border, .sig"
      via: "Tailwind v4 CSS variable resolution"
      pattern: "--color-bg.*#0a0a0a"
    - from: "src/__tests__/home/"
      to: "src/components/home/ (not yet created)"
      via: "dynamic import inside each test"
      pattern: "import.*@/components/home/"
---

<objective>
Wave 0 blockers: Swap the globals.css palette from Crimson Poster to Crimson Line and remove the deck CSS block, drop a 1x1 dark WebP placeholder so FallbackPoster does not 404 during development, and create all four test scaffold files that subsequent implementation waves will fill.

Purpose: Nothing in the new component tree can be built correctly until the palette tokens are correct. A wrong token value in globals.css means every component is reworked after the swap. The placeholder prevents 404 errors in CI and fallback rendering paths before the real Playwright poster exists. The test scaffolds establish the mock patterns that executors in later plans will extend — writing them first prevents duplicated mock setup.

Output: Updated globals.css with Crimson Line palette; public/hero-blob-poster.webp (1x1 placeholder); four test files in src/__tests__/home/ with correct mocks, passing or skipping.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-slide-deck-homepage-3d-hero/15-RESEARCH.md
@.planning/phases/15-slide-deck-homepage-3d-hero/15-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap globals.css palette to Crimson Line + strip deck styles</name>

  <read_first>
    - src/app/globals.css (full file — 257 lines; read entirely before editing)
    - .planning/phases/15-slide-deck-homepage-3d-hero/15-PATTERNS.md section "src/app/globals.css" (exact before/after token values)
    - .planning/phases/15-slide-deck-homepage-3d-hero/15-RESEARCH.md section "Pattern 1: Globals.css Palette Swap"
    - .planning/sketches/themes/crimson-line.css (authoritative source for token values)
  </read_first>

  <files>src/app/globals.css</files>

  <action>
    Replace the @theme inline block (lines 3-33 in the current file) entirely with the Crimson Line tokens. The exact replacement values are in PATTERNS.md under "src/app/globals.css". Key change: --color-bg changes from #d93c1e to #0a0a0a. Add the five new tokens not present in the Crimson Poster block: --color-bg-2 (#0d0d0f), --color-surface (#141416, was #cc3719 under a different role), --accent-hover (#ff4d4d), --accent-deep (#b51d1d), --accent-glow (rgba(226,56,56,0.40)), --blob-core (#161617), --blob-rim (#e23838). The --accent value flips from #0a0503 (near-black) to #e23838 (crimson).

    Update the :root sig vars block (lines 69-73). Change --sig from var(--color-bg, #d93c1e) to var(--color-text) (off-white fill). Change --sig-shadow from 0.055em 0.055em 0 #0a0503 to 0.055em 0.055em 0 var(--accent-deep) (crimson-deep shadow).

    Remove the entire "Phase 15 — Deck Styles" CSS block (lines 134-257). This includes .deck-scroller, .deck-slide, .deck-slide--footer, .deck-foot, .deck-dots, .deck-dot, and all their variants and media queries. These classes are superseded; removing them frees ~123 lines and prevents stale styles from bleeding into the new layout.

    Keep unchanged: the html/body rules, a/link rules, .sig/.sig-out class bodies (only the :root vars change, not the class rules), .prose blocks, @keyframes scroll, and any other non-deck, non-palette rules. Type scale vars (--text-xs through --text-mega) and font vars (--font-display, --font-sans, --font-mono) carry forward unchanged.

    After editing, verify the .sig-out rule still references var(--accent) — it will now render as crimson stroke on near-black background, which is correct for the Crimson Line palette.
  </action>

  <verify>
    <automated>grep -c '--color-bg:.*#0a0a0a' src/app/globals.css && grep -c 'deck-slide' src/app/globals.css | grep -q '^0$' && echo "PASS" || echo "FAIL"</automated>
  </verify>

  <acceptance_criteria>
    - src/app/globals.css contains exactly: --color-bg:             #0a0a0a (the exact string with the value #0a0a0a)
    - src/app/globals.css contains --accent:               #e23838
    - src/app/globals.css contains --color-text:           #f5f5f0
    - src/app/globals.css contains --blob-rim:             #e23838
    - src/app/globals.css contains --accent-deep:          #b51d1d
    - src/app/globals.css does NOT contain #d93c1e (the old Crimson Poster bg)
    - src/app/globals.css does NOT contain deck-slide (the deck CSS block is removed)
    - src/app/globals.css does NOT contain .deck-scroller
    - :root block contains --sig:        var(--color-text)
    - :root block contains --sig-shadow: 0.055em 0.055em 0 var(--accent-deep)
    - npx next build exits 0 (CSS parse error would break build — run a quick build check)
  </acceptance_criteria>

  <done>globals.css has Crimson Line tokens and no deck CSS; build does not error on CSS.</done>
</task>

<task type="auto">
  <name>Task 2: Create 1x1 dark WebP placeholder for hero-blob-poster.webp</name>

  <read_first>
    - .planning/phases/15-slide-deck-homepage-3d-hero/15-RESEARCH.md section "Pitfall 7: FallbackPoster 404 until poster is captured"
    - .planning/phases/15-slide-deck-homepage-3d-hero/15-RESEARCH.md section "Environment Availability" (confirms public/hero-blob-poster.webp does not exist)
    - src/components/home-deck/fallback-poster.tsx (confirms src="/hero-blob-poster.webp" is the expected path)
  </read_first>

  <files>public/hero-blob-poster.webp</files>

  <action>
    Create a minimal valid 1x1 pixel dark WebP file at public/hero-blob-poster.webp. This file must be a real WebP binary (not a renamed PNG or empty file) so Next.js Image optimization does not error on it.

    Method: Use Node.js with the sharp package (already installed as a Next.js dependency) to generate a 1x1 pixel image with fill color #0a0a0a (near-black, matching the new --color-bg token) and save it as WebP with quality 80.

    Run this command:
    node -e "const sharp = require('sharp'); sharp({ create: { width: 1, height: 1, channels: 3, background: { r: 10, g: 10, b: 10 } } }).webp({ quality: 80 }).toFile('public/hero-blob-poster.webp', (err) => { if (err) { console.error(err); process.exit(1); } console.log('placeholder created'); });"

    If sharp is not directly resolvable from the project root, try: node -e "const sharp = require('./node_modules/sharp'); ..." with the same body.

    After creation, verify the file exists and is a non-empty binary with a valid WebP signature (bytes 0-3 are 52 49 46 46 = RIFF in ASCII).
  </action>

  <verify>
    <automated>ls -la public/hero-blob-poster.webp && file public/hero-blob-poster.webp | grep -i webp</automated>
  </verify>

  <acceptance_criteria>
    - public/hero-blob-poster.webp exists and is a non-zero-byte file
    - file command (or xxd first 4 bytes) identifies it as a WebP/RIFF file, not a text or empty file
    - The file is under 5KB (it's a 1x1 placeholder, not a full image)
    - No 404 appears in browser network tab when navigating to http://localhost:3000 on a narrow viewport that triggers FallbackPoster
  </acceptance_criteria>

  <done>public/hero-blob-poster.webp exists as a valid 1x1 dark WebP placeholder; FallbackPoster will not 404.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create test scaffolds for all four home/ test files</name>

  <read_first>
    - .planning/phases/15-slide-deck-homepage-3d-hero/15-PATTERNS.md sections for each test file (hero-blob.test.tsx, canvas-loader.test.tsx, explorative-homepage.test.tsx, section-building.test.tsx)
    - .planning/phases/15-slide-deck-homepage-3d-hero/15-RESEARCH.md section "Validation Architecture" (test framework: Vitest 4.x + @react-three/test-renderer 9.1.0 + @testing-library/react 16.3)
    - src/__tests__/home-deck/hero-blob.test.tsx (analog for R3F + CSM mock patterns — full file)
    - src/__tests__/home-deck/slides.test.tsx (analog for render + screen assertion pattern)
    - src/__tests__/home-deck/use-webgl-support.test.ts (analog for matchMedia mock pattern)
    - vitest.config.ts (confirm test root and globals setting)
  </read_first>

  <files>
    src/__tests__/home/hero-blob.test.tsx,
    src/__tests__/home/canvas-loader.test.tsx,
    src/__tests__/home/explorative-homepage.test.tsx,
    src/__tests__/home/section-building.test.tsx
  </files>

  <behavior>
    - hero-blob.test.tsx: HeroBlob renders scene graph without crash (with CSM mock)
    - canvas-loader.test.tsx: renders null before idle fires; renders canvas after setTimeout(300) in fake timers
    - explorative-homepage.test.tsx: pointer:coarse matchMedia → shows fallback-poster testid; WebGL2=null → shows fallback-poster testid
    - section-building.test.tsx: SectionBuilding renders text "Building", "Writing", "Doing"
  </behavior>

  <action>
    Create the directory src/__tests__/home/ (it does not exist). Create four test files using the exact patterns from PATTERNS.md. All four files will have failing imports (the src/components/home/ files they import do not exist yet) — this is expected. Use vi.mock to stub every missing module so the test files compile and either pass with skipped imports or skip gracefully. Do NOT use vi.importActual on files that don't exist.

    For hero-blob.test.tsx: Copy the vi.mock blocks for three/examples/jsm/environments/RoomEnvironment.js and three (PMREMGenerator stub) verbatim from the home-deck analog (src/__tests__/home-deck/hero-blob.test.tsx). Add the three-custom-shader-material/vanilla mock from PATTERNS.md. Mock @/components/home/hero-blob to return a stub component so the test compiles without the real file. Write the describe block with a single it test asserting the stub is defined (placeholder assertion until the real file exists in Wave 1).

    For canvas-loader.test.tsx: Mock next/dynamic to return a simple div stub. Mock @/components/home/canvas-loader as a stub. The test asserts the stub is importable. Use vi.useFakeTimers / vi.useRealTimers guards. Mark the mount-after-idle test as todo (vi.todo) so it compiles but skips until the real component exists.

    For explorative-homepage.test.tsx: Mock motion/react (useReducedMotion returns false), mock @/components/home/canvas-loader (returns div[data-testid="canvas-loader"]), mock @/components/home/fallback-poster (returns div[data-testid="fallback-poster"]), mock @/components/home/explorative-homepage as a stub. Include the standard matchMedia mock from PATTERNS.md Shared Patterns. Mark gate-assertion tests as vi.todo until the real component exists in Wave 1.

    For section-building.test.tsx: Mock @/components/v3/big-list and @/components/v3/section-label as simple div stubs. Mock @/components/home/section-building as a stub. The single test asserts the stub is importable. Mark BigList content assertion as vi.todo.

    All four files must compile (no TypeScript errors) and all non-todo tests must pass when the components don't exist yet. The pattern: mock the component module at the top, assert the mock is truthy, mark real behavior tests as vi.todo.
  </action>

  <verify>
    <automated>npx vitest run src/__tests__/home/ --reporter=verbose 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - src/__tests__/home/ directory exists with four .test.tsx files
    - npx vitest run src/__tests__/home/ exits 0 (all tests pass or are explicitly vi.todo skipped — no failures)
    - Each file contains the correct describe label matching its requirement ID: "TD-01", "TD-02", "TD-03 + HD-05", "HD-04"
    - hero-blob.test.tsx contains vi.mock("three-custom-shader-material/vanilla", ...)
    - explorative-homepage.test.tsx contains vi.spyOn(window, "matchMedia") mock with the standard MediaQueryList stub shape from PATTERNS.md
    - No TypeScript compile error reported by vitest (exit 0 is sufficient; no "cannot find module" hard failures)
  </acceptance_criteria>

  <done>Four test scaffold files exist, vitest run exits 0, mock patterns are in place for Wave 1 implementation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS token resolution | globals.css @theme tokens drive all Tailwind utility classes; incorrect values affect every rendered component globally |
| Public asset serving | public/ files are served statically; a corrupt placeholder could cause Image component errors at build time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01-SC | Tampering | npm install (Wave 0 has no new installs — deps installed in Wave 1 plan) | accept | No new packages in this plan; existing sharp dep used for placeholder generation |
| T-15-02 | Information Disclosure | globals.css CSS variables exposed in browser DevTools | accept | Design tokens are intentionally public; no secrets in CSS variables |
| T-15-03 | Denial of Service | Corrupt WebP placeholder causes Image optimization to error and crash the dev server | mitigate | Verify with `file public/hero-blob-poster.webp` that it is a valid WebP binary before committing; use sharp (not echo/dd) to generate it |
</threat_model>

<verification>
Run after all three tasks complete:

1. CSS palette check: grep -c '--color-bg:.*#0a0a0a' src/app/globals.css → must return 1
2. Deck CSS absent: grep -c 'deck-slide' src/app/globals.css → must return 0
3. Placeholder exists: ls -la public/hero-blob-poster.webp → must show non-zero size
4. Test suite: npx vitest run src/__tests__/home/ → must exit 0 with no failures
5. Build check: npx next build → must exit 0 (palette syntax must be valid CSS)
</verification>

<success_criteria>
- globals.css has Crimson Line palette (near-black bg, off-white text, crimson accent) with no Crimson Poster remnants and no deck CSS
- public/hero-blob-poster.webp is a valid 1x1 dark WebP placeholder
- Four test scaffold files exist in src/__tests__/home/ and vitest run exits 0
- Next.js build does not error (CSS is valid)
- Visual: opening /v3-specimen in browser shows text is now off-white on near-black (not dark-on-crimson)
</success_criteria>

<output>
Create .planning/phases/15-slide-deck-homepage-3d-hero/15-01-SUMMARY.md when done
</output>
