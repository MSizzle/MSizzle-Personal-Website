---
phase: 15
plan: 05
type: execute
wave: 4
depends_on:
  - 15-02
  - 15-03
  - 15-04
files_modified:
  - public/hero-blob-poster.webp
  - src/components/home/hero-blob-canvas.tsx
  - src/components/home/hero-blob.tsx
autonomous: false
requirements:
  - TD-01
  - TD-02
  - TD-03
  - HD-04
  - HD-05

must_haves:
  truths:
    - "D-15: GLB swap-in seam built (modelUrl prop, defaults null/procedural) — v1 ships on the procedural stand-in and is never blocked on the asset workstream"
    - "public/hero-blob-poster.webp is a real captured frame of the rendered WebGL hero (not the 1x1 placeholder)"
    - "Desktop (fine pointer, WebGL2) shows the live 3D blob on the homepage"
    - "Mobile viewport (< 760px) shows the poster image, no canvas"
    - "LCP element in Lighthouse trace is the h1 text or poster image — not the canvas"
    - "Scroll-cue: object scales from 1.0 toward 0.65 and camera.z moves from 4.4 to 5.6 as user scrolls first 30% of page"
    - "Vercel preview build passes (vercel build --prod exits 0)"
    - "Visual: /v3-specimen page renders correctly on near-black background (palette swap regression check)"
  artifacts:
    - path: "public/hero-blob-poster.webp"
      provides: "Real Playwright-captured poster frame replacing the 1x1 placeholder"
    - path: "src/components/home/hero-blob-canvas.tsx"
      provides: "Canvas with scroll-cue driver via addEffect + scrollProgress ref"
      contains: "addEffect"
    - path: "src/components/home/hero-blob.tsx"
      provides: "GPU morph blob with asset swap-in seam (modelUrl prop, null = procedural)"
      contains: "modelUrl"
  key_links:
    - from: "src/components/home/hero-blob-canvas.tsx"
      to: "@react-three/fiber addEffect"
      via: "useEffect(() => addEffect(() => { scrollProgressRef.current = ... }), [])"
      pattern: "addEffect"
    - from: "public/hero-blob-poster.webp"
      to: "src/components/home/fallback-poster.tsx"
      via: "Image src prop"
      pattern: "hero-blob-poster.webp"
---

<objective>
Finalize the v1 homepage: capture the real Playwright poster to replace the 1x1 placeholder, wire in the scroll-cue camera dolly/scale animation, add the GLB asset swap-in seam to HeroBlob, and run the Vercel preview build gate. Closes with a human visual verification checkpoint.

Purpose: The 1x1 placeholder was a temporary unblock — the real poster must exist for the mobile fallback to look correct. The scroll-cue (D-09) makes the hero responsive to user scrolling, delivering the "wandering" feel (D-06). The swap-in seam (D-15) ensures the procedural blob can be replaced with a real GLB model without touching the gate/canvas architecture. Vercel preview build confirms the v3 branch is shippable.

Output: Real poster WebP; scroll-cue wired in HeroBlobCanvas; modelUrl seam in HeroBlob; Vercel preview build passes; human confirms visual output.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-slide-deck-homepage-3d-hero/15-RESEARCH.md
@.planning/phases/15-slide-deck-homepage-3d-hero/15-PATTERNS.md

Prior plan context needed:
@.planning/phases/15-slide-deck-homepage-3d-hero/15-02-SUMMARY.md
@.planning/phases/15-slide-deck-homepage-3d-hero/15-03-SUMMARY.md
@.planning/phases/15-slide-deck-homepage-3d-hero/15-04-SUMMARY.md
</context>

<interfaces>
<!-- Key patterns from RESEARCH.md the executor needs. -->

addEffect scroll-cue pattern (RESEARCH.md Pattern 4, Option 2):
import { addEffect } from "@react-three/fiber";
useEffect(() => {
  return addEffect(() => {
    const max = document.body.scrollHeight - window.innerHeight;
    scrollProgressRef.current = max > 0 ? Math.min(window.scrollY / max, 1) : 0;
  });
}, []);

useFrame scroll-driven camera dolly (RESEARCH.md Pattern 4):
useFrame(({ camera }) => {
  const p = scrollProgressRef.current;
  camera.position.z = 4.4 + p * 1.2;
  if (blobRef.current) blobRef.current.scale.setScalar(1.0 - p * 0.35);
});

GLB swap-in seam pattern (D-15 — build the seam, not the GLB):
HeroBlob accepts an optional modelUrl prop (string | null). When null (default), render the procedural blob mesh as today. When modelUrl is set, render a useGLTF-loaded model instead. This seam does NOT need to load a real GLB in v1 — modelUrl defaults to null and the procedural path is the v1 behavior.

Playwright poster capture (RESEARCH.md Pattern 6):
npx playwright screenshot http://localhost:3000 public/hero-blob-poster.png --viewport-size=800,800 --wait-for-selector "[data-testid='hero-canvas-container']"
npx sharp-cli -i public/hero-blob-poster.png -o public/hero-blob-poster.webp --format webp --quality 85
OR using canvas.toDataURL in-browser dev capture as fallback if Playwright cannot access GPU.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add scroll-cue to HeroBlobCanvas + GLB swap-in seam to HeroBlob</name>

  <read_first>
    - src/components/home/hero-blob-canvas.tsx (current file from Plan 02 — add scroll-cue driver here)
    - src/components/home/hero-blob.tsx (current file from Plan 02 — add modelUrl prop/seam here)
    - .planning/phases/15-slide-deck-homepage-3d-hero/15-RESEARCH.md section "Pattern 4: Lenis Scroll Progress in R3F useFrame" (Option 2: addEffect from @react-three/fiber — recommended approach)
    - .planning/phases/15-slide-deck-homepage-3d-hero/15-CONTEXT.md D-09 (scroll-cue: object scales / camera dollies on scroll via Lenis) and D-15 (procedural stand-in; build swap-in seam)
  </read_first>

  <files>
    src/components/home/hero-blob-canvas.tsx,
    src/components/home/hero-blob.tsx
  </files>

  <action>
    Update src/components/home/hero-blob-canvas.tsx to add the scroll-cue driver:

    Add import: addEffect from "@react-three/fiber", useEffect and useRef from "react".
    Add a scrollProgressRef: const scrollProgressRef = useRef(0).
    Add a useEffect that registers an addEffect callback: return addEffect(() => { const max = document.body.scrollHeight - window.innerHeight; scrollProgressRef.current = max > 0 ? Math.min(window.scrollY / max, 1) : 0; }). The cleanup returns the unsubscribe function that addEffect returns.

    Pass scrollProgressRef to a new inline ScrollCueDriver component inside the Canvas, or integrate the useFrame call directly inside a child functional component that lives inside the Canvas JSX tree. The scroll-cue useFrame: camera.position.z = 4.4 + p * 1.2 (camera pulls back from 4.4 to 5.6 over scroll); blob scale via a blobRef passed down from HeroBlob or accessed via useRef on a group wrapper. Use the simpler approach: add a Group ref that wraps HeroBlob and scale it via group.scale.setScalar(1.0 - p * 0.35) in useFrame. Scale only applies over first 30% scroll (clamp p to 0.3 before the scale calculation to prevent the blob from disappearing).

    The camera dolly and scale should be smooth. Since this reads DOM scroll and updates in R3F's RAF loop, it is one frame behind — acceptable for a subtle easing animation.

    Separately, add a data-testid="hero-canvas-container" attribute to a wrapping div outside the Canvas element (in hero-blob-canvas.tsx). This testid is used by the Playwright poster capture command to know when the canvas has settled.

    Update src/components/home/hero-blob.tsx to add the GLB swap-in seam per D-15:

    Add an optional modelUrl prop: modelUrl?: string | null (default null). The existing procedural blob render is the v1 behavior (modelUrl === null). Add a conditional: if (modelUrl) { /* future: load GLB via useGLTF(modelUrl) and return the model */ return null; }. In v1, modelUrl is always null so this branch never executes. This seam ensures the asset swap-in can happen by changing a single prop without restructuring the canvas architecture.

    The modelUrl prop stub does NOT need a useGLTF import — the GLB loading is the v2/swap-in workstream concern. Just add the prop type and the conditional guard that returns null when modelUrl is set. This is the contractual seam, not the implementation.
  </action>

  <verify>
    <automated>grep -c 'addEffect' src/components/home/hero-blob-canvas.tsx && grep -c 'modelUrl' src/components/home/hero-blob.tsx && npx vitest run src/__tests__/home/ --reporter=verbose 2>&1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - src/components/home/hero-blob-canvas.tsx contains "addEffect" import and usage
    - src/components/home/hero-blob-canvas.tsx contains "scrollProgressRef" or equivalent scroll progress ref
    - hero-blob-canvas.tsx useFrame updates camera.position.z based on scroll progress (value range 4.4 to ~5.6)
    - hero-blob-canvas.tsx contains data-testid="hero-canvas-container" on a wrapper element
    - src/components/home/hero-blob.tsx contains "modelUrl" prop (the GLB swap-in seam)
    - hero-blob.tsx defaults modelUrl to null (procedural blob is v1 behavior — no real GLB loaded)
    - npx vitest run src/__tests__/home/ exits 0 (all four test files still pass)
    - npx next build exits 0
  </acceptance_criteria>

  <done>Scroll-cue wired in hero-blob-canvas.tsx; GLB seam added to hero-blob.tsx; tests still pass; build clean.</done>
</task>

<task type="auto">
  <name>Task 2: Capture real hero poster with Playwright</name>

  <read_first>
    - .planning/phases/15-slide-deck-homepage-3d-hero/15-RESEARCH.md section "Pattern 6: Poster Capture Workflow" (three capture approaches; Playwright recommended; fallback to canvas.toDataURL)
    - .planning/phases/15-slide-deck-homepage-3d-hero/15-RESEARCH.md section "Environment Availability" (Playwright 1.61.0 confirmed available; sharp confirmed available)
    - .planning/phases/15-slide-deck-homepage-3d-hero/15-RESEARCH.md section "Assumption A4" (Playwright may not capture WebGL on macOS — fallback to canvas.toDataURL)
    - public/hero-blob-poster.webp (current 1x1 placeholder from Plan 15-01 — will be overwritten)
  </read_first>

  <files>public/hero-blob-poster.webp</files>

  <action>
    Start the Next.js dev server: npx next dev &

    Wait for the dev server to be ready (watch for "Ready" in output, typically 3-5 seconds with Turbopack).

    Navigate the page and let the WebGL canvas settle. The canvas mounts after requestIdleCallback fires (~200-500ms after LCP). Allow at least 2-3 seconds total.

    Run Playwright screenshot capture targeting the hero container:
    npx playwright screenshot http://localhost:3000 public/hero-blob-poster.png --viewport-size=800,800 --wait-for-selector "[data-testid='hero-canvas-container']" --timeout 10000

    If Playwright screenshot successfully captures (file exists, > 50KB indicating real rendered content):
    Convert to WebP with sharp: node -e "require('./node_modules/sharp')('public/hero-blob-poster.png').webp({ quality: 85 }).toFile('public/hero-blob-poster.webp', (e) => { if(e) { console.error(e); process.exit(1); } console.log('poster captured'); })"
    Remove the intermediate PNG: rm public/hero-blob-poster.png

    If Playwright produces a file < 10KB or all-black/transparent image (GPU not accessible in screenshot mode — see Assumption A4):
    Fall back to in-browser canvas capture. Add a temporary "DEV CAPTURE" button to hero-blob-canvas.tsx that calls gl.domElement.toBlob((blob) => { const a=document.createElement('a'); a.href=URL.createObjectURL(blob); a.download='hero-blob-poster.webp'; a.click(); }, 'image/webp', 0.85). Visit http://localhost:3000, click the button, save the downloaded file to public/hero-blob-poster.webp. Then remove the capture button from the component.

    After capture, verify the WebP file is > 20KB (real rendered content, not a blank frame). Stop the dev server.
  </action>

  <verify>
    <automated>ls -la public/hero-blob-poster.webp | awk '{print $5}' | xargs -I{} sh -c 'test {} -gt 20000 && echo "PASS: poster > 20KB" || echo "FAIL: poster too small"'</automated>
  </verify>

  <acceptance_criteria>
    - public/hero-blob-poster.webp exists and is greater than 20KB (real rendered frame, not a blank or 1x1 placeholder)
    - The file is a valid WebP binary (file command identifies it as WebP/RIFF)
    - Visually (open the file): the poster shows the dark near-black hero area with the blob shape visible, not a blank black frame
    - The file dimensions are at least 400x400px (captured from an 800x800 viewport)
    - The 1x1 placeholder from Plan 15-01 has been overwritten by this real capture
  </acceptance_criteria>

  <done>Real hero poster captured and saved to public/hero-blob-poster.webp; FallbackPoster now shows actual hero frame.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification + Vercel preview build gate</name>

  <what-built>
    Claude has built the complete Phase 15 v1 WebGL homepage:
    - Crimson Line palette (near-black background, off-white text, crimson accent)
    - Desktop: live WebGL hero blob with GPU vertex-shader morph, bloom postprocessing, crimson rim, podium, scroll-cue camera dolly
    - Mobile/touch/reduced-motion/no-WebGL2: static hero-blob-poster.webp via FallbackPoster (fetchPriority="high")
    - Four section beats: Building (BigList index), Writing (static essays), Newsletter, Footer
    - After-LCP canvas mount (requestIdleCallback + Safari fallback)
    - Vercel preview build run: vercel build --prod (will be run before this checkpoint)

    Run before checkpoint:
    npx next dev (or use an already-running server from Task 2)
    vercel build --prod → confirm exit 0
  </what-built>

  <how-to-verify>
    Please verify the following, in order:

    1. PALETTE CHECK — Visit http://localhost:3000/v3-specimen (or start the dev server if not running)
       - Background should be near-black (#0a0a0a)
       - Text should be off-white (#f5f5f0)
       - Accent elements (buttons, links on hover, section labels) should be crimson (#e23838)
       - No red-on-red: if you see dark text on a crimson background, the palette swap did not apply

    2. DESKTOP HERO — On a desktop browser (not mobile emulation), visit http://localhost:3000
       - The 3D blob should appear and animate autonomously (morphing blob shape)
       - After ~0.5-2 seconds (after-LCP delay), the canvas should mount
       - Scrolling down should cause the blob to scale down and camera to dolly back slightly
       - The blob should have a visible crimson rim glow and bloom effect

    3. MOBILE FALLBACK — Open Chrome DevTools, switch to mobile viewport (iPhone 12 or similar, < 760px)
       - Refresh the page
       - A static image (the poster) should appear instead of the canvas
       - The poster image should not be broken/404 — it should show the hero blob shape

    4. SCROLL STORY — Scrolling down the homepage should reveal:
       - Section "01 What I'm" with Building / Writing / Doing links
       - Section "02 Writing" with essay links and "All essays" button
       - A Newsletter section
       - A footer section with nav columns (Site, More, Elsewhere, Contact)
       - NO shared footer appearing twice

    5. VERCEL BUILD — Run in terminal: vercel build --prod
       - Must exit 0 with no errors
       - Check the terminal output for any TypeScript or build errors

    Type "approved" if all five checks pass.
    Type issues like "fail: [specific problem]" if anything is wrong and Claude will fix it.
  </how-to-verify>

  <resume-signal>Type "approved" to complete Phase 15 v1, or describe any issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playwright screenshot | Launches a browser to screenshot localhost — no external network access, no user data |
| addEffect / scroll progress | Reads window.scrollY and document.body.scrollHeight — DOM APIs, no user input |
| Vercel build | Production build gate — no secrets exposed in build output |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-14 | Denial of Service | scroll-cue camera dolly causing visual disorientation or seizure risk | mitigate | Scale clamped to 30% scroll max (prevents blob from disappearing); motion obeys prefers-reduced-motion gate already in place in explorative-homepage.tsx — reduced-motion users never see the canvas |
| T-15-15 | Information Disclosure | Dev capture button left in production code (canvas.toDataURL path) | mitigate | Acceptance criteria for Task 2 requires capture button removal after use; acceptance criteria for Task 1 confirms data-testid is the only dev addition (testid on a container is safe) |
| T-15-16 | Tampering | vercel build --prod failing due to missing env vars | accept | Build uses NEXT_PUBLIC_UMAMI_* from Vercel environment; local build may fail on env; use vercel build (without --prod) locally if env vars are missing, and accept that the full Vercel CI build is the authoritative gate |
</threat_model>

<verification>
Run after all tasks, before the checkpoint:
1. Scroll cue: grep -c 'addEffect' src/components/home/hero-blob-canvas.tsx → returns 1
2. Swap seam: grep -c 'modelUrl' src/components/home/hero-blob.tsx → returns 1
3. Real poster: ls -la public/hero-blob-poster.webp | awk '{print $5}' → must be > 20000 bytes
4. Full test suite: npx vitest run → exits 0
5. Build: npx next build → exits 0
6. Requirement coverage: grep requirements 15-0{1..5}-PLAN.md confirms TD-01 TD-02 TD-03 HD-04 HD-05 all present
</verification>

<success_criteria>
- Real poster captured (> 20KB WebP)
- Scroll-cue camera dolly + scale wired via addEffect in hero-blob-canvas.tsx
- GLB modelUrl swap-in seam exists in hero-blob.tsx (defaults null = procedural)
- All five vitest test files pass (npx vitest run exits 0)
- npx next build exits 0
- Human visual verification: near-black bg, off-white text, crimson accent, working WebGL hero on desktop, poster on mobile, four section beats, no duplicate footer
- PHASE 15 v1 COMPLETE
</success_criteria>

<output>
Create .planning/phases/15-slide-deck-homepage-3d-hero/15-05-SUMMARY.md when done
</output>
