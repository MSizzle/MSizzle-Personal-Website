---
phase: 15
plan: 04
type: execute
wave: 3
depends_on:
  - 15-01
  - 15-03
files_modified:
  - src/components/home/fallback-poster.tsx
  - src/components/home/section-building.tsx
  - src/components/home/section-writing.tsx
  - src/components/home/section-newsletter.tsx
  - src/components/home/section-footer.tsx
  - src/__tests__/home/section-building.test.tsx
  - src/__tests__/home/fallback-poster.test.tsx
autonomous: true
requirements:
  - HD-04
  - HD-05
  - TD-03

must_haves:
  truths:
    - "fallback-poster.tsx has fetchPriority='high' and priority on the Next Image (LCP quirk — memory nextjs16-fetchpriority-quirk)"
    - "fallback-poster.tsx src is /hero-blob-poster.webp (the placeholder created in 15-01 exists)"
    - "section-building.tsx renders a BigList with items for Building (href /projects), Writing (href /writing), Doing (href https://prometheus.today)"
    - "section-building.tsx does NOT have deck-slide or deck-slide--index class names"
    - "section-writing.tsx shows at least 2 static essay links and a Button linking to /writing"
    - "section-newsletter.tsx has min-h-dvh class and no deck-slide classes"
    - "section-footer.tsx renders nav columns (Site, More, Elsewhere, Contact) and no deck-* classes"
    - "HD-04 test in section-building.test.tsx exits 0 (not todo)"
    - "fallback-poster.test.tsx TD-03/HD-05 test exits 0 (not todo)"
    - "npx next build exits 0 with all six new components"
  artifacts:
    - path: "src/components/home/fallback-poster.tsx"
      provides: "Mobile/fallback hero image (SSR'd, LCP path)"
      contains: "fetchPriority"
    - path: "src/components/home/section-building.tsx"
      provides: "Big-type building/writing/doing index (HD-04)"
      contains: "BigList"
    - path: "src/components/home/section-writing.tsx"
      provides: "Static writing section with essay links"
      contains: "SectionLabel"
    - path: "src/components/home/section-newsletter.tsx"
      provides: "Newsletter section beat"
      contains: "NewsletterCarousel"
    - path: "src/components/home/section-footer.tsx"
      provides: "Footer beat (homepage's only footer)"
      contains: "Let's be friends"
  key_links:
    - from: "src/components/home/fallback-poster.tsx"
      to: "public/hero-blob-poster.webp"
      via: "Next Image src prop"
      pattern: "hero-blob-poster.webp"
    - from: "src/components/home/section-building.tsx"
      to: "src/components/v3/big-list.tsx"
      via: "BigList component import"
      pattern: "BigList"
    - from: "src/components/layout/conditional-footer.tsx"
      to: "src/components/home/section-footer.tsx"
      via: "conditional-footer returns null on pathname==='/' so section-footer is the only homepage footer"
      pattern: "pathname.*/"
---

<objective>
Build all content components needed to complete the scroll-story homepage: the mobile fallback poster, and the four section beats (Building, Writing, Newsletter, Footer). These are all Server Components or simple client components with no new architectural complexity — they follow established patterns from the deck's slide components.

Purpose: With gate logic (Plan 03) and the hero (Plan 02) in place, the homepage has a structure but no content below the hero. These five components fill the scroll-story and make the page shippable as a complete v1 homepage. SectionFooter replaces the shared InkFooter on the homepage (conditional-footer.tsx already suppresses on "/" — confirmed in PATTERNS.md).

Output: fallback-poster.tsx, section-building.tsx, section-writing.tsx, section-newsletter.tsx, section-footer.tsx — all in src/components/home/. Updated section-building test. Updated fallback-poster test. Build passes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-slide-deck-homepage-3d-hero/15-RESEARCH.md
@.planning/phases/15-slide-deck-homepage-3d-hero/15-PATTERNS.md

Prior plan context needed:
@.planning/phases/15-slide-deck-homepage-3d-hero/15-01-SUMMARY.md
@.planning/phases/15-slide-deck-homepage-3d-hero/15-03-SUMMARY.md
</context>

<interfaces>
<!-- Analog files the executor must read before implementing. Extracted from PATTERNS.md. -->

src/components/home-deck/fallback-poster.tsx — analog for fallback-poster (verbatim copy, path update only)
  Key: import Image from "next/image"; Image props priority + fetchPriority="high" + loading="eager" + sizes + fill + className="object-contain"

src/components/home-deck/slide-index.tsx — analog for section-building (strip deck-slide, keep BigList)
  Key: SectionLabel numeral="02", BigList items array pattern

src/components/home-deck/slide-hero.tsx — analog for section-writing (static editorial content pattern)
src/app/writing/page.tsx — analog for section-writing content shape (static essay list)

src/components/home-deck/slide-newsletter.tsx — analog for section-newsletter (strip deck-slide, keep content)
  Key: SectionLabel, heading text, paragraph, NewsletterCarousel, Button

src/components/home-deck/slide-footer.tsx — analog for section-footer (strip deck-* classes, keep all link columns)
  Key: "Let's be friends." heading, nav columns (Site, More, Elsewhere, Contact), copyright text

src/components/v3/big-list.tsx — BigList component (read to understand the items prop shape)
src/components/v3/section-label.tsx — SectionLabel component (read to understand numeral prop)
src/components/v3/button.tsx — Button component (read to understand href prop)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create fallback-poster.tsx (mobile / no-WebGL LCP path) + update fallback-poster test</name>

  <read_first>
    - src/components/home-deck/fallback-poster.tsx (full file — 34 lines; verbatim source for migration; read entirely)
    - .planning/phases/15-slide-deck-homepage-3d-hero/15-PATTERNS.md section "src/components/home/fallback-poster.tsx" (exact fields to carry forward, sizes to update)
    - .planning/phases/15-slide-deck-homepage-3d-hero/15-RESEARCH.md section "Pattern 6: Poster Capture Workflow" (confirms fetchPriority="high" requirement from memory nextjs16-fetchpriority-quirk)
    - src/__tests__/home/fallback-poster.test.tsx (scaffold from 15-01 Task 3 — promote vi.todo to real assertion here)
  </read_first>

  <files>
    src/components/home/fallback-poster.tsx,
    src/__tests__/home/fallback-poster.test.tsx
  </files>

  <action>
    Create src/components/home/fallback-poster.tsx as a VERBATIM copy of src/components/home-deck/fallback-poster.tsx with these two changes only:

    1. The import of cn: verify the import path "@/utils/cn" still resolves from the new location — it will, since @ maps to src/. No change needed.
    2. The sizes attribute: update from the deck-specific sizes to "(max-width: 760px) 100vw, 45vw" — this makes the poster full-width on mobile (where it will always show) and 45vw on desktop viewports where it would only appear for reduced-motion/no-WebGL users.

    Every other line carries forward verbatim. The critical properties that MUST be preserved exactly:
    - priority (boolean, no value — triggers preload link in head)
    - fetchPriority="high" — this is the key Next 16 quirk: priority alone does not emit the fetchpriority resource hint in Next 16. This explicit prop is mandatory for LCP image discovery. See memory entry nextjs16-fetchpriority-quirk.
    - loading="eager" (belt-and-suspenders alongside priority)
    - fill (so the image fills its positioned parent)
    - src="/hero-blob-poster.webp" (matches the placeholder created in 15-01)

    Do NOT add "use client" to fallback-poster.tsx — it should remain a Server Component (no client hooks needed). Next Image with priority works in Server Components.

    After creating fallback-poster.tsx, update src/__tests__/home/fallback-poster.test.tsx: promote the vi.todo stubs to real assertions. Remove the mock of @/components/home/fallback-poster (import the real component). Mock next/image as: vi.mock("next/image", () => ({ default: (props: Record<string, unknown>) => React.createElement("img", { ...props, "data-testid": "fallback-image" }) })). Render FallbackPoster and assert: (a) getByTestId("fallback-image") is in the document; (b) the img has src containing "hero-blob-poster.webp".
  </action>

  <verify>
    <automated>npx vitest run src/__tests__/home/fallback-poster.test.tsx --reporter=verbose 2>&1 | tail -15</automated>
  </verify>

  <acceptance_criteria>
    - src/components/home/fallback-poster.tsx exists
    - Contains fetchPriority="high" (exact string with attribute name casing as shown)
    - Contains priority (boolean Image prop)
    - Contains loading="eager"
    - Contains src="/hero-blob-poster.webp"
    - Contains fill (fills the absolute-positioned parent in explorative-homepage.tsx)
    - Does NOT have "use client" (Server Component)
    - Does NOT have deck-specific classes or references
    - File is under 50 lines (it is a simple wrapper, not a complex component)
    - npx vitest run src/__tests__/home/fallback-poster.test.tsx exits 0 with the TD-03/HD-05 test passing (not todo)
    - grep 'fetchPriority="high"' src/components/home/fallback-poster.tsx returns 1 match
  </acceptance_criteria>

  <done>fallback-poster.tsx exists with correct LCP props; fallback-poster test passes; mobile/fallback path renders the placeholder poster.</done>
</task>

<task type="auto">
  <name>Task 2: Create section-building.tsx + section-writing.tsx + update building test</name>

  <read_first>
    - src/components/home-deck/slide-index.tsx (full analog — section-building source; read entirely)
    - src/components/home-deck/slide-hero.tsx (analog for editorial content pattern)
    - src/app/writing/page.tsx (read for real essay titles to use as static content)
    - src/components/v3/big-list.tsx (read to understand the items prop: label, href, tag, outline fields)
    - src/components/v3/button.tsx (read to understand href prop and className behavior)
    - .planning/phases/15-slide-deck-homepage-3d-hero/15-PATTERNS.md sections for section-building.tsx and section-writing.tsx
    - src/__tests__/home/section-building.test.tsx (test scaffold to update with real assertions)
  </read_first>

  <files>
    src/components/home/section-building.tsx,
    src/components/home/section-writing.tsx,
    src/__tests__/home/section-building.test.tsx
  </files>

  <action>
    Create src/components/home/section-building.tsx. No "use client" — this is a Server Component. Import BigList from "@/components/v3/big-list" and SectionLabel from "@/components/v3/section-label".

    Layout class: "min-h-dvh flex flex-col justify-center px-[8vw] py-[15vh]" — strip the "deck-slide deck-slide--index" classes from the analog. The section element class must NOT include any deck-* classes.

    SectionLabel numeral="01" with content "What I'm" — carry forward from analog (numeral changes from "02" to "01" since deck slide numbering is superseded).

    BigList items array (per D-12 and PATTERNS.md):
    - { label: "Building", href: "/projects", tag: "WORKS" }
    - { label: "Writing", href: "/writing", tag: "ESSAYS" }
    - { label: "Doing", href: "https://prometheus.today", tag: "PROMETHEUS", outline: true }

    Use "Doing" (not "Prometheus") as the label — copy rule: no em dashes, and "Prometheus.today" is the link, matching existing site copy conventions.

    Create src/components/home/section-writing.tsx. No "use client" — Server Component. Import Link from "next/link", SectionLabel from "@/components/v3/section-label", Button from "@/components/v3/button".

    Read src/app/writing/page.tsx to get 2-3 real essay titles that actually exist on the site. Use those real titles in the FEATURED_ESSAYS static array (D-13: static/curated in v1, Notion-wiring is Phase 16). The array shape: [{ title: string, href: string, date: string }]. Use the actual writing page slugs from the writing index — do not invent fake slugs.

    Layout class: "min-h-dvh flex flex-col justify-center px-[8vw] py-[15vh]". SectionLabel numeral="02" content "Writing". Below the label, map over FEATURED_ESSAYS to render Link rows (each a flex row with justify-between, border-t border-border, essay title left in font-display font-bold text-xl text-text, date right in font-mono text-xs text-text-muted). Below the list, a div with mt-8 border-t border-border pt-6 containing Button href="/writing" with label "All essays".

    After creating section-building.tsx, update src/__tests__/home/section-building.test.tsx to replace vi.todo stubs with real assertions. Remove the mock of @/components/home/section-building (import the real component). Also mock @/components/v3/big-list as a component that renders its items labels (so screen.getByText can find "Building", "Writing", "Doing"). Mock @/components/v3/section-label as a div. The test imports SectionBuilding and calls render(<SectionBuilding />), then asserts getByText(/Building/i), getByText(/Writing/i), getByText(/Doing/i).
  </action>

  <verify>
    <automated>npx vitest run src/__tests__/home/section-building.test.tsx --reporter=verbose 2>&1 | tail -15</automated>
  </verify>

  <acceptance_criteria>
    - src/components/home/section-building.tsx exists
    - section-building.tsx does NOT contain "deck-slide" class name
    - section-building.tsx contains "BigList" import and usage
    - section-building.tsx BigList items include href="/projects", href="/writing", href="https://prometheus.today"
    - src/components/home/section-writing.tsx exists
    - section-writing.tsx does NOT contain "deck-slide" class name
    - section-writing.tsx contains FEATURED_ESSAYS array with at least 2 entries with real slugs (not placeholder-slug-1)
    - section-writing.tsx contains Button with href="/writing"
    - npx vitest run src/__tests__/home/section-building.test.tsx exits 0 with HD-04 test passing (not todo)
    - Neither component has "use client" (both are Server Components — no hooks needed)
  </acceptance_criteria>

  <done>SectionBuilding and SectionWriting exist as Server Components; HD-04 test passes with real assertions.</done>
</task>

<task type="auto">
  <name>Task 3: Create section-newsletter.tsx + section-footer.tsx</name>

  <read_first>
    - src/components/home-deck/slide-newsletter.tsx (full analog — 57 lines; section-newsletter source; read entirely)
    - src/components/home-deck/slide-footer.tsx (full analog — 146 lines; section-footer source; read entirely)
    - .planning/phases/15-slide-deck-homepage-3d-hero/15-PATTERNS.md sections for section-newsletter.tsx and section-footer.tsx (exact class changes)
    - src/components/layout/conditional-footer.tsx (verify it returns null on pathname==="/" — confirmed in PATTERNS.md; no change needed)
  </read_first>

  <files>
    src/components/home/section-newsletter.tsx,
    src/components/home/section-footer.tsx
  </files>

  <action>
    Create src/components/home/section-newsletter.tsx. No "use client" unless the analog uses client hooks. Read the full analog (slide-newsletter.tsx) to determine if it needs "use client". The section carries all content verbatim from the analog with ONE change: replace the outer section element class.

    Class change: from "deck-slide deck-slide--newsletter flex flex-col justify-center min-h-dvh px-[8vw]" to "min-h-dvh flex flex-col justify-center px-[8vw] py-[15vh]"

    All interior content (SectionLabel, heading, paragraph, NewsletterCarousel import and usage, Button) carries forward verbatim from the analog (slide-newsletter.tsx lines 12-57). Do not change copy, component usage, or props. The Crimson Line palette tokens (text-text, border-border, hover:text-accent, accent) render correctly on dark background after the 15-01 palette swap.

    Create src/components/home/section-footer.tsx. No "use client". Read the full analog (slide-footer.tsx — 146 lines) to get all link columns. The change is ONE class replacement on the outermost section element.

    Class change: from "deck-slide deck-slide--footer deck-foot flex flex-col justify-end h-auto min-h-full pt-[12vh] px-[8vw] pb-14" to "flex flex-col justify-end min-h-[40vh] px-[8vw] pb-14 pt-[12vh]"

    All interior content (the "Let's be friends." heading, four nav columns — Site, More, Elsewhere, Contact — with all their link items, copyright line, colophon line, and the horizontal rule) carries forward verbatim from the analog. Do not change copy, hrefs, or token class names. The site copy rules apply: no em dashes, "Georgetown University" only, sole professional identity Founder of Prometheus — these rules were already applied in the deck version.

    After creating both files, do NOT modify conditional-footer.tsx — it already returns null on pathname==="/" (confirmed in PATTERNS.md "conditional-footer.tsx" section). SectionFooter will be the only footer on the homepage.
  </action>

  <verify>
    <automated>npx vitest run src/__tests__/home/ --reporter=verbose 2>&1 | tail -10 && npx next build 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - src/components/home/section-newsletter.tsx exists
    - section-newsletter.tsx does NOT contain "deck-slide" class name
    - section-newsletter.tsx contains "min-h-dvh" and "NewsletterCarousel" (content preserved)
    - src/components/home/section-footer.tsx exists
    - section-footer.tsx does NOT contain "deck-slide" or "deck-foot" or "deck-slide--footer" class names
    - section-footer.tsx contains "Let's be friends" (the heading copy — verbatim from analog)
    - section-footer.tsx contains at least four distinct nav link column labels (Site, More, Elsewhere, Contact or similar)
    - npx vitest run src/__tests__/home/ exits 0 (all five test files pass — this is the primary verify)
    - npx next build exits 0 with all six home/ components in place (fallback-poster, section-building, section-writing, section-newsletter, section-footer; hero components from Plan 02)
    - No TypeScript errors reported in home/ components during build
  </acceptance_criteria>

  <done>All four section beats and fallback-poster exist; full home/ test suite passes; build passes; homepage is a complete shippable v1.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static essay links | hrefs in SectionWriting are hardcoded static strings — no user input |
| External link in SectionBuilding | https://prometheus.today is an external href; no rel="noopener noreferrer" enforcement needed (opens in same tab by default with Link from next/link) |
| FallbackPoster image source | Points to public/ static file; no user-controlled path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-11 | Information Disclosure | LCP image (FallbackPoster) missing fetchPriority causing LCP regression | mitigate | Acceptance criteria asserts fetchPriority="high" is present; this is the top LCP regression risk for mobile path (memory nextjs16-fetchpriority-quirk) |
| T-15-12 | Spoofing | External link in SectionBuilding (https://prometheus.today) | accept | Link is to a site owned/operated by Monty; intentional external link; standard rel behavior is acceptable |
| T-15-13 | Tampering | Deck CSS classes accidentally left on section components causing wrong layout | mitigate | Acceptance criteria asserts deck-slide class is absent from each section component; build check runs as final verify |
</threat_model>

<verification>
Run after all three tasks complete:
1. LCP props: grep 'fetchPriority' src/components/home/fallback-poster.tsx → must show "high"
2. No deck classes: grep -r 'deck-slide' src/components/home/ → must return 0 matches
3. BigList wired: grep 'BigList' src/components/home/section-building.tsx → must match
4. Footer heading: grep "Let's be friends" src/components/home/section-footer.tsx → must match
5. Test: npx vitest run src/__tests__/home/ → exits 0 (all five test files)
6. Build: npx next build → exits 0
</verification>

<success_criteria>
- fallback-poster.tsx has fetchPriority="high", priority, loading="eager", src="/hero-blob-poster.webp"
- fallback-poster.test.tsx TD-03/HD-05 test passes (not todo)
- section-building.tsx has BigList with Building/Writing/Doing items and SectionLabel numeral="01"
- section-writing.tsx has 2-3 real essay links and Button href="/writing"
- section-newsletter.tsx has min-h-dvh and all newsletter content from the deck analog
- section-footer.tsx has all four nav columns and no deck-* class names
- HD-04 test passes; full vitest suite exits 0 for src/__tests__/home/
- npx next build exits 0 — complete shippable v1 homepage
</success_criteria>

<output>
Create .planning/phases/15-slide-deck-homepage-3d-hero/15-04-SUMMARY.md when done
</output>
