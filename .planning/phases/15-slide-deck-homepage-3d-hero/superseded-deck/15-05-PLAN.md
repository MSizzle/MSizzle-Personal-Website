---
phase: 15-slide-deck-homepage-3d-hero
plan: 05
type: execute
wave: 2
depends_on: [15-02, 15-03, 15-04]
files_modified:
  - src/components/home-deck/deck-homepage.tsx
  - src/app/page.tsx
  - src/app/layout.tsx
  - src/app/globals.css
autonomous: true
requirements: [HD-01, HD-02, HD-03, HD-04, HD-05, TD-01, TD-02, TD-03]

must_haves:
  truths:
    - "src/app/page.tsx is a Server Component that renders DeckHomepage and JsonLd; no Notion data fetched on homepage"
    - "DeckHomepage is a 'use client' orchestrator that conditionally renders the deck or native-scroll fallback"
    - "Lenis is stopped on homepage mount (showDeck=true) and started on unmount — via useLenisControl()"
    - "HeroBlobCanvas is dynamically imported with ssr:false inside DeckHomepage (never from a Server Component)"
    - "WebGL detection uses webgl2 + failIfMajorPerformanceCaveat:true"
    - "Touch/small-screen detection: (pointer:coarse) OR innerWidth<760 → native scroll"
    - "prefers-reduced-motion → native scroll (same path as touch per D-09)"
    - "The deck layout uses position:fixed for #objstage and #scroller; background stays static"
    - "objEnter is called on every slide change via onSlideChange callback"
    - "Progress dots are rendered and highlight the active slide"
    - "The global <InkFooter /> in layout.tsx is suppressed on pathname=='/'"
    - "MainOffset padding does not push the fixed deck layout (homepage bypasses it)"
    - "Native-scroll fallback renders all 5 slides as a normal vertically scrollable page with poster"
  artifacts:
    - path: src/components/home-deck/deck-homepage.tsx
      provides: Full deck orchestrator (Client Component)
      exports: [DeckHomepage]
    - path: src/app/page.tsx
      provides: Homepage Server Component
      exports: [default Home, revalidate]
    - path: src/app/layout.tsx
      provides: InkFooter suppressed on /
  key_links:
    - from: src/components/home-deck/deck-homepage.tsx
      to: src/components/home-deck/hero-blob-canvas.tsx
      via: dynamic(() => import('./hero-blob-canvas'), { ssr: false })
      pattern: "ssr: false"
    - from: src/components/home-deck/deck-homepage.tsx
      to: src/components/providers/lenis-provider.tsx
      via: useLenisControl()
      pattern: "useLenisControl"
    - from: src/app/layout.tsx
      to: pathname === "/"
      via: usePathname() guard around InkFooter
      pattern: "InkFooter"
    - from: src/components/home-deck/deck-homepage.tsx
      to: scrollerRef (HTMLDivElement)
      via: useDeckController({ scrollerRef })
      pattern: "scrollerRef"
---

<objective>
Wire all Phase 15 pieces together: DeckHomepage orchestrator, page.tsx replacement, InkFooter suppression in layout, and deck CSS. After this plan the homepage is fully functional — deck controller, 3D object, fallback, all 5 slides, keyboard/touch nav, progress dots, and Lenis conflict resolved.

Purpose: This is the integration step. All Wave 1 pieces (controller, blob, slides) converge here.
Output: Functional slide-deck homepage on http://localhost:3000 with full feature set; all 8 requirements covered end-to-end.
</objective>

<execution_context>
@/Users/Montster/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Montster/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/15-slide-deck-homepage-3d-hero/15-CONTEXT.md
@.planning/phases/15-slide-deck-homepage-3d-hero/15-RESEARCH.md
@.planning/phases/15-slide-deck-homepage-3d-hero/15-PATTERNS.md
@.planning/phases/15-slide-deck-homepage-3d-hero/15-02-SUMMARY.md
@.planning/phases/15-slide-deck-homepage-3d-hero/15-03-SUMMARY.md
@.planning/phases/15-slide-deck-homepage-3d-hero/15-04-SUMMARY.md

<interfaces>
<!-- Contracts from Wave 1 that this plan consumes -->

From src/components/home-deck/deck-controller.ts (Plan 02):
export function useDeckController({ scrollerRef, slideCount, onSlideChange }): { idxRef }
export function objEnter(objWrapRef: React.RefObject<HTMLElement | null>): void

From src/components/providers/lenis-provider.tsx (Plan 02):
export function useLenisControl(): { stop: () => void; start: () => void } | null

From src/components/home-deck/hero-blob-canvas.tsx (Plan 03):
export default function HeroBlobCanvas(): JSX.Element
(This is the dynamic import target — imported via next/dynamic ssr:false)

From src/components/home-deck/slide-*.tsx (Plan 04):
export function SlideHero(): JSX.Element   — div.deck-slide.deck-slide--hero
export function SlideIndex(): JSX.Element  — div.deck-slide.deck-slide--index
export function SlidePrometheus(): JSX.Element  — div.deck-slide.deck-slide--prometheus
export function SlideNewsletter(): JSX.Element  — div.deck-slide.deck-slide--newsletter
export function SlideFooter(): JSX.Element — div.deck-slide.deck-slide--footer
export function FallbackPoster({ className? }): JSX.Element  — next/image wrapper

Current src/app/layout.tsx structure (line 75):
<MainOffset>{children}</MainOffset>
<InkFooter />   ← this must be suppressed when pathname === "/"

Current src/app/page.tsx:
Server Component with Notion data fetches (getPublishedPosts, getFeaturedProjects, getUpcomingEvents) — this entire file is replaced. The revalidate should be `false` (static, no Notion on homepage, D-10).

From .planning/sketches/002-full-site-model/assets/site.css:
#objstage: position:fixed; top:0; right:0; width:45vw; height:100dvh; z-index:10; pointer-events:none
.objwrap: position:absolute; width:100%; height:100%; top:0; will-change:transform; transform:translateX(38vw) (initial settled position)
#scroller: position:fixed; top:0; left:0; width:100%; height:100dvh; overflow-y:scroll (or auto); z-index:20; overscroll-behavior:contain
.deck-slide: height:100dvh; flex-shrink:0 (flexbox children of #scroller)
.deck-dots: position:fixed; right:1.5rem; top:50%; transform:translateY(-50%); z-index:30; display:flex; flex-direction:column; gap:0.5rem; (hide at ≤760px)
.atmosphere: fixed, z-index:0, background fill with subtle radial pattern (optional polish layer)

PATTERNS.md §MainOffset: layout.tsx line 74 wraps children in MainOffset which adds pt-16 md:pt-0. The homepage's fixed-position #scroller needs top:0 regardless — set the scroller to position:fixed;top:0 and it ignores the MainOffset padding. Alternatively, add a css class that overrides it for the homepage.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build DeckHomepage orchestrator + add deck CSS to globals.css</name>
  <files>src/components/home-deck/deck-homepage.tsx, src/app/globals.css</files>
  <read_first>
    - .planning/phases/15-slide-deck-homepage-3d-hero/15-RESEARCH.md §Pattern 1 (complete DeckHomepage structure: dynamic import, touch detection, WebGL check, Lenis stop/start, showDeck/showCanvas branching)
    - .planning/phases/15-slide-deck-homepage-3d-hero/15-PATTERNS.md §deck-homepage.tsx (orchestrator structure; reduced-motion gate; dynamic import ssr:false constraint)
    - .planning/phases/15-slide-deck-homepage-3d-hero/15-RESEARCH.md §Pattern 6 (Lenis conflict: useLenisControl stop/start in useEffect gated on showDeck)
    - .planning/phases/15-slide-deck-homepage-3d-hero/15-RESEARCH.md §Pattern 5 (objEnter call site — called from onSlideChange callback)
    - .planning/phases/15-slide-deck-homepage-3d-hero/15-RESEARCH.md §Pitfall 2 (dynamic ssr:false must be in "use client" file)
    - .planning/phases/15-slide-deck-homepage-3d-hero/15-RESEARCH.md §Pitfall 7 (objEnter runs on mount before canvas — poster covers, acceptable)
    - src/components/home-v2/manifesto-reveal.tsx (mount-time detection + useReducedMotion branch guard pattern)
    - .planning/sketches/002-full-site-model/assets/site.css (objstage, objwrap, scroller, deck-slide, deck-dots dimensions; atmosphere layer)
    - src/app/globals.css (existing Tailwind v4 global CSS — append deck styles here; check existing CSS for potential conflicts)
  </read_first>
  <action>
    CREATE src/components/home-deck/deck-homepage.tsx as "use client":

    Imports:
    - dynamic from "next/dynamic"
    - { useReducedMotion } from "motion/react"
    - { useEffect, useRef, useState } from "react"
    - { useDeckController, objEnter } from "./deck-controller"
    - { useLenisControl } from "@/components/providers/lenis-provider"
    - { SlideHero, SlideIndex, SlidePrometheus, SlideNewsletter, SlideFooter } from their respective files
    - { FallbackPoster } from "./fallback-poster"

    Dynamic import (must be inside this "use client" file, NOT in page.tsx):
    const HeroBlobCanvas = dynamic(() => import("./hero-blob-canvas"), { ssr: false, loading: () => null })

    State and refs:
    - prefersReduced = useReducedMotion()
    - [isTouchOrSmall, setIsTouchOrSmall] = useState(false)
    - [webglOk, setWebglOk] = useState(false)
    - scrollerRef = useRef<HTMLDivElement>(null)
    - objWrapRef = useRef<HTMLDivElement>(null)
    - [activeIdx, setActiveIdx] = useState(0)

    Mount-time detection (single useEffect):
    - Touch detection: window.matchMedia("(pointer: coarse)").matches || window.innerWidth < 760
    - setIsTouchOrSmall(coarse || window.innerWidth < 760)
    - WebGL detection: document.createElement("canvas").getContext("webgl2", { failIfMajorPerformanceCaveat: true })
    - setWebglOk(!!ctx)

    Derived flags:
    - showDeck = !isTouchOrSmall && !prefersReduced
    - showCanvas = showDeck && webglOk

    Lenis stop/start (separate useEffect gated on showDeck):
    const lenis = useLenisControl()
    useEffect(() => {
      if (showDeck) { lenis?.stop(); return () => lenis?.start(); }
    }, [showDeck, lenis])

    onSlideChange callback (useCallback):
    (idx: number) => {
      setActiveIdx(idx)
      objEnter(objWrapRef)
    }
    Note: setActiveIdx updates the progress dots. objEnter drives the object entrance. Both called on every slide change.

    Deck controller (only when showDeck):
    useDeckController({ scrollerRef, slideCount: 5, onSlideChange }) — call unconditionally but pass a no-op or skip inside the hook if scrollerRef is null.

    Call objEnter(objWrapRef) on initial mount (once, after isTouchOrSmall/webglOk are set and showDeck is true):
    useEffect(() => { if (showDeck) objEnter(objWrapRef); }, [showDeck])

    Render — TWO layouts depending on showDeck:

    DECK LAYOUT (showDeck=true):
    Return a wrapper div with className="deck-homepage" (position:relative so fixed children position against viewport):
    - <div id="atmosphere" /> (fixed bg layer, z-0)
    - <div id="objstage" style/className per deck CSS> 
        <div className="objwrap" ref={objWrapRef}>
          {showCanvas ? <HeroBlobCanvas /> : <FallbackPoster />}
        </div>
      </div>
    - Progress dots: <nav className="deck-dots" aria-label="Slide navigation">
        {[0,1,2,3,4].map(i => (
          <button key={i} className={i===activeIdx ? "deck-dot deck-dot--active" : "deck-dot"} onClick={() => goToSlide(i)} aria-label={`Go to slide ${i+1}`} />
        ))}
      </nav>
      Note: expose a goToSlide function via the deck controller or via imperative refs. If useDeckController doesn't expose goTo directly, call scrollerRef.current.scrollTop = slides[i].offsetTop as a fallback for dot clicks.
    - <div id="scroller" ref={scrollerRef} className="deck-scroller">
        <SlideHero />
        <SlideIndex />
        <SlidePrometheus />
        <SlideNewsletter />
        <SlideFooter />
      </div>

    NATIVE-SCROLL LAYOUT (showDeck=false — touch OR reduced-motion):
    Return a div className="deck-homepage deck-homepage--native":
    - <div className="objstage-static">
        <FallbackPoster />
      </div>
    - All 5 slides rendered as normal flow block elements (not fixed; normal document scroll)

    ADD deck styles to src/app/globals.css:
    Append a /* Phase 15 — Deck Styles */ comment section at the end of globals.css.
    CSS rules to add (Tailwind v4 CSS-first — use @layer components or raw CSS):
      #scroller, .deck-scroller { position:fixed; top:0; left:0; width:100%; height:100dvh; overflow-y:scroll; overscroll-behavior:contain; z-index:20; scrollbar-width:none; }
      #scroller::-webkit-scrollbar, .deck-scroller::-webkit-scrollbar { display:none; }
      #objstage { position:fixed; top:0; right:0; width:45vw; height:100dvh; z-index:10; pointer-events:none; }
      .objwrap { position:absolute; width:100%; height:100%; top:0; will-change:transform; transform:translateX(38vw); }
      .deck-slide { height:100dvh; flex-shrink:0; display:flex; padding:0 8vw; }
      .deck-slide--footer, .deck-foot { height:auto; min-height:100dvh; justify-content:flex-end; padding-top:12vh; }
      .deck-dots { position:fixed; right:1.5rem; top:50%; transform:translateY(-50%); z-index:30; display:flex; flex-direction:column; gap:0.5rem; }
      @media(max-width:760px) { .deck-dots { display:none; } }
      .deck-dot { width:0.5rem; height:0.5rem; border-radius:50%; background:var(--color-text-muted); opacity:0.4; border:none; cursor:pointer; }
      .deck-dot--active { background:var(--color-text); opacity:1; }
      #atmosphere { position:fixed; top:0; left:0; width:100%; height:100dvh; z-index:0; background:var(--color-bg); }
      .deck-homepage--native #atmosphere { display:none; }
      .deck-homepage--native .deck-slide { height:auto; min-height:auto; }
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep "deck-homepage" | head -10; echo "deck-homepage tsc done"</automated>
  </verify>
  <acceptance_criteria>
    - src/components/home-deck/deck-homepage.tsx exists with "use client" directive
    - HeroBlobCanvas dynamically imported with ssr:false inside this file
    - showDeck = !isTouchOrSmall && !prefersReduced (per D-09: both touch AND reduced-motion trigger fallback)
    - showCanvas = showDeck && webglOk
    - Lenis stop() called on showDeck=true mount; start() called on unmount
    - useDeckController wired to scrollerRef and slideCount=5
    - objEnter(objWrapRef) called on initial mount and in onSlideChange callback
    - Progress dots rendered (5 buttons, active dot highlighted by activeIdx)
    - Native-scroll layout rendered when showDeck=false (all 5 slides as flow elements + FallbackPoster)
    - globals.css contains deck-scroller, objstage, objwrap, deck-slide, deck-dots, deck-dot CSS
    - npx tsc --noEmit no errors in deck-homepage.tsx or globals.css-adjacent files
  </acceptance_criteria>
  <done>DeckHomepage orchestrator wired; deck CSS in globals.css; TypeScript clean.</done>
</task>

<task type="auto">
  <name>Task 2: Replace page.tsx + suppress InkFooter in layout.tsx</name>
  <files>src/app/page.tsx, src/app/layout.tsx</files>
  <read_first>
    - src/app/page.tsx (full file — MUST read first to understand current v2 structure being replaced: Notion data fetches, v2 components, current metadata pattern)
    - src/app/layout.tsx (full file — MUST read first; InkFooter is line 75 — need to add pathname check; MainOffset at line 74)
    - src/app/template.tsx (usePathname() pattern — already a "use client" with pathname; shows how to get pathname)
    - .planning/phases/15-slide-deck-homepage-3d-hero/15-PATTERNS.md §page.tsx (thin Server Component pattern; JsonLd + buildPersonSchema; revalidate=false)
    - .planning/phases/15-slide-deck-homepage-3d-hero/15-PATTERNS.md §slide-footer.tsx (footer suppression decision: usePathname in layout, or route group)
    - .planning/phases/15-slide-deck-homepage-3d-hero/15-RESEARCH.md §Open Question 3 (footer suppression: pathname-check in layout is the recommendation)
  </read_first>
  <action>
    REPLACE src/app/page.tsx:
    New file is a Server Component (no "use client"). Remove all Notion data fetches.
    Imports: { JsonLd } from "@/components/seo/json-ld"; { buildPersonSchema } from "@/lib/seo/schemas"; { DeckHomepage } from "@/components/home-deck/deck-homepage"
    export const revalidate = false   ← static, no Notion data on homepage (D-10)
    export default function Home() {
      return (
        <>
          <JsonLd data={buildPersonSchema()} />
          <DeckHomepage />
        </>
      )
    }
    NOTE: The existing metadata is in layout.tsx already (default title + description). No export const metadata needed on page.tsx — the layout.tsx metadata handles it.

    EDIT src/app/layout.tsx — suppress InkFooter on homepage:
    Option chosen: pathname-check client boundary (PATTERNS.md §slide-footer.tsx recommendation).
    layout.tsx is currently a Server Component. InkFooter renders unconditionally.
    Strategy: Extract the InkFooter conditional into a new tiny "use client" component.

    Create src/components/layout/conditional-footer.tsx (new file):
    "use client"
    import { usePathname } from "next/navigation"
    import { InkFooter } from "@/components/home-v2/ink-footer"
    export function ConditionalFooter() {
      const pathname = usePathname()
      if (pathname === "/") return null
      return <InkFooter />
    }

    Then in layout.tsx: replace `<InkFooter />` with `<ConditionalFooter />` and update the import.
    Add import: { ConditionalFooter } from "@/components/layout/conditional-footer"
    Remove import: { InkFooter } from "@/components/home-v2/ink-footer" (no longer directly used in layout)

    ALSO CONSIDER MainOffset: The homepage's deck-homepage uses position:fixed for its scroller, so the MainOffset pt-16 md:pt-0 wrapper will not affect fixed children. However, DeckHomepage itself may be wrapped in pt-16 padding from MainOffset. To prevent this, also suppress MainOffset on pathname="/":
    Create or update conditional-footer.tsx to also handle this, OR make deck-homepage.tsx apply a negative margin-top compensating for the pt-16 in its wrapper div.
    Simpler approach: add a className to the deck-homepage wrapper div: -mt-16 md:-mt-0 to counteract MainOffset's pt-16 md:pt-0.
    Choose the margin compensation approach (no new layout component needed for this).

    Add `-mt-16 md:mt-0` to the deck-homepage wrapper div className in deck-homepage.tsx to counteract MainOffset's `pt-16 md:pt-0` (PATTERNS.md §MainOffset confirms layout.tsx line 74 applies this padding unconditionally — no inspection branch needed).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep "page\|layout\|conditional" | head -10; npx vitest run src/__tests__/home-deck/ 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/app/page.tsx: imports DeckHomepage; exports default Home with DeckHomepage + JsonLd; revalidate=false; NO Notion data fetches (no getPublishedPosts, getFeaturedProjects, getUpcomingEvents)
    - src/components/layout/conditional-footer.tsx: "use client"; usePathname(); returns null when pathname==="/"
    - src/app/layout.tsx: imports ConditionalFooter (not InkFooter directly); renders <ConditionalFooter /> in place of <InkFooter />
    - npx tsc --noEmit exits 0 (no TypeScript errors in any of these files)
    - npx vitest run exits 0 (full suite green — all 8 test stubs now GREEN)
    - npx next build does not throw a "ssr: false in Server Component" error (DeckHomepage is client boundary)
  </acceptance_criteria>
  <done>page.tsx replaced with deck; InkFooter suppressed on /; TypeScript clean; all 8 tests GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser events → DeckHomepage | wheel/keyboard/touch events from user input reach the deck controller |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-04 | Spoofing | DeckHomepage (usePathname) | accept | usePathname() reads the current route to suppress InkFooter on "/". This value comes from Next.js internal routing — it is not user-controlled input. No trust concern. |
| T-15-05 | Information Disclosure | page.tsx replacement | accept | Removing Notion data fetches from page.tsx means less server-side data exposure. The new page is fully static. Lower risk than the file it replaces. |

No ASVS categories apply — presentation-only phase, no auth, no sessions, no user input sent to any server (RESEARCH.md §Security Domain).
</threat_model>

<verification>
npx vitest run exits 0 (all 8 home-deck tests GREEN).
npx tsc --noEmit exits 0.
grep -c "getPublishedPosts\|getFeaturedProjects" src/app/page.tsx returns 0.
grep -c "DeckHomepage" src/app/page.tsx returns 1.
grep -c "ConditionalFooter" src/app/layout.tsx returns 1.
grep -c "revalidate = false" src/app/page.tsx returns 1.
</verification>

<success_criteria>
- page.tsx: static Server Component with DeckHomepage + JsonLd, revalidate=false
- layout.tsx: InkFooter suppressed on pathname="/" via ConditionalFooter
- DeckHomepage: touch+reduced-motion → native scroll; showCanvas only when WebGL ok; Lenis stopped on deck mount
- Deck CSS: objstage/objwrap/scroller/deck-slide/deck-dots all in globals.css
- All 8 vitest tests GREEN (HD-01..HD-05, TD-01..TD-03)
- npx next build passes (no ssr:false-in-server-component error)
</success_criteria>

<output>
Create `.planning/phases/15-slide-deck-homepage-3d-hero/15-05-SUMMARY.md` when done.
</output>
