---
phase: 15-slide-deck-homepage-3d-hero
plan: 04
type: execute
wave: 1
depends_on: [15-01]
files_modified:
  - src/components/home-deck/slide-hero.tsx
  - src/components/home-deck/slide-index.tsx
  - src/components/home-deck/slide-prometheus.tsx
  - src/components/home-deck/slide-newsletter.tsx
  - src/components/home-deck/slide-footer.tsx
  - src/components/home-deck/fallback-poster.tsx
  - src/__tests__/home-deck/slides.test.tsx
autonomous: true
requirements: [HD-03, HD-04, HD-05]

must_haves:
  truths:
    - "SlideHero renders 'Monty Singer' in both sig (filled) and sig-out (outline) variants"
    - "SlideHero contains role sub-lines and at least two CTA buttons"
    - "SlideIndex renders BigList with exactly 3 items: Works/Writing/Prometheus"
    - "SlideIndex has SectionLabel 'What I'm' above the BigList (D-10)"
    - "SlidePrometheus renders Prometheus content in v3 token classes (text-text, not text-ink)"
    - "SlideNewsletter uses NewsletterCarousel or equivalent static newsletter section"
    - "SlideFooter replicates InkFooter structure as a deck-slide (no global InkFooter on /)"
    - "FallbackPoster renders next/image with priority + fetchPriority='high' + fill + sizes"
    - "All slide components add className='deck-slide' wrapper so deck-controller querySelectorAll finds them"
  artifacts:
    - path: src/components/home-deck/slide-hero.tsx
      provides: Slide 1 — hero name + sub-roles + CTAs
      exports: [SlideHero]
    - path: src/components/home-deck/slide-index.tsx
      provides: Slide 2 — BigList index (HD-04)
      exports: [SlideIndex]
    - path: src/components/home-deck/slide-prometheus.tsx
      provides: Slide 3 — Prometheus content
      exports: [SlidePrometheus]
    - path: src/components/home-deck/slide-newsletter.tsx
      provides: Slide 4 — Monty Monthly newsletter
      exports: [SlideNewsletter]
    - path: src/components/home-deck/slide-footer.tsx
      provides: Slide 5 — footer-as-last-slide
      exports: [SlideFooter]
    - path: src/components/home-deck/fallback-poster.tsx
      provides: Poster image slot (WebGL fallback)
      exports: [FallbackPoster]
  key_links:
    - from: src/components/home-deck/slide-index.tsx
      to: src/components/v3/big-list.tsx
      via: import BigList
      pattern: "BigList"
    - from: src/components/home-deck/fallback-poster.tsx
      to: /public/hero-blob-poster.webp
      via: Image src
      pattern: "hero-blob-poster"
---

<objective>
Build the 5 static slide components and the fallback poster component. All are pure presentational — no deck controller, no R3F. They compose Phase 14 v3 primitives (BigList, Button, SectionLabel, NewsletterCarousel) and use Crimson Poster token classes. Turns the slides.test.tsx HD-04 test GREEN.

Purpose: Separating slide content from the orchestrator (Plan 05) means content authors can iterate on slide copy without touching controller logic, and these Server Components can be SSR'd for SEO.
Output: 6 presentational components (5 slides + fallback poster); HD-04 test GREEN.
</objective>

<execution_context>
@/Users/Montster/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Montster/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/15-slide-deck-homepage-3d-hero/15-CONTEXT.md
@.planning/phases/15-slide-deck-homepage-3d-hero/15-PATTERNS.md
@.planning/phases/15-slide-deck-homepage-3d-hero/15-01-SUMMARY.md

<interfaces>
<!-- Phase 14 primitive exports — executor uses these, no codebase exploration needed -->

From src/components/v3/big-list.tsx:
export function BigList({ items }: { items: { label: string; href: string; tag: string; outline?: boolean }[] })
Usage from v3-specimen: items=[{label:"Writing",href:"/writing",tag:"ESSAYS"},{label:"Works",href:"/projects",tag:"WORK"},{label:"Prometheus",href:"https://prometheus.today",tag:"COMPANY",outline:true}]

From src/components/v3/section-label.tsx:
export function SectionLabel({ numeral, children }: { numeral: string; children: React.ReactNode })

From src/components/v3/button.tsx:
export function Button({ href, children, variant? }: { href?: string; children: React.ReactNode; variant?: "outline" | "default" })

From src/components/v3/page-hero.tsx:
Contains sig/sig-out pattern — but slide-hero.tsx implements its own hero layout (not wrapping PageHero)

From src/components/v3/newsletter-carousel.tsx:
export function NewsletterCarousel({ issues }: { issues: { title: string; date: string; href: string }[] })
Server Component — no "use client"

Tailwind v4 token classes (use these, NOT v2 classes):
  text-text (not text-ink), text-text-muted (not text-muted), bg-bg (not bg-background),
  border-border, text-text-dim, font-display (Space Grotesk), font-mono (JetBrains Mono)
  sig (filled crimson + shadow), sig-out (outline stroke)

Prototype index.html: 5 slides with class deck-slide, deck-slide--hero, deck-slide--index, deck-slide--prometheus, deck-slide--newsletter, deck-foot
All slides need height: 100dvh or equivalent — CSS provided by deck-homepage.tsx orchestrator; slide components just need the deck-slide className.

MEMORY.md LCP image quirk:
"Next.js 16 Image priority does not auto-emit fetchPriority — set fetchPriority='high' explicitly on the Image."
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build slide-hero, slide-index, slide-prometheus slide components + turn HD-04 GREEN</name>
  <files>
    src/components/home-deck/slide-hero.tsx,
    src/components/home-deck/slide-index.tsx,
    src/components/home-deck/slide-prometheus.tsx,
    src/__tests__/home-deck/slides.test.tsx
  </files>
  <read_first>
    - .planning/sketches/002-full-site-model/index.html (prototype markup for slides 1, 2, 3 — hero grid layout, deck-slide--index BigList structure, deck-slide--prometheus content and copy)
    - .planning/sketches/002-full-site-model/assets/site.css (object stage, deck-slide, hero-grid, home-name, big-list sizing — use as layout reference for Tailwind equivalents)
    - .planning/phases/15-slide-deck-homepage-3d-hero/15-PATTERNS.md §slide-hero.tsx (sig/sig-out usage from v3-specimen; sub-roles pattern; Button import)
    - .planning/phases/15-slide-deck-homepage-3d-hero/15-PATTERNS.md §slide-index.tsx (BigList exact usage; SectionLabel above; "What I'm Building / Writing / Doing" text per D-10)
    - .planning/phases/15-slide-deck-homepage-3d-hero/15-PATTERNS.md §slide-prometheus.tsx (3-col editorial grid; v3 token class names)
    - src/components/v3/big-list.tsx (BigList implementation — read items prop shape and renders)
    - src/app/v3-specimen/page.tsx (lines 108-118 for sig/sig-out pattern; lines 207-215 for BigList usage)
    - src/__tests__/home-deck/slides.test.tsx (current HD-04 stub — replace with real test)
  </read_first>
  <action>
    CREATE src/components/home-deck/slide-hero.tsx (Server Component — no "use client"):
    Wrapper div with className="deck-slide deck-slide--hero" (flex, items-center, full height, position:relative, overflow:hidden)
    Content grid matching prototype hero-grid: split left content zone and right object stage (objstage/objwrap live in deck-homepage.tsx, not here).
    Left zone content:
    - Oversized name block with two stacked divs:
        className="font-display font-bold uppercase sig" → "Monty" on one line, "Singer." on the next (match prototype's multi-line name treatment)
        className="font-display font-bold uppercase sig-out" → second layer for outline effect
    - Sub-roles list: "Founder, Prometheus" / "Writer, Monty Monthly" / "Builder" — styled font-mono text-text-muted uppercase text-sm
    - Two CTA Buttons: "Works →" href="/projects" and "Writing →" href="/writing"
    - Scroll cue: a small "↓" or "scroll" text (per prototype scroll-cue element)
    Use Tailwind v4 classes: bg-bg (or transparent), text-text for body, sig / sig-out for name.
    Apply SITE COPY RULES (from STATE.md accumulated context): "Georgetown University" only, sole professional identity is "Founder of Prometheus", no em dashes, no location.

    CREATE src/components/home-deck/slide-index.tsx (Server Component — no "use client"):
    Imports: { BigList } from "@/components/v3/big-list"; { SectionLabel } from "@/components/v3/section-label"
    Wrapper: div className="deck-slide deck-slide--index" (flex, flex-col, justify-center, full height)
    Content:
      <SectionLabel numeral="02">What I'm</SectionLabel>
      <BigList
        items={[
          { label: "Building", href: "/projects", tag: "WORKS" },
          { label: "Writing",  href: "/writing",  tag: "ESSAYS" },
          { label: "Doing",    href: "https://prometheus.today", tag: "PROMETHEUS", outline: true },
        ]}
      />
    Note on D-10 copy: "What I'm Building / Writing / Doing" per CONTEXT.md D-10. The numeral-label is "What I'm" with the BigList items being the three verbs. Adjust SectionLabel text if the primitive doesn't support mid-sentence placement — use the closest match to the prototype's heading structure.

    CREATE src/components/home-deck/slide-prometheus.tsx (Server Component — no "use client"):
    Imports: { Rule } from "@/components/v3/rule"; { Button } from "@/components/v3/button"
    Wrapper: div className="deck-slide deck-slide--prometheus" (flex, flex-col, justify-center, full height, px-[8vw])
    Content structure (3-column or full-width editorial block):
    - SectionLabel / header: "Prometheus" (large display text using sig or font-display text-text)
    - Tagline: current work description — "AI integrations and education company." in text-text-dim
    - Single CTA: <Button href="https://prometheus.today">prometheus.today →</Button>
    Use v3 token classes only: text-text, text-text-muted, border-border. Not text-ink, text-muted.

    UPDATE src/__tests__/home-deck/slides.test.tsx (replace HD-04 stub with real test):
    import { describe, it, expect, vi } from "vitest"
    import { render, screen, within } from "@testing-library/react"
    describe("SlideIndex (HD-04)", () => {
      it("renders BigList with Works, Writing, Prometheus links", async () => {
        const { SlideIndex } = await import("@/components/home-deck/slide-index");
        render(<SlideIndex />);
        // HD-04: "Slide 2 is the brutalist big-type index linking to Works / Writing / Prometheus"
        expect(screen.getByText(/Building/i)).toBeDefined();
        expect(screen.getByText(/Writing/i)).toBeDefined();
        expect(screen.getByText(/Doing/i)).toBeDefined();
      });
      it("has deck-slide class on wrapper", async () => {
        const { SlideIndex } = await import("@/components/home-deck/slide-index");
        const { container } = render(<SlideIndex />);
        expect(container.querySelector('.deck-slide')).toBeTruthy();
      });
    });
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/home-deck/slides.test.tsx 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - slide-hero.tsx: exports SlideHero; contains "deck-slide" className; contains sig and sig-out class usage; contains "Monty" and "Singer" text; has at least one Button; no "use client"
    - slide-index.tsx: exports SlideIndex; imports BigList from "@/components/v3/big-list"; BigList items include labels Building, Writing, Doing with correct hrefs (/projects, /writing, prometheus.today)
    - slide-prometheus.tsx: exports SlidePrometheus; uses v3 token class text-text (not text-ink); has "Prometheus" heading; has prometheus.today link
    - npx vitest run src/__tests__/home-deck/slides.test.tsx exits 0 (HD-04 GREEN)
    - npx vitest run exits 0 (full suite clean)
  </acceptance_criteria>
  <done>Slides 1-3 built; HD-04 GREEN; full suite clean.</done>
</task>

<task type="auto">
  <name>Task 2: Build slide-newsletter, slide-footer, fallback-poster</name>
  <files>
    src/components/home-deck/slide-newsletter.tsx,
    src/components/home-deck/slide-footer.tsx,
    src/components/home-deck/fallback-poster.tsx
  </files>
  <read_first>
    - .planning/sketches/002-full-site-model/index.html (prototype slides 4 and 5: newsletter card and deck-foot structure; deck-foot has height:auto, min-height:100%, justify-content:flex-end, padding-top:12vh)
    - .planning/phases/15-slide-deck-homepage-3d-hero/15-PATTERNS.md §slide-newsletter.tsx (NewsletterCarousel usage from v3-specimen lines 296-305)
    - .planning/phases/15-slide-deck-homepage-3d-hero/15-PATTERNS.md §slide-footer.tsx (InkFooter content as a deck slide; footer suppression decision)
    - .planning/phases/15-slide-deck-homepage-3d-hero/15-PATTERNS.md §fallback-poster.tsx (Image pattern: priority + fetchPriority="high" + fill + sizes; MEMORY.md LCP quirk)
    - src/components/home-v2/ink-footer.tsx (footer content to replicate as slide 5)
    - src/components/v3/newsletter-carousel.tsx (import shape: NewsletterCarousel + issues prop)
    - src/app/page.tsx lines 77-86 (LCP-safe Image pattern with explicit fetchPriority="high")
  </read_first>
  <action>
    CREATE src/components/home-deck/slide-newsletter.tsx (Server Component — no "use client"):
    Imports: { NewsletterCarousel } from "@/components/v3/newsletter-carousel"; { Button } from "@/components/v3/button"
    Wrapper: div className="deck-slide deck-slide--newsletter" (flex, flex-col, justify-center, full height)
    Content:
    - SectionLabel "04" or heading "Monty Monthly" in font-display sig treatment
    - NewsletterCarousel with 3 static placeholder issues matching prototype copy:
        issues={[
          { title: "Vol. 1 — Builders, Breakers, Believers", date: "Jun 2026", href: "#" },
          { title: "Vol. 2 — The Quiet Builders", date: "May 2026", href: "#" },
          { title: "Vol. 3 — First Principles", date: "Apr 2026", href: "#" },
        ]}
    - Button: "Subscribe →" href="#newsletter" or Substack URL (use "#" if URL unknown)

    CREATE src/components/home-deck/slide-footer.tsx (Server Component — no "use client"):
    Reads src/components/home-v2/ink-footer.tsx as reference. This is the footer CONTENT reproduced as the last slide.
    Wrapper: div className="deck-slide deck-slide--footer deck-foot" with styles: h-auto min-h-full flex flex-col justify-end pt-[12vh] px-[8vw]
    Content: replicate ink-footer.tsx structure — big "Let's be friends." link (or equivalent), site nav columns (Writing/Works/Prometheus/About, More: Monty Monthly/Watching/Uses/Events/Links, Elsewhere: social links, Contact), copyright line.
    Use v3 token classes: text-text, text-text-muted, border-border.
    IMPORTANT: This component intentionally duplicates footer content because on `/` the global <InkFooter /> is suppressed (handled by Plan 05). This component is ONLY rendered in the deck. Outside the deck, the global InkFooter renders as normal.

    CREATE src/components/home-deck/fallback-poster.tsx (Server Component — no "use client"):
    Imports: Image from "next/image"
    Props: { className?: string }
    Returns:
      <div className={cn("relative w-full h-full", className)}>
        <Image
          src="/hero-blob-poster.webp"
          alt="3D hero object — morphing blob with crimson rim"
          fill
          priority
          fetchPriority="high"
          loading="eager"
          sizes="(max-width: 760px) 0vw, 45vw"
          className="object-contain"
        />
      </div>
    Note: hero-blob-poster.webp does NOT exist yet — it is produced in Plan 06 (human checkpoint). For now the Image will 404 gracefully in dev (Next.js shows a broken image placeholder). The file path /public/hero-blob-poster.webp is the committed location after Plan 06.
    fetchPriority="high" must be set explicitly — per MEMORY.md note: "Next.js 16 Image priority does not auto-emit fetchPriority."
    Import cn from "@/utils/cn" for className merging.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep "slide-\|fallback-poster" | head -10; echo "slides tsc check done"</automated>
  </verify>
  <acceptance_criteria>
    - slide-newsletter.tsx: exports SlideNewsletter; wraps NewsletterCarousel with static issues array; has deck-slide className; no "use client"
    - slide-footer.tsx: exports SlideFooter; className includes "deck-slide" and "deck-foot"; has flex-col justify-end; replicates footer nav link structure in v3 token classes
    - fallback-poster.tsx: exports FallbackPoster; uses next/image with priority AND fetchPriority="high" (both required); src="/hero-blob-poster.webp"; fill prop present; sizes="(max-width: 760px) 0vw, 45vw"
    - grep -c "fetchPriority" src/components/home-deck/fallback-poster.tsx returns 1
    - npx tsc --noEmit passes for all three files
    - npx vitest run exits 0 (full suite remains green)
  </acceptance_criteria>
  <done>Slides 4-5 and fallback poster built; TypeScript clean; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries — all slide content is hardcoded JSX (D-10). No user input. No server mutation. No dangerouslySetInnerHTML.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|

No applicable threats. Presentation-only phase, static JSX, no untrusted input or server-side mutation (RESEARCH.md §Security Domain).
</threat_model>

<verification>
npx vitest run src/__tests__/home-deck/slides.test.tsx exits 0 (HD-04 GREEN).
npx vitest run exits 0 (full suite).
grep -c "deck-slide" src/components/home-deck/slide-index.tsx returns at least 1.
grep -c "fetchPriority" src/components/home-deck/fallback-poster.tsx returns 1.
</verification>

<success_criteria>
- 5 slide components exist in src/components/home-deck/ each with deck-slide class wrapper
- slide-index.tsx uses BigList with Building/Writing/Doing items per D-10
- slide-footer.tsx replicates InkFooter content as deck slide 5 in v3 token classes
- fallback-poster.tsx uses next/image with priority+fetchPriority="high" (MEMORY.md quirk honored)
- HD-04 test GREEN
- Full vitest suite exits 0
</success_criteria>

<output>
Create `.planning/phases/15-slide-deck-homepage-3d-hero/15-04-SUMMARY.md` when done.
</output>
