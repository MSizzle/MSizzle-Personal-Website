---
phase: 21.5-v5-palette-foundation-row-rotation
plan: 03
type: execute
wave: 3
depends_on: ["21.5-01", "21.5-02"]
files_modified:
  - .planning/phases/21.5-v5-palette-foundation-row-rotation/21.5-VALIDATION.md
autonomous: false
requirements: [MO-01, MO-02, MO-03, MO-05]

must_haves:
  truths:
    - "The bone ground and all ten accent tokens survive compilation into the served CSS bundle, not just the source file"
    - "D-V5-08: the AA 4.5:1 proof is re-stated against the shipped values, not only against the locked table"
    - "The full suite reports 0 failed against the documented baseline of 257 passed / 16 todo / 3 skipped files"
    - "npm run build exits 0 and npm run lint has not gained errors over the 199 baseline"
    - "D-V5-06: a human confirms the hero contact link and the active nav cell are still ink, not accent"
    - "D-V5-05: a human confirms the Writing rows continue the run instead of restarting at the first colour"
    - "D-V5-11: the pinboard and all three OG routes are untouched, and the pinboard's now-visible white card frame is recorded as a Phase 22 input rather than fixed here"
  artifacts:
    - path: ".planning/phases/21.5-v5-palette-foundation-row-rotation/21.5-VALIDATION.md"
      provides: "the served-bundle evidence, the contrast table as shipped, the suite/build/lint numbers, and the human verdict"
      contains: "SERVED CSS"
  key_links:
    - from: "src/app/globals.css"
      to: ".next/static/css/*.css"
      via: "next build, Lightning CSS"
      pattern: "f5f2eb"
---

<objective>
Prove the phase, then get Monty's eyes on it.

Purpose: this repo has a known Turbopack failure mode where a `globals.css` change is silently
absent from the served bundle, so a source-file assertion is not evidence that anything shipped.
And Monty signed off on `.playwright-mcp/accent-compare.html`, not on the built page -- Phase 21
already burned one auto-approved human-verify checkpoint, and the three claims it skipped had to be
re-walked by hand afterwards. This plan produces machine evidence first, then a real human pass.
Output: `21.5-VALIDATION.md` carrying the served-bundle grep output, the shipped contrast table,
the suite/build/lint numbers, and Monty's verdict.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@CLAUDE.md
@.planning/phases/21.5-v5-palette-foundation-row-rotation/21.5-01-SUMMARY.md
@.planning/phases/21.5-v5-palette-foundation-row-rotation/21.5-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prove the palette reaches the served bundle and record the gate numbers</name>
  <files>.planning/phases/21.5-v5-palette-foundation-row-rotation/21.5-VALIDATION.md</files>
  <action>
Run the four gates and write the raw output into `21.5-VALIDATION.md` rather than summarising it.

Gate 1, served CSS. A source grep is not evidence in this repo: Turbopack can serve a stale CSS
chunk, and only `rm -rf .next` clears it -- not `.next/cache`, not touching the file, not restarting
dev. So verify against a real compiled artifact. Run `rm -rf .next` then `npm run build`, then grep
the emitted stylesheets under `.next/static/css/` for the bone value and for the palette tokens.
Grep case-insensitively: Lightning CSS may lowercase hex literals, so `#F5F2EB` can land as
`#f5f2eb`. Confirm all ten `--ac-N` custom properties survived (a tree-shaken palette is the specific
failure this gate exists to catch) and that the two `var(--ac` fill references survived. Paste the
matching filenames and the extracted `--ac-*` declarations into the doc.

Note for the executor: a dev server is already running on port 3000. Do not start a second one.
`npm run build` writing into `.next` may leave that dev server serving stale assets; if the browser
looks wrong during Task 2, the remedy is `rm -rf .next && npm run dev`, and that is Monty's call to
make, not something to do mid-run.

Gate 2, suite. Run `npx vitest run`. The documented baseline before this phase is 257 passed,
0 failed, 16 todo, 3 skipped files. This phase adds tests, so the passed count is expected to rise.
The gate is 0 failed. Record the actual four numbers.

Gate 3, build. `npm run build` exit code from Gate 1. Record it.

Gate 4, lint. Run `npm run lint` and record the error and warning counts. The baseline is 199 errors
and 6518 warnings, essentially all `any` in test files. Do not fix any of them. The gate is that the
error count did not increase.

Then write the shipped contrast table into the doc: for each of the ten `--ac-N` values actually
found in the served CSS, compute the WCAG ratio against the served `--color-bg` and list
index, name, hex, ratio, pass/fail against 4.5:1. Reuse the helper logic from
`src/__tests__/styles/v5-tokens.test.ts` via a throwaway `node -e`; do not add a script to the repo.
Expected, in index order: 10.12 / 7.39 / 4.81 / 9.80 / 8.61 / 11.41 / 7.99 / 6.10 / 10.60 / 5.85.
If any number disagrees with that list, stop and report rather than adjusting the expectation.

Finally, record the scope boundary as fact, not intent: run
`git diff --stat main -- src/components/home/pinboard.tsx "src/app/**/opengraph-image.tsx"` and paste
the empty result, and add a short "Phase 22 inputs" section noting that `.pb-frame--cream` now
renders a white card on the bone ground and that the pinboard's neutral `rgba(0,0,0,0.08)` media
fills were deliberately left cold.

Use no em dashes anywhere in the document.
  </action>
  <verify>
    <automated>rm -rf .next && npm run build && node -e "const fs=require('fs'),p='.next/static/css';const files=fs.readdirSync(p).filter(f=>f.endsWith('.css'));const css=files.map(f=>fs.readFileSync(p+'/'+f,'utf8')).join('\n');const miss=[];for(let i=0;i<10;i++){if(!new RegExp('--ac-'+i+':').test(css))miss.push(i)}if(!/f5f2eb/i.test(css))miss.push('bone');const fills=(css.match(/var\(--ac[,)]/g)||[]).length;if(miss.length||fills<2){console.error('FAIL missing=',miss,'fillRefs=',fills);process.exit(1)}console.log('SERVED_CSS_OK files=',files.join(','),'fillRefs=',fills)"</automated>
  </verify>
  <done>`21.5-VALIDATION.md` exists and contains: the served-CSS filenames plus all ten extracted `--ac-*` declarations, the four vitest numbers with 0 failed, `npm run build` exit 0, the lint error/warning counts with errors not above 199, the ten-row shipped contrast table all passing 4.5:1, an empty pinboard/OG diff, and a "Phase 22 inputs" section.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human visual pass on the v5 palette and the row rotation</name>
  <action>
Stop. Present the verification walkthrough below to Monty and wait for his response. Do not
auto-approve this checkpoint and do not infer a pass from the machine gates in Task 1, which
cannot see colour. Phase 21 auto-approved its equivalent checkpoint under AUTO_MODE and the
three skipped claims had to be re-walked by hand afterwards. If Monty reports a problem,
capture his exact wording in `21.5-VALIDATION.md` and hand back rather than fixing in place.
  </action>
  <what-built>
Phases 21.5-01 and 21.5-02 retheme the site to the v5 warm system and add the rotating hover accent:

- Ground is bone `#F5F2EB`, ink is near-black `#111111`, and every dim / muted / border / inverse
  value is re-derived from that ink.
- Ten accent colours are declared as `--ac-0` .. `--ac-9` in max-contrast rotation order.
- Homepage Building rows and Writing rows fill with their colour on hover and flip their text to
  bone. Nothing carries accent at rest.
- The counter runs continuously in document order and does not reset at the section head.
- The hero contact link and the active nav cell deliberately stay ink.
- The pinboard and the three OG image routes were not touched; they belong to Phases 22 and 23.

Machine evidence is already in `21.5-VALIDATION.md`: all ten fills clear WCAG AA 4.5:1 against bone,
the palette survives into the served CSS bundle, the suite is at 0 failed, and the build exits 0.
What a machine cannot judge is whether it reads as "more personable and less stark," which was the
whole reason for the direction change.
  </what-built>
  <how-to-verify>
A dev server is already running. Open http://localhost:3000.

If the page still looks pure white, that is the known Turbopack stale-chunk bug, not the work:
stop the dev server, run `rm -rf .next`, run `npm run dev`, reload. Nothing else clears it.

1. Ground and ink. The page background should read as warm bone, not white, and the type as
   near-black, not pure black. Check the hero, the Building band, and the Writing band.
2. Rest state. Scroll the Building and Writing sections without hovering. There should be zero
   colour anywhere: no coloured row titles, no coloured left rules, no tinted borders.
3. Hover, Building. Hover each of the three Building rows in turn. Each should fill edge to edge
   with a different colour and flip all of its text, including the small mono number and status,
   to bone. Expected order top to bottom: deep red, slate teal, ochre.
4. Hover, Writing, and this is the one that matters. Hover the five Writing rows. They must NOT
   restart at deep red. Expected order: indigo, forest green, plum, deep teal, bronze. If the first
   Writing row is deep red again, the counter reset and the phase failed criterion 3.
5. Chrome stays black. In the hero paragraph, hover the "we'll get along" contact link. It should
   underline in ink and take no colour. In the sticky header, the nav cell for the page you are on
   should still be plain bold ink, and hovering any nav cell should still drop the ink block, not a
   coloured one.
6. Keyboard. Tab through the Building rows. The row should fill and the focus ring should stay
   visible against the fill.
7. Mobile width. Narrow the window to about 375px. Rows should still collapse rather than overflow,
   and no row should be stuck in a filled state after you move the pointer away.
8. Overall. Does it read as a warmer, more personable version of the same site, or does it read as
   busy? The accent is meant to be a reward for hovering, not decoration.
  </how-to-verify>
  <resume-signal>Type "approved" to close the phase, or describe what is wrong. For item 4 specifically, name the colour you actually see on the first Writing row.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none crossed | This plan runs the existing build and test commands and writes one planning document. No source file changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21.5-06 | Repudiation | An auto-approved human checkpoint | mitigate | Phase 21's checkpoint was auto-approved under AUTO_MODE and had to be re-walked later. This one is `gate="blocking"` with a named colour as the resume signal, so a rubber stamp is distinguishable from a real look. |
| T-21.5-07 | Tampering | Stale served CSS masking a failed change | mitigate | Gate 1 runs `rm -rf .next` before `npm run build` and asserts against the compiled bundle, not the source. |
| T-21.5-SC | Tampering | npm/pip/cargo installs | accept | This plan installs no packages. |
</threat_model>

<verification>
1. `21.5-VALIDATION.md` exists and every gate in Task 1's `<done>` is filled with real output.
2. The human checkpoint returned "approved", or its findings were fed back as a gap-closure plan.
3. `git status` shows no unintended source changes from the verification run.
</verification>

<success_criteria>
- The bone ground and all ten accent tokens are proven present in the compiled CSS bundle.
- All ten fills clear WCAG AA 4.5:1 against bone as shipped, matching the locked ratios.
- Suite at 0 failed, build exit 0, lint errors not above 199.
- Monty has hovered the rows and confirmed the counter does not reset at the Writing section head.
</success_criteria>

<output>
Create `.planning/phases/21.5-v5-palette-foundation-row-rotation/21.5-03-SUMMARY.md` when done, and
`21.5-VALIDATION.md` as described in Task 1.
</output>
