---
phase: "17.1"
plan: 2
type: execute
wave: 2
depends_on:
  - "17.1-01"
files_modified:
  - src/components/home/section-work.tsx
  - src/components/home/section-loves.tsx
  - src/components/home/explorative-homepage.tsx
autonomous: true
requirements: []
# Phase 17.1 has no formal REQUIREMENTS.md REQ-IDs. Coverage by CONTEXT.md D-01..D-12.

must_haves:
  truths:
    - "D-06: explorative-homepage.tsx section assembly order is: hero (inline) then SectionBuilding,
       SectionWork, SectionLoves, SectionNewsletter, SectionFooter — matching the D-06 narrative arc.
       SectionWriting import is removed."
    - "D-10: Both section-work.tsx and section-loves.tsx contain only hardcoded JSX (no Notion
       fetch, no async/await, no revalidate export); copy is static arrays or inline JSX."
    - "D-11: Copy in new sections contains no em dashes; all external Prometheus links point to
       https://prometheus.today with rel='noopener noreferrer'."
  artifacts:
    - path: "src/components/home/section-work.tsx"
      provides: "Selected work / portfolio teaser section — SectionLabel + project list + link to /projects"
      exports: ["SectionWork"]
    - path: "src/components/home/section-loves.tsx"
      provides: "Things I love teaser section — brief framing + link to /uses"
      exports: ["SectionLoves"]
    - path: "src/components/home/explorative-homepage.tsx"
      provides: "Orchestrator updated with SectionWork + SectionLoves imports, SectionWriting removed,
                 correct narrative arc section order"
      contains: "SectionWork"
  key_links:
    - from: "src/components/home/explorative-homepage.tsx"
      to: "src/components/home/section-work.tsx"
      via: "SectionWork named import, rendered after SectionBuilding"
      pattern: "SectionWork"
    - from: "src/components/home/explorative-homepage.tsx"
      to: "src/components/home/section-loves.tsx"
      via: "SectionLoves named import, rendered after SectionWork"
      pattern: "SectionLoves"
---

<objective>
Create the two new homepage teaser sections — selected work (SectionWork) and things I love
(SectionLoves) — and wire them into the orchestrator in the correct D-06 narrative arc order,
replacing SectionWriting in the assembly.

Purpose: Sections 3 and 4 of the six-section narrative arc do not exist yet. This plan creates
them as plain Server Components following the section-building.tsx pattern and slots them into
the orchestrator. Plan 17.1-03 (parallel) adapts the copy of the existing sections; this plan
focuses on the new teaser sections and orchestrator wiring.

Output: section-work.tsx and section-loves.tsx as new Server Components; explorative-homepage.tsx
with the final six-section import list and render order.
</objective>

<execution_context>
@/Users/Montster/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Montster/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/17.1-homepage-rebuild-personal-brand-narrative-arc-who-am-i-what-/17.1-CONTEXT.md
@.planning/phases/17.1-homepage-rebuild-personal-brand-narrative-arc-who-am-i-what-/17.1-PATTERNS.md
@.planning/phases/17.1-homepage-rebuild-personal-brand-narrative-arc-who-am-i-what-/17.1-01-SUMMARY.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. -->

From src/components/home/section-building.tsx (closest analog — follow this pattern exactly):
- Server Component (no "use client")
- section wrapper className: "min-h-dvh flex flex-col justify-center px-[8vw] py-[15vh]"
- imports: SectionLabel from "@/components/v3/section-label"
  usage: <SectionLabel numeral="02">Selected Work</SectionLabel>
- imports: BigList from "@/components/v3/big-list"
  usage: <BigList items={[{ label, href, tag, outline? }]} />
- import: Button from "@/components/v3/button"
  usage: <Button href="/path">Label</Button> or <Button href="..." accent>Label →</Button>
- import: Link from "next/link"
  usage: <Link href="..." className="... hover:text-accent transition-colors duration-150">

From src/components/v3/big-list.tsx (inferred from section-building.tsx usage):
BigList items shape: { label: string; href: string; tag: string; outline?: boolean }
outline: true = .sig-out stroke variant (accent treatment)

Token classes to use (from globals.css @theme):
text-text, text-text-dim, text-text-muted, text-accent, hover:text-accent
border-border, font-display, font-mono, font-bold, uppercase
transition-colors duration-150
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create section-work.tsx — selected work / portfolio teaser</name>
  <files>src/components/home/section-work.tsx</files>
  <read_first>
    - src/components/home/section-building.tsx — exact pattern to follow (section wrapper,
      SectionLabel, BigList, component imports, token classes)
    - src/app/globals.css — token class definitions (lines 1-52, sig/sig-out lines 73-91)
    - .planning/phases/17.1-homepage-rebuild-personal-brand-narrative-arc-who-am-i-what-/17.1-CONTEXT.md
      — D-06 section 3 spec, D-07/D-10/D-11 copy constraints
  </read_first>
  <action>
    Create src/components/home/section-work.tsx as a Server Component (no "use client").

    Imports: SectionLabel from "@/components/v3/section-label", BigList from "@/components/v3/big-list",
    Button from "@/components/v3/button".

    Export a named function SectionWork. Section wrapper: className "min-h-dvh flex flex-col
    justify-center px-[8vw] py-[15vh]" (identical to section-building.tsx wrapper).

    SectionLabel: numeral "03" (third section in the arc after hero+building), label text
    "Selected Work".

    Above the BigList, add a short prose paragraph (p element) with className "mt-4 mb-8
    max-w-[52ch] text-text-dim font-display" and style={{ fontSize: "clamp(1rem,1.8vw,1.3rem)" }}.
    Text: "A handful of things I have built. More lives at Prometheus." (no em dashes per D-11;
    "Prometheus" here is the company, not a link — keep it as plain text in the sentence).

    BigList items array (hardcoded per D-10):
    - { label: "Prometheus", href: "https://prometheus.today", tag: "CURRENT", outline: true }
    - { label: "Projects", href: "/projects", tag: "ARCHIVE" }

    Below the BigList, add a Button component (from "@/components/v3/button") with
    href="/projects" and text "All work". This is a navigation link to the portfolio index,
    not a contact CTA (D-07 compliant). The Button wraps in a div with className "mt-8".

    External link to prometheus.today: if BigList handles the href, it renders an anchor. Ensure
    the link to prometheus.today uses target="_blank" and rel="noopener noreferrer" if BigList
    supports these props; if BigList does not support these anchor attributes (it may only use
    Next.js Link internally), use a prose Link element instead of BigList for the Prometheus item.
    Read src/components/v3/big-list.tsx before deciding. The external link safety requirement
    (noopener noreferrer) takes precedence over using BigList for that item.
  </action>
  <verify>
    <automated>npx tsc --noEmit --project tsconfig.json 2>&1 | grep "section-work" | head -5; echo "tsc exit:$?"</automated>
  </verify>
  <acceptance_criteria>
    - src/components/home/section-work.tsx exists and exports SectionWork
    - grep "use client" src/components/home/section-work.tsx returns no match (Server Component)
    - grep "Selected Work" src/components/home/section-work.tsx returns a match
    - grep "/projects" src/components/home/section-work.tsx returns a match (links to portfolio)
    - grep "prometheus.today" src/components/home/section-work.tsx returns a match
    - grep "noopener noreferrer" src/components/home/section-work.tsx returns a match if prometheus.today is an anchor with target="_blank"
    - No em dashes in copy (grep "\-\-\|—" src/components/home/section-work.tsx returns no content matches)
    - npx tsc --noEmit reports no errors in section-work.tsx
  </acceptance_criteria>
  <done>section-work.tsx is a valid Server Component with portfolio teaser content, SectionLabel
  numeral 03, link to /projects, and external Prometheus link using safe rel attributes.</done>
</task>

<task type="auto">
  <name>Task 2: Create section-loves.tsx + wire both new sections into orchestrator</name>
  <files>src/components/home/section-loves.tsx, src/components/home/explorative-homepage.tsx</files>
  <read_first>
    - src/components/home/section-building.tsx — section wrapper + SectionLabel pattern (analog)
    - src/components/home/section-writing.tsx — Link list pattern + Button at bottom (analog for
      loves teaser, which shows a few "loves" items before linking to /uses)
    - src/components/home/explorative-homepage.tsx — current state after Plan 17.1-01 (read the
      file as it exists after plan 01 completes; it will still import SectionWriting)
    - .planning/phases/17.1-homepage-rebuild-personal-brand-narrative-arc-who-am-i-what-/17.1-CONTEXT.md
      — D-06 section 4 spec (things I love teaser, links to /uses as-is)
  </read_first>
  <action>
    PART A — Create src/components/home/section-loves.tsx as a Server Component (no "use client").

    Imports: SectionLabel from "@/components/v3/section-label", Button from "@/components/v3/button".

    Export a named function SectionLoves. Section wrapper: className "min-h-dvh flex flex-col
    justify-center px-[8vw] py-[15vh]" (identical to other sections).

    SectionLabel: numeral "04", label text "Things I Love".

    Below the label, a prose paragraph (p element) with className "mt-4 mb-8 max-w-[52ch]
    text-text-dim font-display" and style={{ fontSize: "clamp(1rem,1.8vw,1.3rem)" }}.
    Text: "The tools, books, and ideas I keep coming back to." (no em dashes; short, understated).

    Then a short hardcoded list (D-10) of 3 items rendered as a flex-col stack of Links (from
    "next/link"). Each item: className "flex justify-between border-t border-border py-[1.4vh]
    text-text hover:text-accent transition-colors duration-150". Inside each Link: a span with
    className "font-display font-bold text-xl" for the item name, and a span with className
    "font-mono text-xs text-text-muted self-center" for a short tag. Use items:
    - { name: "Tools and stack", tag: "USES", href: "/uses" }
    - { name: "What I am reading", tag: "BOOKS", href: "/uses" }
    - { name: "Things I recommend", tag: "ALL", href: "/uses" }
    All three link to /uses (the existing route per D-06 scope fence — the /uses reframe is
    Phase 17.2, not this plan).

    Below the list, add a div with className "mt-8 border-t border-border pt-6" wrapping a
    Button with href="/uses" and text "See the full list".

    ---

    PART B — Update src/components/home/explorative-homepage.tsx to wire in new sections.

    Read the file as produced by Plan 17.1-01 (Server Component, no blob gate, four existing
    section imports). Make the following targeted changes:

    1. Add import for SectionWork from "./section-work"
    2. Add import for SectionLoves from "./section-loves"
    3. Remove the import for SectionWriting from "./section-writing"
    4. In the JSX return, update the section render order to:
       (a) hero section — inline (already in place from Plan 17.1-01)
       (b) <SectionBuilding />
       (c) <SectionWork />
       (d) <SectionLoves />
       (e) <SectionNewsletter />
       (f) <SectionFooter />

    This is the final D-06 narrative arc assembly order:
    who (hero) → what I'm building (Building) → selected work (Work) →
    things I love (Loves) → writing + newsletter (Newsletter) → footer contact (Footer).
  </action>
  <verify>
    <automated>npx tsc --noEmit --project tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/home/section-loves.tsx exists and exports SectionLoves
    - grep "use client" src/components/home/section-loves.tsx returns no match
    - grep "Things I Love" src/components/home/section-loves.tsx returns a match
    - grep "/uses" src/components/home/section-loves.tsx returns 3+ matches
    - grep "SectionWork\|SectionLoves" src/components/home/explorative-homepage.tsx returns 2+ matches
    - grep "SectionWriting" src/components/home/explorative-homepage.tsx returns no match (removed)
    - Section order in explorative-homepage.tsx JSX: SectionBuilding, SectionWork, SectionLoves,
      SectionNewsletter, SectionFooter (in this order)
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>section-loves.tsx created; explorative-homepage.tsx imports and renders all six sections
  in the D-06 narrative arc order; SectionWriting removed from the home render path; TypeScript
  compiles clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→site | Fully static SSG; section-work.tsx and section-loves.tsx are Server Components with no user input |
| site→external | New outbound link to prometheus.today (target="_blank") in section-work.tsx |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17.1-02 | Information Disclosure | prometheus.today outbound link in section-work.tsx | mitigate | rel="noopener noreferrer" on any target="_blank" anchor (enforced in Task 1 acceptance criteria) |
| T-17.1-03 | Spoofing | New sections have no forms, no auth, no user input | accept | Pure static render, no untrusted data |
| T-17.1-SC | Tampering | npm install | accept | No new npm packages introduced; no slopcheck needed |
</threat_model>

<verification>
After both tasks complete:
- ls src/components/home/section-work.tsx exits 0
- ls src/components/home/section-loves.tsx exits 0
- grep "SectionWriting" src/components/home/explorative-homepage.tsx returns no match
- grep -c "SectionWork\|SectionLoves\|SectionBuilding\|SectionNewsletter\|SectionFooter" src/components/home/explorative-homepage.tsx returns 5
- npx tsc --noEmit exits 0
</verification>

<success_criteria>
- section-work.tsx exports SectionWork: selected work teaser with portfolio items, link to /projects
- section-loves.tsx exports SectionLoves: things I love teaser with items, link to /uses
- explorative-homepage.tsx renders sections in D-06 order; SectionWriting retired
- External links in new sections use rel="noopener noreferrer"
- TypeScript compiles clean
</success_criteria>

<output>
Create .planning/phases/17.1-homepage-rebuild-personal-brand-narrative-arc-who-am-i-what-/17.1-02-SUMMARY.md when done
</output>
