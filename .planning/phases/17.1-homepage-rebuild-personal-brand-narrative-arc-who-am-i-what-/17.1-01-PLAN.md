---
phase: "17.1"
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/home/explorative-homepage.tsx
  - src/app/page.tsx
autonomous: true
requirements: []
# Phase 17.1 has no formal REQUIREMENTS.md REQ-IDs (TBD per ROADMAP).
# Coverage is governed by CONTEXT.md D-01..D-12. Citations are in must_haves truths.

must_haves:
  truths:
    - "D-01/D-02: No new palette tokens, font families, or import statements for styling libraries
       introduced; hero uses only existing token classes (sig, font-display, font-mono, text-text-dim,
       text-text-muted, text-accent, hover:text-accent, border-border) from globals.css and Tailwind v4."
    - "D-03 [informational]: Orange-intensity dial is deferred to post-first-pass tuning; the Pumpkin
       Amber field (#ff7a14) is kept at full saturation in this pass. Not a blocking requirement."
    - "D-05: explorative-homepage.tsx contains zero imports of CanvasLoader, FallbackPoster,
       HeroBlobCanvas, or canvas-loader. No 'use client' directive (component becomes Server Component)."
    - "D-10: Homepage is fully hardcoded JSX (revalidate=false preserved in page.tsx); no Notion API
       call or ISR revalidation time added."
    - "D-11: Hero h1 text is 'Founder of Prometheus' (sole professional identity). Copy contains no
       em dashes. Georgetown University and Prometheus are the only named institutions."
  artifacts:
    - path: "src/components/home/explorative-homepage.tsx"
      provides: "Refactored orchestrator — Server Component, text-forward hero inline, no blob gate logic"
      contains: "Founder of Prometheus"
    - path: "src/app/page.tsx"
      provides: "Homepage entry — updated JSDoc comment, no structural change"
  key_links:
    - from: "src/app/page.tsx"
      to: "src/components/home/explorative-homepage.tsx"
      via: "ExplorativeHomepage named export (name preserved, no page.tsx import change needed)"
      pattern: "ExplorativeHomepage"
---

<objective>
Remove the WebGL blob gate from the homepage orchestrator and replace the blob hero section with
a text-forward hero anchored on "Founder of Prometheus". The component drops its "use client"
directive and device-detection hooks (no longer needed without the canvas), becoming a plain
Server Component that assembles the existing section components in preparation for the full
narrative arc wired in Plans 17.1-02 and 17.1-03.

Purpose: This is the critical-path demolition step. Nothing else in Phase 17.1 can ship until
the blob stack is removed and the orchestrator is a clean section assembler.

Output: explorative-homepage.tsx as a Server Component with a text-forward hero section
(inline JSX), no canvas logic, existing four section imports preserved for Plan 17.1-02
to reorder and extend.
</objective>

<execution_context>
@/Users/Montster/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Montster/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17.1-homepage-rebuild-personal-brand-narrative-arc-who-am-i-what-/17.1-CONTEXT.md
@.planning/phases/17.1-homepage-rebuild-personal-brand-narrative-arc-who-am-i-what-/17.1-PATTERNS.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. -->

From src/app/globals.css (lines 76-91):
.sig class = color: var(--sig) + text-shadow: var(--sig-shadow)
[roasted-cocoa ink fill + hard cream drop shadow, lifted off amber field]
.sig-out class = -webkit-text-stroke: 2px var(--accent); color: transparent

Pumpkin Amber token utilities (Tailwind v4 @theme):
text-text (--color-text #2a1808), text-text-dim (rgba(42,24,8,0.72)),
text-text-muted (rgba(42,24,8,0.55)), text-accent (#0c6b74),
hover:text-accent, bg-bg (#ff7a14), border-border (rgba(42,24,8,0.18))
font-display = Space Grotesk (var(--font-space-grotesk))
font-mono = JetBrains Mono (var(--font-jetbrains-mono))

From src/components/v3/page-hero.tsx (the hero pattern analog):
h1 className pattern: "font-display font-bold uppercase leading-[0.86] tracking-[-0.03em]
text-[clamp(2.8rem,11vw,8rem)]" + .sig for filled display treatment
sub pattern: mt-6 max-w-[54ch] text-text-dim text-lg font-display
crumb pattern: font-mono text-xs uppercase tracking-[0.12em] text-text-muted

Current explorative-homepage.tsx exports:
export function ExplorativeHomepage() — MUST keep this name (page.tsx imports it)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor explorative-homepage.tsx — drop blob gate, add text-forward hero</name>
  <files>src/components/home/explorative-homepage.tsx</files>
  <read_first>
    - src/components/home/explorative-homepage.tsx — the file being replaced (read current structure fully)
    - src/components/v3/page-hero.tsx — closest analog for hero typography/spacing pattern
    - src/app/globals.css — sig treatment, token class definitions (lines 1-100)
  </read_first>
  <action>
    Rewrite explorative-homepage.tsx as a Server Component. Remove the "use client" directive.
    Remove all imports from react (useEffect, useState) and motion/react (useReducedMotion).
    Remove imports for CanvasLoader (./canvas-loader) and FallbackPoster (./fallback-poster).
    Remove the isTouchOrSmall state, webglOk state, the useEffect device-detection block, and
    the showCanvas const. Keep the four existing section imports: SectionBuilding, SectionWriting,
    SectionNewsletter, SectionFooter — Plan 17.1-02 will remove SectionWriting and add the new
    teaser sections.

    Replace the blob hero section (the relative/absolute canvas slot) with a text-forward hero
    section. The hero section wrapper uses className "min-h-dvh flex flex-col justify-center px-[8vw]"
    matching the section-building.tsx section wrapper pattern (no relative/absolute needed since
    there is no canvas overlay).

    Inside the hero section place three elements stacked vertically:

    (1) An h1 element with className "font-display font-bold uppercase sig
    text-[clamp(2.8rem,11vw,8rem)] leading-[0.9] tracking-[-0.03em]" and text content
    "Founder of Prometheus" (per D-11: sole professional identity).

    (2) A p element with className "mt-6 max-w-[54ch] text-text-dim font-display" and
    style={{ fontSize: "clamp(1rem, 2vw, 1.4rem)" }} with text content: "Building a company.
    Writing about what I am learning." (no em dashes per D-11; copy is intentionally spare
    — executor may sharpen the one-liner but must keep it under two sentences with no em dashes).

    (3) A p element with className "mt-8 font-mono text-xs uppercase tracking-[0.12em]
    text-text-muted" containing the woven Monty Monthly way in (D-07/D-09 — prose-style,
    not a button): the text "I write about building monthly." followed by a Next.js Link
    component pointing to "https://montymonthly.substack.com" with target="_blank" and
    rel="noopener noreferrer" and className "hover:text-accent transition-colors duration-150"
    and link text "Join Monty Monthly". The Link import comes from "next/link".

    The outer container div keeps className "min-h-screen bg-bg" but rename the CSS class
    string from "explorative-homepage" to "personal-homepage" to signal the refactor.

    IMPORTANT: src/components/home/hero-blob.tsx has an uncommitted modification (a shader fix,
    now moot). Do NOT stage or commit hero-blob.tsx in this plan. Leave it as a modified-but-untracked
    file. The blob files (canvas-loader.tsx, hero-blob.tsx, hero-blob-canvas.tsx, fallback-poster.tsx)
    are left on disk as orphans — do not delete them.

    The exported function name MUST remain ExplorativeHomepage (src/app/page.tsx imports it by
    name; no page.tsx change is needed for the function).
  </action>
  <verify>
    <automated>grep -r "CanvasLoader\|FallbackPoster\|HeroBlobCanvas\|canvas-loader" src/components/home/explorative-homepage.tsx; echo "exit:$?"</automated>
  </verify>
  <acceptance_criteria>
    - grep "CanvasLoader\|FallbackPoster\|HeroBlobCanvas\|canvas-loader" returns no matches in explorative-homepage.tsx
    - grep '"use client"' src/components/home/explorative-homepage.tsx returns no match
    - grep "useState\|useEffect\|webglOk\|isTouchOrSmall\|showCanvas" src/components/home/explorative-homepage.tsx returns no matches
    - grep "Founder of Prometheus" src/components/home/explorative-homepage.tsx returns a match
    - grep "montymonthly.substack.com" src/components/home/explorative-homepage.tsx returns a match
    - grep "noopener noreferrer" src/components/home/explorative-homepage.tsx returns a match (outbound link safety)
    - SectionBuilding, SectionWriting, SectionNewsletter, SectionFooter are still imported (Plan 17.1-02 will remove SectionWriting)
  </acceptance_criteria>
  <done>explorative-homepage.tsx is a Server Component with no blob gate, a text-forward hero
  showing "Founder of Prometheus" with one-liner and woven Monty Monthly link, and existing
  four section imports intact.</done>
</task>

<task type="auto">
  <name>Task 2: Update page.tsx JSDoc comment to reflect new purpose</name>
  <files>src/app/page.tsx</files>
  <read_first>
    - src/app/page.tsx — the file being updated (read fully before touching)
  </read_first>
  <action>
    Replace the JSDoc comment block above the Home function. The current comment describes
    the WebGL orchestrator purpose. Replace it with a comment describing the new purpose:
    the homepage is now a personal-brand narrative arc (who am I, what I'm building, how to engage),
    rendered as a fully static Server Component with hardcoded JSX copy (D-10, revalidate=false).
    No structural change to the component body — the ExplorativeHomepage import and JsonLd render
    remain unchanged.

    Confirm revalidate = false is still exported (do not remove it). Confirm JsonLd with
    buildPersonSchema() is still rendered (do not remove it).
  </action>
  <verify>
    <automated>npx tsc --noEmit --project tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/app/page.tsx still exports revalidate = false
    - src/app/page.tsx still renders JsonLd with buildPersonSchema()
    - src/app/page.tsx still imports ExplorativeHomepage from ./components/home/explorative-homepage
    - npx tsc --noEmit exits 0 (no TypeScript errors introduced by the orchestrator Server Component refactor)
    - grep "WebGL\|CanvasLoader\|webgl" src/app/page.tsx returns no matches (old comment language gone)
  </acceptance_criteria>
  <done>page.tsx comment updated; TypeScript compiles clean; no structural changes to the
  component body.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→site | Visitor loads a fully static (SSG) page; no login, no form submission, no API call on the home path |
| site→external | Outbound links to Substack, Prometheus, GitHub, LinkedIn, X |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17.1-01 | Information Disclosure | Outbound `target="_blank"` links in hero section | mitigate | All `target="_blank"` anchors carry `rel="noopener noreferrer"` — prevents new tab from accessing `window.opener`; confirmed in acceptance criteria |
| T-17.1-02 | Spoofing / Tampering | Static homepage — no user input, no auth, no API | accept | No untrusted data enters the render path; page is fully hardcoded JSX with `revalidate=false` |
| T-17.1-SC | Tampering | npm install | accept | This plan introduces zero new npm packages; no slopcheck needed |

> Honest surface note: this is a hardcoded static page with no forms, no sessions, and no Notion
> fetch on the home path. The threat surface is the outbound link safety pattern, nothing else.
</threat_model>

<verification>
After both tasks complete:
- npx tsc --noEmit exits 0
- grep -r "CanvasLoader\|FallbackPoster\|HeroBlobCanvas" src/app/page.tsx src/components/home/explorative-homepage.tsx returns no matches
- grep "Founder of Prometheus" src/components/home/explorative-homepage.tsx returns 1 match
- src/app/page.tsx: revalidate=false, JsonLd, ExplorativeHomepage import all present
</verification>

<success_criteria>
- explorative-homepage.tsx is a Server Component (no "use client", no React hooks)
- Blob gate logic fully removed; CanvasLoader/FallbackPoster orphaned (not deleted, not staged)
- Hero renders "Founder of Prometheus" as primary h1 in .sig treatment
- One-liner subtitle present; woven Monty Monthly link present as prose, not a button
- TypeScript compiles clean
</success_criteria>

<output>
Create .planning/phases/17.1-homepage-rebuild-personal-brand-narrative-arc-who-am-i-what-/17.1-01-SUMMARY.md when done
</output>
