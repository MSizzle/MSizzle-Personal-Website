---
phase: "17.1"
plan: 4
type: execute
wave: 3
depends_on:
  - "17.1-02"
  - "17.1-03"
files_modified: []
autonomous: false

requirements: []
# Phase 17.1 has no formal REQUIREMENTS.md REQ-IDs. Coverage by CONTEXT.md D-01..D-12.

must_haves:
  truths:
    - "D-12: The homepage renders the full personal-brand narrative arc at localhost:3000 and
       Monty can see and react to it (human visual verification gate)."
    - "npm run build exits 0 with no TypeScript errors and no missing-module errors; the home
       route produces a static HTML output (fully static SSG, revalidate=false)."
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Homepage route — unchanged structure, confirms build passes"
  key_links:
    - from: "npm run build"
      to: "src/app/page.tsx (home route)"
      via: "Next.js static generation"
      pattern: "revalidate = false"
---

<objective>
Validate the complete Phase 17.1 build: first as an automated production build gate, then as
a human visual checkpoint at localhost:3000. These are the only two verification steps in the
phase.

Purpose: D-12 mandates human visual review because Monty is design-driven and reacts to what
he sees. The automated build gate catches TypeScript and import errors before the visual review
so Monty's session is not interrupted by compile failures.

Output: A passing build and an approved human visual review of the narrative arc homepage.
</objective>

<execution_context>
@/Users/Montster/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Montster/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/17.1-homepage-rebuild-personal-brand-narrative-arc-who-am-i-what-/17.1-CONTEXT.md
@.planning/phases/17.1-homepage-rebuild-personal-brand-narrative-arc-who-am-i-what-/17.1-01-SUMMARY.md
@.planning/phases/17.1-homepage-rebuild-personal-brand-narrative-arc-who-am-i-what-/17.1-02-SUMMARY.md
@.planning/phases/17.1-homepage-rebuild-personal-brand-narrative-arc-who-am-i-what-/17.1-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated build gate — npm run build + blob-removal audit</name>
  <files></files>
  <read_first>
    - src/app/page.tsx — confirm revalidate=false and ExplorativeHomepage import still intact
    - src/components/home/explorative-homepage.tsx — confirm no canvas/blob imports
  </read_first>
  <action>
    Run the following verification commands in sequence and record all output in the SUMMARY:

    1. Type-check: npx tsc --noEmit
    2. Blob-removal audit:
       grep -r "CanvasLoader\|FallbackPoster\|HeroBlobCanvas\|canvas-loader" src/app/page.tsx src/components/home/explorative-homepage.tsx
       Expected: no output (zero matches on the home render path).
    3. Hero headline presence: grep "Founder of Prometheus" src/components/home/explorative-homepage.tsx
       Expected: at least one match.
    4. No Contact button: grep -rn '<Button.*[Cc]ontact\|button.*[Cc]ontact' src/components/home/
       Expected: no matches.
    5. Production build: npm run build
       Expected: exits 0, no errors; the / route is listed as static (●) in the build output.

    If npm run build fails, diagnose the error, fix it (likely a missing import or TypeScript
    mismatch), and re-run. Do not proceed to the human checkpoint until the build passes.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - npx tsc --noEmit exits 0
    - grep "CanvasLoader\|FallbackPoster\|HeroBlobCanvas" src/components/home/explorative-homepage.tsx returns no matches (D-05)
    - grep "Founder of Prometheus" src/components/home/explorative-homepage.tsx returns a match (D-11)
    - npm run build exits 0 with the / route generated as a static page
    - Build output does not contain "Error:" or "Failed to compile"
  </acceptance_criteria>
  <done>npm run build exits 0; blob stack confirmed absent from home render path; "Founder of
  Prometheus" confirmed present in hero; no Contact button in home components.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human visual review — narrative arc homepage at localhost:3000</name>
  <what-built>
    Phase 17.1 rebuilt the homepage around a personal-brand narrative arc. Here is what was
    built across the four plans:

    Plan 17.1-01: Removed the WebGL blob canvas gate from explorative-homepage.tsx (now a
    Server Component). Added a text-forward hero section with "Founder of Prometheus" as the
    primary display headline, a one-liner subtitle, and a woven prose link to Monty Monthly.

    Plan 17.1-02: Created section-work.tsx (selected work teaser linking to /projects) and
    section-loves.tsx (things I love teaser linking to /uses). Updated the orchestrator to
    render sections in D-06 narrative arc order: hero, Building, Work, Loves, Newsletter, Footer.

    Plan 17.1-03: Adapted section-building.tsx (Prometheus-forward prose + BigList), adapted
    section-newsletter.tsx (added 2 open essay teasers above the newsletter carousel), and
    adapted section-footer.tsx (Contact column heading softened; email + socials woven in).
  </what-built>
  <how-to-verify>
    1. Run: npm run dev
    2. Open: http://localhost:3000
    3. Scroll through the full page top to bottom. Verify each section:

       HERO (top): "Founder of Prometheus" displays as the large display headline in the
       sig (roasted cocoa + cream shadow) treatment on the Pumpkin Amber field. A one-liner
       subtitle is below it. A small woven text link to Monty Monthly is present — it should
       be understated (mono-xs style), not a button.

       WHAT I'M BUILDING (section 1): Prometheus named in a prose paragraph. BigList of
       current projects. Tone is confident and specific. No Contact CTA button.

       SELECTED WORK (section 2): SectionLabel "Selected Work". A few project items with
       a link to /projects. Brief prose intro.

       THINGS I LOVE (section 3): SectionLabel "Things I Love". Short list with link to /uses.

       WRITING + MONTY MONTHLY (section 4): Two essay titles and brief excerpts visible
       OPEN (no gate in front of them) above the newsletter carousel. "Monty Monthly" displays
       as a large .sig heading. Newsletter issue carousel present. Substack subscribe button present.

       FOOTER (section 5): Understated nav grid. Email (monty@prometheus.today) woven in as
       a text link. No button labeled "Contact" anywhere. Colophon at bottom.

    4. Confirm: no WebGL canvas, no fallback poster image, no blob sphere appears anywhere on
       the page.

    5. Confirm: no element is a <button> or styled as a button that says "Contact".
  </how-to-verify>
  <resume-signal>
    Type "approved" if the narrative arc reads correctly and the page looks intentional.
    Type "issues: [describe]" if anything is broken, misaligned, or copy needs tuning —
    the executor will fix and re-present.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→site | Build and visual review only; no new code written in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17.1-06 | Verification | Blob removal audit | mitigate | grep audit in Task 1 confirms zero blob imports on home render path before human sees the page |
| T-17.1-SC | Tampering | npm install | accept | No new packages in this plan |
</threat_model>

<verification>
- npm run build exits 0 (Task 1 automated gate)
- Human "approved" signal received (Task 2 checkpoint)
- grep "CanvasLoader\|FallbackPoster" src/components/home/explorative-homepage.tsx returns no matches
</verification>

<success_criteria>
- Production build passes with / route as static output
- Human visual review approved: all six narrative arc sections visible, no blob, no Contact button
- Phase 17.1 complete
</success_criteria>

<output>
Create .planning/phases/17.1-homepage-rebuild-personal-brand-narrative-arc-who-am-i-what-/17.1-04-SUMMARY.md when done
</output>
