---
phase: "17.1"
plan: 4
title: "Validate Phase 17.1 build — automated gate + human visual review"
status: complete
completed: 2026-07-01
tasks_total: 2
tasks_done: 2
---

# Summary — 17.1-04: Build gate + human visual review

Validation-only plan (no source files modified by the plan itself). Two gates:
an automated production-build gate, then the D-12 human visual review at
localhost:3000. Both passed. One copy iteration was applied during the human
review (hero headline).

## Task 1 — Automated build gate (PASS)

- `npx tsc --noEmit`: no TypeScript errors on the homepage render path
  (`src/components/home/*`, `src/app/page.tsx`). Pre-existing errors in unrelated
  test files (`uses.test.tsx`, `watching.test.tsx`, `robots.test.ts`) predate this
  phase and are out of scope.
- Blob-removal audit: `grep CanvasLoader|FallbackPoster|HeroBlobCanvas|canvas-loader`
  over `page.tsx` + `explorative-homepage.tsx` → zero matches (D-05 satisfied).
- Hero headline present (D-11): confirmed.
- No Contact button in `src/components/home/`: confirmed.
- `npm run build`: exits 0; `/` route generated as static (SSG, `revalidate=false`).

## Task 2 — Human visual review (APPROVED)

Dev server started (`npm run dev`, http://localhost:3000, 200 OK). Monty reviewed
the full six-section narrative arc: hero → What I'm Building → Selected Work →
Things I Love → Writing + Monty Monthly → Footer. No WebGL/blob/fallback poster
anywhere; no "Contact" button.

### Iteration applied during review (approved)

Monty requested the hero lead with his **guiding principle**, not his job title.

- Headline changed: `Founder of Prometheus` → **`Create Order from Chaos`**.
- Professional identity moved into the subtitle:
  "Founder of Prometheus. I build companies and write about what I am learning."
- This **revises decision D-11** (previously "sole professional identity" as the
  hero). The revision is the user's explicit call, captured in the checkpoint.
- Commit: `03671bc feat(17.1-04): lead hero with guiding principle 'Create Order from Chaos'`.

Re-reviewed and **approved** by Monty.

## Verification

- ✅ `npm run build` exits 0, `/` route static.
- ✅ Blob stack absent from home render path (grep audit).
- ✅ Human "approved" signal received after hero iteration.

## Key files

- Modified: `src/components/home/explorative-homepage.tsx` (hero copy iteration only).

## Notes / follow-ups

- `src/components/home/hero-blob.tsx` remains in the tree with an uncommitted shader
  fix from the pre-phase pause; it is now dead code (blob removed from the render
  path). Removal is deferred to Phase 17.2 (IA cleanup), per the handoff.
- Essay teaser links (`/blog/on-building-things-that-matter`,
  `/blog/the-quiet-compounders`) are intentional stubs that 404 until posts exist
  (T-17.1-05, accepted).
