---
context: phase
phase: 17.1-homepage-rebuild
task: null
total_tasks: null
status: paused
last_updated: 2026-07-01T14:44:08.776Z
---

# Handoff — Phase 17.1 Homepage Rebuild (paused before build)

_No blocking constraints. Resume is not gated._

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| GLSL inside a JS template literal | Putting backticks (`` ` ``) inside a `/* glsl */\`...\`` template string closes the string early → "Expected a semicolon" build error. Hit this while fixing hero-blob.tsx. | advisory | Never use backticks inside a shader template literal; keep comments backtick-free. |
| insert-phase reverse doc order | `phase.insert` places each new decimal directly after the target integer, so inserting 3 phases "after 17" produced doc order 17.3, 17.2, 17.1 and `next_phase` resolved to 17.3. Already corrected. | advisory | After multi-insert, verify `roadmap.analyze` `next_phase` and reorder blocks so document order matches numeric order. |

<current_state>
Mid-restructure. The whole site direction was pivoted this session. We just formalized + committed the roadmap (phases 17.1 → 17.2 → 17.3 inserted before the Phase 18 ship gate) and are paused right BEFORE planning/building Phase 17.1 (the homepage). Nothing of the new homepage is built yet. Dev server was stopped for a clean pause.
</current_state>

<completed_work>

- Diagnosed + fixed the broken WebGL hero blob (GLSL `tangent`-attribute shader compile error) — UNCOMMITTED in `src/components/home/hero-blob.tsx`; becomes moot once the blob is removed in 17.1.
- Pivoted the site to a personal-brand "business page": who am I → what I'm building → how to engage.
- Mocked the Prometheus flame palette + Fraunces/Space Grotesk type → Monty REJECTED both. Locked: keep the current Pumpkin Amber aesthetic + current fonts.
- Captured the full agreed direction to project memory `personal-brand-homepage-direction` (superseded `homepage-webgl-direction`).
- Inserted + committed roadmap phases 17.1/17.2/17.3 before the ship gate; fixed the reverse-order insert and the dependency chain (18 now depends on 17.3).
</completed_work>

<remaining_work>

- Phase 17.1: plan + build the new homepage (current aesthetic, blob removed, who→what→engage arc), show in dev server, iterate.
- Phase 17.2: Site Information Architecture (new nav; cut /events + /photos; delete /specimen + /v3-specimen; fold /links → footer; fold /watching → reframed /uses "Things I love"; merge /writing + /newsletter → one "Writing / Monty Monthly" surface).
- Phase 17.3: Portfolio (proud-of past work on the existing /projects Notion pipeline, linked from the homepage).
- Phase 18: ship gate (parked; its existing plans are now partly STALE vs the new structure — e.g. the WebGL-LCP checks and cut pages).
</remaining_work>

<decisions_made>

- KEEP the current Pumpkin Amber aesthetic + fonts; rebuild STRUCTURE only (flame palette + Fraunces/Space Grotesk mock rejected).
- Homepage = who→what→engage narrative; remove the WebGL blob from the home path.
- Velvet-rope engagement: no CTA button, no contact page; woven contextual invitations; reaching Monty is deliberately not easy.
- Hybrid writing: Monty Monthly is the headline + subscribe (velvet rope); a few flagship essays shown open; merge /writing + /newsletter.
- Cut /events + /photos; delete /specimen + /v3-specimen; fold /links → footer; fold /watching → reframed /uses; keep /about + /prometheus dedicated.
</decisions_made>

<blockers>
- None.
</blockers>

## Required Reading (in order)
1. Project memory `personal-brand-homepage-direction` — the full agreed direction (LOCKED aesthetic decision, IA, velvet-rope, hybrid newsletter).
2. `.planning/ROADMAP.md` §Phase 17.1 / 17.2 / 17.3 — goals + success criteria.
3. `.planning/sketches/006-flame-personal-brand/mock.html` — the REJECTED flame/type mock (so you don't re-propose it).

## Infrastructure State
- Dev server: STOPPED (was `npm run dev` on localhost:3000). Restart with `npm run dev` when building 17.1 so Monty can see the page.
- Branch: `v3`. Working tree has one uncommitted change: `src/components/home/hero-blob.tsx` (shader fix; safe to keep or discard).
- Notion pipeline was serving content locally this session (homepage/interior pages rendered), so real content should flow in.

<context>
Monty is design-driven and reacts to what he SEES, not descriptions — every step, spin up the dev server and show him, then iterate on the real page. He bounced off two mocks already; converged on "keep the current look, fix the structure." Keep momentum, small visible increments, check in on design.
</context>

<next_action>
Start with: `/gsd-plan-phase 17.1` to decompose the homepage rebuild (reads the memory + ROADMAP goal), then build a first pass in the current aesthetic and show Monty in `npm run dev` to iterate.
</next_action>
