---
phase: "17.1"
plan: 3
type: execute
wave: 2
depends_on:
  - "17.1-01"
files_modified:
  - src/components/home/section-building.tsx
  - src/components/home/section-newsletter.tsx
  - src/components/home/section-footer.tsx
autonomous: true
requirements: []
# Phase 17.1 has no formal REQUIREMENTS.md REQ-IDs. Coverage by CONTEXT.md D-01..D-12.

must_haves:
  truths:
    - "D-04: section-building.tsx reads as a narrative intro to what Monty is currently building,
       with Prometheus named explicitly as the main project. The tone is confident and specific,
       not a generic brutalist index."
    - "D-06/D-09: section-newsletter.tsx positions Monty Monthly as the headline + primary engagement
       channel. Above the NewsletterCarousel, 1-2 essay teasers (title + 2-3 sentence excerpt,
       hardcoded) are shown OPEN as proof-of-thinking per D-06 section 5."
    - "D-07: No <button> element with text 'Contact' or a call-to-action labeled 'Contact' exists
       in section-building.tsx, section-newsletter.tsx, or section-footer.tsx. Any invitation to
       connect is a text link or inline prose, not a Button component with contact intent."
    - "D-08: section-building.tsx copy uses selective/present-tense framing such as 'currently
       building' to signal active work without sales language."
    - "D-09: section-newsletter.tsx Substack Button (the subscribe/see-issues path) is kept as
       the primary engagement channel — the newsletter CTA is not removed (D-07 forbids Contact
       buttons, not the newsletter subscribe path which is the velvet rope per D-09)."
    - "D-10: All three sections contain only hardcoded JSX (no Notion fetch, no async function,
       no revalidate export)."
    - "D-11: No em dashes in any copy in these three files. Georgetown University is the only
       university named if any educational affiliation appears. Sole professional identity is
       'Founder of Prometheus'."
  artifacts:
    - path: "src/components/home/section-building.tsx"
      provides: "What I'm building — Prometheus-forward narrative section with woven connect prose"
      contains: "Prometheus"
    - path: "src/components/home/section-newsletter.tsx"
      provides: "Writing + Monty Monthly gate — 1-2 open essay teasers + carousel + Substack CTA"
      contains: "Monty Monthly"
    - path: "src/components/home/section-footer.tsx"
      provides: "Understated footer contact — email + socials woven in, velvet-rope tone, no Contact CTA button"
  key_links:
    - from: "src/components/home/section-newsletter.tsx"
      to: "https://montymonthly.substack.com"
      via: "Button CTA (accent variant) as primary subscribe path"
      pattern: "montymonthly.substack.com"
    - from: "src/components/home/section-footer.tsx"
      to: "mailto:monty@prometheus.today"
      via: "Woven email link in footer (no Contact heading or button)"
      pattern: "monty@prometheus.today"
---

<objective>
Adapt the three existing section components to serve the personal-brand narrative arc and the
velvet-rope engagement model. section-building.tsx gets a Prometheus-forward prose reframe.
section-newsletter.tsx gains 1-2 open essay teasers above the carousel as proof-of-thinking
(the D-06 combined "writing + Monty Monthly gate" section). section-footer.tsx is tuned for
understated restraint (D-07/D-08: no Contact CTA button, email + socials woven in).

Purpose: These three sections hold the emotional heart of the arc — "what I'm building" signals
authenticity, the essay teasers demonstrate thinking quality before the newsletter gate, and the
footer communicates exclusivity without eagerness.

Output: Three adapted Server Component files with updated copy and structure, ready for visual
review in Plan 17.1-04.
</objective>

<execution_context>
@/Users/Montster/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Montster/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/17.1-homepage-rebuild-personal-brand-narrative-arc-who-am-i-what-/17.1-CONTEXT.md
@.planning/phases/17.1-homepage-rebuild-personal-brand-narrative-arc-who-am-i-what-/17.1-PATTERNS.md
@.planning/phases/17.1-homepage-rebuild-personal-brand-narrative-arc-who-am-i-what-/17.1-01-SUMMARY.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. -->

section-building.tsx current structure (exact):
- Section wrapper: className "min-h-dvh flex flex-col justify-center px-[8vw] py-[15vh]"
- SectionLabel numeral="01" "What I'm"
- BigList items: Building/Writing/Doing

section-newsletter.tsx current structure (exact):
- SectionLabel numeral="03" "Newsletter"
- h2 "Monty Monthly" with .sig, clamp(2.2rem,6vw,4.4rem)
- p description with text-text-dim, clamp(1rem,2vw,1.5rem), max-width 30ch
- NewsletterCarousel with 3 hardcoded issues
- Button href="https://montymonthly.substack.com" accent "See the issues →"

section-footer.tsx current structure:
- Big greeting Link to /links: "Let's be friends." (already a soft link, not a button)
- Nav grid: Site | More | Elsewhere | Contact columns
- Contact column: a[href="mailto:monty@prometheus.today"] + a[href="https://prometheus.today"]
- Colophon: "© 2026 Monty Singer" + "Founder of Prometheus · Builder · Writer"

Token classes:
text-text, text-text-dim, text-text-muted, text-accent, hover:text-accent
border-border, font-display, font-mono, sig, font-bold, uppercase
transition-colors duration-150
clamp() sizing for responsive headings

Prose paragraph pattern (used across sections):
className "font-display text-text-dim" style={{ fontSize: "clamp(1rem, 2vw, 1.4rem)", maxWidth: "52ch" }}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adapt section-building.tsx — Prometheus-forward narrative copy</name>
  <files>src/components/home/section-building.tsx</files>
  <read_first>
    - src/components/home/section-building.tsx — full current file (read entirely before touching)
    - src/components/v3/section-label.tsx — SectionLabel props to confirm numeral + children API
    - src/components/v3/big-list.tsx — BigList items prop shape, any external href support
    - src/app/globals.css — token classes (lines 1-52)
    - .planning/phases/17.1-homepage-rebuild-personal-brand-narrative-arc-who-am-i-what-/17.1-CONTEXT.md
      — D-04 (narrative arc feel), D-07 (no Contact CTA), D-08 (scarcity/selective cues), D-11 (copy rules)
  </read_first>
  <action>
    Adapt section-building.tsx in place. Keep the Server Component structure, imports for BigList
    and SectionLabel, and section wrapper className unchanged.

    SectionLabel: keep numeral "01", update label text from "What I'm" to "What I'm Building"
    (single line label for the narrative arc reading; executor may keep two-line "What I'm" +
    "Building" if SectionLabel supports children split, as long as the reading is clear).

    Add a prose paragraph ABOVE the BigList. Use a p element with className "mt-4 mb-8
    max-w-[52ch] text-text-dim font-display" and style={{ fontSize: "clamp(1rem,1.8vw,1.3rem)" }}.
    Text for the paragraph (D-08 selective framing, D-11 copy rules, no em dashes):
    "I am currently building Prometheus, a company helping founders think more clearly about
    the businesses they are creating. I write about this, too." (Executor may sharpen this
    sentence while keeping: Prometheus named, "currently building" scarcity cue, no em dashes,
    no location, no contact ask in the paragraph itself — the paragraph is declaration not invitation.)

    BigList items: Update to make Prometheus the anchor. Suggest:
    - { label: "Prometheus", href: "https://prometheus.today", tag: "BUILDING", outline: true }
    - { label: "Writing", href: "/writing", tag: "ESSAYS" }
    - { label: "Projects", href: "/projects", tag: "WORKS" }

    If BigList does not natively support target="_blank" rel="noopener noreferrer" on external
    hrefs (check big-list.tsx), replace the Prometheus BigList item with a standalone display
    link element (a or Link) matching BigList's visual treatment rather than forcing unsafe
    external navigation through BigList. External link safety (noopener noreferrer) takes
    precedence over using BigList for the Prometheus item.

    Remove any "Doing" label. Keep section numeral "01" (this is the first section after the
    hero, before Work and Loves).
  </action>
  <verify>
    <automated>grep "Prometheus\|currently" src/components/home/section-building.tsx</automated>
  </verify>
  <acceptance_criteria>
    - grep "Prometheus" src/components/home/section-building.tsx returns a match
    - grep "currently" src/components/home/section-building.tsx returns a match (D-08 selective framing)
    - A prose paragraph (p element) is present above BigList in the JSX
    - grep "use client" src/components/home/section-building.tsx returns no match (stays Server Component)
    - grep "\-\-\|—" src/components/home/section-building.tsx returns no content matches in copy strings (no em dashes)
    - External prometheus.today link (if an anchor) has rel="noopener noreferrer"
    - npx tsc --noEmit reports no errors in section-building.tsx
  </acceptance_criteria>
  <done>section-building.tsx reframed as Prometheus-forward narrative with prose paragraph +
  BigList; no Contact CTA; copy rules honored; TypeScript clean.</done>
</task>

<task type="auto">
  <name>Task 2: Adapt section-newsletter.tsx (add essay teasers) + section-footer.tsx (velvet-rope tone)</name>
  <files>src/components/home/section-newsletter.tsx, src/components/home/section-footer.tsx</files>
  <read_first>
    - src/components/home/section-newsletter.tsx — full current file (read entirely)
    - src/components/home/section-footer.tsx — full current file (read entirely)
    - src/components/home/section-writing.tsx — Link list pattern + essay type shape (analog for
      essay teasers to add above the carousel)
    - src/app/globals.css — token classes
    - .planning/phases/17.1-homepage-rebuild-personal-brand-narrative-arc-who-am-i-what-/17.1-CONTEXT.md
      — D-06 section 5 spec (1-2 essays open), D-07 (no Contact button), D-09 (newsletter primary), D-11
  </read_first>
  <action>
    SECTION-NEWSLETTER.MD CHANGES:

    Update SectionLabel numeral from "03" to "05" (fifth section in the new arc). Keep label
    text "Writing" or update to "Writing & Newsletter" — executor's discretion, but must be
    clear this section contains both essay teasers and the newsletter gate.

    ESSAY TEASERS — Insert ABOVE the h2 "Monty Monthly" heading a new essay teaser block.
    Define a hardcoded type FeaturedEssay = { title: string; excerpt: string; href: string;
    date: string } and a const FEATURED_ESSAYS array with 2 items (D-10 hardcoded, no Notion):

    Item 1: title "On Building Things That Matter", excerpt "There is a kind of builder who
    builds because they cannot imagine not building. The work is not about the outcome. It is
    about the process of making something real.", href "/blog/on-building-things-that-matter",
    date "Jun 2026"

    Item 2: title "The Quiet Compounders", excerpt "Most of the people changing the world are
    not talking about changing the world. They are building quietly, compounding daily, and
    letting the work speak.", href "/blog/the-quiet-compounders", date "Apr 2026"

    Render each essay teaser as a block. Use a div or Link wrapping: a heading (h3 or Link
    styled as heading) with className "font-display font-bold text-[clamp(1.4rem,3vw,2rem)]
    leading-[1.1] text-text hover:text-accent transition-colors duration-150", the excerpt in
    a p element with className "mt-2 text-text-dim font-display max-w-[60ch]" and style
    {{ fontSize: "clamp(0.9rem,1.5vw,1.15rem)" }}, and a date/meta span with className
    "mt-2 block font-mono text-xs uppercase tracking-[0.1em] text-text-muted". Separate the
    two essays with a border-t border-border py-[2vh] pattern. Wrap the teaser block in a
    div with className "mb-12 mt-6".

    Keep EVERYTHING else in section-newsletter.tsx unchanged: h2 "Monty Monthly" with .sig
    treatment, the description paragraph, NewsletterCarousel with existing issues, and the
    Button component linking to Substack (D-09 primary engagement channel — the newsletter
    subscribe CTA is preserved).

    ---

    SECTION-FOOTER.TSX CHANGES:

    The "Contact" column heading. The current section-footer.tsx has a column labeled "Contact"
    in the nav grid (h4 "Contact" with the email address and Prometheus link). Per D-07, rename
    this column heading from "Contact" to "Reach out" (or just "Connect") to remove the explicit
    Contact label without removing the actual woven email + links. Change the h4 text string only.

    The big greeting Link "Let's be friends." currently links to /links. Update the href to
    "#" (a no-op anchor) for now since the /links route restructure is Phase 17.2 (out of scope
    here). Alternatively leave it at /links if the route currently exists; check src/app/links/
    or the ROADMAP to confirm the route is live. If /links exists, keep it. If it is being
    removed in 17.2, keep it here anyway — the link stays broken-in-place for now, Phase 17.2
    cleans it up. Do not overthink this — the copy change is the priority.

    No other structural changes to section-footer.tsx. Leave the nav grid columns (Site, More,
    Elsewhere) untouched. The email address (monty@prometheus.today) stays as a woven text
    link, not a button.
  </action>
  <verify>
    <automated>grep -c "FEATURED_ESSAYS\|essay\|teaser\|excerpt" src/components/home/section-newsletter.tsx</automated>
  </verify>
  <acceptance_criteria>
    - grep "FEATURED_ESSAYS" src/components/home/section-newsletter.tsx returns a match (essay data constant exists)
    - grep -c "excerpt" src/components/home/section-newsletter.tsx returns 2+ (two essay excerpts)
    - grep "Monty Monthly" src/components/home/section-newsletter.tsx returns a match (heading preserved)
    - grep "montymonthly.substack.com" src/components/home/section-newsletter.tsx returns a match (Substack CTA preserved per D-09)
    - grep '"Contact"' src/components/home/section-footer.tsx returns no match (column heading renamed)
    - grep 'monty@prometheus.today' src/components/home/section-footer.tsx returns a match (email woven in)
    - grep '"use client"' src/components/home/section-newsletter.tsx src/components/home/section-footer.tsx returns no matches
    - No em dashes in any copy strings (check: grep "\-\-\|—" both files returns no content matches)
    - npx tsc --noEmit exits 0 for both files
  </acceptance_criteria>
  <done>section-newsletter.tsx has 2 open essay teasers above the newsletter carousel; Substack
  CTA preserved; section-footer.tsx Contact column renamed to soft label; email link intact;
  no Contact CTA button; TypeScript clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→site | All three files are Server Components with no user input; no auth, no API |
| site→external | Outbound links to Substack (section-newsletter) and prometheus.today (section-building, section-footer) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17.1-04 | Information Disclosure | External outbound links (prometheus.today, substack.com) | mitigate | All `target="_blank"` anchors carry `rel="noopener noreferrer"` — verified in acceptance criteria |
| T-17.1-05 | Spoofing | Hardcoded essay hrefs (/blog/on-building-things-that-matter, /blog/the-quiet-compounders) | accept | These link to internal routes. If the routes do not exist, the link results in a 404 (not a security issue; a content hygiene issue the executor should note in SUMMARY) |
| T-17.1-SC | Tampering | npm install | accept | No new npm packages introduced in this plan |
</threat_model>

<verification>
After both tasks complete:
- grep "Prometheus" src/components/home/section-building.tsx returns a match
- grep "FEATURED_ESSAYS" src/components/home/section-newsletter.tsx returns a match
- grep '"Contact"' src/components/home/section-footer.tsx returns no match
- grep '"use client"' src/components/home/section-building.tsx src/components/home/section-newsletter.tsx src/components/home/section-footer.tsx returns no matches
- npx tsc --noEmit exits 0
</verification>

<success_criteria>
- section-building.tsx: Prometheus-forward prose + BigList, scarcity framing, no Contact CTA
- section-newsletter.tsx: 2 open essay teasers above the carousel (proof-of-thinking), Monty Monthly
  heading + Substack CTA preserved as primary velvet-rope channel (D-09)
- section-footer.tsx: "Contact" column heading renamed to soft label; email + socials woven in
- Copy in all three files: no em dashes, no location, sole identity "Founder of Prometheus"
- TypeScript compiles clean
</success_criteria>

<output>
Create .planning/phases/17.1-homepage-rebuild-personal-brand-narrative-arc-who-am-i-what-/17.1-03-SUMMARY.md when done
</output>
