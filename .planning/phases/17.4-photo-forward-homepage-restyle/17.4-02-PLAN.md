---
phase: 17.4-photo-forward-homepage-restyle
plan: 02
type: execute
wave: 2
depends_on: ["17.4-01"]
files_modified:
  - src/components/home/rail-box.tsx
  - src/components/home/photo.tsx
  - src/components/home/credibility-strip.tsx
autonomous: true
requirements: [D-04, D-05, D-07, D-11]
must_haves:
  truths:
    - "D-05: A RailBox renders an index numeral (accent) + uppercase label inside a high-contrast box that auto-inverts against whatever band it sits in."
    - "D-07: A Photo placeholder component renders a labeled block (hatch + icon + caption) supporting a light and a dark variant, ready to receive real photography later."
    - "D-11: A CredibilityStrip renders an 'as seen in' label plus placeholder logo marks."
    - "D-04: RailBox, Photo, and CredibilityStrip marks use hard corners (radius 0)."
  artifacts:
    - path: "src/components/home/rail-box.tsx"
      provides: "Auto-inverting index+label rail box (Server Component)"
      contains: "rail-box"
    - path: "src/components/home/photo.tsx"
      provides: "Placeholder photo block with light/dark variant + optional aspect-ratio/caption props"
      contains: "photo"
    - path: "src/components/home/credibility-strip.tsx"
      provides: "'As seen in' placeholder-logo strip (D-11)"
      contains: "proof"
  key_links:
    - from: "rail-box.tsx"
      to: "globals.css .rail-box / .rail-box .num / .rail-box .lbl"
      via: "className"
      pattern: "rail-box"
    - from: "photo.tsx"
      to: "globals.css .photo system"
      via: "className"
      pattern: "className=\"photo"
---

<objective>
Build the three static presentational primitives the reskinned sections and hero consume: the auto-inverting RailBox (D-05), the placeholder Photo block with light/dark variants (D-07), and the CredibilityStrip (D-11). All Server Components emitting the class names authored in Plan 01 — hard corners throughout (D-04).

Purpose: Interface-first — define these contracts before the sections (Wave 3) and hero (Wave 3) that import them, so executors receive the props/structure rather than exploring.
Output: `rail-box.tsx`, `photo.tsx`, `credibility-strip.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/17.4-photo-forward-homepage-restyle/17.4-CONTEXT.md
@.planning/phases/17.4-photo-forward-homepage-restyle/17.4-PATTERNS.md

<interfaces>
<!-- These are the contracts Wave-3 sections/hero will import. -->
RailBox props: { num: string; label: string }  → renders .rail-box > .num(num) + .lbl(label)
Photo props: { caption?: string; dark?: boolean; aspectRatio?: string; breathe?: boolean; className?: string }
  → renders div.photo[.dark] (style aspect-ratio) > div.img[.kenburns] + span.icon(◲) + span.cap(caption)
CredibilityStrip: no props; renders div.proof > .lbl + .marks(5x .m "Logo")
All are Server Components (no "use client"). Ken-burns/breathe are CSS classes gated by reduced-motion in Plan 01.
Analog: src/components/v3/section-label.tsx (numeral layout, does NOT invert — RailBox adds the inversion).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RailBox + CredibilityStrip primitives (D-05, D-11)</name>
  <read_first>
    - .planning/sketches/010-structured-bands-carousel/index.html (.rail-box CSS 94-97 + markup 278/292/309; .proof CSS 208-213 + markup 269-272)
    - src/components/v3/section-label.tsx (numeral analog — structural reference only)
    - src/app/globals.css (the .rail-box, .proof, .m rules authored in Plan 01)
  </read_first>
  <action>
    Create src/components/home/rail-box.tsx as a Server Component exporting `RailBox({ num, label })`: render `<div className="rail">` &gt; `<div className="rail-box">` containing `<div className="num">{num}</div>` and `<div className="lbl">{label}</div>`. No color classes — the box inverts via the globals CSS (background:var(--color-text)). Create src/components/home/credibility-strip.tsx as a Server Component exporting `CredibilityStrip()`: render `<div className="proof">` with a `.lbl` reading "Building and writing at the edge of AI · as seen in" (no em dashes — use a middot or period per site copy rules) and a `.marks` row of five `<div className="m">Logo</div>` placeholders (D-11; keep-or-cut deferred). Both hard-cornered via inherited radius-0.
  </action>
  <verify>
    <automated>grep -q 'rail-box' src/components/home/rail-box.tsx && grep -q 'proof' src/components/home/credibility-strip.tsx && npx tsc --noEmit && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - rail-box.tsx exports `RailBox` taking `num` and `label`, emits `.rail-box > .num + .lbl`; no `"use client"`.
    - credibility-strip.tsx exports `CredibilityStrip`, emits `.proof > .lbl + .marks` with 5 `.m` marks; label contains no em dash (—).
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>RailBox and CredibilityStrip exist as Server Components emitting the Plan-01 classes; typecheck clean.</done>
</task>

<task type="auto">
  <name>Task 2: Photo placeholder primitive (D-04, D-07)</name>
  <read_first>
    - .planning/sketches/010-structured-bands-carousel/index.html (.photo system CSS 76-88; usages: hero portrait 261, building 282, work grid 296-299, loves 410, mm cover 429)
    - src/app/globals.css (the .photo/.photo.dark/.photo .icon/.photo .cap rules from Plan 01)
  </read_first>
  <action>
    Create src/components/home/photo.tsx as a Server Component exporting `Photo({ caption, dark = false, aspectRatio, breathe = false, className })`: render `<div className={cn("photo", dark && "dark", breathe && "breathe", className)} style={aspectRatio ? { aspectRatio } : undefined}>` containing `<div className="img kenburns" />`, `<span className="icon">◲</span>`, and, when caption is provided, `<span className="cap">{caption}</span>`. Use the existing `cn` util from @/utils/cn. The `.img.kenburns` and `.breathe` classes are the ambient-drift hooks (reduced-motion-gated in Plan 01) — no JS. Ensure no rounded corners are added (hard corners, D-04). This is the single placeholder that later receives real photography (deferred).
  </action>
  <verify>
    <automated>grep -q 'className.*"photo\|cn("photo' src/components/home/photo.tsx && grep -q 'kenburns' src/components/home/photo.tsx && npx tsc --noEmit && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - photo.tsx exports `Photo` with props `caption?`, `dark?`, `aspectRatio?`, `breathe?`, `className?`; no `"use client"`.
    - Emits `.photo` (with optional `.dark`/`.breathe`), an inner `.img.kenburns`, `.icon` glyph, and conditional `.cap`.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Photo renders the placeholder block with light/dark + breathe variants and optional caption/aspect-ratio; typecheck clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| none (static presentational) | Server Components render fixed placeholder markup; no user input, no external fetch |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17.4-02 | Info Disclosure | placeholder caption text | accept | Static hardcoded labels; no PII, no dynamic data. |
</threat_model>

<verification>
- Three new Server Components compile and export the documented contracts.
- No `"use client"` in any of the three (they are static).
</verification>

<success_criteria>
RailBox (D-05), Photo (D-07), and CredibilityStrip (D-11) exist as hard-cornered (D-04) Server Components emitting Plan-01 classes, ready for Wave-3 consumers.
</success_criteria>

<output>
Create `.planning/phases/17.4-photo-forward-homepage-restyle/17.4-02-SUMMARY.md` when done.
</output>
