---
phase: 17.4-photo-forward-homepage-restyle
plan: 03
type: execute
wave: 2
depends_on: ["17.4-01"]
files_modified:
  - src/components/home/scroll-reveals.tsx
  - src/components/home/photo-marquee.tsx
autonomous: true
requirements: [D-07, D-08]
must_haves:
  truths:
    - "D-07: On scroll, elements with .reveal/.slide/.shadowed gain an .in class so photos slide in from the side (alternating L/R) and settle with the even black drop-shadow."
    - "D-08: The reveal wiring is scroll-triggered only (IntersectionObserver), never cursor-dependent; under prefers-reduced-motion every target is marked .in immediately so the static layout holds."
    - "D-07: A PhotoMarquee renders a horizontally-scrolling row of placeholder photo cards for 'Things I Love', animation-gated by reduced motion."
    - "D-08: No WebGL and no Lenis are imported by either island."
  artifacts:
    - path: "src/components/home/scroll-reveals.tsx"
      provides: "Headless client island: IntersectionObserver that toggles .in on .reveal/.slide/.shadowed"
      contains: "IntersectionObserver"
    - path: "src/components/home/photo-marquee.tsx"
      provides: "Client marquee of placeholder photo cards with reduced-motion gate"
      contains: "useReducedMotion"
  key_links:
    - from: "scroll-reveals.tsx"
      to: ".reveal, .slide, .shadowed elements in the DOM"
      via: "document.querySelectorAll + IntersectionObserver"
      pattern: "querySelectorAll\\(.*reveal"
---

<objective>
Build the two scroll/ambient motion islands (D-07, D-08): a headless `ScrollReveals` island that wires the IntersectionObserver toggling `.in` (the genuinely-new IO pattern the codebase lacks), and a `PhotoMarquee` client island rendering the "Things I Love" placeholder photo row. Both honor `prefers-reduced-motion` and leave the static layout intact; neither touches WebGL/Lenis.

Purpose: These islands make the Plan-01 CSS actually animate on scroll while keeping the client surface minimal — one observer wires every section's photos.
Output: `scroll-reveals.tsx`, `photo-marquee.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/17.4-photo-forward-homepage-restyle/17.4-CONTEXT.md
@.planning/phases/17.4-photo-forward-homepage-restyle/17.4-PATTERNS.md

<interfaces>
<!-- ScrollReveals is mounted ONCE by the orchestrator (Plan 08). It renders null. -->
ScrollReveals(): "use client"; useEffect → new IntersectionObserver(threshold:0, rootMargin:'0px 0px -38% 0px')
  observing every `.reveal, .slide, .shadowed` not already `.in`; on intersect add `.in` + unobserve.
  Reduced-motion (matchMedia '(prefers-reduced-motion: reduce)') → add `.in` to all targets immediately, skip observer.
PhotoMarquee props: { items: string[] } → duplicated track of .photo (aspect-ratio 3/4) cards with caption=item.
  Uses useReducedMotion() from motion/react; animation via the .marquee/.track CSS (Plan 01), paused when reduced.
Analog: src/components/v3/marquee.tsx (reduced-motion gate + doubled track) and src/components/v3/reveal.tsx (reduced-motion short-circuit).
LazyMotion strict: any motion/react usage must import from motion/react; PhotoMarquee only needs useReducedMotion (a hook, safe). ScrollReveals uses raw matchMedia (no motion import needed).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ScrollReveals IntersectionObserver island (D-07, D-08)</name>
  <read_first>
    - .planning/sketches/010-structured-bands-carousel/index.html (IO reveal wiring, script lines 444-445; .reveal/.slide/.shadowed CSS 169-176)
    - src/components/v3/reveal.tsx (reduced-motion short-circuit precedent, lines 26-50)
    - .planning/phases/17.4-photo-forward-homepage-restyle/17.4-PATTERNS.md (New Pattern 1, lines 192-206)
  </read_first>
  <action>
    Create src/components/home/scroll-reveals.tsx as a `"use client"` headless island exporting `ScrollReveals()` that returns null. In a `useEffect`: if `window.matchMedia('(prefers-reduced-motion: reduce)').matches`, immediately add class `in` to every `.reveal, .slide, .shadowed` element and return (static layout, D-08). Otherwise create one `IntersectionObserver` with `{ threshold: 0, rootMargin: '0px 0px -38% 0px' }` (matching the sketch), observe every `.reveal, .slide, .shadowed` element that does not already have `.in`, and on `isIntersecting` add `.in` and `unobserve` the target. Clean up (disconnect) on unmount. Do NOT import WebGL or Lenis. Guard for SSR (effect only runs client-side). This single observer intentionally wires every section's photos globally (minimal client surface).
  </action>
  <verify>
    <automated>grep -q 'IntersectionObserver' src/components/home/scroll-reveals.tsx && grep -q 'prefers-reduced-motion' src/components/home/scroll-reveals.tsx && grep -q '"use client"' src/components/home/scroll-reveals.tsx && grep -q "rootMargin" src/components/home/scroll-reveals.tsx && npx tsc --noEmit && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - scroll-reveals.tsx begins with `"use client"`, exports `ScrollReveals`, returns null.
    - Uses `IntersectionObserver` with `rootMargin: '0px 0px -38% 0px'`; adds `.in` + unobserves on intersect; disconnects on cleanup.
    - Reduced-motion branch adds `.in` to all targets immediately with no observer.
    - `grep -i 'webgl\|lenis' src/components/home/scroll-reveals.tsx` returns nothing.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>The IO island toggles .in on scroll, honors reduced-motion by static reveal, imports no heavy scroll stack; typecheck clean.</done>
</task>

<task type="auto">
  <name>Task 2: PhotoMarquee island for Things I Love (D-07, D-08)</name>
  <read_first>
    - .planning/sketches/010-structured-bands-carousel/index.html (.marquee/.track CSS 118-121; loves track JS 407-410; photo cards aspect-ratio 3/4)
    - src/components/v3/marquee.tsx (doubled-track + useReducedMotion gate)
    - src/components/home/photo.tsx (Photo primitive from Plan 02 — reuse for cards)
  </read_first>
  <action>
    Create src/components/home/photo-marquee.tsx as a `"use client"` island exporting `PhotoMarquee({ items }: { items: string[] })`. Duplicate items back-to-back (`[...items, ...items]`) so the CSS translateX(-50%) loop is seamless. Render `<div className="marquee">` &gt; `<div className="track" style={{ gap: 18 }}>` mapping each entry to a fixed-width cell (`flex:0 0 clamp(220px,25vw,310px)`) wrapping a `<Photo aspectRatio="3/4" caption={item} />`. Import `useReducedMotion` from motion/react; when reduced, do not apply the running animation (the `.marquee .track` keyframe is also killed by the Plan-01 reduced-motion CSS, but gate here too for belt-and-suspenders, mirroring marquee.tsx). Mark the duplicated (second-half) cards `aria-hidden`. Default `items` in the consumer (Plan 07) will be the "A place I go / A tool I trust / Off the clock / Reading now" set.
  </action>
  <verify>
    <automated>grep -q 'useReducedMotion' src/components/home/photo-marquee.tsx && grep -q 'marquee' src/components/home/photo-marquee.tsx && grep -q '"use client"' src/components/home/photo-marquee.tsx && npx tsc --noEmit && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - photo-marquee.tsx begins with `"use client"`, exports `PhotoMarquee({ items })`, doubles the track.
    - Renders `.marquee > .track` of `Photo` cards (aspect-ratio 3/4) with captions from `items`; second half `aria-hidden`.
    - Uses `useReducedMotion` from `motion/react` to gate the running animation.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>PhotoMarquee renders a duplicated placeholder photo row, animation gated by reduced motion; typecheck clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client DOM | Islands read layout state (scroll/intersection) only; no user data, no network |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17.4-03 | Denial | JS-disabled / reduced-motion | mitigate | Reduced-motion branch + Plan-01 CSS media query guarantee content visible even if the observer never runs (D-08). |
</threat_model>

<verification>
- Both islands compile; ScrollReveals imports no WebGL/Lenis.
- Reduced-motion paths present in both.
</verification>

<success_criteria>
ScrollReveals (IO toggler, D-07/D-08) and PhotoMarquee (D-07) exist as reduced-motion-safe client islands with no heavy scroll stack.
</success_criteria>

<output>
Create `.planning/phases/17.4-photo-forward-homepage-restyle/17.4-03-SUMMARY.md` when done.
</output>
