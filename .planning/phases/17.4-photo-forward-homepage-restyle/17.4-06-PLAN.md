---
phase: 17.4-photo-forward-homepage-restyle
plan: 06
type: execute
wave: 3
depends_on: ["17.4-01", "17.4-02", "17.4-03"]
files_modified:
  - src/components/home/section-building.tsx
  - src/components/home/section-work.tsx
  - src/__tests__/home/section-building.test.tsx
  - src/__tests__/home/section-work.test.tsx
autonomous: true
requirements: [D-03, D-04, D-05, D-07, D-13]
must_haves:
  truths:
    - "D-05: SectionBuilding leads with a rail box (index 01 · Building); SectionWork with a rail box (index 02 · Selected work)."
    - "D-07: SectionBuilding renders a wide slide-in dark photo (from-left, even black drop-shadow); SectionWork renders a 2x2 work grid of slide-in photos alternating from-left/from-right."
    - "D-13: SectionWork preserves the /portfolio link and shows no /projects link; the Prometheus external link keeps rel=noopener noreferrer; no contact CTA is introduced."
    - "D-03: Both sections use token classes only (text-text/border-border) so they auto-invert when the orchestrator wraps Building in a dark band and Work in a light band — no hardcoded band colors."
  artifacts:
    - path: "src/components/home/section-building.tsx"
      provides: "Building beat: rail-box 01 + headline + body w/ inline accent link + wide slide-in dark photo"
      contains: "rail"
    - path: "src/components/home/section-work.tsx"
      provides: "Work beat: rail-box 02 + headline + 2x2 slide-in work grid, /portfolio preserved"
      contains: "work-grid"
  key_links:
    - from: "section-work.tsx"
      to: "/portfolio"
      via: "next/link (preserved 17.3 invariant)"
      pattern: "/portfolio"
    - from: "section-building.tsx / section-work.tsx"
      to: "RailBox + Photo primitives"
      via: "import"
      pattern: "RailBox|Photo"
---

<objective>
Reskin the first two content beats to the sketch-010 structure (D-03/D-05/D-07): SectionBuilding (dark band, rail box 01, wide slide-in dark photo) and SectionWork (light band, rail box 02, 2x2 slide-in work grid). Preserve the Phase-17.3 SectionWork invariants (D-13): the /portfolio link, no /projects, and rel=noopener on the Prometheus external link. Rewrite the stale section-building test and update the section-work test to the new structure.

Purpose: These beats are structurally symmetric (rail + content grid + slide photos) and share the RailBox/Photo/slide contracts, so they reskin together.
Output: reskinned section-building.tsx, section-work.tsx, and their tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/17.4-photo-forward-homepage-restyle/17.4-CONTEXT.md
@.planning/phases/17.4-photo-forward-homepage-restyle/17.4-PATTERNS.md

<interfaces>
<!-- Sections return the beat CONTENT (a .wrap > .beat-grid); the orchestrator (Plan 08) supplies the
     outer band section (band / band-dark) + id and the light/dark alternation. -->
RailBox({ num, label }) — Plan 02.  Photo({ caption, dark, aspectRatio, className }) — Plan 02.
Slide-in: wrap a photo in a div with classes "shadowed slide from-left" or "shadowed slide from-right" (Plan-01 CSS); ScrollReveals (Plan 03) toggles the .in class.
section-work.test invariants to KEEP (T-17.1-02 / SC-2): a /portfolio link exists; NO /projects link; the Prometheus anchor rel contains noopener+noreferrer; a "SELECTED" kicker string is present.
section-building.test is STALE (asserts BigList Building/Writing/Doing) — rewrite to the new rail-box + photo structure.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reskin SectionBuilding (dark beat, rail 01, slide-in photo) — D-03/D-05/D-07</name>
  <read_first>
    - src/components/home/section-building.tsx (current file — preserve the Prometheus external-link rel treatment)
    - .planning/sketches/010-structured-bands-carousel/index.html (#building markup 275-286: rail-box 01, h2, p.body inline link, wide shadowed slide from-left photo aspect 16/6.5)
    - src/components/home/rail-box.tsx, src/components/home/photo.tsx (Plan 02 contracts)
  </read_first>
  <action>
    Rewrite src/components/home/section-building.tsx as a Server Component exporting SectionBuilding() that returns the beat CONTENT only (no outer section min-h wrapper, no band class — the orchestrator supplies the dark band + id). Render a div.wrap containing div.beat-grid with: a left rail div.rail.reveal wrapping RailBox num="01" label="Building"; a right column with an h2.reveal Prometheus-forward headline (e.g. "Prometheus: AI integrations and education for people who refuse to be left behind." — NO em dash, use a colon), a p.body.reveal carrying a woven inline accent link (an anchor with className "inline" to "#writing" reading "let's talk" — velvet-rope, no button), and a div with classes "shadowed slide from-left" wrapping Photo dark aspectRatio="16/6.5" caption="Wide Prometheus shot". Use token classes (text-text/text-text-dim) so the dark band inverts automatically (D-03). Keep any Prometheus external anchor as target="_blank" rel="noopener noreferrer". No em dashes anywhere.
  </action>
  <verify>
    <automated>grep -q 'RailBox' src/components/home/section-building.tsx && grep -q 'slide from-left' src/components/home/section-building.tsx && ! grep -q '—' src/components/home/section-building.tsx && ! grep -q 'use client' src/components/home/section-building.tsx && npx tsc --noEmit && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - section-building.tsx is a Server Component returning .wrap > .beat-grid with RailBox num="01" label="Building", an h2, a .body with an inline woven link, and a shadowed/slide/from-left Photo (dark, 16/6.5).
    - No em dash; no outer band class/min-h wrapper; any external anchor keeps rel=noopener noreferrer.
    - npx tsc --noEmit exits 0.
  </acceptance_criteria>
  <done>Building beat matches the sketch structure using RailBox + slide-in dark photo, band-agnostic and em-dash-free; typecheck clean.</done>
</task>

<task type="auto">
  <name>Task 2: Reskin SectionWork (light beat, rail 02, 2x2 work grid) — preserve 17.3 invariants (D-13)</name>
  <read_first>
    - src/components/home/section-work.tsx (current file — /portfolio link, Prometheus rel, SELECTED kicker to preserve)
    - src/__tests__/home/section-work.test.tsx (the LIVE invariants: /portfolio present, no /projects, rel=noopener, SELECTED)
    - .planning/sketches/010-structured-bands-carousel/index.html (#work markup 289-304: rail-box 02, h2, work-grid 2x2 alternating from-left/from-right photos aspect 3/2.2)
    - src/components/home/rail-box.tsx, src/components/home/photo.tsx
  </read_first>
  <action>
    Rewrite src/components/home/section-work.tsx as a Server Component exporting SectionWork() returning the beat CONTENT only. Render div.wrap > div.beat-grid with a left RailBox num="02" label="Selected work" in div.rail.reveal; a right column with an h2.reveal ("A few things I am proud of.") and a div.work-grid (2 columns) of four cells each with classes "shadowed slide from-left" or "shadowed slide from-right" (alternate L/R) wrapping a Photo aspectRatio="3/2.2" caption="Project ... · 2025". PRESERVE the Phase-17.3 invariants (D-13): include a next/link to /portfolio (a "See selected work →" affordance carrying a "SELECTED" kicker string), render NO link to /projects, and keep any external Prometheus anchor with rel="noopener noreferrer". Band-agnostic token classes only. No em dashes.
  </action>
  <verify>
    <automated>grep -q '/portfolio' src/components/home/section-work.tsx && ! grep -q '/projects' src/components/home/section-work.tsx && grep -q 'work-grid' src/components/home/section-work.tsx && grep -q 'SELECTED' src/components/home/section-work.tsx && ! grep -q '—' src/components/home/section-work.tsx && npx tsc --noEmit && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - section-work.tsx renders RailBox num="02", a .work-grid of 4 slide-in Photos alternating from-left/from-right, a /portfolio link, and a "SELECTED" kicker.
    - No /projects link anywhere; any Prometheus external anchor keeps rel=noopener noreferrer; no em dash.
    - npx tsc --noEmit exits 0.
  </acceptance_criteria>
  <done>Work beat matches the sketch 2x2 grid while preserving the /portfolio + rel + SELECTED invariants; typecheck clean.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite section-building test, update section-work test</name>
  <read_first>
    - src/__tests__/home/section-building.test.tsx (STALE — asserts BigList Building/Writing/Doing; rewrite)
    - src/__tests__/home/section-work.test.tsx (update selectors while keeping the SC-2 / T-17.1-02 assertions)
    - the two reskinned section files from Tasks 1-2 (final structure to assert against)
  </read_first>
  <action>
    Rewrite src/__tests__/home/section-building.test.tsx to assert the NEW structure: mock @/components/home/rail-box and @/components/home/photo (render props/children into simple test divs), render SectionBuilding, and assert the rail box shows "01" and "Building" and that a slide-in photo container is present. Remove the obsolete BigList/Building-Writing-Doing assertions (HD-04 stale per project memory). Update src/__tests__/home/section-work.test.tsx: keep the five live assertions (renders /portfolio, no /projects, portfolio text, "SELECTED" kicker, Prometheus rel=noopener+noreferrer) but adjust mocks/selectors (mock RailBox/Photo as needed) so they resolve against the reskinned component. Both suites must pass. Do not weaken the section-work invariants.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/home/section-building.test.tsx src/__tests__/home/section-work.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - section-building.test.tsx no longer references BigList/Doing; asserts rail "01"/"Building" + a photo container; passes.
    - section-work.test.tsx retains all five SC-2 / T-17.1-02 invariant assertions and passes.
    - vitest run for both files exits 0 with all tests passing.
  </acceptance_criteria>
  <done>Both section tests pass against the reskinned components; section-work invariants intact, section-building test modernized.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| outbound link → Prometheus | External navigation target (reverse-tabnabbing surface) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17.4-06 | Tampering (reverse tabnabbing) | Prometheus target=_blank anchor | mitigate | rel="noopener noreferrer" preserved and asserted by section-work.test. |
| T-17.4-06b | Info Disclosure | placeholder captions | accept | Static hardcoded labels; no dynamic data. |
</threat_model>

<verification>
- Both sections compile band-agnostically (token classes only); Building has no dark/light hardcode.
- section-work invariants (/portfolio, no /projects, rel, SELECTED) preserved and green.
- No em dash in either section file.
</verification>

<success_criteria>
Building (dark, rail 01, slide-in photo) and Work (light, rail 02, 2x2 grid) beats match sketch-010 (D-03/D-05/D-07) while preserving the velvet-rope + 17.3 link invariants (D-13); tests pass.
</success_criteria>

<output>
Create `.planning/phases/17.4-photo-forward-homepage-restyle/17.4-06-SUMMARY.md` when done.
</output>
