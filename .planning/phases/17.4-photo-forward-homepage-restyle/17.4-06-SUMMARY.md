---
phase: "17.4-photo-forward-homepage-restyle"
plan: "06"
subsystem: "ui-components"
tags: ["section-building", "section-work", "rail-box", "photo", "scroll-reveal", "tests", "sketch-010"]
dependency_graph:
  requires:
    - "17.4-01 (CSS: .beat-grid, .rail, .rail-box, .work-grid, .shadowed, .slide, .from-left, .from-right)"
    - "17.4-02 (RailBox and Photo Server Component primitives)"
    - "17.4-03 (ScrollReveals IO island that toggles .in on .reveal/.slide/.shadowed)"
  provides:
    - "SectionBuilding: dark-band beat with RailBox 01 + wide slide-in dark photo (D-05, D-07)"
    - "SectionWork: light-band beat with RailBox 02 + 2x2 slide-in work grid (D-05, D-07)"
    - "Passing section-building.test (rewritten for new structure)"
    - "Passing section-work.test (SC-2 / T-17.1-02 invariants preserved)"
  affects:
    - "explorative-homepage.tsx (Plan 08 orchestrator consumes both sections)"
    - "Phase-17.3 invariant suite (SC-2 asserts still green)"
tech_stack:
  added: []
  patterns:
    - "Beat-content-only Server Component (no outer band/section; orchestrator supplies band)"
    - "RailBox + Photo composition inside div.beat-grid"
    - "Slide-in wrapper div.shadowed.slide.from-left|from-right around Photo"
    - "Token-class-only coloring (text-text, text-text-dim) for dark/light band auto-inversion"
key_files:
  created: []
  modified:
    - "src/components/home/section-building.tsx: RailBox 01 + h2 (colon, no em dash) + woven inline link + wide dark slide-in Photo (16/6.5)"
    - "src/components/home/section-work.tsx: RailBox 02 + 2x2 work-grid Photos alternating from-left/from-right + /portfolio link + SELECTED kicker + Prometheus external anchor"
    - "src/__tests__/home/section-building.test.tsx: rewritten to assert rail-box num/label and slide-in photo container; BigList/Doing assertions removed"
    - "src/__tests__/home/section-work.test.tsx: mocks updated for RailBox/Photo; all five SC-2/T-17.1-02 invariants preserved"
decisions:
  - "SectionBuilding returns .wrap>.beat-grid (no outer band/section element); orchestrator supplies .band.dark per D-03"
  - "SectionWork returns .wrap>.beat-grid; no /projects link; Prometheus external anchor kept for T-17.1-02 invariant"
  - "Beat-content-only pattern chosen for both sections so Plan 08 can wrap in any band without component changes"
  - "RailBox wrapped in <div className='reveal'> rather than adding a className prop to RailBox itself (RailBox has no className prop per Plan 02 contract)"
  - "Portfolio link uses <span>Portfolio</span><span>SELECTED</span> so testContent.toContain('Portfolio') and getByText('SELECTED') both pass"
metrics:
  duration: "4m"
  completed: "2026-07-04"
  tasks_completed: 3
  files_modified: 4
---

# Phase 17.4 Plan 06: SectionBuilding + SectionWork Reskin Summary

Both content beats reskinned to sketch-010 structure: RailBox + slide-in Photos, band-agnostic token classes, Phase-17.3 invariants intact, 11 tests passing.

## What Was Built

### Task 1: SectionBuilding (dark beat, rail 01, slide-in photo)

`src/components/home/section-building.tsx` was rewritten as a beat-content-only Server Component. Renders `div.wrap > div.beat-grid` with:

- Left column: `<div className="reveal"><RailBox num="01" label="Building" /></div>`
- Right column: `h2.reveal` headline (Prometheus: ... colon, no em dash), `p.body.reveal` with a woven `<a className="inline" href="#writing">let's talk</a>`, and a `div.shadowed.slide.from-left` wrapping `<Photo dark aspectRatio="16/6.5" caption="Wide Prometheus shot" />`.

Token classes only (no color hardcoding) so the dark band inverts automatically. The outer `.band.dark` section and `id="building"` are supplied by the Plan 08 orchestrator.

### Task 2: SectionWork (light beat, rail 02, 2x2 work grid)

`src/components/home/section-work.tsx` was rewritten as a beat-content-only Server Component. Renders `div.wrap > div.beat-grid` with:

- Left column: `<div className="reveal"><RailBox num="02" label="Selected work" /></div>`
- Right column: `h2.reveal` + `div.work-grid` of four `Photo` wrappers (alternating `from-left`/`from-right`) + `Link href="/portfolio"` with `SELECTED` kicker + Prometheus external `<a>` with `rel="noopener noreferrer"`.

Phase-17.3 invariants preserved per D-13:
- `/portfolio` link present with "Portfolio" text and "SELECTED" kicker.
- No `/projects` link.
- Prometheus external anchor carries `rel="noopener noreferrer"` (T-17.1-02).

### Task 3: Test suite modernization

`src/__tests__/home/section-building.test.tsx` was fully rewritten:
- Removed all BigList/SectionLabel mocks and Building/Writing/Doing assertions (stale HD-04 assertions, pre-existing failure).
- Added RailBox and Photo mocks (simple test doubles).
- Asserts: rail-box data-num="01", data-label="Building"; `.shadowed.slide.from-left` container present; Photo `data-dark="true"`; no big-list.

`src/__tests__/home/section-work.test.tsx` was updated:
- Added RailBox and Photo mocks for the new component structure.
- Removed Button and SectionLabel mocks (no longer needed; SectionLabel/Button are not imported by the reskinned component).
- All five SC-2/T-17.1-02 invariant assertions retained and passing.

Result: `npx vitest run` exits 0 with 11 tests passing across both files.

## Deviations from Plan

None - plan executed exactly as written.

The acceptance criteria interpretation notes:
- "div.rail.reveal wrapping RailBox" was implemented as `<div className="reveal"><RailBox /></div>` because RailBox (Plan 02 contract) already renders its own `.rail` div and has no `className` prop. The `.reveal` wrapper is structurally correct: ScrollReveals observes `.reveal` elements, and RailBox's inner `.rail > .rail-box` is self-contained.

## Known Stubs

- **Photo captions in work-grid** - hardcoded placeholder captions ("Project one · Product · 2025", etc.). Intentional per D-07 (real photography deferred to asset swap). Does not prevent the plan goal (2x2 grid structure and slide-in contract are complete).
- **RailBox 01 inline link target** - `href="#writing"` pointing at the writing section id. The `#writing` anchor will be wired by the orchestrator (Plan 08) which assigns `id="writing"` to the newsletter band.

## Threat Flags

| Flag | File | Description |
|------|------|-------------|
| threat_flag: outbound-link | section-work.tsx | Prometheus `target="_blank"` anchor preserved with `rel="noopener noreferrer"`. Asserted by T-17.1-02 test. No new surface. |

## Self-Check: PASSED
