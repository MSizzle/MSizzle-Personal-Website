---
phase: 17.4-photo-forward-homepage-restyle
plan: 09
type: execute
wave: 5
depends_on: ["17.4-08"]
files_modified: []
autonomous: false
requirements: [D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08, D-09, D-10, D-11, D-12, D-13]
must_haves:
  truths:
    - "D-01/D-02: An automated gate proves no cobalt hex remains on the home path and Hanken is wired; the project builds and / renders as a static route."
    - "D-08: The static layout holds with motion off / prefers-reduced-motion — verified both by the reduced-motion CSS guards and a human toggle check."
    - "All of D-01..D-13 are confirmed present by a human visual verification at localhost:3000 against sketch 010."
  artifacts: []
  key_links: []
---

<objective>
Final gate for the photo-forward restyle: an automated build/type/test + token/font/no-WebGL grep gate, plus a human visual verification at localhost:3000 against sketch-010 covering all D-01..D-13 and the D-08 reduced-motion proof. Follows the 17.1-04 precedent (autonomous:false human checkpoint).

Purpose: There is no visual-regression harness; the authoritative sign-off is an automated correctness gate plus a human eyes-on comparison to the locked sketch.
Output: a green automated gate and a recorded human verdict.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/17.4-photo-forward-homepage-restyle/17.4-CONTEXT.md
@.planning/sketches/010-structured-bands-carousel/index.html
@.planning/sketches/010-structured-bands-carousel/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated correctness gate (build + types + tests + token/font/no-WebGL greps)</name>
  <read_first>
    - .planning/phases/17.4-photo-forward-homepage-restyle/17.4-CONTEXT.md (D-01..D-13 to gate)
    - src/app/globals.css, src/app/layout.tsx (tokens + font to assert)
    - src/components/home/ (home path files to grep for cobalt / WebGL)
  </read_first>
  <action>
    Run the full automated gate and capture results in the SUMMARY. Commands: (a) npx tsc --noEmit exits 0; (b) npx vitest run exits 0 (home suites green — section-work invariants, rewritten section-building + explorative-homepage tests); (c) npm run build exits 0 and reports / as a Static (○) route; (d) cobalt-absence on the home path: grep -rE '#1a4fd6|#1740ad|#14328a|26,79,214' over src/components/home and src/app/globals.css filtering comment lines (use grep -v '^\s*/\*' where needed) returns zero; (e) Hanken wired: grep confirms Hanken_Grotesk in layout.tsx and --font-hanken bound to --font-display/--font-sans in globals.css; (f) no heavy scroll stack on the home render path: grep -rniE 'webgl|lenis|three|@react-three' over src/components/home returns zero, and grep confirms src/app/page.tsx has no "use client" and exports revalidate = false. Record every command + result. If any check fails, stop and report which D-decision it maps to.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run && npm run build 2>&1 | tee /tmp/174build.log | grep -qE '○|Static' && ! grep -rE '#1a4fd6|#1740ad|#14328a' src/components/home src/app/globals.css && grep -q 'Hanken_Grotesk' src/app/layout.tsx && grep -q 'font-display: var(--font-hanken)' src/app/globals.css && ! grep -rniE 'webgl|lenis|@react-three' src/components/home && grep -q 'revalidate = false' src/app/page.tsx && echo GATE_OK</automated>
  </verify>
  <acceptance_criteria>
    - tsc, vitest, and build all exit 0; build reports / as a Static route.
    - Zero cobalt hex across src/components/home and globals.css; Hanken wired in layout + globals.
    - Zero webgl/lenis/react-three references under src/components/home; page.tsx is static (revalidate=false, no "use client").
    - All results recorded in the SUMMARY, each mapped to its D-decision.
  </acceptance_criteria>
  <done>The automated gate is green and evidences D-01/D-02/D-08 (partial) + the static/no-WebGL home invariant.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human visual verification at localhost:3000 vs sketch 010 (all D-01..D-13)</name>
  <action>Start the dev server (npm run dev), open http://localhost:3000, and walk the how-to-verify checklist below against sketch 010, confirming each of D-01..D-13 and the D-08 reduced-motion proof. Record the verdict (and any per-decision discrepancies) in the SUMMARY. This task is a blocking human checkpoint: it pauses for the operator and is never auto-approved.</action>
  <what-built>
    The full photo-forward homepage restyle: Vermilion tokens + Hanken 800 (D-01/D-02), the marker-block hero with 44% portrait, status tag and pinned ticker (D-06), alternating light/dark bands with hard corners and auto-inverting rail boxes in the exact D-03 order, scroll slide-in photos with even black drop-shadow (D-07), the Things-I-Love photo marquee, the Monty Monthly carousel with arrows/dots/scroll-snap and Substack link-out (D-09), the sticky Subscribe mini-nav past the hero (D-10), the "as seen in" credibility strip (D-11), and the rich footer with the "Available for select work" tag (D-12). Velvet-rope IA preserved (D-13). Automated gate (Task 1) already green.
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` and open http://localhost:3000.
    2. Hero (D-06): "Create Order" sits in a solid Vermilion marker block hugging the glyphs; "from Chaos" is ink; a large ~44% portrait breathes; a black "Currently, building Prometheus" status tag shows; a black section-link marquee is pinned at the base of the hero viewport. Accent reads Vermilion #e5411f, type reads Hanken 800 (D-01/D-02).
    3. Bands (D-03/D-04/D-05): scroll top-to-bottom and confirm the order credibility (light) → Building (dark) → Work (light) → Things I Love (dark) → Writing (light) → footer (dark); corners are hard everywhere; each section's rail box (01/02/03) inverts correctly against its band.
    4. Photos (D-07): Building's wide photo and Work's 2x2 grid slide in from the sides as they enter view and settle with an even black drop-shadow (not an angled frame); the Things-I-Love marquee scrolls.
    5. Writing (D-09): the Monty Monthly carousel scroll-snaps; prev/next arrows work; dots track position; the subscribe card opens https://montymonthly.substack.com in a new tab; there is NO email field anywhere.
    6. Sticky nav (D-10): after scrolling past the hero, the sticky mini-nav slides down with a persistent Subscribe CTA and sits above the existing header.
    7. Footer (D-12) + IA (D-13): the footer shows "Available for select work" + Colophon/Navigate/Elsewhere; there is no contact page and no contact CTA button anywhere on the page.
    8. Reduced motion (D-08): enable OS "Reduce Motion" (or the dev toolbar motion toggle if present) and reload — all photos/reveals are visible and settled, the marquee/ken-burns/breathe stop, and the full layout stays intact and usable. Confirm nothing depends on cursor movement.
    9. (Optional) Sanity-check an interior page (e.g. /about, /writing) still renders acceptably given the site-wide Hanken body-font change (D-02).
  </how-to-verify>
  <acceptance_criteria>
    - Every D-01..D-13 item above is visually confirmed against sketch 010, or discrepancies are listed for gap closure.
    - The reduced-motion / motion-off state leaves the static layout fully intact (D-08).
  </acceptance_criteria>
  <verify>
    <human-check>Operator confirms at localhost:3000 that all D-01..D-13 items and the reduced-motion static-layout proof match sketch 010, or lists discrepancies by D-decision for gap closure.</human-check>
  </verify>
  <resume-signal>Type "approved" to sign off the restyle, or describe the discrepancies (which D-decision) to route into gap closure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| verification only | No code changes; gate + human review |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17.4-09 | Repudiation | sign-off provenance | mitigate | Human verdict recorded in the SUMMARY with the reviewed commit; automated gate results attached. |
</threat_model>

<verification>
- Automated gate green (Task 1); human verdict recorded (Task 2).
- Any discrepancy is mapped to a D-decision for gap closure.
</verification>

<success_criteria>
The restyle passes the automated correctness gate and a human visual sign-off against sketch 010 covering all of D-01..D-13, including the D-08 reduced-motion proof.
</success_criteria>

<output>
Create `.planning/phases/17.4-photo-forward-homepage-restyle/17.4-09-SUMMARY.md` when done, recording the gate results and the human verdict.
</output>
