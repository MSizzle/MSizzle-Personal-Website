---
phase: "17.4-photo-forward-homepage-restyle"
plan: "08"
subsystem: "home-orchestrator"
tags: ["orchestrator", "server-component", "band-sequence", "islands", "tests", "sketch-010"]
dependency_graph:
  requires:
    - "17.4-01 (CSS: .band, .band-dark, .beat, band inversion mechanism)"
    - "17.4-02 (CredibilityStrip Server Component)"
    - "17.4-03 (ScrollReveals headless IO island)"
    - "17.4-04 (StickyNav scroll-triggered island)"
    - "17.4-05 (Hero Server Component with own light band)"
    - "17.4-06 (SectionBuilding, SectionWork band-agnostic beat components)"
    - "17.4-07 (SectionLoves, SectionNewsletter, SectionFooter band-agnostic beat components)"
  provides:
    - "ExplorativeHomepage: complete sketch-010 band-sequence orchestrator (D-03)"
    - "StickyNav and ScrollReveals mounted as fixed islands (D-08, D-10)"
    - "All four anchor ids: #building #work #loves #writing wired for nav/ticker/footer links"
    - "Rewritten explorative-homepage test: 6 passing assertions on new band structure"
  affects:
    - "page.tsx (unchanged: static Server Component, revalidate=false, Person JSON-LD)"
    - "Homepage / — full sketch-010 band sequence now live"
tech_stack:
  added: []
  patterns:
    - "Server Component orchestrator composing all child islands and beats into band wrappers"
    - "Fixed overlay islands (StickyNav, ScrollReveals) mounted first in document order"
    - "Band ownership pattern: orchestrator owns section.band/band-dark + id; sections are band-agnostic"
key_files:
  modified:
    - "src/components/home/explorative-homepage.tsx — full rewrite: sketch-010 band sequence, islands, Server Component shell"
    - "src/__tests__/home/explorative-homepage.test.tsx — full rewrite: stale WebGL gate dropped, new band-id assertions, 6 tests passing"
decisions:
  - "CredibilityStrip wrapped in .proof container inside light band to match sketch proof-pad pattern"
  - "StickyNav and ScrollReveals placed first in JSX (before Hero) so they are fixed overlays above all bands"
  - "SectionFooter band wrapper omits beat padding class (footer.site handles its own padding per Plan 07 CSS)"
  - "Test stubs return non-null elements (div/span) for StickyNav and ScrollReveals so data-testid assertions work"
  - "Test uses per-test dynamic import to avoid vi.mock hoisting vs module isolation issues"
metrics:
  duration: "~6m"
  completed: "2026-07-04"
  tasks_completed: 2
  files_modified: 2
---

# Phase 17.4 Plan 08: Orchestrator Band Sequence + Test Rewrite Summary

ExplorativeHomepage rewritten to the exact sketch-010 D-03 band order (hero light -> credibility light -> building dark -> work light -> loves dark -> writing light -> footer dark) with StickyNav (D-10) and ScrollReveals (D-08) islands mounted; stale WebGL test replaced by 6 passing band-structure assertions.

## What Was Built

### Task 1: Orchestrator rewrite

`src/components/home/explorative-homepage.tsx` was fully rewritten as a Server Component (no `"use client"`, no GL imports) assembling the complete sketch-010 page in this exact order:

1. **StickyNav** (fixed overlay, z-9000 from Plan 01 CSS, mounted first) — satisfies D-10.
2. **ScrollReveals** (headless IO island, mounted first alongside StickyNav) — satisfies D-08.
3. **Hero** — owns its own `section.band.hero-band` wrapper with pinned ticker (Plan 05).
4. **Credibility strip band** — `section.band.beat` wrapping `div.proof > CredibilityStrip` — light band directly under the hero (D-11).
5. **Building band** — `section.band.band-dark.beat#building` wrapping `SectionBuilding` — dark band (D-03).
6. **Work band** — `section.band.beat#work` wrapping `SectionWork` — light band (D-03).
7. **Loves band** — `section.band.band-dark.beat#loves` wrapping `SectionLoves` — dark band (D-03).
8. **Writing band** — `section.band.beat#writing` wrapping `SectionNewsletter` — light band (D-03).
9. **Footer band** — `section.band.band-dark` wrapping `SectionFooter` — dark band (D-12).

All four anchor ids (`#building`, `#work`, `#loves`, `#writing`) are on the correct elements, satisfying sticky-nav anchor links, hero-ticker links, and footer navigate links from downstream plans.

`page.tsx` unchanged: `export const revalidate = false`, `<JsonLd data={buildPersonSchema()} />`, `<ExplorativeHomepage />`.

### Task 2: Test rewrite

`src/__tests__/home/explorative-homepage.test.tsx` was fully rewritten:

- Dropped all stale assertions: `canvas-loader`, `fallback-poster`, `pointer:coarse`, `WebGL2` gate (these tested the old Phase 17.1 orchestrator's device-detection logic which no longer exists; pre-existing failures TD-03/HD-05).
- Mocked Hero, CredibilityStrip, StickyNav, ScrollReveals, SectionBuilding, SectionWork, SectionLoves, SectionNewsletter, SectionFooter as lightweight stubs.
- Six new assertions:
  1. StickyNav island mounts (`data-testid="sticky-nav"` present).
  2. ScrollReveals island mounts (`data-testid="scroll-reveals"` present).
  3. `#building` band carries `band-dark` class (dark band assertion).
  4. `#work` band does NOT carry `band-dark` class (light band assertion).
  5. `#loves` band carries `band-dark` class (dark band assertion).
  6. `#writing` band does NOT carry `band-dark` class (light band assertion).
- Suite result: 1 file, 6 tests, all passing.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Removed forbidden strings from orchestrator comment**
- **Found during:** Task 1 verification
- **Issue:** The acceptance criteria grep `! grep -qiE 'canvas|webgl|lenis|use client'` matched a JSDoc comment line: `* No canvas/WebGL/Lenis on the home render path (D-08).` The check scans the entire file including comments.
- **Fix:** Replaced with `* Motion is scroll-triggered + ambient only; no GL or heavy scroll stack (D-08).`
- **Files modified:** `src/components/home/explorative-homepage.tsx`
- **Commit:** a1131b8

## Known Stubs

None. The orchestrator is a pure structural composition component; it contains no data sources or content that could be stubbed.

## Threat Flags

No new threat surfaces introduced. Confirmed:
- No `"use client"` in orchestrator (T-17.4-08: client-hook-elevation mitigated).
- All four anchor ids (#building/#work/#loves/#writing) present on band wrappers (T-17.4-08b: missing-anchors mitigated).

## Self-Check: PASSED

Files:
- `src/components/home/explorative-homepage.tsx` — FOUND
- `src/__tests__/home/explorative-homepage.test.tsx` — FOUND

Commits:
- a1131b8: feat(17.4-08): rewrite orchestrator to sketch-010 band sequence (D-03/D-08/D-10/D-13)
- 28a200c: test(17.4-08): rewrite explorative-homepage test to new band structure

Grep verifications:
- `band-dark` present: PASS
- `id="building"` present: PASS
- `id="writing"` present: PASS
- StickyNav import: PASS
- ScrollReveals import: PASS
- No forbidden strings (canvas/webgl/lenis/use client): PASS
- `revalidate = false` in page.tsx: PASS
- TypeScript: pre-existing errors only (uses.test.tsx TS2349, robots.test.ts TS2339 — documented as out-of-scope since Plan 01)
- vitest run explorative-homepage.test.tsx: 6/6 PASS
