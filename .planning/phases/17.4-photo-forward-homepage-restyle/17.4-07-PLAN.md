---
phase: 17.4-photo-forward-homepage-restyle
plan: 07
type: execute
wave: 3
depends_on: ["17.4-01", "17.4-02", "17.4-03", "17.4-04"]
files_modified:
  - src/components/home/section-loves.tsx
  - src/components/home/section-newsletter.tsx
  - src/components/home/section-footer.tsx
autonomous: true
requirements: [D-03, D-05, D-09, D-12, D-13]
must_haves:
  truths:
    - "D-05: SectionLoves leads with a rail box (index 03 · Things I love) and a full-bleed PhotoMarquee of placeholder photo cards."
    - "D-09: The Writing section (section-newsletter) renders the Monty Monthly carousel (issue cards + subscribe card) with a 'Monty Monthly' eyebrow, linking out to Substack with no on-site email capture."
    - "D-12: SectionFooter carries an 'Available for select work' black status tag plus Colophon / Navigate / Elsewhere columns."
    - "D-13: No contact page or contact CTA button is introduced; Monty Monthly / Substack remains the primary woven way-in; external links keep rel=noopener noreferrer."
  artifacts:
    - path: "src/components/home/section-loves.tsx"
      provides: "Loves beat: rail-box 03 + headline + full-bleed PhotoMarquee"
      contains: "PhotoMarquee"
    - path: "src/components/home/section-newsletter.tsx"
      provides: "Writing beat: Monty Monthly eyebrow + MontyMonthlyCarousel"
      contains: "MontyMonthlyCarousel"
    - path: "src/components/home/section-footer.tsx"
      provides: "Rich footer: avail tag + colophon/navigate/elsewhere columns"
      contains: "avail"
  key_links:
    - from: "section-newsletter.tsx"
      to: "MontyMonthlyCarousel"
      via: "import + issues array"
      pattern: "MontyMonthlyCarousel"
    - from: "section-loves.tsx"
      to: "PhotoMarquee"
      via: "import"
      pattern: "PhotoMarquee"
---

<objective>
Reskin the remaining three beats to sketch-010: SectionLoves (dark band, rail 03, full-bleed PhotoMarquee), the Writing section / section-newsletter (light band, Monty Monthly carousel, D-09), and SectionFooter (dark band, "Available for select work" avail tag + Colophon/Navigate/Elsewhere columns, D-12). Velvet-rope preserved throughout (D-13).

Purpose: These three beats close out the band sequence and depend on the PhotoMarquee (Plan 03) and MontyMonthlyCarousel (Plan 04) islands; grouping them keeps the island wiring in one plan.
Output: reskinned section-loves.tsx, section-newsletter.tsx, section-footer.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/17.4-photo-forward-homepage-restyle/17.4-CONTEXT.md
@.planning/phases/17.4-photo-forward-homepage-restyle/17.4-PATTERNS.md

<interfaces>
<!-- Sections return beat CONTENT; orchestrator (Plan 08) supplies the band section + id. -->
RailBox({ num, label }) — Plan 02.  PhotoMarquee({ items }) — Plan 03.  MontyMonthlyCarousel({ issues }) — Plan 04.
MontyMonthlyCarousel issues item: { num, date, title, excerpt, href? }. Existing issue copy lives in current section-newsletter.tsx.
Footer must stay the ONLY footer on / (conditional-footer.tsx returns null on "/"). Keep external anchors (Substack/GitHub/LinkedIn/X, mailto) with rel=noopener noreferrer.
Keep the export name SectionNewsletter (orchestrator imports it) even though its content is now the "Writing / Monty Monthly" surface.
Loves marquee items (sketch 407): "A place I go", "A tool I trust", "Off the clock", "Reading now".
Footer avail tag + columns: sketch 334-353 (Colophon / Navigate / Elsewhere).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reskin SectionLoves (dark beat, rail 03, PhotoMarquee) — D-03/D-05</name>
  <read_first>
    - src/components/home/section-loves.tsx (current file)
    - .planning/sketches/010-structured-bands-carousel/index.html (#loves markup 307-313: rail-box 03, h2, full-bleed .marquee outside .wrap)
    - src/components/home/rail-box.tsx, src/components/home/photo-marquee.tsx (Plan 02/03 contracts)
  </read_first>
  <action>
    Rewrite src/components/home/section-loves.tsx as a Server Component exporting SectionLoves() returning beat CONTENT: a div.wrap > div.beat-grid with a left RailBox num="03" label="Things I love" in div.rail.reveal and a right column h2.reveal ("The stuff that keeps me curious. Always running."), FOLLOWED BY a full-bleed div.marquee wrapper (outside .wrap) rendering PhotoMarquee items={["A place I go","A tool I trust","Off the clock","Reading now"]}. Note PhotoMarquee is a client island — importing it into this Server Component is fine (it becomes a client boundary). Band-agnostic token classes only (dark band inverts automatically). No em dashes. Remove the old /uses BigList rows and Button (replaced by the marquee); if a "see the full list" affordance is desired, keep a single subtle /uses link, not a prominent CTA (velvet-rope).
  </action>
  <verify>
    <automated>grep -q 'PhotoMarquee' src/components/home/section-loves.tsx && grep -q 'RailBox' src/components/home/section-loves.tsx && ! grep -q '—' src/components/home/section-loves.tsx && ! grep -q 'use client' src/components/home/section-loves.tsx && npx tsc --noEmit && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - section-loves.tsx renders RailBox num="03" label="Things I love" + a full-bleed PhotoMarquee with the four love items.
    - Server Component (no "use client"); band-agnostic token classes; no em dash.
    - npx tsc --noEmit exits 0.
  </acceptance_criteria>
  <done>Loves beat matches the sketch: rail 03 + full-bleed photo marquee; typecheck clean.</done>
</task>

<task type="auto">
  <name>Task 2: Reskin section-newsletter → Writing carousel (light beat) — D-09/D-13</name>
  <read_first>
    - src/components/home/section-newsletter.tsx (current file — issue array + Substack CTA to carry over)
    - .planning/sketches/010-structured-bands-carousel/index.html (#writing markup 315-331: mm-head eyebrow + h2 + mm-ctrls + mm-track; issues data 421-426)
    - src/components/home/monty-monthly-carousel.tsx (Plan 04 contract)
  </read_first>
  <action>
    Rewrite src/components/home/section-newsletter.tsx as a Server Component (keep the export name SectionNewsletter) returning beat CONTENT: a div.wrap with a div.mm-head containing an .eyebrow "Monty Monthly" and an h2.reveal ("Notes on building, one issue at a time."), then render MontyMonthlyCarousel with an issues array (map the existing FEATURED essays / add issue metadata: num, date, title, excerpt — reuse current copy, keep 4 issues per sketch). D-09/D-13: the carousel's subscribe card and read links point to https://montymonthly.substack.com; there is NO on-site email input/form here. Remove the old NewsletterCarousel + separate essay-teaser markup + big display heading (the carousel now carries the writing surface). No em dashes; sole identity "Founder of Prometheus"; no location.
  </action>
  <verify>
    <automated>grep -q 'MontyMonthlyCarousel' src/components/home/section-newsletter.tsx && grep -q 'Monty Monthly' src/components/home/section-newsletter.tsx && ! grep -qiE '<input|<form' src/components/home/section-newsletter.tsx && ! grep -q '—' src/components/home/section-newsletter.tsx && npx tsc --noEmit && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - section-newsletter.tsx exports SectionNewsletter; renders a "Monty Monthly" eyebrow + MontyMonthlyCarousel fed an issues array.
    - No email input/form anywhere (D-09 link-out only); no em dash.
    - npx tsc --noEmit exits 0.
  </acceptance_criteria>
  <done>Writing beat is the Monty Monthly carousel with Substack link-out and no email capture; typecheck clean.</done>
</task>

<task type="auto">
  <name>Task 3: Reskin SectionFooter (dark beat, avail tag, three columns) — D-12/D-13</name>
  <read_first>
    - src/components/home/section-footer.tsx (current file — column links + rel=noopener to carry over)
    - .planning/sketches/010-structured-bands-carousel/index.html (footer markup 334-353: .avail tag, .selective line, .foot-grid Colophon/Navigate/Elsewhere)
    - src/components/layout/conditional-footer.tsx behavior (this stays the only footer on /)
  </read_first>
  <action>
    Rewrite src/components/home/section-footer.tsx as a Server Component exporting SectionFooter() returning beat CONTENT: a div.wrap with an .avail black status tag (a .dot + "Available for select work", D-12), a .selective line ("I take on very little. If it is the right thing, reach out." with the accent-bold word), and a .foot-grid of three .foot-col columns — Colophon (a short colophon paragraph mentioning Notion + Vercel and a "© 2026 Monty Singer" footmark), Navigate (Building #building, Work #work, Things I love #loves, Writing #writing, Uses /uses), and Elsewhere (Email mailto:monty@prometheus.today, X/Twitter, LinkedIn, Monty Monthly → Substack). Keep all external anchors target="_blank" rel="noopener noreferrer". D-13: no "Contact" heading, no contact button; the reach-out is woven into the .selective line + mailto. No em dashes; keep identity "Founder of Prometheus".
  </action>
  <verify>
    <automated>grep -q 'avail' src/components/home/section-footer.tsx && grep -q 'Available for select work' src/components/home/section-footer.tsx && grep -q 'foot-grid' src/components/home/section-footer.tsx && grep -c 'rel="noopener noreferrer"' src/components/home/section-footer.tsx | grep -qvx 0 && ! grep -q '—' src/components/home/section-footer.tsx && npx tsc --noEmit && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - section-footer.tsx renders an .avail "Available for select work" tag, a .selective line, and a .foot-grid with Colophon / Navigate / Elsewhere columns.
    - All external anchors keep rel="noopener noreferrer"; no "Contact" heading/button (D-13); no em dash.
    - npx tsc --noEmit exits 0.
  </acceptance_criteria>
  <done>Footer matches the sketch (avail tag + three columns) and preserves velvet-rope + external-link safety; typecheck clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| outbound links → Substack / GitHub / LinkedIn / X | External navigation targets (reverse-tabnabbing surface) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17.4-07 | Tampering (reverse tabnabbing) | footer + subscribe external anchors | mitigate | rel="noopener noreferrer" on every external anchor (grep-verified in footer). |
| T-17.4-07b | Info Disclosure | contact surface | mitigate | Only a mailto + Substack link-out; no form, no PII capture (D-13). |
</threat_model>

<verification>
- Loves uses PhotoMarquee; Writing uses MontyMonthlyCarousel; Footer has avail tag + three columns.
- No email form in the Writing beat; external anchors carry rel=noopener noreferrer; no em dashes.
</verification>

<success_criteria>
Loves (D-05 marquee), Writing (D-09 carousel), and Footer (D-12 avail + columns) beats match sketch-010 and preserve the velvet-rope model (D-13).
</success_criteria>

<output>
Create `.planning/phases/17.4-photo-forward-homepage-restyle/17.4-07-SUMMARY.md` when done.
</output>
