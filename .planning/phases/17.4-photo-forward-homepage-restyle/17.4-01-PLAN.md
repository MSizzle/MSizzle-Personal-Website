---
phase: 17.4-photo-forward-homepage-restyle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/app/layout.tsx
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-05, D-07, D-08]
must_haves:
  truths:
    - "D-01: The home path renders the Vermilion accent #e5411f (no cobalt #1a4fd6 remains anywhere in globals.css)."
    - "D-02: Display and body type resolve to Hanken Grotesk (weight 800 available for display/sig) via a next/font instance."
    - "D-03: A dark-band mechanism exists so token classes (text-text/bg-bg/border-border) auto-invert inside a dark band without per-component color hardcoding."
    - "D-04: Hard-corner radius tokens (0) back the photo/card/box/button treatments."
    - "D-05: A .rail-box rule renders an auto-inverting high-contrast box (background:var(--color-text); color:var(--color-bg))."
    - "D-07: Photo placeholder, .slide/.shadowed and ken-burns/breathe classes exist to support scroll slide-in photos with an even black drop-shadow settle."
    - "D-08: prefers-reduced-motion (and a body.no-motion escape hatch) forces .reveal/.slide visible and kills ambient keyframes, leaving the static layout intact; no WebGL/Lenis added."
  artifacts:
    - path: "src/app/globals.css"
      provides: "Vermilion tokens, Hanken font map, band-dark inversion, photo/rail/marquee/carousel/sticky/motion CSS, reduced-motion block"
      contains: "band-dark"
    - path: "src/app/layout.tsx"
      provides: "Hanken_Grotesk next/font instance with --font-hanken variable applied to <html>"
      contains: "Hanken_Grotesk"
  key_links:
    - from: "src/app/globals.css --font-display/--font-sans"
      to: "--font-hanken"
      via: "next/font CSS variable"
      pattern: "font-display:\\s*var\\(--font-hanken\\)"
    - from: ".band-dark"
      to: "--color-text/--color-bg/--color-border overrides"
      via: "local custom-property re-declaration"
      pattern: "band-dark"
---

<objective>
Establish the Photo-Forward design substrate: re-theme the accent to Vermilion (D-01), wire Hanken Grotesk 800 (D-02), and author every new CSS primitive the sketch-010 homepage needs — the dark-band inversion mechanism (D-03, the linchpin), hard-corner radius (D-04), the auto-inverting rail box (D-05), the photo/slide/shadow/ken-burns motion system (D-07), and the reduced-motion escape hatches (D-08).

Purpose: This is the foundation every downstream plan depends on. Sections and islands emit these class names; if the band-inversion + token substrate is correct here, each reskin is cheap and stays token-driven.
Output: Re-themed `globals.css` (tokens + fonts + full class library) and a Hanken font instance in `layout.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17.4-photo-forward-homepage-restyle/17.4-CONTEXT.md
@.planning/phases/17.4-photo-forward-homepage-restyle/17.4-PATTERNS.md

<interfaces>
<!-- Tailwind v4 @theme inline emits utilities that read the token var at use-site:
     .text-text{color:var(--color-text)}, .bg-bg{background:var(--color-bg)}, .border-border{...}.
     Because of `inline`, a local re-declaration of --color-text on a wrapper cascades to children.
     This is why .band-dark can invert every token-class child without touching the child. -->
Current cobalt tuple (globals.css lines 21-24, 30-32, 36) → Vermilion target:
  --accent #1a4fd6 → #e5411f ; --accent-hover #1740ad → #c8381a ;
  --accent-deep #14328a → #a52d13 ; --accent-glow rgba(26,79,214,.35) → rgba(229,65,31,.32) ;
  --color-accent/-hover/-deep mirror the same ; --blob-rim #1a4fd6 → #e5411f
Current font map (globals.css lines 49-51): --font-display/--font-sans = var(--font-space-grotesk)
`.sig-shadow` (line 90) already = var(--accent) → auto-vermilion after the swap; no literal edit.
Verified: zero cobalt hex outside globals.css; --font-space-grotesk referenced only in globals.css.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-theme accent cobalt → Vermilion (D-01)</name>
  <read_first>
    - src/app/globals.css (the @theme inline block, lines 20-36; :root sig vars 88-91)
    - .planning/sketches/010-structured-bands-carousel/index.html (accent tuple, script line 383)
    - .planning/phases/17.4-photo-forward-homepage-restyle/17.4-PATTERNS.md (globals.css assignment, lines 40-69)
  </read_first>
  <action>
    In src/app/globals.css replace the cobalt family with the Vermilion tuple ['#e5411f','#c8381a','#a52d13','rgba(229,65,31,.32)']:
    set `--accent`=#e5411f, `--accent-hover`=#c8381a, `--accent-deep`=#a52d13, `--accent-glow`=rgba(229,65,31,0.32);
    mirror the `--color-accent`=#e5411f, `--color-accent-hover`=#c8381a, `--color-accent-deep`=#a52d13 aliases;
    set `--blob-rim`=#e5411f. Do NOT touch `--color-bg` (#faf9f7) or `--color-text` (#171717) — near-white paper + near-black ink are RETAINED per D-01. Leave `--sig-shadow` referencing var(--accent) (it re-themes automatically). Update the code comments that say "cobalt" to "vermilion".
  </action>
  <verify>
    <automated>grep -c '#1a4fd6\|#1740ad\|#14328a\|26,79,214' src/app/globals.css | grep -qx 0 && grep -q '#e5411f' src/app/globals.css && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E '#1a4fd6|#1740ad|#14328a|26,79,214' src/app/globals.css` returns nothing (zero cobalt).
    - globals.css contains `--accent: #e5411f`, `--accent-deep: #a52d13`, `--color-accent: #e5411f`, `--blob-rim: #e5411f`.
    - `--color-bg` still `#faf9f7` and `--color-text` still `#171717` (base retained).
  </acceptance_criteria>
  <done>All accent tokens are the Vermilion family; no cobalt hex remains in globals.css; paper/ink base unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Hanken Grotesk 800 and repoint font tokens (D-02)</name>
  <read_first>
    - src/app/layout.tsx (Space_Grotesk/JetBrains_Mono instances, lines 2, 13-25; html className line 67)
    - src/app/globals.css (font map lines 48-52; body font-family line 59)
    - .planning/phases/17.4-photo-forward-homepage-restyle/17.4-PATTERNS.md (layout.tsx assignment, lines 86-110)
    - .planning/sketches/010-structured-bands-carousel/index.html (font :root lines 12-16, sig-weight 800)
  </read_first>
  <action>
    In src/app/layout.tsx add a `Hanken_Grotesk` import from next/font/google and a new instance: variable `--font-hanken`, subsets ["latin"], weight ["400","500","700","800"] (800 is the display/sig weight), display "swap". Append `${hanken.variable}` to the `<html className>` string. In src/app/globals.css repoint `--font-display: var(--font-hanken)` AND `--font-sans: var(--font-hanken)` (sketch uses Hanken for display + body + labels; keep `--font-mono` = JetBrains). Note in a comment that repointing --font-sans changes the body font site-wide per D-02 (accepted; verified at the Phase gate). Then remove the now-unused `Space_Grotesk` import and instance from layout.tsx ONLY if the grep guard in acceptance passes (no `font-space-grotesk`/`--font-space-grotesk` references remain outside the lines you are editing); otherwise leave the instance in place and note it in the SUMMARY.
  </action>
  <verify>
    <automated>grep -q 'Hanken_Grotesk' src/app/layout.tsx && grep -q -- '--font-hanken' src/app/layout.tsx && grep -q 'font-display: var(--font-hanken)' src/app/globals.css && grep -q 'font-sans:    var(--font-hanken)\|font-sans: var(--font-hanken)' src/app/globals.css && npx tsc --noEmit && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - layout.tsx contains `Hanken_Grotesk` with `weight` array including `"800"` and `variable: "--font-hanken"`; `<html>` className includes `hanken.variable`.
    - globals.css: `--font-display` and `--font-sans` both resolve to `var(--font-hanken)`; `--font-mono` unchanged.
    - `grep -rn 'font-space-grotesk\|--font-space-grotesk' src` returns only lines you intentionally kept (ideally zero after removal).
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Hanken 800 loads via next/font; display + body tokens resolve to Hanken; Space Grotesk removed (or documented as retained); typecheck clean.</done>
</task>

<task type="auto">
  <name>Task 3: Author the sketch-010 CSS system (bands, rail, photo, motion, reduced-motion)</name>
  <read_first>
    - .planning/sketches/010-structured-bands-carousel/index.html (the entire &lt;style&gt; block, lines 11-236 — the authoritative CSS source)
    - src/app/globals.css (existing @keyframes scroll line 94; append new rules after it)
    - .planning/phases/17.4-photo-forward-homepage-restyle/17.4-PATTERNS.md (Shared Patterns: band inversion lines 241-255; New Pattern 4 lines 228-235)
    - src/components/v3/reveal.tsx and src/components/v3/marquee.tsx (reduced-motion precedent)
  </read_first>
  <action>
    Append plain-CSS rules to src/app/globals.css (authored CSS, not @theme utilities, so Tailwind will not purge them), porting from the sketch &lt;style&gt;. Add hard-corner radius vars `--radius-sm:0; --radius-md:0` (D-04). Add the DARK-BAND MECHANISM (D-03, linchpin): a `.band-dark` class that locally re-declares `--color-text:#f5f3ee; --color-text-dim:rgba(245,243,238,.76); --color-text-muted:rgba(245,243,238,.54); --color-border:rgba(255,255,255,.15); --color-border-strong:rgba(255,255,255,.32); background:#141416;` so token-class children auto-invert (sketch lines 34-37). Add `.rail-box`/`.rail-box .num`/`.rail-box .lbl` (D-05, auto-invert via var(--color-text)/var(--color-bg), .num uses var(--accent), sketch 94-97). Add the photo placeholder system `.photo`, `.photo .img`, `.photo.dark .img`, `.photo .icon`, `.photo .cap` (sketch 76-88). Add keyframes `drift`(sketch 73), `pulse`(103), `kb`(178), `breathe`(180); reuse the existing `@keyframes scroll` for marquee `slide` (both do translateX(-50%)) or alias a `slide` keyframe to match. Add motion classes `.reveal`/`.reveal.in`, `.slide`/`.slide.from-right`/`.slide.in`, `.shadowed .photo`/`.shadowed.in .photo` (even black drop-shadow settle, D-07, sketch 169-176), `.kenburns`, `.breathe`. Add supporting component CSS the later plans emit: `.eyebrow`(49-51), `.marker`/`h1.sig .hw` marker block (60-61), `.statustag`+`.dot`(99-102), `.hero-ticker`+`.track`+`.tick-link`(105-110), `.marquee`+`.track`(118-121), the Monty Monthly `.mm-*` carousel classes (123-150), `.stickynav`+`.stickynav.show`+`.inner`/`.cta`(195-206, z-index 9000), `.proof`+`.m`(208-213), `.avail`+`.foot-grid`+`.foot-col`(215-224). Finally add the D-08 reduced-motion guards: `body.no-motion .reveal,.slide{opacity:1;transform:none}` + kill `.kenburns/.breathe/.blob/.marquee .track` animations + force the settled `.shadowed .photo` box-shadow, AND the identical `@media(prefers-reduced-motion:reduce)` block (sketch 182-188). Do NOT add WebGL or Lenis CSS. Preserve the existing `.sig`/`.sig-out`/`.prose` rules.
  </action>
  <verify>
    <automated>grep -q 'band-dark' src/app/globals.css && grep -q 'rail-box' src/app/globals.css && grep -q 'prefers-reduced-motion' src/app/globals.css && grep -q 'shadowed' src/app/globals.css && grep -q 'stickynav' src/app/globals.css && npm run build 2>&1 | grep -qE '○|Static|Compiled|success' && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - globals.css contains rules for `.band-dark`, `.rail-box`, `.photo`, `.reveal`, `.slide`, `.shadowed`, `.kenburns`, `.breathe`, `.hero-ticker`, `.marquee`, `.mm-track`, `.stickynav`, `.proof`, `.avail`, `.foot-grid`, `.statustag`, `.eyebrow`, and `h1.sig .hw`.
    - `--radius-sm` and `--radius-md` are `0` (D-04).
    - A `@media (prefers-reduced-motion: reduce)` block AND a `body.no-motion` block both force `.reveal`/`.slide` to `opacity:1; transform:none` and disable ambient keyframes (D-08).
    - `grep -i 'webgl\|lenis' src/app/globals.css` returns nothing new on the home CSS.
    - `npm run build` compiles without CSS errors.
  </acceptance_criteria>
  <done>Every class the sketch-010 homepage references exists in globals.css; band inversion and reduced-motion guards are in place; build compiles.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| build-time CSS/font | No runtime user input; static asset generation only |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17.4-01 | Tampering | next/font Hanken fetch | accept | next/font self-hosts Google fonts at build; no runtime third-party request. No packages installed (all deps present). |
| T-17.4-01b | Denial | reduced-motion/no-motion path | mitigate | D-08 guards guarantee static layout usable with animation disabled. |
</threat_model>

<verification>
- `npx tsc --noEmit` and `npm run build` both succeed.
- No cobalt hex anywhere in globals.css; Vermilion present.
- Band-dark + reduced-motion mechanisms present and grep-verifiable.
</verification>

<success_criteria>
Vermilion tokens (D-01), Hanken 800 (D-02), and the full sketch-010 CSS class library (band inversion D-03, hard corners D-04, rail box D-05, photo/motion D-07, reduced-motion D-08) are in place and the project builds.
</success_criteria>

<output>
Create `.planning/phases/17.4-photo-forward-homepage-restyle/17.4-01-SUMMARY.md` when done.
</output>
