---
phase: 17.4-photo-forward-homepage-restyle
plan: 05
type: execute
wave: 3
depends_on: ["17.4-01", "17.4-02"]
files_modified:
  - src/components/home/hero.tsx
autonomous: true
requirements: [D-06, D-08, D-13]
must_haves:
  truths:
    - "D-06: The hero renders a two-line headline — 'Create Order' inside a solid Vermilion marker block hugging the glyphs, 'from Chaos' in ink — beside a large ~44%-width portrait column (breathing placeholder)."
    - "D-06: The hero carries a 'Founder of Prometheus' eyebrow, a black status tag ('Currently — building Prometheus'), and a full-bleed black section-link marquee strip pinned to the base of the hero viewport."
    - "D-13: The hero's only engagement is a woven 'Join Monty Monthly' way-in link (no CTA button, no contact link)."
    - "D-08: Hero motion (blob drift, portrait breathe/ken-burns, marquee) is ambient CSS only, killed under prefers-reduced-motion with the static layout intact; no cursor dependence."
  artifacts:
    - path: "src/components/home/hero.tsx"
      provides: "Marker-block hero: eyebrow, sig headline w/ marker, subtitle, way-in, status tag, portrait, pinned hero-ticker"
      contains: "hw"
  key_links:
    - from: "hero.tsx portrait"
      to: "Photo primitive (breathe)"
      via: "import"
      pattern: "Photo"
    - from: "hero.tsx way-in"
      to: "Monty Monthly (#writing)"
      via: "anchor"
      pattern: "Monty Monthly"
---

<objective>
Build the sketch-010 marker-block hero (D-06): a structured split with the Vermilion-marker "Create Order" / ink "from Chaos" headline, a ~44% breathing portrait column, a black status tag, and a full-bleed black section-link marquee pinned to the base of the hero viewport. Engagement stays a single woven Monty Monthly way-in (D-13); all motion is ambient CSS, reduced-motion-safe (D-08).

Purpose: The hero is the centerpiece of the restyle and the LCP surface — it stays a Server Component (CSS-animated children only, no JS) so `/` remains static.
Output: `src/components/home/hero.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/17.4-photo-forward-homepage-restyle/17.4-CONTEXT.md
@.planning/phases/17.4-photo-forward-homepage-restyle/17.4-PATTERNS.md

<interfaces>
<!-- Consumed by the orchestrator (Plan 08) as the first (light) band. -->
Hero(): Server Component (no "use client"). Structure per sketch lines 247-266:
  section.hero-band (min-h 100dvh, flex column) > .wrap(flex column) > .hero(grid 1fr / minmax(320px,44%)) +
  a pinned .hero-ticker(.track) of section links at the band base.
  .hero copy col: .eyebrow "Founder of Prometheus"; h1.sig with span.hl>span.hw "Create Order" (marker) + span.hl "from Chaos";
  .subtitle; .wayin "I write about building monthly. <a href='#writing'>Join Monty Monthly →</a>"; .statustag "Currently — building Prometheus".
  .hero photo col: <Photo breathe caption="…" /> filling the column; a .blob ambient glow div.
Photo primitive: src/components/home/photo.tsx (Plan 02).
IMPORTANT: do NOT render an in-hero site nav — the global EditorialHeader (layout.tsx) owns site nav (out of scope per CONTEXT). The sketch's nav.site row is a sketch-only artifact.
Ticker links (sketch 413): Building #building, Work #work, Writing #writing, Things I Love #loves, Uses /uses.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Marker-block hero with pinned ticker (D-06, D-08, D-13)</name>
  <read_first>
    - .planning/sketches/010-structured-bands-carousel/index.html (hero markup 247-266; .hero/.col-copy/.col-photo CSS 53-73; h1.sig .hw marker 57-61; .statustag 99-103; .hero-ticker 105-110; ticker links JS 413)
    - src/components/home/photo.tsx (Photo primitive contract from Plan 02)
    - src/components/home/explorative-homepage.tsx (current text-forward hero being replaced — copy/tone reference: "Create Order from Chaos", Founder of Prometheus, Join Monty Monthly)
    - .planning/phases/17.4-photo-forward-homepage-restyle/17.4-PATTERNS.md (hero row of File Classification, line 29)
  </read_first>
  <action>
    Create src/components/home/hero.tsx as a Server Component exporting `Hero()`. Render `<section className="band hero-band">` sized to `min-h-[100dvh]` as a flex column. Inside a `.wrap` flex column: the `.hero` grid (`grid-template-columns:1fr minmax(320px,44%)`) with a copy column and a photo column. Copy column: `<div className="eyebrow">Founder of Prometheus</div>`; `<h1 className="sig"><span className="hl"><span className="hw">Create Order</span></span><span className="hl">from Chaos</span></h1>` (the `.hw` marker block from Plan 01 gives the Vermilion glyph-hugging highlight, D-06); a `.subtitle` paragraph ("I build companies and write about what I am learning…"); a `.wayin` paragraph reading "I write about building monthly." with an anchor `<a href="#writing">Join Monty Monthly →</a>` (D-13 single way-in, no button); a `.statustag` with a `.dot` reading "Currently — building Prometheus" (use an en-dash-free separator; the site copy rule forbids em dashes — the status tag word "Currently —" in the sketch uses an em dash, so render it as "Currently, building Prometheus" or "Currently: building Prometheus"). Photo column: a `<Photo breathe caption="Monty — portrait" />` filling the column (min-height ~56vh) plus a `.blob` ambient glow div. At the band base, render the pinned `<div className="hero-ticker"><div className="track">…</div></div>` duplicating a group of section-link anchors (Building #building, Work #work, Writing #writing, Things I Love #loves, Uses /uses) so the CSS slide loop is seamless. Do NOT add `"use client"`, do NOT render a site nav row, do NOT import any canvas/WebGL. All animation is the ambient CSS classes (breathe/kenburns/drift/slide) already reduced-motion-gated in Plan 01.
  </action>
  <verify>
    <automated>grep -q 'hw' src/components/home/hero.tsx && grep -q 'hero-ticker' src/components/home/hero.tsx && grep -q 'statustag' src/components/home/hero.tsx && grep -q 'Monty Monthly' src/components/home/hero.tsx && ! grep -qiE 'use client|canvas|webgl|—' src/components/home/hero.tsx && npx tsc --noEmit && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - hero.tsx is a Server Component (no `"use client"`) exporting `Hero`; imports `Photo`.
    - Renders `h1.sig` with a `.hw` marker span wrapping "Create Order" and an ink "from Chaos"; a `.eyebrow` "Founder of Prometheus"; a `.statustag`; a pinned `.hero-ticker` with duplicated section-link anchors.
    - Way-in link targets `#writing`/Monty Monthly; there is no CTA button and no contact link (D-13).
    - No em dash (—) anywhere in the file (site copy rule); no canvas/WebGL import.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>The marker-block hero renders with portrait, status tag, pinned ticker and single Monty Monthly way-in, as a static Server Component; typecheck clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| none (static server render) | Hardcoded hero markup; no user input, no external fetch, no client JS |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17.4-05 | Info Disclosure | hero copy | accept | Static hardcoded copy; identity limited to "Founder of Prometheus" per site copy rules; no location. |
| T-17.4-05b | Denial | ambient CSS motion | mitigate | Reduced-motion CSS (Plan 01) keeps the hero fully legible with animation off (D-08). |
</threat_model>

<verification>
- Hero compiles as a Server Component with no client directive and no WebGL.
- Marker headline, status tag, ticker, and single way-in present.
</verification>

<success_criteria>
The marker-block hero (D-06) renders statically with reduced-motion-safe ambient motion (D-08) and preserves the velvet-rope way-in (D-13).
</success_criteria>

<output>
Create `.planning/phases/17.4-photo-forward-homepage-restyle/17.4-05-SUMMARY.md` when done.
</output>
