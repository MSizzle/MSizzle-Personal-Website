---
phase: 17.4-photo-forward-homepage-restyle
plan: 04
type: execute
wave: 2
depends_on: ["17.4-01"]
files_modified:
  - src/components/home/monty-monthly-carousel.tsx
  - src/components/home/sticky-nav.tsx
autonomous: true
requirements: [D-09, D-10, D-13]
must_haves:
  truths:
    - "D-09: The Writing carousel is a horizontal Monty Monthly track of issue cards plus a Vermilion subscribe card, driven by native scroll-snap with prev/next arrows and dot indicators that sync to scroll position."
    - "D-09: The subscribe card links out to https://montymonthly.substack.com (rel=noopener) with no on-site email capture."
    - "D-10: A sticky mini-nav slides down once scrolled past ~82% of the viewport, carrying a persistent Vermilion Subscribe CTA; it sits above the existing mobile header (z-index 9000)."
    - "D-13: Engagement stays velvet-rope — the only CTA is Subscribe (Monty Monthly), no contact button, no email form."
  artifacts:
    - path: "src/components/home/monty-monthly-carousel.tsx"
      provides: "Client carousel: scroll-snap track + arrows + dots + scroll-sync + Substack subscribe card"
      contains: "scrollBy"
    - path: "src/components/home/sticky-nav.tsx"
      provides: "Scroll-triggered sticky mini-nav with Subscribe CTA (z-9000)"
      contains: "stickynav"
  key_links:
    - from: "monty-monthly-carousel.tsx subscribe card"
      to: "https://montymonthly.substack.com"
      via: "anchor rel=noopener"
      pattern: "montymonthly.substack.com"
    - from: "sticky-nav.tsx"
      to: "window scroll position"
      via: "passive scroll listener toggling .show"
      pattern: "scrollY"
---

<objective>
Build the two scroll-driven control islands: the Monty Monthly carousel (D-09) with arrows + dots + scroll-position sync, and the sticky mini-nav (D-10) that appears past the hero with a persistent Subscribe CTA. Both preserve the velvet-rope model (D-13): Subscribe is the only call to action, linking out to Substack, no on-site email capture.

Purpose: These are the two most interaction-heavy new patterns (no analog for either). Isolating them in one plan keeps their scroll logic away from the presentational reskins.
Output: `monty-monthly-carousel.tsx`, `sticky-nav.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/17.4-photo-forward-homepage-restyle/17.4-CONTEXT.md
@.planning/phases/17.4-photo-forward-homepage-restyle/17.4-PATTERNS.md

<interfaces>
<!-- Consumed by Wave-3 section-newsletter (carousel) and Wave-4 orchestrator (sticky nav). -->
MontyMonthlyCarousel props: { issues: { num: string; date: string; title: string; excerpt: string; href?: string }[] }
  → .mm-track (scroll-snap x mandatory) of .mm-card issue covers + a trailing .mm-sub subscribe card;
    .mm-ctrls with #mm-prev/#mm-next buttons (scrollBy ±clientWidth*0.62) and .mm-dots that sync to nearest-centered card.
StickyNav: no props; renders .stickynav (fixed, z-9000) with brand mark, anchor links (#building/#work/#loves/#writing) and .cta Subscribe → #writing (scrolls to carousel) OR Substack; toggles .show when window.scrollY > innerHeight*0.82.
Analogs: src/components/v3/newsletter-carousel.tsx (scroll-snap track markup) ; src/components/nav/navigation.tsx (existing fixed mobile header at z-50 — sticky nav MUST sit above it).
Substack URL: https://montymonthly.substack.com (rel="noopener noreferrer" on all external anchors — section-work.test invariant applies site-wide).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Monty Monthly carousel island (D-09, D-13)</name>
  <read_first>
    - .planning/sketches/010-structured-bands-carousel/index.html (mm CSS 123-150; carousel JS 420-441; issues data 421-426; subscribe card 432-434)
    - src/components/v3/newsletter-carousel.tsx (scroll-snap track markup analog)
    - src/components/home/photo.tsx (Photo primitive — reuse `dark` variant for issue covers)
    - src/components/home/section-newsletter.tsx (current issues array the reskin will pass in)
  </read_first>
  <action>
    Create src/components/home/monty-monthly-carousel.tsx as a `"use client"` island exporting `MontyMonthlyCarousel({ issues })`. Render a `.mm-track` (ref'd) with `[scroll-snap-type:x_mandatory]`/smooth (the `.mm-track` CSS from Plan 01 supplies this) containing one `.mm-card` per issue — each an `<article>` with a `.cover` using `<Photo dark aspectRatio="16/8" caption="Issue cover" />` and a `.meta` block (`.mm-issue` = "Issue {num} · {date}", `.mm-title`, `.mm-ex` excerpt, `.mm-read` "Read issue →" link to issue.href ?? "https://montymonthly.substack.com") — plus a trailing `.mm-sub` subscribe card (Vermilion, `<a className="btn" href="https://montymonthly.substack.com" target="_blank" rel="noopener noreferrer">Subscribe →</a>`, D-09/D-13: link-out only, no form). Render `.mm-ctrls` with prev/next `<button className="mm-btn">` (aria-labels) that call `track.scrollBy({ left: ±track.clientWidth*0.62, behavior:'smooth' })`, and a `.mm-dots` row (one dot per slide incl. subscribe card). Add a passive `scroll` listener on the track that, via requestAnimationFrame, computes the nearest-centered slide and toggles `.on` on the matching dot (sketch `syncDots` 438); clicking a dot scrolls that slide into center. Guard refs for SSR. Do NOT add any email input.
  </action>
  <verify>
    <automated>grep -q 'scrollBy' src/components/home/monty-monthly-carousel.tsx && grep -q 'montymonthly.substack.com' src/components/home/monty-monthly-carousel.tsx && ! grep -qiE '<input|<form' src/components/home/monty-monthly-carousel.tsx && grep -q '"use client"' src/components/home/monty-monthly-carousel.tsx && npx tsc --noEmit && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Exports `MontyMonthlyCarousel({ issues })`; renders `.mm-track` of `.mm-card`s + trailing `.mm-sub`; `.mm-ctrls` prev/next call `scrollBy` on the track ref; `.mm-dots` sync to scroll position.
    - Subscribe card and "Read issue" fallback link to `https://montymonthly.substack.com` with `rel="noopener noreferrer"`.
    - No `<input>` or `<form>` anywhere (D-09/D-13: no on-site email capture).
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Carousel scroll-snaps with working arrows + scroll-synced dots and a Substack-only subscribe card; typecheck clean.</done>
</task>

<task type="auto">
  <name>Task 2: Sticky mini-nav island (D-10, D-13)</name>
  <read_first>
    - .planning/sketches/010-structured-bands-carousel/index.html (.stickynav CSS 195-206; markup 239-243; scroll JS 452-454, threshold innerHeight*0.82)
    - src/components/nav/navigation.tsx (existing fixed mobile header at z-50 — coordinate stacking)
    - .planning/phases/17.4-photo-forward-homepage-restyle/17.4-PATTERNS.md (New Pattern 2, lines 208-217)
  </read_first>
  <action>
    Create src/components/home/sticky-nav.tsx as a `"use client"` island exporting `StickyNav()`. Render `<div className="stickynav">` (fixed, z-index 9000 via the Plan-01 `.stickynav` rule — sits above the z-50 mobile header) &gt; `.inner` with a brand mark ("Monty."), a `<ul>` of anchor links to `#building`, `#work`, `#loves`, `#writing` (matching the orchestrator's band ids from Plan 08), and a persistent `.cta` Subscribe link. The Subscribe CTA points to `#writing` (smooth-scrolls to the carousel, whose subscribe card is the Substack link-out) — this keeps the velvet-rope single-CTA model (D-13). Use `useState` for a `show` boolean; in a `useEffect` attach a passive `scroll` listener that sets `show = window.scrollY > window.innerHeight * 0.82`, toggling the `.show` class; remove the listener on cleanup. Do NOT add a competing full site nav or a contact button — this is additive to the existing global EditorialHeader. Guard for SSR.
  </action>
  <verify>
    <automated>grep -q 'stickynav' src/components/home/sticky-nav.tsx && grep -q 'scrollY' src/components/home/sticky-nav.tsx && grep -qi 'subscribe' src/components/home/sticky-nav.tsx && ! grep -qi 'contact' src/components/home/sticky-nav.tsx && grep -q '"use client"' src/components/home/sticky-nav.tsx && npx tsc --noEmit && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Exports `StickyNav`; renders `.stickynav` toggling `.show` when `scrollY > innerHeight*0.82` via a passive listener with cleanup.
    - Contains anchor links `#building/#work/#loves/#writing` and a persistent Subscribe CTA; no "Contact" text, no contact button (D-13).
    - Relies on the z-9000 `.stickynav` rule so it layers above the existing z-50 mobile header.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Sticky mini-nav appears past the hero fold with a persistent Subscribe CTA above the existing header; typecheck clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| outbound link → Substack | External navigation target (reverse-tabnabbing surface) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17.4-04 | Tampering (reverse tabnabbing) | Substack `target=_blank` anchor | mitigate | All external anchors carry `rel="noopener noreferrer"` (site invariant asserted by section-work.test). |
| T-17.4-04b | Denial | scroll listeners | mitigate | Passive listeners + cleanup on unmount; no layout thrash beyond rAF-batched dot sync. |
</threat_model>

<verification>
- Both islands compile; carousel has no email form; sticky nav has no contact CTA.
- External anchors use rel=noopener noreferrer.
</verification>

<success_criteria>
Monty Monthly carousel (D-09) and sticky Subscribe mini-nav (D-10) exist as velvet-rope-preserving (D-13) client islands.
</success_criteria>

<output>
Create `.planning/phases/17.4-photo-forward-homepage-restyle/17.4-04-SUMMARY.md` when done.
</output>
