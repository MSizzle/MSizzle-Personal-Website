---
phase: 17.4-photo-forward-homepage-restyle
plan: 08
type: execute
wave: 4
depends_on: ["17.4-01", "17.4-02", "17.4-03", "17.4-04", "17.4-05", "17.4-06", "17.4-07"]
files_modified:
  - src/components/home/explorative-homepage.tsx
  - src/__tests__/home/explorative-homepage.test.tsx
autonomous: true
requirements: [D-03, D-08, D-10, D-13]
must_haves:
  truths:
    - "D-03: The orchestrator composes the exact band sequence — hero (light) → credibility strip (light) → Building (dark) → Work (light) → Things I Love (dark) → Writing (light) → footer (dark) — wrapping each content section in a band or band-dark section with the matching anchor id (#building/#work/#loves/#writing)."
    - "D-10: The sticky mini-nav island is mounted so it can appear past the hero."
    - "D-08: The ScrollReveals island is mounted once so slide-in photos animate; the page remains free of WebGL/Lenis on the home render path."
    - "D-13: The orchestrator stays a Server-Component shell; page.tsx remains a static Server Component (revalidate=false, Person JSON-LD) with no client hooks hoisted into it."
  artifacts:
    - path: "src/components/home/explorative-homepage.tsx"
      provides: "Band-ordered orchestrator mounting Hero, CredibilityStrip, sections, StickyNav, ScrollReveals"
      contains: "band-dark"
  key_links:
    - from: "explorative-homepage.tsx"
      to: "Hero / CredibilityStrip / SectionBuilding / SectionWork / SectionLoves / SectionNewsletter / SectionFooter / StickyNav / ScrollReveals"
      via: "import + band wrappers with ids"
      pattern: "id=\"building\""
---

<objective>
Rewrite the orchestrator to assemble the sketch-010 band sequence (D-03) and mount the professional-layer islands: it wraps each reskinned section in a light/dark band with the correct anchor id, places the CredibilityStrip under the hero, and mounts the StickyNav (D-10) and headless ScrollReveals (D-08). page.tsx stays a static Server Component (D-13). Rewrite the stale explorative-homepage test to the new structure.

Purpose: This is the single place band order and alternation live (sections are band-agnostic), and the only client boundary mounting point — keeping the client surface minimal and `/` static.
Output: reskinned explorative-homepage.tsx + rewritten explorative-homepage.test.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/17.4-photo-forward-homepage-restyle/17.4-CONTEXT.md
@.planning/phases/17.4-photo-forward-homepage-restyle/17.4-PATTERNS.md
@src/app/page.tsx

<interfaces>
<!-- All consumed pieces already exist from Waves 2-3. -->
Hero() — Plan 05 (renders its own light hero band + pinned ticker).
CredibilityStrip() — Plan 02.  StickyNav() — Plan 04 (fixed, client).  ScrollReveals() — Plan 03 (headless, client).
SectionBuilding/SectionWork/SectionLoves/SectionNewsletter/SectionFooter — Plans 06/07 (return beat CONTENT, band-agnostic).
Band wrapper contract: light = <section className="band beat" id="…">, dark = <section className="band band-dark beat" id="…">.
Sketch structure ref: index.html lines 244-354 (page order); ids #building/#work/#loves/#writing (sticky nav + ticker + footer anchors depend on these).
page.tsx MUST remain static: export const revalidate = false, JsonLd buildPersonSchema, no "use client", no canvas.
Stale test to rewrite: src/__tests__/home/explorative-homepage.test.tsx (asserts a WebGL fallback-poster gate that no longer exists — TD-03/HD-05 predate this phase).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite orchestrator to the sketch-010 band sequence + island mounts (D-03/D-08/D-10/D-13)</name>
  <read_first>
    - src/components/home/explorative-homepage.tsx (current orchestrator to rewrite)
    - .planning/sketches/010-structured-bands-carousel/index.html (page order + band classes 244-354; sticky nav 239-243)
    - src/app/page.tsx (confirm it stays static — do not change unless needed)
    - Hero/CredibilityStrip/StickyNav/ScrollReveals + the five section components (final contracts from Waves 2-3)
  </read_first>
  <action>
    Rewrite src/components/home/explorative-homepage.tsx as a Server-Component shell exporting ExplorativeHomepage(). Import Hero, CredibilityStrip, StickyNav, ScrollReveals, SectionBuilding, SectionWork, SectionLoves, SectionNewsletter, SectionFooter. Render, in this exact D-03 order: (1) StickyNav (fixed island, first so it overlays), (2) ScrollReveals (headless island), (3) Hero (its own light hero band with pinned ticker), (4) a light band wrapping CredibilityStrip (section.band with a proof pad), (5) section.band.band-dark id="building" wrapping SectionBuilding, (6) section.band id="work" wrapping SectionWork, (7) section.band.band-dark id="loves" wrapping SectionLoves, (8) section.band id="writing" wrapping SectionNewsletter, (9) section.band.band-dark wrapping SectionFooter. Give the content bands the beat padding class. The orchestrator OWNS band alternation + ids; sections stay band-agnostic. Do NOT hoist any client hook into page.tsx; verify page.tsx still exports revalidate=false and renders JsonLd + ExplorativeHomepage unchanged (edit page.tsx only if an import path broke). Do NOT import canvas/WebGL/Lenis. Remove the old text-forward inline hero and the old min-h-screen wrapper.
  </action>
  <verify>
    <automated>grep -q 'band-dark' src/components/home/explorative-homepage.tsx && grep -q 'id="building"' src/components/home/explorative-homepage.tsx && grep -q 'id="writing"' src/components/home/explorative-homepage.tsx && grep -q 'StickyNav' src/components/home/explorative-homepage.tsx && grep -q 'ScrollReveals' src/components/home/explorative-homepage.tsx && ! grep -qiE 'canvas|webgl|lenis|use client' src/components/home/explorative-homepage.tsx && grep -q 'revalidate = false' src/app/page.tsx && npx tsc --noEmit && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - explorative-homepage.tsx renders the exact band order (hero light → credibility light → building dark → work light → loves dark → writing light → footer dark) with ids #building/#work/#loves/#writing on the matching content bands.
    - StickyNav and ScrollReveals are mounted; no canvas/WebGL/Lenis import; orchestrator has no "use client".
    - page.tsx still exports revalidate=false and renders JsonLd + ExplorativeHomepage.
    - npx tsc --noEmit exits 0.
  </acceptance_criteria>
  <done>The orchestrator assembles the full sketch-010 band sequence and mounts the islands while keeping / static; typecheck clean.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite explorative-homepage test to the new structure</name>
  <read_first>
    - src/__tests__/home/explorative-homepage.test.tsx (STALE WebGL fallback-poster gate test — rewrite)
    - the rewritten orchestrator from Task 1 (structure to assert)
  </read_first>
  <action>
    Rewrite src/__tests__/home/explorative-homepage.test.tsx: drop the canvas-loader/fallback-poster/section-writing mocks and the pointer:coarse / WebGL2 gate assertions (that gate no longer exists — TD-03/HD-05 stale). Mock the child components (Hero, CredibilityStrip, StickyNav, ScrollReveals, the five sections) as lightweight stubs, render ExplorativeHomepage, and assert the new invariants: the band-dark sections carry ids "building"/"loves" and the light sections carry ids "work"/"writing" (query by id via container), and that StickyNav + ScrollReveals stubs render. Keep it a fast unit test (mock motion/react useReducedMotion → false). The suite must pass.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/home/explorative-homepage.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - The test no longer references canvas-loader/fallback-poster/pointer:coarse/WebGL2.
    - It asserts the band ids (#building/#work/#loves/#writing) and that StickyNav + ScrollReveals mount.
    - vitest run for the file exits 0 with all tests passing.
  </acceptance_criteria>
  <done>The orchestrator test asserts the new band structure and passes; the stale WebGL gate assertions are gone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| server render shell | Static composition; client islands are self-contained; page.tsx has no dynamic input |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17.4-08 | Elevation | client hooks leaking into page.tsx | mitigate | Assert page.tsx stays revalidate=false Server Component; all interactivity confined to child islands. |
| T-17.4-08b | Denial | missing anchor ids break sticky-nav/ticker links | mitigate | Grep-verified ids on band wrappers so in-page anchors resolve. |
</threat_model>

<verification>
- Band order + ids present; islands mounted; page.tsx static; no WebGL/Lenis on home path.
- Orchestrator + rewritten test both green.
</verification>

<success_criteria>
The homepage assembles the exact sketch-010 band sequence (D-03) with sticky nav (D-10) and scroll reveals (D-08) mounted, staying a static Server Component (D-13).
</success_criteria>

<output>
Create `.planning/phases/17.4-photo-forward-homepage-restyle/17.4-08-SUMMARY.md` when done.
</output>
