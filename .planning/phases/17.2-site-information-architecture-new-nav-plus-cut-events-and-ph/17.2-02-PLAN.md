---
phase: "17.2"
plan: 02
type: execute
wave: 2
depends_on:
  - "17.2-01"
files_modified:
  - src/components/home-v2/editorial-header.tsx
  - src/components/nav/navigation.tsx
  - src/components/layout/v3-footer.tsx
  - src/components/home/section-footer.tsx
  - src/components/home-deck/slide-footer.tsx
autonomous: true
requirements:
  - D-08
  - D-09
  - D-10

must_haves:
  truths:
    - "D-08: editorial-header.tsx LINKS array has exactly 4 entries: About/about, Projects/projects, Writing/writing, Uses/uses. No /events, /links. Label 'Building' renamed to 'Projects'."
    - "D-08: navigation.tsx MOBILE_LINKS contains no /events, /links, /watching entries. activeLabel type union is 'Projects' | 'Writing' | 'About' | 'Uses' | undefined."
    - "D-09: v3-footer.tsx 'Let's be friends.' signature link points to /about (not /links). Footer contains no links to /events, /photos, /newsletter, /watching, /links."
    - "D-09: v3-footer.tsx has woven socials row (email, GitHub, X/Twitter, LinkedIn as external <a> tags) replacing the removed /links page function."
    - "D-10: section-footer.tsx and slide-footer.tsx have 'Let's be friends.' linking to /about; 'More' column (newsletter/events/links) deleted entirely."
  artifacts:
    - path: src/components/home-v2/editorial-header.tsx
      provides: "4-item lean primary nav per D-08"
      exports: ["EditorialHeader"]
    - path: src/components/nav/navigation.tsx
      provides: "Updated MOBILE_LINKS + activeLabel with no cut routes"
      exports: ["Navigation"]
    - path: src/components/layout/v3-footer.tsx
      provides: "Restructured footer with woven socials + no cut-route links per D-09"
      exports: ["V3Footer"]
  key_links:
    - from: editorial-header.tsx Props.active
      to: navigation.tsx activeLabel
      via: "active prop passed down to EditorialHeader"
      pattern: "active?"
    - from: v3-footer.tsx
      to: /about
      via: "Let's be friends. link href"
      pattern: "href=\"/about\""
---

<objective>
Update all 5 navigation/footer component files to the new link sets: 4-item primary nav, no cut routes in mobile drawer, footer restructured with woven socials. This is a find-and-replace sweep driven by PATTERNS.md before/after excerpts — no new design, no new components.

Purpose: Every <Link> pointing to a cut route must be removed from the component layer before the validation gate in Plan 04. Routing infrastructure (Plan 01) is already in place by the time this plan runs.

Output: editorial-header.tsx, navigation.tsx, v3-footer.tsx, section-footer.tsx, slide-footer.tsx — all updated to the D-08/D-09/D-10 link sets.
</objective>

<execution_context>
@/Users/Montster/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Montster/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/17.2-site-information-architecture-new-nav-plus-cut-events-and-ph/17.2-CONTEXT.md
@.planning/phases/17.2-site-information-architecture-new-nav-plus-cut-events-and-ph/17.2-RESEARCH.md
@.planning/phases/17.2-site-information-architecture-new-nav-plus-cut-events-and-ph/17.2-PATTERNS.md
@.planning/phases/17.2-site-information-architecture-new-nav-plus-cut-events-and-ph/17.2-UI-SPEC.md

<interfaces>
From src/components/home-v2/editorial-header.tsx (current — pre-update):
  type Props = { active?: "Building" | "Writing" | "Events" | "About" | "Links" | "Uses" | "Watching" }
  const LINKS = [ {label:"Building",href:"/projects"}, {label:"Writing",href:"/writing"}, {label:"Events",href:"/events"}, {label:"About",href:"/about"}, {label:"Links",href:"/links"} ] as const

From src/components/nav/navigation.tsx (current — pre-update):
  const MOBILE_LINKS = [ {href:'/projects',label:'Building'}, {href:'/writing',label:'Writing'}, {href:'/events',label:'Events'}, {href:'/about',label:'About'}, {href:'/links',label:'Links'}, {href:'/uses',label:'Uses'}, {href:'/watching',label:'Watching'}, {href:'/prometheus',label:'Prometheus'} ]
  activeLabel type: 'Building'|'Writing'|'Events'|'About'|'Links'|'Uses'|'Watching'|undefined

From src/components/layout/v3-footer.tsx (current — pre-update):
  - 5-column structure: Building, Writing, Community, Archive, About
  - Signature "Let's be friends." linked to href="/links"
  - Community column includes /events and /links
  - Archive column includes /photos and /watching
  - Writing column includes /newsletter

From src/components/home/section-footer.tsx (current — pre-update):
  - "Let's be friends." linked to href="/links"
  - "More" column contains /newsletter, /events, /links

From src/components/home-deck/slide-footer.tsx (current — pre-update):
  - Identical structure to section-footer.tsx
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Primary nav — EditorialHeader + Navigation (D-08:, D-10:)</name>
  <files>src/components/home-v2/editorial-header.tsx, src/components/nav/navigation.tsx</files>

  <read_first>
    - src/components/home-v2/editorial-header.tsx (current full file — note LINKS array and Props type)
    - src/components/nav/navigation.tsx (current full file — note MOBILE_LINKS array and activeLabel switch)
    - .planning/phases/17.2-site-information-architecture-new-nav-plus-cut-events-and-ph/17.2-PATTERNS.md §navigation.tsx and §editorial-header.tsx (exact before/after excerpts with line numbers)
    - .planning/phases/17.2-site-information-architecture-new-nav-plus-cut-events-and-ph/17.2-UI-SPEC.md §Primary Navigation (final item table)
  </read_first>

  <action>
    Step 1 — editorial-header.tsx, per D-08::
    Replace the LINKS const array with exactly 4 items in this order: {label:"About", href:"/about"}, {label:"Projects", href:"/projects"}, {label:"Writing", href:"/writing"}, {label:"Uses", href:"/uses"}.
    Note the rename: "Building" → "Projects" (label changes, href stays /projects). Remove Events and Links entries entirely.
    Update the Props type `active?` union from its current 7-member union to exactly: "About" | "Projects" | "Writing" | "Uses". Remove "Building", "Events", "Links", "Watching" from the union.
    No other changes to this file (class names, rendering logic, import/export signatures unchanged).

    Step 2 — navigation.tsx, per D-08:, D-10::
    Replace MOBILE_LINKS array. Remove the entries for /events (label 'Events'), /links (label 'Links'), /watching (label 'Watching'). Rename the /projects entry label from 'Building' to 'Projects'. Final MOBILE_LINKS must contain: /projects (Projects), /writing (Writing), /about (About), /uses (Uses), /prometheus (Prometheus) — 5 items in that order.
    Update the activeLabel computation (the ternary chain or switch). Remove the 'Events', 'Links', 'Watching' cases. Rename 'Building' case to 'Projects' (when pathname === '/projects' return 'Projects'). Keep 'Writing' (with startsWith('/blog')), 'About', 'Uses'.
    Update the activeLabel TypeScript type annotation to: 'Projects' | 'Writing' | 'About' | 'Uses' | undefined.
    If the EditorialHeader active prop value for /projects was previously 'Building', it is now 'Projects' — update the value passed in the render call to match.
    No other changes.
  </action>

  <acceptance_criteria>
    - `grep -c "href=\"/events\"\|href=\"/links\"\|href=\"/watching\"" src/components/home-v2/editorial-header.tsx` equals 0.
    - `grep -c "href=\"/events\"\|href=\"/links\"\|href=\"/watching\"" src/components/nav/navigation.tsx` equals 0.
    - `grep "LINKS" src/components/home-v2/editorial-header.tsx | grep -c "About\|Projects\|Writing\|Uses"` is at least 4.
    - `grep "MOBILE_LINKS" src/components/nav/navigation.tsx` is present and "Building" | "Events" | "Watching" strings do not appear in the array.
    - `npm run test -- --run src/__tests__/components/navigation.test.tsx` exits 0.
  </acceptance_criteria>

  <verify>
    <automated>grep -n "href=\"/events\"\|href=\"/links\"\|href=\"/watching\"\|href=\"/photos\"\|href=\"/newsletter\"" src/components/home-v2/editorial-header.tsx src/components/nav/navigation.tsx && echo "FAIL: cut routes found" || echo "PASS: no cut routes"</automated>
  </verify>

  <done>editorial-header.tsx LINKS has 4 items (About, Projects, Writing, Uses) with correct hrefs. navigation.tsx MOBILE_LINKS has 5 items with no cut routes. activeLabel type is 'Projects'|'Writing'|'About'|'Uses'|undefined. navigation.test.tsx passes.</done>
</task>

<task type="auto">
  <name>Task 2: Footer sweep — V3Footer + section-footer + slide-footer (D-09:, D-10:)</name>
  <files>src/components/layout/v3-footer.tsx, src/components/home/section-footer.tsx, src/components/home-deck/slide-footer.tsx</files>

  <read_first>
    - src/components/layout/v3-footer.tsx (current full file — note 5-column structure and "Let's be friends." link)
    - src/components/home/section-footer.tsx (current full file — note "More" column and "Let's be friends." link)
    - src/components/home-deck/slide-footer.tsx (current full file — identical structure to section-footer)
    - .planning/phases/17.2-site-information-architecture-new-nav-plus-cut-events-and-ph/17.2-PATTERNS.md §v3-footer.tsx and §section-footer.tsx and §slide-footer.tsx (column-by-column before/after)
    - .planning/phases/17.2-site-information-architecture-new-nav-plus-cut-events-and-ph/17.2-UI-SPEC.md §Footer Navigation (proposed column table + woven socials spec)
  </read_first>

  <action>
    Step 1 — v3-footer.tsx, per D-09:, D-10::
    Change the "Let's be friends." big signature link's href from "/links" to "/about" per D-01:.
    In the Building column: keep /projects (Works) and /uses (Things I Love — update label from "Stack" to "Things I Love" to match the /uses reframe). No deletions here.
    In the Writing column: keep /writing (Essays) link. Delete the /newsletter direct link per D-04: (newsletter is now folded into /writing).
    In the Community column: delete the /events link and the /links link per D-01:. Keep the prometheus.today external link with its existing target="_blank" rel="noopener noreferrer".
    Delete the Archive column entirely (contained /photos and /watching — both cut per D-01:, D-07:).
    Keep the About column (/about link).
    Add a Prometheus column (or add /prometheus as an internal link alongside or below prometheus.today): link label "Prometheus" href "/prometheus".
    Add a woven socials row below or within the footer prose per D-09:. This row uses external <a> tags (not Next Link) with target="_blank" rel="noopener noreferrer" for: email (mailto:montydsinger@gmail.com), GitHub, X/Twitter, LinkedIn. The existing Elsewhere/socials data from section-footer.tsx is the source of the correct URLs — check those before writing.
    No palette changes (Pumpkin Amber tokens unchanged).

    Step 2 — section-footer.tsx, per D-09:, D-10::
    Change the "Let's be friends." link href from "/links" to "/about" per D-01:.
    Delete the "More" column entirely (contains /newsletter, /events, /links — all cut routes). The other columns (Site, Elsewhere, Connect) are untouched.
    No other changes.

    Step 3 — slide-footer.tsx, per D-09:, D-10::
    Apply the same changes as section-footer.tsx: "Let's be friends." href to "/about", delete "More" column. No other changes.
  </action>

  <acceptance_criteria>
    - `grep -c "href=\"/events\"\|href=\"/links\"\|href=\"/newsletter\"\|href=\"/watching\"\|href=\"/photos\"" src/components/layout/v3-footer.tsx` equals 0.
    - `grep -c "href=\"/events\"\|href=\"/links\"\|href=\"/newsletter\"" src/components/home/section-footer.tsx` equals 0.
    - `grep -c "href=\"/events\"\|href=\"/links\"\|href=\"/newsletter\"" src/components/home-deck/slide-footer.tsx` equals 0.
    - `grep "Let" src/components/layout/v3-footer.tsx | grep -c "/about"` equals 1 (signature link to /about).
    - `grep "Let" src/components/home/section-footer.tsx | grep -c "/about"` equals 1.
    - `grep "Let" src/components/home-deck/slide-footer.tsx | grep -c "/about"` equals 1.
    - `npm run test -- --run src/__tests__/components/footer.test.tsx` exits 0.
  </acceptance_criteria>

  <verify>
    <automated>grep -rn "href=\"/events\"\|href=\"/links\"\|href=\"/newsletter\"\|href=\"/watching\"\|href=\"/photos\"" src/components/layout/v3-footer.tsx src/components/home/section-footer.tsx src/components/home-deck/slide-footer.tsx && echo "FAIL: cut routes found" || echo "PASS: no cut routes"</automated>
  </verify>

  <done>V3Footer has no links to any cut route; signature links to /about; woven socials row present. section-footer.tsx and slide-footer.tsx "More" column deleted; signature links to /about. footer.test.tsx passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Component render → DOM | Nav/footer links render as <a> tags in the DOM; no user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17.2-03 | Information Disclosure | Woven socials (external links) | accept | External social links use target="_blank" rel="noopener noreferrer" on all <a> tags (existing pattern in section-footer.tsx Elsewhere column). No new risk introduced. |
| T-17.2-SC | Tampering | npm/pip installs | accept | No new packages installed this plan. No supply-chain risk. |
</threat_model>

<verification>
Run component tests after both tasks:
`npm run test -- --run src/__tests__/components/`

All 5 files must have zero occurrences of href="/events", href="/links", href="/newsletter", href="/watching", href="/photos". Confirm with:
`grep -rn "href=\"/events\"\|href=\"/photos\"\|href=\"/links\"\|href=\"/newsletter\"\|href=\"/watching\"" src/components/`
Expected: empty output.
</verification>

<success_criteria>
- editorial-header.tsx has exactly 4-item LINKS array (About, Projects, Writing, Uses).
- navigation.tsx MOBILE_LINKS has 5 items with no cut routes; activeLabel type is 'Projects'|'Writing'|'About'|'Uses'|undefined.
- v3-footer.tsx: "Let's be friends." → /about; no /events, /photos, /newsletter, /watching, /links; woven socials row present.
- section-footer.tsx and slide-footer.tsx: "More" column deleted; "Let's be friends." → /about.
- `npm run test -- --run src/__tests__/components/` exits 0.
</success_criteria>

<output>
Create `.planning/phases/17.2-site-information-architecture-new-nav-plus-cut-events-and-ph/17.2-02-SUMMARY.md` when done.
</output>
