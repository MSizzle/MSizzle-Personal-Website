---
phase: "17.2"
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - next.config.ts
  - src/app/sitemap.ts
  - src/app/events/page.tsx
  - src/app/photos/page.tsx
  - src/app/links/page.tsx
  - src/app/newsletter/page.tsx
  - src/app/watching/page.tsx
  - src/app/specimen/page.tsx
  - src/app/v3-specimen/page.tsx
  - src/__tests__/seo/sitemap.test.ts
  - src/__tests__/pages/links.test.tsx
  - src/__tests__/pages/watching.test.tsx
  - src/__tests__/components/footer.test.tsx
  - src/__tests__/components/navigation.test.tsx
autonomous: true
requirements:
  - D-01
  - D-02
  - D-04
  - D-05
  - D-07

must_haves:
  truths:
    - "D-01: /events returns HTTP 301 to /; /photos returns HTTP 301 to /; /links returns HTTP 301 to /about; /newsletter returns HTTP 301 to /writing; /watching returns HTTP 301 to /uses"
    - "D-01: /specimen and /v3-specimen return 404 (directories deleted, no redirect)"
    - "D-02: sitemap.ts staticRoutes array contains exactly 6 entries: /, /about, /prometheus, /projects, /writing, /uses"
    - "D-02: sitemap.test.ts no longer asserts /watching exists and static-route count is >= 6 not >= 9"
    - "D-07: src/lib/watching.ts is NOT deleted (its WATCHING_ITEMS are reused in Plan 03 for /uses)"
  artifacts:
    - path: next.config.ts
      provides: "6 redirect entries in redirects() including all 5 new cut-route 301s"
      contains: "source: '/events'"
    - path: src/app/sitemap.ts
      provides: "6-entry staticRoutes array; no /events, /photos, /links, /newsletter, /watching"
  key_links:
    - from: next.config.ts
      to: Vercel edge runtime
      via: "redirects() permanent:true"
      pattern: "permanent: true"
    - from: src/app/sitemap.ts
      to: Googlebot sitemap crawl
      via: "MetadataRoute.Sitemap export"
      pattern: "staticRoutes"
---

<objective>
Register 5 permanent redirects for public cut routes and 2 hard-deletes for internal design-system routes. Update sitemap.ts to reflect the new 6-route set. Update all affected SEO + component test assertions in lockstep so the Phase 17 regression gate stays green.

Purpose: The redirect/delete step and the test-update step must land in the same wave. If routes disappear but tests still assert they exist, the gate breaks immediately. Keeping both in Plan 01 avoids a window where the build is broken.

Output: next.config.ts with 5 new 301s, sitemap.ts with 6 static routes, 7 route directories deleted, 2 test files deleted, 3 test files updated.
</objective>

<execution_context>
@/Users/Montster/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Montster/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17.2-site-information-architecture-new-nav-plus-cut-events-and-ph/17.2-CONTEXT.md
@.planning/phases/17.2-site-information-architecture-new-nav-plus-cut-events-and-ph/17.2-RESEARCH.md
@.planning/phases/17.2-site-information-architecture-new-nav-plus-cut-events-and-ph/17.2-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register 5 redirects + delete 7 route dirs + remove 5 sitemap entries</name>
  <files>next.config.ts, src/app/sitemap.ts, src/app/events/ (delete), src/app/photos/ (delete), src/app/links/ (delete), src/app/newsletter/ (delete), src/app/watching/ (delete), src/app/specimen/ (delete), src/app/v3-specimen/ (delete)</files>

  <read_first>
    - next.config.ts (current redirects() array — one existing entry for /blog)
    - src/app/sitemap.ts (current staticRoutes array — 11 entries to trim to 6)
    - .planning/phases/17.2-site-information-architecture-new-nav-plus-cut-events-and-ph/17.2-RESEARCH.md §Route Inventory and §Pattern 1 (exact redirect pairs)
    - .planning/phases/17.2-site-information-architecture-new-nav-plus-cut-events-and-ph/17.2-PATTERNS.md §next.config.ts and §sitemap.ts (before/after excerpts)
  </read_first>

  <action>
    Step 1 — Extend next.config.ts redirects(). Inside the existing `async redirects()` return array, after the `/blog` entry, add five new objects per D-01:, D-04:, D-07::
    source '/events' destination '/' permanent true;
    source '/photos' destination '/' permanent true;
    source '/links' destination '/about' permanent true;
    source '/newsletter' destination '/writing' permanent true;
    source '/watching' destination '/uses' permanent true.
    Keep the existing '/blog' → '/writing' entry. All entries use permanent:true (HTTP 301, Vercel edge-cached). No other changes to next.config.ts.

    Step 2 — Delete 7 route directories entirely. The config-level redirects fire before App Router looks for page files, so source pages are safe to remove. Delete these directories in their entirety:
    src/app/events/, src/app/photos/, src/app/links/, src/app/newsletter/, src/app/watching/ (301 redirect sources per D-01:, D-04:, D-07:).
    src/app/specimen/, src/app/v3-specimen/ (hard-delete 404 per D-01: — these are internal design-system pages, never public-facing; no redirect needed).
    IMPORTANT: Do NOT delete src/lib/watching.ts — that file holds WATCHING_ITEMS which Plan 03 imports into /uses.

    Step 3 — Trim src/app/sitemap.ts staticRoutes array per D-02:. Remove the five entries whose url ends with /newsletter, /photos, /watching, /links, /events. The resulting staticRoutes array must contain exactly these 6 entries (order may vary): /, /about, /prometheus, /projects, /writing, /uses. Do not modify the dynamic route sections (blog posts, projects slugs). Do not change priority or changeFrequency values on kept entries.
  </action>

  <acceptance_criteria>
    - next.config.ts redirects() return array has exactly 6 entries (1 existing /blog + 5 new).
    - `grep -c "permanent: true" next.config.ts` equals 6.
    - `ls src/app/events src/app/photos src/app/links src/app/newsletter src/app/watching src/app/specimen src/app/v3-specimen 2>&1 | grep -c "No such file"` equals 7.
    - `grep -c "SITE_URL.*events\|SITE_URL.*photos\|SITE_URL.*links\|SITE_URL.*newsletter\|SITE_URL.*watching" src/app/sitemap.ts` equals 0.
    - `grep -c "SITE_URL" src/app/sitemap.ts` equals 6 (one per kept static route).
    - src/lib/watching.ts still exists (grep -l WATCHING_ITEMS src/lib/ returns watching.ts).
  </acceptance_criteria>

  <verify>
    <automated>grep -c "permanent: true" next.config.ts && grep -c "SITE_URL" src/app/sitemap.ts</automated>
  </verify>

  <done>next.config.ts has 6 redirect entries including all 5 cut-route 301s. sitemap.ts has exactly 6 static SITE_URL references. 7 route directories are deleted. src/lib/watching.ts is intact.</done>
</task>

<task type="auto">
  <name>Task 2: Test lockstep — update SEO + component assertions, delete 2 obsolete test files</name>
  <files>src/__tests__/seo/sitemap.test.ts, src/__tests__/components/footer.test.tsx, src/__tests__/components/navigation.test.tsx, src/__tests__/pages/links.test.tsx (delete), src/__tests__/pages/watching.test.tsx (delete)</files>

  <read_first>
    - src/__tests__/seo/sitemap.test.ts (3 tests; lines 22-28 assert /watching; line 38 asserts >= 9)
    - src/__tests__/components/footer.test.tsx (lines 46-50 assert /watching; lines 66-70 assert /photos)
    - src/__tests__/components/navigation.test.tsx (lines 80-85 assert Watching activeLabel; lines 97-103 assert /watching in MOBILE_LINKS)
    - .planning/phases/17.2-site-information-architecture-new-nav-plus-cut-events-and-ph/17.2-RESEARCH.md §Pitfall 4 and §Validation Architecture §Wave 0 Gaps
    - .planning/phases/17.2-site-information-architecture-new-nav-plus-cut-events-and-ph/17.2-PATTERNS.md §Test File Updates
  </read_first>

  <action>
    Step 1 — Update src/__tests__/seo/sitemap.test.ts per D-02::
    Delete the entire "includes /watching entry" it-block (lines 22-28 in the current file — the test calling result.find(entry => entry.url.endsWith('/watching'))).
    In the surviving static-count test, change the assertion value from 9 to 6: `toBeGreaterThanOrEqual(6)`. Update the comment on the adjacent line to read "Static routes: /, /about, /prometheus, /projects, /writing, /uses = 6" so it matches the new reality per D-02:.

    Step 2 — Delete src/__tests__/pages/links.test.tsx entirely. The /links page is gone (301 to /about per D-01:); its test is no longer needed.

    Step 3 — Delete src/__tests__/pages/watching.test.tsx entirely. The /watching page is gone (301 to /uses per D-07:); its test is no longer needed.

    Step 4 — Update src/__tests__/components/footer.test.tsx per D-09:, D-10::
    Delete the it-block "renders /watching link in footer nav (Archive column)" (currently lines 46-50).
    Delete the it-block "renders /photos link in footer nav (Archive column)" (currently lines 66-70).
    Do NOT remove: "Let's be friends." text assertion, /uses link test, prometheus external link test, noopener noreferrer test, Pumpkin Amber token test. Those must remain.

    Step 5 — Update src/__tests__/components/navigation.test.tsx per D-08:, D-10::
    Delete the it-block "active label is Watching when pathname is /watching (D-13)" (currently lines 80-85).
    Delete the it-block "MOBILE_LINKS includes /watching link" (currently lines 97-103).
    Do NOT remove: brand link test, /uses activeLabel test, /uses MOBILE_LINKS test, /prometheus MOBILE_LINKS test, EditorialHeader mock test. Those must remain.
  </action>

  <acceptance_criteria>
    - `npm run test -- --run src/__tests__/seo/sitemap.test.ts` exits 0 with 2 passing tests (not 3).
    - `npm run test -- --run src/__tests__/components/footer.test.tsx` exits 0 with 4 passing tests (not 6).
    - `npm run test -- --run src/__tests__/components/navigation.test.tsx` exits 0 with 5 passing tests (not 7).
    - `ls src/__tests__/pages/links.test.tsx src/__tests__/pages/watching.test.tsx 2>&1 | grep -c "No such file"` equals 2.
    - `grep "toBeGreaterThanOrEqual" src/__tests__/seo/sitemap.test.ts` shows value 6, not 9.
    - `grep "/watching" src/__tests__/seo/sitemap.test.ts` returns empty (no /watching assertion remains).
  </acceptance_criteria>

  <verify>
    <automated>npm run test -- --run src/__tests__/seo/sitemap.test.ts src/__tests__/components/footer.test.tsx src/__tests__/components/navigation.test.tsx</automated>
  </verify>

  <done>All 3 updated test files pass. 2 obsolete test files deleted. No test asserts a cut route. sitemap count assertion reflects 6 routes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vercel edge → App Router | Redirect responses served by Vercel CDN before any page code runs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17.2-01 | Spoofing / Tampering | next.config.ts redirects() | accept | All redirect destinations are static, hardcoded, same-origin paths (no user-controlled input in source or destination). No open-redirect risk. |
| T-17.2-02 | Information Disclosure | Deleted route dirs | accept | Deleting directories removes page source from the file tree; no compiled artifact leaks; Next.js build regenerates clean. |
| T-17.2-SC | Tampering | npm/pip installs | accept | No new packages installed this plan. No supply-chain risk. |
</threat_model>

<verification>
Run the full SEO test suite after both tasks:
`npm run test -- --run src/__tests__/seo/`

Expected: all tests pass. If any test references a deleted route, fix the assertion before proceeding to Wave 2.
</verification>

<success_criteria>
- next.config.ts redirects() returns 6 entries (1 existing + 5 new cut-route 301s).
- sitemap.ts staticRoutes has exactly 6 entries (no /events, /photos, /links, /newsletter, /watching).
- 7 route directories deleted from src/app/.
- src/lib/watching.ts preserved.
- 5 test files updated or deleted; `npm run test -- --run src/__tests__/seo/ src/__tests__/components/` exits 0.
</success_criteria>

<output>
Create `.planning/phases/17.2-site-information-architecture-new-nav-plus-cut-events-and-ph/17.2-01-SUMMARY.md` when done.
</output>
