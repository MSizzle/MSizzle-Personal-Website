---
phase: "17.2"
plan: 04
type: execute
wave: 3
depends_on:
  - "17.2-02"
  - "17.2-03"
files_modified: []
autonomous: false
requirements:
  - D-01
  - D-02
  - D-03
  - D-04
  - D-05
  - D-06
  - D-07
  - D-08
  - D-09
  - D-10

must_haves:
  truths:
    - "D-01 through D-10 verified: grep finds zero internal hrefs pointing to /events, /photos, /links, /newsletter, /watching, /specimen, /v3-specimen anywhere in src/"
    - "D-01: npm run test -- --run exits 0 (all tests green after Wave 1+2 changes)"
    - "D-02: npm run build exits 0 (no TypeScript errors, no missing imports)"
    - "D-06: Human confirms /uses renders 'Things I Love' heading with four visible sections including Watching grid"
    - "D-04: Human confirms /writing renders essay archive followed by a Monty Monthly section"
    - "D-08: Human confirms primary nav shows exactly: About / Projects / Writing / Uses (4 items, no Events or Links)"
  artifacts:
    - path: ".planning/phases/17.2-site-information-architecture-new-nav-plus-cut-events-and-ph/17.2-04-SUMMARY.md"
      provides: "Phase gate pass/fail record"
  key_links:
    - from: "grep result (zero cut-route hrefs)"
      to: "Phase 18 QA gate"
      via: "No broken internal links reaching production"
      pattern: "grep -rn exits empty"
---

<objective>
Run the full automated validation suite and a human visual checkpoint to confirm all 10 decisions are satisfied. This plan makes no file changes — it gates on the output of Plans 01, 02, and 03.

Purpose: The RESEARCH.md Validation Architecture specifies a route/redirect/link validation pass as the phase completion gate. Catching any missed cut-route links, test failures, or build errors here prevents them from reaching Phase 17.3 or Phase 18.

Output: A GO or NO-GO signal. If NO-GO, fixes are applied inline before this plan closes.
</objective>

<execution_context>
@/Users/Montster/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Montster/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md §Phase 17.2 success criteria
@.planning/phases/17.2-site-information-architecture-new-nav-plus-cut-events-and-ph/17.2-RESEARCH.md §Validation Architecture
@.planning/phases/17.2-site-information-architecture-new-nav-plus-cut-events-and-ph/17.2-04-SUMMARY.md (from this plan's output — does not exist yet)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated gate — link grep + full vitest suite + build</name>
  <files></files>

  <read_first>
    - .planning/phases/17.2-site-information-architecture-new-nav-plus-cut-events-and-ph/17.2-RESEARCH.md §Validation Architecture (exact commands and expected results)
    - .planning/phases/17.2-site-information-architecture-new-nav-plus-cut-events-and-ph/17.2-01-SUMMARY.md (confirm Plan 01 completed successfully)
    - .planning/phases/17.2-site-information-architecture-new-nav-plus-cut-events-and-ph/17.2-02-SUMMARY.md (confirm Plan 02 completed successfully)
    - .planning/phases/17.2-site-information-architecture-new-nav-plus-cut-events-and-ph/17.2-03-SUMMARY.md (confirm Plan 03 completed successfully)
  </read_first>

  <action>
    Run the following checks in order. For each failure, diagnose and fix before proceeding to the next check. Log each result.

    Check 1 — Cut-route href scan (D-10:): Run a grep across the entire src/ directory for any remaining internal hrefs pointing to deleted/redirected routes:
    `grep -rn "href=\"/events\"\|href=\"/photos\"\|href=\"/links\"\|href=\"/newsletter\"\|href=\"/watching\"\|href=\"/specimen\"\|href=\"/v3-specimen\"" src/ --include="*.tsx" --include="*.ts"`
    Expected: zero matches. If any match is found, edit the offending file to remove or update the link (it was missed in Plans 02 or 03).

    Check 2 — Full vitest suite (D-02:): Run `npm run test -- --run`. All test files must pass. Expected: 0 failures. If failures exist, trace to the specific test file, diagnose whether a Plan 01/02/03 step was incomplete, and fix it.

    Check 3 — Production build (D-02:): Run `npm run build`. Expected: exits 0, no TypeScript errors, no missing module errors. Pay attention to any errors in: uses/page.tsx, writing/page.tsx, v3-footer.tsx, navigation.tsx, editorial-header.tsx — these are the files changed in Plans 02 and 03.

    Check 4 — Sitemap static-route count verification (D-02:): Run `npm run test -- --run src/__tests__/seo/sitemap.test.ts` and confirm it passes. If it fails with "expected X to be >= 6", the sitemap.ts or test was not updated correctly in Plan 01.

    If ALL 4 checks pass, proceed to Task 2. If any check fails, fix inline and re-run the failing check before proceeding.
  </action>

  <acceptance_criteria>
    - Check 1: `grep -rn "href=\"/events\"\|href=\"/photos\"\|href=\"/links\"\|href=\"/newsletter\"\|href=\"/watching\"\|href=\"/specimen\"\|href=\"/v3-specimen\"" src/ --include="*.tsx" --include="*.ts"` returns empty output.
    - Check 2: `npm run test -- --run` exits 0.
    - Check 3: `npm run build` exits 0.
    - Check 4: `npm run test -- --run src/__tests__/seo/sitemap.test.ts` exits 0 with 2 passing tests.
  </acceptance_criteria>

  <verify>
    <automated>npm run test -- --run && npm run build && grep -rn "href=\"/events\"\|href=\"/photos\"\|href=\"/links\"\|href=\"/newsletter\"\|href=\"/watching\"\|href=\"/specimen\"\|href=\"/v3-specimen\"" src/ --include="*.tsx" --include="*.ts" && echo "WARN: cut-route hrefs found" || echo "PASS: no cut-route hrefs"</automated>
  </verify>

  <done>All 4 automated checks pass. Zero internal links to cut routes. Full test suite green. Build succeeds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human visual verification — /uses, /writing, primary nav</name>

  <what-built>
    Plans 01-03 restructured the site IA. Automated checks passed. This checkpoint confirms the three ROADMAP success criteria are visually satisfied before the phase closes.
  </what-built>

  <how-to-verify>
    Start the dev server with `npm run dev` and visit http://localhost:3000.

    Check 1 — Primary nav (D-08:):
    Look at the top header. It should show exactly 4 items: About / Projects / Writing / Uses.
    "Events" and "Links" must NOT appear.
    "Building" must NOT appear (it's now "Projects").

    Check 2 — /uses page (D-06:, D-07:):
    Navigate to http://localhost:3000/uses.
    Page heading must read "Things I Love" (not "Uses").
    Scroll down and confirm four sections are visible: Tools, Books, Watching, Recommendations (order matters).
    The Watching section must show a card grid (VideoCards with YouTube thumbnails or titles), not a blank space.

    Check 3 — /writing page (D-03:, D-04:):
    Navigate to http://localhost:3000/writing.
    Essay cards should still appear (existing content unchanged per D-05:).
    Below the essays, a "Monty Monthly" section heading must appear with a newsletter carousel or CTA below it.
    There must be NO link labeled "Newsletter" pointing anywhere in the page.

    Check 4 — Redirect spot-check (D-01:):
    Visit http://localhost:3000/events. It should redirect to http://localhost:3000/ (home).
    Visit http://localhost:3000/links. It should redirect to http://localhost:3000/about.
    Visit http://localhost:3000/newsletter. It should redirect to http://localhost:3000/writing.
    Note: Next.js dev server honors next.config.ts redirects, so these should redirect cleanly.

    Check 5 — Footer (D-09:):
    Scroll to the bottom of any page. "Let's be friends." must link to /about (click it and verify you land on /about, not /links).
    The footer must NOT show an "Events" link, a "Photos" link, a "Watching" link, a "Newsletter" standalone link.
    Social links (email, GitHub, X/Twitter, LinkedIn) must appear somewhere in the footer.
  </how-to-verify>

  <resume-signal>
    Type "approved" if all 5 checks pass. Or describe any issues found (e.g., "Watching section missing" or "footer still shows Events link") so they can be fixed before the phase closes.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Validation only | This plan runs no new code and modifies no files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17.2-06 | Tampering | grep validation gap | accept | If grep misses a cut-route href (e.g., in a string variable not a JSX href attribute), it would slip through. Risk is low: the codebase uses Next Link components with literal href strings; no dynamic route assembly for these simple paths. |
| T-17.2-SC | Tampering | npm/pip installs | accept | No new packages installed this plan. No supply-chain risk. |
</threat_model>

<verification>
Final phase-level gate:
1. `grep -rn "href=\"/events\"\|href=\"/photos\"\|href=\"/links\"\|href=\"/newsletter\"\|href=\"/watching\"\|href=\"/specimen\"\|href=\"/v3-specimen\"" src/ --include="*.tsx" --include="*.ts"` returns empty.
2. `npm run test -- --run` exits 0.
3. `npm run build` exits 0.
4. Human approves visual check.

All 4 conditions must be true before SUMMARY.md is written.
</verification>

<success_criteria>
- Zero internal hrefs to any cut route in the entire src/ directory.
- Full vitest suite green (npm run test -- --run exits 0).
- Build succeeds (npm run build exits 0).
- Human confirms: primary nav = 4 items, /uses shows "Things I Love" + Watching section, /writing shows Monty Monthly section, footer links to /about (not /links), redirects resolve correctly.
- Phase 17.2 ROADMAP success criteria 1, 2, and 3 all verified.
</success_criteria>

<output>
Create `.planning/phases/17.2-site-information-architecture-new-nav-plus-cut-events-and-ph/17.2-04-SUMMARY.md` when done. Include the GO/NO-GO verdict and any fixes applied inline.
</output>
