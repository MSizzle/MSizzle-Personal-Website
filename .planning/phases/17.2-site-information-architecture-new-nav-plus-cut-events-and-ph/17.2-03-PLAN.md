---
phase: "17.2"
plan: 03
type: execute
wave: 2
depends_on:
  - "17.2-01"
files_modified:
  - src/app/uses/page.tsx
  - src/app/writing/page.tsx
  - src/__tests__/pages/uses.test.tsx
  - src/__tests__/pages/writing.test.tsx
autonomous: true
requirements:
  - D-03
  - D-04
  - D-05
  - D-06
  - D-07

must_haves:
  truths:
    - "D-06: /uses page metadata.title equals 'Things I Love | Monty Singer'. PageHero title is 'Things I Love'."
    - "D-06: /uses page renders four sections in order: Tools, Books, Watching, Recommendations."
    - "D-07: Watching section renders VideoCard grid sourced from WATCHING_ITEMS imported from src/lib/watching.ts."
    - "D-03: /uses canonical stays /uses (unchanged path). /uses metadata.alternates.canonical equals '/uses'."
    - "D-04: /writing page renders NewsletterCarousel or WritingSubscribeCTA in a dedicated Monty Monthly section below the essay archive."
    - "D-05: /blog/[slug] links in /writing are unchanged. /writing metadata canonical stays '/writing'."
    - "D-03: /writing does NOT link to /newsletter anywhere in its JSX."
  artifacts:
    - path: src/app/uses/page.tsx
      provides: "'Things I Love' reframe with Watching section (VideoCard grid)"
      contains: "Things I Love"
    - path: src/app/writing/page.tsx
      provides: "Merged essay archive + Monty Monthly newsletter section"
      contains: "NewsletterCarousel or WritingSubscribeCTA"
    - path: src/__tests__/pages/uses.test.tsx
      provides: "Updated assertions matching 'Things I Love' title"
  key_links:
    - from: src/app/uses/page.tsx
      to: src/lib/watching.ts
      via: "import WATCHING_ITEMS"
      pattern: "WATCHING_ITEMS"
    - from: src/app/uses/page.tsx
      to: src/components/v3/video-card.tsx
      via: "VideoCard component render"
      pattern: "VideoCard"
    - from: src/app/writing/page.tsx
      to: src/components/v3/newsletter-carousel.tsx
      via: "NewsletterCarousel import"
      pattern: "NewsletterCarousel"
---

<objective>
Reframe the /uses page as "Things I Love" (fold /watching YouTube grid into a new Watching section) and merge the /newsletter content into /writing (fold NewsletterCarousel + Monty Monthly section). Update the two page tests to match the new titles and content.

Purpose: With /watching and /newsletter deleted (Plan 01), these two pages must absorb their content so nothing is permanently lost. src/lib/watching.ts holds WATCHING_ITEMS already; src/components/v3/newsletter-carousel.tsx already exists. This is a composition task, not a build task.

Output: /uses renders as "Things I Love" with 4 sections; /writing renders essay archive + Monty Monthly section; both page tests updated to reflect new titles.
</objective>

<execution_context>
@/Users/Montster/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Montster/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/17.2-site-information-architecture-new-nav-plus-cut-events-and-ph/17.2-CONTEXT.md
@.planning/phases/17.2-site-information-architecture-new-nav-plus-cut-events-and-ph/17.2-RESEARCH.md
@.planning/phases/17.2-site-information-architecture-new-nav-plus-cut-events-and-ph/17.2-PATTERNS.md
@.planning/phases/17.2-site-information-architecture-new-nav-plus-cut-events-and-ph/17.2-UI-SPEC.md

<interfaces>
From src/lib/watching.ts (data source — NOT deleted in Plan 01):
  WATCHING_ITEMS — array of YouTube video objects used by the deleted /watching page.
  Shape expected by VideoCard: { id: string, title: string, channel?: string, url: string } (verify actual shape before writing).

From src/components/v3/video-card.tsx:
  VideoCard component — renders a YouTube video card. Verify its required props from the file before composing.

From src/components/v3/uses-list.tsx:
  UsesList — takes groups: UsesGroup[] where UsesGroup = { heading: string, items: { term: string, detail: string }[] }.
  NOTE: UsesList uses dt/dd list format. WATCHING_ITEMS are YouTube objects — they do NOT fit UsesGroup items shape.
  Therefore: render Watching section as a sibling to UsesList (not inside it), using VideoCard grid directly.

From src/components/v3/newsletter-carousel.tsx:
  NewsletterCarousel — check its import signature and props before using.
  If it requires async data fetch, use it in the async page component or verify it handles fetching internally.

From src/components/home-v2/writing-subscribe-cta.tsx:
  WritingSubscribeCTA — simpler CTA component already used at bottom of /writing. Fallback if NewsletterCarousel is complex.

From src/app/uses/page.tsx (current — pre-update):
  metadata.title: "Uses | Monty Singer"
  metadata.description: "The tools and software I use daily."
  metadata.alternates.canonical: "/uses"
  PageHero title: "Uses" crumb: "Home / Uses"
  Renders Breadcrumbs + PageHero + RuleStrong + UsesList(USES_DATA) + RuleStrong

From src/app/writing/page.tsx (current — pre-update):
  revalidate = 1800 (keep)
  metadata.title: "Writing | Monty Singer" (keep or update to "Writing / Monty Monthly | Monty Singer")
  metadata.canonical: "/writing" (keep)
  Async: getPublishedPosts() → groupPostsByYear → YearBlock + Card grid
  Ends with WritingSubscribeCTA
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: /uses reframe — "Things I Love" + Watching section (D-06:, D-07:)</name>
  <files>src/app/uses/page.tsx, src/__tests__/pages/uses.test.tsx</files>

  <read_first>
    - src/app/uses/page.tsx (full current file — note metadata, PageHero props, USES_DATA import)
    - src/lib/watching.ts (full file — extract WATCHING_ITEMS shape: {id, title, channel?, url})
    - src/components/v3/video-card.tsx (full file — extract required props interface)
    - src/components/v3/uses-list.tsx (check UsesGroup type to confirm VideoCard cannot be embedded inside it)
    - src/lib/uses.ts (check USES_DATA section headings: Tools / Books / Recommendations or similar — need exact current headings to verify section order)
    - src/__tests__/pages/uses.test.tsx (full current file — note exact string assertions for title and crumb)
    - .planning/phases/17.2-site-information-architecture-new-nav-plus-cut-events-and-ph/17.2-PATTERNS.md §uses/page.tsx
    - .planning/phases/17.2-site-information-architecture-new-nav-plus-cut-events-and-ph/17.2-UI-SPEC.md §Surface 1 and §Copywriting Contract
  </read_first>

  <action>
    Step 1 — Update src/app/uses/page.tsx metadata per D-06::
    Change metadata.title to "Things I Love | Monty Singer".
    Change metadata.description to "Tools, books, and ideas I keep coming back to."
    Keep metadata.alternates.canonical as "/uses" (URL unchanged per D-03:).
    Update openGraph.title to "Things I Love | Monty Singer".
    Update openGraph.description to match.

    Step 2 — Update PageHero per D-06:: title → "Things I Love", crumb → "Home / Things I Love", sub → "Tools, books, and ideas I keep coming back to."

    Step 3 — Add Watching section per D-07:. The VideoCard component does not fit inside UsesList (different data shape). After the existing UsesList(USES_DATA) section, add a new sibling section for Watching:
    Import WATCHING_ITEMS from "@/lib/watching" and VideoCard from "@/components/v3/video-card".
    Add a SectionLabel or heading with text "Watching" (use the existing SectionLabel component pattern from the rest of the file, or an h2 with the current heading style).
    Render a grid of VideoCard components mapping over WATCHING_ITEMS. Use the same 3-column auto-fill grid pattern that was on the /watching page (class: `grid grid-cols-[repeat(auto-fill,minmax(320px,1fr))] gap-6` or the exact class used in src/app/watching/page.tsx — check the deleted file's test or PATTERNS.md for the class).
    Each VideoCard receives the props its interface requires (id, title, url, channel or similar — verify from the actual video-card.tsx interface read above).
    Position the Watching section between the Books section and Recommendations section per D-06: section order: Tools → Books → Watching → Recommendations.
    If USES_DATA currently only has Tools and Recommendations (not Books), add the Watching section after all existing UsesList content and before any final rule. The exact section order per D-06: is: Tools, Books, Watching, Recommendations. Inspect USES_DATA actual headings and arrange accordingly.
    Wrap the entire Watching section in a wrapping div with the same px-6 md:px-40 outer padding pattern as the rest of the page content.

    Step 4 — Update src/__tests__/pages/uses.test.tsx:
    Change the `toBe("Uses | Monty Singer")` assertion (appears in metadata.title and openGraph.title tests) to `toBe("Things I Love | Monty Singer")`.
    Change the breadcrumb 'Uses' assertion `screen.getAllByText("Uses")` to `screen.getAllByText("Things I Love")`.
    Update the describe block string and any comments referencing "Uses page" to "Things I Love page" for clarity.
    No other changes — keep the canonical, Breadcrumbs Home, and UsesList mock tests unchanged.
  </action>

  <acceptance_criteria>
    - `grep "Things I Love" src/app/uses/page.tsx | wc -l` is at least 3 (metadata.title, openGraph.title, PageHero title).
    - `grep "WATCHING_ITEMS" src/app/uses/page.tsx | wc -l` is at least 1 (import present).
    - `grep "VideoCard" src/app/uses/page.tsx | wc -l` is at least 2 (import + usage).
    - `grep "canonical.*uses" src/app/uses/page.tsx | wc -l` is 1 (canonical unchanged).
    - `npm run test -- --run src/__tests__/pages/uses.test.tsx` exits 0.
  </acceptance_criteria>

  <verify>
    <automated>npm run test -- --run src/__tests__/pages/uses.test.tsx</automated>
  </verify>

  <done>/uses page metadata.title = "Things I Love | Monty Singer". Watching section with VideoCard grid present. Canonical stays /uses. uses.test.tsx passes with updated title assertions.</done>
</task>

<task type="auto">
  <name>Task 2: /writing merge — fold Monty Monthly newsletter section (D-03:, D-04:, D-05:)</name>
  <files>src/app/writing/page.tsx, src/__tests__/pages/writing.test.tsx</files>

  <read_first>
    - src/app/writing/page.tsx (full current file — note WritingSubscribeCTA at bottom and full page structure)
    - src/components/v3/newsletter-carousel.tsx (full file — check props interface and whether it fetches its own data or requires props)
    - src/components/home-v2/writing-subscribe-cta.tsx (full file — simpler fallback CTA)
    - src/__tests__/pages/writing.test.tsx (full file — note PageHero title assertion "Writing")
    - .planning/phases/17.2-site-information-architecture-new-nav-plus-cut-events-and-ph/17.2-PATTERNS.md §writing/page.tsx
    - .planning/phases/17.2-site-information-architecture-new-nav-plus-cut-events-and-ph/17.2-UI-SPEC.md §Surface 2 and §Copywriting Contract
  </read_first>

  <action>
    Step 1 — Evaluate NewsletterCarousel complexity per UI-SPEC Option A vs Option B guidance. Read src/components/v3/newsletter-carousel.tsx. If it: (a) accepts no required props and handles its own data fetch internally, OR (b) accepts a simple array prop — use Option A (NewsletterCarousel). If it requires complex async setup that would need significant changes to writing/page.tsx — use Option B (WritingSubscribeCTA already at the bottom is sufficient; add a SectionLabel "Monty Monthly" above it).

    Step 2 — Update src/app/writing/page.tsx per D-04::
    Keep the existing essay archive section (getPublishedPosts, groupPostsByYear, YearBlock + Card grid) entirely unchanged per D-05:.
    Keep the existing WritingSubscribeCTA (it's already at the bottom).
    Add a Monty Monthly section between the final essay RuleStrong and the WritingSubscribeCTA:
    - Add RuleStrong (divider).
    - Add a SectionLabel or heading with text "Monty Monthly" (use existing SectionLabel component, matching the heading style elsewhere on the page).
    - If using Option A: render NewsletterCarousel (import from "@/components/v3/newsletter-carousel") with whatever props it accepts. Place it between the SectionLabel and the existing WritingSubscribeCTA.
    - If using Option B: the SectionLabel "Monty Monthly" above the existing WritingSubscribeCTA is sufficient; no new component needed.
    Ensure no link to /newsletter remains anywhere in the file per D-04:.
    Keep revalidate = 1800 unchanged.
    Keep metadata canonical "/writing" unchanged per D-03:.

    Step 3 — Metadata decision (Claude's discretion): The UI-SPEC gives the option to keep the page title as "Writing" or change to "Writing / Monty Monthly | Monty Singer". Keep it as "Writing | Monty Singer" (no change) to avoid breaking the existing writing.test.tsx title assertion and keep the nav label simple. The merged nature is communicated by the Monty Monthly section on the page, not the title.

    Step 4 — writing.test.tsx: Since the page title stays "Writing", the existing test "renders PageHero with title Writing" still passes. No test updates required UNLESS NewsletterCarousel is added and it introduces new mocks. If adding NewsletterCarousel and it causes test failures (e.g., unresolved import or async issues), add a vi.mock for "@/components/v3/newsletter-carousel" that returns a simple div. Document this in the task output.
  </action>

  <acceptance_criteria>
    - `grep -c "newsletter\|/newsletter" src/app/writing/page.tsx` equals 0 if the word appears in a link href — specifically `grep 'href.*newsletter' src/app/writing/page.tsx | wc -l` equals 0.
    - `grep "Monty Monthly" src/app/writing/page.tsx | wc -l` is at least 1 (section label or heading present).
    - `grep "canonical.*writing" src/app/writing/page.tsx | wc -l` equals 1 (canonical unchanged).
    - `npm run test -- --run src/__tests__/pages/writing.test.tsx` exits 0.
    - `npm run build 2>&1 | tail -5` shows no TypeScript errors related to writing/page.tsx or uses/page.tsx.
  </acceptance_criteria>

  <verify>
    <automated>npm run test -- --run src/__tests__/pages/uses.test.tsx src/__tests__/pages/writing.test.tsx</automated>
  </verify>

  <done>/writing renders essay archive + Monty Monthly section (NewsletterCarousel or SectionLabel + WritingSubscribeCTA). No href to /newsletter. Canonical stays /writing. writing.test.tsx passes. /uses and /writing page tests both green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client browser → /uses, /writing | SSR pages; no user input surfaces changed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17.2-04 | Information Disclosure | WATCHING_ITEMS import | accept | WATCHING_ITEMS is a static local data array (YouTube public URLs, no secrets). No risk introduced by importing into a different page. |
| T-17.2-05 | Tampering | NewsletterCarousel data fetch | accept | If NewsletterCarousel fetches external Substack feed, it already existed on /newsletter page; no new trust boundary. External fetch is read-only. |
| T-17.2-SC | Tampering | npm/pip installs | accept | No new packages installed this plan. No supply-chain risk. |
</threat_model>

<verification>
Run both page tests together:
`npm run test -- --run src/__tests__/pages/uses.test.tsx src/__tests__/pages/writing.test.tsx`

Additional checks:
- `grep "Things I Love" src/app/uses/page.tsx` returns non-empty.
- `grep "WATCHING_ITEMS" src/app/uses/page.tsx` returns non-empty.
- `grep "Monty Monthly" src/app/writing/page.tsx` returns non-empty.
- `grep 'href.*newsletter' src/app/writing/page.tsx` returns empty.
</verification>

<success_criteria>
- /uses page: title "Things I Love | Monty Singer", canonical /uses, Watching section with VideoCard grid from WATCHING_ITEMS, section order Tools/Books/Watching/Recommendations.
- /writing page: essay archive unchanged, Monty Monthly section added, no /newsletter links, canonical /writing.
- uses.test.tsx and writing.test.tsx both pass.
- No TypeScript build errors introduced.
</success_criteria>

<output>
Create `.planning/phases/17.2-site-information-architecture-new-nav-plus-cut-events-and-ph/17.2-03-SUMMARY.md` when done.
</output>
