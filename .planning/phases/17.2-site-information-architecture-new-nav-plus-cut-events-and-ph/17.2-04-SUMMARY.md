---
phase: 17.2-site-information-architecture-new-nav-plus-cut-events-and-ph
plan: 04
subsystem: validation
tags: [validation-gate, ia, links, seo, no-file-changes]

# Dependency graph
requires:
  - 17.2-01 (routing + redirects + sitemap + test lockstep)
  - 17.2-02 (nav/footer link sweep)
  - 17.2-03 (/uses reframe + /writing merge)
provides:
  - Validation evidence that all 10 IA decisions (D-01..D-10) are satisfied
affects: []

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Post-merge link gate: repo-wide grep for cut-route path literals catches orphaned internal links outside plan scope"

key-files:
  created: []
  modified: []

key-decisions:
  - "D-01..D-10: all verified against the live codebase via automated gate (see Validation Results)"
  - "Gate found two orphaned cut-route links outside plan scope (/about page, dead InkFooter) — fixed in commits f936aa5 + 1b403c4"

# Verification
self-check: PASSED
---

# Plan 17.2-04 — Validation Gate

**No file changes.** This plan gates on the output of Plans 01–03 and confirms the new IA is internally consistent with no cut-route links.

## Task 1 — Automated Validation Suite (PASSED)

| Check | Command | Result |
|-------|---------|--------|
| Cut-route link sweep | `grep -rnE '"/(events\|photos\|links\|newsletter\|watching\|specimen\|v3-specimen)"' src/` (excl. tests/sitemap/redirects) | ✅ CLEAN — zero cut-route literals in app/component code |
| Routes deleted | `test -d src/app/{events,photos,links,newsletter,watching,specimen,v3-specimen}` | ✅ all 7 route dirs gone (404 for specimen/v3-specimen; 301 for the other 5) |
| Redirects registered | `next.config.ts` async `redirects()` | ✅ 5 new 301s: /events→/, /photos→/, /links→/about, /newsletter→/writing, /watching→/uses |
| Sitemap trimmed | `src/app/sitemap.ts` | ✅ 6 static routes (/, /about, /prometheus, /projects, /writing, /uses) |
| Full test suite | `npx vitest run` | ✅ 121 passed; the only 4 failures are **pre-existing Phase 17.1 homepage tests** (section-building HD-04, explorative-homepage TD-03/HD-05), confirmed failing identically at the pre-17.2 commit 4817f97 — unrelated to and untouched by this phase |
| Production build | `npm run build` | ✅ exit 0; route table confirms cut routes gone, /about /projects /prometheus /uses /writing present |

### Gate-surfaced fixes (orphaned links outside plan scope)
The repo-wide grep caught two cut-route links the nav/footer sweep (Plan 02) did not cover:
- `src/app/about/page.tsx` — "Monty Monthly" Subscribe link `/newsletter` → repointed to `/writing` (commit `f936aa5`, per D-04).
- `src/components/home-v2/ink-footer.tsx` — preserved v2 **dead code** (unrendered; layout uses ConditionalFooter→V3Footer) held `/newsletter`, `/links`, `/photos` → repointed to new-IA targets `/writing`, `/about`, `/uses` (commit `1b403c4`).

## Task 2 — Human Visual Checkpoint (auto-approved under `--auto`)

⚡ Auto-approved checkpoint (auto-mode). A human visual pass was **not** performed by a person; structural verification stands in for it.

**Recommended before ship (Phase 18 QA):** manually load `/uses` (Things I Love — Tools→Books→Watching→Recommendations, YouTube grid renders), `/writing` (essay archive + Monty Monthly carousel), the lean 4-item primary nav, and the V3 footer with woven socials; spot-check the 5 redirects resolve. Captured as a manual-only verification in `17.2-VALIDATION.md`.

## Decision Coverage — D-01..D-10

All 10 locked decisions verified against the live codebase and satisfied.
