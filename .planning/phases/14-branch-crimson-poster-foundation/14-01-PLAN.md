---
phase: 14-branch-crimson-poster-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/app/layout.tsx
  - package.json
  - .planning/phases/14-branch-crimson-poster-foundation/14-DEPLOY-BASELINE.md
autonomous: true
requirements: [DS-01, DS-02, DS-03, DQ-01]
must_haves:
  truths:
    - "A long-lived v3 git branch exists, created from claude/phase-8-resume, with main as a verified ancestor"
    - "The current production alias target is recorded so the eventual Phase 18 swap target is unambiguous"
    - "globals.css renders the Crimson Poster palette: canvas #d93c1e, accent black #0a0503, ink #120604, no gradients"
    - "Display headings can be lifted by a hard black drop shadow via a reusable .sig utility; an outline (-webkit-text-stroke) variant exists"
    - "Space Grotesk (display) and JetBrains Mono (mono) load via next/font/google as CSS variables on a defined @theme type scale"
    - "The v2 warm-paper palette, second :root block, .section-inverted block, and v1 compat aliases are gone"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Crimson Poster @theme inline tokens, .sig utility, @keyframes scroll, reskinned .prose"
      contains: "--color-bg: #d93c1e"
    - path: "src/app/layout.tsx"
      provides: "Space Grotesk + JetBrains Mono next/font wiring; crimson body classes"
      contains: "Space_Grotesk"
    - path: ".planning/phases/14-branch-crimson-poster-foundation/14-DEPLOY-BASELINE.md"
      provides: "Recorded production alias target + v3 branch name for the Phase 18 swap"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/app/globals.css"
      via: "--font-display / --font-mono CSS variables bound in @theme inline"
      pattern: "var\\(--font-space-grotesk\\)"
---

<objective>
Create the long-lived `v3` branch, capture the production-alias baseline, and replace the v2 warm-paper design foundation with the fixed "Crimson Poster" theme — Tailwind v4 `@theme inline` tokens, the signature display-shadow utility, and the Space Grotesk / JetBrains Mono fonts.

Purpose: This is the foundation every later primitive and page composes against. Tokens + fonts must land before any primitive can consume them as utilities.
Output: A `v3` branch on its own preview baseline, a fully reskinned `globals.css`, swapped fonts in `layout.tsx`, and a recorded deploy baseline.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-branch-crimson-poster-foundation/14-CONTEXT.md
@.planning/phases/14-branch-crimson-poster-foundation/14-PATTERNS.md

<interfaces>
<!-- Tailwind v4 token-as-utility idiom (existing globals.css lines 25-30) — REUSE this mechanism. -->
<!-- Tokens in @theme inline auto-generate utilities: --color-bg → bg-bg/text-bg/border-bg; -->
<!-- --text-3xl → text-3xl (also applies --line-height/--letter-spacing/--font-weight sub-props). -->
<!-- Existing primitives consume tokens as utilities (text-display, text-ink, bg-paper, border-rule). -->

<!-- Existing font binding (globals.css lines 85-87): --font-sans: var(--font-inter); -->
<!-- Existing Inter wiring (layout.tsx lines 13-17, 59): -->
<!--   const inter = Inter({ variable: "--font-inter", subsets: ["latin"], weight: ["400","700"] }); -->
<!--   <html className={inter.variable}> -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the v3 branch and record the deploy baseline (D-01, D-02, DQ-01)</name>
  <files>.planning/phases/14-branch-crimson-poster-foundation/14-DEPLOY-BASELINE.md</files>
  <read_first>
    - .planning/phases/14-branch-crimson-poster-foundation/14-CONTEXT.md (D-01, D-02, D-03 — branch + alias decisions)
    - $HOME/.claude/projects/-Users-Montster-MSizzle-Personal-Website/memory/vercel-prod-deploy-gotchas.md (NEVER use --prebuilt --prod; alias drift)
  </read_first>
  <action>
    Create a long-lived local branch named `v3` from the current branch (`claude/phase-8-resume`) using `git branch v3` (do NOT use `git checkout`/`git switch` to leave it — stay on the current working branch; the executor's remaining work happens here and merges/continues per the team's flow). Verify `main` is an ancestor with `git merge-base --is-ancestor main v3` (exit 0 = pass). Record the verification result. Then capture the current production alias target: run `npx vercel alias ls` (or `npx vercel inspect <prod-deployment>`) to read what `montysinger.com` currently points at; if the Vercel CLI is unauthenticated, create a checkpoint is NOT required here — instead record the alias target as "TBD — capture before Phase 18 swap" with the exact command to run, so the baseline doc is unambiguous. Write 14-DEPLOY-BASELINE.md containing: the v3 branch name, the `main`-ancestor verification result, the captured (or TBD) production alias target, and an explicit note "NEVER use --prebuilt --prod on this project" per the memory file. Do NOT deploy production in this phase (D-02: production untouched until Phase 18).
  </action>
  <verify>
    <automated>git -C "$PWD" rev-parse --verify v3 && git -C "$PWD" merge-base --is-ancestor main v3 && echo "v3 ok, main ancestor"</automated>
  </verify>
  <acceptance_criteria>
    - `git rev-parse --verify v3` succeeds (the v3 branch exists)
    - `git merge-base --is-ancestor main v3` exits 0 (main is an ancestor of v3)
    - 14-DEPLOY-BASELINE.md exists and contains the string "NEVER use --prebuilt --prod"
    - 14-DEPLOY-BASELINE.md records either the captured production alias target for montysinger.com or an explicit "TBD" with the exact capture command
    - No production deploy was performed (no `vercel deploy --prod` / `--prebuilt --prod` invocation)
  </acceptance_criteria>
  <done>The v3 branch exists with main as a verified ancestor, and the deploy baseline doc records the alias target and the prod-deploy guardrail.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Replace globals.css with the Crimson Poster theme + sig utility (D-04, D-05, D-06)</name>
  <files>src/app/globals.css</files>
  <read_first>
    - src/app/globals.css (the file being replaced — current @theme inline warm-paper block, the second :root block lines 90-99, .section-inverted lines 152-164, v1 compat aliases lines 16-23)
    - .planning/sketches/themes/default.css (locked token VALUES, lines 9-53)
    - .planning/sketches/002-full-site-model/assets/site.css (.grad-text line 15; .prose lines 82-90; @keyframes scroll line 80)
    - .planning/phases/14-branch-crimson-poster-foundation/14-PATTERNS.md (token-as-utility GOTCHA; blocks-to-DELETE list; sig-type utility guidance)
  </read_first>
  <action>
    In the `@theme inline` block, DELETE the v2 warm-paper palette, the v1 compatibility aliases (current lines 16-23), and the entire v2 editorial type scale. Add the Crimson Poster color tokens translated into @theme token names so Tailwind generates utilities: --color-bg: #d93c1e, --color-bg-2: #c8341a, --color-surface: #cc3719, --accent: #0a0503, --color-border: rgba(10,5,3,0.26), --color-border-strong: rgba(10,5,3,0.5), --color-text: #120604, --color-text-dim: rgba(10,5,3,0.74), --color-text-muted: rgba(10,5,3,0.52). Add the type scale as @theme tokens: --text-xs: 0.72rem, --text-sm: 0.85rem, --text-base: 1rem, --text-lg: 1.2rem, --text-xl: 1.6rem, --text-2xl: 2.2rem, --text-3xl: 3.2rem, --text-mega: clamp(3.5rem, 16vw, 16rem) (translate the prototype's :root names per PATTERNS.md — do NOT paste the prototype's plain :root block). Bind fonts: --font-display: var(--font-space-grotesk), --font-sans: var(--font-space-grotesk), --font-mono: var(--font-jetbrains-mono). DELETE the entire second `:root` block (current lines 90-99) and the `.section-inverted` block (current lines 152-164). Update `body` to use the crimson canvas/ink (font-family var(--font-sans); the page-level bg/text come from layout.tsx body classes per Task 3). Reskin `.prose` to the crimson values from site.css lines 82-90 (max-width 68ch, color var(--color-text-dim), .prose h2 display font near-black, .prose a accent underline, blockquote 2px accent left border). Add the signature utility per D-06: a `.sig` class = `color: var(--sig); text-shadow: var(--sig-shadow);` and an outline variant `.sig-out` = `-webkit-text-stroke: 2px var(--accent); color: transparent; text-shadow: none;`, declaring `--sig: var(--color-bg)` and `--sig-shadow: 0.055em 0.055em 0 #0a0503` (use the literal hex, not var, to avoid @theme/:root resolution order issues — confirm it resolves). Add `@keyframes scroll { to { transform: translateX(-50%); } }` for the marquee. Do NOT add the prototype's `@import url(...googleapis...)` line — fonts come from next/font (Task 3). No gradients anywhere.
  </action>
  <verify>
    <automated>grep -v '^\s*/\*' src/app/globals.css | grep -c -- '--color-bg: #d93c1e' && grep -q 'section-inverted' src/app/globals.css && echo "FAIL: section-inverted still present" || echo "section-inverted removed"</automated>
  </verify>
  <acceptance_criteria>
    - globals.css contains `--color-bg: #d93c1e`, `--accent: #0a0503`, and `--color-text: #120604`
    - globals.css contains `--text-mega: clamp(3.5rem, 16vw, 16rem)`
    - globals.css contains a `.sig` rule with `text-shadow: var(--sig-shadow)` and a `.sig-out` rule with `-webkit-text-stroke`
    - globals.css contains `@keyframes scroll`
    - globals.css binds `--font-display: var(--font-space-grotesk)` and `--font-mono: var(--font-jetbrains-mono)`
    - `grep section-inverted src/app/globals.css` returns nothing (block removed)
    - `grep -- '--color-paper' src/app/globals.css` returns nothing (v2 warm-paper palette removed)
    - `grep -- 'color-background\|color-foreground' src/app/globals.css` returns nothing (v1 compat aliases removed)
    - No `@import url(` referencing fonts.googleapis exists in globals.css
    - No gradient syntax (`linear-gradient`, `radial-gradient`) appears in globals.css
  </acceptance_criteria>
  <done>globals.css renders the single fixed Crimson Poster theme with the reusable sig utility, the marquee keyframes, and all v2/v1 palette/dark-mode artifacts removed.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Swap fonts to Space Grotesk + JetBrains Mono and recolor body (D-09, D-05)</name>
  <files>src/app/layout.tsx, package.json</files>
  <read_first>
    - src/app/layout.tsx (current Inter next/font wiring lines 13-17, html className line 59, body classes line 62)
    - .planning/sketches/themes/default.css (font families + weights 400;500;700, lines 7, 38-40)
    - .planning/phases/14-branch-crimson-poster-foundation/14-PATTERNS.md (D-09 swap details; next-themes-is-unused correction; GOTCHA on next/font vs @import)
    - package.json (next-themes line 22 — unused dependency)
  </read_first>
  <action>
    In layout.tsx: remove the `Inter` import and its config object. Import `Space_Grotesk` and `JetBrains_Mono` from `next/font/google`. Configure `const spaceGrotesk = Space_Grotesk({ variable: "--font-space-grotesk", subsets: ["latin"], weight: ["400", "500", "700"], display: "swap" })` and `const jetbrainsMono = JetBrains_Mono({ variable: "--font-jetbrains-mono", subsets: ["latin"], weight: ["400", "500", "700"], display: "swap" })` (weight 500 is required for .row .ti / .card h3 medium per the prototype). Compose both variables on `<html>`: `className={`${spaceGrotesk.variable} ${jetbrainsMono.variable}`}` (or via cn from @/utils/cn). Replace the body classes `bg-background text-foreground` (current line 62) — those v1-alias utilities are removed by D-05 — with `bg-bg text-text` (the new crimson token utilities). Keep `min-h-screen` and `antialiased`. Leave the existing providers (LenisProvider > MotionProvider), Navigation, MainOffset, InkFooter, VisitSurvey, UmamiAnalytics in place — the nav/footer rebuild is Phases 15-16 and is OUT of scope here. Per Claude's discretion (D-05 / D-09 note) and the PATTERNS.md correction that next-themes is imported nowhere in src/, REMOVE the unused `next-themes` dependency from package.json (run `npm uninstall next-themes`); there is no provider to unwire.
  </action>
  <verify>
    <automated>grep -q 'Space_Grotesk' src/app/layout.tsx && grep -q 'JetBrains_Mono' src/app/layout.tsx && grep -q 'Inter' src/app/layout.tsx && echo "FAIL: Inter still imported" || echo "fonts swapped"</automated>
  </verify>
  <acceptance_criteria>
    - layout.tsx imports `Space_Grotesk` and `JetBrains_Mono` from `next/font/google`
    - layout.tsx no longer imports or references `Inter`
    - The Space Grotesk config includes `weight: ["400", "500", "700"]` and `variable: "--font-space-grotesk"`
    - The JetBrains Mono config includes `variable: "--font-jetbrains-mono"`
    - The `<html>` element composes both font variables in its className
    - The `<body>` element uses `bg-bg text-text` (not `bg-background text-foreground`)
    - `grep next-themes package.json` returns nothing (dependency removed)
    - `npm ls next-themes` reports it is no longer a dependency
  </acceptance_criteria>
  <done>layout.tsx wires Space Grotesk + JetBrains Mono via next/font as CSS variables, the body uses crimson token utilities, Inter is gone, and the unused next-themes dependency is removed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev → npm registry | `npm uninstall next-themes` mutates the dependency tree / lockfile |
| dev → Vercel CLI | alias inspection reads production deployment metadata |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering | accidental production deploy during baseline capture | mitigate | Task 1 explicitly forbids `vercel deploy --prod` / `--prebuilt --prod`; baseline capture is read-only (`alias ls` / `inspect`) |
| T-14-02 | Denial of Service | broken font/token swap crashing all routes | mitigate | Build gate in Plan 04 (`next build`) blocks the phase if the foundation does not compile |
| T-14-SC | Tampering | npm dependency removal (`npm uninstall next-themes`) | accept | Removal only; no new packages installed this plan. No `[ASSUMED]`/`[SUS]` installs to gate. PATTERNS.md confirms next-themes is imported nowhere in src/, so removal is safe. |
</threat_model>

<verification>
- `git rev-parse --verify v3` succeeds and `main` is an ancestor of `v3`
- globals.css contains the Crimson Poster tokens, `.sig`/`.sig-out`, and `@keyframes scroll`; all v2/v1 palette + dark-mode blocks are gone
- layout.tsx loads Space Grotesk + JetBrains Mono via next/font and recolors the body
- next-themes is removed from package.json
- (Full `next build` gate runs in Plan 04 after primitives + showcase exist)
</verification>

<success_criteria>
- The v3 branch exists, with main verified as an ancestor, and the deploy baseline is recorded (DQ-01)
- The Crimson Poster palette and type scale are live as @theme tokens with no gradients (DS-01, DS-03)
- The signature display treatment is a reusable utility with an outline variant (DS-02)
- Space Grotesk + JetBrains Mono load via next/font (DS-03)
</success_criteria>

<output>
Create `.planning/phases/14-branch-crimson-poster-foundation/14-01-SUMMARY.md` when done. Record the captured production alias target (or the exact command to capture it before Phase 18) in the summary as well as the baseline doc.
</output>
