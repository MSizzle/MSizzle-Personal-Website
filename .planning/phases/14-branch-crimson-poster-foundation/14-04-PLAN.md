---
phase: 14-branch-crimson-poster-foundation
plan: 04
type: execute
wave: 3
depends_on: ["14-01", "14-02", "14-03"]
files_modified:
  - src/app/v3-specimen/page.tsx
autonomous: true
requirements: [DS-01, DS-02, DS-03, DS-04, DS-05, DQ-01]
must_haves:
  truths:
    - "A noindex primitives showcase route renders the Crimson Poster palette, type scale, and every v3 primitive in isolation"
    - "The showcase demonstrates the crimson sig display treatment and the outline-stroke variant"
    - "The showcase route does NOT collide with the existing /specimen route"
    - "The production build (next build) compiles cleanly on the v3 branch, proving foundation readiness for a Vercel preview (DQ-01)"
  artifacts:
    - path: "src/app/v3-specimen/page.tsx"
      provides: "v3 primitives + tokens showcase, robots noindex/nofollow"
      contains: "robots"
  key_links:
    - from: "src/app/v3-specimen/page.tsx"
      to: "src/components/v3/*"
      via: "imports every v3 primitive and renders it with a labeled caption"
      pattern: "@/components/v3/"
---

<objective>
Deliver the v3 primitives showcase route (D-08) so the Crimson Poster system can be reviewed in isolation, then run the production build gate that proves the foundation is preview-ready (DQ-01).

Purpose: This is the human-reviewable proof that DS-01..DS-05 are met, and the build gate confirms the new tokens/fonts/primitives compile on the v3 branch before any pages are built on top.
Output: A noindex `/v3-specimen` route + a clean `next build`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/14-branch-crimson-poster-foundation/14-CONTEXT.md
@.planning/phases/14-branch-crimson-poster-foundation/14-PATTERNS.md
@.planning/phases/14-branch-crimson-poster-foundation/14-02-SUMMARY.md
@.planning/phases/14-branch-crimson-poster-foundation/14-03-SUMMARY.md

<interfaces>
<!-- src/app/specimen/page.tsx is the EXACT structural template. Mirror its shape: -->
<!--   export const metadata = { ..., robots: { index: false, follow: false } }; -->
<!--   const swatches = [...]; const typeSpecimens = [...]; -->
<!--   <main> with a Palette section (swatch grid), a Type Scale section, and a Primitives section -->
<!--   that renders each primitive under a `text-label` caption. -->
<!-- It imports from @/components/editorial/*; the v3 showcase imports from @/components/v3/*. -->

<!-- v3 primitive exports come from the Plan 02 + Plan 03 SUMMARY files (read them for exact prop signatures): -->
<!--   Plan 02: Rule, RuleStrong, SectionLabel, Chip, ListRow, Button, BigList, PageHero, Marquee -->
<!--   Plan 03: Card, VideoCard, NewsletterCarousel, UsesList, Reveal -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the /v3-specimen showcase route</name>
  <files>src/app/v3-specimen/page.tsx</files>
  <read_first>
    - src/app/specimen/page.tsx (EXACT structural template — metadata noindex, swatches array, typeSpecimens array, sectioned layout, labeled-caption primitive demos)
    - .planning/phases/14-branch-crimson-poster-foundation/14-02-SUMMARY.md (Plan 02 primitive exports + prop signatures)
    - .planning/phases/14-branch-crimson-poster-foundation/14-03-SUMMARY.md (Plan 03 primitive exports + prop signatures + the .cards grid container classes)
    - .planning/sketches/themes/default.css (palette + type-scale values to label in the swatch/type sections)
  </read_first>
  <action>
    Create `src/app/v3-specimen/page.tsx` modeled on `src/app/specimen/page.tsx`. Use a DISTINCT route path `/v3-specimen` so it does NOT collide with the existing `/specimen` (both stay until Phase 16). Export `metadata` with `robots: { index: false, follow: false }` (copy the noindex pattern). Page body = `<main className="bg-bg text-text min-h-screen ...">`. Build three sections: (1) PALETTE — a swatch grid driven by a `swatches` const using the crimson tokens: bg #d93c1e, bg-2 #c8341a, surface #cc3719, accent #0a0503, text #120604, text-dim, text-muted, border, border-strong — render each as a token utility swatch with its name/hex/role caption. (2) TYPE SCALE — a `typeSpecimens` const for text-xs … text-3xl and text-mega, each rendered with `font-display` and labeled with its size; include at least one sample wearing the `.sig` utility and one wearing `.sig-out` so DS-02 is visibly demonstrated. (3) PRIMITIVES — import EVERY v3 primitive (Rule, RuleStrong, SectionLabel, Chip, ListRow [default + big], Button [default + accent], BigList [with one outline item], PageHero [normal + outline], Marquee, Card [wrapped in the .cards grid container per the Plan 03 summary], VideoCard, NewsletterCarousel, UsesList, Reveal [wrapping a sample block]) and render each under a `text-label` / mono uppercase caption, mirroring specimen's PRIM-NN structure. Provide realistic sample props (titles, hrefs="#", meta strings, issues/groups arrays). This is a server component page; Marquee and Reveal are client components and may be rendered directly inside it.
  </action>
  <verify>
    <automated>grep -q 'robots' src/app/v3-specimen/page.tsx && grep -c '@/components/v3/' src/app/v3-specimen/page.tsx && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E 'app/v3-specimen' || echo "showcase typechecks"</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/v3-specimen/page.tsx` exists (route path `/v3-specimen`, distinct from `/specimen`)
    - `metadata` exports `robots: { index: false, follow: false }`
    - The page imports all 14 v3 primitives from `@/components/v3/*` (Rule, RuleStrong, SectionLabel, Chip, ListRow, Button, BigList, PageHero, Marquee, Card, VideoCard, NewsletterCarousel, UsesList, Reveal)
    - The Palette section renders swatches for the crimson tokens (#d93c1e canvas, #0a0503 accent, #120604 ink)
    - At least one type sample uses `.sig` and one uses `.sig-out` (DS-02 demonstrated)
    - BigList includes one item with the outline variant; Button shows both default and accent
    - `npx tsc --noEmit` reports no type errors in the showcase page
  </acceptance_criteria>
  <done>The /v3-specimen route renders the Crimson Poster palette, type scale (with the sig + outline variants), and every v3 primitive under labeled captions, noindexed.</done>
</task>

<task type="auto">
  <name>Task 2: Production build gate (DQ-01 readiness) + reduced-motion confirmation</name>
  <files>src/app/v3-specimen/page.tsx</files>
  <read_first>
    - package.json (build script: `next build`)
    - src/components/v3/marquee.tsx (reduced-motion branch)
    - src/components/v3/reveal.tsx (reduced-motion branch)
    - $HOME/.claude/projects/-Users-Montster-MSizzle-Personal-Website/memory/vercel-prod-deploy-gotchas.md (do NOT --prebuilt --prod; this is a LOCAL build gate, not a prod deploy)
  </read_first>
  <action>
    Run `npm run build` (Next.js production build) and confirm it exits 0 with the `/v3-specimen` route in the build output. This is a LOCAL readiness gate proving the new tokens/fonts/primitives compile on the v3 branch — do NOT run `vercel deploy --prod` or `--prebuilt --prod` (production stays untouched until Phase 18 per D-02). If the build fails, fix the offending v3 primitive or showcase code (do NOT modify production-deploy behavior) and re-run until clean. Separately, statically confirm the DS-05 reduced-motion contract: grep that both `src/components/v3/marquee.tsx` and `src/components/v3/reveal.tsx` call `useReducedMotion` and have a branch that omits the animation while still rendering content. Record in the summary the exact `vercel deploy` (non-prod, preview) command the developer can run next to put `/v3-specimen` on a Vercel preview URL (the actual preview deploy is the developer's authenticated step and is captured/confirmed in the phase summary, not auto-run here unless the CLI is already authenticated).
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -40 ; echo "exit:$?" ; grep -l 'useReducedMotion' src/components/v3/marquee.tsx src/components/v3/reveal.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0 (production build compiles cleanly on the v3 branch)
    - The build output lists the `/v3-specimen` route
    - Both `src/components/v3/marquee.tsx` and `src/components/v3/reveal.tsx` contain `useReducedMotion` and a no-animation branch (DS-05)
    - No `vercel deploy --prod` / `--prebuilt --prod` was executed (production untouched, D-02)
    - The summary records the exact preview-deploy command for the developer to run
  </acceptance_criteria>
  <done>The production build passes cleanly with /v3-specimen present, the reduced-motion contract is confirmed, and the foundation is preview-ready without touching production.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build → output | `next build` compiles the foundation; a regression here would break every future page |
| dev → Vercel | preview deploy is an authenticated developer step; production must remain untouched |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-07 | Tampering | accidental production deploy from the build gate | mitigate | Task 2 forbids `vercel deploy --prod` / `--prebuilt --prod`; the gate is a local `next build` only |
| T-14-08 | Information Disclosure | showcase route indexed by search engines | mitigate | metadata `robots: { index: false, follow: false }` on /v3-specimen (mirrors /specimen) |
| T-14-09 | Denial of Service | foundation regression breaks all routes | mitigate | `next build` gate blocks phase completion until the foundation compiles cleanly |
</threat_model>

<verification>
- `/v3-specimen` renders all primitives + palette + type scale, noindexed
- `npm run build` exits 0 with the route present
- Marquee and Reveal honor reduced motion (DS-05)
- Production is untouched (no prod deploy)
</verification>

<success_criteria>
- The primitives showcase demonstrates the full Crimson Poster system in isolation (DS-01..DS-05, D-08)
- The production build compiles cleanly on the v3 branch, proving preview readiness (DQ-01)
</success_criteria>

<output>
Create `.planning/phases/14-branch-crimson-poster-foundation/14-04-SUMMARY.md` when done. Record the `next build` result, the `/v3-specimen` URL path, and the exact (non-prod) Vercel preview-deploy command the developer should run to publish the preview.
</output>
