---
phase: 14-branch-crimson-poster-foundation
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - src/components/v3/rule.tsx
  - src/components/v3/rule-strong.tsx
  - src/components/v3/section-label.tsx
  - src/components/v3/list-row.tsx
  - src/components/v3/big-list.tsx
  - src/components/v3/button.tsx
  - src/components/v3/page-hero.tsx
  - src/components/v3/chip.tsx
  - src/components/v3/marquee.tsx
autonomous: true
requirements: [DS-02, DS-04, DS-05]
must_haves:
  truths:
    - "A reusable set of static brutalist primitives exists in src/components/v3/: Rule, RuleStrong, SectionLabel, ListRow, BigList, Button, PageHero, Chip, Marquee"
    - "Clickable list rows invert to background=text / color=bg on hover, with a reveal arrow"
    - "BigList renders huge sig-shadow display links with an outline (-webkit-text-stroke) variant"
    - "Buttons have a bordered default and a filled accent variant, both with hover-invert"
    - "PageHero renders a crimson sig-treatment h1 with crumb and sub, plus an outline variant"
    - "The marquee scrolls via the @keyframes scroll animation and disables under prefers-reduced-motion"
  artifacts:
    - path: "src/components/v3/list-row.tsx"
      provides: "Clickable hover-invert index row (grid 60px/1fr/auto, numeral, title, arrow, excerpt, meta)"
      exports: ["ListRow"]
    - path: "src/components/v3/big-list.tsx"
      provides: "Big-type stacked clickable display links with outline variant"
      exports: ["BigList"]
    - path: "src/components/v3/button.tsx"
      provides: "Button + accent variant supporting Link and action modes"
      exports: ["Button"]
    - path: "src/components/v3/marquee.tsx"
      provides: "Infinite-scroll marquee band with duplicated track and reduced-motion fallback"
      exports: ["Marquee"]
    - path: "src/components/v3/page-hero.tsx"
      provides: "Page-title hero with sig-treatment h1, crumb, sub, outline variant"
      exports: ["PageHero"]
  key_links:
    - from: "src/components/v3/page-hero.tsx"
      to: "src/app/globals.css"
      via: ".sig / .sig-out utility class from D-06"
      pattern: "sig"
---

<objective>
Build the FRESH v3 static brutalist primitives (D-07), ported pixel-faithfully from the prototype's `assets/site.css`, consuming the Crimson Poster tokens from Plan 01 as Tailwind utilities. These are the non-animated building blocks every later page composes.

Purpose: Pages in Phases 15-16 compose these. They must match the prototype's look exactly and follow the established primitive conventions (named exports, typed Props, `cn`, `next/link`).
Output: Nine primitive components under `src/components/v3/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/14-branch-crimson-poster-foundation/14-CONTEXT.md
@.planning/phases/14-branch-crimson-poster-foundation/14-PATTERNS.md

<interfaces>
<!-- Project utilities and conventions the executor MUST use directly — no exploration needed. -->

<!-- cn merge util (import EXACTLY this path — there is no @/lib/utils here): -->
import { cn } from "@/utils/cn";

<!-- Existing primitive shape (src/components/editorial/list-row.tsx) — copy the prop/cn/Link convention: -->
//   import Link from "next/link";
//   import type { ReactNode } from "react";
//   type Props = { title: ReactNode; href: string; extra?: ReactNode; meta?: ReactNode; big?: boolean };
//   export function ListRow({ title, href, extra, meta, big = false }: Props) { return <Link href={href} className={cn(...)}>...</Link>; }

<!-- SectionLabel convention (src/components/editorial/section-label.tsx): { children, numeral } flex-baseline-between -->
<!-- AllLink/Button convention (src/components/editorial/all-link.tsx): { children, href } Link + className -->

<!-- Crimson token utilities now available from Plan 01 @theme inline: -->
<!--   bg-bg / text-bg / border-bg (#d93c1e), bg-bg-2, bg-surface -->
<!--   text-accent / bg-accent / border-accent (#0a0503) -->
<!--   text-text (#120604), text-text-dim, text-text-muted, border-border, border-border-strong -->
<!--   text-xs … text-3xl, text-mega; font-display / font-mono / font-sans -->
<!--   .sig (color:var(--sig);text-shadow:var(--sig-shadow)) and .sig-out (-webkit-text-stroke) utilities -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rule, RuleStrong, SectionLabel, Chip (static atoms)</name>
  <files>src/components/v3/rule.tsx, src/components/v3/rule-strong.tsx, src/components/v3/section-label.tsx, src/components/v3/chip.tsx</files>
  <read_first>
    - src/components/editorial/rule.tsx (structural analog — exact shape)
    - src/components/editorial/section-label.tsx (structural analog — { children, numeral })
    - .planning/sketches/002-full-site-model/assets/site.css (.rule line 48; .rule-strong line 49; .slabel/.shead lines 51-53; .chip line 102)
    - .planning/phases/14-branch-crimson-poster-foundation/14-PATTERNS.md (Rule/SectionLabel/Chip pattern assignments)
  </read_first>
  <action>
    Create four named-export server components (no "use client"; typed Props; consume crimson token utilities). Rule: an `<hr>` styled per site.css line 48 — height 1px, background var(--color-border) → use `border-0 border-t border-border` (the existing editorial rule uses `border-t border-rule`; swap to `border-border`), aria-hidden. RuleStrong per site.css line 49 — 2px, background var(--color-text) at opacity 0.85 → `border-0 border-t-2 border-text opacity-85` (or a styled div). SectionLabel: port the prototype's `.slabel` (mono, uppercase, text-sm, letter-spacing 0.12em, text-accent) plus the `.shead` flex-baseline-between behavior; keep the `{ children, numeral }` prop convention from the editorial analog; use `font-mono uppercase text-sm tracking-[0.12em] text-accent` and a baseline-between flex when a numeral is present. Chip: a presentational `<span>` per site.css line 102 — `font-mono text-xs uppercase tracking-[0.08em] border border-border rounded-full px-3 py-[7px] text-text-dim`. Reserve arbitrary-value classes only where no token utility exists (letter-spacing values, exact paddings).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E 'components/v3/(rule|rule-strong|section-label|chip)' || echo "no type errors in atoms"</automated>
  </verify>
  <acceptance_criteria>
    - All four files export a single named function component (Rule, RuleStrong, SectionLabel, Chip) and have no `"use client"` directive
    - SectionLabel accepts `{ children, numeral }` and renders the numeral in a baseline-aligned position
    - Rule uses `border-border` and RuleStrong uses `border-text` (the crimson tokens, not bg-rule/border-rule)
    - Chip renders a rounded-full bordered mono uppercase span
    - `import { cn } from "@/utils/cn"` is used in any component with a variant/className prop
    - `npx tsc --noEmit` reports no type errors in these four files
  </acceptance_criteria>
  <done>The four static atom primitives compile and match the prototype styling for .rule, .rule-strong, .slabel/.shead, .chip.</done>
</task>

<task type="auto">
  <name>Task 2: ListRow (hover-invert) + Button (default/accent)</name>
  <files>src/components/v3/list-row.tsx, src/components/v3/button.tsx</files>
  <read_first>
    - src/components/editorial/list-row.tsx (exact prop/cn/Link structural analog)
    - src/components/editorial/all-link.tsx (Button structural analog — { children, href } Link + className)
    - .planning/sketches/002-full-site-model/assets/site.css (.row + descendants lines 55-66; .btn / .btn-accent lines 68-73)
    - .planning/phases/14-branch-crimson-poster-foundation/14-PATTERNS.md (ListRow hover-invert via group/group-hover; Button accent variant)
  </read_first>
  <action>
    ListRow: port site.css lines 55-66 as a `<Link>` with `Props = { numeral?: string; title: ReactNode; href: string; excerpt?: ReactNode; meta?: ReactNode; big?: boolean }`. Layout = grid `60px 1fr auto`, gap 18px, items-center, py-[22px], border-bottom border-border. Implement hover-invert: the prototype uses descendant `.row:hover{background:var(--color-text);color:var(--color-bg)}` and `.row:hover .n/.m` recolor — translate to Tailwind `group` on the Link plus `group-hover:bg-text group-hover:text-bg` and `group-hover:text-bg` on the numeral (.n) and meta (.m). The numeral (.n) is mono text-sm text-text-muted; the title (.ti) is `font-display font-medium text-xl uppercase tracking-[-0.01em]` with a reveal arrow (.ar) that is `opacity-0 group-hover:opacity-100`; the excerpt (.ex) is sans text-sm text-text-muted (normal case); the meta (.m) is mono text-xs uppercase text-text-muted whitespace-nowrap. Use `cn` for the `big` variant sizing. Button: port site.css lines 68-73 with `Props = { children: ReactNode; href?: string; onClick?: () => void; accent?: boolean }`. Default `.btn` = inline-flex items-center gap-2 font-mono text-sm uppercase tracking-[0.08em] px-6 py-[14px] border border-border-strong text-text, hover → `hover:bg-text hover:text-bg`. Accent variant via cn = `bg-accent border-accent text-bg hover:bg-transparent hover:text-accent`. Render as `<Link href>` when href is provided, otherwise a `<button type="button" onClick>` (mirror the intro-link.tsx link-vs-element branching pattern). No "use client" needed (onClick on a plain button passed from a server parent is fine only if the parent is a client component — keep Button itself a server component that accepts the handler; if lint flags the handler, mark Button "use client").
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E 'components/v3/(list-row|button)' || echo "no type errors in listrow/button"</automated>
  </verify>
  <acceptance_criteria>
    - ListRow is a `<Link>` with a `group` class and uses `group-hover:bg-text group-hover:text-bg` to achieve the hover-invert
    - The ListRow title row contains a reveal arrow element that is `opacity-0 group-hover:opacity-100`
    - ListRow uses a `grid` with the 60px / 1fr / auto column layout
    - Button supports both a `<Link>` (when `href` set) and a `<button>` (when `onClick` set) render path
    - Button's accent variant applies `bg-accent text-bg` and inverts on hover to transparent/accent
    - `import { cn } from "@/utils/cn"` is used in both files
    - `npx tsc --noEmit` reports no type errors in these two files
  </acceptance_criteria>
  <done>ListRow renders a hover-inverting clickable index row with a reveal arrow, and Button renders bordered + accent variants with hover-invert.</done>
</task>

<task type="auto">
  <name>Task 3: BigList (sig outline variant), PageHero, Marquee (reduced-motion)</name>
  <files>src/components/v3/big-list.tsx, src/components/v3/page-hero.tsx, src/components/v3/marquee.tsx</files>
  <read_first>
    - src/components/editorial/list-row.tsx (prop/cn/Link convention for BigList)
    - src/components/home-v2/editorial-header.tsx (PageHero structural analog — large heading block)
    - .planning/sketches/002-full-site-model/assets/site.css (.big-list lines 154-167; .page-hero lines 38-45; .marquee lines 75-80 + @keyframes scroll line 80)
    - src/app/globals.css (the .sig / .sig-out utilities + @keyframes scroll added in Plan 01)
    - .planning/phases/14-branch-crimson-poster-foundation/14-PATTERNS.md (BigList -webkit-text-stroke GOTCHA; Marquee no-analog; sig-type utility)
  </read_first>
  <action>
    BigList: port site.css lines 154-167 — accept `Props = { items: { label: ReactNode; href: string; tag?: string; outline?: boolean }[] }`. Each item is a `<Link>` flex justify-between border-top border-border, padding 1.4vh 0, `font-display font-bold uppercase text-[clamp(2rem,9.5vw,8rem)] leading-[1.02] tracking-[-0.03em]`, and the `.sig` utility for the crimson+shadow fill; hover → `hover:text-accent` and remove the shadow (use a `hover:[text-shadow:none]` arbitrary or a hover utility). The outline variant (per-item `outline: true`) applies `.sig-out` instead of `.sig`. The optional `.tag` is mono text-xs text-text-muted, hidden below 760px (`hidden md:inline` / `max-md:hidden`). PageHero: port site.css lines 38-45 — `Props = { title: ReactNode; crumb?: ReactNode; sub?: ReactNode; outline?: boolean }`. Padding `clamp(90px,16vh,180px)` top; `.crumb` mono text-xs uppercase text-text-muted; the `<h1>` is `font-display font-bold uppercase leading-[0.86] tracking-[-0.03em] text-[clamp(2.8rem,11vw,8rem)]` with the `.sig` utility (or `.sig-out` when `outline`); `.sub` is text-lg text-text-dim max-w-[54ch] mt-6. Marquee: NEW component (no analog) — `"use client"` because it must honor reduced motion. Port site.css lines 75-80: an overflow-hidden band with `border-y border-border py-4`, an inner `.track` `inline-block` that runs `animation: scroll 30s linear infinite` (the @keyframes is in globals.css from Plan 01 — apply via an arbitrary class `[animation:scroll_30s_linear_infinite]` or a small custom class). Accept `Props = { items: { text: string; hot?: boolean }[] }` and render the items DUPLICATED back-to-back inside the track so the -50% translate loops seamlessly. `hot` items use `.sig-out`-style stroke (`-webkit-text-stroke:1.5px var(--accent); color:transparent`) — use an arbitrary class. CRITICAL (DS-05): call `useReducedMotion()` from `motion/react`; when it returns true, render the track WITHOUT the animation class (static, fully readable) — do not hide the content. Each marquee span is `font-display font-bold uppercase text-2xl px-[26px] text-text-muted`.
  </action>
  <verify>
    <automated>grep -q 'useReducedMotion' src/components/v3/marquee.tsx && grep -q '"use client"' src/components/v3/marquee.tsx && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E 'components/v3/(big-list|page-hero|marquee)' || echo "ok"</automated>
  </verify>
  <acceptance_criteria>
    - BigList items use the `.sig` utility by default and `.sig-out` when an item's `outline` prop is true
    - BigList renders an optional `.tag` that is hidden below the md breakpoint
    - PageHero's `<h1>` applies `.sig` (and `.sig-out` when `outline` is set) and supports `crumb` and `sub` slots
    - marquee.tsx begins with `"use client"` and imports/calls `useReducedMotion` from `motion/react`
    - When `useReducedMotion()` is true, the marquee track does NOT carry the `scroll` animation class but still renders all item text (content stays usable — DS-05)
    - The marquee renders its items duplicated back-to-back for a seamless `translateX(-50%)` loop
    - `npx tsc --noEmit` reports no type errors in these three files
  </acceptance_criteria>
  <done>BigList, PageHero, and Marquee are built to the prototype spec; the sig outline variant works, and the marquee honors prefers-reduced-motion by rendering static-but-readable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → navigation | ListRow/BigList/Button hrefs route the user; hrefs are author-provided (static, not user input this phase) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-03 | Information Disclosure | external links opened from Button | mitigate | When Button/links target external URLs in later phases, use rel="noopener noreferrer"; this phase's primitives are internal next/link routes |
| T-14-04 | Denial of Service | runaway CSS animation on low-power devices | mitigate | Marquee honors prefers-reduced-motion (DS-05), disabling the autonomous animation while keeping content readable |
</threat_model>

<verification>
- All nine primitives compile under `npx tsc --noEmit` with no type errors
- ListRow hover-inverts; BigList + PageHero use the sig utilities; Button has both variants
- Marquee is "use client", uses useReducedMotion, and stays readable when motion is reduced
- (Visual render verification happens in the Plan 04 showcase + build gate)
</verification>

<success_criteria>
- The full static brutalist primitive set exists under src/components/v3/ (DS-04)
- Display headings use the reusable crimson sig treatment with an outline variant (DS-02)
- The marquee honors prefers-reduced-motion (DS-05)
</success_criteria>

<output>
Create `.planning/phases/14-branch-crimson-poster-foundation/14-02-SUMMARY.md` when done. List each primitive's exported name and its prop signature so Plan 04 (showcase) and later phases can compose them without re-reading the files.
</output>
