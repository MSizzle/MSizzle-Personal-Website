---
phase: 14-branch-crimson-poster-foundation
plan: 03
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - src/components/v3/card.tsx
  - src/components/v3/video-card.tsx
  - src/components/v3/newsletter-carousel.tsx
  - src/components/v3/uses-list.tsx
  - src/components/v3/reveal.tsx
autonomous: true
requirements: [DS-04, DS-05]
must_haves:
  truths:
    - "Composite primitives exist in src/components/v3/: Card, VideoCard, NewsletterCarousel, UsesList"
    - "Cards render a 1px-gap grid with hover background shift; video cards lift on hover and show a play-triangle that inverts on hover"
    - "The newsletter carousel is a horizontal scroll-snap row of issue cards with placeholder thumbs"
    - "UsesList renders grouped dl/dt/dd rows that collapse responsively"
    - "A reveal-on-scroll wrapper fades/translates content into view and short-circuits to a static visible state under prefers-reduced-motion"
  artifacts:
    - path: "src/components/v3/card.tsx"
      provides: "Essay/works grid card (kicker, h3, blurb) with hover bg shift"
      exports: ["Card"]
    - path: "src/components/v3/video-card.tsx"
      provides: "Video card with play-triangle thumb and hover lift"
      exports: ["VideoCard"]
    - path: "src/components/v3/reveal.tsx"
      provides: "Reveal-on-scroll wrapper honoring prefers-reduced-motion with an SSR-stable static fallback"
      exports: ["Reveal"]
  key_links:
    - from: "src/components/v3/reveal.tsx"
      to: "motion/react"
      via: "m component + useReducedMotion + whileInView (LazyMotion strict requires m, not motion)"
      pattern: "useReducedMotion"
---

<objective>
Build the v3 composite/data primitives (Card, VideoCard, NewsletterCarousel, UsesList) and the animated reveal-on-scroll wrapper (Reveal), ported from the prototype and following the established motion-as-`m` convention. Reveal is the only motion primitive in the foundation and must honor reduced motion.

Purpose: These complete the brutalist primitive set (DS-04) and deliver the scroll-reveal pattern (DS-05) that later pages reuse. They run in parallel with the static primitives (Plan 02) — disjoint files, both depend only on the Plan 01 tokens.
Output: Five components under `src/components/v3/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/14-branch-crimson-poster-foundation/14-CONTEXT.md
@.planning/phases/14-branch-crimson-poster-foundation/14-PATTERNS.md

<interfaces>
<!-- Project conventions the executor MUST use directly. -->
import { cn } from "@/utils/cn";

<!-- footer-col.tsx list-render convention (copy for Carousel/UsesList): -->
//   type Props = { title: string; links: FooterLink[] };
//   export function FooterCol({ title, links }: Props) { return (...{links.map((l) => <li key={l.href}>...)}...); }

<!-- Motion convention (REQUIRED): MotionProvider wraps the app in <LazyMotion features={...} strict>. -->
<!-- Under `strict` you MUST import `m` (NOT `motion`) from motion/react or it throws at runtime. -->
<!-- manifesto-reveal.tsx is the reference for: "use client", useReducedMotion() branch, -->
<!-- SSR-stable static first paint before the animation runs (avoids hydration mismatch / CLS). -->
import { m, useReducedMotion } from "motion/react";

<!-- Crimson token utilities from Plan 01: bg-bg, bg-bg-2, bg-surface, text-accent, text-text, -->
<!-- text-text-dim, text-text-muted, border-border, border-border-strong, text-xs … text-3xl, -->
<!-- font-display / font-mono, and the .sig / .sig-out utilities. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Card + VideoCard (static, hover effects)</name>
  <files>src/components/v3/card.tsx, src/components/v3/video-card.tsx</files>
  <read_first>
    - src/components/editorial/list-row.tsx (Link + cn structural convention)
    - .planning/sketches/002-full-site-model/assets/site.css (.cards/.card lines 92-98; .videos/.video + ::after play-triangle lines 113-123)
    - .planning/phases/14-branch-crimson-poster-foundation/14-PATTERNS.md (Card/VideoCard pattern assignments)
  </read_first>
  <action>
    Card: port site.css lines 92-98 as a presentational component (server component, no "use client"). `Props = { kicker?: string; title: ReactNode; blurb?: ReactNode; href?: string }`. When href is set render a `<Link>`, else a `<div>`. Style the card itself = `bg-bg p-[26px] hover:bg-bg-2 transition-colors`; the kicker (.k) = `font-mono text-xs text-accent block mb-[14px]`; the h3 = `font-display font-medium text-lg uppercase mb-2`; the blurb (.p) = `text-sm text-text-dim`. Note: the `.cards` 1px-gap grid CONTAINER (`grid auto-fill minmax(260px,1fr) gap-px bg-border border border-border`) is a layout the showcase/page provides — Card is the cell; document the container classes in the summary so the showcase wraps cards correctly. VideoCard: port site.css lines 113-123. `Props = { title: ReactNode; channel?: string; href: string }` — always a `<Link>` (links out to YouTube in later phases; for now internal/placeholder). The card = `block border border-border hover:-translate-y-1 transition-transform`; the thumb = `aspect-[16/9] bg-surface border-b border-border relative flex items-center justify-center` with the play-triangle via a `::after`-equivalent — since Tailwind cannot easily do the CSS triangle, render an explicit triangle element (a span with `border-l-[24px] border-l-accent border-y-[15px] border-y-transparent`) centered, and on hover invert: `group-hover:bg-accent` on the thumb and the triangle's left-border to `group-hover:border-l-bg`. Use `group` on the Link. The body (.b) = `p-4 flex justify-between gap-3 items-baseline`; h3 = `font-display font-medium text-base uppercase tracking-[-0.01em]`; channel (.ch) = `font-mono text-xs text-text-muted whitespace-nowrap`.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E 'components/v3/(card|video-card)' || echo "no type errors in cards"</automated>
  </verify>
  <acceptance_criteria>
    - Card exports a single named function; supports an optional `href` (Link) vs non-link (div) render path; has no `"use client"`
    - Card uses `bg-bg` with `hover:bg-bg-2` and a mono `text-accent` kicker
    - VideoCard renders a `group` Link with a play-triangle that inverts on hover and a thumb that fills with accent on hover, and lifts with `hover:-translate-y-1`
    - VideoCard's channel label is `font-mono text-xs text-text-muted whitespace-nowrap`
    - `npx tsc --noEmit` reports no type errors in these two files
  </acceptance_criteria>
  <done>Card and VideoCard match the prototype's .card and .video styling including the hover-invert play triangle and lift.</done>
</task>

<task type="auto">
  <name>Task 2: NewsletterCarousel + UsesList (list-render composites)</name>
  <files>src/components/v3/newsletter-carousel.tsx, src/components/v3/uses-list.tsx</files>
  <read_first>
    - src/components/editorial/footer-col.tsx (list-map + typed Props convention — copy this)
    - .planning/sketches/002-full-site-model/assets/site.css (.carousel/.issue + thumb::after lines 104-111; .uses-grp dl/dt/dd + responsive collapse lines 125-131)
    - .planning/phases/14-branch-crimson-poster-foundation/14-PATTERNS.md (Carousel/UsesList partial analogs)
  </read_first>
  <action>
    NewsletterCarousel: port site.css lines 104-111 (server component). `Props = { issues: { title: string; date: string; href?: string }[] }`. Container = `flex gap-[18px] overflow-x-auto [scroll-snap-type:x_mandatory] pb-[18px]`; each issue = `flex-[0_0_300px] [scroll-snap-align:start] border border-border bg-bg-2`. The thumb = `aspect-[3/2] bg-surface border-b-2 border-accent flex items-center justify-center` with a placeholder glyph 'MM' rendered as a child span styled `font-display font-bold text-2xl text-text-muted` (replace the CSS `::after{content:'MM'}` with an explicit element). The body = `p-[18px]`; h3 = `font-display font-medium text-base mb-2`; the date (.d) = `font-mono text-xs text-text-muted`. Map issues with `key` per the footer-col convention. UsesList: port site.css lines 125-131. `Props = { groups: { heading: string; items: { term: string; detail: string }[] }[] }`. Each group (.uses-grp) wraps an h3 (`font-mono text-sm uppercase tracking-[0.12em] text-accent mb-[18px]`) and a `<dl>` `grid grid-cols-[200px_1fr] gap-x-6 gap-y-[14px]` that collapses to a single column below 600px (`max-[600px]:grid-cols-1`); dt = `font-display font-medium`; dd = `text-text-dim text-sm`. Map groups and items with stable keys.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E 'components/v3/(newsletter-carousel|uses-list)' || echo "no type errors in carousel/uses"</automated>
  </verify>
  <acceptance_criteria>
    - NewsletterCarousel renders a horizontally scrollable scroll-snap row of issue cards; each issue has a 3:2 thumb with a placeholder glyph and a 2px accent bottom border
    - UsesList renders one group per heading with a `<dl>` of dt/dd pairs in a 200px/1fr grid that collapses to one column below 600px
    - Both components map their data with stable `key` props (footer-col convention)
    - Both are server components (no `"use client"`)
    - `npx tsc --noEmit` reports no type errors in these two files
  </acceptance_criteria>
  <done>NewsletterCarousel and UsesList render their data per the prototype, with the scroll-snap carousel and the responsive uses grid.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Reveal — scroll-reveal wrapper honoring reduced motion (DS-05)</name>
  <files>src/components/v3/reveal.tsx</files>
  <read_first>
    - src/components/home-v2/manifesto-reveal.tsx (THE motion pattern: "use client", import m not motion, useReducedMotion branch, SSR-stable static first paint)
    - src/components/providers/motion-provider.tsx (LazyMotion strict — why `m` is mandatory)
    - .planning/sketches/002-full-site-model/assets/site.css (.reveal / .reveal.in lines 142-144 — opacity 0 + translateY(30px) → opacity 1 + none on scroll-in)
    - .planning/phases/14-branch-crimson-poster-foundation/14-PATTERNS.md (Reveal pattern assignment; prefer motion/react whileInView over hand-rolled IntersectionObserver)
  </read_first>
  <action>
    Create a `"use client"` wrapper `Reveal`. `Props = { children: ReactNode; className?: string; delay?: number }`. Import `m` and `useReducedMotion` from "motion/react" (MUST be `m`, not `motion` — LazyMotion strict throws otherwise, per motion-provider.tsx). Port the prototype behavior (site.css lines 142-144): from `{ opacity: 0, y: 30 }` to `{ opacity: 1, y: 0 }` when scrolled into view. Use motion/react's `whileInView` with `viewport={{ once: true, margin: "0px 0px -10% 0px" }}` and a ~0.9s ease-out transition (matching the prototype's 0.9s var(--ease-out)). CRITICAL (DS-05): call `useReducedMotion()`; when true, render the children in a plain static `<div className={className}>` with NO initial-hidden state and NO animation (fully visible, content usable) — short-circuit before any motion component (mirror manifesto-reveal Branch A). Render an SSR-stable element: use `<m.div initial={{opacity:0,y:30}} whileInView={...}>` only in the animated branch; the reduced-motion branch returns a non-motion `<div>`. Do NOT hand-roll an IntersectionObserver — use the motion/react whileInView API to match the established `m`-component convention.
  </action>
  <verify>
    <automated>grep -q '"use client"' src/components/v3/reveal.tsx && grep -q 'useReducedMotion' src/components/v3/reveal.tsx && grep -Eq 'import \{[^}]*\bm\b' src/components/v3/reveal.tsx && grep -q 'whileInView' src/components/v3/reveal.tsx && echo "reveal ok"</automated>
  </verify>
  <acceptance_criteria>
    - reveal.tsx begins with `"use client"`
    - It imports `m` (not `motion`) and `useReducedMotion` from `motion/react`
    - The animated branch uses `whileInView` with `viewport={{ once: true }}` and animates from opacity 0 / y 30 to opacity 1 / y 0
    - When `useReducedMotion()` returns true, the component returns a plain `<div>` with children fully visible and NO motion component (DS-05 short-circuit)
    - No hand-rolled `IntersectionObserver` is used
    - `npx tsc --noEmit` reports no type errors in reveal.tsx
  </acceptance_criteria>
  <done>Reveal fades/translates content into view via motion/react whileInView and renders static-visible content under prefers-reduced-motion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → motion runtime | Reveal mounts a client motion component; LazyMotion strict enforces the `m` import contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-05 | Denial of Service | scroll-reveal animation on reduced-motion users / low-power devices | mitigate | Reveal short-circuits to a static visible render under useReducedMotion (DS-05) |
| T-14-06 | Tampering | runtime crash from importing `motion` instead of `m` under LazyMotion strict | mitigate | Plan mandates `m` import; verify grep gate asserts the `m` import; Plan 04 build gate catches a regression |
</threat_model>

<verification>
- All five components compile under `npx tsc --noEmit`
- Reveal is "use client", imports `m` + useReducedMotion, uses whileInView, and short-circuits under reduced motion
- Card/VideoCard/Carousel/UsesList match the prototype styling
- (Visual render verification happens in the Plan 04 showcase + build gate)
</verification>

<success_criteria>
- The composite brutalist primitives complete the DS-04 set
- The reveal-on-scroll pattern honors prefers-reduced-motion (DS-05)
</success_criteria>

<output>
Create `.planning/phases/14-branch-crimson-poster-foundation/14-03-SUMMARY.md` when done. List each component's exported name + prop signature, and record the `.cards` grid CONTAINER classes (the wrapper the showcase must use around Card cells) so Plan 04 composes them correctly.
</output>
