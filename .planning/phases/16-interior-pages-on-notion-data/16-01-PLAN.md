---
phase: 16-interior-pages-on-notion-data
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - next.config.ts
  - src/lib/uses.ts
  - src/lib/watching.ts
  - src/__tests__/components/uses-list.test.tsx
  - src/__tests__/components/video-card.test.tsx
  - src/__tests__/components/navigation.test.tsx
  - src/__tests__/components/footer.test.tsx
  - src/__tests__/pages/writing.test.tsx
  - src/__tests__/pages/blog-slug.test.tsx
  - src/__tests__/pages/projects.test.tsx
  - src/__tests__/components/breadcrumbs.test.tsx
autonomous: true
requirements:
  - PG-02
  - PG-03
  - PG-01
  - IN-01
  - IN-02

must_haves:
  truths:
    - "next.config.ts remotePatterns includes img.youtube.com so YouTube thumbnails load on Vercel"
    - "The /uses redirect to /about no longer exists in next.config.ts"
    - "src/lib/uses.ts exports USES_DATA: UsesGroup[] with four groups (AI & Development, Productivity, Communication, Hardware) and Hardware entries contain TODO placeholder strings"
    - "src/lib/watching.ts exports WATCHING_ITEMS: WatchingItem[] with 6 placeholder entries each having id, title, channel, url fields"
    - "Test scaffold files exist in src/__tests__/ for all Wave 0 gaps listed in VALIDATION.md"
  artifacts:
    - path: "next.config.ts"
      provides: "YouTube image remotePattern + /uses redirect removed"
      contains: "img.youtube.com"
    - path: "src/lib/uses.ts"
      provides: "Hardcoded uses data (D-05, D-06)"
      exports: ["USES_DATA", "UsesGroup", "UsesItem"]
    - path: "src/lib/watching.ts"
      provides: "Hardcoded watching data (D-07, D-08, D-10)"
      exports: ["WATCHING_ITEMS", "WatchingItem"]
    - path: "src/__tests__/components/uses-list.test.tsx"
      provides: "Wave 0 test scaffold for PG-02"
    - path: "src/__tests__/pages/writing.test.tsx"
      provides: "Wave 0 test scaffold for PG-01 writing index"
  key_links:
    - from: "src/app/watching/page.tsx"
      to: "img.youtube.com"
      via: "Next.js Image remotePatterns"
      pattern: "img.youtube.com"
    - from: "src/lib/uses.ts"
      to: "src/app/uses/page.tsx"
      via: "import USES_DATA"
      pattern: "USES_DATA"
    - from: "src/lib/watching.ts"
      to: "src/app/watching/page.tsx"
      via: "import WATCHING_ITEMS"
      pattern: "WATCHING_ITEMS"
---

<objective>
Lay the config and data foundation for Phase 16: add YouTube to remotePatterns, remove the stale /uses redirect, create typed hardcoded data modules (uses.ts, watching.ts) following the photos.ts pattern, and scaffold all Wave 0 test files so later plans have automated verification targets.

Purpose: These files are prerequisites for Plans 02-09. Without img.youtube.com in remotePatterns the /watching page will fail on Vercel preview. Without the data modules /uses and /watching cannot render. Without test scaffolds the Nyquist rule fails across this phase.

Output: Updated next.config.ts, two new data modules, nine new test scaffold files (stubs that import-and-exist but verify the contracts each task must fulfill).
</objective>

<execution_context>
@/Users/Montster/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Montster/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/Montster/MSizzle Personal Website/.planning/PROJECT.md
@/Users/Montster/MSizzle Personal Website/.planning/ROADMAP.md
@/Users/Montster/MSizzle Personal Website/.planning/phases/16-interior-pages-on-notion-data/16-CONTEXT.md
@/Users/Montster/MSizzle Personal Website/.planning/phases/16-interior-pages-on-notion-data/16-RESEARCH.md
@/Users/Montster/MSizzle Personal Website/.planning/phases/16-interior-pages-on-notion-data/16-PATTERNS.md
@/Users/Montster/MSizzle Personal Website/.planning/phases/16-interior-pages-on-notion-data/16-UI-SPEC.md
@/Users/Montster/MSizzle Personal Website/.planning/phases/16-interior-pages-on-notion-data/16-VALIDATION.md

<interfaces>
<!-- Key types sourced from codebase. Read these before writing; do not re-derive. -->

From src/lib/photos.ts (template pattern for uses.ts / watching.ts):
  export type ArchivePhoto = { filename: string; year: number; alt: string; caption?: string }
  export const PHOTOS_BY_YEAR: ArchivePhoto[] = [...]
  export function groupPhotosByYear(): Map<number, ArchivePhoto[]>

Target shape for src/lib/uses.ts (per D-05, D-06):
  export type UsesItem = { term: string; detail: string }
  export type UsesGroup = { heading: string; items: UsesItem[] }
  export const USES_DATA: UsesGroup[] = [four groups per D-06]

Target shape for src/lib/watching.ts (per D-07, D-08, D-10):
  export type WatchingItem = { id: string; title: string; channel: string; url: string }
  export const WATCHING_ITEMS: WatchingItem[] = [6 placeholder entries per D-10]

From next.config.ts (existing remotePatterns to preserve):
  { protocol: "https", hostname: "prod-files-secure.s3.us-east-1.amazonaws.com" }
  { protocol: "https", hostname: "www.notion.so" }
  { protocol: "https", hostname: "images.unsplash.com" }
  { protocol: "https", hostname: "substackcdn.com" }
  { protocol: "https", hostname: "substack-post-media.s3.amazonaws.com" }
  { protocol: "https", hostname: "*.substackcdn.com" }
  — ADD: { protocol: "https", hostname: "img.youtube.com" }
  
Current redirects (lines 17-22):
  { source: '/uses', destination: '/about', permanent: true }  ← DELETE this line
  { source: '/blog', destination: '/writing', permanent: true }  ← KEEP

Vitest config (for scaffold tests):
  environment: 'jsdom'
  setupFiles: ['./src/__tests__/setup.ts']
  include: ['src/**/*.test.{ts,tsx}']
  resolve alias: '@' → './src'

Existing test pattern (src/__tests__/components/project-card.test.tsx):
  vi.mock('next/link', () => ({ default: ({ children, href, ...props }) => <a href={href} {...props}>{children}</a> }))
  vi.mock('motion/react', ...) for motion components
  describe / it / expect from vitest
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update next.config.ts — add YouTube remotePattern + remove /uses redirect</name>
  <files>next.config.ts</files>
  <read_first>
    - next.config.ts (read entire file before editing — must preserve all existing keys including turbopack, transpilePackages, localPatterns, and all remotePatterns)
    - 16-PATTERNS.md section "next.config.ts (config, add remotePatterns)" — exact diff to apply
    - RESEARCH.md Pitfall 2 — why img.youtube.com must be added before /watching is built
  </read_first>
  <action>
    In next.config.ts make exactly two changes:
    (1) In the redirects() array: delete the entry { source: '/uses', destination: '/about', permanent: true }. Keep the '/blog' → '/writing' redirect. This unblocks the /uses route which will be created in Plan 05 (per D-05).
    (2) In images.remotePatterns array: append { protocol: "https", hostname: "img.youtube.com" } as a new entry after the existing *.substackcdn.com entry. This enables Next.js Image to serve YouTube CDN thumbnails on the /watching page (per D-08).
    Do NOT modify any other section (turbopack, transpilePackages, localPatterns, other remotePatterns).
  </action>
  <verify>
    <automated>grep -c "img.youtube.com" /Users/Montster/MSizzle\ Personal\ Website/next.config.ts</automated>
  </verify>
  <acceptance_criteria>
    - next.config.ts remotePatterns array contains exactly one entry with hostname "img.youtube.com"
    - next.config.ts redirects() no longer contains source: '/uses'
    - The '/blog' → '/writing' redirect still exists
    - npx tsc --noEmit passes (no TypeScript errors introduced)
  </acceptance_criteria>
  <done>next.config.ts updated with YouTube domain and stale /uses redirect removed; TypeScript clean</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create src/lib/uses.ts and src/lib/watching.ts data modules</name>
  <files>src/lib/uses.ts, src/lib/watching.ts</files>
  <read_first>
    - src/lib/photos.ts — the exact template to mirror (structure, export pattern, TypeScript conventions)
    - 16-CONTEXT.md D-05, D-06, D-07, D-08, D-10 — locked group names, placeholder content, WatchingItem shape
    - 16-PATTERNS.md sections "src/lib/uses.ts" and "src/lib/watching.ts" — adapter guidance from the photos.ts template
    - 16-UI-SPEC.md section "/uses Page (NEW)" — four groups and UsesList layout expectations
  </read_first>
  <behavior>
    - USES_DATA has exactly 4 group headings: "AI & Development", "Productivity", "Communication", "Hardware"
    - Hardware items have detail strings starting with "TODO:" per D-06
    - WATCHING_ITEMS has exactly 6 entries per D-10
    - Each WatchingItem has id (YouTube video ID string), title, channel, url (full YouTube URL)
    - All exports are TypeScript-strict with no `any`
  </behavior>
  <action>
    Create src/lib/uses.ts: export UsesItem type ({ term: string; detail: string }), export UsesGroup type ({ heading: string; items: UsesItem[] }), export const USES_DATA: UsesGroup[] with four groups per D-06. Group 1 "AI & Development": LLM/Claude, IDE/Cursor, version control/Git. Group 2 "Productivity": Notion, calendar. Group 3 "Communication": Gmail/Superhuman, Slack. Group 4 "Hardware": Laptop with detail "TODO: [Monty to fill in]", Phone with detail "TODO: [Monty to fill in]". No helper function is needed (page iterates USES_DATA directly, unlike photos.ts which has groupPhotosByYear).

    Create src/lib/watching.ts: export WatchingItem type ({ id: string; title: string; channel: string; url: string }), export const WATCHING_ITEMS: WatchingItem[] with 6 entries. Use descriptive placeholder titles such as "The Philosophy of Building" covering topics like AI, design, philosophy, writing, technology, and entrepreneurship. Video IDs should be plausible YouTube-format strings (e.g., "dQw4w9WgXcQ"). URLs must be full YouTube watch URLs matching the id field. Per D-10 these are placeholders Monty will swap out with real video IDs.
  </action>
  <verify>
    <automated>cd /Users/Montster/MSizzle\ Personal\ Website && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/uses.ts exists and exports USES_DATA, UsesGroup, UsesItem
    - USES_DATA.length === 4
    - USES_DATA[3].heading === "Hardware" and USES_DATA[3].items every item's detail starts with "TODO:"
    - src/lib/watching.ts exists and exports WATCHING_ITEMS, WatchingItem
    - WATCHING_ITEMS.length === 6
    - Every WatchingItem has non-empty id, title, channel, url fields
    - npx tsc --noEmit passes with no errors in these files
  </acceptance_criteria>
  <done>Both data modules created, TypeScript-strict, matching the photos.ts pattern and D-05/D-06/D-07/D-08/D-10 decisions</done>
</task>

<task type="auto">
  <name>Task 3: Create Wave 0 test scaffolds for all Phase 16 verification targets</name>
  <files>
    src/__tests__/components/uses-list.test.tsx,
    src/__tests__/components/video-card.test.tsx,
    src/__tests__/components/navigation.test.tsx,
    src/__tests__/components/footer.test.tsx,
    src/__tests__/components/breadcrumbs.test.tsx,
    src/__tests__/pages/writing.test.tsx,
    src/__tests__/pages/blog-slug.test.tsx,
    src/__tests__/pages/projects.test.tsx
  </files>
  <read_first>
    - src/__tests__/components/project-card.test.tsx — existing test pattern (vi.mock setup, describe/it/expect style, RTL render+screen)
    - src/__tests__/setup.ts — global test setup
    - 16-VALIDATION.md — Per-Task Verification Map; Wave 0 requirements list
    - 16-RESEARCH.md Validation Architecture section — test command patterns
    - src/components/v3/uses-list.tsx — interface to test against (read actual props before writing scaffold)
    - src/components/v3/video-card.tsx — interface to test against
    - src/components/nav/navigation.tsx — interface to test against
    - src/components/seo/breadcrumbs.tsx — interface to test against
  </read_first>
  <action>
    Create stub test files that: (a) import the target component/module, (b) contain at minimum one describe block and one it() that asserts the contract this plan establishes, (c) use vi.mock for Next.js dependencies (next/link → plain anchor, next/navigation → returns mock pathname, next/image → plain img). Each file must run without error and the stub tests must pass once the target component exists. Use the existing project-card.test.tsx pattern.

    Specifically:
    - uses-list.test.tsx: renders UsesList with USES_DATA; asserts "AI & Development" group heading appears; asserts Hardware group renders TODO text
    - video-card.test.tsx: if src/components/v3/video-card.tsx exists already (it does), render VideoCard with {title, channel, href}; assert anchor has target="_blank" and rel="noopener noreferrer"; assert title text appears
    - navigation.test.tsx: mock usePathname to return '/uses'; render Navigation; assert active label includes "Uses". Mock usePathname to return '/watching'; assert "Watching" active label appears
    - footer.test.tsx: render the footer component (InkFooter or whatever the v3 footer will be named — stub import so test compiles); assert links for /uses, /watching, /prometheus appear in rendered output
    - breadcrumbs.test.tsx: render Breadcrumbs with items [{name:'Home',href:'/'},{name:'Writing',href:'/writing'},{name:'Test Post'}]; assert all three names appear in DOM
    - writing.test.tsx: stub page that mocks getPublishedPosts returning [] and asserts empty-state copy "No essays yet" appears
    - blog-slug.test.tsx: stub that asserts import of calculateReadingTime from @/utils/reading-time does not throw and returns a number for an empty blocks array
    - projects.test.tsx: stub page that mocks getPublishedProjects returning [] and asserts "No projects yet" appears

    For tests involving server components (writing, projects, blog-slug pages), use async component rendering via RTL render with act wrapper or mark the test as integration with mock data — follow the existing pages/ test pattern in src/__tests__/pages/.
  </action>
  <verify>
    <automated>cd /Users/Montster/MSizzle\ Personal\ Website && npx vitest run src/__tests__/components/video-card.test.tsx src/__tests__/components/breadcrumbs.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - All 8 test scaffold files exist under src/__tests__/components/ and src/__tests__/pages/
    - npx vitest run src/__tests__/components/video-card.test.tsx passes (VideoCard exists; link attributes verifiable)
    - npx vitest run src/__tests__/components/breadcrumbs.test.tsx passes
    - Other scaffold files may have skipped/pending tests but must not fail with import errors
    - npx vitest run (full suite) shows no new compilation errors from these files
  </acceptance_criteria>
  <done>All Wave 0 test scaffolds created; video-card and breadcrumbs tests pass; other stubs compile and are ready to be fleshed out in later plans</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| next.config.ts → Vercel build | remotePatterns is a security allowlist; adding img.youtube.com widens the allowable external image sources |
| src/lib/*.ts → build bundle | Hardcoded TS data is compiled into the bundle; no runtime data sink |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Tampering | next.config.ts remotePatterns | mitigate | Scope img.youtube.com to hostname-only (no wildcard path); only adds YouTube CDN thumbnails, not arbitrary URLs |
| T-16-02 | Information Disclosure | src/lib/uses.ts | accept | Hardcoded; no secrets; content is intentionally public (tools list). TODO placeholders are non-sensitive |
| T-16-03 | Information Disclosure | src/lib/watching.ts | accept | Hardcoded placeholder IDs; Monty swaps in real values — no sensitive data |
| T-16-SC | Tampering | npm installs | accept | No new npm packages in this plan; all changes are config + static TS files |
</threat_model>

<verification>
After all tasks:
- grep -c "img.youtube.com" next.config.ts returns 1
- grep "source.*uses.*destination" next.config.ts returns empty (redirect removed)
- npx tsc --noEmit passes
- npx vitest run src/__tests__/components/video-card.test.tsx src/__tests__/components/breadcrumbs.test.tsx passes
</verification>

<success_criteria>
- next.config.ts has img.youtube.com in remotePatterns and no /uses redirect
- src/lib/uses.ts and src/lib/watching.ts exist with correct TypeScript exports
- 8 test scaffold files exist and compile without errors
- Full vitest suite runs without new failures
</success_criteria>

<output>
Create `.planning/phases/16-interior-pages-on-notion-data/16-01-SUMMARY.md` when done
</output>
