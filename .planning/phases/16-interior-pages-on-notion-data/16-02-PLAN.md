---
phase: 16-interior-pages-on-notion-data
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/nav/navigation.tsx
  - src/components/layout/conditional-footer.tsx
  - src/components/layout/v3-footer.tsx
autonomous: true
requirements:
  - PG-05
  - PG-02
  - PG-03

must_haves:
  truths:
    - "D-11: The focused primary desktop nav keeps exactly 5 links (Work, Writing, Events, About, Links); /uses and /watching do NOT appear in the desktop top bar -- they live in the footer and mobile drawer only"
    - "D-12: The footer is the full sitemap -- v3-footer.tsx includes columns covering /uses, /watching, /prometheus, /newsletter, /photos in addition to the primary 5 routes"
    - "Navigation active-label mapping covers /uses (-> 'Uses') and /watching (-> 'Watching') for active state highlighting when on those routes"
    - "MOBILE_LINKS includes /uses and /watching entries (secondary routes accessible from mobile drawer, not desktop top nav per D-11)"
    - "A v3 footer component (v3-footer.tsx) renders the full sitemap with columns: Building, Writing, Community, Archive, About"
    - "ConditionalFooter renders v3-footer instead of InkFooter on all non-homepage routes"
    - "Footer big signature line is exactly \"Let's be friends.\" -- sourced from prototype 002 index.html"
  artifacts:
    - path: "src/components/nav/navigation.tsx"
      provides: "Extended active-label mapping for /uses and /watching (D-13)"
      contains: "pathname === '/uses'"
    - path: "src/components/layout/v3-footer.tsx"
      provides: "Full-sitemap footer (D-12)"
      exports: ["V3Footer"]
    - path: "src/components/layout/conditional-footer.tsx"
      provides: "Swap InkFooter -> V3Footer on non-homepage routes"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/components/layout/conditional-footer.tsx"
      via: "ConditionalFooter import"
      pattern: "ConditionalFooter"
    - from: "src/components/layout/conditional-footer.tsx"
      to: "src/components/layout/v3-footer.tsx"
      via: "V3Footer import"
      pattern: "V3Footer"
    - from: "src/components/nav/navigation.tsx"
      to: "EditorialHeader"
      via: "active prop"
      pattern: "activeLabel.*Uses|activeLabel.*Watching"
---

<objective>
Extend the shared navigation for the Pumpkin Amber v3 system: add /uses and /watching to the active-label mapping and mobile drawer, and replace the v2 InkFooter with a new V3Footer that implements the full sitemap layout per D-12.

Purpose: Every interior page inherits the updated nav (via app/layout.tsx -> Navigation). The footer must include the new and secondary routes (Uses, Watching, Prometheus, Newsletter, Photos) alongside the primary 5. Navigation and footer both run in Wave 1 so Plans 04-09 can rely on them.

Output: Updated navigation.tsx, new v3-footer.tsx, updated conditional-footer.tsx. The existing InkFooter is NOT deleted (leave it untouched as a reference until Phase 17 confirms the swap is complete).
</objective>

<execution_context>
@/Users/Montster/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Montster/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/Montster/MSizzle Personal Website/.planning/PROJECT.md
@/Users/Montster/MSizzle Personal Website/.planning/phases/16-interior-pages-on-notion-data/16-CONTEXT.md
@/Users/Montster/MSizzle Personal Website/.planning/phases/16-interior-pages-on-notion-data/16-PATTERNS.md
@/Users/Montster/MSizzle Personal Website/.planning/phases/16-interior-pages-on-notion-data/16-UI-SPEC.md

<interfaces>
<!-- Read these files before editing. Exact contracts the executor needs. -->

From src/components/nav/navigation.tsx (current active mapping at lines 26-32):
  'use client'
  const MOBILE_LINKS = [
    { href: '/projects', label: 'Building' },
    { href: '/writing',  label: 'Writing'  },
    { href: '/events',   label: 'Events'   },
    { href: '/about',    label: 'About'    },
    { href: '/links',    label: 'Links'    },
  ]
  const activeLabel: 'Building'|'Writing'|'Events'|'About'|'Links'|undefined =
    pathname === '/projects' ? 'Building'
    : pathname === '/writing' || pathname.startsWith('/blog') ? 'Writing'
    : pathname === '/events' ? 'Events'
    : pathname === '/about' ? 'About'
    : pathname === '/links' ? 'Links'
    : undefined

From src/components/layout/conditional-footer.tsx (current):
  "use client"
  import { InkFooter } from "@/components/home-v2/ink-footer"
  export function ConditionalFooter() {
    const pathname = usePathname()
    if (pathname === "/") return null
    return <InkFooter />
  }

From src/components/home-v2/ink-footer.tsx (reference for column structure):
  4 columns (Colophon, Studio, Library, Living)
  Uses v2 tokens: bg-footer-bg, text-footer-fg, border-footer-rule (do NOT copy these)

From 16-UI-SPEC.md Footer section:
  Footer columns (Pumpkin Amber system):
    Building: /projects links
    Writing: /writing, /blog, essays
    Community: /events, /newsletter, /links, /prometheus
    Archive: /photos, /watching, /uses
    About & Contact
  Footer border-top: var(--color-border)
  Grid: grid auto-fit minmax(140px, 1fr), gap-24
  Big signature line: font-display clamp(2.4rem, 9vw, 7rem), font-weight 700, line-height 0.9
  Colophon: font-mono xs text-text-muted

From prototype 002 (index.html footer -- the canonical source for the big signature line):
  The big display-font line is: "Let's be friends."
  This is the locked copy. Do NOT use "Monty Singer", "Let's build something.", or any other variant.
  The line is a link to /links in the prototype; in V3Footer it may be a plain heading or a Link to /links.
  Copy rules: no em dashes, no location. "Let's be friends." complies.

Token mapping for footer (Pumpkin Amber):
  background: bg-[var(--color-surface)]  (dark roasted cocoa for footer)
  text: text-[var(--color-text-inverse)]  (vanilla cream on dark)
  border: border-[var(--color-border-strong)]
  links hover: hover:text-[var(--accent)]
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend navigation.tsx active-label mapping and MOBILE_LINKS for /uses and /watching</name>
  <files>src/components/nav/navigation.tsx</files>
  <read_first>
    - src/components/nav/navigation.tsx -- read the full file before editing; exact line numbers for MOBILE_LINKS (lines 11-17) and activeLabel (lines 26-32) must be confirmed
    - 16-CONTEXT.md D-11, D-13 -- "focused primary desktop nav stays at 5 routes; /uses and /watching go in mobile drawer and footer not the desktop top bar"
    - 16-PATTERNS.md section "src/components/nav/navigation.tsx" -- exact extended mapping to apply
    - 16-UI-SPEC.md Navigation & Breadcrumbs -- MOBILE_LINKS extended set
  </read_first>
  <behavior>
    - usePathname() returns '/uses' -> activeLabel === 'Uses'
    - usePathname() returns '/watching' -> activeLabel === 'Watching'
    - MOBILE_LINKS array contains entries for /uses (label 'Uses'), /watching (label 'Watching'), /prometheus (label 'Prometheus')
    - The 5 primary desktop nav links (Building, Writing, Events, About, Links) are UNCHANGED -- /uses and /watching do NOT appear in the desktop top bar per D-11
  </behavior>
  <action>
    Edit src/components/nav/navigation.tsx with minimal changes:

    (1) Extend MOBILE_LINKS array to append three new entries after the existing 5:
      { href: '/uses', label: 'Uses' }
      { href: '/watching', label: 'Watching' }
      { href: '/prometheus', label: 'Prometheus' }
    (Moving Prometheus from footer to mobile drawer per D-13 / UI-SPEC)

    (2) Extend the activeLabel type union to include 'Uses' | 'Watching' and extend the ternary chain with two new branches:
      : pathname === '/uses' ? 'Uses'
      : pathname === '/watching' ? 'Watching'
    These branches go AFTER the existing '/links' branch and BEFORE the final : undefined fallback.

    Do NOT change the desktop EditorialHeader nav link array -- the primary 5 links displayed there are unchanged per D-11.
  </action>
  <verify>
    <automated>cd /Users/Montster/MSizzle\ Personal\ Website && npx vitest run src/__tests__/components/navigation.test.tsx 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "pathname.*'/uses'" src/components/nav/navigation.tsx returns 1 (in activeLabel mapping)
    - grep -c "'/uses'" src/components/nav/navigation.tsx returns at least 2 (MOBILE_LINKS + activeLabel)
    - grep -c "'/watching'" src/components/nav/navigation.tsx returns at least 2 (MOBILE_LINKS + activeLabel)
    - grep -c "'/prometheus'" src/components/nav/navigation.tsx returns at least 1 (in MOBILE_LINKS)
    - npx vitest run src/__tests__/components/navigation.test.tsx passes
    - npx tsc --noEmit clean
  </acceptance_criteria>
  <done>Navigation active-label mapping covers /uses and /watching; mobile drawer includes new routes + prometheus; desktop primary nav unchanged (D-11)</done>
</task>

<task type="auto">
  <name>Task 2: Create V3Footer and swap ConditionalFooter from InkFooter to V3Footer</name>
  <files>src/components/layout/v3-footer.tsx, src/components/layout/conditional-footer.tsx</files>
  <read_first>
    - src/components/home-v2/ink-footer.tsx -- current footer structure to replace (understand column pattern; do NOT use its v2 tokens)
    - src/components/editorial/footer-col.tsx -- check if FooterCol component can be reused or needs token repaint
    - 16-UI-SPEC.md Footer section -- exact column structure, Pumpkin Amber tokens, big signature line, colophon spec
    - src/app/globals.css -- verify --color-surface, --color-text-inverse, --color-border, --accent are defined (they are; confirmed in Pumpkin Amber @theme block)
    - src/components/layout/conditional-footer.tsx -- current file to update
    - 16-CONTEXT.md D-12 -- footer must include Uses, Watching, Prometheus, Newsletter, Photos in addition to primary 5 routes
    - .planning/sketches/002-full-site-model/index.html -- prototype source for the big signature line
  </read_first>
  <action>
    Create src/components/layout/v3-footer.tsx as a Server Component (no 'use client' -- pure markup, no hooks). Structure:

    Big signature line (LOCKED COPY from prototype 002): render the exact string "Let's be friends." as the display-font headline. Use font-display text-[clamp(2.4rem,9vw,7rem)] font-bold leading-[0.9] text-[var(--color-text-inverse)]. In the prototype this is a link to /links -- wrap it in a Next.js Link to /links to preserve that intent.

    Five columns in a responsive grid (grid-cols-1 md:grid-auto-fit minmax(140px,1fr) gap-24 style using Tailwind):
    - Column "Building": links to /projects (Works), /uses (Stack)
    - Column "Writing": links to /writing (Essays), /newsletter (Newsletter)
    - Column "Community": links to /events (Events), /prometheus (Prometheus), /links (Links)
    - Column "Archive": links to /photos (Photos), /watching (Watching)
    - Column "About": link to /about (About)

    Below columns: colophon row (flex justify-between, font-mono text-xs text-[var(--color-text-muted)]) with copyright year and site name.

    Outer footer element: bg-[var(--color-surface)] (dark roasted cocoa) text-[var(--color-text-inverse)] px-7 py-14 md:px-40 md:py-20. Border-top: border-t border-[var(--color-border-strong)].

    Column heading style: font-mono text-xs uppercase tracking-[0.12em] text-[var(--color-text-muted)] mb-4.
    Column links: block font-display text-sm text-[var(--color-text-inverse)] hover:text-[var(--accent)] transition-colors py-1. External links (prometheus.today) get target="_blank" rel="noopener noreferrer".

    IMPORTANT: Copy rules apply -- no em dashes, no location. Professional identity: "Founder of Prometheus" only (do not repeat if already in column links). Georgetown University only if needed for "About" copy.

    Then update conditional-footer.tsx: change the import from InkFooter to V3Footer and render V3Footer instead. Leave InkFooter import/file untouched.
  </action>
  <verify>
    <automated>cd /Users/Montster/MSizzle\ Personal\ Website && npx vitest run src/__tests__/components/footer.test.tsx 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/components/layout/v3-footer.tsx exists and exports V3Footer as a named export
    - V3Footer renders without 'use client' (Server Component -- no hooks)
    - Footer HTML includes href="/uses", href="/watching", href="/prometheus", href="/photos"
    - External links to prometheus.today have rel="noopener noreferrer" target="_blank"
    - conditional-footer.tsx imports V3Footer (not InkFooter) and renders it for non-homepage routes
    - grep -c "Let's be friends" src/components/layout/v3-footer.tsx returns 1 (exact locked signature line from prototype 002)
    - grep -v '^//' src/components/layout/v3-footer.tsx | grep -c "Monty Singer\|Let's build something" returns 0 (no alternate signature copy -- only "Let's be friends." is permitted)
    - npx vitest run src/__tests__/components/footer.test.tsx passes (test must assert "Let's be friends" text is present in rendered output)
    - npx tsc --noEmit clean
    - grep -v '^#' src/components/layout/v3-footer.tsx | grep -c "text-ink\|text-muted\|bg-paper" returns 0 (no v2 tokens)
  </acceptance_criteria>
  <done>V3Footer created with full sitemap columns + "Let's be friends." signature (locked from prototype 002) + Pumpkin Amber tokens; ConditionalFooter swapped to render V3Footer; no v2 tokens in footer</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Footer external links -> external sites | prometheus.today and any other external hrefs cross a trust boundary |
| client nav -> URL routing | usePathname() reads from Next.js router; no user-supplied input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-04 | Tampering | V3Footer external links | mitigate | All external links (prometheus.today) must have rel="noopener noreferrer" target="_blank" to prevent window.opener attacks and referrer leakage |
| T-16-05 | Information Disclosure | Navigation active-label | accept | Pathname is read from URL (public); active state is presentational only; no sensitive data exposed |
| T-16-SC | Tampering | npm installs | accept | No new npm packages; only existing component code modified |
</threat_model>

<verification>
After all tasks:
- grep -c "Uses" src/components/nav/navigation.tsx returns at least 2 (in type union + in MOBILE_LINKS)
- grep -c "v3-footer" src/components/layout/conditional-footer.tsx returns 1
- src/components/layout/v3-footer.tsx exists
- grep -c "Let's be friends" src/components/layout/v3-footer.tsx returns 1
- npx vitest run src/__tests__/components/navigation.test.tsx src/__tests__/components/footer.test.tsx passes
</verification>

<success_criteria>
- Navigation maps /uses and /watching to active labels; mobile drawer includes all new routes
- Desktop primary nav stays at the focused 5 links (D-11); /uses and /watching only in footer + mobile drawer
- V3Footer replaces InkFooter in ConditionalFooter; full sitemap with D-12 routes
- Footer signature line is exactly "Let's be friends." (locked from prototype 002, no substitutes)
- No v2 tokens (text-ink, bg-paper, text-muted) in any new or modified file
- vitest passes for navigation and footer tests
</success_criteria>

<output>
Create `.planning/phases/16-interior-pages-on-notion-data/16-02-SUMMARY.md` when done
</output>
