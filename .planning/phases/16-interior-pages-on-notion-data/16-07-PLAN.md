---
phase: 16-interior-pages-on-notion-data
plan: 07
type: execute
wave: 3
depends_on:
  - 16-03
  - 16-04
files_modified:
  - src/app/projects/[slug]/page.tsx
autonomous: true
requirements:
  - PG-01
  - PG-04
  - IN-01
  - IN-02

must_haves:
  truths:
    - "Project detail renders full-bleed cover image (when project.cover exists) via /api/notion-cover with fetchPriority='high'"
    - "Breadcrumb shows Home / Building / [Title]"
    - "Pumpkin Amber tokens used throughout; no text-ink or text-muted remain"
    - "export const revalidate = 1800 present; generateStaticParams present (IN-01)"
    - "NotionRenderer renders project body blocks (IN-02)"
    - "External project URL (externalUrl) renders as anchor with rel='noopener noreferrer' target='_blank'"
  artifacts:
    - path: "src/app/projects/[slug]/page.tsx"
      provides: "Repainted project detail with full-bleed cover, breadcrumb, Pumpkin Amber tokens"
      contains: "revalidate = 1800"
  key_links:
    - from: "src/app/projects/[slug]/page.tsx"
      to: "src/app/api/notion-cover/route.ts"
      via: "Image src /api/notion-cover?pageId"
      pattern: "notion-cover"
    - from: "src/app/projects/[slug]/page.tsx"
      to: "src/components/notion/notion-renderer.tsx"
      via: "NotionRenderer import"
      pattern: "NotionRenderer"
---

<objective>
Repaint the project detail page (/projects/[slug]) with v3 Pumpkin Amber tokens and add the full-bleed cover hero per D-02. This page already exists with generateStaticParams and NotionRenderer wired — it needs a token repaint, PageHero, breadcrumb, and the conditional cover image block with fetchPriority="high".

Purpose: PG-01 and PG-04 deliverable for Works detail. The full-bleed cover completes the D-02 "large/full-bleed photos on all four surfaces" commitment.

Output: Updated src/app/projects/[slug]/page.tsx. NotionRenderer, ISR, SEO metadata, and loader are UNCHANGED (IN-01/IN-02).
</objective>

<execution_context>
@/Users/Montster/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Montster/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/Montster/MSizzle Personal Website/.planning/phases/16-interior-pages-on-notion-data/16-CONTEXT.md
@/Users/Montster/MSizzle Personal Website/.planning/phases/16-interior-pages-on-notion-data/16-PATTERNS.md
@/Users/Montster/MSizzle Personal Website/.planning/phases/16-interior-pages-on-notion-data/16-UI-SPEC.md

<interfaces>
<!-- Read these before editing. Extract from codebase first pass. -->

From src/app/projects/[slug]/page.tsx (existing — read the full file):
  import { notFound } from 'next/navigation'
  import { getPublishedProjects, getProjectBySlug } from '@/lib/notion-projects'
  import { getBlocks } from '@/lib/notion'
  import { NotionRenderer } from '@/components/notion/notion-renderer'
  import { Breadcrumbs } from '@/components/seo/breadcrumbs'
  import { buildProjectMetadata } from '@/lib/seo/project-metadata'
  export const revalidate = 1800
  export async function generateStaticParams()  ← PRESERVE
  export async function generateMetadata()      ← PRESERVE
  Current v2 tokens: text-section-feature text-ink (h1), text-muted, text-sm text-muted (tags)

From src/lib/notion-projects.ts:
  export interface Project { id, title, slug, description, tags, image, externalUrl, lastEdited, cover, ... }
  — project.cover exists when Notion page has a cover image
  — project.externalUrl is the external URL for the project (may be null/undefined)
  — project.id is used as the pageId for /api/notion-cover

From src/components/v3/page-hero.tsx:
  export function PageHero({ title, crumb, sub, outline? }: PageHeroProps)

From src/components/seo/breadcrumbs.tsx:
  Breadcrumbs({ items: BreadcrumbItem[] })
  For projects: [{name:'Home',href:'/'},{name:'Building',href:'/projects'},{name:project.title}]

From 16-UI-SPEC.md "Project Detail (/projects/[slug])":
  Layout:
    1. Full-bleed cover image (if project.cover)
       src: /api/notion-cover?pageId={project.id}
       alt: {project.title} cover
       fill + priority + fetchPriority="high" + sizes="100vw" + className="object-cover"
       wrapper: relative w-full h-[400px] md:h-[600px]
    2. Breadcrumbs (semantic sr-only)
    3. PageHero: title={project.title}, crumb="Home / Building", sub={project.description ?? ""}
    4. Optional external URL link: text-[var(--accent)] underline hover:text-[var(--accent-hover)]
       Must have target="_blank" rel="noopener noreferrer"
    5. .prose section (NotionRenderer)
  
  Pumpkin Amber tokens:
    h1: text-[var(--color-text)] (replaces text-ink)
    description: text-[var(--color-text-dim)] (replaces text-muted in subtitle)
    tags: text-[var(--color-text-muted)] (replaces text-sm text-muted)

From 16-CONTEXT.md D-02:
  Full-bleed photos on project detail hero (Notion cover via /api/notion-cover)
  Memory: nextjs16-fetchpriority-quirk — fetchPriority="high" required on LCP image
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Repaint project detail page with full-bleed cover, PageHero, breadcrumb, and v3 tokens</name>
  <files>src/app/projects/[slug]/page.tsx</files>
  <read_first>
    - src/app/projects/[slug]/page.tsx — read the FULL existing file first; map every import, every prop, every className
    - src/app/blog/[slug]/page.tsx — sibling pattern from Plan 06; follow the same structure for the hero + breadcrumb + PageHero block
    - src/components/v3/page-hero.tsx — confirm PageHero props (title, crumb, sub)
    - 16-PATTERNS.md section "src/app/projects/[slug]/page.tsx" — exact template to follow
    - 16-UI-SPEC.md "Project Detail (/projects/[slug])" — layout diagram and specs
    - 16-CONTEXT.md D-02 — full-bleed on project detail hero; D-14 does not apply here (no reading time for project detail)
    - Memory: nextjs16-fetchpriority-quirk — fetchPriority="high" on hero Image
  </read_first>
  <behavior>
    - When project.cover exists: full-bleed Image renders before Breadcrumbs with fetchPriority="high" and sizes="100vw"
    - When project.cover is absent: Breadcrumbs and PageHero render directly (no image placeholder)
    - Breadcrumbs items: [{name:'Home',href:'/'},{name:'Building',href:'/projects'},{name:project.title}]
    - PageHero renders project.title + crumb="Home / Building"
    - External project link (project.externalUrl) renders with rel="noopener noreferrer" target="_blank"
    - NotionRenderer blocks rendered in .prose section
    - generateStaticParams and generateMetadata UNCHANGED
    - export const revalidate = 1800 UNCHANGED
  </behavior>
  <action>
    Edit src/app/projects/[slug]/page.tsx:

    (1) Add Image import from 'next/image' (may already be imported — check before adding).
    (2) Add PageHero import from '@/components/v3/page-hero'.

    (3) In the page component, restructure JSX:

    Before the main article/section:
    a) Conditional full-bleed cover block:
       {project.cover && (
         <div className="relative w-full h-[400px] md:h-[600px]">
           <Image
             src={`/api/notion-cover?pageId=${project.id}`}
             alt={`${project.title} cover`}
             fill priority fetchPriority="high"
             sizes="100vw"
             className="object-cover"
           />
         </div>
       )}

    b) Breadcrumbs with items=[{name:'Home',href:'/'},{name:'Building',href:'/projects'},{name:project.title}]

    c) PageHero title={project.title} crumb="Home / Building" sub={project.description ?? ""}

    d) If project.externalUrl:
       <a href={project.externalUrl} target="_blank" rel="noopener noreferrer"
          className="inline-block mt-4 px-6 md:px-40 text-[var(--accent)] underline hover:text-[var(--accent-hover)]">
         View Project ↗
       </a>

    e) Article section for prose:
       <article className="mx-auto max-w-[68ch] px-6 pb-16 pt-8 md:px-0">
         <div className="prose max-w-none">
           <NotionRenderer blocks={blocks} />
         </div>
       </article>

    (4) Replace all inline v2 token classes:
       text-ink → text-[var(--color-text)]
       text-muted (inline) → text-[var(--color-text-muted)]
       text-section-feature → font-display font-bold text-[clamp(2.8rem,11vw,8rem)] leading-[0.86] tracking-[-0.03em] (or defer to PageHero which already handles this)

    (5) Remove tags inline display if it was using text-sm text-muted — either move tags to PageHero chips or display with text-[var(--color-text-muted)].

    PRESERVE UNCHANGED: export const revalidate = 1800; generateStaticParams (with the defensive try/catch); generateMetadata calling buildProjectMetadata; NotionRenderer; all imports not explicitly listed for removal.

    IMPORTANT: generateStaticParams for projects doesn't have the NOTION_TOKEN guard that blog has — check the existing file; if it lacks the defensive check, add it for consistency:
      if (!process.env.NOTION_TOKEN || !process.env.NOTION_PROJECTS_DATABASE_ID) return []
      try { ... } catch { return [] }
  </action>
  <verify>
    <automated>cd /Users/Montster/MSizzle\ Personal\ Website && npx vitest run src/__tests__/pages/projects.test.tsx 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - grep "text-ink\|text-muted\|text-section-feature" src/app/projects/[slug]/page.tsx returns no matches
    - grep "revalidate = 1800" src/app/projects/[slug]/page.tsx returns 1 match
    - grep "fetchPriority" src/app/projects/[slug]/page.tsx returns at least 1 match
    - grep "notion-cover" src/app/projects/[slug]/page.tsx returns at least 1 match
    - grep "noopener noreferrer" src/app/projects/[slug]/page.tsx returns at least 1 match (on externalUrl anchor)
    - grep "NotionRenderer" src/app/projects/[slug]/page.tsx returns at least 1 match
    - grep "generateStaticParams" src/app/projects/[slug]/page.tsx returns 1 match (unchanged)
    - npx vitest run src/__tests__/pages/projects.test.tsx passes
    - npx tsc --noEmit clean
  </acceptance_criteria>
  <done>Project detail repainted with full-bleed cover (fetchPriority high), breadcrumb (Home / Building / Title), PageHero, Pumpkin Amber tokens; externalUrl has noopener noreferrer; ISR + NotionRenderer unchanged</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| project.externalUrl → browser navigation | User-provided URLs from Notion externalUrl field cross a trust boundary when rendered as anchors |
| /api/notion-cover → Notion API → browser | Image proxy fetches from Notion; the proxy is an existing route (IN-02 preserve) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-15 | Tampering | project.externalUrl anchor | mitigate | All externalUrl anchors MUST have rel="noopener noreferrer" target="_blank" to prevent window.opener attacks. This is user-controlled data from Notion. Verified in acceptance criteria |
| T-16-16 | Information Disclosure | NotionRenderer | accept | Existing vetted component; no changes; assume sanitized (RESEARCH.md security domain) |
| T-16-17 | Denial of Service | Cover image LCP | mitigate | fetchPriority="high" + sizes="100vw" required; prevents LCP regression per memory nextjs16-fetchpriority-quirk |
| T-16-SC | Tampering | npm installs | accept | No new packages |
</threat_model>

<verification>
After the task:
- grep "text-ink\|text-muted" src/app/projects/[slug]/page.tsx returns empty
- grep "revalidate = 1800" returns 1 match
- grep "fetchPriority" returns at least 1 match
- grep "noopener noreferrer" returns at least 1 match
- npx vitest run src/__tests__/pages/projects.test.tsx passes
</verification>

<success_criteria>
- Project detail shows full-bleed cover with fetchPriority="high" when cover exists (D-02)
- Breadcrumb: Home / Building / [Title] (PG-04, PG-05)
- External project URL opens in new tab with noopener noreferrer
- All v2 tokens removed; Pumpkin Amber vars used
- ISR (revalidate=1800), generateStaticParams, NotionRenderer unchanged (IN-01, IN-02)
</success_criteria>

<output>
Create `.planning/phases/16-interior-pages-on-notion-data/16-07-SUMMARY.md` when done
</output>
