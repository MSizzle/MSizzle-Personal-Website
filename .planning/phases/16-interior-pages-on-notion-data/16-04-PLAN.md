---
phase: 16-interior-pages-on-notion-data
plan: 04
type: execute
wave: 2
depends_on:
  - 16-01
  - 16-03
files_modified:
  - src/components/v3/card.tsx
  - src/app/writing/page.tsx
  - src/app/projects/page.tsx
autonomous: true
requirements:
  - PG-01
  - PG-04
  - IN-01
  - IN-02

must_haves:
  truths:
    - "D-01: Writing and Works indexes are calm + reading-first -- the card grid uses color-only hover (bg-bg-2 on hover, 150ms transition) with no marquees, no animated type, and reducedMotion respected"
    - "D-02: Writing and Works indexes are photo-forward -- each card displays a cover image loaded via the /api/notion-cover proxy (by post.id / project.id)"
    - "D-03: Writing and Works indexes render a PHOTO GRID OF CARDS (not ListRow lists) -- each Card shows cover image + title + excerpt in a responsive auto-fill grid (minmax 260px); ListRow is NOT used on these two index pages"
    - "D-04: Year-grouping is preserved as year section headers (YearBlock or equivalent mono accent heading) above each year's card grid, not as list rows"
    - "Writing index: PageHero (v3) is the page title block; no v2 inline header or atmosphere photo section remains"
    - "Works index: PageHero (v3) is the page title block; no v2 inline header or atmosphere photo section remains"
    - "Cover image for writing: rendered via /api/notion-cover?pageId={post.id}; graceful fallback renders the card without an image when the proxy returns 404 or post.cover is null"
    - "Cover image for works: rendered via /api/notion-cover?pageId={project.id}; graceful fallback renders the card without an image when project.image is null"
    - "Both pages retain export const revalidate = 1800 (ISR 30min, IN-01)"
    - "Both pages retain the defensive try/catch around getPublishedPosts()/getPublishedProjects() and show empty-state copy on failure"
    - "All v2 tokens (text-ink, text-muted, border-rule, bg-rule-strong) removed from both page files and from the extended card.tsx"
  artifacts:
    - path: "src/components/v3/card.tsx"
      provides: "Extended Card component with optional coverSrc + coverAlt props for index grid cover images"
      contains: "coverSrc"
    - path: "src/app/writing/page.tsx"
      provides: "Writing index: photo grid of cards grouped by year with PageHero (v3)"
      contains: "revalidate = 1800"
    - path: "src/app/projects/page.tsx"
      provides: "Works index: photo grid of cards grouped by year with PageHero (v3)"
      contains: "revalidate = 1800"
  key_links:
    - from: "src/app/writing/page.tsx"
      to: "src/lib/notion.ts"
      via: "getPublishedPosts import"
      pattern: "getPublishedPosts"
    - from: "src/app/writing/page.tsx"
      to: "src/components/v3/card.tsx"
      via: "Card import with coverSrc prop"
      pattern: "Card.*coverSrc"
    - from: "src/app/writing/page.tsx"
      to: "/api/notion-cover"
      via: "coverSrc prop value /api/notion-cover?pageId="
      pattern: "notion-cover\\?pageId"
    - from: "src/app/projects/page.tsx"
      to: "src/lib/notion-projects.ts"
      via: "getPublishedProjects import"
      pattern: "getPublishedProjects"
    - from: "src/app/projects/page.tsx"
      to: "src/components/v3/card.tsx"
      via: "Card import with coverSrc prop"
      pattern: "Card.*coverSrc"
---

<objective>
Rebuild the Writing and Works index pages as photo grids of cards, per D-02 and D-03 (explicit user decisions). The current ListRow-based layout is replaced with a responsive Card grid where each card shows a cover image (from the Notion cover proxy), a title, and an excerpt. Year-grouping is retained as section headers above each year's grid (D-04). PageHero replaces the old two-column atmosphere-photo title block. D-01 (calm + reading-first) is satisfied by color-only hover transitions and no autonomous animation.

Purpose: D-03 is the highest-priority layout decision for these two pages. The existing ListRow structure directly contradicts the locked "photo grid of cards" decision and must be replaced, not repainted.

Output: Extended card.tsx (optional coverSrc prop), two rebuilt page files. Notion loaders, ISR, and grouping logic are UNCHANGED (IN-01). All v2 tokens removed.
</objective>

<execution_context>
@/Users/Montster/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Montster/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/Montster/MSizzle Personal Website/.planning/phases/16-interior-pages-on-notion-data/16-CONTEXT.md
@/Users/Montster/MSizzle Personal Website/.planning/phases/16-interior-pages-on-notion-data/16-PATTERNS.md
@/Users/Montster/MSizzle Personal Website/.planning/phases/16-interior-pages-on-notion-data/16-UI-SPEC.md

<interfaces>
<!-- Exact types and functions the executor needs. Extracted from codebase. Do not re-derive via exploration. -->

From src/lib/notion.ts (verified in codebase):
  export interface BlogPost {
    id: string;
    slug: string;
    title: string;
    description: string;
    published: boolean;
    date: string;
    tags: string[];
    cover: string | null;   // Notion page cover metadata (non-null = cover exists)
    emoji: string | null;
    lastEdited: string;
  }
  export async function getPublishedPosts(): Promise<BlogPost[]>

From src/lib/notion-projects.ts (verified in codebase):
  export interface Project {
    id: string;
    slug: string;
    title: string;
    description: string;
    image: string | null;   // Project image (NOTE: field is "image", NOT "cover")
    emoji: string | null;
    externalUrl: string;
    tags: string[];
    featured: boolean;
    published: boolean;
    lastEdited: string;
  }
  export async function getPublishedProjects(): Promise<Project[]>

IMPORTANT: Project has NO "cover" field. Use project.image to determine whether a cover
exists. Pass coverSrc={`/api/notion-cover?pageId=${project.id}`} regardless -- the proxy
fetches the Notion page cover by page ID; project.image is used only as the existence
signal for conditional rendering (if project.image !== null, render the cover image slot).
Similarly for writing: if post.cover !== null, render the cover slot with
src={`/api/notion-cover?pageId=${post.id}`}.

From src/app/api/notion-cover/route.ts (verified):
  GET /api/notion-cover?pageId={id}
  Returns proxied image with Cache-Control: max-age=2700, stale-while-revalidate=300
  Returns 404 JSON if no cover on the page -- Next.js Image handles gracefully with
  unoptimized={true} or via onError (see Task 1 action for the recommended pattern)

From src/components/v3/card.tsx (current -- will be extended in Task 1):
  type Props = {
    kicker?: string;
    title: ReactNode;
    blurb?: ReactNode;
    href?: string;
  }
  export function Card({ kicker, title, blurb, href }: Props)
  -- Card is a link block (bg-bg p-[26px] hover:bg-bg-2 transition-colors) wrapping
     kicker + h3 + blurb. Does NOT currently have a cover image slot.

After Task 1 extends card.tsx, the new Props will be:
  type Props = {
    kicker?: string;
    title: ReactNode;
    blurb?: ReactNode;
    href?: string;
    coverSrc?: string;    // NEW -- URL passed directly (the proxy URL or external)
    coverAlt?: string;    // NEW -- alt text for the cover image
  }

From src/components/v3/page-hero.tsx (confirmed to exist):
  export function PageHero({ title, crumb, sub, outline? }: PageHeroProps)
  - title: string
  - crumb: string (breadcrumb label like "Home / Writing")
  - sub: string (subtitle below title)
  - outline: boolean (optional, false = filled sig)

PageHero props for /writing:
  title="Writing"
  crumb="Home / Writing"
  sub="Essays on philosophy, technology, and the texture of an attentive life."

PageHero props for /projects:
  title="Building"
  crumb="Home / Building"
  sub="Projects, products, and AI systems I'm building or have built -- through Prometheus and independent work."

Card grid container (from 16-UI-SPEC.md Card Grid Specifications):
  className="grid [grid-template-columns:repeat(auto-fill,minmax(260px,1fr))] gap-px bg-[var(--color-border)] border border-[var(--color-border)]"

Year section header: YearBlock from Plan 03's repainted editorial component (unchanged).
  Import: import { YearBlock } from "@/components/editorial/year-block"
  Usage: wrap the card grid for that year's items inside a year section container;
         OR render YearBlock as a standalone year heading above the grid container.
  DECISION: YearBlock currently wraps children (it renders the year label + its children).
  Because the card grid is a flat div (not a list), render YearBlock as a heading only
  (pass no children) then render the card grid as a sibling, OR if YearBlock's children
  API is flexible enough, wrap the grid div as a child. Read YearBlock's current interface
  before deciding -- keep it simple, avoid fighting the component API.

Between year sections: RuleStrong (already imported).
After all year sections: RuleStrong.
Writing page footer: WritingSubscribeCTA (keep at bottom).

Empty-state copy (per UI-SPEC.md Copywriting Contract):
  /writing: "No essays yet. Check back soon." -- text-[var(--color-text-muted)] text-center py-12
  /projects: "No projects yet. Check back soon." -- same styling

Token mapping (Pumpkin Amber, all v2 tokens must be removed):
  text-ink         --> text-[var(--color-text)]
  text-muted       --> text-[var(--color-text-muted)]
  border-rule      --> border-[var(--color-border)]
  bg-rule-strong   --> bg-[var(--color-border-strong)] (atmosphere photo wrapper bg)
  (Plan 03 already repainted the shared editorial components; only inline tokens in
   these page.tsx files and in card.tsx need attention here)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend Card component with optional cover image slot</name>
  <files>src/components/v3/card.tsx</files>
  <read_first>
    - src/components/v3/card.tsx -- read the entire current file; understand the existing Props type and JSX before extending
    - src/app/globals.css lines 1-80 -- verify --color-bg, --color-bg-2 are defined (they are in Pumpkin Amber @theme; confirm token names match what card.tsx already uses: bg-bg, hover:bg-bg-2)
    - 16-UI-SPEC.md "Standard Photo Grid (Writing, Works, Related Essays)" -- card content spec (kicker + h3 + blurb + optional cover image)
  </read_first>
  <behavior>
    - When coverSrc is provided: Next.js Image renders above the text content block, inside the link/div wrapper, with aspect-video or aspect-[4/3] and object-cover
    - When coverSrc is absent: card renders exactly as before (kicker + h3 + blurb only); no empty image placeholder
    - Existing Props (kicker, title, blurb, href) are UNCHANGED -- backward compatible
    - coverAlt defaults to "" (decorative) when not provided
    - Card link wrapper and hover styles are unchanged (bg-bg p-[26px] hover:bg-bg-2)
    - The cover image does NOT have its own padding -- it bleeds to the card edges above the padded text block
  </behavior>
  <action>
    Edit src/components/v3/card.tsx to add an optional cover image slot:

    (1) Add Image import from 'next/image' at the top.

    (2) Extend the Props type with two optional fields:
      coverSrc?: string;
      coverAlt?: string;

    (3) Add coverSrc and coverAlt to the destructured parameters.

    (4) Restructure the card inner layout so the cover image (when present) appears ABOVE
    the padded text block. The image renders full-width with no horizontal padding, and the
    text block (kicker + h3 + blurb) retains the p-[26px] padding.

    Concretely: change the outer wrapper from a single element with p-[26px] to a flex-col
    container WITHOUT padding, and move p-[26px] to a wrapping div around the text content
    (the existing inner variable). The cover image renders as the first child of the outer
    wrapper, before the text div.

    Cover image element:
      <div className="relative w-full aspect-[4/3] overflow-hidden">
        <Image
          src={coverSrc}
          alt={coverAlt ?? ""}
          fill
          sizes="(max-width: 768px) 100vw, (max-width: 1200px) 50vw, 33vw"
          className="object-cover"
        />
      </div>

    Render this div conditionally: only when coverSrc is defined.

    The text div wrapping the existing inner content:
      <div className="p-[26px]">
        {inner}
      </div>

    The outer Link (when href) becomes:
      <Link href={href} className="block bg-bg hover:bg-bg-2 transition-colors">
        {coverSrc && <div className="relative w-full aspect-[4/3] overflow-hidden">...</div>}
        <div className="p-[26px]">{inner}</div>
      </Link>

    The outer div (when no href) follows the same pattern.

    CRITICAL: The existing p-[26px] on the top-level Link/div MUST be removed and moved
    to the inner text div, so the cover image is not inset by card padding.

    Do NOT add fetchPriority to this card-level image -- these cards appear in a grid and
    are not guaranteed to be LCP. The pages themselves will pass coverSrc; if the first
    row of cards is above the fold, the page component should handle priority on those
    specific cards (see Task 2 note).
  </action>
  <verify>
    <automated>cd /Users/Montster/MSizzle\ Personal\ Website && npx vitest run src/__tests__/components/card.test.tsx 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - grep "coverSrc" src/components/v3/card.tsx returns at least 2 matches (in Props type + in component body)
    - grep "aspect-\[4/3\]" src/components/v3/card.tsx returns 1 match (cover image wrapper)
    - grep "import Image" src/components/v3/card.tsx returns 1 match
    - Existing Card usage without coverSrc renders identically to before (backward compatible)
    - npx vitest run src/__tests__/components/card.test.tsx passes -- test must assert: (a) coverSrc renders an image element, (b) absent coverSrc renders no img, (c) href wraps in Link, (d) text content (title/blurb) renders correctly in both cases
    - npx tsc --noEmit clean
  </acceptance_criteria>
  <done>Card component extended with optional coverSrc + coverAlt props; cover image renders above text block full-width; backward compatible with existing usage</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rebuild Writing index as photo grid of cards with PageHero and year-grouped sections</name>
  <files>src/app/writing/page.tsx</files>
  <read_first>
    - src/app/writing/page.tsx -- read the ENTIRE current file; confirm every import and the exact JSX structure before touching anything
    - src/components/v3/card.tsx -- read after Task 1 completes; confirm new coverSrc prop signature
    - src/components/v3/page-hero.tsx -- confirm PageHero props (title, crumb, sub, outline)
    - src/components/editorial/year-block.tsx -- confirm whether YearBlock wraps children or is a standalone heading; adapt usage accordingly
    - 16-UI-SPEC.md "Writing Index (/writing)" and "Standard Photo Grid" sections -- grid container classes
    - 16-CONTEXT.md D-01, D-02, D-03, D-04 -- photo grid with year section headers; calm hover only
  </read_first>
  <behavior>
    - PageHero renders as the page title block (replaces old two-column grid with atmosphere photo)
    - For each year group: a year section heading appears above a card grid div
    - Card grid div uses the standard photo grid container classes (auto-fill minmax 260px, gap-px, bg-border)
    - Each Card in the grid: coverSrc=/api/notion-cover?pageId={post.id} (when post.cover is non-null), title=post.title, blurb=post.description, href=/blog/{post.slug}, kicker=post.tags[0] (when tags exist)
    - Cards with post.cover === null: coverSrc omitted (Card renders text-only, still in the grid)
    - Empty state "No essays yet. Check back soon." renders when yearEntries.length === 0
    - export const revalidate = 1800 remains at top level
    - groupPostsByYear function and ISR/metadata exports are UNCHANGED
    - WritingSubscribeCTA remains at bottom
    - No ListRow usage remains in this file
    - No Image import for atmosphere photo remains
    - No v2 tokens (text-ink, text-muted, bg-rule-strong) remain in this file
  </behavior>
  <action>
    Edit src/app/writing/page.tsx:

    (1) Imports -- remove: Image (atmosphere photo no longer used), IntroLink. Add: PageHero
    from @/components/v3/page-hero, Card from @/components/v3/card. Keep: Fragment, Metadata,
    getPublishedPosts, BlogPost, formatMonthYear, RuleStrong, Rule, YearBlock,
    WritingSubscribeCTA. Remove: ListRow (no longer used on this page).

    (2) Replace the entire first <section> (the two-column atmosphere-photo title grid with
    text-label, text-page-title, text-ink, text-muted classes) with:
      <PageHero
        title="Writing"
        crumb="Home / Writing"
        sub="Essays on philosophy, technology, and the texture of an attentive life."
      />

    (3) In the year-grouped content section, keep the outer structure but replace the YearBlock
    + ListRow pattern with a YearBlock heading + card grid div for each year:

      For each year group:
        a) Render YearBlock with the year value (check YearBlock's API after reading the file;
           if it wraps children, pass the card grid as a child; if it renders standalone,
           render it then render the card grid as a sibling).
        b) Below (or inside) the YearBlock, render the card grid container:
             <div className="grid [grid-template-columns:repeat(auto-fill,minmax(260px,1fr))] gap-px bg-[var(--color-border)] border border-[var(--color-border)]">
               {yearPosts.map((post) => (
                 <Card
                   key={post.id}
                   href={`/blog/${post.slug}`}
                   title={post.title}
                   blurb={post.description}
                   kicker={post.tags?.[0]}
                   coverSrc={post.cover ? `/api/notion-cover?pageId=${post.id}` : undefined}
                   coverAlt={post.cover ? post.title : undefined}
                 />
               ))}
             </div>
        c) Add a RuleStrong divider between year sections (same as current logic with
           {i < arr.length - 1 && <RuleStrong />}).

    (4) Empty state: when yearEntries.length === 0, render:
      <p className="text-center py-12 text-[var(--color-text-muted)]">No essays yet. Check back soon.</p>

    (5) Keep: export const revalidate = 1800; groupPostsByYear; metadata export;
    RuleStrong after year sections; WritingSubscribeCTA at bottom.

    (6) Remove any remaining inline className strings containing text-ink, text-muted,
    bg-rule-strong, border-rule, or other v2 semantic tokens from this file.

    NOTE on page-level padding: the existing content section uses px-6 md:px-40 padding.
    Keep page-level horizontal padding on the section wrapper (or use the same pattern as
    the current file). The card grid's gap-px approach works inside any padded container.
  </action>
  <verify>
    <automated>cd /Users/Montster/MSizzle\ Personal\ Website && npx vitest run src/__tests__/pages/writing.test.tsx 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - grep "ListRow" src/app/writing/page.tsx returns no output (ListRow is not used on this page)
    - grep "text-ink\|text-muted\|bg-rule-strong\|border-rule" src/app/writing/page.tsx returns no output (zero v2 tokens)
    - grep "revalidate = 1800" src/app/writing/page.tsx returns 1 match
    - grep "PageHero" src/app/writing/page.tsx returns at least 1 match
    - grep "Card" src/app/writing/page.tsx returns at least 2 matches (import + usage)
    - grep "notion-cover" src/app/writing/page.tsx returns at least 1 match (coverSrc prop)
    - grep "getPublishedPosts" src/app/writing/page.tsx returns at least 1 match
    - grep -c "No essays yet" src/app/writing/page.tsx returns 1
    - npx vitest run src/__tests__/pages/writing.test.tsx passes -- test must assert: (a) Card components render for each post (not ListRow), (b) when getPublishedPosts returns [] the text "No essays yet" appears, (c) PageHero renders with title="Writing"
    - npx tsc --noEmit clean
  </acceptance_criteria>
  <done>Writing index rebuilt as photo grid of cards grouped by year with PageHero; ListRow removed; ISR + Notion loader preserved; empty state "No essays yet. Check back soon." present and verified by test; all v2 tokens removed</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Rebuild Works (Projects) index as photo grid of cards with PageHero and year-grouped sections</name>
  <files>src/app/projects/page.tsx</files>
  <read_first>
    - src/app/projects/page.tsx -- read the ENTIRE current file
    - src/components/v3/card.tsx -- confirm new coverSrc prop signature (from Task 1)
    - src/components/v3/page-hero.tsx -- confirm PageHero props (same as Task 2)
    - src/components/editorial/year-block.tsx -- same pattern as Task 2
    - 16-UI-SPEC.md "Works Index (/projects)" -- same structure as Writing Index; excerpt source is project.description; cover via notion-cover proxy
    - 16-CONTEXT.md D-02, D-03, D-04 -- photo-forward card grid + year section headers
  </read_first>
  <behavior>
    - PageHero renders as the page title block (replaces old two-column grid with atmosphere photo)
    - For each year group: year section heading above a card grid div
    - Each Card: coverSrc=/api/notion-cover?pageId={project.id} (when project.image is non-null, as image is the existence signal for projects), title=project.title, blurb=project.description, href=/projects/{project.slug}
    - Cards with project.image === null: coverSrc omitted
    - Empty state "No projects yet. Check back soon." renders when yearEntries.length === 0
    - export const revalidate = 1800 remains
    - groupProjectsByYear and ISR/metadata exports are UNCHANGED
    - No ListRow usage remains in this file
    - No Image import for atmosphere photo remains
    - No v2 tokens remain in this file
  </behavior>
  <action>
    Edit src/app/projects/page.tsx following the same pattern as Task 2:

    (1) Imports -- remove: Image, IntroLink. Add: PageHero from @/components/v3/page-hero,
    Card from @/components/v3/card. Keep: Fragment, Metadata, getPublishedProjects, Project,
    formatMonthYear, RuleStrong, Rule, YearBlock. Remove: ListRow.

    (2) Replace the entire first <section> (the two-column grid with the atmosphere photo and
    inline text-label/text-page-title/text-ink/text-muted classes) with:
      <PageHero
        title="Building"
        crumb="Home / Building"
        sub="Projects, products, and AI systems I'm building or have built -- through Prometheus and independent work."
      />
    (Use -- not an em dash; follow CLAUDE.md copy rules)

    (3) Year-grouped card grid -- same pattern as Task 2:
      Each Card:
        key={project.id}
        href={`/projects/${project.slug}`}
        title={project.title}
        blurb={project.description}
        kicker={project.tags?.[0]}
        coverSrc={project.image ? `/api/notion-cover?pageId=${project.id}` : undefined}
        coverAlt={project.image ? project.title : undefined}

    NOTE: project.image is the existence signal (not used as coverSrc value). The proxy
    always uses project.id regardless of what project.image contains. This matches how the
    notion-cover proxy works: it fetches the Notion page cover by page ID, not by the
    stored image URL.

    (4) Empty state: when yearEntries.length === 0, render:
      <p className="text-center py-12 text-[var(--color-text-muted)]">No projects yet. Check back soon.</p>

    (5) Keep: export const revalidate = 1800; groupProjectsByYear; metadata export;
    RuleStrong after year sections.

    (6) Remove any remaining v2 token classes from inline className strings in this file.
  </action>
  <verify>
    <automated>cd /Users/Montster/MSizzle\ Personal\ Website && npx vitest run src/__tests__/pages/projects.test.tsx 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - grep "ListRow" src/app/projects/page.tsx returns no output
    - grep "text-ink\|text-muted\|bg-rule-strong\|border-rule" src/app/projects/page.tsx returns no output
    - grep "revalidate = 1800" src/app/projects/page.tsx returns 1 match
    - grep "PageHero" src/app/projects/page.tsx returns at least 1 match
    - grep "Card" src/app/projects/page.tsx returns at least 2 matches (import + usage)
    - grep "notion-cover" src/app/projects/page.tsx returns at least 1 match (coverSrc prop)
    - grep "getPublishedProjects" src/app/projects/page.tsx returns at least 1 match
    - Card hrefs point to /projects/[slug] not /blog/[slug] (grep "projects.*slug" returns at least 1 match)
    - grep -c "No projects yet" src/app/projects/page.tsx returns 1
    - npx vitest run src/__tests__/pages/projects.test.tsx passes -- test must assert: (a) Card components render for each project (not ListRow), (b) when getPublishedProjects returns [] the text "No projects yet" appears, (c) PageHero renders with title="Building"
    - npx tsc --noEmit clean
  </acceptance_criteria>
  <done>Works index rebuilt as photo grid of cards grouped by year with PageHero; ListRow removed; ISR + Notion loader preserved; empty state "No projects yet. Check back soon." present and verified by test; all v2 tokens removed</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Notion API → page render | getPublishedPosts/getPublishedProjects fetch from Notion; content is author-controlled |
| /api/notion-cover proxy → Next.js Image → browser | Cover proxy fetches Notion's signed S3 URLs; existing proxy (IN-02); not rewritten here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-07 | Information Disclosure | Notion API failure handling | mitigate | Retain defensive try/catch in both pages; on Notion failure, return empty array and show empty-state UI -- no stack trace leaked to client |
| T-16-08 | Injection | Notion content in Card title/blurb | accept | post.title and post.description are author-controlled data from Monty's private Notion workspace; no public contributor form; React escapes all string props by default |
| T-16-09 | Information Disclosure | notion-cover proxy 404 response | accept | Proxy returns 404 JSON when no cover; Next.js Image renders a broken image; cards with no cover simply omit coverSrc (graceful fallback baked into the plan); no sensitive data in 404 body |
| T-16-SC | Tampering | npm installs | accept | No new npm packages; editing existing page files and extending card.tsx only |
</threat_model>

<verification>
After all tasks:
- grep "ListRow" src/app/writing/page.tsx src/app/projects/page.tsx returns empty (no ListRow usage on index pages)
- grep "text-ink\|text-muted\|bg-rule-strong\|border-rule" src/app/writing/page.tsx src/app/projects/page.tsx returns empty
- grep "coverSrc" src/components/v3/card.tsx returns at least 2 matches
- grep "revalidate = 1800" src/app/writing/page.tsx src/app/projects/page.tsx returns 2 matches (one per file)
- grep -c "No essays yet" src/app/writing/page.tsx returns 1
- grep -c "No projects yet" src/app/projects/page.tsx returns 1
- npx vitest run src/__tests__/pages/writing.test.tsx src/__tests__/pages/projects.test.tsx src/__tests__/components/card.test.tsx passes
- npx tsc --noEmit clean
</verification>

<success_criteria>
- Both index pages use PageHero (v3) for title block
- Both index pages render a photo grid of Cards (D-03) -- not ListRows
- Year-grouping preserved as section headers above each year's card grid (D-04)
- Cover images load via /api/notion-cover proxy; graceful fallback when null (D-02)
- Card hover is color-only (bg-bg-2); no autonomous animations (D-01)
- Card component extended with coverSrc prop, backward compatible
- All v2 semantic color tokens removed from all three modified files
- Notion loaders, ISR (30min), and year-grouping logic unchanged
- Empty states render correctly and are verified by automated tests
- Tests pass for card.tsx, writing/page.tsx, projects/page.tsx
</success_criteria>

<output>
Create `.planning/phases/16-interior-pages-on-notion-data/16-04-SUMMARY.md` when done
</output>
