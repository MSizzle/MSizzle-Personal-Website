---
phase: 16-interior-pages-on-notion-data
plan: 04
type: execute
wave: 2
depends_on:
  - 16-01
  - 16-03
files_modified:
  - src/app/writing/page.tsx
  - src/app/projects/page.tsx
autonomous: true
requirements:
  - PG-01
  - PG-04
  - IN-01

must_haves:
  truths:
    - "Writing index renders year-grouped essays with Pumpkin Amber tokens; no v2 text-ink or text-muted classes remain in the file"
    - "Writing index shows essay excerpt (description field) in each ListRow via the 'extra' prop"
    - "Writing index uses PageHero (v3 component) for the page title block instead of the old inline header"
    - "Projects index renders year-grouped works with Pumpkin Amber tokens; no v2 tokens remain"
    - "Projects index shows project description as excerpt in each ListRow"
    - "Both pages retain export const revalidate = 1800 (ISR 30min, IN-01)"
    - "Both pages retain the defensive try/catch around getPublishedPosts()/getPublishedProjects() and show empty-state copy on failure"
  artifacts:
    - path: "src/app/writing/page.tsx"
      provides: "Repainted writing index with v3 PageHero + Pumpkin Amber tokens"
      contains: "revalidate = 1800"
    - path: "src/app/projects/page.tsx"
      provides: "Repainted projects index with v3 PageHero + Pumpkin Amber tokens"
      contains: "revalidate = 1800"
  key_links:
    - from: "src/app/writing/page.tsx"
      to: "src/lib/notion.ts"
      via: "getPublishedPosts import"
      pattern: "getPublishedPosts"
    - from: "src/app/projects/page.tsx"
      to: "src/lib/notion-projects.ts"
      via: "getPublishedProjects import"
      pattern: "getPublishedProjects"
---

<objective>
Repaint the Writing and Works index pages from v2 paper/ink editorial styling onto Pumpkin Amber v3 tokens. Both pages keep the year-grouped ListRow structure per D-01/D-04 — the photo-forward card grid was considered but the reading-first calm interior direction (D-01) and the locked prototype 002 rows confirm ListRows are correct for indexes. PageHero (v3) replaces the old inline title block in each page.

Purpose: These are the highest-traffic interior pages. Repainted with v3 tokens and the shared v3 PageHero, they become the template for all other interior pages.

Output: Two repainted page files. Notion loaders, ISR, and grouping logic are UNCHANGED (IN-01 verification). All v2 tokens removed.
</objective>

<execution_context>
@/Users/Montster/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Montster/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/Montster/MSizzle Personal Website/.planning/phases/16-interior-pages-on-notion-data/16-CONTEXT.md
@/Users/Montster/MSizzle Personal Website/.planning/phases/16-interior-pages-on-notion-data/16-PATTERNS.md
@/Users/Montster/MSizzle Personal Website/.planning/phases/16-interior-pages-on-notion-data/16-UI-SPEC.md

<interfaces>
<!-- Exact types and functions the executor needs. Do not re-derive from codebase exploration. -->

From src/lib/notion.ts (verified in codebase):
  export interface BlogPost { id, title, slug, date, tags, description, emoji, cover, ... }
  export async function getPublishedPosts(): Promise<BlogPost[]>
  export function getPostExcerpt(blocks): string  (use post.description for index excerpt instead)

From src/lib/notion-projects.ts (verified in codebase):
  export interface Project { id, title, slug, description, tags, image, externalUrl, lastEdited, cover, ... }
  export async function getPublishedProjects(): Promise<Project[]>

From src/components/v3/page-hero.tsx (confirmed to exist in src/components/v3/):
  export function PageHero({ title, crumb, sub, outline? }: PageHeroProps)
  - title: string (displayed as sig h1)
  - crumb: string (breadcrumb label like "Home / Writing")
  - sub: string (subtitle below title)
  - outline: boolean (false = filled sig; true = sig-out stroke)

From 16-UI-SPEC.md Writing Index section:
  PageHero props for /writing:
    title="Writing"
    crumb="Home / Writing"
    sub="Essays on philosophy, technology, and life."
  PageHero props for /projects:
    title="Building"
    crumb="Home / Building"
    sub="Projects, products, and AI systems."

  Row layout (ListRow with big prop):
    title = post.title
    href = `/blog/${post.slug}`
    extra = post.description (excerpt)
    meta = formatMonthYear(post.date)

Year section headers: YearBlock (from Plan 03's repainted editorial component)
Between year groups: RuleStrong
After all groups: RuleStrong
Writing page footer: WritingSubscribeCTA (existing component — keep it)

Token mapping (from Plan 03):
  All text-ink → text-[var(--color-text)]
  All text-muted → text-[var(--color-text-muted)]
  All border-rule → border-[var(--color-border)] (now in ListRow per Plan 03)

Empty-state copy (per UI-SPEC.md Copywriting Contract):
  /writing: "No essays yet. Check back soon." — text-[var(--color-text-muted)] centered
  /projects: "No projects yet. Check back soon." — same styling
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Repaint Writing index page with v3 PageHero and Pumpkin Amber tokens</name>
  <files>src/app/writing/page.tsx</files>
  <read_first>
    - src/app/writing/page.tsx — read the ENTIRE existing file before editing; confirm every class name and import before touching anything
    - src/components/v3/page-hero.tsx — read to understand PageHero props interface (title, crumb, sub, outline)
    - 16-PATTERNS.md section "src/app/writing/page.tsx" — exact before/after diff guidance
    - 16-UI-SPEC.md "Writing Index (/writing)" section — layout diagram and spacing specs
    - src/components/editorial/ — Plan 03 already repainted Rule, ListRow, YearBlock; do NOT repaint them again here
  </read_first>
  <behavior>
    - Writing index renders with PageHero (v3) replacing the old inline 2-column title grid
    - YearBlock year sections appear with ListRow items for each essay
    - Each ListRow receives: title=post.title, href=/blog/slug, extra=post.description, meta=formatMonthYear(post.date), big=true
    - Empty state "No essays yet. Check back soon." renders when posts array is empty
    - export const revalidate = 1800 remains at top level
  </behavior>
  <action>
    Edit src/app/writing/page.tsx:

    (1) Imports: remove Image import (no longer needed for the atmosphere photo). Remove IntroLink import if the atmosphere-photo two-column layout is removed. Keep: Fragment, Metadata, getPublishedPosts, BlogPost, formatMonthYear, RuleStrong, Rule, ListRow, YearBlock, WritingSubscribeCTA. Add: PageHero from @/components/v3/page-hero.

    (2) Replace the first section (the old 2-column title grid with the Patricof09.jpg atmosphere photo and inline text-ink/text-muted classes) with:
      <PageHero title="Writing" crumb="Home / Writing" sub="Essays on philosophy, technology, and the texture of an attentive life." />

    (3) In the year-grouped content section: the existing YearBlock + ListRow loop is CORRECT — do not change its logic. Verify that ListRow now receives extra={post.description} (excerpt). If it was using extra={post.excerpt} or something else, switch it to extra={post.description}. The description field is already on BlogPost.

    (4) Replace all remaining inline v2 token classes (text-ink → text-[var(--color-text)], text-muted → text-[var(--color-text-muted)]) in any section of this file that the Plan 03 shared component repaint doesn't cover (i.e., any inline className strings directly on div/p/h elements within this page.tsx file).

    (5) Empty state: if posts.length === 0 after the try/catch, the year-grouped section renders nothing. Add an empty state paragraph "No essays yet. Check back soon." inside the year-grouped section when yearEntries.length === 0. Style: text-[var(--color-text-muted)] text-center py-12.

    (6) Keep: export const revalidate = 1800; groupPostsByYear function; metadata export; WritingSubscribeCTA at the bottom.
  </action>
  <verify>
    <automated>cd /Users/Montster/MSizzle\ Personal\ Website && npx vitest run src/__tests__/pages/writing.test.tsx 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - grep "text-ink\|text-muted\|border-rule" src/app/writing/page.tsx returns no output (zero inline v2 color tokens remain)
    - grep "revalidate = 1800" src/app/writing/page.tsx returns 1 match
    - grep "PageHero" src/app/writing/page.tsx returns at least 1 match
    - grep "getPublishedPosts" src/app/writing/page.tsx returns at least 1 match
    - npx vitest run src/__tests__/pages/writing.test.tsx passes
    - npx tsc --noEmit clean
  </acceptance_criteria>
  <done>Writing index repainted with v3 PageHero and Pumpkin Amber tokens; ISR + Notion loader preserved; empty state added</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Repaint Projects (Works) index page with v3 PageHero and Pumpkin Amber tokens</name>
  <files>src/app/projects/page.tsx</files>
  <read_first>
    - src/app/projects/page.tsx — read the ENTIRE existing file before editing
    - src/components/v3/page-hero.tsx — confirm PageHero props (same as Task 1)
    - 16-PATTERNS.md section "src/app/projects/page.tsx" — role-match guidance; same pattern as writing/page.tsx
    - 16-UI-SPEC.md "Works Index (/projects)" section — "same structure as Writing Index, with data from getPublishedProjects()"
  </read_first>
  <behavior>
    - Works index renders with PageHero replacing the old inline 2-column title grid
    - Year-grouped projects via groupProjectsByYear (uses lastEdited date — verify this function exists and is unchanged)
    - Each ListRow: title=project.title, href=/projects/slug, extra=project.description, meta=formatMonthYear(project.lastEdited), big=true
    - Empty state "No projects yet. Check back soon." renders when projects array is empty
    - export const revalidate = 1800 remains
  </behavior>
  <action>
    Edit src/app/projects/page.tsx following the same pattern as Task 1:

    (1) Remove Image import (atmosphere photo removed). Remove IntroLink if used. Keep: Fragment, Metadata, getPublishedProjects, Project, formatMonthYear, RuleStrong, Rule, ListRow, YearBlock. Add: PageHero from @/components/v3/page-hero.

    (2) Replace the first section (old 2-column title grid) with:
      <PageHero title="Building" crumb="Home / Building" sub="Projects, products, and AI systems I&rsquo;m building or have built -- through Prometheus and independent work." />
    (Use -- not em dash per CLAUDE.md site copy rules)

    (3) In the groupProjectsByYear loop: ensure ListRow gets href={`/projects/${project.slug}`} (not /blog/). Ensure extra={project.description}. The description field is already on Project type.

    (4) Replace all remaining inline v2 token classes in page.tsx inline className strings.

    (5) Empty state: when yearEntries.length === 0, render "No projects yet. Check back soon." with text-[var(--color-text-muted)] text-center py-12.

    (6) Keep: export const revalidate = 1800; groupProjectsByYear function; metadata export.
  </action>
  <verify>
    <automated>cd /Users/Montster/MSizzle\ Personal\ Website && npx vitest run src/__tests__/pages/projects.test.tsx 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - grep "text-ink\|text-muted\|border-rule" src/app/projects/page.tsx returns no output
    - grep "revalidate = 1800" src/app/projects/page.tsx returns 1 match
    - grep "PageHero" src/app/projects/page.tsx returns at least 1 match
    - grep "getPublishedProjects" src/app/projects/page.tsx returns at least 1 match
    - ListRow hrefs point to /projects/[slug] not /blog/[slug]
    - npx vitest run src/__tests__/pages/projects.test.tsx passes
    - npx tsc --noEmit clean
  </acceptance_criteria>
  <done>Projects index repainted with v3 PageHero and Pumpkin Amber tokens; uses project.description for excerpts; ISR preserved</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Notion API → page render | getPublishedPosts/getPublishedProjects fetch from Notion; content is author-controlled |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-07 | Information Disclosure | Notion API failure handling | mitigate | Retain defensive try/catch in both pages; on Notion failure, return empty array and show empty-state UI — no stack trace leaked to client |
| T-16-08 | Injection | Notion content in ListRow | accept | post.title and post.description are author-controlled data from Monty's private Notion workspace; no public contributor form; React escapes all string props by default |
| T-16-SC | Tampering | npm installs | accept | No new npm packages; editing existing page files only |
</threat_model>

<verification>
After all tasks:
- grep "text-ink\|text-muted\|border-rule" src/app/writing/page.tsx src/app/projects/page.tsx returns empty
- grep "revalidate = 1800" src/app/writing/page.tsx src/app/projects/page.tsx returns 2 matches (one per file)
- npx vitest run src/__tests__/pages/writing.test.tsx src/__tests__/pages/projects.test.tsx passes
- npx tsc --noEmit clean
</verification>

<success_criteria>
- Both index pages use PageHero (v3) for title block
- All v2 semantic color tokens removed from both files
- Notion loaders, ISR (30min), and year-grouping logic are unchanged
- Empty states render correctly
- Tests pass
</success_criteria>

<output>
Create `.planning/phases/16-interior-pages-on-notion-data/16-04-SUMMARY.md` when done
</output>
