---
phase: 16-interior-pages-on-notion-data
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/editorial/list-row.tsx
  - src/components/editorial/year-block.tsx
  - src/components/editorial/rule.tsx
  - src/components/editorial/rule-strong.tsx
  - src/components/editorial/section-label.tsx
  - src/components/editorial/all-link.tsx
autonomous: true
requirements:
  - PG-01
  - PG-05

must_haves:
  truths:
    - "list-row.tsx uses Pumpkin Amber CSS vars (--color-text, --color-text-muted, --color-border) not v2 semantic tokens (text-ink, text-muted, border-rule)"
    - "year-block.tsx uses --color-text not text-ink"
    - "rule.tsx border uses --color-border not border-rule"
    - "rule-strong.tsx border uses --color-border-strong not border-rule-strong"
    - "section-label.tsx text uses --color-text not text-ink"
    - "all-link.tsx border and text use Pumpkin Amber vars not v2 tokens"
    - "No v2 semantic tokens (text-ink, text-muted, border-rule, border-rule-strong, border-ink) remain in any of the six files"
  artifacts:
    - path: "src/components/editorial/list-row.tsx"
      provides: "Repainted ListRow with Pumpkin Amber tokens"
      contains: "var(--color-text)"
    - path: "src/components/editorial/rule.tsx"
      provides: "Repainted Rule with var(--color-border)"
    - path: "src/components/editorial/rule-strong.tsx"
      provides: "Repainted RuleStrong with var(--color-border-strong)"
  key_links:
    - from: "src/app/events/page.tsx"
      to: "src/components/editorial/list-row.tsx"
      via: "ListRow import"
      pattern: "ListRow"
    - from: "src/app/writing/page.tsx"
      to: "src/components/editorial/year-block.tsx"
      via: "YearBlock import"
      pattern: "YearBlock"
---

<objective>
Repaint the six v2 editorial shared components (ListRow, YearBlock, Rule, RuleStrong, SectionLabel, AllLink) from v2 paper/ink semantic tokens onto Pumpkin Amber CSS variable references. These components are used across all interior index pages (writing, projects, events, links, about) so this Wave 1 plan unblocks all index-page plans in Waves 2-3.

Purpose: All index pages use these shared components. Token repaint here flows through to every consumer automatically — no per-page repaint needed for these primitives.

Output: Six files in src/components/editorial/ with v2 tokens replaced by Pumpkin Amber var() references. Structure and logic are UNCHANGED — only className token strings are updated.
</objective>

<execution_context>
@/Users/Montster/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Montster/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/Montster/MSizzle Personal Website/.planning/phases/16-interior-pages-on-notion-data/16-CONTEXT.md
@/Users/Montster/MSizzle Personal Website/.planning/phases/16-interior-pages-on-notion-data/16-PATTERNS.md
@/Users/Montster/MSizzle Personal Website/.planning/phases/16-interior-pages-on-notion-data/16-UI-SPEC.md

<interfaces>
<!-- Token mapping contract. Source: globals.css @theme block (lines 3-44). -->

v2 semantic token  →  v3 Pumpkin Amber replacement
─────────────────────────────────────────────────────────────
text-ink           →  text-[var(--color-text)]          (#2a1808 Roasted Cocoa)
text-muted         →  text-[var(--color-text-muted)]    (rgba(42,24,8,0.55))
border-rule        →  border-[var(--color-border)]       (rgba(42,24,8,0.18))
border-rule-strong →  border-[var(--color-border-strong)](rgba(42,24,8,0.40))
border-ink         →  border-[var(--color-text)]         (used in all-link underline)

NOTE: text-label, text-caption, text-meta, text-list-title, text-list-title-home are
TYPE SCALE classes (not color tokens) — they define font-size/weight/leading and are
defined in globals.css @theme. Verify they still exist in the v3 globals.css before
assuming they need replacing. If they use --color-text already, leave them. If they
have embedded color, remap.

Current editorial component tokens to repaint (grep output confirmed):
  list-row.tsx:    text-ink, text-muted, border-rule
  year-block.tsx:  text-ink (in year label)
  rule.tsx:        border-rule (in HR classname)
  rule-strong.tsx: border-rule-strong (in HR classname)
  section-label.tsx: text-ink, text-muted
  all-link.tsx:    border-ink, text-ink (in link underline)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repaint list-row.tsx, year-block.tsx, section-label.tsx, all-link.tsx</name>
  <files>
    src/components/editorial/list-row.tsx,
    src/components/editorial/year-block.tsx,
    src/components/editorial/section-label.tsx,
    src/components/editorial/all-link.tsx
  </files>
  <read_first>
    - src/components/editorial/list-row.tsx — read the entire file; confirm all class names before editing
    - src/components/editorial/year-block.tsx — read the entire file
    - src/components/editorial/section-label.tsx — read the entire file
    - src/components/editorial/all-link.tsx — read the entire file
    - src/app/globals.css (lines 1-80) — verify the @theme block; confirm text-label, text-caption, text-meta, text-list-title are type-scale tokens (not color), verify border-rule is NOT defined (it's a v2 semantic class that should be replaced)
    - 16-PATTERNS.md "Editorial Component Token Repaints" section — per-class mapping guidance
    - 16-UI-SPEC.md Color section — exact token values to use
  </read_first>
  <action>
    For each file, perform the token substitutions using the mapping above. Only change className strings — do not alter component logic, props, JSX structure, or TypeScript types.

    list-row.tsx:
      - Replace "border-rule" → "border-[var(--color-border)]" (in the cn() call on the outer div)
      - Replace "text-ink" → "text-[var(--color-text)]" (title div)
      - Replace "text-muted" → "text-[var(--color-text-muted)]" (extra div, meta div)
      - Keep "text-list-title", "text-list-title-home", "text-caption", "text-meta" unchanged (type scale, not color)

    year-block.tsx:
      - Replace "text-ink" → "text-[var(--color-text)]" (year label div)
      - Keep "text-label" unchanged (type scale class)

    section-label.tsx:
      - Replace "text-ink" → "text-[var(--color-text)]"
      - Replace "text-muted" → "text-[var(--color-text-muted)]" (if numeral span uses text-muted)

    all-link.tsx:
      - Replace "border-ink" → "border-[var(--color-text)]" (bottom border underline)
      - Replace "text-ink" → "text-[var(--color-text)]"
      - Keep "text-label", "uppercase" unchanged (type scale and utility)

    After each file: verify grep finds zero v2 tokens (text-ink, text-muted, border-rule, border-ink) remaining.
  </action>
  <verify>
    <automated>grep -r "text-ink\|text-muted\|border-rule\|border-ink" /Users/Montster/MSizzle\ Personal\ Website/src/components/editorial/list-row.tsx /Users/Montster/MSizzle\ Personal\ Website/src/components/editorial/year-block.tsx /Users/Montster/MSizzle\ Personal\ Website/src/components/editorial/section-label.tsx /Users/Montster/MSizzle\ Personal\ Website/src/components/editorial/all-link.tsx 2>/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - grep for "text-ink|text-muted|border-rule|border-ink" in these 4 files returns no output (zero matches)
    - Each file retains its original export name, props interface, and JSX structure
    - npx tsc --noEmit passes
    - All Pumpkin Amber replacements use the var() CSS variable syntax: text-[var(--color-text)], text-[var(--color-text-muted)], border-[var(--color-border)]
  </acceptance_criteria>
  <done>Four editorial components repainted to Pumpkin Amber tokens; no v2 semantic color tokens remain; structure unchanged</done>
</task>

<task type="auto">
  <name>Task 2: Repaint rule.tsx and rule-strong.tsx divider components</name>
  <files>
    src/components/editorial/rule.tsx,
    src/components/editorial/rule-strong.tsx
  </files>
  <read_first>
    - src/components/editorial/rule.tsx — read the full file (it's tiny; confirm exact className)
    - src/components/editorial/rule-strong.tsx — read the full file
    - src/app/globals.css (lines 1-30) — confirm --color-border and --color-border-strong exist in @theme
    - 16-PATTERNS.md section "src/components/editorial/rule.tsx" and "src/components/editorial/rule-strong.tsx"
  </read_first>
  <action>
    rule.tsx: The single hr element has className "border-0 border-t border-rule". Replace "border-rule" with "border-[var(--color-border)]". Result: className="border-0 border-t border-[var(--color-border)]". No other changes.

    rule-strong.tsx: The single hr element has className "border-0 border-t border-rule-strong". Replace "border-rule-strong" with "border-[var(--color-border-strong)]". Result: className="border-0 border-t border-[var(--color-border-strong)]". No other changes.

    Both files remain Server Components (no client import needed). Both remain one-liners.
  </action>
  <verify>
    <automated>grep -c "border-rule" /Users/Montster/MSizzle\ Personal\ Website/src/components/editorial/rule.tsx /Users/Montster/MSizzle\ Personal\ Website/src/components/editorial/rule-strong.tsx 2>/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - grep "border-rule" in rule.tsx returns empty (no matches — the old "border-rule" class is gone)
    - grep "border-rule-strong" in rule-strong.tsx returns empty
    - rule.tsx contains "border-[var(--color-border)]"
    - rule-strong.tsx contains "border-[var(--color-border-strong)]"
    - npx tsc --noEmit clean
  </acceptance_criteria>
  <done>Rule and RuleStrong repainted to Pumpkin Amber border tokens; v2 border-rule and border-rule-strong classes removed</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS class strings → Tailwind JIT | Tailwind v4 scans class strings at build time; inline var() syntax is safe and standard |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-06 | Tampering | Editorial component token repaint | accept | Pure CSS class renaming; no data flow changes; no user input; no security surface change |
| T-16-SC | Tampering | npm installs | accept | No new npm packages; pure string substitution in existing component files |
</threat_model>

<verification>
After all tasks:
- grep -r "text-ink\|text-muted\|border-rule\|border-ink\|border-rule-strong" src/components/editorial/ returns zero lines (excluding comments)
- npx tsc --noEmit passes
- All 6 editorial files still export their original named exports (ListRow, YearBlock, Rule, RuleStrong, SectionLabel, AllLink)
</verification>

<success_criteria>
- Zero v2 semantic color tokens (text-ink, text-muted, border-rule, border-rule-strong, border-ink) remain in any of the six editorial component files
- Pumpkin Amber var() references used in place of each removed token
- No logic or structural changes — only className token substitutions
- TypeScript clean
</success_criteria>

<output>
Create `.planning/phases/16-interior-pages-on-notion-data/16-03-SUMMARY.md` when done
</output>
