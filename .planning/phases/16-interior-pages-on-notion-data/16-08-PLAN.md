---
phase: 16-interior-pages-on-notion-data
plan: 08
type: execute
wave: 3
depends_on:
  - 16-02
  - 16-03
files_modified:
  - src/app/about/page.tsx
  - src/app/prometheus/page.tsx
  - src/app/newsletter/page.tsx
  - src/app/events/page.tsx
  - src/app/links/page.tsx
autonomous: true
requirements:
  - PG-01
  - PG-05
  - IN-01

must_haves:
  truths:
    - "All five pages (About, Prometheus, Newsletter, Events, Links) use Pumpkin Amber tokens — no v2 text-ink, text-muted, border-rule, border-ink tokens remain in any of these five files"
    - "All five pages use PageHero (v3) for the page title block, replacing inline 2-column title grids"
    - "About page copy follows CLAUDE.md rules: no em dashes, no location, Georgetown University only, professional identity = Founder of Prometheus"
    - "Events page retains its Notion loader (getUpcomingEvents/getPastEvents) and revalidate = 1800 (IN-01)"
    - "Links page retains its external link list structure; external links have rel='noopener noreferrer' target='_blank'"
    - "Prometheus page external link to prometheus.today has rel='noopener noreferrer' target='_blank'"
  artifacts:
    - path: "src/app/about/page.tsx"
      provides: "Repainted About page with v3 PageHero + Pumpkin Amber tokens"
    - path: "src/app/events/page.tsx"
      provides: "Repainted Events page; Notion loader + ISR preserved (IN-01)"
    - path: "src/app/links/page.tsx"
      provides: "Repainted Links page with v3 PageHero + Pumpkin Amber tokens"
  key_links:
    - from: "src/app/events/page.tsx"
      to: "src/lib/notion-events.ts"
      via: "getUpcomingEvents/getPastEvents import"
      pattern: "getUpcomingEvents\|getPastEvents"
    - from: "src/app/about/page.tsx"
      to: "src/components/v3/page-hero.tsx"
      via: "PageHero import"
      pattern: "PageHero"
---

<objective>
Repaint the five remaining static/semi-static interior pages (About, Prometheus, Newsletter, Events, Links) onto Pumpkin Amber v3 tokens, replacing inline title grids with PageHero in each. The Events page Notion loader (getUpcomingEvents / getPastEvents) and ISR are preserved unchanged (IN-01). This plan runs in Wave 3 parallel with Plans 06 and 07 since all five files are distinct from those plans' files.

Purpose: Completes PG-01 coverage across all interior pages. After this plan, all interior routes render in the v3 Pumpkin Amber system.

Output: Five repainted page files. No Notion loaders are changed. Copy rules from CLAUDE.md (no em dashes, no location, Georgetown University only, Founder of Prometheus identity) must be verified in About/Prometheus copy.
</objective>

<execution_context>
@/Users/Montster/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Montster/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/Montster/MSizzle Personal Website/.planning/CLAUDE.md
@/Users/Montster/MSizzle Personal Website/.planning/phases/16-interior-pages-on-notion-data/16-CONTEXT.md
@/Users/Montster/MSizzle Personal Website/.planning/phases/16-interior-pages-on-notion-data/16-PATTERNS.md
@/Users/Montster/MSizzle Personal Website/.planning/phases/16-interior-pages-on-notion-data/16-UI-SPEC.md

<interfaces>
<!-- Read each page file before editing. Below are the structural patterns to follow. -->

Token repaint mapping (applies to ALL five files):
  text-ink          → text-[var(--color-text)]
  text-muted        → text-[var(--color-text-muted)]
  border-rule       → border-[var(--color-border)]
  border-ink        → border-[var(--color-text)]  (used in all-link underlines)
  text-body-lead    → text-lg text-[var(--color-text-dim)]  (if body-lead was a combined utility)
  text-page-title   → leave if it's a pure type-scale token; only replace if it embeds color

PageHero props for each page:
  /about:      title="About"      crumb="Home / About"      sub="Builder, writer. Founder of Prometheus."
  /prometheus: title="Prometheus" crumb="Home / Prometheus" sub="AI integrations and education."
  /newsletter: title="Newsletter" crumb="Home / Newsletter" sub="Monty Monthly -- a monthly dispatch."
  /events:     title="Events"     crumb="Home / Events"     sub="Speaking, panels, and public appearances."
  /links:      title="Links"      crumb="Home / Links"      sub="Things worth reading and watching."
  (Use -- not em dashes per CLAUDE.md copy rules)

From src/lib/notion-events.ts (do not modify; use these):
  export async function getUpcomingEvents(): Promise<EventItem[]>
  export async function getPastEvents(): Promise<EventItem[]>
  export async function getPublishedEvents(): Promise<EventItem[]>
  export type EventItem { id, title, date, location, description, ... }

Events page ISR (PRESERVE UNCHANGED):
  export const revalidate = 1800

Links page external links:
  All external anchors: target="_blank" rel="noopener noreferrer" (already present; verify)

Prometheus page external link to prometheus.today:
  Must have target="_blank" rel="noopener noreferrer"

About page copy rules (from CLAUDE.md + UI-SPEC copywriting contract):
  - No em dashes (use -- instead)
  - No location ("Based in Washington D.C." must be REMOVED from any copy)
  - "Georgetown University" only (not "Georgetown" alone)
  - Sole professional identity: "Founder of Prometheus" (not "startup founder" or vague)
  - Check existing about/page.tsx for violations before accepting the repaint

Newsletter page:
  Likely contains a Substack outbound link; keep it; verify target="_blank" rel="noopener noreferrer" on Substack links
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repaint About and Prometheus pages with PageHero and Pumpkin Amber tokens</name>
  <files>src/app/about/page.tsx, src/app/prometheus/page.tsx</files>
  <read_first>
    - src/app/about/page.tsx — read the ENTIRE file; map every text-ink/text-muted occurrence; check for location copy ("Washington" or "D.C.") to remove; check for em dashes to replace with --
    - src/app/prometheus/page.tsx — read the ENTIRE file; check external links for noopener noreferrer
    - src/components/v3/page-hero.tsx — confirm PageHero props
    - 16-UI-SPEC.md "About Page (/about)" section
    - CLAUDE.md project instructions — site copy rules (no em dash, no location, Georgetown University only, Founder of Prometheus)
  </read_first>
  <action>
    For BOTH files, apply the same pattern:

    (1) Add PageHero import from '@/components/v3/page-hero'.
    (2) Remove any Image imports for the atmosphere photo (2-column grid with photo removed).
    (3) Replace the inline 2-column title grid (text-ink h1 + text-muted p) with PageHero with the correct title/crumb/sub per the interface above.
    (4) Apply token repaint mapping to ALL remaining inline className strings in the file.
    (5) Remove any import no longer used after removing the atmosphere photo (e.g., Image from next/image — only remove if no other images remain on the page).

    ABOUT PAGE SPECIFIC:
    - Scan the full file for "Washington" or "D.C." in copy strings — remove or rewrite without location per CLAUDE.md
    - Scan for em dashes (—) in copy strings — replace with " -- " per CLAUDE.md
    - Verify the professional identity copy uses "Founder of Prometheus" (not abbreviated)
    - Georgetown University references: ensure it's spelled "Georgetown University" (not just "Georgetown")

    PROMETHEUS PAGE SPECIFIC:
    - Locate the prometheus.today anchor — confirm it has target="_blank" rel="noopener noreferrer"
    - If it doesn't, add those attributes
    - Scan for JSON-LD import (JsonLd, buildFaqPageSchema) — keep these; they are SEO infrastructure
    - Remove atmosphere photo 2-column layout; replace with PageHero
  </action>
  <verify>
    <automated>grep -n "text-ink\|text-muted\|border-rule\|Washington\|D\.C\." /Users/Montster/MSizzle\ Personal\ Website/src/app/about/page.tsx /Users/Montster/MSizzle\ Personal\ Website/src/app/prometheus/page.tsx 2>/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - grep "text-ink\|text-muted\|border-rule" about/page.tsx returns no matches
    - grep "text-ink\|text-muted\|border-rule" prometheus/page.tsx returns no matches
    - grep "Washington\|D\.C\." about/page.tsx returns no matches (location removed per CLAUDE.md)
    - grep "PageHero" about/page.tsx returns at least 1 match
    - grep "PageHero" prometheus/page.tsx returns at least 1 match
    - grep "noopener noreferrer" prometheus/page.tsx returns at least 1 match (prometheus.today link)
    - npx tsc --noEmit clean
  </acceptance_criteria>
  <done>About and Prometheus pages repainted; PageHero replaces inline title blocks; location copy removed from About; prometheus.today link has noopener noreferrer; v3 tokens throughout</done>
</task>

<task type="auto">
  <name>Task 2: Repaint Newsletter, Events, and Links pages with PageHero and Pumpkin Amber tokens</name>
  <files>src/app/newsletter/page.tsx, src/app/events/page.tsx, src/app/links/page.tsx</files>
  <read_first>
    - src/app/newsletter/page.tsx — read the ENTIRE file; check for Substack outbound links (must have target="_blank" rel="noopener noreferrer"); check v2 tokens
    - src/app/events/page.tsx — read the ENTIRE file; note that events has ISR (revalidate=1800) and two Notion loader calls — DO NOT CHANGE loader logic; map all v2 tokens; check for border-rule, border-ink usages throughout the file (events has many)
    - src/app/links/page.tsx — read the ENTIRE file; check external link anchors for target and rel; map v2 tokens
    - src/components/v3/page-hero.tsx — confirm props (same as Task 1)
    - 16-PATTERNS.md shared pattern guidance
    - 16-UI-SPEC.md for each page section
  </read_first>
  <action>
    Apply the same pattern as Task 1 to all three files:

    NEWSLETTER PAGE:
    - Add PageHero import; replace inline title grid with PageHero title="Newsletter" crumb="Home / Newsletter" sub="Monty Monthly -- a monthly dispatch."
    - All Substack links must have target="_blank" rel="noopener noreferrer"
    - Token repaint: text-ink → text-[var(--color-text)], text-muted → text-[var(--color-text-muted)]
    - Remove em dashes from copy strings (replace with --)

    EVENTS PAGE:
    - Add PageHero import; replace inline title grid with PageHero title="Events" crumb="Home / Events" sub="Speaking, panels, and public appearances."
    - PRESERVE: export const revalidate = 1800; getUpcomingEvents(); getPastEvents() calls; all event rendering logic
    - Token repaint throughout (events has many border-rule, border-ink, text-ink, text-muted occurrences — each must be mapped)
    - Event location strings in the JSX: remove if they contain "D.C." in hardcoded copy (but event.location from Notion API is dynamic data, not author copy — leave it)
    - Check: any em dashes in static copy strings → replace with --

    LINKS PAGE:
    - Add PageHero import; replace inline title grid with PageHero title="Links" crumb="Home / Links" sub="Things worth reading and watching."
    - Verify all link anchors pointing to external URLs have target="_blank" rel="noopener noreferrer"
    - Token repaint: text-ink, text-muted, border-rule → Pumpkin Amber vars
    - border-rule (in the custom row className at links/page.tsx:29) → border-[var(--color-border)]
  </action>
  <verify>
    <automated>grep -c "text-ink\|text-muted\|border-rule\|border-ink" /Users/Montster/MSizzle\ Personal\ Website/src/app/newsletter/page.tsx /Users/Montster/MSizzle\ Personal\ Website/src/app/events/page.tsx /Users/Montster/MSizzle\ Personal\ Website/src/app/links/page.tsx 2>/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - grep "text-ink\|text-muted\|border-rule\|border-ink" in all three files returns no matches (0 across all files)
    - grep "revalidate = 1800" src/app/events/page.tsx returns 1 match (ISR preserved)
    - grep "getUpcomingEvents\|getPastEvents" src/app/events/page.tsx returns at least 1 match (loader preserved)
    - grep "PageHero" src/app/newsletter/page.tsx returns at least 1 match
    - grep "PageHero" src/app/events/page.tsx returns at least 1 match
    - grep "PageHero" src/app/links/page.tsx returns at least 1 match
    - grep "noopener noreferrer" src/app/newsletter/page.tsx returns at least 1 match (Substack link)
    - npx tsc --noEmit clean
  </acceptance_criteria>
  <done>Newsletter, Events, and Links repainted with PageHero and Pumpkin Amber tokens; Events ISR + Notion loaders unchanged; external links have noopener noreferrer</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Links page external URLs → browser navigation | /links page renders user-curated external anchors |
| Newsletter Substack link → external service | Outbound link to montymonthly.substack.com |
| Prometheus.today → external site | Outbound link from Prometheus page |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-18 | Tampering | External links (links/prometheus/newsletter pages) | mitigate | All external link anchors must have rel="noopener noreferrer" target="_blank". Verified per acceptance criteria on each file |
| T-16-19 | Information Disclosure | Notion Events data | accept | Event data fetched from Monty's private Notion workspace; public read-only content; no auth, no sensitive fields |
| T-16-20 | Injection | Static page copy | accept | All copy is hardcoded in source files (no user input); author-controlled only; React escapes string props by default |
| T-16-SC | Tampering | npm installs | accept | No new packages |
</threat_model>

<verification>
After both tasks:
- grep "text-ink\|text-muted\|border-rule\|border-ink" in all five files returns 0 matches
- grep "revalidate = 1800" src/app/events/page.tsx returns 1 match
- PageHero import present in all five files
- npx tsc --noEmit clean
- npx vitest run (full suite) green
</verification>

<success_criteria>
- All five pages (About, Prometheus, Newsletter, Events, Links) use PageHero (v3) and Pumpkin Amber tokens
- No v2 semantic color tokens (text-ink, text-muted, border-rule, border-ink) remain in any of the five files
- About page copy is CLAUDE.md compliant (no location, no em dashes, Georgetown University only)
- Events ISR + Notion loaders preserved unchanged (IN-01)
- All external links have rel="noopener noreferrer" target="_blank"
- TypeScript clean
</success_criteria>

<output>
Create `.planning/phases/16-interior-pages-on-notion-data/16-08-SUMMARY.md` when done
</output>
