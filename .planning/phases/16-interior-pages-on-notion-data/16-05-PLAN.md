---
phase: 16-interior-pages-on-notion-data
plan: 05
type: execute
wave: 2
depends_on:
  - 16-01
  - 16-02
files_modified:
  - src/app/uses/page.tsx
  - src/app/watching/page.tsx
autonomous: true
requirements:
  - PG-02
  - PG-03
  - PG-05

must_haves:
  truths:
    - "/uses page renders PageHero + UsesList with all four D-06 groups (AI & Development, Productivity, Communication, Hardware)"
    - "/watching page renders PageHero + VideoCard grid with 6 placeholder entries from WATCHING_ITEMS"
    - "VideoCard anchor elements have target='_blank' and rel='noopener noreferrer' (D-09)"
    - "YouTube thumbnail URLs use the pattern img.youtube.com/vi/{id}/hqdefault.jpg (D-08)"
    - "/uses page has export const revalidate = 1800"
    - "/watching page has export const revalidate = 1800"
    - "Both pages have metadata exports (title, description, canonical)"
    - "Breadcrumbs appear on /uses and /watching per D-13"
  artifacts:
    - path: "src/app/uses/page.tsx"
      provides: "New /uses page (D-05, D-06, PG-02)"
    - path: "src/app/watching/page.tsx"
      provides: "New /watching page (D-07, D-08, D-09, D-10, PG-03)"
  key_links:
    - from: "src/app/uses/page.tsx"
      to: "src/lib/uses.ts"
      via: "USES_DATA import"
      pattern: "USES_DATA"
    - from: "src/app/watching/page.tsx"
      to: "src/lib/watching.ts"
      via: "WATCHING_ITEMS import"
      pattern: "WATCHING_ITEMS"
    - from: "src/app/watching/page.tsx"
      to: "img.youtube.com"
      via: "Next.js Image src pattern"
      pattern: "img.youtube.com/vi"
---

<objective>
Build the two new pages /uses and /watching. Both are static hardcoded pages (no Notion fetch) using data from Plans 01's data modules. /uses renders grouped tools via UsesList (v3 component). /watching renders a YouTube card grid via VideoCard (v3 component) with thumbnails auto-derived from video IDs and cards opening YouTube in a new tab.

Purpose: PG-02 and PG-03 deliverables. These are the only new routes this phase — all others are repaints of existing routes.

Output: src/app/uses/page.tsx and src/app/watching/page.tsx. Both are Server Components (no 'use client' — static data, no hooks). Both include Breadcrumbs per D-13.
</objective>

<execution_context>
@/Users/Montster/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Montster/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/Montster/MSizzle Personal Website/.planning/phases/16-interior-pages-on-notion-data/16-CONTEXT.md
@/Users/Montster/MSizzle Personal Website/.planning/phases/16-interior-pages-on-notion-data/16-PATTERNS.md
@/Users/Montster/MSizzle Personal Website/.planning/phases/16-interior-pages-on-notion-data/16-UI-SPEC.md

<interfaces>
<!-- Exact component signatures. Read source files before implementing. -->

From src/components/v3/page-hero.tsx (confirmed to exist):
  export function PageHero({ title, crumb, sub, outline? }: PageHeroProps)

From src/components/v3/uses-list.tsx (confirmed to exist):
  export function UsesList({ groups }: { groups: UsesGroup[] })
  — expects UsesGroup[] which matches USES_DATA type from src/lib/uses.ts

From src/components/v3/video-card.tsx (confirmed to exist):
  export function VideoCard({ title, channel, href }: VideoCardProps)
  — href is the full YouTube URL; component should render <a target="_blank" rel="noopener noreferrer">
  — Read the component file to confirm it accepts these exact props before coding page.tsx

From src/components/editorial/rule-strong.tsx (repainted in Plan 03):
  export function RuleStrong()

From src/components/seo/breadcrumbs.tsx (confirmed to exist):
  export function Breadcrumbs({ items }: { items: BreadcrumbItem[] })
  type BreadcrumbItem = { name: string; href?: string; disabled?: boolean }
  
From src/lib/uses.ts (created in Plan 01):
  export const USES_DATA: UsesGroup[]
  
From src/lib/watching.ts (created in Plan 01):
  export const WATCHING_ITEMS: WatchingItem[]
  type WatchingItem = { id: string; title: string; channel: string; url: string }

YouTube thumbnail URL pattern (D-08):
  https://img.youtube.com/vi/{item.id}/hqdefault.jpg
  — requires img.youtube.com in next.config.ts remotePatterns (done in Plan 01)
  — Next.js Image: width=480, height=360, sizes="(max-width:600px) 100vw, (max-width:1024px) 50vw, 33vw"
  — alt text = item.title

From src/app/photos/page.tsx (analog for page structure):
  export const revalidate = 1800
  export const metadata: Metadata = { title, description, alternates, openGraph }
  Static Server Component returning JSX with PageHero + content section

From 16-UI-SPEC.md:
  /uses spacing: section px-6 md:px-40 pb-16
  /watching grid: grid [grid-template-columns:repeat(auto-fill,minmax(320px,1fr))] gap-[22px]
  /watching section: px-6 md:px-40 py-16
  Breadcrumbs on /uses: items=[{name:'Home',href:'/'},{name:'Uses'}]
  Breadcrumbs on /watching: items=[{name:'Home',href:'/'},{name:'Watching'}]
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create /uses page (src/app/uses/page.tsx)</name>
  <files>src/app/uses/page.tsx</files>
  <read_first>
    - src/app/photos/page.tsx — exact analog page structure (SSG + revalidate + metadata + PageHero + content)
    - src/components/v3/uses-list.tsx — read the actual props interface and verify it accepts UsesGroup[]
    - src/components/v3/page-hero.tsx — confirm title, crumb, sub props
    - src/components/seo/breadcrumbs.tsx — confirm BreadcrumbItem shape
    - src/lib/uses.ts (created in Plan 01) — confirm USES_DATA is exported and has the right shape
    - 16-CONTEXT.md D-05, D-06 — hardcoded data, four groups, Hardware TODO placeholders
    - 16-UI-SPEC.md "/uses Page (NEW)" section — exact layout and spacing
    - 16-PATTERNS.md "src/app/uses/page.tsx" section — template with exact imports
  </read_first>
  <behavior>
    - Page renders PageHero with title="Uses", crumb="Home / Uses", sub="The tools and software I use daily."
    - Breadcrumbs renders with [{name:'Home',href:'/'},{name:'Uses'}]
    - UsesList renders USES_DATA (all four groups appear in DOM)
    - Hardware group items include "TODO:" text
    - Page is a Server Component (no 'use client')
    - revalidate = 1800 and metadata exported
  </behavior>
  <action>
    Create src/app/uses/page.tsx as a Server Component. Do NOT add 'use client'.

    Structure:
    1. Export const revalidate = 1800 (matches /writing and /photos pattern)
    2. Export const metadata: Metadata with title "Uses | Monty Singer", description "The tools and software I use daily.", alternates canonical "/uses", openGraph type "website"
    3. Import USES_DATA from @/lib/uses
    4. Import PageHero from @/components/v3/page-hero
    5. Import UsesList from @/components/v3/uses-list
    6. Import RuleStrong from @/components/editorial/rule-strong
    7. Import Breadcrumbs from @/components/seo/breadcrumbs

    Page component:
    - Render Breadcrumbs with items=[{name:'Home',href:'/'},{name:'Uses'}]
    - Render PageHero with title="Uses", crumb="Home / Uses", sub="The tools and software I use daily."
    - Render RuleStrong
    - Render a section with className="px-6 md:px-40 pb-16" containing UsesList groups={USES_DATA}
    - Render RuleStrong at bottom

    No Notion fetching — this is pure static.
  </action>
  <verify>
    <automated>cd /Users/Montster/MSizzle\ Personal\ Website && npx vitest run src/__tests__/components/uses-list.test.tsx 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/app/uses/page.tsx exists
    - grep "revalidate = 1800" src/app/uses/page.tsx returns 1 match
    - grep "USES_DATA" src/app/uses/page.tsx returns at least 1 match
    - grep "PageHero" src/app/uses/page.tsx returns at least 1 match
    - grep "Breadcrumbs" src/app/uses/page.tsx returns at least 1 match
    - File does NOT contain 'use client'
    - npx tsc --noEmit clean
    - npx vitest run src/__tests__/components/uses-list.test.tsx passes
  </acceptance_criteria>
  <done>/uses page created as Server Component; renders UsesList with USES_DATA; breadcrumbs, PageHero, metadata all present</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create /watching page (src/app/watching/page.tsx)</name>
  <files>src/app/watching/page.tsx</files>
  <read_first>
    - src/app/photos/page.tsx — exact analog page structure
    - src/components/v3/video-card.tsx — READ THIS FILE carefully; confirm it accepts {title, channel, href} and that it renders anchor with target="_blank" rel="noopener noreferrer". If the component does NOT include rel/target, add them to the VideoCard anchor inside this task's action (note: if VideoCard doesn't handle target, the page.tsx must wrap VideoCard in an anchor with target="_blank" rel="noopener noreferrer")
    - src/lib/watching.ts (created in Plan 01) — confirm WATCHING_ITEMS shape
    - 16-CONTEXT.md D-07, D-08, D-09, D-10 — card opens in new tab, thumbnail from YouTube CDN, 6 placeholders
    - 16-UI-SPEC.md "/watching Page (NEW)" section — VideoCard grid layout, gap-[22px], minmax(320px,1fr)
    - 16-PATTERNS.md "src/app/watching/page.tsx" section — full template
    - next.config.ts — verify img.youtube.com is in remotePatterns (done by Plan 01)
  </read_first>
  <behavior>
    - Page renders PageHero with title="Watching", crumb="Home / Watching", sub="Favorite videos and talks."
    - Breadcrumbs renders with [{name:'Home',href:'/'},{name:'Watching'}]
    - VideoCard grid renders 6 cards from WATCHING_ITEMS
    - Each card/anchor has target="_blank" and rel="noopener noreferrer" (D-09)
    - Thumbnail src pattern: https://img.youtube.com/vi/{item.id}/hqdefault.jpg
    - Page is a Server Component (no 'use client')
  </behavior>
  <action>
    Create src/app/watching/page.tsx as a Server Component. Do NOT add 'use client'.

    First: read src/components/v3/video-card.tsx to determine whether VideoCard internally renders its own anchor with target="_blank" rel="noopener noreferrer". If yes, just pass href={item.url}. If no, wrap VideoCard in a plain anchor tag with target="_blank" rel="noopener noreferrer".

    Structure:
    1. Export const revalidate = 1800
    2. Export const metadata: Metadata with title "Watching | Monty Singer", description "Favorite videos and talks.", alternates canonical "/watching", openGraph type "website"
    3. Import WATCHING_ITEMS from @/lib/watching
    4. Import Image from next/image (for thumbnail rendering — if VideoCard doesn't handle Image internally)
    5. Import PageHero from @/components/v3/page-hero
    6. Import VideoCard from @/components/v3/video-card
    7. Import Breadcrumbs from @/components/seo/breadcrumbs

    Page component:
    - Render Breadcrumbs items=[{name:'Home',href:'/'},{name:'Watching'}]
    - Render PageHero title="Watching", crumb="Home / Watching", sub="Favorite videos and talks."
    - Render a section with className="px-6 md:px-40 py-16" containing:
      div with className="grid [grid-template-columns:repeat(auto-fill,minmax(320px,1fr))] gap-[22px]"
      Inside: WATCHING_ITEMS.map(item => VideoCard with title={item.title}, channel={item.channel}, href={item.url})
      Key prop: item.id

    If VideoCard needs a thumbnail passed separately: add a thumbnail prop or render Image inside the map loop before VideoCard, using src={`https://img.youtube.com/vi/${item.id}/hqdefault.jpg`} width={480} height={360} alt={item.title} sizes="(max-width:600px) 100vw, (max-width:1024px) 50vw, 33vw".

    CRITICAL: Ensure every card/anchor has target="_blank" rel="noopener noreferrer" per D-09.
  </action>
  <verify>
    <automated>cd /Users/Montster/MSizzle\ Personal\ Website && npx vitest run src/__tests__/components/video-card.test.tsx 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/app/watching/page.tsx exists
    - grep "revalidate = 1800" src/app/watching/page.tsx returns 1 match
    - grep "WATCHING_ITEMS" src/app/watching/page.tsx returns at least 1 match
    - grep "noopener noreferrer" src/app/watching/page.tsx returns at least 1 match (OR VideoCard component contains it — either location is acceptable)
    - grep "target.*_blank" src/app/watching/page.tsx returns at least 1 match (OR in VideoCard)
    - grep "img.youtube.com" src/app/watching/page.tsx returns at least 1 match (thumbnail URL in page or VideoCard)
    - File does NOT contain 'use client'
    - npx tsc --noEmit clean
    - npx vitest run src/__tests__/components/video-card.test.tsx passes (asserts target=_blank and rel=noopener)
  </acceptance_criteria>
  <done>/watching page created; VideoCard grid renders WATCHING_ITEMS; YouTube CDN thumbnails; all anchors open in new tab with noopener noreferrer</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| /watching anchor links → YouTube (external) | Clicking a VideoCard opens youtube.com in a new tab |
| img.youtube.com CDN → Next.js Image | External image source; now allowlisted in remotePatterns |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-09 | Tampering | VideoCard external links | mitigate | Every VideoCard link to YouTube must have rel="noopener noreferrer" target="_blank" to prevent window.opener reverse tabnapping. Verified in acceptance criteria |
| T-16-10 | Information Disclosure | YouTube CDN img.youtube.com | accept | Thumbnails are public YouTube content; Next.js Image optimization handles caching; no auth credentials involved |
| T-16-11 | Injection | Hardcoded watching/uses data | accept | Data is compiled from src/lib/*.ts — TypeScript-checked at build time; no runtime user input; no XSS vector |
| T-16-SC | Tampering | npm installs | accept | No new npm packages |
</threat_model>

<verification>
After all tasks:
- src/app/uses/page.tsx and src/app/watching/page.tsx both exist
- Both have revalidate = 1800 and metadata
- Neither has 'use client'
- YouTube links have rel="noopener noreferrer" target="_blank"
- npx vitest run src/__tests__/components/uses-list.test.tsx src/__tests__/components/video-card.test.tsx passes
- npx tsc --noEmit clean
</verification>

<success_criteria>
- /uses page renders UsesList with four groups including Hardware TODO placeholders
- /watching page renders VideoCard grid with YouTube thumbnails from img.youtube.com CDN
- All YouTube card links open in new tab with noopener noreferrer (D-09)
- Breadcrumbs present on both pages (D-13)
- Both pages are Server Components with ISR 30min and metadata
</success_criteria>

<output>
Create `.planning/phases/16-interior-pages-on-notion-data/16-05-SUMMARY.md` when done
</output>
