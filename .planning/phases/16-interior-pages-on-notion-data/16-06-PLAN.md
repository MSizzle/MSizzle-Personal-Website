---
phase: 16-interior-pages-on-notion-data
plan: 06
type: execute
wave: 3
depends_on:
  - 16-03
  - 16-04
files_modified:
  - src/app/blog/[slug]/page.tsx
autonomous: true
requirements:
  - PG-01
  - PG-04
  - IN-01
  - IN-02

must_haves:
  truths:
    - "Essay reading view shows breadcrumb (Home / Writing / [Title]), reading time, publish date, prose via NotionRenderer, and RelatedEssays"
    - "Full-bleed essay cover image renders when post.cover exists, with fetchPriority='high' and sizes='100vw'"
    - "Essay hero falls back to type-only (no image) when post.cover is absent"
    - "All v2 tokens (text-ink, text-muted) removed from blog/[slug]/page.tsx"
    - "export const revalidate = 1800 unchanged; generateStaticParams unchanged; NotionRenderer unchanged (IN-01, IN-02)"
    - "calculateReadingTime called with blocks; result displayed as '{N} min read'"
    - "Breadcrumb href for Writing section points to /writing (not /blog)"
  artifacts:
    - path: "src/app/blog/[slug]/page.tsx"
      provides: "Repainted essay reading view with v3 PageHero, full-bleed cover, breadcrumb, reading time"
      contains: "revalidate = 1800"
  key_links:
    - from: "src/app/blog/[slug]/page.tsx"
      to: "src/components/notion/notion-renderer.tsx"
      via: "NotionRenderer import"
      pattern: "NotionRenderer"
    - from: "src/app/blog/[slug]/page.tsx"
      to: "src/app/api/notion-cover/route.ts"
      via: "Image src /api/notion-cover"
      pattern: "notion-cover"
    - from: "src/app/blog/[slug]/page.tsx"
      to: "src/components/blog/related-essays.tsx"
      via: "RelatedEssays import"
      pattern: "RelatedEssays"
---

<objective>
Repaint the essay reading view (/blog/[slug]) onto the Pumpkin Amber v3 system. Key additions per D-14 and PG-04: a PageHero with breadcrumb, reading time, and publish date displayed in a meta row; optional full-bleed cover image when post.cover exists (D-02); Pumpkin Amber token repaint throughout. NotionRenderer, generateStaticParams, ISR, and RelatedEssays are UNCHANGED.

Purpose: The essay view is the most content-rich page; it receives the most layout investment (full-bleed hero, reading meta, related essays). This is the PG-04 primary deliverable.

Output: Updated src/app/blog/[slug]/page.tsx. NotionRenderer, image proxy, ISR, and SEO metadata remain intact (IN-01/IN-02).
</objective>

<execution_context>
@/Users/Montster/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Montster/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/Montster/MSizzle Personal Website/.planning/phases/16-interior-pages-on-notion-data/16-CONTEXT.md
@/Users/Montster/MSizzle Personal Website/.planning/phases/16-interior-pages-on-notion-data/16-PATTERNS.md
@/Users/Montster/MSizzle Personal Website/.planning/phases/16-interior-pages-on-notion-data/16-UI-SPEC.md

<interfaces>
<!-- Read all of these before editing blog/[slug]/page.tsx. -->

From src/app/blog/[slug]/page.tsx (existing, confirmed in codebase):
  import { calculateReadingTime } from '@/utils/reading-time'
  import { Breadcrumbs } from '@/components/seo/breadcrumbs'
  import { RelatedEssays } from '@/components/blog/related-essays'
  import { NewsletterCta } from '@/components/blog/newsletter-cta'
  import { NotionRenderer } from '@/components/notion/notion-renderer'
  export const revalidate = 1800
  export async function generateStaticParams() { ... } — PRESERVE EXACTLY
  export async function generateMetadata() { ... } — PRESERVE EXACTLY

Current v2 tokens to replace in this file:
  text-section-feature text-ink (h1) → text-[clamp(2.8rem,11vw,8rem)] font-bold leading-[0.86] tracking-[-0.03em] text-[var(--color-text)]
  text-muted (multiple) → text-[var(--color-text-muted)]

From src/components/v3/page-hero.tsx:
  export function PageHero({ title, crumb, sub, outline? }: PageHeroProps)
  — Used for the title block + breadcrumb display; check if PageHero accepts a meta slot or if meta row is inline below PageHero

From 16-UI-SPEC.md Essay Reading View section:
  Layout:
    - Full-bleed cover image (when post.cover exists) BEFORE PageHero
    - PageHero: breadcrumb (Home / Writing / [Title]), h1 title
    - Meta row below PageHero: "{N} min read · {month year} · {tag}" — flex gap-[18px] font-mono text-xs text-[var(--color-text-muted)] with .dot separators colored accent
    - RuleStrong below meta
    - Prose section (.prose, max-w-[68ch], mx-auto, px-6) via NotionRenderer
    - RelatedEssays below prose
    - NewsletterCta at bottom

  Hero image when post.cover exists:
    src: /api/notion-cover?pageId={post.id}
    alt: post.title + " cover"
    fill: true OR width=1200 height=800
    priority: true
    fetchPriority: "high"  — CRITICAL: Next.js 16 quirk (memory: nextjs16-fetchpriority-quirk)
    sizes: "100vw"
    className: "object-cover"
    Wrapper: relative w-full h-[400px] md:h-[600px]

  Breadcrumbs component (semantic sr-only + JSON-LD):
    items=[{name:'Home',href:'/'},{name:'Writing',href:'/writing'},{name:post.title}]

  Reading time: readingTime variable from calculateReadingTime(blocks) — display as "{readingTime} min read"
  Publish date: formatMonthYear(post.date) from @/lib/dates

  Prose section:
    article.mx-auto.max-w-[68ch].px-6.pb-16.pt-8.md:px-0
    prose div: uses .prose class (already styled in globals.css lines 92-143 for Pumpkin Amber)
    NotionRenderer stays INSIDE the prose div — do not wrap in another container

  Tag display: post.tags[0] in meta row (if any); graceful if no tags

From src/components/blog/related-essays.tsx:
  Confirmed to exist; accepts { currentSlug: string }
  Renders below article element

From @/utils/reading-time:
  Confirmed: import { calculateReadingTime } from '@/utils/reading-time'
  Accepts blocks: BlockObjectResponse[]; returns number (minutes)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Repaint essay reading view with PageHero, full-bleed cover, reading meta, and v3 tokens</name>
  <files>src/app/blog/[slug]/page.tsx</files>
  <read_first>
    - src/app/blog/[slug]/page.tsx — read the ENTIRE current file to understand every existing import, every className, and the current Breadcrumbs/NotionRenderer placement
    - src/components/v3/page-hero.tsx — confirm exactly what props it accepts and how breadcrumbs are rendered inside it (does it take a crumb string or BreadcrumbItem array?)
    - src/components/blog/related-essays.tsx — confirm currentSlug prop interface
    - src/components/blog/newsletter-cta.tsx — confirm it needs no props
    - 16-PATTERNS.md "src/app/blog/[slug]/page.tsx" section — shows the target repaint structure
    - 16-UI-SPEC.md "Essay Reading View (/blog/[slug])" — exact layout diagram and spacing specs
    - 16-CONTEXT.md D-02 — full-bleed hero on essay pages when post.cover exists
    - 16-CONTEXT.md D-14 — breadcrumb, reading time, publish date, prose, related essays required
    - Memory: nextjs16-fetchpriority-quirk — fetchPriority="high" required on hero image
  </read_first>
  <behavior>
    - When post.cover exists: full-bleed Image renders before PageHero with fetchPriority="high" sizes="100vw"
    - When post.cover is absent: type-only PageHero renders (no image placeholder)
    - PageHero shows title, breadcrumb path "Home / Writing / [Title]"
    - Meta row below header shows reading time + publish date + optional first tag
    - Prose section (NotionRenderer output) is unchanged in data — only container classes updated
    - RelatedEssays and NewsletterCta remain at bottom
    - Breadcrumbs semantic nav uses href="/writing" for Writing section (not /blog)
    - export const revalidate = 1800 unchanged
    - generateStaticParams and generateMetadata UNCHANGED
  </behavior>
  <action>
    Edit src/app/blog/[slug]/page.tsx:

    (1) Add Image import from 'next/image' if not already present.
    (2) Add PageHero import from @/components/v3/page-hero.
    (3) Remove any v2-specific imports no longer needed (check each import; do not remove generateMetadata, calculateReadingTime, Breadcrumbs, etc.).

    (4) In the page component body, restructure the JSX layout:

    BEFORE the article element, add:
    a) Conditional full-bleed hero image block:
       if (post.cover) render:
         div[className="relative w-full h-[400px] md:h-[600px]"]
           Image src={`/api/notion-cover?pageId=${post.id}`} alt={`${post.title} cover`}
             fill priority fetchPriority="high" sizes="100vw" className="object-cover"
       (The post.cover field contains the Notion cover metadata; proxy serves via /api/notion-cover)

    b) The semantic Breadcrumbs component (sr-only JSON-LD) — keep exactly as is but update href for Writing: items=[{name:'Home',href:'/'},{name:'Writing',href:'/writing'},{name:post.title}]

    c) Replace the old inline h1 (text-section-feature text-ink) with PageHero:
       PageHero title={post.title} crumb="Home / Writing" sub="" (or no sub for essay views — check PageHero if sub is required; if so, pass sub={post.description ?? ""}

    d) Meta row below PageHero (inside article header or directly below PageHero):
       div[className="flex flex-wrap items-center gap-[18px] font-mono text-xs text-[var(--color-text-muted)] px-6 md:px-40 mb-6"]
         span {readingTime} min read
         span[className="text-[var(--accent)]"] · (dot separator)
         span {formatMonthYear(post.date)}
         if (post.tags?.[0]):
           span[className="text-[var(--accent)]"] ·
           span {post.tags[0]}

    e) RuleStrong below meta row

    (5) Prose article section:
       article[className="mx-auto max-w-[68ch] px-6 pb-16 pt-8 md:px-0"]
         div[className="prose max-w-none"]
           NotionRenderer blocks={blocks}   ← UNCHANGED; preserve exactly

    (6) Below article: NewsletterCta, then RelatedEssays currentSlug={slug}

    (7) Replace ALL remaining text-ink → text-[var(--color-text)] and text-muted → text-[var(--color-text-muted)] in inline className strings. The .prose styles are already in globals.css (lines 92-143) for Pumpkin Amber — do not add inline prose color overrides.

    PRESERVE UNCHANGED: export const revalidate = 1800; generateStaticParams; generateMetadata; NotionRenderer; RelatedEssays logic; NewsletterCta; import { calculateReadingTime }.
  </action>
  <verify>
    <automated>cd /Users/Montster/MSizzle\ Personal\ Website && npx vitest run src/__tests__/pages/blog-slug.test.tsx 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - grep "text-ink\|text-muted\|text-section-feature" src/app/blog/[slug]/page.tsx returns no matches
    - grep "revalidate = 1800" src/app/blog/[slug]/page.tsx returns 1 match
    - grep "NotionRenderer" src/app/blog/[slug]/page.tsx returns at least 1 match
    - grep "fetchPriority" src/app/blog/[slug]/page.tsx returns at least 1 match (on the cover Image)
    - grep "notion-cover" src/app/blog/[slug]/page.tsx returns at least 1 match (hero image src)
    - grep "RelatedEssays" src/app/blog/[slug]/page.tsx returns at least 1 match
    - grep "href.*'/writing'" src/app/blog/[slug]/page.tsx returns at least 1 match (Breadcrumbs Writing link)
    - Breadcrumbs items use /writing not /blog for the Writing section link
    - npx vitest run src/__tests__/pages/blog-slug.test.tsx passes
    - npx tsc --noEmit clean
  </acceptance_criteria>
  <done>Essay reading view repainted with PageHero, optional full-bleed cover (fetchPriority high), reading meta row, Pumpkin Amber tokens; NotionRenderer, ISR, generateStaticParams, RelatedEssays all unchanged</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Notion API → essay content | getPostBySlug + getBlocks fetch from Notion; content is author-controlled |
| /api/notion-cover → Next.js Image → browser | Image proxy fetches Notion cover; feeds into Next.js Image optimization |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-12 | Information Disclosure | Notion API error | mitigate | Keep existing try/catch defensive pattern in generateStaticParams; notFound() on missing slug prevents 500 |
| T-16-13 | Injection | NotionRenderer XSS | accept | NotionRenderer is an existing vetted component (preserved per IN-02); no changes to it; assume sanitized per RESEARCH.md security domain note |
| T-16-14 | Denial of Service | Full-bleed hero LCP impact | mitigate | fetchPriority="high" + sizes="100vw" on hero Image per nextjs16-fetchpriority-quirk memory; prevents LCP regression on mobile PSI |
| T-16-SC | Tampering | npm installs | accept | No new packages; editing existing page file only |
</threat_model>

<verification>
After the task:
- grep "text-ink\|text-muted\|text-section-feature" src/app/blog/[slug]/page.tsx returns empty
- grep "revalidate = 1800" src/app/blog/[slug]/page.tsx returns 1 match
- grep "fetchPriority" src/app/blog/[slug]/page.tsx returns at least 1 match
- grep "notion-cover" src/app/blog/[slug]/page.tsx returns at least 1 match
- npx vitest run src/__tests__/pages/blog-slug.test.tsx passes
</verification>

<success_criteria>
- Essay view shows breadcrumb (Home / Writing / Title), reading time, publish date, prose, related essays (D-14, PG-04)
- Full-bleed hero image renders when cover exists, type-only when absent (D-02)
- fetchPriority="high" on hero image (Next.js 16 quirk)
- All v2 tokens removed
- ISR (revalidate=1800), generateStaticParams, NotionRenderer, RelatedEssays all unchanged (IN-01, IN-02)
</success_criteria>

<output>
Create `.planning/phases/16-interior-pages-on-notion-data/16-06-SUMMARY.md` when done
</output>
