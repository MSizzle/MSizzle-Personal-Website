---
phase: 16-interior-pages-on-notion-data
plan: 09
type: execute
wave: 4
depends_on:
  - 16-04
  - 16-05
  - 16-06
  - 16-07
  - 16-08
files_modified: []
autonomous: false
requirements:
  - PG-01
  - PG-02
  - PG-03
  - PG-04
  - PG-05
  - IN-01
  - IN-02

must_haves:
  truths:
    - "Full test suite (npx vitest run) passes with no failures"
    - "npm run build passes without TypeScript or compilation errors"
    - "Every interior page on the v3 Vercel preview renders in the Pumpkin Amber palette (amber field, roasted cocoa ink, teal accent)"
    - "No v2 paper/ink visual styling is visible on any interior page"
    - "The footer on all interior pages shows the V3Footer sitemap (not InkFooter)"
    - "Navigation active states work on /writing, /projects, /uses, /watching"
    - "Notion-sourced content (essays, projects, events) displays correctly on the v3 preview"
    - "Notion cover images load through /api/notion-cover proxy on project detail and essay view"
    - "YouTube thumbnails load on /watching page"
    - "ISR revalidate = 1800 confirmed on all dynamic pages via grep"
  artifacts:
    - path: ".planning/phases/16-interior-pages-on-notion-data/16-09-SUMMARY.md"
      provides: "Phase 16 completion sign-off"
  key_links: []
---

<objective>
Phase 16 sign-off: automated full-suite gate followed by a human visual checkpoint across all interior pages on the Vercel v3 preview. After human approval, the phase is marked complete and ready for Phase 17.

Purpose: All Wave 1-3 plans must complete before this plan can run. This is the quality gate that confirms every interior page renders correctly in the Pumpkin Amber system with real Notion data and that the automated test suite is green.

Output: Phase 16 marked complete. SUMMARY created.
</objective>

<execution_context>
@/Users/Montster/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Montster/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/Montster/MSizzle Personal Website/.planning/ROADMAP.md
@/Users/Montster/MSizzle Personal Website/.planning/phases/16-interior-pages-on-notion-data/16-CONTEXT.md
@/Users/Montster/MSizzle Personal Website/.planning/phases/16-interior-pages-on-notion-data/16-VALIDATION.md

<!-- Prior plan SUMMARYs are referenced if needed to confirm what was built -->
<!-- Executor: read the 16-01 through 16-08 SUMMARY files if context on prior decisions is needed -->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated gate — full test suite + ISR + token audit + build check</name>
  <files></files>
  <read_first>
    - 16-VALIDATION.md — full verification checklist and per-task verification map
    - All prior SUMMARY files (16-01 through 16-08) — confirm what was built and any known deviations
  </read_first>
  <action>
    Run the automated gate sequence in order. Stop and fix any failure before proceeding to the human checkpoint.

    (1) Full test suite: npx vitest run
    If any test fails: identify the failing test, fix the root cause, re-run until green.

    (2) ISR audit: confirm export const revalidate = 1800 is present in each dynamic interior page.
    Run each grep individually (not a directory glob) to get exact file-by-file confirmation:

      grep -l "export const revalidate = 1800" \
        src/app/writing/page.tsx \
        src/app/projects/page.tsx \
        "src/app/blog/[slug]/page.tsx" \
        "src/app/projects/[slug]/page.tsx" \
        src/app/events/page.tsx \
        src/app/uses/page.tsx \
        src/app/watching/page.tsx

    Expected: exactly 7 file paths printed (one per line). If any file is missing from the output,
    add export const revalidate = 1800 to that file and re-run the check.

    Note: /uses and /watching are static hardcoded pages (no Notion fetch) but carry revalidate = 1800
    for consistency with the rest of the interior page set, as specified in Plans 05 (D-05, D-07).

    (3) Token audit: confirm no v2 semantic color tokens remain in any interior page or editorial component:
      grep -r "text-ink\|text-muted\|border-rule\|border-ink\|border-rule-strong\|bg-paper" src/app/ src/components/editorial/ --include="*.tsx" --include="*.ts"
    Expected: zero matches in interior pages (src/app/writing, blog, projects, about, events, links, newsletter, prometheus, uses, watching) and all editorial components. If matches found, fix and re-run.

    (4) External link security audit: confirm all external links on interior pages have rel="noopener noreferrer":
      grep -rn "target.*_blank" src/app/links/ src/app/prometheus/ src/app/newsletter/ src/app/watching/ --include="*.tsx"
      For each match, confirm rel="noopener noreferrer" is present on the same element.

    (5) Build gate: npm run build
    If build fails: read the error output, fix the root cause, re-run until clean.

    Report all check results before proceeding to Task 2 (human checkpoint).
  </action>
  <verify>
    <automated>cd /Users/Montster/MSizzle\ Personal\ Website && npx vitest run 2>&1 | tail -20 && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - npx vitest run shows 0 failing tests
    - ISR grep -l returns exactly 7 file paths: src/app/writing/page.tsx, src/app/projects/page.tsx, src/app/blog/[slug]/page.tsx, src/app/projects/[slug]/page.tsx, src/app/events/page.tsx, src/app/uses/page.tsx, src/app/watching/page.tsx
    - Token grep shows 0 matches for v2 color tokens in interior page files
    - External link grep shows all target="_blank" elements also have rel="noopener noreferrer"
    - npm run build exits with code 0 (no compilation errors)
  </acceptance_criteria>
  <done>Automated gate passed: tests green, build clean, ISR verified on all 7 dynamic route files, v2 tokens fully replaced, external links secured</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 16: Interior Pages on Notion Data.

    All interior pages have been rebuilt in the Pumpkin Amber v3 system:
    - /writing — year-grouped essays with v3 PageHero + ListRow; Pumpkin Amber palette
    - /projects — year-grouped projects with v3 PageHero + ListRow
    - /blog/[slug] — essay reading view with optional full-bleed cover, reading time, breadcrumb, related essays
    - /projects/[slug] — project detail with full-bleed cover, breadcrumb, Pumpkin Amber tokens
    - /about, /prometheus, /newsletter, /events, /links — repainted with PageHero + Pumpkin Amber
    - /uses — NEW hardcoded page with UsesList (4 groups, AI/Dev, Productivity, Communication, Hardware)
    - /watching — NEW hardcoded page with YouTube VideoCard grid (6 placeholder cards)

    Shared changes:
    - Navigation: /uses and /watching added to active-label mapping + mobile drawer
    - Footer: V3Footer with full sitemap (Building, Writing, Community, Archive, About) replaces InkFooter
    - Editorial components: ListRow, YearBlock, Rule, RuleStrong, SectionLabel, AllLink all repainted to Pumpkin Amber
  </what-built>
  <how-to-verify>
    Open the v3 Vercel preview URL (from `vercel ls` or Vercel dashboard — the preview on the `v3` branch).

    Walk through each route and verify:

    1. /writing
       - Amber background (#ff7a14), roasted cocoa text (#2a1808)
       - "Writing" in the PageHero sig display font
       - Year-grouped essay rows with titles and excerpts
       - No white backgrounds, no gray text (no v2 paper/ink)

    2. /projects
       - Same Pumpkin Amber palette as /writing
       - "Building" PageHero
       - Year-grouped project rows

    3. /blog/[slug] (click into any essay)
       - If the essay has a Notion cover: full-bleed hero image renders at top
       - Breadcrumb shows "Home / Writing / [Title]"
       - Reading time and date in mono small text
       - Essay prose renders via NotionRenderer
       - "Related Essays" section at bottom

    4. /projects/[slug] (click into any project)
       - If project has a Notion cover: full-bleed hero image at top
       - Breadcrumb shows "Home / Building / [Title]"
       - NotionRenderer content

    5. /about, /prometheus, /newsletter, /events, /links
       - Each has PageHero with correct title
       - Pumpkin Amber palette throughout
       - No white backgrounds

    6. /uses (NEW)
       - PageHero "Uses" renders
       - Four groups visible: AI & Development, Productivity, Communication, Hardware
       - Hardware group shows TODO placeholder text

    7. /watching (NEW)
       - PageHero "Watching" renders
       - Grid of 6 video cards with YouTube thumbnail images
       - Clicking a card opens YouTube in a new tab

    8. Navigation (desktop)
       - Navigate to /uses: nav does NOT highlight any top-level link (correct — /uses is footer-only on desktop per D-11)
       - Navigate to /writing: "Writing" link in top nav is highlighted/active

    9. Mobile hamburger (resize to < 680px)
       - Hamburger opens drawer containing: Building, Writing, Events, About, Links, Uses, Watching, Prometheus

    10. Footer (on any interior page)
        - V3Footer renders with columns: Building, Writing, Community, Archive, About
        - Big signature line reads "Let's be friends."
        - Links to /uses, /watching, /prometheus visible in footer
        - Dark background (roasted cocoa) with cream text

    11. Notion content check
        - Writing index shows real essays from Notion (not empty-state)
        - Essay reading view shows real content from NotionRenderer
        - Events shows upcoming/past events

    Record which items you checked and any issues found.
  </how-to-verify>
  <escalation>
    If human approval cannot be obtained in this run (e.g., Vercel preview is not yet deployed,
    or the session ends before the walk-through is complete):

    - Task 1 (automated gate) still fully gates phase completion — it MUST pass before any sign-off.
    - The visual QA items above are recorded as DEFERRED in the SUMMARY as "Pending visual QA for Phase 18".
    - Phase 18 (v3.0 QA and alias-swap gate) is the designated catch-all for pre-production visual
      verification before the production alias is pointed to v3. These items will be verified there.
    - Write the SUMMARY with status "automated-gate-passed / human-QA-deferred-to-phase-18" and
      list the unverified visual items explicitly so Phase 18 picks them up.
    - Do NOT block Phase 17 work. The codebase is in a shippable state once Task 1 passes.
  </escalation>
  <resume-signal>Type "approved" to mark Phase 16 complete, or describe any issues found so they can be fixed before phase sign-off. If you need to defer visual QA to Phase 18, type "defer-qa" and the SUMMARY will record the deferred items.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vercel preview → browser | Human verification occurs on the preview URL; no production traffic affected |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-21 | Information Disclosure | v3 preview URL | accept | Preview is Vercel branch preview; accessible to anyone with URL but not indexed; production site unaffected |
| T-16-SC | Tampering | npm installs | accept | No packages installed in this plan |
</threat_model>

<verification>
Automated gate (Task 1):
  npx vitest run → 0 failures
  npm run build → exit code 0
  ISR grep -l → exactly 7 files: writing/page.tsx, projects/page.tsx, blog/[slug]/page.tsx, projects/[slug]/page.tsx, events/page.tsx, uses/page.tsx, watching/page.tsx
  Token grep → 0 v2 color token matches in interior pages

Human gate (Task 2):
  All 11 verification items checked on v3 Vercel preview
  "approved" signal received OR deferred to Phase 18 with explicit deferred-items list in SUMMARY
</verification>

<success_criteria>
Phase 16 is complete when:
- Full test suite green
- Build clean
- ISR grep confirms all 7 route files carry revalidate = 1800
- Human approved visual walk-through on v3 Vercel preview OR visual QA formally deferred to Phase 18
- All 7 requirement IDs (PG-01 through PG-05, IN-01, IN-02) verified by the automated + human gates above
</success_criteria>

<output>
Create `.planning/phases/16-interior-pages-on-notion-data/16-09-SUMMARY.md` when done. Update ROADMAP.md Phase 16 status to "complete".
</output>
