---
phase: 21-mono-homepage-rebuild
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/home/hero.tsx
  - src/__tests__/home/hero.test.tsx
  - src/components/home/section-loves.tsx
  - src/__tests__/home/section-loves.test.tsx
  - src/app/globals.css
autonomous: true
requirements: [HP-01, MS-01]

must_haves:
  truths:
    - "A visitor lands on the homepage and sees type only above the fold: no photograph, no portrait carousel, no marker-block headline."
    - "The hero's pulsing status dot and looping link marquee no longer animate anywhere on the page (they are deleted, not paused)."
    - "The Things I Love band header reads as a plain mono label, matching the Building/Writing sections' register, not the old boxed RailBox chip."
  artifacts:
    - path: "src/components/home/hero.tsx"
      provides: "Type-only Swiss hero: mono eyebrow, H1, subtitle, 3-cell meta row"
      min_lines: 40
    - path: "src/__tests__/home/hero.test.tsx"
      provides: "New coverage for HP-01 (no image nodes, exact copy, no motion classes)"
    - path: "src/components/home/section-loves.tsx"
      provides: "Plain mono `03 · Things I Love` label replacing the RailBox chip"
  key_links:
    - from: "src/components/home/hero.tsx"
      to: "src/app/globals.css"
      via: "Tailwind utility classes only (no .hero/.pcarousel/.statustag/.hero-ticker selectors survive)"
      pattern: "grep -c \"pcarousel|pslide|statustag|hero-ticker\" src/components/home/hero.tsx"
---

<objective>
Rebuild `hero.tsx` as the type-only Swiss hero locked by HP-01, deleting the portrait carousel,
marker-block headline, pulsing status dot, and hero link marquee (MS-01). Restyle the Things I
Love band's header from the boxed `RailBox` chip to the plain mono section-label pattern that
`01 · Building` and `02 · Writing` will use, so all three homepage bands share one label register.

Purpose: HP-01 is the phase's first, most visible success criterion — a visitor who lands on `/`
must see no photograph at all above the fold. MS-01 kills the hero's two most attention-grabbing
animations (the marquee and the pulsing dot) outright, not behind a reduced-motion guard.
Output: rebuilt `hero.tsx` + new `hero.test.tsx`; `section-loves.tsx` header restyled + its test
updated; `globals.css` pruned of every CSS rule whose only consumer was the old hero.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-mono-homepage-rebuild/21-UI-SPEC.md
@.planning/phases/21-mono-homepage-rebuild/21-RESEARCH.md
@.planning/sketches/015-mono-passive-home/index.html
@src/components/layout/site-footer.tsx
</context>

<interfaces>
<!-- Hero has no incoming props today and none are added — it stays a static Server Component.
     The only "interface" downstream plans rely on is the exported function name `Hero` (unchanged)
     and the fact that it renders zero `<Image>`/`<img>` nodes, which 21-05's motion-audit test and
     21-05's orchestrator rewrite assume. -->
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rebuild hero.tsx as the type-only Swiss hero (HP-01)</name>
  <files>src/components/home/hero.tsx, src/__tests__/home/hero.test.tsx</files>
  <read_first>
    src/components/home/hero.tsx (current implementation being replaced — see the exact classes
    being deleted: `.eyebrow`, `.sig`/`.hl`/`.hw`, `.subtitle`, `.wayin`, `.statustag`, `.pcarousel`,
    `.hero-ticker`)
    .planning/phases/21-mono-homepage-rebuild/21-UI-SPEC.md — section "1. Hero (HP-01)" for exact
    wrapper/spacing/type/copy values, and the "Copywriting Contract" table for the exact H1/
    subtitle/meta strings
    src/components/layout/site-footer.tsx — `ELSEWHERE` array (lines 23-28), source of the real
    X/LinkedIn/Email URLs the hero's "Elsewhere" meta cell must reuse verbatim
  </read_first>
  <behavior>
    - Renders exactly one `<h1>` with text "I build things and write about it." (no other H1 on
      the page subtree)
    - Renders a `<p>` subtitle containing the exact string "Right now that mostly means
      Prometheus, an AI integrations and education company. Everything else here is the residue:
      past projects, monthly essays, and a running list of things I like."
    - Renders a mono eyebrow containing the text "Monty Singer"
    - Renders a 3-cell meta row: "Currently" → "Building Prometheus" (plain text, no link),
      "Writes" → "Monty Monthly" (plain text, no link), "Elsewhere" → three links with visible
      text "X", "LinkedIn", "Email"
    - The "Elsewhere" links resolve to exactly: X → `https://x.com/themontysinger` (rel contains
      "noopener" and "noreferrer", target="_blank"); LinkedIn → `https://linkedin.com/in/monty-singer`
      (same rel/target); Email → `mailto:monty@prometheus.today` (no target, no rel required)
    - Zero `<img>` or `next/image` nodes anywhere in the hero's rendered subtree
    - Zero elements carrying any of the classes `pcarousel`, `pslide`, `statustag`, `hero-ticker`,
      `tick-link`, `tick-sep`, `sig`, `hl`, `hw`, `wayin`, `subtitle` (the literal old class name —
      the new subtitle uses a Tailwind utility string, not a class called "subtitle")
  </behavior>
  <action>
    Replace the entire contents of `src/components/home/hero.tsx` with a Server Component (no
    `"use client"`) with no import of `./photo`. Structure, top to bottom, per UI-SPEC §1:

    Outer wrapper: `<section className="wrap py-16 md:py-32">` (UI-SPEC's `py-32` desktop /
    `py-16` mobile — Tailwind's responsive prefix handles the breakpoint, no custom media query
    needed since `.wrap` already provides the 1180px max-width + clamp() gutter).

    Inside, four elements in order, each carrying `className="reveal"` in addition to its own
    styling (this is the one surviving MS-02 fade-up motion — every top-level hero element gets
    it individually, matching the sketch's per-element `.fade` application):
    1. Eyebrow: `<div className="reveal font-mono text-xs uppercase tracking-[0.12em] text-text-muted">Monty Singer</div>`
    2. `<h1 className="reveal font-display text-2xl md:text-3xl font-extrabold leading-[0.95] tracking-[-0.03em] max-w-[14ch] mt-6">` with the exact copy string above.
    3. `<p className="reveal font-sans font-light text-base leading-[1.6] text-text-dim max-w-[46ch] mt-6">` with the exact subtitle copy string above (font-weight 300 — "font-light" — per UI-SPEC's explicit discretion call to honor the loaded 300 weight).
    4. Meta row: `<div className="reveal grid grid-cols-1 md:grid-cols-3 gap-8 border-t border-border pt-4 mt-16">` containing three cells, each `<div>` with a mono label line (`text-xs font-mono uppercase tracking-[0.12em] text-text-muted`) above a value line (`text-sm`):
       - Cell 1 label "Currently", value plain text "Building Prometheus"
       - Cell 2 label "Writes", value plain text "Monty Monthly"
       - Cell 3 label "Elsewhere", value: three `<a>` links separated by " · " (space-middot-space) — `<a href="https://x.com/themontysinger" target="_blank" rel="noopener noreferrer">X</a>` · `<a href="https://linkedin.com/in/monty-singer" target="_blank" rel="noopener noreferrer">LinkedIn</a>` · `<a href="mailto:monty@prometheus.today">Email</a>`

    No CTA button, no filled-button styling anywhere in this component (UI-SPEC §1 is explicit:
    "this hero never uses filled-button styling at all").

    Write `src/__tests__/home/hero.test.tsx` (new file) covering every item in `<behavior>` above,
    following the existing test conventions in `src/__tests__/home/section-loves.test.tsx`
    (`@testing-library/react`, `render`/`screen`/`cleanup` from `vitest`).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/home/hero.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/__tests__/home/hero.test.tsx` passes with every `<behavior>` assertion green
    - `grep -c "next/image\|<Image" src/components/home/hero.tsx` returns 0
    - `grep -c "pcarousel\|pslide\|statustag\|hero-ticker" src/components/home/hero.tsx` returns 0
    - `grep -c "prometheus.today\|x.com/themontysinger\|linkedin.com/in/monty-singer\|mailto:monty@prometheus.today" src/components/home/hero.tsx` returns 4 (Prometheus link is NOT in this component per UI-SPEC — verify it does NOT appear; only the three Elsewhere links + zero Prometheus references, so this count should be exactly 3, not 4 — confirm no stray Prometheus link exists in the rebuilt hero)
    - `grep -ri "e5411f\|--color-accent" src/components/home/hero.tsx` returns 0
  </acceptance_criteria>
  <done>hero.tsx renders the type-only Swiss hero with exact UI-SPEC copy and zero photo/motion
  classes; hero.test.tsx exists and is green.</done>
</task>

<task type="auto">
  <name>Task 2: Delete hero-only CSS (MS-01: statustag, hero-ticker, pcarousel/pslide, marker/wayin/subtitle)</name>
  <files>src/app/globals.css</files>
  <read_first>
    src/app/globals.css lines 203-262 (`.band.band-dark` comment block, `.band`/`.band.hero-band`,
    `.eyebrow`, `.marker`/`h1.sig .hw`) — DO NOT touch `.band.band-dark`/`.band-dark` (still consumed
    by the orchestrator's Building/Writing sections until Plan 21-05) or `.eyebrow` (still consumed
    by `section-newsletter.tsx` until Plan 21-05)
    src/app/globals.css lines 266-374 (`.hero` grid, `.col-copy`, `.col-photo`, `.pcarousel`,
    `.pslide*`, `@keyframes pfade`, `.subtitle`, `.wayin`, `.wayin a`)
    src/app/globals.css lines 446-489 (`.statustag`, `.statustag .dot`, `@keyframes pulse`,
    `@keyframes slide` — DO NOT delete `@keyframes slide`, `.marquee .track` still uses it until
    Plan 21-04; DO NOT delete `@keyframes kb`/`@keyframes breathe`, `photo.tsx` still applies
    `.kenburns`/`.breathe` until Plan 21-04)
    src/app/globals.css lines 491-529 (`.hero-ticker`, `.hero-ticker .track`, `.tick-link`, `.tick-sep`)
    .planning/phases/21-mono-homepage-rebuild/21-RESEARCH.md — "Motion Teardown Mechanics" table
    for the authoritative current-session line numbers (this task's line numbers were re-verified
    against the live file during this planning session and should match; re-grep if they have drifted)
  </read_first>
  <action>
    Delete the following CSS rules from `src/app/globals.css` in full, verifying each selector's
    current line range by grep immediately before deleting (line numbers shift as earlier deletions
    in this same task land):
    - `.hero` (grid layout), `.hero .eyebrow`, `.hero .col-copy`, `.hero .col-photo`,
      `.hero .col-photo .pcarousel`, `.hero .col-photo .pcarousel .photo`, `.pslide` and all its
      `:first-child`/`:nth-child(2)`/`:nth-child(3)` variants, `.hero .col-photo .pslide .img`,
      `@keyframes pfade` — the full block currently spanning roughly lines 266-355
    - `.subtitle`, `.wayin`, `.wayin a` — roughly lines 356-374
    - `.marker, h1.sig .hw` (the highlight-block rule; the `.sig`/`h1.sig`/`.sig-out`/`.hl` base
      classes themselves are shared by `src/components/v3/page-hero.tsx` and
      `src/components/v3/big-list.tsx` and MUST survive — delete only the `h1.sig .hw` combined
      selector and the bare `.marker` class, since `grep -rn "\"marker\"\|\"hw\"" src --include=*.tsx`
      confirms zero remaining consumers of either after this task)
    - `.statustag`, `.statustag .dot` — roughly lines 446-466
    - `@keyframes pulse` — roughly lines 473-476
    - `.hero-ticker`, `.hero-ticker .track`, `.hero-ticker:hover .track`, `.hero-ticker .tick-link`
      (both declaration blocks), `.hero-ticker .tick-link:hover`, `.hero-ticker .tick-sep` — the
      full block roughly lines 491-529
    - `.band.hero-band { background: var(--hero-bg); }` and its preceding comment line — roughly
      lines 219-222 (leave the `--hero-bg: #ffffff;` token declaration at line 121 untouched; it is
      harmless dead weight and not worth a second edit for zero visual effect)

    Do NOT touch: `@keyframes slide` (still used by `.marquee .track`, dies in Plan 21-04),
    `@keyframes kb`/`@keyframes breathe` (still used by `.kenburns`/`.breathe`, die in Plan 21-04),
    `.eyebrow`/`.eyebrow::before` (still used by `section-newsletter.tsx`, dies in Plan 21-05),
    `.band.band-dark, .band-dark { ... }` (still used by the orchestrator, dies in Plan 21-05).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/home/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^\.hero {" src/app/globals.css` returns 0
    - `grep -c "^\.pcarousel\|\.pcarousel {" src/app/globals.css` returns 0
    - `grep -c "^\.pslide" src/app/globals.css` returns 0
    - `grep -c "^\.statustag" src/app/globals.css` returns 0
    - `grep -c "^\.hero-ticker" src/app/globals.css` returns 0
    - `grep -c "@keyframes pulse\|@keyframes pfade" src/app/globals.css` returns 0
    - `grep -c "@keyframes slide\|@keyframes kb\|@keyframes breathe" src/app/globals.css` returns 3 (unchanged — these survive this task)
    - `grep -c "^\.band-dark\|^\.band\.band-dark" src/app/globals.css` returns 1 (unchanged — survives this task)
    - `npx vitest run src/__tests__/home/` shows zero new failures beyond the pre-existing baseline
  </acceptance_criteria>
  <done>Every hero-exclusive CSS rule is deleted; every rule still shared with other not-yet-rebuilt
  components is intact; homepage test suite unchanged from baseline.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Restyle Things I Love band header to the plain mono label pattern</name>
  <files>src/components/home/section-loves.tsx, src/__tests__/home/section-loves.test.tsx</files>
  <read_first>
    src/components/home/section-loves.tsx (current implementation — replacing the `RailBox`
    header only; the `Pinboard`/`PhotoMarquee` branching logic below it is UNCHANGED, and
    `pinboard.tsx` itself is never touched — hard fence)
    src/__tests__/home/section-loves.test.tsx (current test — its `RailBox` mock and the
    Pinboard/PhotoMarquee branching assertions)
    .planning/phases/21-mono-homepage-rebuild/21-UI-SPEC.md — Component Disposition table row for
    `section-loves.tsx` ("Replace the RailBox header with the plain mono .lbl section-label
    pattern... to match the Building/Writing sections")
  </read_first>
  <behavior>
    - No longer imports or renders `RailBox`
    - Renders a mono label element containing the exact text "03 · Things I Love"
    - Still renders `<Pinboard items={items} categoryOrder={categoryOrder} />` when `items.length > 0`
    - Still renders `<PhotoMarquee>` with the same fallback items when `items` is empty
  </behavior>
  <action>
    In `src/components/home/section-loves.tsx`: delete the `import { RailBox } from
    "@/components/home/rail-box";` line (do NOT delete `rail-box.tsx` itself yet — it is still
    imported by `section-building.tsx` until Plan 21-02 and `section-work.tsx` until Plan 21-05;
    deleting the file now would break the build). Replace the `<div className="reveal"><RailBox
    num="03" label="Things I love" /></div>` block with a single element:
    `<h2 className="reveal font-mono text-xs uppercase tracking-[0.12em] text-text-muted">03 · Things I Love</h2>`
    (real heading semantics per UI-SPEC's Accessibility Contract point 5, styled as a small mono
    tag). Keep the existing `<h2 className="reveal">Things I love outside of work.</h2>` line as a
    second, separate element directly below it — but since two `<h2>` siblings is invalid document
    outline, demote this second line to a `<p className="reveal text-text-dim">Things I love
    outside of work.</p>` (same copy, same position, now a supporting paragraph under the real
    `<h2>` label). Remove the now-single-column `beat-grid` wrapper `<div className="beat-grid">`
    (its two-column `[180px, 1fr]` grid made sense with a rail box sitting beside the copy; with a
    plain mono label there is nothing to put in the left column) — keep the outer `<div
    className="wrap">` and both elements simply stack vertically inside it.

    Update `src/__tests__/home/section-loves.test.tsx`: delete the `vi.mock("@/components/home/rail-box", ...)` block entirely (no longer imported by the component under test). Replace the
    `expect(screen.getByTestId("rail").getAttribute("data-num")).toBe("03")` assertion in the first
    `it()` with `expect(screen.getByText("03 · Things I Love")).toBeDefined()`. Leave the
    `PhotoMarquee`/`Pinboard` mocks and the two branching `it()` blocks unchanged.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/home/section-loves.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/__tests__/home/section-loves.test.tsx` passes, all 3 `it()` blocks green
    - `grep -c "RailBox" src/components/home/section-loves.tsx` returns 0
    - `grep -c "03 · Things I Love" src/components/home/section-loves.tsx` returns 1
    - `grep -c "beat-grid" src/components/home/section-loves.tsx` returns 0
    - `grep -c "Pinboard\b" src/components/home/section-loves.tsx` returns 2 (import + JSX usage, unchanged)
  </acceptance_criteria>
  <done>section-loves.tsx no longer imports RailBox and shows the plain mono "03 · Things I Love"
  label; Pinboard/PhotoMarquee branching is untouched; test suite updated and green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Hero meta row → external social links | The hero now renders three outbound links (X, LinkedIn) in a new tab, plus a `mailto:` link. No user input crosses this boundary; hrefs are hardcoded literals in source, not derived from any request or Notion field. |

This is a static marketing-homepage restyle: no auth, no forms, no user-controlled strings reach
the DOM in this plan (the hero's copy and links are all hardcoded), and no new npm dependency is
installed. The threat surface is genuinely small.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01 | Tampering (reverse tabnabbing) | `hero.tsx` Elsewhere links (X, LinkedIn) | mitigate | Both `target="_blank"` anchors carry `rel="noopener noreferrer"` — verified in Task 1's acceptance criteria via grep on the literal href+rel pairing. |
| T-21-02 | Information Disclosure | `hero.tsx` copy strings | accept | All copy is a hardcoded literal (no Notion/user data), so there is nothing to leak beyond what is already public marketing copy. |
| T-21-SC | Tampering (supply chain) | npm installs | accept | This plan installs no new packages (confirmed in 21-RESEARCH.md's Package Legitimacy Audit: not applicable). |
</threat_model>

<verification>
Run `npx vitest run src/__tests__/home/` after each task. Full-suite gate before moving to Plan
21-02: `npx vitest run` must report `182 passed, 16 todo, 1 failed` (the one pre-existing
`projects.test.tsx:188` failure) plus the new `hero.test.tsx` assertions passing — i.e. total
passing count increases by the number of new `hero.test.tsx` tests, with no other regression.
</verification>

<success_criteria>
- Hero renders type-only per UI-SPEC §1 with zero photo/motion classes remaining in its markup
- `hero.test.tsx` exists and is green
- Things I Love band header uses the plain mono `03 · Things I Love` label, matching the register
  Building/Writing will use in Plans 21-02/21-03
- `section-loves.test.tsx` still fully covers the Pinboard/PhotoMarquee branching, unmodified in
  behavior
- Every hero-exclusive CSS rule (`.hero`, `.pcarousel`, `.pslide*`, `.statustag*`, `.hero-ticker*`,
  `.subtitle`, `.wayin*`, `.marker`/`h1.sig .hw`, `.band.hero-band`) is deleted from `globals.css`
- `npx vitest run` stays at baseline (182 passed / 16 todo / 1 pre-existing failure) plus new tests
</success_criteria>

<output>
Create `.planning/phases/21-mono-homepage-rebuild/21-01-SUMMARY.md` when done
</output>
