---
phase: 21-mono-homepage-rebuild
plan: 02
type: execute
wave: 2
depends_on: ["21-01"]
files_modified:
  - src/components/home/section-building.tsx
  - src/__tests__/home/section-building.test.tsx
  - src/app/globals.css
autonomous: true
requirements: [HP-02, MS-02]

must_haves:
  truths:
    - "A visitor sees the Building band as a Swiss numbered index (001, 002, 003...) rather than a single Prometheus narrative block."
    - "Hovering or focusing any Building row inverts the ENTIRE row to a solid black block with white type, and a keyboard user tabbing through sees the identical inversion via :focus-visible."
    - "Row 001 is always Prometheus; every row after it comes from the real, already-fetched Notion Featured Projects list, not invented placeholder projects."
  artifacts:
    - path: "src/components/home/section-building.tsx"
      provides: "Swiss numbered index row renderer, absorbs section-work.tsx's Notion data role"
      exports: ["SectionBuilding"]
    - path: "src/app/globals.css"
      provides: ".a-row family: grid layout, hover/focus full-row invert, 760px responsive collapse"
  key_links:
    - from: "src/components/home/section-building.tsx"
      to: "src/lib/notion-projects.ts"
      via: "projects: Project[] prop (already fetched in page.tsx, forwarded by the orchestrator in Plan 21-05)"
      pattern: "projects\\.map"
    - from: "src/components/home/section-building.tsx"
      to: "src/app/globals.css"
      via: ".a-row class + ::before hover-invert pseudo-element"
      pattern: "\\.a-row"
---

<objective>
Rebuild `section-building.tsx` as the Swiss numbered index locked by HP-02: zero-padded 3-digit
rows on hairline rules, full-row hover/focus inversion as the site's only hover language, absorbing
`section-work.tsx`'s Notion Featured Projects data role so that component can be deleted in Plan
21-05. Add the `.a-row` CSS family to `globals.css` (new — the sketch's variant-E reference
implementation, adapted to this project's shipped token names).

Purpose: HP-02 is the second of the three registers this phase locks in (Swiss editorial for
Building). This is also where the row-hover-invert mechanism is built for the first time — Plan
21-03 reuses the identical mechanism for Writing terminal rows.
Output: rebuilt `section-building.tsx` accepting a `projects` prop; rewritten
`section-building.test.tsx`; new `.a-row`/`.a-sec` CSS in `globals.css`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-mono-homepage-rebuild/21-UI-SPEC.md
@.planning/phases/21-mono-homepage-rebuild/21-RESEARCH.md
@.planning/sketches/015-mono-passive-home/index.html
@src/lib/notion-projects.ts
</context>

<interfaces>
From `src/lib/notion-projects.ts` (unmodified, already exists):
```
export interface Project {
  id: string; slug: string; title: string; description: string;
  cover: string | null; image: string | null; emoji: string | null;
  externalUrl: string; tags: string[]; featured: boolean; published: boolean;
  lastEdited: string;
}
```
`SectionBuilding`'s new signature: `SectionBuilding({ projects = [] }: { projects?: Project[] })`.
This is a breaking change to the component's prop contract — Plan 21-05 is the plan that updates
`explorative-homepage.tsx` to actually pass `projects` to `SectionBuilding` instead of the
soon-to-be-deleted `SectionWork`. Until Plan 21-05 lands, the orchestrator still imports the old
`SectionBuilding` with no props (harmless — `projects` defaults to `[]`, row 001 alone still renders).

Sketch reference CSS (`.a-row` family, from `.planning/sketches/015-mono-passive-home/index.html`
lines 95-115), adapted to this repo's shipped `--color-*` tokens and 0.12em tracking convention
(not the sketch's own unadopted `--tracking-label: 0.14em`):
- `.a-sec { padding: var(--space-32, 128px) 0 0; }` — section wrapper, top padding only
- `.a-row { display: grid; grid-template-columns: 64px 1fr 200px 90px; gap: 24px; align-items: baseline; padding: 32px 0; border-top: 1px solid var(--color-border); text-decoration: none; position: relative; }`
- `.a-row:last-of-type { border-bottom: 1px solid var(--color-border); }`
- `.a-row .num { font-family: var(--font-mono); font-size: var(--text-xs); color: var(--color-text-muted); letter-spacing: 0.12em; }`
- `.a-row .ttl { font-size: var(--text-2xl); font-weight: 600; letter-spacing: -0.02em; }`
- `.a-row .dsc { font-size: var(--text-sm); color: var(--color-text-dim); line-height: 1.5; }`
- `.a-row .status { font-family: var(--font-mono); font-size: var(--text-xs); color: var(--color-text-muted); text-align: right; }`
- `.a-row::before { content: ""; position: absolute; inset: 0 -20px; background: var(--color-invert); z-index: -1; opacity: 0; transition: opacity 150ms ease; }`
- `.a-row:hover::before, .a-row:focus-visible::before { opacity: 1; }`
- `.a-row:hover, .a-row:hover .num, .a-row:hover .dsc, .a-row:hover .status, .a-row:focus-visible, .a-row:focus-visible .num, .a-row:focus-visible .dsc, .a-row:focus-visible .status { color: var(--color-text-inverse); }`
- `.a-row:focus-visible { outline-color: currentColor; outline-offset: 2px; }` — scoped override of
  Phase 20 D-10's global `outline: 2px solid var(--color-invert)` rule, which would otherwise render
  invisible black-on-black once the row inverts. Scope this ONLY to `.a-row` (Plan 21-03 needs the
  identical override scoped to `.e-post`; do not make this a global change).
- 760px responsive: `.a-row { grid-template-columns: 40px 1fr; } .a-row .dsc, .a-row .status { grid-column: 2; text-align: left; }` inside the existing `@media (max-width: 760px) { ... }` breakpoint pattern already used elsewhere in `globals.css` (see lines ~1379, ~1483).
- Touch guard: gate the `:hover` rule (not `:focus-visible`) behind `@media (hover: hover)` so
  coarse-pointer devices are not stuck mid-invert after a tap.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the .a-row Swiss index CSS family to globals.css</name>
  <files>src/app/globals.css</files>
  <read_first>
    .planning/phases/21-mono-homepage-rebuild/21-UI-SPEC.md — section "2. Building index (HP-02)"
    for the full row-anatomy and hover-invert spec (this plan's `<interfaces>` block above is a
    condensed, ready-to-paste version of the same spec)
    src/app/globals.css lines 1379-1420 and 1483-1500 — existing `@media (max-width: 760px)`
    breakpoint pattern to match convention
  </read_first>
  <action>
    Add a new CSS section to `globals.css` (a good location: immediately before the "── MOTION
    SYSTEM" comment block, so the new index/terminal rules sit together as "Phase 21: homepage
    rebuild" — the exact byte position is not load-bearing, any location outside another rule's
    block is fine). Add exactly the `.a-sec` and `.a-row` family listed in this plan's
    `<interfaces>` block verbatim, plus the `@media (hover: hover)` touch guard wrapping only the
    `:hover` (not `:focus-visible`) rule, plus the `@media (max-width: 760px)` responsive override
    inside the existing breakpoint block (append to the existing `@media (max-width: 760px)` rule
    rather than opening a second one, if one already exists at that breakpoint — grep first).
  </action>
  <verify>
    <automated>grep -c "\.a-row" src/app/globals.css</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "\.a-row {" src/app/globals.css` returns 1
    - `grep -c "\.a-row::before" src/app/globals.css` returns 1
    - `grep -c "\.a-row:focus-visible" src/app/globals.css` returns at least 2 (the invert-trigger rule and the outline-color override)
    - `grep -c "outline-color: currentColor" src/app/globals.css` returns at least 1
    - `grep -c "@media (hover: hover)" src/app/globals.css` returns 1
  </acceptance_criteria>
  <done>The `.a-row` CSS family exists in globals.css exactly as specified, including the
  touch-hover guard and the scoped focus-ring override.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rebuild section-building.tsx as the Swiss numbered index (HP-02)</name>
  <files>src/components/home/section-building.tsx, src/__tests__/home/section-building.test.tsx</files>
  <read_first>
    src/components/home/section-building.tsx (current implementation, being fully replaced —
    note it currently imports `RailBox` and `Photo`, both dropped)
    src/lib/notion-projects.ts — `Project` interface (already read into this plan's `<interfaces>`
    block above)
    src/components/home/section-work.tsx — the OLD `kickerFor()` helper for reference on how
    project tags/year were previously combined; this plan's status-column logic is deliberately
    simpler (first tag OR year, not both) per UI-SPEC §2
    .planning/phases/21-mono-homepage-rebuild/21-UI-SPEC.md — section "2. Building index (HP-02)",
    "Row content" bullet list, for the exact row 001 hardcoded copy and the Notion-row mapping rule
  </read_first>
  <behavior>
    - With no `projects` prop, renders exactly one row: numeral "001", title "Prometheus",
      description "AI integrations and education. Practical leverage, not hype.", status
      "Current", href "https://prometheus.today" with target="_blank" rel="noopener noreferrer"
    - With a `projects` array of length N, renders N+1 rows total: row 001 (the hardcoded
      Prometheus row, always first), then rows 002..00(N+1) mapped one-to-one from `projects` in
      array order
    - Every numeral is exactly 3 digits, zero-padded (`/^\d{3}$/`), never "1." or "01"
    - Notion-derived rows use `project.title`/`project.description` verbatim, `href="/building/${project.slug}"`, and a status column that is `project.tags[0]` when present, else the 4-digit UTC year of `project.lastEdited`
    - Does NOT render "This site" as a row (no real href exists for it — CONTEXT.md's own standard
      is to omit unknowns rather than guess, and neither CONTEXT.md nor UI-SPEC supplies a
      destination for that row)
    - Does NOT import or render `RailBox`, `Photo`, or any element with class `shadowed`, `slide`,
      `beat-grid`, `prometheus-shot`
    - Every row is wrapped in `.a-row` and is reachable via `:focus-visible` (has `tabIndex`
      implicit via being an `<a href>`)
  </behavior>
  <action>
    Replace the entire contents of `src/components/home/section-building.tsx`. Server Component,
    no `"use client"`. Signature: `export function SectionBuilding({ projects = [] }: { projects?: Project[] })`
    importing `type { Project } from "@/lib/notion-projects"`.

    Structure:
    ```
    <section className="wrap a-sec" id="building">
      <h2 className="reveal font-mono text-xs uppercase tracking-[0.12em] text-text-muted">01 · Building</h2>
      {rows.map((row, i) => <a className="a-row reveal" href={row.href} {...(row.external ? {target:"_blank", rel:"noopener noreferrer"} : {})}>
        <span className="num">{String(i + 1).padStart(3, "0")}</span>
        <span className="ttl">{row.title}</span>
        <span className="dsc">{row.description}</span>
        <span className="status">{row.status}</span>
      </a>)}
    </section>
    ```
    (Illustrative shape only — write real JSX, not this literal string.) Build the `rows` array as:
    row 0 = `{ title: "Prometheus", description: "AI integrations and education. Practical
    leverage, not hype.", status: "Current", href: "https://prometheus.today", external: true }`,
    followed by one entry per `projects[i]` = `{ title: project.title, description:
    project.description, status: project.tags?.[0] || String(new
    Date(project.lastEdited).getUTCFullYear()), href: \`/building/${project.slug}\`, external:
    false }`.

    Do NOT wrap the `<section>` with an outer `.beat`/`.beat-grid` — the id/`.wrap`/`.a-sec`
    combination is the entire structural container this phase needs (the `id="building"` on this
    section itself is new; verify in Plan 21-05 that the orchestrator's own wrapping `<section
    id="building">` is either removed in favor of this one, or that only one `id="building"`
    element exists in the final DOM — do not create a duplicate id).

    Rewrite `src/__tests__/home/section-building.test.tsx` completely (its current `RailBox` mock
    and `.shadowed.slide.from-left` assertions are fully invalidated). Cover every item in
    `<behavior>` above, including a case with a 2-element `projects` array asserting 3 total rows
    (001 Prometheus + 002/003 from the array) with correct numerals, titles, hrefs, and status
    columns (one project with a tag, one without a tag to exercise the year fallback).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/home/section-building.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/__tests__/home/section-building.test.tsx` passes, covering every `<behavior>` item
    - `grep -c "RailBox\|Photo\b" src/components/home/section-building.tsx` returns 0
    - `grep -c "This site" src/components/home/section-building.tsx` returns 0
    - `grep -c "padStart(3" src/components/home/section-building.tsx` returns 1
    - `grep -c "prometheus.today" src/components/home/section-building.tsx` returns 1
    - `grep -ri "e5411f\|--color-accent" src/components/home/section-building.tsx` returns 0
  </acceptance_criteria>
  <done>section-building.tsx renders the Swiss numbered index with real Prometheus + Notion data,
  full-row hover/focus invert via the new `.a-row` CSS, and its rewritten test suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Notion Featured Projects → Building row title/description | `project.title`/`project.description` are Notion-authored strings rendered as plain React text children (auto-escaped), never `dangerouslySetInnerHTML`. |
| Building rows → `/building/{slug}` and external Prometheus link | Internal slugs are Notion-controlled but only reach an internal route path, never an executed script; the one external link carries `rel="noopener noreferrer"`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-03 | Tampering (reverse tabnabbing) | `section-building.tsx` Prometheus row (`target="_blank"`) | mitigate | `rel="noopener noreferrer"` is set on the one external row — verified by grep in Task 2's acceptance criteria. |
| T-21-04 | Information Disclosure / Injection | `section-building.tsx` rendering `project.title`/`project.description` | accept | React's default JSX text-child escaping applies; no `dangerouslySetInnerHTML` is introduced by this plan. Notion content is already editorially controlled by Monty (single-author CMS, ASVS L1 does not require additional sanitization here). |
| T-21-SC | Tampering (supply chain) | npm installs | accept | No new packages installed this plan. |
</threat_model>

<verification>
`npx vitest run src/__tests__/home/section-building.test.tsx` after Task 2; full homepage sweep
`npx vitest run src/__tests__/home/` after both tasks; full-suite gate
`npx vitest run` before moving to Plan 21-03, expecting baseline-equivalent (only
`projects.test.tsx:188` red) plus the new/updated Building assertions green.
</verification>

<success_criteria>
- `.a-row` CSS family exists with full-row hover/focus invert, 150ms transition, scoped
  `outline-color: currentColor` override, and a 760px responsive collapse to `40px 1fr`
- `section-building.tsx` renders zero-padded 3-digit numerals, real Prometheus + Notion row data,
  and no "This site" placeholder row
- `section-building.test.tsx` is fully rewritten and green
- No regression in `npx vitest run` beyond the known pre-existing failure
</success_criteria>

<output>
Create `.planning/phases/21-mono-homepage-rebuild/21-02-SUMMARY.md` when done
</output>
