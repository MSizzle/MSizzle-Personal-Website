---
phase: 21-mono-homepage-rebuild
plan: 06
type: execute
wave: 6
depends_on: ["21-05"]
files_modified:
  - src/components/home/scroll-reveals.tsx
  - src/app/globals.css
autonomous: false
requirements: [MS-02, HP-04]

must_haves:
  truths:
    - "The one surviving scroll motion (opacity fade-up) uses the sketch's exact retuned timing (700ms, translateY(14px), threshold 0.05, rootMargin -8%), not the pre-Phase-21 values."
    - "The IntersectionObserver driving the fade no longer observes any element by a class this phase deleted (.slide, .shadowed)."
    - "A human has visually confirmed the fade reads as deliberate (not a slow page load), the Swiss/terminal rows collapse gracefully at 375px, and the mono system reads as intentional."
  artifacts:
    - path: "src/components/home/scroll-reveals.tsx"
      provides: "IntersectionObserver narrowed to .reveal only, threshold 0.05, rootMargin -8%"
    - path: "src/app/globals.css"
      provides: "Retuned .reveal timing; reduced-motion guard blocks narrowed to .reveal only; dead .stickynav .cta rule removed"
  key_links:
    - from: "src/components/home/scroll-reveals.tsx"
      to: "src/app/globals.css"
      via: ".reveal class + its CSS transition, both retuned to the sketch's authoritative values together"
      pattern: "translateY\\(14px\\)"
---

<objective>
Close out Phase 21's motion system: retune `.reveal`'s CSS transition to the sketch's exact
values (this is a deliberate correction over the pre-existing shipped values, not a preservation),
narrow `scroll-reveals.tsx`'s `IntersectionObserver` selector and config to match, narrow both
reduced-motion guard blocks in `globals.css` down to `.reveal` alone (every other class they used
to list is now deleted from the codebase), delete the now-fully-dead `.stickynav .cta` rule, run
the no-accent grep gate, and get Monty's sign-off on the three behaviors that cannot be
mechanically verified.

Purpose: this is the last plan in the phase and the only one with a human checkpoint. Every prior
plan's automated tests can prove structure and absence of classes; none of them can prove the fade
motion "feels" right, that the Swiss/terminal rows genuinely collapse at 375px rather than merely
fitting, or that the finished page reads as an intentional mono system rather than an unstyled one
— all three are explicitly called out as manual-only in `21-VALIDATION.md`.
Output: retuned `.reveal` motion; narrowed reduced-motion guards; deleted `.stickynav .cta`; a
recorded human GO/NO-GO on the phase's three manual-only checks.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-mono-homepage-rebuild/21-UI-SPEC.md
@.planning/phases/21-mono-homepage-rebuild/21-RESEARCH.md
@.planning/phases/21-mono-homepage-rebuild/21-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Retune .reveal motion, narrow scroll-reveals.tsx + reduced-motion guards, delete dead CTA CSS</name>
  <files>src/components/home/scroll-reveals.tsx, src/app/globals.css</files>
  <read_first>
    src/components/home/scroll-reveals.tsx (current — the `.reveal, .slide, .shadowed` selector
    and `{ threshold: 0, rootMargin: "0px 0px -38% 0px" }` IO config being narrowed/retuned)
    src/app/globals.css lines 1107-1188 (`.reveal`/`.reveal.in` current values, both
    reduced-motion guard blocks — `body.no-motion` and `@media (prefers-reduced-motion: reduce)`
    — both currently list dead classes deleted in Plans 21-01/21-04/21-05: `.slide`, `.kenburns`,
    `.breathe`, `.blob`, `.pslide`, `.marquee .track`, `.hero-ticker .track`; `.slide` itself was
    already deleted from these blocks in Plan 21-05's Task 2 — verify current state by grep before
    editing rather than assuming Plan 21-05's exact wording landed byte-for-byte)
    src/app/globals.css lines 1005-1015 (`.stickynav .cta` — grep-confirmed zero consumers in
    `sticky-nav.tsx` today; dead weight, unrelated to any component deleted this phase, cleaned up
    here as a final sweep item)
    .planning/phases/21-mono-homepage-rebuild/21-RESEARCH.md — "Motion Teardown Mechanics" >
    "The one survivor — retune spec" table for the exact current-vs-target values
  </read_first>
  <action>
    In `src/app/globals.css`, change `.reveal`'s transition properties: `transform:
    translateY(30px)` → `translateY(14px)`; `transition: opacity 0.8s var(--ease-out), transform
    0.8s var(--ease-out)` → `transition: opacity 0.7s cubic-bezier(0.22, 0.61, 0.36, 1), transform
    0.7s cubic-bezier(0.22, 0.61, 0.36, 1)`. Leave `.reveal.in { opacity: 1; transform: none; }`
    unchanged.

    In both reduced-motion guard blocks (`body.no-motion` and `@media (prefers-reduced-motion:
    reduce)`), grep-verify their current selector lists and narrow each animation-kill line down
    to whatever subset of `.kenburns`/`.breathe`/`.blob`/`.pslide`/`.marquee .track`/`.hero-ticker
    .track`/`.slide` still has a live consumer anywhere in `src/` (expected result: none of them
    do, per 21-RESEARCH.md's own investigation — `grep -rln "\bblob\b" src --include="*.tsx"`
    returns nothing). If zero classes remain to list, delete the animation-kill line entirely from
    both blocks, keeping only the `.reveal { opacity: 1 !important; transform: none !important; }`
    reset lines (do not delete the `.reveal` lines — reduced-motion must still disable the one
    surviving fade).

    Delete `.stickynav .cta { ... }` (its selector, `background: var(--color-invert); color: #fff;
    padding...`) — grep-confirmed zero consumers in `sticky-nav.tsx`.

    In `src/components/home/scroll-reveals.tsx`: change `const selector = ".reveal, .slide,
    .shadowed";` to `const selector = ".reveal";`. Change the `IntersectionObserver` config object
    from `{ threshold: 0, rootMargin: "0px 0px -38% 0px" }` to `{ threshold: 0.05, rootMargin:
    "0px 0px -8% 0px" }`.
  </action>
  <verify>
    <automated>npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "translateY(14px)" src/app/globals.css` returns 1
    - `grep -c "0.7s cubic-bezier(0.22, 0.61, 0.36, 1)" src/app/globals.css` returns at least 1
    - `grep -c "translateY(30px)\|0.8s var(--ease-out)" src/app/globals.css` returns 0
    - `grep -c "\.slide, \.shadowed\|\.reveal, \.slide" src/components/home/scroll-reveals.tsx` returns 0
    - `grep -c "threshold: 0.05" src/components/home/scroll-reveals.tsx` returns 1
    - `grep -c "rootMargin: \"0px 0px -8% 0px\"" src/components/home/scroll-reveals.tsx` returns 1
    - `grep -c "\.stickynav \.cta" src/app/globals.css` returns 0
    - `grep -ri "e5411f\|--color-accent" src/components/home/ src/app/page.tsx` returns 0 (site-wide no-accent gate, MO-02 carryforward)
    - `npx vitest run` reports `182 passed, 16 todo, 1 failed` (only `projects.test.tsx:188`) plus every Phase 21 test added in Plans 21-01 through 21-05, all green
  </acceptance_criteria>
  <done>`.reveal` matches the sketch's exact retuned timing; scroll-reveals.tsx observes only
  `.reveal` at the retuned threshold/rootMargin; both reduced-motion guards list only live classes;
  `.stickynav .cta` is deleted; the no-accent grep gate returns zero hits; full suite is green
  modulo the one known pre-existing failure.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human sign-off — fade timing, 375px collapse, mono intentionality</name>
  <what-built>
    The complete Phase 21 mono homepage rebuild: type-only hero, Swiss numbered Building index
    with full-row hover/focus invert, terminal-format Writing log, one continuous white ground, and
    every animation subtracted except a single retimed opacity fade-up on scroll.
  </what-built>
  <how-to-verify>
    Run `npm run dev` (or use the `v4-mono` branch's existing Vercel preview) and open `/` in a
    real browser. Confirm three things `npx vitest run` cannot check:
    1. **Fade timing** — scroll the page at normal reading speed. Each band's fade-up should read
       as a deliberate reveal, not as a page that is loading slowly (21-VALIDATION.md's own
       framing of the risk).
    2. **375px collapse** — open Chrome DevTools, set the viewport to 375px, and walk the whole
       page. Confirm: no horizontal scrollbar/overflow on any of the three sections; the Building
       index's numeral/title/description/status stack cleanly (not just "fits"); the Writing
       band's read-time column disappears cleanly (not truncated); the hero meta row's three
       cells are each fully legible stacked.
    3. **"Mono reads as intentional"** — compare the finished page side-by-side against
       `.planning/sketches/015-mono-passive-home/preview-a-swiss.png` and sketch variant E. Confirm
       the page reads as a deliberate brutalist mono system (hairlines, whitespace, type weight
       carrying the hierarchy) rather than an unstyled page that failed to load its stylesheet —
       this is the sketch's own stated main risk for this direction.
    Also spot-check: the footer's "Things I Love" link (`/#loves`) still scrolls to the right
    section from any interior route; the Building/Writing rows invert to solid black on both mouse
    hover and keyboard Tab focus.
  </how-to-verify>
  <resume-signal>Type "approved" to close the phase, or describe what needs adjustment.</resume-signal>
</task>

</tasks>

<threat_model>
This plan touches only CSS timing values, an `IntersectionObserver` config object, and deletes one
dead CSS rule. No user input, no auth, no new network surface, no rendering of untrusted strings.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-SC | Tampering (supply chain) | npm installs | accept | No new packages installed this plan. |
</threat_model>

<verification>
`npx vitest run` after Task 1, expecting `182 passed, 16 todo, 1 failed` (only the known
pre-existing `projects.test.tsx:188`) plus every Phase 21 test green. Task 2 is the phase's closing
human checkpoint — do not mark the phase complete until "approved" is received.
</verification>

<success_criteria>
- `.reveal` matches the sketch's authoritative retuned values (700ms, 14px, its cubic-bezier,
  threshold 0.05, rootMargin -8%)
- `scroll-reveals.tsx` observes only `.reveal`
- Both reduced-motion guard blocks list only classes with live consumers (expected: none — both
  blocks may end up with just the `.reveal` reset line)
- `.stickynav .cta` is deleted
- `grep -ri "e5411f|--color-accent" src/components/home/ src/app/page.tsx` returns 0 hits
- Monty has approved the three manual-only checks (fade timing, 375px collapse, mono
  intentionality) and the site-wide `/#loves` footer link + row keyboard-invert spot checks
- `npx vitest run` is green at baseline plus every Phase 21 test
</success_criteria>

<output>
Create `.planning/phases/21-mono-homepage-rebuild/21-06-SUMMARY.md` when done
</output>
