---
phase: 21-mono-homepage-rebuild
plan: 05
type: execute
wave: 5
depends_on: ["21-01", "21-02", "21-03", "21-04"]
files_modified:
  - src/app/page.tsx
  - src/components/home/explorative-homepage.tsx
  - src/components/home/section-work.tsx
  - src/components/home/section-newsletter.tsx
  - src/components/home/monty-monthly-carousel.tsx
  - src/components/home/rail-box.tsx
  - src/__tests__/home/explorative-homepage.test.tsx
  - src/__tests__/home/section-work.test.tsx
  - src/__tests__/home/motion-audit.test.tsx
  - src/app/globals.css
autonomous: true
requirements: [HP-02, HP-03, HP-04, HP-05]

must_haves:
  truths:
    - "The homepage renders one continuous #ffffff ground end to end: zero elements anywhere in the rendered tree carry a band-dark (or equivalent dark-ground) class."
    - "A visitor can browse the Building index and the Writing log without meeting any subscribe CTA, button, or styled card above the footer."
    - "The site-wide footer's /#loves link still resolves — the rebuilt Things I Love band keeps id=\"loves\"."
    - "Every deleted component's dedicated test file is deleted in the same commit, so the suite never fails to resolve a missing module."
  artifacts:
    - path: "src/components/home/explorative-homepage.tsx"
      provides: "Rebuilt orchestrator: Hero -> 01 Building -> 02 Writing -> 03 Things I Love, one continuous ground"
    - path: "src/app/page.tsx"
      provides: "Adds the getPublishedPosts() fetch and forwards posts/projects/montyIssues to the new band shape"
    - path: "src/__tests__/home/motion-audit.test.tsx"
      provides: "New mechanical MS-01/MS-02 gate: renders the real (unmocked) homepage tree and asserts zero kenburns/breathe/slide/shadowed/hero-ticker/statustag anywhere in it"
  key_links:
    - from: "src/app/page.tsx"
      to: "src/lib/notion.ts"
      via: "getPublishedPosts() — new fetch, same try/catch -> [] convention as the other three fetches"
      pattern: "getPublishedPosts"
    - from: "src/components/home/explorative-homepage.tsx"
      to: "src/components/layout/site-footer.tsx"
      via: "id=\"loves\" on the wrapping Things I Love section — footer.test.tsx's a[href=\"/#loves\"] assertion depends on this surviving"
      pattern: "id=\"loves\""
---

<objective>
Wire the three rebuilt bands (Hero, Building, Writing) plus the restyled Loves header into a
rebuilt `explorative-homepage.tsx` orchestrator on one continuous ground (HP-04), add the
`getPublishedPosts()` fetch to `page.tsx` and forward `posts`/`projects` to their new consumers,
delete every component this phase retires (`section-work.tsx`, `section-newsletter.tsx`,
`monty-monthly-carousel.tsx`, `rail-box.tsx`) along with their now-dead CSS and test files
(HP-05's subscribe-CTA removal happens here, since `section-newsletter.tsx` and
`monty-monthly-carousel.tsx` are the promotional surfaces being retired), and rewrite
`explorative-homepage.test.tsx` plus add the new `motion-audit.test.tsx` mechanical gate.

Purpose: this is the phase's integration point — every component built in Plans 21-01 through
21-04 only becomes the actual homepage once this plan wires them together and removes the last
survivors of the old band structure. HP-04 (no `band-dark`) and HP-05 (no subscribe CTA) cannot be
proven true until this plan lands, because both success criteria are properties of the assembled
page, not any single component.
Output: rebuilt `page.tsx` + `explorative-homepage.tsx`; four files deleted; two test files
deleted/rewritten, one new test file added; `globals.css` pruned of every class whose last
consumer was one of the four deleted components.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-mono-homepage-rebuild/21-UI-SPEC.md
@.planning/phases/21-mono-homepage-rebuild/21-RESEARCH.md
@src/components/layout/site-footer.tsx
</context>

<interfaces>
`SectionBuilding` (Plan 21-02) now takes `{ projects?: Project[] }`. `SectionWriting` (Plan 21-03,
new file `section-writing.tsx`) takes `{ posts?: BlogPost[]; montyIssues?: MontyMonthlyIssue[] }`.
`SectionLoves` (Plan 21-01) is unchanged in signature (`{ items?: LoveItem[]; categoryOrder?:
string[] }`). `Hero` (Plan 21-01) takes no props.

`ExplorativeHomepage`'s new prop shape adds `posts` alongside the three existing props:
Props type: `{ projects?: Project[]; montyIssues?: MontyMonthlyIssue[]; posts?: BlogPost[]; loves?:
LoveItem[]; loveCategories?: string[]; }`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Wire page.tsx + rebuild explorative-homepage.tsx (HP-04)</name>
  <files>src/app/page.tsx, src/components/home/explorative-homepage.tsx</files>
  <read_first>
    src/app/page.tsx (current — the three existing defensive fetches and their try/catch
    convention to replicate for the new fourth fetch)
    src/components/home/explorative-homepage.tsx (current — the band structure being replaced)
    .planning/phases/21-mono-homepage-rebuild/21-RESEARCH.md — "Data Flow" section for the exact
    `getPublishedPosts()` import path and the ISR/`revalidate = 1800` note (unchanged, one more
    `await` in the same async body)
    .planning/phases/21-mono-homepage-rebuild/21-RESEARCH.md — "Fragment-Link Contract" section:
    `site-footer.tsx` links to `/#loves` site-wide; whatever wraps the rebuilt Things I Love band
    MUST keep `id="loves"`
  </read_first>
  <action>
    In `src/app/page.tsx`: add `import { getPublishedPosts, type BlogPost } from "@/lib/notion";`.
    Add `let posts: BlogPost[] = [];` alongside the other `let` declarations, and `try { posts =
    await getPublishedPosts(); } catch {}` alongside the other three try/catch blocks (same
    pattern, do not add a comment explaining why — the existing three already establish the
    convention). Pass `posts={posts}` as a new prop on `<ExplorativeHomepage>` alongside the
    existing `projects`/`montyIssues`/`loves`/`loveCategories` props.

    Replace the entire contents of `src/components/home/explorative-homepage.tsx`. Keep the
    exported function name `ExplorativeHomepage` and the Server Component nature (no `"use
    client"`). New `Props` type per this plan's `<interfaces>` block. New band order: a root `<div
    className="min-h-screen bg-bg">` containing, in order: `<StickyNav />`, `<ScrollReveals />`,
    `<Hero />`, `<SectionBuilding projects={projects} />`, `<SectionWriting posts={posts}
    montyIssues={montyIssues} />`, then `<section className="band" id="loves"><SectionLoves
    items={loves} categoryOrder={loveCategories} /></section>`.

    Note that `SectionBuilding` and `SectionWriting` each already render their OWN `<section
    id="building">`/`<section id="writing">` wrapper internally (per Plans 21-02/21-03's `<action>`
    sections) — do NOT wrap them in an additional orchestrator-level `<section>` here, and do NOT
    apply `band`/`band-dark`/`beat` classes to them (there is nothing left to invert against; HP-04
    requires exactly one continuous `#ffffff` ground for the whole page). `SectionLoves` does NOT
    render its own wrapper section (unlike Building/Writing), so the orchestrator supplies
    `<section className="band" id="loves">` around it, exactly as today — this is the one element
    in the whole page that MUST carry `id="loves"` verbatim (Fragment-Link Contract). Do not add
    `band-dark` to it or to any other element — the entire page uses the single `.band` (`#ffffff`
    ground) treatment, with zero `.band-dark` applications anywhere.

    Remove the imports of `SectionWork` and `SectionNewsletter` (both deleted in Task 2 of this
    plan) and add an import of `SectionWriting` from `./section-writing`.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/home/section-building.test.tsx src/__tests__/home/section-writing.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "getPublishedPosts" src/app/page.tsx` returns 1
    - `grep -c "posts={posts}" src/app/page.tsx` returns 1
    - `grep -c "band-dark" src/components/home/explorative-homepage.tsx` returns 0
    - `grep -c "id=\"loves\"" src/components/home/explorative-homepage.tsx` returns 1
    - `grep -c "SectionWork\|SectionNewsletter" src/components/home/explorative-homepage.tsx` returns 0
    - `grep -c "SectionWriting" src/components/home/explorative-homepage.tsx` returns at least 1
  </acceptance_criteria>
  <done>page.tsx fetches posts defensively and forwards them; explorative-homepage.tsx assembles
  Hero/Building/Writing/Loves on one continuous ground with id="loves" preserved and zero
  band-dark anywhere.</done>
</task>

<task type="auto">
  <name>Task 2: Delete retired components + their dead CSS (HP-05, Pitfall 1, Pitfall 4)</name>
  <files>src/components/home/section-work.tsx, src/components/home/section-newsletter.tsx, src/components/home/monty-monthly-carousel.tsx, src/components/home/rail-box.tsx, src/__tests__/home/section-work.test.tsx, src/app/globals.css</files>
  <read_first>
    .planning/phases/21-mono-homepage-rebuild/21-RESEARCH.md — "Deletion Blast Radius" table
    (confirms each of these four files' only real-code consumer was the pre-rebuild
    `explorative-homepage.tsx`, already rewritten in Task 1) and "Pitfall 1" (delete
    `section-work.test.tsx` in the same task as `section-work.tsx` or the suite fails to resolve
    the module)
    src/app/globals.css lines 203-212 (`.band.band-dark, .band-dark { ... }` — its last two
    consumers, `#building`/`#writing`, were removed from `explorative-homepage.tsx` in Task 1)
    src/app/globals.css lines 232-249 (`.eyebrow`, `.eyebrow::before` — its last consumer,
    `section-newsletter.tsx`, is deleted in this task)
    src/app/globals.css lines 410-443 (`section.beat`, `.beat-grid`, the `.rail-box` comment +
    `.rail-box`/`.rail-box .num`/`.rail-box .lbl` — last consumers were `section-work.tsx` and
    `rail-box.tsx` itself, both deleted in this task; `section-building.tsx` and
    `section-loves.tsx` already stopped applying `beat-grid`/importing `RailBox` in Plans
    21-01/21-02)
    src/app/globals.css lines 532-539 (`.beat h2`), lines 568-582 (`.prometheus-shot .photo`,
    `.work-grid` — last consumer was `section-work.tsx`/old `section-building.tsx`, both already
    gone), lines 1231-1237 (`@media (max-width: 860px)` responsive overrides for `.beat-grid`,
    `.work-grid`, `.prometheus-shot .photo`)
    src/app/globals.css lines 743-894 (`.mm-*` block including `.band-dark .mm-card` — last
    consumers were `monty-monthly-carousel.tsx` and `section-newsletter.tsx`, both deleted in this
    task)
    src/app/globals.css lines 1117-1140 (`.slide`, `.slide.from-right`, `.slide.in`, `.shadowed
    .photo/.title-card`, `.shadowed.in ...` — last consumers were the old
    `section-building.tsx`'s wide photo and `section-work.tsx`'s work-grid; both are gone as of
    this task's completion)
  </read_first>
  <action>
    Delete these four files entirely: `src/components/home/section-work.tsx`,
    `src/components/home/section-newsletter.tsx`, `src/components/home/monty-monthly-carousel.tsx`,
    `src/components/home/rail-box.tsx`. Delete `src/__tests__/home/section-work.test.tsx` in the
    same task (Pitfall 1 — it imports `section-work.tsx` directly, not mocked; leaving it behind
    breaks module resolution for the entire suite).

    In `src/app/globals.css`, delete (grep-verify each current line range before deleting, since
    earlier deletions in this same task shift later line numbers):
    - `.band.band-dark, .band-dark { ... }` and its preceding "DARK BAND MECHANISM" comment block
    - `.eyebrow`, `.eyebrow::before`
    - `section.beat`, `.beat-grid`, `.beat h2`
    - `.rail-box` comment block, `.rail-box`, `.rail-box .num`, `.rail-box .lbl`
    - `.prometheus-shot .photo`, `.work-grid`
    - The entire `.mm-*` block (`.mm-head` through `.mm-dots span.on`, including `.band-dark
      .mm-card`)
    - `.slide`, `.slide.from-right`, `.slide.in`, `.shadowed .photo, .shadowed .title-card`,
      `.shadowed.in .photo, .shadowed.in .title-card` (leave `.reveal`/`.reveal.in` — those
      survive and are retuned in Plan 21-06, not deleted)
    - The `@media (max-width: 860px)` block's `.beat-grid`, `.work-grid`, and
      `.prometheus-shot .photo` lines specifically (leave `.hero { grid-template-columns: 1fr; }`
      alone only if `.hero` still exists — it does not, Plan 21-01 already deleted it, so also
      remove that now-dead line from this same media query if it survived; leave `.stickynav ul`
      and `.foot-grid` lines in that block untouched, they belong to unrelated site-wide
      components)

    Do NOT touch: `.reveal`/`.reveal.in` (retuned in Plan 21-06), the reduced-motion guard blocks'
    remaining `.reveal`/`.slide` entries (Plan 21-06 narrows those), `.stickynav .cta` (deleted in
    Plan 21-06), `.title-card*` CSS (shared component, still used by `/writing`/`/building`
    routes — do not touch).
  </action>
  <verify>
    <automated>npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/components/home/section-work.tsx` fails (file does not exist)
    - `test -f src/components/home/section-newsletter.tsx` fails (file does not exist)
    - `test -f src/components/home/monty-monthly-carousel.tsx` fails (file does not exist)
    - `test -f src/components/home/rail-box.tsx` fails (file does not exist)
    - `test -f src/__tests__/home/section-work.test.tsx` fails (file does not exist)
    - `grep -c "^\.band-dark\|^\.band\.band-dark" src/app/globals.css` returns 0
    - `grep -c "^\.eyebrow" src/app/globals.css` returns 0
    - `grep -c "^\.mm-\|^\.mm-head\|^\.mm-track" src/app/globals.css` returns 0
    - `grep -c "^\.rail-box" src/app/globals.css` returns 0
    - `grep -c "^\.beat-grid\|^\.work-grid" src/app/globals.css` returns 0
    - `npx vitest run` shows zero module-resolution failures and no regression beyond the known
      pre-existing `projects.test.tsx:188` failure (the orchestrator/motion-audit tests in Task 3
      are not written yet at this point in sequence — this run is only checking that deletion did
      not break anything else)
  </acceptance_criteria>
  <done>All four retired components and their dedicated test file are deleted; every CSS rule whose
  last consumer was one of them is pruned from globals.css.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Rewrite explorative-homepage.test.tsx + add motion-audit.test.tsx (HP-04, HP-05)</name>
  <files>src/__tests__/home/explorative-homepage.test.tsx, src/__tests__/home/motion-audit.test.tsx</files>
  <read_first>
    src/__tests__/home/explorative-homepage.test.tsx (current — every one of its 6 `it()` blocks
    asserts `.band-dark` IS present on `#building`/`#writing`, the exact inverse of HP-04; this is
    a full rewrite, not an edit)
    src/__tests__/home/section-loves.test.tsx — the mocking convention to mirror for
    Hero/SectionBuilding/SectionWriting/SectionLoves mocks in the rewritten orchestrator test
    .planning/phases/21-mono-homepage-rebuild/21-VALIDATION.md — "Per-Task Verification Map" rows
    for HP-04/HP-05/MS-01/MS-02, and the "Wave 0 Requirements" list describing exactly what
    `motion-audit.test.tsx` must assert
  </read_first>
  <behavior>
    explorative-homepage.test.tsx (mocked children, orchestrator-only concerns):
    - Mounts `StickyNav` and `ScrollReveals` (same two `it()` blocks as today, kept)
    - `container.querySelectorAll('[class*="band-dark"]').length === 0` across the whole rendered
      tree
    - `#loves` element exists in the DOM
    - `#building` and `#writing` elements exist in the DOM (rendered by the mocked
      SectionBuilding/SectionWriting components — mock them to render a `data-testid` element with
      that id, mirroring the current file's mocking style)
    - `screen.queryByText(/subscribe/i)` returns null anywhere in the rendered tree

    motion-audit.test.tsx (real, UNMOCKED children — renders the actual homepage tree):
    - Renders `<ExplorativeHomepage />` with no props (all four data arrays default to `[]`,
      exercising `SectionLoves`'s real `PhotoMarquee` fallback path, which itself renders real,
      unmocked `Photo` components)
    - `container.querySelectorAll('.kenburns, .breathe, .slide, .shadowed, .hero-ticker,
      .statustag').length === 0`
  </behavior>
  <action>
    Rewrite `src/__tests__/home/explorative-homepage.test.tsx` completely. Keep the existing
    `vi.mock("motion/react", ...)`, `vi.mock("@/components/home/sticky-nav", ...)`, and
    `vi.mock("@/components/home/scroll-reveals", ...)` blocks as-is. Replace the `Hero`,
    `SectionBuilding` (was: also mocking the deleted `SectionWork`), `SectionLoves` mocks, and add
    a new `SectionWriting` mock, each rendering a `data-testid`-bearing element with the
    appropriate id where relevant (`#building` on the SectionBuilding mock, `#writing` on the
    SectionWriting mock — mirroring how the real components self-wrap with those ids per Plans
    21-02/21-03). Delete the `SectionWork`/`SectionNewsletter` mock blocks entirely. Write every
    item in `<behavior>` above as a separate `it()` block, following the file's existing
    `describe`/`afterEach(cleanup)` structure.

    Create `src/__tests__/home/motion-audit.test.tsx` (new file). Do NOT mock `Hero`,
    `SectionBuilding`, `SectionWriting`, `SectionLoves`, `Photo`, or `PhotoMarquee` — this test's
    entire purpose is to catch a stray motion class anywhere in the REAL rendered tree. Mock only
    `motion/react`'s `useReducedMotion` (returns `false`, same convention as the orchestrator test)
    if `PhotoMarquee` still imports it after Plan 21-04 (grep-verify first — Plan 21-04 removed
    that import, so this mock is likely unnecessary; only add it if the import still exists).
    Render `<ExplorativeHomepage />` with no props and assert the single `<behavior>` query above
    returns a `NodeList` of length 0.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/home/explorative-homepage.test.tsx src/__tests__/home/motion-audit.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/__tests__/home/explorative-homepage.test.tsx src/__tests__/home/motion-audit.test.tsx` passes, covering every `<behavior>` item
    - `grep -c "toContain(\"band-dark\")" src/__tests__/home/explorative-homepage.test.tsx` returns 0 (the old inverse assertion must not survive)
    - `grep -c "band-dark" src/__tests__/home/explorative-homepage.test.tsx` returns at least 1 (as part of the NEW absence assertion, e.g. `querySelectorAll('[class*="band-dark"]')`)
    - `grep -c "subscribe" src/__tests__/home/explorative-homepage.test.tsx` returns at least 1 (the HP-05 absence assertion)
    - `npx vitest run` reports `182 passed, 16 todo, 1 failed` PLUS every test in the new/rewritten
      files above, with the 1 failure being only `projects.test.tsx:188`
  </acceptance_criteria>
  <done>explorative-homepage.test.tsx asserts the post-rebuild band structure (zero band-dark,
  id=loves present, no subscribe CTA); motion-audit.test.tsx exists and proves the real rendered
  homepage tree carries none of the six MS-01/MS-02 motion classes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| `page.tsx` → `ExplorativeHomepage` → all child bands | Server-side prop-passing only; no client
input crosses this boundary. All four data arrays are already-fetched, already-typed Notion/RSS
results, defensively defaulted to `[]` on fetch failure. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-08 | Denial of Service (unhandled fetch rejection) | `page.tsx`'s new `getPublishedPosts()` call | mitigate | Wrapped in the same `try {} catch {} → []` pattern as the other three fetches, so a Notion outage degrades to an empty Writing list rather than crashing the page. |
| T-21-09 | Availability (dangling reference) | `explorative-homepage.tsx` deleting imports of `SectionWork`/`SectionNewsletter` | mitigate | Both deletions happen in the same plan/wave as their source files' removal, verified by `npx vitest run` (a stale import would fail the build/test run immediately). |
| T-21-SC | Tampering (supply chain) | npm installs | accept | No new packages installed this plan. |
</threat_model>

<verification>
`npx vitest run src/__tests__/home/` after each task; full-suite gate `npx vitest run` after Task
3, expecting `182 passed, 16 todo, 1 failed` plus every new/updated homepage test passing, with the
one failure being only `projects.test.tsx:188`.
</verification>

<success_criteria>
- `page.tsx` fetches `posts` defensively and forwards it alongside the three existing data arrays
- `explorative-homepage.tsx` renders Hero → Building → Writing → Loves on one continuous
  `#ffffff` ground with zero `band-dark` anywhere and `id="loves"` preserved
- `section-work.tsx`, `section-newsletter.tsx`, `monty-monthly-carousel.tsx`, `rail-box.tsx`, and
  `section-work.test.tsx` are all deleted
- Every CSS rule whose last consumer was one of those four components is pruned from `globals.css`
- `explorative-homepage.test.tsx` is fully rewritten to assert the new band structure
- `motion-audit.test.tsx` exists and proves the real rendered tree is free of all six MS-01/MS-02
  motion classes
- `npx vitest run` stays at baseline (182 passed / 16 todo / 1 pre-existing failure) plus every new
  test passing
</success_criteria>

<output>
Create `.planning/phases/21-mono-homepage-rebuild/21-05-SUMMARY.md` when done
</output>
