---
phase: 21-mono-homepage-rebuild
plan: 04
type: execute
wave: 4
depends_on: ["21-03"]
files_modified:
  - src/components/home/photo.tsx
  - src/components/home/photo-marquee.tsx
  - src/app/globals.css
autonomous: true
requirements: [MS-01, MS-02]

must_haves:
  truths:
    - "No photo anywhere on the homepage ever ken-burns-zooms or breathes, for any visitor, regardless of motion preference (MS-01 kills these outright, not behind a reduced-motion guard)."
    - "The Things I Love band's PhotoMarquee fallback (shown when Notion has no published items) renders as a static row, not a looping horizontal slide."
  artifacts:
    - path: "src/components/home/photo.tsx"
      provides: "Photo frame primitive with no kenburns/breathe classes or props"
    - path: "src/components/home/photo-marquee.tsx"
      provides: "Static (non-animated) fallback photo row"
  key_links:
    - from: "src/components/home/photo-marquee.tsx"
      to: "src/components/home/photo.tsx"
      via: "Photo component (unchanged public API minus the removed breathe prop)"
      pattern: "import \\{ Photo \\} from \"\\./photo\""
---

<objective>
Finish MS-01's photo-motion teardown: remove the unconditional `.kenburns` wrapper class and the
`breathe` prop from `photo.tsx` (Hero and the old `section-building.tsx` were its only other
consumers, and both have already stopped using `Photo` entirely as of Plans 21-01/21-02 — after
this plan, `photo.tsx`'s only remaining consumer anywhere in `src/` is `photo-marquee.tsx`), and
strip the sliding `.marquee .track` animation from `photo-marquee.tsx` so the Things I Love
fallback row renders static.

Purpose: `photo.tsx` currently applies `.img.kenburns` unconditionally to every image it renders,
regardless of any prop — this is the one remaining site-wide source of ambient photo motion MS-01
requires removing outright. This plan must land before Plan 21-05's `motion-audit.test.tsx` (which
renders the real, unmocked homepage tree — including `SectionLoves`'s `PhotoMarquee` fallback path
— and asserts zero `.kenburns`/`.breathe` anywhere in it).
Output: `photo.tsx` and `photo-marquee.tsx` with all ambient animation removed; `globals.css`
pruned of `.kenburns`/`.breathe`/`@keyframes kb`/`@keyframes breathe`/`@keyframes slide`/the
`.marquee .track` animation property.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-mono-homepage-rebuild/21-UI-SPEC.md
@.planning/phases/21-mono-homepage-rebuild/21-RESEARCH.md
</context>

<interfaces>
`Photo`'s current props: `{ src?, alt?, caption?, dark?, aspectRatio?, breathe?, priority?, sizes?,
objectPosition?, className? }`. New props: identical minus `breathe`. Every consumer of `Photo`
left in the codebase after Plans 21-01/21-02/21-03 land is `photo-marquee.tsx` alone (grep-verify
before editing: `grep -rln "from \"\\./photo\"\|from \"@/components/home/photo\"" src` should
return only `photo-marquee.tsx` and its own test/mock references).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Strip kenburns/breathe from photo.tsx, marquee animation from photo-marquee.tsx</name>
  <files>src/components/home/photo.tsx, src/components/home/photo-marquee.tsx</files>
  <read_first>
    src/components/home/photo.tsx (current implementation — the `breathe` prop and the
    unconditional `<div className="img kenburns">` wrapper being removed)
    src/components/home/photo-marquee.tsx (current implementation — the `useReducedMotion()` +
    inline `animationPlayState` logic tied to the CSS `slide` animation being removed)
    .planning/phases/21-mono-homepage-rebuild/21-UI-SPEC.md — Component Disposition table rows for
    `photo.tsx` ("Remove the breathe prop and .kenburns class usage") and `photo-marquee.tsx`
    ("Remove the sliding .marquee .track animation... renders as a static row")
  </read_first>
  <behavior>
    - `Photo` no longer accepts a `breathe` prop (removed from the type and the destructure)
    - `Photo`'s rendered image wrapper carries class `"img"` only, never `"kenburns"`
    - `Photo`'s outer wrapper never carries class `"breathe"`, under any prop combination
    - `PhotoMarquee` renders its doubled track with no `animationPlayState` style and no
      `useReducedMotion()` call (the animation it was pausing no longer exists)
    - `PhotoMarquee`'s track still renders every item (doubled, second half `aria-hidden`) — only
      the animation is removed, the DOM structure and accessibility behavior are unchanged
  </behavior>
  <action>
    In `src/components/home/photo.tsx`: delete the `breathe?: boolean` prop from the `Props` type
    and its destructure/default. Change `<div className={cn("photo", dark && "dark", breathe &&
    "breathe", className)}` to `<div className={cn("photo", dark && "dark", className)}`. Change
    `<div className="img kenburns">` to `<div className="img">`.

    In `src/components/home/photo-marquee.tsx`: remove the `"use client"` directive's reason for
    existing — but keep it `"use client"` only if still needed; re-check after the edit (the
    component no longer calls `useReducedMotion()` or sets any inline style, so if nothing else in
    the file requires client-side JS, it can become a plain Server Component; if unsure, keep
    `"use client"` for safety since converting the directive is not required by any requirement in
    this phase — do not spend effort here beyond removing the dead animation code). Remove the
    `import { useReducedMotion } from "motion/react";` line and the `const reducedMotion =
    useReducedMotion();` line. Remove the `style={{ gap: 18, animationPlayState: reducedMotion ?
    "paused" : "running" }}` inline style's `animationPlayState` key entirely (keep `gap: 18`).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/home/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "breathe" src/components/home/photo.tsx` returns 0
    - `grep -c "kenburns" src/components/home/photo.tsx` returns 0
    - `grep -c "useReducedMotion\|animationPlayState" src/components/home/photo-marquee.tsx` returns 0
    - `npx vitest run src/__tests__/home/` shows zero new failures beyond the pre-existing baseline
  </acceptance_criteria>
  <done>Photo renders with no kenburns/breathe classes under any props; PhotoMarquee's track has
  no animation-pausing logic left because the animation itself no longer exists.</done>
</task>

<task type="auto">
  <name>Task 2: Delete kenburns/breathe/marquee-slide CSS from globals.css</name>
  <files>src/app/globals.css</files>
  <read_first>
    src/app/globals.css lines 1141-1146 (`.kenburns`, `.breathe`) and lines 482-489 (`@keyframes
    kb`, `@keyframes breathe`)
    src/app/globals.css lines 735-739 (`.marquee .track { animation: slide 30s linear infinite; }`)
    — this is the ONLY remaining consumer of `@keyframes slide` after Plan 21-01 already deleted
    `.hero-ticker .track`'s reference to it; verify via `grep -c "animation: slide" src/app/globals.css`
    returns exactly 1 before this task's edits (confirming `.marquee .track` is the sole survivor)
    src/app/globals.css lines 1148-1188 (both reduced-motion guard blocks — `body.no-motion` and
    `@media (prefers-reduced-motion: reduce)` — both currently list `.kenburns, .breathe, .blob,
    .pslide, .marquee .track, .hero-ticker .track` as animation-kill targets)
  </read_first>
  <action>
    Delete `.kenburns { animation: kb 22s ease-in-out infinite alternate; }` and `.breathe {
    animation: breathe 7s ease-in-out infinite; }` (lines ~1141-1146). Delete `@keyframes kb { ...
    }` and `@keyframes breathe { ... }` (lines ~482-489). In the `.marquee .track` rule, delete the
    `animation: slide 30s linear infinite;` declaration (keep the rest of `.marquee`/`.track`'s
    layout CSS — flex/gap/display properties — untouched; only the animation property goes). Once
    that edit lands, `@keyframes slide` has zero remaining consumers (verify: `grep -c "animation:
    slide" src/app/globals.css` returns 0) — delete `@keyframes slide { ... }` too (lines
    ~478-481).

    In both reduced-motion guard blocks (`body.no-motion .kenburns, .breathe, .blob, .pslide,
    .marquee .track, .hero-ticker .track { animation: none !important; }` and its `@media
    (prefers-reduced-motion: reduce)` twin), narrow the selector list to drop `.kenburns`,
    `.breathe`, `.pslide`, `.hero-ticker .track` (all now nonexistent classes with nothing left to
    kill), and `.marquee .track` (its animation property no longer exists, nothing to guard). Leave
    `.blob` in the list only if `grep -rln "\bblob\b" src --include="*.tsx"` finds a live consumer
    anywhere in the repo; if that grep returns nothing (as 21-RESEARCH.md's own investigation
    found), drop `.blob` too and delete both guard blocks' animation-kill lines entirely if nothing
    remains to list (leave the `.reveal`/`.slide` opacity/transform reset lines in these same
    blocks untouched — those are handled in Plan 21-06, not here).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/home/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^\.kenburns\|^\.breathe {" src/app/globals.css` returns 0
    - `grep -c "@keyframes kb\|@keyframes breathe\|@keyframes slide" src/app/globals.css` returns 0
    - `grep -c "animation: slide" src/app/globals.css` returns 0
    - `grep -c "\.pslide\|\.hero-ticker .track" src/app/globals.css` returns 0
    - `npx vitest run src/__tests__/home/` shows zero new failures beyond the pre-existing baseline
  </acceptance_criteria>
  <done>Every kenburns/breathe/marquee-slide CSS rule and keyframe is deleted; the reduced-motion
  guard blocks no longer reference any now-nonexistent class.</done>
</task>

</tasks>

<threat_model>
This plan touches no user input, no auth, no new network surface, and no rendering of untrusted
strings — it is a pure CSS/prop removal on an existing, already-audited component. No new threats
are introduced.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-SC | Tampering (supply chain) | npm installs | accept | No new packages installed this plan; `motion/react`'s `useReducedMotion` import is removed, not added. |
</threat_model>

<verification>
`npx vitest run src/__tests__/home/` after each task; full-suite gate `npx vitest run` before
moving to Plan 21-05, expecting baseline-equivalent (only `projects.test.tsx:188` red).
</verification>

<success_criteria>
- `photo.tsx` never applies `.kenburns`/`.breathe` under any prop combination
- `photo-marquee.tsx`'s track has no animation left to pause/guard
- `globals.css` has zero remaining `.kenburns`/`.breathe`/`@keyframes kb`/`@keyframes
  breathe`/`@keyframes slide`/`animation: slide` references
- Reduced-motion guard blocks no longer list dead class names
- No regression in `npx vitest run` beyond the known pre-existing failure
</success_criteria>

<output>
Create `.planning/phases/21-mono-homepage-rebuild/21-04-SUMMARY.md` when done
</output>
