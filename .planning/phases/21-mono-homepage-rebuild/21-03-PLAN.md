---
phase: 21-mono-homepage-rebuild
plan: 03
type: execute
wave: 3
depends_on: ["21-02"]
files_modified:
  - src/components/home/section-writing.tsx
  - src/__tests__/home/section-writing.test.tsx
  - src/app/globals.css
autonomous: true
requirements: [HP-03]

must_haves:
  truths:
    - "A visitor sees the Writing band as a terminal-format log (~/writing header, dates left, read time right) with no box or frame around it."
    - "The list merges real blog essays and real Monty Monthly issues, sorted newest first, capped at 5 rows, with an all posts -> link to /writing."
    - "Hovering or focusing a Writing row inverts the entire row identically to a Building row (same mechanism, same 150ms timing)."
  artifacts:
    - path: "src/components/home/section-writing.tsx"
      provides: "New terminal Writing band: merges getPublishedPosts + fetchMontyMonthlyIssues data"
      exports: ["SectionWriting"]
    - path: "src/app/globals.css"
      provides: ".e-term family: mono terminal type, header rule, full-row invert, 760px rd-column hide"
  key_links:
    - from: "src/components/home/section-writing.tsx"
      to: "src/utils/reading-time.ts"
      via: "estimateReadingTime(text: string): number — greenfield wiring, zero prior call sites in the repo"
      pattern: "estimateReadingTime\\("
    - from: "src/components/home/section-writing.tsx"
      to: "src/app/globals.css"
      via: ".e-post class + shared full-row invert mechanism"
      pattern: "\\.e-post"
---

<objective>
Build the new `section-writing.tsx` component locked by HP-03: a terminal-register `~/writing` log
merging real blog posts and real Monty Monthly issues, newest-first, capped at 5 rows, no frame —
just the header's `border-bottom` rule. Add the `.e-term`/`.e-post` CSS family to `globals.css`,
reusing the identical full-row hover/focus-invert mechanism Plan 21-02 built for Building rows.

Purpose: HP-03 is the third and final register lock (terminal, the only mono-type surface on the
page — this is what makes the blog read as a log rather than a pitch). This plan is also where
`estimateReadingTime()` gets its first call site in the entire repo (Pitfall 3 in 21-RESEARCH.md:
there is no existing pattern to copy — `/writing`'s own page uses a different, heavier
per-post-Notion-fetch function).
Output: new `section-writing.tsx` + new `section-writing.test.tsx`; new `.e-term` CSS.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-mono-homepage-rebuild/21-UI-SPEC.md
@.planning/phases/21-mono-homepage-rebuild/21-RESEARCH.md
@.planning/sketches/015-mono-passive-home/index.html
@src/lib/notion.ts
@src/lib/rss/substack.ts
@src/utils/reading-time.ts
</context>

<interfaces>
From `src/lib/notion.ts` (unmodified):
```
export interface BlogPost {
  id: string; slug: string; title: string; description: string;
  published: boolean; date: string; tags: string[];
  cover: string | null; emoji: string | null; lastEdited: string;
}
export async function getPublishedPosts(): Promise<BlogPost[]>
```
`date` is an ISO string OR `""` if unset — filter falsy dates before sorting (Pitfall 5;
`writing/page.tsx`'s own `groupPostsByYear()` does the same `if (!post.date) continue;` guard).

From `src/lib/rss/substack.ts` (unmodified):
```
export type MontyMonthlyIssue = {
  title: string; link: string; pubDate: string; // RFC-822, e.g. "Wed, 02 Jul 2026 12:00:00 GMT"
  description: string; thumbnail: string | null;
};
```

From `src/utils/reading-time.ts` (unmodified, greenfield — zero existing call sites anywhere in
the repo before this plan; do not go looking for a "pattern to copy"):
```
export function estimateReadingTime(text: string): number // words/200, min 1, sync, pure
```

`SectionWriting`'s new signature: `SectionWriting({ posts = [], montyIssues = [] }: { posts?:
BlogPost[]; montyIssues?: MontyMonthlyIssue[] })`. Plan 21-05 is the plan that adds the
`getPublishedPosts()` fetch to `page.tsx` and actually wires `posts`/`montyIssues` through the
orchestrator into this component — until then this component is testable standalone with mocked
props.

Sketch reference CSS (`.e-term` family, from
`.planning/sketches/015-mono-passive-home/index.html` lines 190-212), adapted to shipped tokens:
- `.e-term { font-family: var(--font-mono); font-size: var(--text-sm); line-height: 1.85; }`
- `.e-term .hd { color: var(--color-text-muted); font-size: var(--text-xs); letter-spacing: 0.12em; text-transform: uppercase; padding-bottom: 12px; margin-bottom: 16px; border-bottom: 1px solid var(--color-border); }`
- `.e-post { display: grid; grid-template-columns: 104px 1fr 92px; gap: 16px; align-items: baseline; padding: 5px 8px; margin: 0 -8px; text-decoration: none; position: relative; }`
- `.e-post::before { content: ""; position: absolute; inset: 0; background: var(--color-invert); z-index: -1; opacity: 0; transition: opacity 150ms ease; }` — same full-row-invert mechanism as `.a-row`, but `.e-post` bleeds to its own `margin: 0 -8px` edges (no `-20px` overhang needed, it already has negative margin).
- `.e-post:hover::before, .e-post:focus-visible::before { opacity: 1; }` (gate `:hover` behind `@media (hover: hover)`, same as `.a-row`)
- `.e-post:hover, .e-post:focus-visible { color: var(--color-text-inverse); }`
- `.e-post .dt { color: var(--color-text-muted); }` `.e-post:hover .dt, .e-post:focus-visible .dt, .e-post:hover .rd, .e-post:focus-visible .rd { color: var(--color-text-inverse-dim); }`
- `.e-post .rd { color: var(--color-text-muted); text-align: right; }`
- `.e-post:focus-visible { outline-color: currentColor; outline-offset: 2px; }` — same scoped
  override pattern as `.a-row:focus-visible` in Plan 21-02; do not touch the global D-10 rule.
- `.e-term .more { display: inline-block; margin-top: 16px; color: var(--color-text-muted); text-decoration: none; }` `.e-term .more:hover { color: var(--color-text); box-shadow: inset 0 -1px 0 var(--color-text); }`
- 760px responsive (append to the existing `@media (max-width: 760px)` block): `.e-post { grid-template-columns: 84px 1fr; } .e-post .rd { display: none; }`
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the .e-term terminal CSS family to globals.css</name>
  <files>src/app/globals.css</files>
  <read_first>
    .planning/phases/21-mono-homepage-rebuild/21-UI-SPEC.md — section "3. Writing list (HP-03)"
    (this plan's `<interfaces>` block above is a condensed, ready-to-paste version)
    src/app/globals.css — locate the `.a-row` block Plan 21-02 just added (grep `\.a-row {`) and
    place the new `.e-term` family immediately after it, so the two row-invert mechanisms sit
    together
  </read_first>
  <action>
    Add exactly the `.e-term` family listed in this plan's `<interfaces>` block verbatim
    (`.e-term`, `.e-term .hd`, `.e-post`, `.e-post::before`, hover/focus-visible rules, `.e-post
    .dt`/`.rd`, the scoped `:focus-visible` outline override, `.e-term .more` + hover), plus the
    `@media (hover: hover)` guard around only the `.e-post:hover::before` rule, plus the 760px
    responsive `.e-post` column collapse appended to the existing `@media (max-width: 760px)`
    block (do not open a second one — grep for the block Plan 21-02 already used/created and
    append inside it).
  </action>
  <verify>
    <automated>grep -c "\.e-post" src/app/globals.css</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "\.e-term {" src/app/globals.css` returns 1
    - `grep -c "\.e-post {" src/app/globals.css` returns 1
    - `grep -c "\.e-post::before" src/app/globals.css` returns 1
    - `grep -c "grid-template-columns: 84px 1fr" src/app/globals.css` returns 1
    - `grep -c "\.e-post .rd { display: none" src/app/globals.css` returns 1
  </acceptance_criteria>
  <done>The `.e-term`/`.e-post` CSS family exists exactly as specified, including the 760px
  read-time column hide and the scoped focus-ring override.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build section-writing.tsx — merged terminal Writing list (HP-03)</name>
  <files>src/components/home/section-writing.tsx, src/__tests__/home/section-writing.test.tsx</files>
  <read_first>
    src/app/writing/page.tsx lines ~90-110 — confirms `calculateReadingTime(await
    getBlocks(post.id))` is that page's OWN pattern (a real per-post Notion fetch) and is NOT what
    this component should replicate; this component uses the lighter `estimateReadingTime(text)`
    instead, per 21-RESEARCH.md Pitfall 3
    src/components/home/section-newsletter.tsx — the component this one supersedes in role (not
    in file identity — `section-newsletter.tsx` itself is deleted in Plan 21-05, not modified
    here); note its `formatIssueDate()` helper pattern for parsing `pubDate` defensively
    .planning/phases/21-mono-homepage-rebuild/21-UI-SPEC.md — section "3. Writing list (HP-03)",
    "Content" bullet list, for the exact merge/sort/cap/date-format rules
  </read_first>
  <behavior>
    - With both `posts` and `montyIssues` empty, renders the `~/writing` header and the exact
      empty-state text "Nothing here yet. Check back soon." (no `.e-post` rows)
    - With a mix of posts and issues, renders at most 5 `.e-post` rows total, sorted by date
      descending across both sources combined (not two separate lists)
    - Skips any post whose `date` is a falsy/empty string entirely (never renders `Invalid Date`)
    - Each row's date column renders in `YYYY-MM` format (e.g. "2026-07"), not a long-form date
    - Each row's read-time column renders the output of `estimateReadingTime()` on that item's
      `description`, formatted as `"{n} min"`
    - Blog post rows link to `/blog/{post.slug}` (internal, no target/rel needed)
    - Monty Monthly rows link to `issue.link` with `target="_blank" rel="noopener noreferrer"`
    - Renders exactly one `all posts →` link with `href="/writing"` after the row list
    - No element in the rendered tree carries a class implying a 2px box/frame around the list —
      only the header (`.hd`) carries a `border-bottom`
  </behavior>
  <action>
    Create `src/components/home/section-writing.tsx` as a Server Component (no `"use client"`).
    Signature: `export function SectionWriting({ posts = [], montyIssues = [] }: { posts?:
    BlogPost[]; montyIssues?: MontyMonthlyIssue[] })`, importing `type { BlogPost } from
    "@/lib/notion"`, `type { MontyMonthlyIssue } from "@/lib/rss/substack"`, `{
    estimateReadingTime } from "@/utils/reading-time"`.

    Internal merge algorithm (module-scope helper functions, unit-testable): map `posts` (filter
    `!post.date` out first) to `{ date: post.date, title: post.title, readTime:
    estimateReadingTime(post.description), href: \`/blog/${post.slug}\`, external: false }`; map
    `montyIssues` to `{ date: issue.pubDate, title: issue.title, readTime:
    estimateReadingTime(issue.description), href: issue.link, external: true }`; concatenate both
    arrays, sort descending by `new Date(item.date).getTime()`, `.slice(0, 5)`. Format each row's
    displayed date with a small helper `formatYYYYMM(dateStr)` returning
    `` `${d.getUTCFullYear()}-${String(d.getUTCMonth() + 1).padStart(2, "0")}` `` (return `""` if
    `Number.isNaN(d.getTime())` — defensive, should be unreachable given the pre-filter).

    Structure:
    ```
    <section className="wrap a-sec" id="writing">
      <h2 className="reveal font-mono text-xs uppercase tracking-[0.12em] text-text-muted">02 · Writing</h2>
      <div className="e-term reveal">
        <div className="hd">~/writing</div>
        {rows.length === 0
          ? <p>Nothing here yet. Check back soon.</p>
          : rows.map((row) => <a className="e-post" href={row.href} {...(row.external ? {target:"_blank", rel:"noopener noreferrer"} : {})}>
              <span className="dt">{formatYYYYMM(row.date)}</span>
              <span>{row.title}</span>
              <span className="rd">{row.readTime} min</span>
            </a>)}
        <a className="more" href="/writing">all posts →</a>
      </div>
    </section>
    ```
    (Illustrative shape only — write real JSX.) The empty-state `<p>` should sit inside `.e-term`
    below the `.hd` header, styled in the same mono register (no extra class needed beyond
    inheriting `.e-term`'s `font-family`/`font-size`).

    Write `src/__tests__/home/section-writing.test.tsx` (new file) mirroring the existing test
    conventions in `src/__tests__/home/section-loves.test.tsx`. Cover every item in `<behavior>`
    above using literal fixture data for `posts`/`montyIssues` (at least one post with a falsy
    `date` to prove the filter works, and enough combined items to prove the 5-row cap and
    cross-source date-descending sort — e.g. 4 posts + 3 issues with interleaved dates, assert the
    5 most recent by date appear in the correct order regardless of source).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/home/section-writing.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/__tests__/home/section-writing.test.tsx` passes, covering every `<behavior>` item
    - `grep -c "estimateReadingTime(" src/components/home/section-writing.tsx` returns 2 (once per source)
    - `grep -c "calculateReadingTime\|getBlocks(" src/components/home/section-writing.tsx` returns 0 (must NOT replicate the heavy per-post Notion fetch pattern)
    - `grep -c "slice(0, 5)" src/components/home/section-writing.tsx` returns 1
    - `grep -c "Nothing here yet. Check back soon." src/components/home/section-writing.tsx` returns 1
    - `grep -c "border\b\|frame\b" src/components/home/section-writing.tsx` returns 0 (no box/frame wrapper class — only the CSS `.hd` rule carries a border, not a component-level class)
    - `grep -ri "e5411f\|--color-accent" src/components/home/section-writing.tsx` returns 0
  </acceptance_criteria>
  <done>section-writing.tsx renders the merged, sorted, capped terminal Writing list with real
  reading-time estimates, no frame, and its new test suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Notion post + Substack RSS data → Writing row title/date/read-time | Both sources are
Monty-authored content rendered as plain React text children; no `dangerouslySetInnerHTML`. |
| Writing rows → external Substack links | `issue.link` is RSS-feed-controlled but points at
`montymonthly.substack.com`, opened with `rel="noopener noreferrer"`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-05 | Tampering (reverse tabnabbing) | `section-writing.tsx` Monty Monthly rows (`target="_blank"`) | mitigate | `rel="noopener noreferrer"` set on every external row — verified by grep in Task 2's acceptance criteria. |
| T-21-06 | Denial of Service (unbounded render) | Merged posts+issues list | mitigate | `.slice(0, 5)` caps the rendered row count regardless of how many posts/issues exist upstream. |
| T-21-07 | Information Disclosure / Injection | Rendering `post.title`/`issue.title`/description-derived read time | accept | Plain React text-child rendering (auto-escaped); both sources are single-author-controlled content, not third-party user input. |
| T-21-SC | Tampering (supply chain) | npm installs | accept | No new packages installed this plan. |
</threat_model>

<verification>
`npx vitest run src/__tests__/home/section-writing.test.tsx` after Task 2; full homepage sweep
`npx vitest run src/__tests__/home/` after both tasks; full-suite gate `npx vitest run` before
moving to Plan 21-04, expecting baseline-equivalent plus the new Writing assertions green.
</verification>

<success_criteria>
- `.e-term`/`.e-post` CSS family exists with full-row hover/focus invert (identical mechanism to
  `.a-row`), 150ms transition, scoped focus-ring override, 760px `.rd` column hide
- `section-writing.tsx` merges real posts + Monty Monthly issues, sorted newest-first, capped at 5,
  with correct `YYYY-MM` dates and real `estimateReadingTime()` values
- No frame/box wrapper — only the `.hd` header carries a visible rule
- `section-writing.test.tsx` is new and green
- No regression in `npx vitest run` beyond the known pre-existing failure
</success_criteria>

<output>
Create `.planning/phases/21-mono-homepage-rebuild/21-03-SUMMARY.md` when done
</output>
