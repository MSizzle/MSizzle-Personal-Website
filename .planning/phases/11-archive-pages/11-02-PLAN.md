---
phase: 11-archive-pages
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/photos.ts
  - src/lib/dates.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "A new data module `src/lib/photos.ts` exports an `ArchivePhoto` type and a `PHOTOS_BY_YEAR` const array of the 6 existing photos with empirical per-file years (D-11 REVISED — 3×2023 + 3×2025, NO 2024)"
    - "`groupPhotosByYear(): Map<number, ArchivePhoto[]>` exists and returns entries sorted by year descending (D-12)"
    - "`formatDayNumeral(iso: string | null): string` exists in src/lib/dates.ts and returns '' for null + the day-of-month digit (as string) for any ISO input (D-17)"
    - "Plans 11-04 and 11-05 can import these symbols without further setup (D-29 — primitives untouched; this is data + helper only)"
  artifacts:
    - path: "src/lib/photos.ts"
      provides: "ArchivePhoto type + PHOTOS_BY_YEAR const + groupPhotosByYear() helper"
      exports: ["ArchivePhoto", "PHOTOS_BY_YEAR", "groupPhotosByYear"]
      contains: "export const PHOTOS_BY_YEAR"
      min_lines: 40
    - path: "src/lib/dates.ts"
      provides: "Existing formatMonthYear + formatMonthDay + NEW formatDayNumeral (D-17)"
      contains: "export function formatDayNumeral"
  key_links:
    - from: "src/lib/photos.ts"
      to: "public/MSizzle-website-photos/*"
      via: "filename string literals"
      pattern: "000092530012\\.jpeg|20230928 MSB_0114\\.jpg|IMG_0028\\.jpeg|IMG_1075\\.JPG|IMG_2129\\.jpeg|Patricof09\\.jpg"
    - from: "src/lib/dates.ts"
      to: "JavaScript Date.getUTCDate()"
      via: "formatDayNumeral implementation"
      pattern: "getUTCDate"
---

<objective>
Land the foundational data + helper pieces consumed by Plans 11-04 and 11-05:

1. **NEW `src/lib/photos.ts`** — declares `ArchivePhoto` shape, `PHOTOS_BY_YEAR` array (D-11 REVISED — empirical year mapping from macOS `mdls kMDItemContentCreationDate`: 3 photos in 2023 + 3 photos in 2025, **NO 2024 photos**), and `groupPhotosByYear(): Map<number, ArchivePhoto[]>` helper sorted descending (D-12). Hardcoded data per D-11/D-24 — Notion-driven photo source is explicitly deferred.
2. **Extend `src/lib/dates.ts`** with `formatDayNumeral(iso: string | null): string` (D-17) — returns the day-of-month digit for an ISO date string (and `""` for null), used by /events Upcoming section giant 84px / 56px numerals (Plan 11-04).

Purpose: separating data + helpers from page composition means Plans 11-04 and 11-05 can render their routes without touching shared modules under wave-parallel conditions.

Output: 1 new file (`src/lib/photos.ts`) + 1 modified file (`src/lib/dates.ts` gains 1 new export).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/11-archive-pages/11-CONTEXT.md
@.planning/phases/11-archive-pages/11-RESEARCH.md
@.planning/phases/11-archive-pages/11-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- Existing src/lib/dates.ts pattern this plan extends — same UTC-based handling, same null-returns-"" guard. -->

Currently in `src/lib/dates.ts`:
```ts
export function formatMonthYear(iso: string | null): string {
  if (!iso) return "";
  return new Date(iso)
    .toLocaleDateString("en-US", { month: "short", year: "numeric" })
    .toUpperCase();
}

export function formatMonthDay(iso: string | null): string {
  if (!iso) return "";
  return new Date(iso)
    .toLocaleDateString("en-US", { month: "short", day: "numeric" })
    .toUpperCase();
}
```

`formatDayNumeral` follows the same `iso: string | null` → `""` for null contract.

<!-- Empirical photo year mapping from RESEARCH.md § "Photo Year Mapping" table (mdls kMDItemContentCreationDate). -->

| Filename | Creation date (UTC) | Year | Caption |
|----------|---------------------|------|---------|
| `000092530012.jpeg` | 2025-11-17 | 2025 | "Film, 2025" (also homepage PHOTOS[0] / epigraph) |
| `IMG_2129.jpeg` | 2025-11-26 | 2025 | "Nov 2025" |
| `Patricof09.jpg` | 2025-02-13 | 2025 | "Feb 2025" (also /writing hero PHOTOS[5]) |
| `IMG_1075.JPG` | 2023-12-11 | 2023 | "Dec 2023" (also /events hero PHOTOS[3]) |
| `20230928 MSB_0114.jpg` | 2023-09-28 | 2023 | "Sep 2023" (also /photos hero PHOTOS[1]) |
| `IMG_0028.jpeg` | 2023-07-03 | 2023 | "Jul 2023" |
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add formatDayNumeral helper to src/lib/dates.ts</name>
  <files>src/lib/dates.ts</files>
  <read_first>
    - src/lib/dates.ts (existing file — formatMonthYear + formatMonthDay define the null-guard + UTC pattern this helper must match)
    - .planning/phases/11-archive-pages/11-CONTEXT.md D-17 (helper contract)
    - .planning/phases/11-archive-pages/11-RESEARCH.md §"formatDayNumeral helper (NEW; Plan 11-02)" (canonical implementation block + reasoning for getUTCDate over getDate)
    - .planning/phases/11-archive-pages/11-RESEARCH.md §"Pitfall 2" (EventItem.date can be null — null guard mandatory)
    - .planning/phases/11-archive-pages/11-VALIDATION.md row 11-02-V grep gate (formatDayNumeral in src/lib/dates.ts)
    - src/lib/notion-events.ts (EventItem.date typing: `string | null`)
  </read_first>
  <action>
Append a new exported function `formatDayNumeral(iso: string | null): string` to `src/lib/dates.ts`. The function must:

1. Match the existing `formatMonthYear` / `formatMonthDay` shape (D-17): take `string | null`, return `""` for null input, otherwise return the day-of-month digit as a string.
2. Use `new Date(iso).getUTCDate().toString()` — UTC (not local) because Notion serializes date-only ISO strings as UTC midnight and `getDate()` (local) can shift the day by ±1 across timezones (RESEARCH § Open Questions §3). This matches the UTC posture already implicit in `formatMonthYear` (`toLocaleDateString` is locale-agnostic for short month + year, but UTC-anchored input parsing is the canonical Notion handling).
3. JSDoc block above the function: explain it returns just the day digit for /events Upcoming giant numerals (84px featured / 56px non-featured per D-18 in Plan 11-04), give 2 example inputs ("2026-06-12" → "12" and "2026-06-12T19:00:00Z" → "12") and the null case ("" return), reference D-17.

Do NOT modify the existing `formatMonthYear` or `formatMonthDay` exports. Do NOT add new utility functions beyond `formatDayNumeral` (scope discipline — Plan 11-02 only owns the helper named in D-17).

Per D-30 run `npm run build` after the edit; expect exit 0.
  </action>
  <verify>
    <automated>grep -E "export function formatDayNumeral" src/lib/dates.ts &amp;&amp; grep -E "getUTCDate" src/lib/dates.ts &amp;&amp; grep -E "if \(!iso\) return" src/lib/dates.ts &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `rg "export function formatDayNumeral" src/lib/dates.ts` returns 1 hit (per 11-02-V)
    - `rg "getUTCDate" src/lib/dates.ts` returns 1 hit (UTC-based per RESEARCH § Open Questions §3)
    - `rg "if \(!iso\) return" src/lib/dates.ts` still returns ≥3 hits (existing 2 + new 1 — null guard preserved)
    - `rg "export function formatMonthYear" src/lib/dates.ts` still returns 1 hit (no regression)
    - `rg "export function formatMonthDay" src/lib/dates.ts` still returns 1 hit (no regression)
    - `npm run build` exits 0 (D-30)
  </acceptance_criteria>
  <done>
    `src/lib/dates.ts` exports 3 functions (`formatMonthYear`, `formatMonthDay`, `formatDayNumeral`). Plan 11-04 can `import { formatDayNumeral, formatMonthYear } from "@/lib/dates"` without further edits.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create src/lib/photos.ts with empirical year mapping + groupPhotosByYear</name>
  <files>src/lib/photos.ts</files>
  <read_first>
    - .planning/phases/11-archive-pages/11-CONTEXT.md D-11 REVISED (empirical year mapping via mdls — 3×2023 + 3×2025, NO 2024), D-12 (groupPhotosByYear contract — Map sorted descending), D-24 (Phase 11 ships with these 6 photos; future milestones add more)
    - .planning/phases/11-archive-pages/11-RESEARCH.md §"Photo Year Mapping" (per-file mdls table with exact creation dates)
    - .planning/phases/11-archive-pages/11-RESEARCH.md §"src/lib/photos.ts (NEW; Plan 11-02) — empirical year mapping" (canonical copy-pasteable file content)
    - .planning/phases/11-archive-pages/11-VALIDATION.md row 11-02-V grep gates (PHOTOS_BY_YEAR and groupPhotosByYear must appear)
    - public/MSizzle-website-photos/ directory listing (verify the 6 filenames exactly — case-sensitive, the `.JPG` uppercase on IMG_1075 matters)
  </read_first>
  <action>
Create `src/lib/photos.ts` as a new pure data + helper module:

1. **Top-of-file comment block** explaining (a) the year values are empirical from macOS `kMDItemContentCreationDate` not guesses, (b) Phase 11 hardcodes these 6 photos per D-11 + D-24, (c) future milestones may move this to a Notion DB.

2. **Type export** `ArchivePhoto`:
   ```ts
   export type ArchivePhoto = {
     filename: string;
     year: number;
     alt: string;
     caption?: string;
   };
   ```

3. **Const export** `PHOTOS_BY_YEAR: ArchivePhoto[]` containing exactly 6 entries with these EXACT filenames + years (lifted verbatim from RESEARCH § Photo Year Mapping table — empirically derived, do NOT change the year values):
   - `"000092530012.jpeg"` → year 2025, alt "Film negative — a year in motion", caption "Film, 2025"
   - `"IMG_2129.jpeg"` → year 2025, alt "Personal moment, late 2025", caption "Nov 2025"
   - `"Patricof09.jpg"` → year 2025, alt "Patricof, February 2025", caption "Feb 2025"
   - `"IMG_1075.JPG"` → year 2023, alt "December 2023", caption "Dec 2023"  (note `.JPG` uppercase — case-sensitive on Vercel build per Next.js static assets behavior)
   - `"20230928 MSB_0114.jpg"` → year 2023, alt "September 2023", caption "Sep 2023" (filename contains a SPACE — Plan 11-05 will need to URL-encode `%20` when building the `next/image` src)
   - `"IMG_0028.jpeg"` → year 2023, alt "Summer 2023", caption "Jul 2023"

4. **Helper export** `groupPhotosByYear(): Map<number, ArchivePhoto[]>` — iterates `PHOTOS_BY_YEAR`, builds a Map keyed by year, then returns `new Map([...groups.entries()].sort(([a], [b]) => b - a))` (D-12: sorted by year descending). JSDoc above the function: "Group photos by year, returning a Map sorted year-descending so iteration order matches page render order (newest first)." Cite D-12 in the JSDoc tail.

The module is server-component-safe (pure data; no side effects, no `'use client'`). Do not import anything other than `type { ArchivePhoto }` references — this file has no external dependencies.

Do NOT add filtering / search / paging helpers beyond `groupPhotosByYear` (scope discipline — Plan 11-02 only owns what D-11 + D-12 specify).

Per D-30 run `npm run build` after; expect exit 0.
  </action>
  <verify>
    <automated>test -f src/lib/photos.ts &amp;&amp; grep -E "export const PHOTOS_BY_YEAR" src/lib/photos.ts &amp;&amp; grep -E "export function groupPhotosByYear" src/lib/photos.ts &amp;&amp; grep -E "export type ArchivePhoto" src/lib/photos.ts &amp;&amp; grep -c "year: 2023" src/lib/photos.ts &amp;&amp; grep -c "year: 2025" src/lib/photos.ts &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/lib/photos.ts` succeeds (per 11-02-V)
    - `rg "PHOTOS_BY_YEAR" src/lib/photos.ts` ≥1 hit (per 11-02-V)
    - `rg "groupPhotosByYear" src/lib/photos.ts` ≥1 hit (per 11-02-V)
    - `rg "export type ArchivePhoto" src/lib/photos.ts` returns 1 hit
    - `rg -c "year: 2023" src/lib/photos.ts` returns 3 (D-11 REVISED — 3 photos in 2023)
    - `rg -c "year: 2025" src/lib/photos.ts` returns 3 (D-11 REVISED — 3 photos in 2025)
    - `rg -c "year: 2024" src/lib/photos.ts` returns 0 (D-11 REVISED — NO 2024 photos exist)
    - All 6 filenames appear verbatim (case-sensitive): `000092530012.jpeg`, `20230928 MSB_0114.jpg`, `IMG_0028.jpeg`, `IMG_1075.JPG`, `IMG_2129.jpeg`, `Patricof09.jpg`
    - `rg "use client" src/lib/photos.ts` returns 0 hits (pure data module)
    - `npm run build` exits 0 (D-30)
  </acceptance_criteria>
  <done>
    `src/lib/photos.ts` exports `ArchivePhoto`, `PHOTOS_BY_YEAR` (6 entries: 3×2023 + 3×2025), and `groupPhotosByYear()`. Plan 11-05 can import all three and render two YearBlock groups (2025 newest first, then 2023). Build green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Phase 11 introduces zero new attack surface. `src/lib/photos.ts` is a hardcoded data constant; `src/lib/dates.ts` is a pure date-formatting helper. No user input, no fetch, no env-var reads. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-02-01 | Information disclosure | src/lib/photos.ts (filenames) | accept | Filenames are public assets under /public — no secrets, already reachable via direct URL |
| T-11-02-02 | Denial of service | formatDayNumeral on invalid ISO | accept | `new Date("garbage").getUTCDate()` returns NaN, `NaN.toString()` returns "NaN" — visually unsightly but non-crashing; null guard handles the empty case (RESEARCH § Pitfall 2) |
| T-11-02-SC | Tampering | npm/pip/cargo installs | accept | Zero new package installs in Plan 11-02 (RESEARCH § Standard Stack confirms); slopcheck N/A |

V14 Configuration: covered by `npm run build` exit-0 gate per D-30 + Vercel preview at phase gate per D-31.
</threat_model>

<verification>
- `test -f src/lib/photos.ts` (file exists)
- `grep` gates from 11-02-V: PHOTOS_BY_YEAR + groupPhotosByYear in photos.ts; formatDayNumeral in dates.ts
- All 6 photo filenames present verbatim (case-sensitive)
- Year-distribution counts: exactly 3 × "year: 2023" and 3 × "year: 2025", zero × "year: 2024"
- `npm run build` exit 0
</verification>

<success_criteria>
- `src/lib/photos.ts` exists with `ArchivePhoto`, `PHOTOS_BY_YEAR` (6 entries empirically dated), `groupPhotosByYear()`
- `src/lib/dates.ts` exports `formatDayNumeral` alongside the existing 2 helpers — no regression
- Year distribution exactly matches RESEARCH § Photo Year Mapping (3×2023 + 3×2025)
- No new dependencies added (CLAUDE.md constraint)
- `npm run build` exits 0
</success_criteria>

<output>
Create `.planning/phases/11-archive-pages/11-02-SUMMARY.md` when done. Include: the file paths created/modified, the 6 PHOTOS_BY_YEAR entries with their year assignments, the formatDayNumeral signature, 11-02-V verify command outputs, and a note that Plan 11-04 (events numerals) and Plan 11-05 (photos page) can now import freely.
</output>
