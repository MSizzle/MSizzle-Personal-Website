---
phase: 11-archive-pages
plan: 05
type: execute
wave: 3
depends_on: ['11-03']
files_modified:
  - src/app/photos/page.tsx
autonomous: true
requirements: ['ARCH-03']
must_haves:
  truths:
    - "D-24: /photos year groupings show 2025 (3 photos) + 2023 (3 photos); no 2024 group per RESEARCH F1 empirical mapping"
    - "D-27: scroll-reveal/lenis-provider/template.tsx preserved (Phase 8/9/10 carryforward)"
    - "D-28: /newsletter route untouched (Phase 8 D-13 carryforward)"
    - "D-30: per-plan npm run build exits 0 before commit"
    - "D-31: phase gate vercel build --prod deferred to Vercel preview deploy per Phase 8/9/10 precedent"
    - "D-32: HUMAN-UAT items captured for perceptual verification + Vercel preview"
    - "D-33: 5 plans across 3 waves — Wave 1 [11-01, 11-02] parallel, Wave 2 [11-03] serializes shared-file edits, Wave 3 [11-04, 11-05] parallel"
    - "Visiting /photos renders editorial header + 2-col title block (label '── The Archive · 04' + Photographs. 120px page title + blurb + 360x480 atmosphere photo PHOTOS[1] = 20230928 MSB_0114.jpg) + RuleStrong + year-grouped YearBlock entries from PHOTOS_BY_YEAR — exactly TWO blocks: 2025 (3 photos) and 2023 (3 photos), in descending year order via groupPhotosByYear (D-04, D-11 REVISED, D-22)"
    - "Each YearBlock renders a grid of photo plates with captions BELOW the images (NOT mix-blend-difference overlay) using saturate(0.92) filter to match Phase 10 homepage photographs treatment (D-23)"
    - "The Homepage Photographs section's existing <AllLink href='/photos'> link (src/app/page.tsx line ~298) no longer 404s after this plan ships — clicking it navigates successfully to the new route (D-06)"
    - "The PERSONAL card 'Photo Archive' on the homepage (src/app/page.tsx line 31) also now resolves successfully (D-06)"
    - "v1.0 chrome stays suppressed on /photos per Plan 11-03's gate extension (D-26)"
  artifacts:
    - path: "src/app/photos/page.tsx"
      provides: "/photos route — ARCH-03"
      exports: ["default", "revalidate", "metadata"]
      contains: "Photographs."
      min_lines: 70
  key_links:
    - from: "src/app/photos/page.tsx"
      to: "src/lib/photos.ts -> PHOTOS_BY_YEAR + groupPhotosByYear (Plan 11-02)"
      via: "import"
      pattern: "PHOTOS_BY_YEAR|groupPhotosByYear"
    - from: "src/app/photos/page.tsx"
      to: "src/components/editorial/year-block.tsx (Plan 11-01)"
      via: "import { YearBlock }"
      pattern: "YearBlock"
    - from: "src/app/page.tsx (Photographs section AllLink)"
      to: "src/app/photos/page.tsx"
      via: "/photos navigation"
      pattern: "href=\"/photos\""
---

<objective>
Ship ARCH-03 — the new /photos editorial archive — using the YearBlock pattern from Plan 11-01 and the empirical year mapping from Plan 11-02.

Concretely Plan 11-05 lands:

1. NEW route src/app/photos/page.tsx per D-04 + D-22:
   - Editorial header
   - 2-col title block matching /writing's pattern, using 20230928 MSB_0114.jpg as the 360x480 atmosphere photo (PHOTOS[1] per D-22)
   - Year-grouped photo grid via groupPhotosByYear() — exactly two YearBlock instances rendered (2025 first, 2023 second per D-11 REVISED + D-24)
   - Each YearBlock's children = a responsive grid of next/image plates with captions BELOW (NOT mix-blend overlay per D-23)

After Plan 11-05 ships: the homepage Photographs section's AllLink href="/photos" (currently 404) becomes a live link (D-06 — no code change to homepage needed; the link already exists). PERSONAL card "Photo Archive" also resolves (D-06).

Additionally, per CONTEXT D-33 and the additional_context instruction, this last plan folds in references to D-30 (per-plan build gate) and D-31 (vercel deferral) explicitly in its threat model + verification sections so the phase-close artifacts capture them.

Output: 1 new file. v1.0 chrome already suppressed on /photos by Plan 11-03 (D-26); no shared-file edits in this plan, which is parallel-safe with Plan 11-04 in Wave 3.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-archive-pages/11-CONTEXT.md
@.planning/phases/11-archive-pages/11-RESEARCH.md
@.planning/phases/11-archive-pages/11-VALIDATION.md
@.planning/phases/11-archive-pages/11-01-SUMMARY.md
@.planning/phases/11-archive-pages/11-02-SUMMARY.md
@.planning/phases/11-archive-pages/11-03-SUMMARY.md
@.planning/research/editorial-redesign-handoff/README.md
@CLAUDE.md
@src/app/page.tsx

Phase 9 primitives consumed as-is (D-29): EditorialHeader from src/components/home-v2/editorial-header.tsx (Plan 11-03); RuleStrong (no props); YearBlock from src/components/editorial/year-block.tsx (Plan 11-01 — props { year: string|number; children: ReactNode }).

Photo data module from Plan 11-02 (src/lib/photos.ts): exports ArchivePhoto type with fields filename + year + alt + optional caption; exports PHOTOS_BY_YEAR const ArchivePhoto[]; exports groupPhotosByYear(): Map<number, ArchivePhoto[]> returning entries sorted descending by year. Empirical year mapping: 3 photos in 2025 (000092530012.jpeg, IMG_2129.jpeg, Patricof09.jpg) + 3 photos in 2023 (IMG_1075.JPG, 20230928 MSB_0114.jpg, IMG_0028.jpeg) — NO 2024 photos.

Atmosphere photo for the title block per D-22 is PHOTOS[1] which is `20230928 MSB_0114.jpg`. The filename CONTAINS A SPACE — when building the next/image src attribute the space MUST be URL-encoded as `%20`. Reference: src/app/page.tsx line 23 already uses this pattern: `"/MSizzle-website-photos/20230928%20MSB_0114.jpg"`. Mirror it exactly.

D-23: photo display uses saturate(0.92) filter (matches Phase 10 homepage HOME_PHOTOS treatment) with captions BELOW the images, NOT mix-blend-difference overlay (that overlay is the homepage-only treatment).
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create /photos route — title block + year-grouped photo grid via YearBlock + PHOTOS_BY_YEAR</name>
  <files>src/app/photos/page.tsx</files>
  <read_first>
    - .planning/phases/11-archive-pages/11-CONTEXT.md D-04 (NEW route), D-06 (homepage AllLink + PERSONAL card become live after this plan), D-22 (page structure), D-23 (saturate 0.92 + captions below NOT overlay), D-24 (photo distribution), D-27/D-29/D-30/D-31 (preservation + gates)
    - .planning/phases/11-archive-pages/11-RESEARCH.md section "Pattern 1: Editorial Archive Title Block" (canonical 2-col title block skeleton — reuse with /photos-specific copy + PHOTOS[1])
    - .planning/phases/11-archive-pages/11-RESEARCH.md section "Photo Year Mapping" (empirical table — confirms 3x2025 + 3x2023 distribution)
    - .planning/phases/11-archive-pages/11-RESEARCH.md section "src/lib/photos.ts (NEW; Plan 11-02)" (ArchivePhoto type, PHOTOS_BY_YEAR shape, groupPhotosByYear sort order)
    - .planning/phases/11-archive-pages/11-RESEARCH.md Open Question #1 (photo captions — recommend month-year strings derived from mdls dates, which Plan 11-02 already stored in the `caption` field; this plan just renders them)
    - .planning/phases/11-archive-pages/11-RESEARCH.md Open Question #4 (photo aspect ratio — recommend aspect-square for visual rhythm)
    - .planning/phases/11-archive-pages/11-VALIDATION.md row 11-05-V (Photographs. + PHOTOS_BY_YEAR/groupPhotosByYear import + YearBlock + build exit 0)
    - .planning/research/editorial-redesign-handoff/README.md section "Open Questions / Decisions Deferred" (confirms /photos follows /writing's YearBlock pattern as safe default)
    - src/app/page.tsx lines 21-28 (HOME_PHOTOS pattern — note filename URL-encoding `20230928%20MSB_0114.jpg` and saturate-[0.92] usage to match D-23)
    - src/app/page.tsx lines 296-300 (the existing AllLink href="/photos" that this plan unblocks per D-06)
    - src/components/editorial/year-block.tsx (Plan 11-01 — { year, children } props)
    - src/components/home-v2/editorial-header.tsx (Plan 11-03 — active prop signature)
    - src/lib/photos.ts (Plan 11-02 — ArchivePhoto + PHOTOS_BY_YEAR + groupPhotosByYear)
  </read_first>
  <action>
Create src/app/photos/page.tsx as a default-export Server Component (no async needed since the data source is a synchronous module constant, not a Notion fetch — keep the function `async` for symmetry with /writing + /events if preferred, but `async` is optional here). Required imports:

Image from next/image; Metadata type from next; Fragment from react (to interleave RuleStrong dividers between YearBlocks); ArchivePhoto type + groupPhotosByYear from @/lib/photos; EditorialHeader from @/components/home-v2/editorial-header; RuleStrong from @/components/editorial/rule-strong; YearBlock from @/components/editorial/year-block.

Module-level exports:

- `export const revalidate = 1800;` (30 minutes — matches /, /writing, /events ISR cadence; harmless for a hardcoded data module but keeps the route consistent and ready for the future Notion-driven photo data source deferred in D-11)
- `export const metadata: Metadata = { title: "Photographs | Monty Singer", description: "A film-led survey of years in motion.", alternates: { canonical: "/photos" }, openGraph: { title: "Photographs | Monty Singer", description: "A film-led survey of years in motion.", url: "/photos", type: "website" } };`

`export default function PhotosPage()` (no async required — pure synchronous module data):

1. Compute `const photosByYear = groupPhotosByYear();` then `const yearEntries = [...photosByYear.entries()];` — gives an array of [year, ArchivePhoto[]] pairs sorted year-descending (Plan 11-02 D-12).

2. Return JSX wrapped in a Fragment:

   a. EditorialHeader. Handoff doesn't specify which nav label to bold on /photos (per Assumption A4 in RESEARCH); default to no active prop OR pass active=undefined explicitly to keep all 5 links muted equally. The simpler form is just `<EditorialHeader />`.

   b. Title block — identical skeleton to /writing's title block (RESEARCH section Pattern 1) with /photos-specific content. Outer section className "px-6 pt-40 pb-24 md:px-40 md:pt-[160px] md:pb-[100px]". Inner div grid grid-cols-1 items-end gap-10 md:grid-cols-[1fr_360px] md:gap-20. Left column: a div text-label uppercase text-muted reading "── The Archive · 04"; h1 mt-6 text-page-title uppercase text-ink reading "Photographs." (D-14 trailing period token); p mt-10 max-w-[35rem] text-body-lead text-muted with copy "A film-led survey of years in motion. Stills, fragments, and frames from a year of quiet attention. Updated when something earns the page." Right column: a div hidden md:block (RESEARCH Pitfall 6 — hide on mobile so page title stays above fold), inside a relative h-[480px] w-[360px] overflow-hidden bg-rule-strong div, inside that Image src "/MSizzle-website-photos/20230928%20MSB_0114.jpg" (PHOTOS[1] per D-22 — note %20 URL-encoded space, matching src/app/page.tsx line 23 pattern) alt="" fill sizes="360px" className "object-cover saturate-[0.92]" (D-23 saturate filter).

   c. `<RuleStrong />`

   d. Year-grouped photo grid — wrap in `section className="px-6 md:px-40"` (YearBlock supplies its own md:py-20). Map yearEntries:

      ```
      yearEntries.map(([year, yearPhotos], i, arr) => (
        <Fragment key={year}>
          <YearBlock year={year}>
            <div className="grid grid-cols-1 gap-6 sm:grid-cols-2 lg:grid-cols-3 lg:gap-8">
              {yearPhotos.map((photo) => (
                <figure key={photo.filename}>
                  <div className="relative aspect-square overflow-hidden bg-rule-strong">
                    <Image
                      src={`/MSizzle-website-photos/${encodeURIComponent(photo.filename).replace(/%2F/g, "/")}`}
                      alt={photo.alt}
                      fill
                      sizes="(max-width: 640px) 100vw, (max-width: 1024px) 50vw, 33vw"
                      className="object-cover saturate-[0.92]"
                    />
                  </div>
                  {photo.caption && (
                    <figcaption className="mt-3 text-meta uppercase text-muted">
                      {photo.caption}
                    </figcaption>
                  )}
                </figure>
              ))}
            </div>
          </YearBlock>
          {i < arr.length - 1 && <RuleStrong />}
        </Fragment>
      ))
      ```

      Notes:
      - `encodeURIComponent(photo.filename).replace(/%2F/g, "/")` handles the space in `20230928 MSB_0114.jpg` -> `20230928%20MSB_0114.jpg` without accidentally encoding any `/` characters (there are none in current filenames, but the .replace guards against future filenames that might include subdirectories).
      - aspect-square per RESEARCH Open Question #4 — keeps the year-grouped grid visually rhythmic. The 3-col lg layout means each year (3 photos) lays out as a single horizontal row at lg+, two rows of stacked at sm, single column on mobile.
      - Captions BELOW the image per D-23 (NOT mix-blend overlay — that's homepage-only). The photo.caption field is already populated by Plan 11-02 with empirical month-year strings ("Film, 2025", "Sep 2023", etc.) per RESEARCH Open Question #1.
      - RuleStrong divider between YearBlocks per the design language (and matches the /writing year-block separator pattern recommended in D-13 step 5).

3. Forbidden (scope discipline):
   - Do NOT add 'use client' — Server Component (pure synchronous module data; no hooks, no event handlers)
   - Do NOT add new package dependencies
   - Do NOT edit src/app/page.tsx — Plan 11-03 owns all homepage edits per D-33 + RESEARCH Pitfall 4 (the homepage AllLink href="/photos" already exists and just becomes live after this route ships per D-06)
   - Do NOT add a mix-blend-difference overlay caption (D-23 explicitly drops this — homepage-only treatment)
   - Do NOT touch src/lib/photos.ts (Plan 11-02 owned it), src/components/editorial/year-block.tsx (Plan 11-01), or any Phase 9 primitive (D-29)
   - Do NOT add a Notion data fetch (D-11 explicitly hardcodes; future milestone introduces Notion-driven source)

Per D-30 run `npm run build` after the file is in; expect exit 0. /photos appears in the routes manifest. After build, verify in dev mode that:
- /photos loads (no 404)
- Homepage AllLink href="/photos" navigates to the new route (D-06 unblock)
- Homepage PERSONAL card "Photo Archive" also navigates successfully (D-06)

Per D-31 the phase-gate vercel build --prod runs on Vercel preview deploy when the branch is pushed (Phase 8/9/10 precedent).
  </action>
  <verify>
    <automated>test -f src/app/photos/page.tsx &amp;&amp; grep -E "Photographs\." src/app/photos/page.tsx &amp;&amp; grep -E "PHOTOS_BY_YEAR|groupPhotosByYear" src/app/photos/page.tsx &amp;&amp; grep -E "YearBlock" src/app/photos/page.tsx &amp;&amp; grep -E "saturate" src/app/photos/page.tsx &amp;&amp; ! grep -E "mix-blend-difference" src/app/photos/page.tsx &amp;&amp; ! grep -E "use client" src/app/photos/page.tsx &amp;&amp; grep -E "20230928" src/app/photos/page.tsx &amp;&amp; grep -E "text-page-title" src/app/photos/page.tsx &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - test -f src/app/photos/page.tsx succeeds (per 11-05-V)
    - rg "Photographs\." src/app/photos/page.tsx returns at least 1 hit (per 11-05-V — D-14 trailing period page title)
    - rg "PHOTOS_BY_YEAR|groupPhotosByYear" src/app/photos/page.tsx returns at least 1 hit (per 11-05-V — consumes Plan 11-02 module)
    - rg "YearBlock" src/app/photos/page.tsx returns at least 1 hit (per 11-05-V — consumes Plan 11-01 primitive)
    - rg "saturate" src/app/photos/page.tsx returns at least 1 hit (D-23 — saturate(0.92) filter applied to plates)
    - rg "mix-blend-difference" src/app/photos/page.tsx returns 0 hits (D-23 — NOT homepage overlay treatment)
    - rg "use client" src/app/photos/page.tsx returns 0 hits (Server Component)
    - rg "20230928" src/app/photos/page.tsx returns at least 1 hit (PHOTOS[1] atmosphere photo per D-22)
    - rg "text-page-title" src/app/photos/page.tsx returns at least 1 hit (D-14 token; no arbitrary 120px value)
    - rg "EditorialHeader" src/app/photos/page.tsx returns at least 1 hit (shared chrome per D-25)
    - rg "aspect-square" src/app/photos/page.tsx returns at least 1 hit (RESEARCH Open Question #4 — visual rhythm)
    - rg "encodeURIComponent" src/app/photos/page.tsx returns at least 1 hit (filename with space URL-encoded — matches src/app/page.tsx line 23 pattern)
    - rg "/photos" src/app/page.tsx returns at least 1 hit (the existing AllLink survives — per D-06, no homepage edit needed in this plan)
    - npm run build exits 0 (D-30); routes manifest includes /photos
  </acceptance_criteria>
  <done>
    Visiting /photos (after build + dev/preview) renders the editorial header, 2-col title block with the 20230928 MSB_0114.jpg atmosphere photo on the right (hidden mobile), and exactly two YearBlock groups (2025 first with 3 plates, 2023 below with 3 plates) using aspect-square next/image plates with saturate(0.92) and captions below each plate. Homepage Photographs AllLink to /photos navigates successfully. PERSONAL card "Photo Archive" navigates successfully. v1.0 chrome stays suppressed on /photos. ARCH-03 success criteria satisfied.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | /photos has no network fetches, no user input, no env-var reads. PHOTOS_BY_YEAR is a hardcoded module constant. next/image serves the 6 plates from /public — already publicly reachable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-05-01 | Information disclosure (photo metadata) | PHOTOS_BY_YEAR constants | accept | filenames + alt text + captions are public; photos themselves are already reachable via direct URLs under /public |
| T-11-05-02 | Tampering (filename injection via next/image src) | Image src=`/MSizzle-website-photos/${encodeURIComponent(photo.filename)}` | mitigate | photo.filename comes from src/lib/photos.ts compile-time constant; encodeURIComponent guards future filenames; no user input |
| T-11-05-03 | Denial of service (broken image asset) | next/image with missing source file | accept | next/image renders the alt text + a layout-preserved placeholder when source is missing; visual issue, not a crash |
| T-11-05-SC | Tampering | npm/pip/cargo installs | accept | Zero new package installs in Plan 11-05 (RESEARCH section Standard Stack); slopcheck N/A |

V14 Configuration:
- D-30: per-task npm run build exit-0 enforced in this plan's verify block
- D-31: phase-gate vercel build --prod runs on Vercel preview deploy when branch is pushed (Phase 8/9/10 precedent; deferred to operator post-merge per D-31)
</threat_model>

<verification>
- File creation: test -f src/app/photos/page.tsx
- 11-05-V grep gates: Photographs. + PHOTOS_BY_YEAR|groupPhotosByYear + YearBlock + npm run build exit 0
- Token + design: saturate + text-page-title + aspect-square + EditorialHeader
- Negative gates: no mix-blend-difference, no use client
- Atmosphere photo: rg "20230928" >= 1 hit (PHOTOS[1] per D-22)
- Homepage parity: rg "/photos" src/app/page.tsx still returns >= 1 hit (existing AllLink unchanged per D-06)
- Build: npm run build exits 0 with /photos in routes manifest
</verification>

<success_criteria>
- /photos route exists, renders the editorial archive per handoff section "Open Questions"
- Year groupings: exactly 2 YearBlock instances (2025 newest first, 2023 below — D-11 REVISED + D-24)
- Photos use saturate(0.92) filter with captions BELOW (D-23 — NOT mix-blend overlay)
- Homepage AllLink href="/photos" navigates successfully (D-06 unblocked)
- PERSONAL card "Photo Archive" navigates successfully (D-06 unblocked)
- v1.0 chrome stays suppressed on /photos (Plan 11-03 owned this)
- ARCH-03 success criteria from ROADMAP all met
- D-30 per-plan build gate: npm run build exits 0
- D-31 phase gate deferred to Vercel preview (operator action post-merge — Phase 8/9/10 precedent)
</success_criteria>

<output>
Create .planning/phases/11-archive-pages/11-05-SUMMARY.md when done. Include: the new file path, the 2 YearBlock entries rendered (2025: 3 plates, 2023: 3 plates with the exact filenames from PHOTOS_BY_YEAR), the atmosphere photo + caption + saturate treatment notes, 11-05-V verify command outputs, dev-mode navigation confirmation that the homepage AllLink + PERSONAL Photo Archive card both now resolve (D-06 unblock), and a flag for the operator that D-31 vercel build --prod runs on next Vercel preview deploy.
</output>
