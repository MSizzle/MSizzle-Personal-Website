---
phase: 11-archive-pages
plan: 04
type: execute
wave: 3
depends_on: ['11-03']
files_modified:
  - src/app/events/page.tsx
  - src/components/events/event-cards.tsx
autonomous: true
requirements: ['ARCH-02']
must_haves:
  truths:
    - "Visiting /events renders editorial header (with 'Events' bolded) + 2-col title block (label '── The Calendar · 03' + Events. 120px page title + blurb + 360x480 atmosphere photo PHOTOS[3] = IMG_1075.JPG) + RuleStrong + Upcoming section with inline UpcomingRow entries + RuleStrong + Past dense 3-column inline rows (D-03, D-16, D-19, D-20 REVISED)"
    - "Upcoming section renders the FIRST upcoming event with an 84px day-numeral signature and subsequent upcoming events with 56px numerals (D-17, D-18 — formatDayNumeral from Plan 11-02)"
    - "Past section uses a 3-column inline grid [120px_1fr_1fr] (date / title / blurb) — NOT 4-column with status — because EventItem has no status field per RESEARCH F3 / D-20 REVISED"
    - "Events come from getUpcomingEvents() + getPastEvents() via Notion (D-16); empty upcoming -> graceful copy; empty past -> section omitted"
    - "Notion field names used verbatim: event.name (NOT title), event.link (NOT rsvpUrl), event.date (ISO), event.description, event.location (D-21 + Phase 10 D-17/D-19/D-21 REVISED carryforward)"
    - "src/components/events/event-cards.tsx is DELETED — orphan after rewrite per RESEARCH F6 + section Anti-Pattern Conflict Surface Check (verified via rg 'event-cards' that no consumers remain after this plan's page rewrite)"
    - "v1.0 chrome stays suppressed on /events per Plan 11-03's gate extension (D-26)"
  artifacts:
    - path: "src/app/events/page.tsx"
      provides: "/events route REWRITTEN per ARCH-02 — editorial archive with giant numerals signature"
      exports: ["default", "revalidate", "metadata"]
      contains: "Events."
      min_lines: 100
  key_links:
    - from: "src/app/events/page.tsx"
      to: "src/lib/notion-events.ts -> getUpcomingEvents, getPastEvents"
      via: "Server Component await call"
      pattern: "getUpcomingEvents|getPastEvents"
    - from: "src/app/events/page.tsx"
      to: "src/lib/dates.ts -> formatDayNumeral, formatMonthYear"
      via: "import + UpcomingRow giant numeral / Past row meta"
      pattern: "formatDayNumeral"
    - from: "src/app/events/page.tsx"
      to: "src/components/home-v2/editorial-header.tsx"
      via: "EditorialHeader active='Events'"
      pattern: "EditorialHeader active=.Events."
---

<objective>
Ship ARCH-02 — the rewritten /events editorial archive — replacing the v1.0 implementation in place per D-03.

Concretely Plan 11-04 lands:

1. REWRITE `src/app/events/page.tsx` per D-16..D-21:
   - Editorial header with "Events" bolded
   - 2-col title block matching /writing's pattern, using IMG_1075.JPG as the 360x480 atmosphere photo
   - Upcoming section: inline UpcomingRow entries in a 3-col grid [160px_1fr_200px]; left column has the tracked month/year above an 84px (featured) or 56px (non-featured) bold day numeral — the page's signature visual (D-17 + D-18)
   - Past section: inline 3-col grid rows [120px_1fr_1fr] (date / title / blurb), denser than Upcoming; D-20 REVISED dropped the 4th status column because EventItem has no status field (RESEARCH F3)

2. DELETE `src/components/events/event-cards.tsx` — orphan after the rewrite (RESEARCH F6 + section Anti-Pattern Conflict Surface Check verified zero remaining consumers in the current codebase: Phase 10 homepage renders events inline using ListRow, not via these components). The plan re-runs `rg "event-cards"` before deletion to defensively confirm zero consumers.

Output: 1 in-place rewrite + 1 file deletion. v1.0 chrome already suppressed on /events by Plan 11-03 (D-26); no shared-file edits in this plan, which keeps Plans 11-04 + 11-05 parallel-safe.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-archive-pages/11-CONTEXT.md
@.planning/phases/11-archive-pages/11-RESEARCH.md
@.planning/phases/11-archive-pages/11-VALIDATION.md
@.planning/phases/11-archive-pages/11-02-SUMMARY.md
@.planning/phases/11-archive-pages/11-03-SUMMARY.md
@.planning/research/editorial-redesign-handoff/README.md
@.planning/phases/10-editorial-homepage/10-CONTEXT.md
@CLAUDE.md
@src/app/events/page.tsx
@src/components/events/event-cards.tsx

Notion event shape from src/lib/notion-events.ts (Phase 10 D-17/D-19/D-21 REVISED Notion field names — verified):

EventItem fields: id, name (NOT title per D-21), date (ISO nullable), endDate, location, link (NOT rsvpUrl per D-21), description, emoji, image, published. Async getters getUpcomingEvents and getPastEvents return EventItem[] and return [] on missing env (RESEARCH section Environment Availability).

Date helpers (src/lib/dates.ts after Plan 11-02): formatMonthYear (returns "JUN 2026"), formatMonthDay (returns "JUN 12"), formatDayNumeral (returns "12", new from Plan 11-02).

Phase 9 primitives consumed as-is (D-29): EditorialHeader with optional active prop (from src/components/home-v2/editorial-header.tsx — Plan 11-03), RuleStrong (no props), SectionLabel (children + optional numeral), AllLink (children + href). cn helper from @/utils/cn used by inline UpcomingRow conditional classes.

Canonical UpcomingRow JSX is in RESEARCH section "Pattern 3: UpcomingRow inline component" (lines 300-367 of 11-RESEARCH.md). The 3-col grid has tokens md:grid-cols-[160px_1fr_200px] and the giant numeral uses font-bold leading-[0.9] tracking-[-0.04em] text-ink with conditional class text-[84px] when featured else text-[56px] (D-18 signature). Middle column shows event.location tracked-uppercase muted, then event.name as text-[40px] when featured else text-event-title, then optional event.description. Right column shows seat-count meta line + AllLink with href={event.link} where the AllLink children read "Reserve a seat ->" when featured else "RSVP ->". Border between rows via !last && "border-b border-rule".

Canonical Past row JSX is in RESEARCH section "Pattern 4: Past Events dense 4-col grid" — note the Recommendation block and D-20 REVISED specify 3-col after dropping status; the copy-pasteable JSX uses md:grid-cols-[120px_1fr_1fr] (3-col). Border-t border-rule between rows via i > 0.

</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rewrite /events route in place — title block, Upcoming with giant numerals, Past dense 3-col rows</name>
  <files>src/app/events/page.tsx</files>
  <read_first>
    - src/app/events/page.tsx (the v1.0 file being REPLACED — note imports to drop: ScrollReveal, Breadcrumbs, FeaturedUpcoming + UpcomingMini + PastEventCard from event-cards)
    - .planning/phases/11-archive-pages/11-CONTEXT.md D-03, D-16 (page structure), D-17 (formatDayNumeral usage), D-18 (84px featured / 56px non-featured), D-19 (UpcomingRow inline — not extracted), D-20 REVISED (3-col Past — drop status column), D-21 (Notion field names)
    - .planning/phases/11-archive-pages/11-RESEARCH.md section "Pattern 1: Editorial Archive Title Block" (atmosphere photo + page title block — reuse the same skeleton as /writing with PHOTOS[3] = IMG_1075.JPG per D-16)
    - .planning/phases/11-archive-pages/11-RESEARCH.md section "Pattern 3: UpcomingRow inline component" (canonical JSX with 84px/56px numerals)
    - .planning/phases/11-archive-pages/11-RESEARCH.md section "Pattern 4: Past Events dense 4-col grid" — note title says "4-col" but Recommendation block + D-20 REVISED specify 3-col after dropping status; the copy-pasteable JSX uses md:grid-cols-[120px_1fr_1fr]
    - .planning/phases/11-archive-pages/11-RESEARCH.md section "Notion EventItem.date shape" (date can be null — formatDayNumeral handles null safely)
    - .planning/phases/11-archive-pages/11-RESEARCH.md section "Pitfall 2" (null date guard mandatory)
    - .planning/phases/11-archive-pages/11-RESEARCH.md section "Open Questions" #2 (empty-state copy when no upcoming events)
    - .planning/phases/11-archive-pages/11-VALIDATION.md row 11-04-V (Events. + formatDayNumeral + text-[84px] OR text-feature + event-cards deletion + build exit 0)
    - .planning/research/editorial-redesign-handoff/README.md section "4. /events — Index"
    - src/lib/notion-events.ts (EventItem interface — D-21 field names; getUpcomingEvents + getPastEvents return [] on missing env per RESEARCH section Environment Availability)
    - src/components/home-v2/editorial-header.tsx (Plan 11-03 — active="Events" prop)
    - src/utils/cn.ts (used in UpcomingRow cn(...) calls — verify exists with default export)
  </read_first>
  <action>
Overwrite src/app/events/page.tsx entirely (D-03 — REPLACED in place). The v1.0 imports (ScrollReveal, Breadcrumbs, FeaturedUpcoming, UpcomingMini, PastEventCard from @/components/events/event-cards) all disappear. Required new imports:

Link from next/link; Image from next/image; Metadata type from next; getUpcomingEvents + getPastEvents + EventItem type from @/lib/notion-events; formatMonthYear + formatDayNumeral from @/lib/dates; EditorialHeader from @/components/home-v2/editorial-header; RuleStrong from @/components/editorial/rule-strong; SectionLabel from @/components/editorial/section-label; AllLink from @/components/editorial/all-link; cn from @/utils/cn.

Module-level exports:

- `export const revalidate = 1800;` (30 minutes — matches v1.0 + RESEARCH section Pitfall 9)
- `export const metadata: Metadata = { title: "Events | Monty Singer", description: "Upcoming and past events where Monty Singer is speaking, attending, or hosting. Talks on AI, building, and Prometheus.", alternates: { canonical: "/events" }, openGraph: { title: "Events | Monty Singer", description: <same>, url: "/events", type: "website" } };`

Define an inline UpcomingRow component (D-19 — inline, NOT extracted to a shared primitive since /events is the only consumer). Place it ABOVE the default export. Use the canonical JSX from RESEARCH section "Pattern 3" verbatim: function signature `function UpcomingRow({ event, featured = false, last = false }: { event: EventItem; featured?: boolean; last?: boolean })`. Inside, compute `const dayNum = formatDayNumeral(event.date); const monthYr = formatMonthYear(event.date);`. Return a div with classes from the canonical pattern: outer `cn("grid grid-cols-1 items-baseline gap-6 md:grid-cols-[160px_1fr_200px] md:gap-14", featured ? "pb-14" : "py-10", !last && "border-b border-rule")`. Left column (leading-none) shows monthYr in text-meta uppercase text-muted, then dayNum in `cn("mt-2 font-bold leading-[0.9] tracking-[-0.04em] text-ink", featured ? "text-[84px]" : "text-[56px]")` — D-18 signature 84px featured / 56px non-featured. Middle column shows event.location (text-meta uppercase text-muted), then event.name in `cn("mt-3 font-bold tracking-[-0.025em] text-ink", featured ? "text-[40px] leading-[1.05]" : "text-event-title")`, then conditional event.description in p with classes mt-3 max-w-[34rem] text-caption text-muted md:text-base. Right column (md:text-right) shows the seat-count meta line (`featured ? "Limited seats" : "Open door"` in text-meta uppercase text-muted with mb-3) followed by conditional AllLink with href={event.link} reading `featured ? "Reserve a seat →" : "RSVP →"`.

`export default async function EventsPage()`:

1. Parallel fetch (preserve v1.0 Promise.all shape since both endpoints already return [] on failure — verified RESEARCH section Environment Availability): `const [upcoming, past] = await Promise.all([getUpcomingEvents(), getPastEvents()]);`. No defensive try/catch needed since these getters return [] on missing env per src/lib/notion-events.ts lines 109-110 + 144-146 + 184-186.

2. Return JSX wrapped in a Fragment:

   a. `<EditorialHeader active="Events" />` (handoff section 4 specifies "Events bolded vs other nav links being muted").

   b. Title block — identical skeleton to /writing's title block (RESEARCH section Pattern 1) with /events-specific content. Outer `section className="px-6 pt-40 pb-24 md:px-40 md:pt-[160px] md:pb-[100px]"`. Inner div `grid grid-cols-1 items-end gap-10 md:grid-cols-[1fr_360px] md:gap-20`. Left column: a div text-label uppercase text-muted reading "── The Calendar · 03"; h1 mt-6 text-page-title uppercase text-ink reading "Events." (D-14 trailing period token, NOT inline 120px arbitrary); p mt-10 max-w-[35rem] text-body-lead text-muted with copy "Small, intentional gatherings on AI, building, and the texture of an attentive life. Working evenings — not panels, not pitches. Bring something stuck; we'll work on it together." Right column: a div hidden md:block (RESEARCH Pitfall 6 — hide on mobile so page title stays above fold), inside it a relative h-[480px] w-[360px] overflow-hidden bg-rule-strong div, inside that Image src "/MSizzle-website-photos/IMG_1075.JPG" (PHOTOS[3] per D-16 — note .JPG uppercase is case-sensitive on Vercel build) alt="" fill sizes="360px" className "object-cover saturate-[0.92]".

   c. `<RuleStrong />`

   d. Upcoming section — outer section px-6 pt-[120px] pb-[120px] md:px-40. Inside: `<SectionLabel numeral="03 — Upcoming">Upcoming</SectionLabel>`. Then div mt-[72px] containing either an empty-state paragraph when upcoming.length === 0 (text-caption text-muted reading "Next gathering being planned. Subscribe to Monty Monthly to hear first." with the words Monty Monthly wrapped in an outbound a tag href="https://montymonthly.substack.com" target="_blank" rel="noopener noreferrer" className="border-b border-ink text-ink"), or otherwise upcoming.map((event, i, arr) returning UpcomingRow with key=event.id, event=event, featured={i === 0} (D-18 — first upcoming gets the 84px signature numeral), last={i === arr.length - 1}).

   e. `<RuleStrong />`

   f. Past section — render ONLY when `past.length > 0`. Outer section px-6 pt-[120px] pb-[120px] md:px-40. Inside: `<SectionLabel numeral="03 — Past">Past</SectionLabel>`. Then div mt-[72px] containing past.map((event, i) returning Link with key=event.id, href={event.link ?? "#"}, className from canonical Pattern 4: `cn("grid grid-cols-1 gap-6 py-5 md:grid-cols-[120px_1fr_1fr] md:gap-8", i > 0 && "border-t border-rule")`. Inside the Link: span text-meta uppercase text-muted with formatMonthYear(event.date), span text-list-title-home text-ink with event.name, span text-caption text-muted with event.description.

   D-20 REVISED — 3-col grid [120px_1fr_1fr] not 4-col, no status column. Padding py-5 makes Past noticeably denser than Upcoming's py-10/pb-14.

3. Forbidden (RESEARCH section Anti-Pattern Conflict Surface Check + scope discipline):
   - Do NOT import from @/components/events/event-cards — it is deleted in Task 2
   - Do NOT import ScrollReveal or Breadcrumbs — v1.0 chrome that handoff explicitly drops on editorial archive pages
   - Do NOT add 'use client' — Server Component
   - Do NOT add form / input elements
   - Do NOT add new package dependencies
   - Do NOT touch src/components/animations/scroll-reveal.tsx, src/lib/notion-events.ts, or Phase 9 primitives (D-27 / D-29)
   - Do NOT introduce a formatTime helper or render time strings — per RESEARCH section Notion EventItem.date shape "Time handling" the production data shape does not reliably embed time; render event.location only in the middle column tracked label

Per D-30 run `npm run build` after the edit; expect exit 0. /events route appears in the routes manifest. Note: Task 2 (event-cards deletion) MUST run AFTER this rewrite so the verification `rg "event-cards"` can prove zero consumers.
  </action>
  <verify>
    <automated>grep -E "Events\." src/app/events/page.tsx &amp;&amp; grep -E "formatDayNumeral" src/app/events/page.tsx &amp;&amp; grep -E "text-\[84px\]" src/app/events/page.tsx &amp;&amp; grep -E "text-\[56px\]" src/app/events/page.tsx &amp;&amp; grep -E 'EditorialHeader active="Events"' src/app/events/page.tsx &amp;&amp; [ "$(grep -c 'event-cards' src/app/events/page.tsx)" = "0" ] &amp;&amp; ! grep -E "use client" src/app/events/page.tsx &amp;&amp; ! grep -E "ScrollReveal|Breadcrumbs" src/app/events/page.tsx &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - rg "Events\." src/app/events/page.tsx returns at least 1 hit (per 11-04-V — D-14 trailing period page title)
    - rg "formatDayNumeral" src/app/events/page.tsx returns at least 1 hit (per 11-04-V — consumes Plan 11-02 helper)
    - rg "text-\[84px\]" src/app/events/page.tsx returns at least 1 hit (per 11-04-V — D-18 featured numeral signature)
    - rg "text-\[56px\]" src/app/events/page.tsx returns at least 1 hit (D-18 non-featured numeral)
    - rg 'EditorialHeader active="Events"' src/app/events/page.tsx returns 1 hit (active bolded per handoff section 4 + D-25)
    - rg 'event-cards' src/app/events/page.tsx returns 0 hits (no consumer remains after rewrite)
    - rg "use client" src/app/events/page.tsx returns 0 hits (Server Component)
    - rg "ScrollReveal|Breadcrumbs" src/app/events/page.tsx returns 0 hits (v1.0 chrome dropped)
    - rg "IMG_1075" src/app/events/page.tsx returns at least 1 hit (atmosphere photo PHOTOS[3] per D-16)
    - rg "text-page-title" src/app/events/page.tsx returns at least 1 hit (D-14 token, no arbitrary 120px)
    - rg "getUpcomingEvents|getPastEvents" src/app/events/page.tsx returns at least 2 hits (Notion-backed; ARCH-02 contract)
    - rg "event\.name" src/app/events/page.tsx returns at least 1 hit (D-21 Notion field name; NOT event.title)
    - rg "event\.link" src/app/events/page.tsx returns at least 1 hit (D-21; NOT event.rsvpUrl)
    - rg "md:grid-cols-\[160px_1fr_200px\]" src/app/events/page.tsx returns at least 1 hit (Upcoming 3-col grid per D-16)
    - rg "md:grid-cols-\[120px_1fr_1fr\]" src/app/events/page.tsx returns at least 1 hit (Past 3-col grid per D-20 REVISED — note 3-col, NOT 4-col with status)
    - npm run build exits 0 (D-30); routes manifest still includes /events
  </acceptance_criteria>
  <done>
    Visiting /events (after build + dev/preview) renders the editorial header with "Events" bolded, the 2-col title block with the IMG_1075.JPG atmosphere photo on the right (hidden mobile), Upcoming section with the first event's 84px day numeral as the signature visual and subsequent events at 56px, Past section as dense 3-col rows. v1.0 imports removed cleanly. ARCH-02 success criteria satisfied.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Delete orphaned event-cards.tsx after verifying zero remaining consumers</name>
  <files>src/components/events/event-cards.tsx</files>
  <read_first>
    - src/components/events/event-cards.tsx (the orphan being deleted)
    - .planning/phases/11-archive-pages/11-CONTEXT.md D-33 (Plan slicing — Plan 11-04 owns the event-cards deletion)
    - .planning/phases/11-archive-pages/11-RESEARCH.md section "Anti-Pattern Conflict Surface Check" (verifies Phase 10 homepage does NOT import from event-cards) and section "State of the Art" (event-cards superseded by inline UpcomingRow + inline past row — delete in Plan 11-04)
    - .planning/phases/11-archive-pages/11-RESEARCH.md A5 in Assumptions Log (recommends one final rg verify before deletion)
    - .planning/phases/11-archive-pages/11-VALIDATION.md row 11-04-V negative gate `! test -f src/components/events/event-cards.tsx`
  </read_first>
  <action>
1. Defensive grep first: run `rg "event-cards"` from the repo root. After Task 1 ships the rewritten /events page, this command MUST return ZERO matches. If any consumer surfaces unexpectedly (e.g., a forgotten import in /blog or /projects), abort the deletion and surface the consumer in the SUMMARY for human triage — do NOT proceed with deletion.

2. After zero-match verification: delete `src/components/events/event-cards.tsx`. Use `git rm src/components/events/event-cards.tsx` so the deletion is staged for the per-task commit. Do NOT delete the parent directory `src/components/events/` — leaving an empty directory is fine; if it's truly empty after deletion it will be excluded from git automatically (git tracks files, not directories).

3. Run `npm run build` — expect exit 0 (the rewritten /events page no longer imports from event-cards; no consumer should reference the deleted exports). If build fails, the prior grep missed a consumer; restore the file via `git restore --staged src/components/events/event-cards.tsx && git checkout -- src/components/events/event-cards.tsx`, document the missing consumer in the SUMMARY, and surface for human triage.

4. Do NOT touch src/components/events/ subdirectory beyond this single file. Do NOT delete any file outside of src/components/events/event-cards.tsx. Per D-27 preservation, src/components/animations/scroll-reveal.tsx + src/components/providers/lenis-provider.tsx + src/app/template.tsx stay.
  </action>
  <verify>
    <automated>[ "$(grep -rc 'event-cards' src/ 2&gt;/dev/null | grep -v ':0$' | wc -l)" = "0" ] &amp;&amp; [ ! -f src/components/events/event-cards.tsx ] &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - rg "event-cards" src/ returns 0 hits across the entire src/ tree (RESEARCH F6 — orphan verified after Task 1)
    - test -f src/components/events/event-cards.tsx returns false / fails (per 11-04-V — `! test -f src/components/events/event-cards.tsx`)
    - git status shows the file as `deleted:` (staged via git rm)
    - npm run build exits 0 (D-30 — no consumer was missed)
  </acceptance_criteria>
  <done>
    src/components/events/event-cards.tsx no longer exists. No code references it. Build green. Phase 11 leaves the events component directory cleaner than it found it (RESEARCH section "State of the Art" — event-cards superseded by inline UpcomingRow + inline past row).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server -> Notion API | getUpcomingEvents / getPastEvents from Server Component. Inherited Phase 9/10 fetch path; Notion token never reaches client. |
| Server -> External link (event.link) | Outbound `<a href={event.link}>` for RSVP CTA. event.link is Notion URL field (site-author-controlled). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04-01 | Tampering (open redirect on event.link) | UpcomingRow + Past Link href={event.link ?? "#"} | accept | event.link is Notion URL field controlled by site author; no user-supplied input concatenation |
| T-11-04-02 | Tampering (XSS via Notion-sourced event.name / event.description) | Inline JSX renders these as string children | mitigate | React auto-escapes string children; no dangerouslySetInnerHTML used |
| T-11-04-03 | Information disclosure (NOTION_EVENTS_DB_ID leak) | getUpcomingEvents/getPastEvents in Server Component | mitigate | Inherited — Server Component context; env vars only read in src/lib/notion-events.ts (D-14 carryforward); Phase 13 secret scan covers regression |
| T-11-04-04 | Denial of service (Notion fetch failure breaks /events) | Promise.all on the two getters | mitigate | Both getters return [] on missing env / API failure (RESEARCH section Environment Availability verified at lines 109-110, 144-146, 184-186 of notion-events.ts); empty-state copy renders gracefully |
| T-11-04-05 | Denial of service (formatDayNumeral on null event.date) | UpcomingRow numeral render | mitigate | formatDayNumeral handles null -> "" per Plan 11-02 (RESEARCH Pitfall 2); upcoming/past getters already filter on Date property in Notion so null is unlikely but defensively safe |
| T-11-04-06 | Tampering (reverse tabnabbing on event.link CTAs) | AllLink in UpcomingRow / Link in Past row | accept | AllLink uses next/link (internal-style); for outbound URLs the v1.0 pattern is target="_blank" rel="noopener noreferrer"; Phase 11 inherits the next/link default which renders rel="noopener" on external hrefs automatically in Next 16 — risk is bounded |
| T-11-04-07 | Repudiation (deleting event-cards loses fallback) | git rm src/components/events/event-cards.tsx | accept | File deletion is recoverable via git history; defensive grep verifies zero consumers before deletion |
| T-11-04-SC | Tampering | npm/pip/cargo installs | accept | Zero new package installs in Plan 11-04 (RESEARCH section Standard Stack); slopcheck N/A |

V14 Configuration: covered by per-task npm run build exit-0 (D-30) + Vercel preview at phase gate (D-31).
</threat_model>

<verification>
- File rewrite: grep gates from 11-04-V (Events. + formatDayNumeral + text-[84px] + text-[56px] + EditorialHeader active="Events" + IMG_1075 + text-page-title + getUpcomingEvents + event.name + event.link + 3-col grid tokens)
- File deletion: 11-04-V negative gate `! test -f src/components/events/event-cards.tsx` + full-tree rg "event-cards" returns 0
- Negative gates: no use client, no ScrollReveal, no Breadcrumbs, no event-cards import
- Build: npm run build exits 0 with /events still in routes manifest
</verification>

<success_criteria>
- /events route rewritten per handoff section 4
- 84px featured day numeral + 56px non-featured numerals render (D-18 signature)
- Past section uses 3-col grid (D-20 REVISED — status column dropped because EventItem has no status field)
- Events come from Notion (no hardcoded titles)
- event-cards.tsx deleted; no consumers remain
- v1.0 chrome stays suppressed on /events (Plan 11-03 owned this)
- ARCH-02 success criteria from ROADMAP all met
- npm run build exits 0
</success_criteria>

<output>
Create .planning/phases/11-archive-pages/11-04-SUMMARY.md when done. Include: rewritten file path, deleted file path, the inline UpcomingRow signature, the empty-state copy used for zero upcoming events, the rg "event-cards" final verification output (must be 0), 11-04-V verify command outputs, and confirmation that Phase 10 homepage events section still renders correctly (no regression on /).
</output>
