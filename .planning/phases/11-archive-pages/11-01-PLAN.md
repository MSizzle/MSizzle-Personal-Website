---
phase: 11-archive-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/editorial/year-block.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "A new server-component primitive `<YearBlock>` exists at src/components/editorial/year-block.tsx (D-07)"
    - "On md+ viewports the year label sticks to top-9 while children scroll past it (D-09, D-10 REVISED)"
    - "On viewports below md the year label renders as a normal non-sticky heading above the entries (D-09)"
    - "The sticky year label includes `md:self-start` so a grid child cannot stretch and break sticky (D-10 REVISED)"
    - "Plan 11-03 / 11-05 can import `<YearBlock>` with `{ year, children }` props and compose ListRow / Image children freely (D-08, D-29)"
  artifacts:
    - path: "src/components/editorial/year-block.tsx"
      provides: "<YearBlock> primitive — year-grouped editorial section block, sticky desktop / stacked mobile"
      contains: "export function YearBlock"
      min_lines: 25
  key_links:
    - from: "src/components/editorial/year-block.tsx"
      to: "Tailwind v4 @theme tokens (text-label, text-ink) in src/app/globals.css"
      via: "class names"
      pattern: "text-label|text-ink"
    - from: "src/components/editorial/year-block.tsx"
      to: "CSS position: sticky"
      via: "md:sticky md:top-9 md:self-start"
      pattern: "md:sticky.*md:top-9.*md:self-start"
---

<objective>
Create the new `<YearBlock>` editorial primitive shared across `/writing` (Plan 11-03) and `/photos` (Plan 11-05). This is the only NEW Phase 9-style primitive Phase 11 adds (D-29 — the other 7 are reused as-is). Layout: 2-column grid `[180px | 1fr]` at md+; year label on the left sticks to `top-9` so it stays visible while the user scrolls through that year's entries. Mobile collapses to a single column with the year label as a normal heading above the entries.

Purpose: every archive route (writing + photos) needs the same "year on the left, content on the right" rhythm. Extracting it as a shared primitive prevents two near-identical inline copies drifting later.

Output: a single new file, `src/components/editorial/year-block.tsx`, exporting a server-component-safe `YearBlock` function. No client code, no `'use client'` directive.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/11-archive-pages/11-CONTEXT.md
@.planning/phases/11-archive-pages/11-RESEARCH.md
@.planning/phases/11-archive-pages/11-VALIDATION.md
@.planning/research/editorial-redesign-handoff/README.md
@CLAUDE.md

<interfaces>
<!-- Phase 9 primitive prop signature pattern this primitive matches. -->
<!-- Reference: src/components/editorial/section-label.tsx — same minimal Props type / named export pattern. -->

From `src/components/editorial/section-label.tsx`:
```tsx
import type { ReactNode } from "react";

type Props = {
  children: ReactNode;
  numeral?: string;
};

export function SectionLabel({ children, numeral }: Props) { /* ... */ }
```

`<YearBlock>` follows the same shape: server component, named export, `Props` type literal, JSDoc on the export.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create <YearBlock> primitive with sticky-left-year layout</name>
  <files>src/components/editorial/year-block.tsx</files>
  <read_first>
    - .planning/phases/11-archive-pages/11-CONTEXT.md (D-07 directory location, D-08 props contract, D-09 layout spec, D-10 REVISED md:self-start requirement)
    - .planning/phases/11-archive-pages/11-RESEARCH.md §"Pattern 2: <YearBlock> primitive" (canonical copy-pasteable example with critical-detail callout about md:self-start)
    - .planning/phases/11-archive-pages/11-RESEARCH.md §"CSS Sticky + Lenis Compatibility" (confirms native sticky cooperates with Lenis from Phase 8 D-12)
    - .planning/phases/11-archive-pages/11-VALIDATION.md row 11-01-V (grep gates that prove sticky and self-start tokens exist)
    - .planning/research/editorial-redesign-handoff/README.md §"3. /writing — Index" YearBlock reference
    - src/components/editorial/section-label.tsx (Phase 9 primitive — sibling prop-signature pattern this primitive matches)
    - src/components/editorial/rule.tsx (Phase 9 primitive — same minimal-export pattern)
  </read_first>
  <action>
Create `src/components/editorial/year-block.tsx` as a server-component primitive. The file must:

1. Import `type { ReactNode } from "react"` only — no client-side hooks, no `'use client'`.
2. Declare a `Props` type literal: `{ year: string | number; children: ReactNode }` per D-08. No optional props beyond these — the inline-then-extract convention from Phase 9 (e.g., section-label.tsx) keeps the surface area minimal; later callers wrap their own dividers.
3. Export a named function `YearBlock({ year, children }: Props)` (matches Phase 9 export convention — see section-label.tsx, all-link.tsx, list-row.tsx).
4. Render a `<section>` wrapper with editorial padding `px-6 py-12 md:px-40 md:py-20` (matches the section rhythm in `src/app/page.tsx` BUILDING / WRITING sections — `md:px-40` is the 160px outer padding from the handoff Design Tokens § Spacing).
5. Inside, render a `<div>` with classes `grid grid-cols-1 gap-6 md:grid-cols-[180px_1fr] md:gap-20` — single column on mobile, 180px year track + content column at md+ (D-09).
6. First child (year label): `<div className="text-label uppercase font-bold text-ink md:sticky md:top-9 md:self-start">{year}</div>` — uses the Phase 9 `text-label` token (D-29), uppercase + bold, and applies `md:sticky md:top-9 md:self-start` per D-10 REVISED. The `md:self-start` token is CRITICAL — without it the grid child stretches to row height and `position: sticky` becomes a no-op (RESEARCH § Pitfall 1).
7. Second child: `<div>{children}</div>` — caller-owned content (ListRow rows on /writing; Image plates grid on /photos).
8. Add a JSDoc block above the export describing the contract: 2-col grid at md+, sticky year label at top-9, mobile collapses, callers render their own dividers between blocks. Reference D-07/D-08/D-09/D-10 by number in the JSDoc tail so future maintainers can trace decisions.

Do NOT add a `'use client'` directive — this is a pure presentational primitive. Do NOT import `cn` from `@/utils/cn` — the class list is fully static, no conditional toggles.

Per D-27 do not touch `src/components/animations/scroll-reveal.tsx`, `src/components/providers/lenis-provider.tsx`, `src/app/template.tsx`. Per D-29 do not modify the existing Phase 9 primitives.

Per D-30 run `npm run build` before commit; expect exit 0 (file is presentational and self-contained, no runtime imports that could fail).
  </action>
  <verify>
    <automated>test -f src/components/editorial/year-block.tsx &amp;&amp; grep -E "md:sticky" src/components/editorial/year-block.tsx &amp;&amp; grep -E "md:self-start" src/components/editorial/year-block.tsx &amp;&amp; grep -E "md:top-9" src/components/editorial/year-block.tsx &amp;&amp; grep -E "export function YearBlock" src/components/editorial/year-block.tsx &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/components/editorial/year-block.tsx` succeeds
    - `rg "position: sticky|md:sticky" src/components/editorial/year-block.tsx` returns ≥1 hit (per 11-01-V)
    - `rg "self-start" src/components/editorial/year-block.tsx` returns ≥1 hit (per 11-01-V — D-10 REVISED critical detail)
    - `rg "md:top-9" src/components/editorial/year-block.tsx` returns ≥1 hit (sticky offset clears editorial header pt-9)
    - `rg "export function YearBlock" src/components/editorial/year-block.tsx` returns 1 hit (named export)
    - `rg "use client" src/components/editorial/year-block.tsx` returns 0 hits (server component)
    - `rg "text-label" src/components/editorial/year-block.tsx` returns ≥1 hit (uses Phase 9 token per D-29, no arbitrary 14px value)
    - `npm run build` exits 0 (D-30)
  </acceptance_criteria>
  <done>
    `<YearBlock>` exports a server-component-safe primitive with the sticky year column at md+. `npm run build` exits 0. Plans 11-03 and 11-05 can `import { YearBlock } from "@/components/editorial/year-block"` and pass `{ year: 2025, children: <…/> }`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Phase 11 introduces zero new attack surface. `<YearBlock>` is a pure presentational server-rendered primitive. No user input, no fetch, no env-var reads, no state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01-01 | Information disclosure | year-block.tsx (server-rendered) | accept | No PII, no env vars, no fetch — server-rendered prop pass-through only |
| T-11-01-02 | Tampering | year-block.tsx children prop | accept | React auto-escapes child text; caller controls children (Server Component caller context) |
| T-11-01-SC | Tampering | npm/pip/cargo installs | accept | Zero new package installs in Plan 11-01 (verified via RESEARCH § Standard Stack); no install tasks → slopcheck N/A |

V14 Configuration: covered by `npm run build` exit-0 gate per D-30 + Vercel preview at phase gate per D-31.
</threat_model>

<verification>
- `test -f src/components/editorial/year-block.tsx` (file exists)
- `grep` gates from 11-01-V (sticky, self-start, top-9 tokens present; named export present)
- `npm run build` exit 0
</verification>

<success_criteria>
- `src/components/editorial/year-block.tsx` exists as a server-component-safe primitive
- Sticky year column behavior present via `md:sticky md:top-9 md:self-start`
- Mobile single-column layout via base `grid-cols-1` (sticky tokens scoped to `md:` only)
- Phase 9 primitives still build cleanly (no edits to them per D-29)
- `npm run build` exits 0
</success_criteria>

<output>
Create `.planning/phases/11-archive-pages/11-01-SUMMARY.md` when done. Include the new file path, the JSDoc summary of `<YearBlock>`, the 11-01-V verify command results, and a note that Plans 11-03 and 11-05 can now import the primitive.
</output>
