---
phase: 11-archive-pages
plan: 03
type: execute
wave: 2
depends_on: ['11-01', '11-02']
files_modified:
  - src/components/home-v2/editorial-header.tsx
  - src/app/writing/page.tsx
  - src/app/page.tsx
  - src/components/nav/navigation.tsx
  - src/components/footer.tsx
  - src/components/main-offset.tsx
autonomous: true
requirements: ['ARCH-01']
must_haves:
  truths:
    - "Visiting /writing renders editorial header → 2-col title block (label · Writing. 120px page title · blurb with IntroLink · 360×480 atmosphere photo PHOTOS[5] Patricof09.jpg) → RuleStrong → year-grouped YearBlocks of essays → RuleStrong → inverted-ink Substack-outbound subscribe footer (D-01, D-13, D-14, D-15 REVISED)"
    - "Essays in YearBlocks come from `getPublishedPosts()` grouped client-side by year(post.date); no hardcoded titles (ARCH-01 contract)"
    - "Sticky year labels stay visible while their year's essays scroll past (md+); mobile collapses to single column (D-10 REVISED via <YearBlock> from Plan 11-01)"
    - "Subscribe footer is a styled outbound <a href='https://montymonthly.substack.com' target='_blank' rel='noopener noreferrer'> — NOT a form, NOT an /api/subscribe call (D-15 REVISED — RESEARCH F2 confirms no in-house pipeline exists; mirrors src/app/newsletter/page.tsx outbound URL)"
    - "Homepage nav link 'Writing' navigates to /writing (was /blog); WRITING-section AllLink targets /writing (was /blog); Footer Library 'Essays' link targets /writing (was /blog); /blog/[slug] post permalinks UNCHANGED per D-02 (D-05)"
    - "Phase 10 inline editorial header is extracted to src/components/home-v2/editorial-header.tsx with optional `active` prop; src/app/page.tsx renders <EditorialHeader /> (no active link bolded), /writing renders <EditorialHeader active='Writing' /> (D-25 confirmed home-v2/ path)"
    - "v1.0 chrome (Navigation, Footer, MainOffset) is suppressed on / AND /writing AND /events AND /photos via pathname inclusion check (D-26 extending Phase 10 D-42 from `pathname === '/'` to `['/', '/writing', '/events', '/photos'].includes(pathname)`)"
  artifacts:
    - path: "src/app/writing/page.tsx"
      provides: "/writing route — ARCH-01"
      exports: ["default", "revalidate", "metadata"]
      contains: "Writing."
      min_lines: 80
    - path: "src/components/home-v2/editorial-header.tsx"
      provides: "<EditorialHeader> shared across /, /writing, /events, /photos with optional `active` bolding (D-25)"
      contains: "export function EditorialHeader"
      min_lines: 30
    - path: "src/app/page.tsx"
      provides: "Phase 10 homepage — Plan 11-03 swaps inline header for <EditorialHeader /> + updates /blog→/writing in 3 places (D-05)"
      contains: "EditorialHeader"
    - path: "src/components/nav/navigation.tsx"
      provides: "v1.0 nav with pathname gate extended to v2.0 routes (D-26)"
      contains: "'/writing'"
    - path: "src/components/footer.tsx"
      provides: "v1.0 footer with pathname gate extended to v2.0 routes (D-26)"
      contains: "'/writing'"
    - path: "src/components/main-offset.tsx"
      provides: "v1.0 fixed-nav pt-16 offset gated off all v2.0 routes (D-26)"
      contains: "'/writing'"
  key_links:
    - from: "src/app/writing/page.tsx"
      to: "src/lib/notion.ts → getPublishedPosts()"
      via: "Server Component await call"
      pattern: "getPublishedPosts"
    - from: "src/app/writing/page.tsx"
      to: "src/components/editorial/year-block.tsx (Plan 11-01)"
      via: "import { YearBlock }"
      pattern: "YearBlock"
    - from: "src/app/writing/page.tsx"
      to: "https://montymonthly.substack.com"
      via: "footer <a> outbound"
      pattern: "substack\\.com"
    - from: "src/app/page.tsx + src/components/footer.tsx"
      to: "/writing"
      via: "href attribute"
      pattern: "href=\"/writing\""
---

<objective>
Ship **ARCH-01** — the `/writing` editorial archive — plus all Phase 11 shared-file edits in one sequential wave-2 plan so Plans 11-04 + 11-05 can ship in parallel afterward.

Concretely Plan 11-03 lands:

1. **NEW route** `src/app/writing/page.tsx` per D-01 + D-13–D-15 REVISED (editorial header → 2-col title block with `Writing.` 120px + Patricof09.jpg atmosphere photo → RuleStrong → year-grouped `<YearBlock>` essays from Notion → RuleStrong → Substack-outbound subscribe footer).
2. **NEW shared component** `src/components/home-v2/editorial-header.tsx` per D-25 (Option A — co-located with manifesto-reveal.tsx). Extracted from the inline header currently in `src/app/page.tsx` lines 60-94. Optional `active` prop bolds the matching nav link per handoff §3/§4.
3. **EDIT `src/app/page.tsx`** per D-05: replace inline header (lines 60-94) with `<EditorialHeader />`; change nav link "Writing" `href="/blog"` → `href="/writing"`; change WRITING section `<AllLink href="/blog">` → `href="/writing"`; change Footer Library "Essays" link from `/blog` → `/writing`. `/blog/[slug]` ListRow permalinks STAY (D-02).
4. **EDIT v1.0 chrome gate** per D-26 — extend the pathname check in `src/components/nav/navigation.tsx` line 18, `src/components/footer.tsx` line 25, and `src/components/main-offset.tsx` line 17 from `pathname === '/'` to `['/', '/writing', '/events', '/photos'].includes(pathname)`. Pre-positions the gate for Plans 11-04 (events) and 11-05 (photos) so those plans only touch their own route files.

Output: 1 new route, 1 new shared component, 4 edits to existing files. After Plan 11-03 ships, Plans 11-04 + 11-05 are safely parallel (no shared file touchpoints).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-archive-pages/11-CONTEXT.md
@.planning/phases/11-archive-pages/11-RESEARCH.md
@.planning/phases/11-archive-pages/11-VALIDATION.md
@.planning/phases/11-archive-pages/11-01-SUMMARY.md
@.planning/phases/11-archive-pages/11-02-SUMMARY.md
@.planning/research/editorial-redesign-handoff/README.md
@.planning/phases/10-editorial-homepage/10-CONTEXT.md
@CLAUDE.md
@src/app/page.tsx
@src/components/nav/navigation.tsx
@src/components/footer.tsx
@src/components/main-offset.tsx
@src/app/newsletter/page.tsx

<interfaces>
<!-- Phase 9 primitives signatures (consumed as-is per D-29) -->

From `src/components/editorial/section-label.tsx`:
```tsx
type Props = { children: ReactNode; numeral?: string };
export function SectionLabel({ children, numeral }: Props)
```

From `src/components/editorial/list-row.tsx`:
```tsx
type Props = { title: ReactNode; href: string; extra?: ReactNode; meta?: ReactNode; big?: boolean };
export function ListRow({ title, href, extra, meta, big = false }: Props)
```

From `src/components/editorial/all-link.tsx`:
```tsx
type Props = { children: ReactNode; href: string };
export function AllLink({ children, href }: Props)
```

From `src/components/editorial/intro-link.tsx`:
```tsx
type Props = { children: ReactNode; href: string };
export function IntroLink({ children, href }: Props)
```

From `src/components/editorial/rule.tsx` / `rule-strong.tsx`:
```tsx
// No props — just `<Rule />` / `<RuleStrong />`
```

<!-- New primitive from Plan 11-01 -->

From `src/components/editorial/year-block.tsx`:
```tsx
type Props = { year: string | number; children: ReactNode };
export function YearBlock({ year, children }: Props)
```

<!-- Notion getter from Phase 9/10 (untouched) -->

From `src/lib/notion.ts`:
```ts
export interface BlogPost {
  id: string;
  slug: string;
  title: string;
  description: string;
  published: boolean;
  date: string;          // ISO; "" when missing — guard with `if (!post.date) continue`
  // ...
}
export async function getPublishedPosts(): Promise<BlogPost[]>;
```

<!-- Date helper from Plan 11-02 + existing -->

From `src/lib/dates.ts`:
```ts
export function formatMonthYear(iso: string | null): string;
export function formatMonthDay(iso: string | null): string;
export function formatDayNumeral(iso: string | null): string; // NEW from Plan 11-02 (not needed by /writing but available)
```

<!-- Substack URL — verified from src/app/newsletter/page.tsx line 39 -->

`https://montymonthly.substack.com` — the existing newsletter "pipeline" (D-15 REVISED + RESEARCH F2 + RESEARCH § Newsletter Pipeline Reuse).

<!-- Current Phase 10 inline header to extract — src/app/page.tsx lines 60-94 -->

```tsx
<header className="flex items-baseline justify-between px-6 pt-7 md:px-40 md:pt-9">
  <Link href="/" className="text-[15px] font-bold tracking-tight text-ink">Monty Singer</Link>
  <nav>
    <ul className="flex list-none flex-wrap items-baseline gap-x-6 gap-y-2 text-nav text-ink md:gap-x-8">
      <li><Link href="/projects" className="flex min-h-11 items-center transition-opacity hover:opacity-60">Building</Link></li>
      <li><Link href="/blog"     className="flex min-h-11 items-center transition-opacity hover:opacity-60">Writing</Link></li>   {/* /blog → /writing in this plan */}
      <li><Link href="/events"   className="flex min-h-11 items-center transition-opacity hover:opacity-60">Events</Link></li>
      <li><Link href="/about"    className="flex min-h-11 items-center transition-opacity hover:opacity-60">About</Link></li>
      <li><Link href="/links"    className="flex min-h-11 items-center transition-opacity hover:opacity-60">Links</Link></li>
    </ul>
  </nav>
</header>
```

<!-- Current chrome-gate lines being extended -->

`src/components/nav/navigation.tsx:18` — `if (pathname === '/') return null` → extend list
`src/components/footer.tsx:25` — `if (pathname === '/') return null` → extend list
`src/components/main-offset.tsx:17` — `const isHome = pathname === '/'` → rename + extend list
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extract <EditorialHeader> + extend v1.0 chrome gate to all v2.0 routes</name>
  <files>src/components/home-v2/editorial-header.tsx, src/components/nav/navigation.tsx, src/components/footer.tsx, src/components/main-offset.tsx, src/app/page.tsx</files>
  <read_first>
    - src/app/page.tsx lines 60-94 (the inline header JSX being extracted)
    - src/app/page.tsx lines 65-92 (the 5 nav <Link> entries — note line 73: "Writing" href="/blog" needs to flip to "/writing" in the extracted component)
    - src/app/page.tsx lines 192-208 (WRITING section AllLink and home `<ListRow href={\`/blog/${post.slug}\`}>` — the AllLink href changes; the ListRow href STAYS per D-02)
    - src/app/page.tsx lines 344-353 (Footer Library "Essays" link — line ~350 `{ label: "Essays", href: "/blog" }` flips to "/writing")
    - src/components/nav/navigation.tsx line 18 (current gate `if (pathname === '/') return null`)
    - src/components/footer.tsx line 25 (same gate)
    - src/components/main-offset.tsx line 17 (current gate `const isHome = pathname === '/'`)
    - src/components/home-v2/manifesto-reveal.tsx (sibling component for path-convention reference; this is why D-25 puts editorial-header.tsx in home-v2/)
    - .planning/phases/11-archive-pages/11-CONTEXT.md D-05, D-25, D-26
    - .planning/phases/11-archive-pages/11-RESEARCH.md §"EditorialHeader extraction" (canonical implementation block lines 644-700+) and §"EditorialHeader Re-export Location" (D-25 path decision)
    - .planning/phases/11-archive-pages/11-RESEARCH.md §"Chrome-gate extension (Plan 11-03)" (exact 3-file edits)
    - .planning/phases/11-archive-pages/11-RESEARCH.md §"Pitfall 4" (parallel-race justification for why 11-03 owns these shared edits) and §"Pitfall 5" (full /blog → /writing sweep — note D-02 keeps /blog/[slug])
  </read_first>
  <action>
**A. Create `src/components/home-v2/editorial-header.tsx`** (per D-25 confirmed home-v2/ path — co-located with `manifesto-reveal.tsx`). Server-component-safe; imports `Link` from `next/link` only. Define:

```tsx
type Props = {
  /** Bolds the matching nav link. Falsy = no link bolded. */
  active?: "Building" | "Writing" | "Events" | "About" | "Links";
};
```

Constant `LINKS` array (define once at module top — Phase 9 convention):

```
{ label: "Building", href: "/projects" }
{ label: "Writing",  href: "/writing"  }    // NEW destination per D-05
{ label: "Events",   href: "/events"   }
{ label: "About",    href: "/about"    }
{ label: "Links",    href: "/links"    }
```

`export function EditorialHeader({ active }: Props)` renders:
- Outer `<header className="flex items-baseline justify-between px-6 pt-7 md:px-40 md:pt-9">` (verbatim from page.tsx line 61).
- Left: `<Link href="/" className="text-[15px] font-bold tracking-tight text-ink">Monty Singer</Link>`.
- Right `<nav><ul className="flex list-none flex-wrap items-baseline gap-x-6 gap-y-2 text-nav md:gap-x-8">…</ul></nav>` — map `LINKS`, render each `<Link>` with `className` toggling between active state (`font-bold text-ink`) and default (`text-muted`); preserve `flex min-h-11 items-center transition-opacity hover:opacity-60` for the 44px tap target (HOME-V2-12 carryforward) and hover behavior. **Critical:** the active branch uses `font-bold text-ink`; the default branch uses `text-muted`. Handoff §3/§4 specifies "Writing bolded vs other nav links muted" on /writing and "Events bolded" on /events.

JSDoc on the export references D-05 + D-25 by number. No `'use client'` (pure presentational).

**B. Update `src/components/nav/navigation.tsx`** — change line 18 from:
```
if (pathname === '/') return null
```
to:
```
if (['/', '/writing', '/events', '/photos'].includes(pathname)) return null
```
(D-26 extending Phase 10 D-42). Update the inline comment above line 17 from `// D-42: v2.0 homepage…` to `// D-42 + D-26: v2.0 routes (/, /writing, /events, /photos) render their own editorial chrome — suppress v1.0 nav`. Do not touch any other code in this file (preserve `NAV_LINKS`, mobile drawer, etc.).

**C. Update `src/components/footer.tsx`** — change line 25 from:
```
if (pathname === '/') return null
```
to:
```
if (['/', '/writing', '/events', '/photos'].includes(pathname)) return null
```
Update the comment block above similarly. The `LINKS` const at line 6-13 ALSO contains `{ href: '/blog', label: 'Writings' }` (line 9) — RESEARCH § Pitfall 5 recommends flipping it to `{ href: '/writing', label: 'Writings' }` so the v1.0 footer (which still renders on /about, /projects, etc.) sends users to the canonical archive instead of the legacy /blog listing. Apply that change too.

**D. Update `src/components/main-offset.tsx`** — rewrite the body of `MainOffset`:
```tsx
const pathname = usePathname()
const isV2Route = ['/', '/writing', '/events', '/photos'].includes(pathname)
return <main className={isV2Route ? '' : 'pt-16'}>{children}</main>
```
Replace the JSDoc block to cite both D-42 + D-26. Keep the file as a client component (existing `'use client'` directive stays — `usePathname` requires it).

**E. Update `src/app/page.tsx`** — three coordinated edits:

  **E.1.** Add `import { EditorialHeader } from "@/components/home-v2/editorial-header";` near the existing `import { ManifestoReveal } from "@/components/home-v2/manifesto-reveal";` (line ~15).

  **E.2.** Delete lines 60-94 inclusive (the inline `<header>` block) and replace with a single line: `<EditorialHeader />`. The home page does not bold any nav link (handoff homepage doesn't single out one section — Writing/Events get bolded only on THEIR routes), so no `active` prop here.

  **E.3.** Update three `/blog` references per D-05:
    - Line ~205: `<AllLink href="/blog">All writing →</AllLink>` → `<AllLink href="/writing">All writing →</AllLink>`.
    - Footer Library array (line ~349): change the entry `{ label: "Essays", href: "/blog" }` → `{ label: "Essays", href: "/writing" }`.
    - **Do NOT change** line ~196: `<ListRow … href={\`/blog/${post.slug}\`} … />` — this is the `/blog/[slug]` post permalink which STAYS per D-02.
    - **Do NOT change** the IntroLink line 133 `<IntroLink href="/blog">essays</IntroLink>` — wait, RESEARCH § Pitfall 5 flags this for review. Re-read D-05: only "Writing" nav link, WRITING section AllLink, and Footer Library "Essays" link are listed. Be conservative — flip ONLY those three. The IntroLink "essays" in the letter-style intro is a sentence reference rather than an archive-pointer; leave it pointing to `/blog` for now (Phase 12 sub-page sweep can re-evaluate during /blog restyle). Document this conservative interpretation in the SUMMARY.
    - **Do NOT change** Footer Studio "Process Notes" `href="/blog"` (line ~339) — D-05 explicitly says "Essays" only; Process Notes is a different label.

Per D-30 run `npm run build` after Task 1 — expect exit 0. The three chrome-gate files are client components that compile cleanly with the extended `includes()` check.
  </action>
  <verify>
    <automated>test -f src/components/home-v2/editorial-header.tsx &amp;&amp; grep -E "export function EditorialHeader" src/components/home-v2/editorial-header.tsx &amp;&amp; grep -E "href: \"/writing\"" src/components/home-v2/editorial-header.tsx &amp;&amp; grep -E "'/writing'" src/components/nav/navigation.tsx &amp;&amp; grep -E "'/writing'" src/components/footer.tsx &amp;&amp; grep -E "'/writing'" src/components/main-offset.tsx &amp;&amp; grep -E "EditorialHeader" src/app/page.tsx &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/components/home-v2/editorial-header.tsx` succeeds
    - `rg "export function EditorialHeader" src/components/home-v2/editorial-header.tsx` returns 1 hit
    - `rg "active\?: \"Building\" \| \"Writing\" \| \"Events\" \| \"About\" \| \"Links\"" src/components/home-v2/editorial-header.tsx` returns 1 hit (typed `active` prop)
    - `rg "'/writing'" src/components/nav/navigation.tsx` returns ≥1 hit (D-26 gate extension)
    - `rg "'/writing'" src/components/footer.tsx` returns ≥1 hit
    - `rg "'/writing'" src/components/main-offset.tsx` returns ≥1 hit
    - `rg "'/events'" src/components/nav/navigation.tsx` returns ≥1 hit (D-26 pre-positions for Plan 11-04)
    - `rg "'/photos'" src/components/nav/navigation.tsx` returns ≥1 hit (D-26 pre-positions for Plan 11-05)
    - `rg "<EditorialHeader" src/app/page.tsx` returns ≥1 hit (Phase 10 inline header replaced)
    - `rg "<header className=\"flex items-baseline" src/app/page.tsx` returns 0 hits (inline header lines 60-94 removed)
    - `npm run build` exits 0 (D-30)
  </acceptance_criteria>
  <done>
    `<EditorialHeader>` is extracted and consumed on the homepage. v1.0 chrome (Navigation, Footer, MainOffset) is suppressed on all four v2.0 routes ahead of Plans 11-04/05 needing it. Homepage navigation Writing link + WRITING AllLink + Footer "Essays" link all target `/writing`. Build green.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create /writing route with title block + year-grouped essays + Substack-outbound footer</name>
  <files>src/app/writing/page.tsx</files>
  <read_first>
    - .planning/phases/11-archive-pages/11-CONTEXT.md D-01, D-13 (page structure), D-14 (page-title token), D-15 REVISED (Substack outbound, NOT a form), D-27 (preservation), D-29 (Phase 9 primitives as-is)
    - .planning/phases/11-archive-pages/11-RESEARCH.md §"Pattern 1: Editorial Archive Title Block" (canonical title-block copy-paste), §"Year-grouping BlogPost[]" (canonical grouping code), §"Pattern 5: /writing Substack subscribe footer", §"Newsletter Pipeline Reuse" (the canonical interpretation of D-15 REVISED)
    - .planning/phases/11-archive-pages/11-VALIDATION.md row 11-03-V (grep gates: Writing., YearBlock, substack.com, href="/writing" ×2 in page.tsx, build exit 0)
    - .planning/research/editorial-redesign-handoff/README.md §"3. /writing — Index" (full layout spec — section labels, padding, paragraphs, footer copy)
    - src/app/page.tsx (Phase 10 homepage — atmosphere photo pattern lines 110-124 confirms `next/image fill` inside `relative h-[480px] w-[360px]` parent)
    - src/app/newsletter/page.tsx line 39 (verify Substack URL: https://montymonthly.substack.com — confirms D-15 REVISED canonical destination)
    - src/lib/notion.ts (getPublishedPosts signature + BlogPost type — `date: string` which is "" when missing; null/empty guard with `if (!post.date) continue`)
    - src/lib/dates.ts (formatMonthYear for the meta column right-side date)
    - src/components/editorial/year-block.tsx (Plan 11-01 — props `{ year, children }`)
    - src/components/editorial/list-row.tsx (Phase 9 — `title/href/extra/meta/big`)
    - src/components/editorial/rule.tsx + rule-strong.tsx (Phase 9 — no props)
    - src/components/editorial/intro-link.tsx (Phase 9 — `children/href`)
    - src/components/home-v2/editorial-header.tsx (Plan 11-03 Task 1 — `<EditorialHeader active?>` signature)
  </read_first>
  <action>
Create `src/app/writing/page.tsx` as a default-export async Server Component. Required imports:
```ts
import Link from "next/link";
import Image from "next/image";
import { Fragment } from "react";
import type { Metadata } from "next";
import { getPublishedPosts, type BlogPost } from "@/lib/notion";
import { formatMonthYear } from "@/lib/dates";
import { EditorialHeader } from "@/components/home-v2/editorial-header";
import { RuleStrong } from "@/components/editorial/rule-strong";
import { Rule } from "@/components/editorial/rule";
import { IntroLink } from "@/components/editorial/intro-link";
import { ListRow } from "@/components/editorial/list-row";
import { YearBlock } from "@/components/editorial/year-block";
```

Module-level exports:

```ts
export const revalidate = 1800; // 30 minutes — matches /, /events ISR cadence (RESEARCH § Pitfall 9)

export const metadata: Metadata = {
  title: "Writing | Monty Singer",
  description: "Long-form essays on philosophy, technology, and the texture of an attentive life.",
  alternates: { canonical: "/writing" },
  openGraph: {
    title: "Writing | Monty Singer",
    description: "Long-form essays on philosophy, technology, and the texture of an attentive life.",
    url: "/writing",
    type: "website",
  },
};
```

Define a module-private helper (NOT exported) near the bottom — keep helper before `export default async function` invocation:

```ts
function groupPostsByYear(posts: BlogPost[]): Map<number, BlogPost[]> {
  const groups = new Map<number, BlogPost[]>();
  for (const post of posts) {
    if (!post.date) continue; // BlogPost.date is "" when Notion record has no date
    const year = new Date(post.date).getUTCFullYear();
    if (Number.isNaN(year)) continue;
    const bucket = groups.get(year) ?? [];
    bucket.push(post);
    groups.set(year, bucket);
  }
  return new Map([...groups.entries()].sort(([a], [b]) => b - a));
}
```

`export default async function WritingPage()`:

1. Defensive fetch (mirror src/app/page.tsx lines 42-54 pattern):
   ```ts
   let posts: Awaited<ReturnType<typeof getPublishedPosts>> = [];
   try { posts = await getPublishedPosts(); } catch {}
   const postsByYear = groupPostsByYear(posts);
   const yearEntries = [...postsByYear.entries()];
   ```

2. Return JSX:

   **EditorialHeader** — `<EditorialHeader active="Writing" />` (handoff §3 specifies "Writing" bolded on /writing).

   **Title block** (§ Pattern 1) — `<section className="px-6 pt-40 pb-24 md:px-40 md:pt-[160px] md:pb-[100px]">` containing a 2-col grid `grid-cols-1 items-end gap-10 md:grid-cols-[1fr_360px] md:gap-20`:
     - Left column:
       - `<div className="text-label uppercase text-muted">── The Library · 02</div>` (handoff section label · 02, with em-dashes per D-14 page-title convention)
       - `<h1 className="mt-6 text-page-title uppercase text-ink">Writing.</h1>` (D-14 + handoff: trailing period intentional; uses Phase 9 `text-page-title` token = 120px/0.95/-0.045em/700)
       - `<p className="mt-10 max-w-[35rem] text-body-lead text-muted">` — handoff copy: "Long-form essays on philosophy, technology, and the texture of an attentive life. Published monthly, sometimes more, never less. Subscribe at <IntroLink href="/newsletter">Monty Monthly</IntroLink>." — the IntroLink stays pointing to /newsletter per D-28 (existing newsletter page preserved).
     - Right column (atmosphere photo `PHOTOS[5] = Patricof09.jpg` per D-13):
       - `<div className="hidden md:block">` (RESEARCH § Pitfall 6 — hide on mobile so page title stays above the fold)
       - Inside: `<div className="relative h-[480px] w-[360px] overflow-hidden bg-rule-strong">` (Pitfall 3 — positioned parent with explicit h/w for `next/image fill`)
       - `<Image src="/MSizzle-website-photos/Patricof09.jpg" alt="" fill sizes="360px" className="object-cover saturate-[0.92]" />` (`saturate-[0.92]` matches D-23 + Phase 10 homepage photographs treatment).

   **`<RuleStrong />`**

   **Year-grouped essays** — wrap in `<section className="px-6 md:px-40">` (the YearBlock primitive supplies its own vertical padding `md:py-20`). Conditional render:

     - If `yearEntries.length === 0`: render an empty-state paragraph using `text-caption text-muted` reading "More essays coming soon." Skip the YearBlocks entirely.
     - Else map `yearEntries`:
       ```tsx
       {yearEntries.map(([year, yearPosts], i, arr) => (
         <Fragment key={year}>
           <YearBlock year={year}>
             {yearPosts.map((post) => (
               <ListRow
                 key={post.id}
                 big
                 href={`/blog/${post.slug}`}   // D-02 — post permalinks stay
                 title={post.title}
                 extra={post.description}
                 meta={formatMonthYear(post.date)}
               />
             ))}
           </YearBlock>
           {i < arr.length - 1 && <Rule />}
         </Fragment>
       ))}
       ```

   **`<RuleStrong />`** (before the footer per D-13 step 6).

   **Substack-outbound subscribe footer (D-15 REVISED — copy block from RESEARCH § Pattern 5):**
   ```tsx
   <footer className="bg-footer-bg text-footer-fg px-7 py-12 md:px-40 md:py-16">
     <div className="text-label uppercase text-footer-mute">── End of archive</div>
     <h2 className="mt-6 max-w-[40rem] text-section-feature text-footer-fg">
       Receive new essays the morning they&rsquo;re published.
     </h2>
     <div className="mt-10">
       <a
         href="https://montymonthly.substack.com"
         target="_blank"
         rel="noopener noreferrer"
         className="inline-block border border-footer-fg/40 px-7 py-3 text-label uppercase text-footer-fg transition-opacity hover:opacity-80"
       >
         Subscribe on Substack →
       </a>
     </div>
     <div className="mt-16 text-meta uppercase text-footer-mute">
       © 2026 Monty Singer · montymonthly.substack.com
     </div>
   </footer>
   ```
   This is a single styled outbound `<a>` to the SAME endpoint `src/app/newsletter/page.tsx` uses (line 39 — verified before plan-check). NO `<form>` element. NO `<input>` element. NO `/api/subscribe` POST. NO `useState` (no client component). The visual "looks like a form" interpretation is explicitly REJECTED per D-15 REVISED + RESEARCH § Newsletter Pipeline Reuse — Substack outbound IS the existing pipeline.

3. Wrap entire return in a Fragment `<>` (Phase 10 page.tsx pattern — no top-level wrapper div).

**Forbidden patterns** (RESEARCH § Anti-Patterns):
- No `<form action=…>` / no `<input type="email">` / no `'use client'`
- No new package additions (CLAUDE.md constraint)
- No edits to `src/components/animations/scroll-reveal.tsx`, `src/components/providers/lenis-provider.tsx`, `src/app/template.tsx` (D-27)
- No edits to `/blog/[slug]` route (D-02)
- No edits to Phase 9 primitives (D-29)

**Build verification (D-30):** `npm run build` after the file is in. Expect:
- Build exits 0
- `/writing` appears in the routes manifest
- ISR revalidate=1800 picked up
- No TypeScript errors against `BlogPost` import or `<YearBlock>` props
  </action>
  <verify>
    <automated>test -f src/app/writing/page.tsx &amp;&amp; grep -E "Writing\\." src/app/writing/page.tsx &amp;&amp; grep -E "YearBlock" src/app/writing/page.tsx &amp;&amp; grep -E "montymonthly\\.substack\\.com" src/app/writing/page.tsx &amp;&amp; ! grep -E "&lt;form|/api/subscribe" src/app/writing/page.tsx &amp;&amp; ! grep -E "use client" src/app/writing/page.tsx &amp;&amp; grep -c "href=\"/writing\"" src/app/page.tsx &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/app/writing/page.tsx` succeeds (per 11-03-V)
    - `rg "Writing\\." src/app/writing/page.tsx` ≥1 hit (per 11-03-V — D-14 trailing period page title)
    - `rg "YearBlock" src/app/writing/page.tsx` ≥1 hit (per 11-03-V — consumes Plan 11-01 primitive)
    - `rg "substack.com" src/app/writing/page.tsx` ≥1 hit (per 11-03-V — D-15 REVISED Substack outbound)
    - `rg "montymonthly.substack.com" src/app/writing/page.tsx` returns 1 hit (exact URL mirrors src/app/newsletter/page.tsx:39)
    - `rg "&lt;form" src/app/writing/page.tsx` returns 0 hits (D-15 REVISED — NOT a form)
    - `rg "/api/subscribe" src/app/writing/page.tsx` returns 0 hits (no in-house pipeline — RESEARCH F2)
    - `rg "use client" src/app/writing/page.tsx` returns 0 hits (Server Component)
    - `rg "EditorialHeader active=\"Writing\"" src/app/writing/page.tsx` returns 1 hit (active link bolded per handoff §3 + D-25)
    - `rg -c 'href="/writing"' src/app/page.tsx` returns ≥2 (per 11-03-V — nav Writing link + WRITING-section AllLink updated per D-05)
    - `rg "Patricof09" src/app/writing/page.tsx` ≥1 hit (atmosphere photo PHOTOS[5] per D-13)
    - `rg "text-page-title" src/app/writing/page.tsx` ≥1 hit (D-14 token, no arbitrary 120px value)
    - `rg "getPublishedPosts" src/app/writing/page.tsx` returns 1 hit (Notion-backed essays, no hardcoded titles per ARCH-01)
    - `rg "/blog/" src/app/writing/page.tsx` returns ≥1 hit (post permalinks preserved per D-02 — ListRow href uses /blog/[slug])
    - `npm run build` exits 0 (D-30); routes manifest includes `/writing` and `/writing.html`/dynamic entry as Next 16 generates
  </acceptance_criteria>
  <done>
    Visiting `/writing` (after build + dev/preview) renders the editorial header with "Writing" bolded, the 2-col title block with the Patricof09.jpg atmosphere photo on the right (hidden mobile), year-grouped essays from Notion with sticky year labels (via `<YearBlock>`), and a Substack-outbound subscribe footer. Homepage Writing nav link + WRITING AllLink + Footer "Essays" link all target `/writing`. v1.0 chrome suppressed on `/writing`. ARCH-01 success criteria satisfied.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server → Notion API | `getPublishedPosts()` call from Server Component. Inherited Phase 9/10 fetch path — Notion token never reaches client. |
| Server → External link (Substack) | Outbound `<a href="https://montymonthly.substack.com">`. Static literal URL, no user input concatenated. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03-01 | Tampering (reverse tabnabbing) | Substack outbound `<a target="_blank">` | mitigate | `rel="noopener noreferrer"` on the link (RESEARCH § Pitfall 7) |
| T-11-03-02 | Tampering (XSS via Notion-sourced title/description) | `<ListRow title={post.title} extra={post.description}>` | mitigate | React auto-escapes string children; no `dangerouslySetInnerHTML` used (RESEARCH § Security Domain) |
| T-11-03-03 | Information disclosure (NOTION_TOKEN leak) | `getPublishedPosts()` in Server Component | mitigate | Inherited — Server Component context; token only read in src/lib/notion.ts (D-14 carryforward from v1.0); D-14 client-bundle scan re-runs in Phase 13 |
| T-11-03-04 | Denial of service (Notion API failure breaks /writing render) | `await getPublishedPosts()` | mitigate | `try { posts = await getPublishedPosts() } catch {}` defensive pattern (mirrors src/app/page.tsx lines 46-54); empty-state copy renders when fetch fails |
| T-11-03-05 | Tampering (open redirect on post slug → /blog/${slug}) | `<ListRow href={`/blog/${post.slug}`}>` | accept | `post.slug` is a Notion rich_text field controlled by site author; no user-supplied input concatenation |
| T-11-03-SC | Tampering | npm/pip/cargo installs | accept | Zero new package installs in Plan 11-03 (RESEARCH § Standard Stack — "no npm install commands needed for Phase 11"); slopcheck N/A |

V14 Configuration: covered by per-task `npm run build` exit-0 (D-30) + Vercel preview at phase gate (D-31).
</threat_model>

<verification>
- File creation: `test -f src/app/writing/page.tsx` + `test -f src/components/home-v2/editorial-header.tsx`
- 11-03-V grep gates: `Writing.` + `YearBlock` + `substack.com` + `href="/writing"` ≥2 in page.tsx + `npm run build` exit 0
- Negative gates: no `<form`, no `/api/subscribe`, no `'use client'`, no inline `<header>` left in src/app/page.tsx
- Chrome gate extension: `'/writing'`, `'/events'`, `'/photos'` literal strings in nav/footer/main-offset
- Build: `npm run build` exits 0 with `/writing` in routes manifest
</verification>

<success_criteria>
- /writing route exists and renders the editorial archive per handoff §3
- /writing essays come from Notion (no hardcoded titles)
- Sticky year labels work (via Plan 11-01's `<YearBlock>`)
- Subscribe footer is a Substack outbound link, not a form
- Homepage nav + AllLink + Footer "Essays" link all flip /blog → /writing
- v1.0 chrome (Nav + Footer + MainOffset pt-16) suppressed on all 4 v2.0 routes
- ARCH-01 success criteria from ROADMAP all met
- `npm run build` exits 0
</success_criteria>

<output>
Create `.planning/phases/11-archive-pages/11-03-SUMMARY.md` when done. Include: file paths created/modified, the EditorialHeader signature, the 3 chrome-gate file edits, the 3 /blog→/writing href updates in page.tsx (and the conservative non-changes — IntroLink "essays" + Footer "Process Notes" stayed), 11-03-V verify command outputs, and note that Plans 11-04 + 11-05 are now parallel-safe.
</output>
