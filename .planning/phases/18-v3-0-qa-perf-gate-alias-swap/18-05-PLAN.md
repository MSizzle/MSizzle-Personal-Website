---
phase: 18-v3-0-qa-perf-gate-alias-swap
plan: 05
type: execute
wave: 2
depends_on:
  - 18-01
files_modified:
  - .planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-05-EVIDENCE.md
autonomous: true
requirements:
  - DQ-02

must_haves:
  truths:
    - "Grep for `secret_` in `.next/static/chunks/*.js` returns 0 matches (no client leak)"
    - "Grep for `NOTION_TOKEN` in `.next/static/chunks/*.js` returns 0 matches (no client leak)"
    - "Grep for `secret_` in `.vercel/output/static/_next/static/chunks/*.js` returns 0 matches"
    - "Grep for `NOTION_TOKEN` in `.vercel/output/static/_next/static/chunks/*.js` returns 0 matches"
    - "`NOTION_TOKEN` appears only in server-side `.vercel/output/functions/` (expected; not a leak)"
    - "Theme mode is documented: either single-mode (Pumpkin Amber only, no dark palette) or FOUC check passed"
    - "`18-05-EVIDENCE.md` records all six grep result rows and the D-10 theme decision"
  artifacts:
    - path: ".planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-05-EVIDENCE.md"
      provides: "D-06 secret scan evidence + D-10 theme/FOUC decision record"
      contains: "D-06"
  key_links:
    - from: ".planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-05-EVIDENCE.md"
      to: ".planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-GO-NO-GO.md"
      via: "D-06 secret scan verdict row + D-10 theme decision row"
      pattern: "D-06.*PASS"
---

<objective>
Run the D-14 dual-tree client-bundle secret scan (D-06 per CONTEXT.md) against the build output from 18-01, and record the D-10 theme/FOUC decision. Both gates are autonomous — no human interaction required.

Purpose: The secret scan is cheap insurance that the presentation-layer v3 changes (Phases 14-17) did not accidentally import a server util into a client component, leaking `NOTION_TOKEN` or internal `secret_*` identifiers into the client bundle. The theme record documents whether v3 ships single-mode (Pumpkin Amber only) or requires a FOUC incognito test.

Output: `18-05-EVIDENCE.md` with a D-06 PASS/FAIL scan result table and the D-10 theme decision record.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-CONTEXT.md
@.planning/phases/13-v2-0-qa-go-no-go/13-05-EVIDENCE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: D-14 dual-tree secret scan</name>
  <files>.planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-05-EVIDENCE.md</files>

  <read_first>
    1. `.planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-CONTEXT.md` — D-06 secret scan scope (dual-tree: `.next/static/chunks/` AND `.vercel/output/static/_next/static/chunks/`; search terms `secret_` and `NOTION_TOKEN`)
    2. `.planning/phases/13-v2-0-qa-go-no-go/13-05-EVIDENCE.md` — the exact scan results table format and D-10 theme section to replicate
    3. `.planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-PATTERNS.md` — `18-05-EVIDENCE.md` document structure, especially the six-row scan results table
  </read_first>

  <action>
    Prerequisites: `.vercel/output/` and `.next/` must exist from the 18-01 `vercel build --prod` run. Verify: `test -d .vercel/output && test -d .next/static/chunks && echo "Build output confirmed"`. If either directory is absent, stop and report — 18-01 must complete before this plan can run.

    Run the six scan commands (D-14 pattern, per CONTEXT.md D-06 and RESEARCH.md Section "D-14 Secret Scan Pattern"):

    Command 1 — Tree 1, `secret_`:
    `grep -r "secret_" .next/static/chunks/*.js 2>/dev/null; echo "exit: $?"`
    Expected: no output (0 matches); exit 0 means no match found by grep (grep returns 1 when no match, but the `;echo` shows that)
    Correction: use `grep -rl "secret_" .next/static/chunks/*.js 2>/dev/null | wc -l` — returns count of files with matches; expected 0

    Command 2 — Tree 1, `NOTION_TOKEN`:
    `grep -rl "NOTION_TOKEN" .next/static/chunks/*.js 2>/dev/null | wc -l`
    Expected: 0

    Command 3 — Tree 2, `secret_`:
    `grep -rl "secret_" .vercel/output/static/_next/static/chunks/*.js 2>/dev/null | wc -l`
    Expected: 0

    Command 4 — Tree 2, `NOTION_TOKEN`:
    `grep -rl "NOTION_TOKEN" .vercel/output/static/_next/static/chunks/*.js 2>/dev/null | wc -l`
    Expected: 0

    Command 5 — Full Vercel output tree, `secret_` (paranoid check):
    `grep -rl "secret_" .vercel/output/ 2>/dev/null | grep -v "functions/" | wc -l`
    Expected: 0 (excludes server functions where it's expected to appear in env names)

    Command 6 — Server-only confirmation, `NOTION_TOKEN` (must exist server-side):
    `grep -rl "NOTION_TOKEN" .vercel/output/functions/ 2>/dev/null | wc -l`
    Expected: > 0 (confirms server-side refs exist; these are NOT leaks)

    For any command that returns a count > 0 in commands 1-5: immediately run the match command to identify the file and line: `grep -rn "secret_" .next/static/chunks/*.js | head -20`. A non-zero match in commands 1-4 is a BLOCKING FAIL — stop the plan and report to the operator with the exact file and line that leaked.
  </action>

  <verify>
    <automated>grep -rl "secret_" .next/static/chunks/*.js 2>/dev/null | wc -l | grep -q "^0$" && grep -rl "NOTION_TOKEN" .next/static/chunks/*.js 2>/dev/null | wc -l | grep -q "^0$" && echo "Secret scan: 0 leaks in .next/static/chunks"</automated>
  </verify>

  <acceptance_criteria>
    - Commands 1-4 all return 0 (zero files matched in client chunk directories)
    - Command 5 returns 0 (no secret_ in the non-functions parts of .vercel/output/)
    - Command 6 returns > 0 (NOTION_TOKEN exists in server functions — confirms server/client boundary intact)
    - If any of commands 1-5 return > 0: plan records D-06 FAIL with the matching file/line excerpt; operator is notified; this is a blocking failure for GO
  </acceptance_criteria>

  <done>Six-command D-14 dual-tree scan completed; 0 client leaks found; server-only NOTION_TOKEN reference confirmed; D-06 result recorded in 18-05-EVIDENCE.md.</done>
</task>

<task type="auto">
  <name>Task 2: D-10 theme/FOUC decision record</name>
  <files>.planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-05-EVIDENCE.md</files>

  <read_first>
    1. `.planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-CONTEXT.md` — D-10 decision: record single-mode ship if no dark v3 palette; else run FOUC incognito check
    2. `src/app/globals.css` — v3 Pumpkin Amber token definitions; check for `.dark` selectors or `[data-theme="dark"]` blocks
    3. `.planning/phases/13-v2-0-qa-go-no-go/13-05-EVIDENCE.md` — D-05 single-mode escape hatch format (the v2.0 analog); replicate for D-10
  </read_first>

  <action>
    Check for dark-mode palette in the v3 codebase:

    Check 1 — globals.css dark selectors:
    `grep -v "^[[:space:]]*//" src/app/globals.css | grep -c "\.dark\|data-theme.*dark\|@media.*prefers-color-scheme.*dark"`
    Expected for single-mode: 0

    Check 2 — ThemeProvider / next-themes imports:
    `grep -r "next-themes\|ThemeProvider" src/app/ src/components/ 2>/dev/null | grep -v "\.d\.ts" | wc -l`
    Expected for single-mode: 0

    Check 3 — Dark token vars:
    `grep -c "\.dark\|prefers-color-scheme: dark" src/app/globals.css`
    Expected for single-mode: 0

    Decision path:
    - If all three checks return 0: v3 is confirmed single-mode (Pumpkin Amber only). Record in 18-05-EVIDENCE.md: "D-10: Single-mode ship — 0 dark palette tokens found in globals.css, 0 ThemeProvider imports. Dark Pumpkin Amber variant deferred to v3.1+. No FOUC test required."
    - If any check returns > 0: a dark palette exists. Run the FOUC incognito test:
      1. Open Chrome Incognito → open the preview URL homepage
      2. With system preference set to Light: confirm Pumpkin Amber renders (no flicker)
      3. Change system preference to Dark (OS Settings): confirm page either (a) stays Pumpkin Amber (theme lock) or (b) switches to the dark variant without FOUC
      4. Record which path applies and whether FOUC was observed
      5. FOUC = flash of unthemed/wrong-theme content on load
      Record in 18-05-EVIDENCE.md: "D-10: Multi-mode detected — N dark tokens found. FOUC test result: [pass/fail]."

    Append the D-10 section to `18-05-EVIDENCE.md` (after the secret scan section). The evidence doc now contains both Gate 1 (D-06 scan) and Gate 2 (D-10 theme decision), matching the 13-05-EVIDENCE.md structure.

    Final overall verdict line: "18-05: D-06 PASS + D-10 PASS — 0 client leaks; single-mode ship recorded."
  </action>

  <verify>
    <automated>test -f ".planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-05-EVIDENCE.md" && grep -q "D-06" ".planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-05-EVIDENCE.md" && grep -q "D-10" ".planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-05-EVIDENCE.md"</automated>
  </verify>

  <acceptance_criteria>
    - `18-05-EVIDENCE.md` has the six-row scan results table with a count for each command
    - D-06 verdict is "PASS" (0 client leaks) or "FAIL" with blocking failure detail
    - D-10 section records either "Single-mode ship" (with zero dark token count) or "Multi-mode FOUC test result"
    - The combined evidence file concludes with an overall verdict line covering both gates
    - `grep -v "^#" src/app/globals.css | grep -c "\.dark"` result is explicitly recorded in the D-10 section
  </acceptance_criteria>

  <done>`18-05-EVIDENCE.md` complete with D-06 six-row scan table (0 client leaks) and D-10 theme decision (single-mode ship or FOUC result); both gates recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build output → secret scan | Scan targets `.next/static/chunks/` (local) AND `.vercel/output/static/_next/static/chunks/` (what actually deploys); dual-tree is required because Turbopack may produce different chunk splits |
| Server functions → client separation | `NOTION_TOKEN` in `.vercel/output/functions/` is expected and NOT a leak; in `.vercel/output/static/` it IS a blocking leak |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-13 | Information Disclosure | NOTION_TOKEN in client chunk | mitigate | D-14 grep pattern explicitly checks BOTH build trees; any match in client chunks = blocking FAIL; operator notified before GO |
| T-18-14 | Information Disclosure | `secret_` prefix leaking internal identifiers | mitigate | grep -rl with exact pattern `secret_` covers all internal secret prefixes; 0 tolerance in client chunks |
| T-18-15 | Tampering | npm/pip/cargo installs | mitigate | No installs in this plan; slopcheck not required; build output is from 18-01 |
| T-18-SC | Tampering | npm/pip/cargo installs | mitigate | slopcheck not required for this plan (no new installs; reads from existing build output only) |
</threat_model>

<verification>
`18-05-EVIDENCE.md` exists with D-06 and D-10 sections. Run: `grep -q "D-06.*PASS\|D-10.*PASS" ".planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-05-EVIDENCE.md" && echo "Security gates PASS"`.
</verification>

<success_criteria>
D-14 dual-tree secret scan returns 0 client-bundle leaks in both `.next/static/chunks/` and `.vercel/output/static/_next/static/chunks/`; server-only NOTION_TOKEN reference confirmed; D-10 theme mode documented (single-mode Pumpkin Amber ship with 0 dark tokens, no FOUC test required); `18-05-EVIDENCE.md` records both gate verdicts.
</success_criteria>

<output>
Create `.planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-05-SUMMARY.md` when done.
</output>
