---
phase: 18
plan: 05
type: execute
wave: 2
depends_on:
  - 18-01
files_modified:
  - .planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-05-EVIDENCE.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "0 matches for `secret_` in .next/static/chunks/ client bundles"
    - "0 matches for `NOTION_TOKEN` in .next/static/chunks/ client bundles"
    - "0 matches for `secret_` in .vercel/output/static/_next/static/chunks/ client bundles"
    - "0 matches for `NOTION_TOKEN` in .vercel/output/static/_next/static/chunks/ client bundles"
    - "NOTION_TOKEN appears only in server-side artifacts (.vercel/output/functions/)"
    - "v3 ships single-mode (Pumpkin Amber only) — no dark palette, no ThemeProvider, no FOUC risk"
  artifacts:
    - path: .planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-05-EVIDENCE.md
      provides: "D-06 secret scan results (dual-tree), D-10 theme decision (single-mode confirmed)"
      contains: "D-06"
  key_links:
    - from: .next/static/chunks/*.js
      to: 0 secret_ or NOTION_TOKEN hits
      via: grep -r dual-tree
      pattern: "0 matches"
    - from: src/app/globals.css
      to: single-mode Pumpkin Amber ship confirmed
      via: grep for dark selectors
      pattern: "0 .dark selectors"
---

<objective>
Run the D-14 dual-tree secret scan against the build output and record the D-10 theme/FOUC decision.

Purpose: Runs in Wave 2 (parallel with 18-02) because it only needs the `.vercel/output/` tree produced by 18-01, not the preview URL. The secret scan is cheap insurance against a client component accidentally importing a server utility. The theme decision records that v3 ships single-mode (Pumpkin Amber only), so the FOUC incognito test is not required.

Output: `18-05-EVIDENCE.md` — dual-tree grep results table for both search terms, theme decision determination, D-06 and D-10 PASS/FAIL verdicts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-CONTEXT.md
@.planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-01-SUMMARY.md

D-06 pattern (from RESEARCH.md / D-14 original): dual-tree grep for `secret_` AND `NOTION_TOKEN` in:
  Tree 1: .next/static/chunks/*.js (local build artifacts)
  Tree 2: .vercel/output/static/_next/static/chunks/*.js (Vercel remote output)
  Full tree paranoid: .vercel/output/ (entire tree)

D-10 (from CONTEXT.md): v3 is the Pumpkin Amber system. Phase 14 tokens and globals.css confirm single fixed theme. If 0 dark tokens found → record single-mode ship; FOUC test is N/A. If dark tokens found → run FOUC incognito check.

Format analog: `.planning/phases/13-v2-0-qa-go-no-go/13-05-EVIDENCE.md`
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run dual-tree secret scan (D-14 pattern)</name>
  <files>.planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-05-EVIDENCE.md (secret scan section)</files>
  <read_first>
    - `.planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-01-EVIDENCE.md` — confirm DQ-02 PASS; confirms .vercel/output/ tree exists
    - `.planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-CONTEXT.md` — D-06 (dual-tree grep contract)
    - `.planning/phases/13-v2-0-qa-go-no-go/13-05-EVIDENCE.md` — grep command verbatim and results table format
  </read_first>
  <action>
    Confirm .vercel/output/ exists: `test -d .vercel/output/static/_next/static/chunks && echo "EXISTS" || echo "MISSING"`. If MISSING: `vercel build --prod` must be re-run first (18-01 gate must pass and produce this directory).

    Run these commands and capture output:

    Tree 1 — local build (.next/):
    `grep -r "secret_" .next/static/chunks/ 2>/dev/null | grep -v "^Binary" || echo "0 matches"`
    `grep -r "NOTION_TOKEN" .next/static/chunks/ 2>/dev/null | grep -v "^Binary" || echo "0 matches"`

    Tree 2 — Vercel output (.vercel/output/):
    `grep -r "secret_" .vercel/output/static/_next/static/chunks/ 2>/dev/null | grep -v "^Binary" || echo "0 matches"`
    `grep -r "NOTION_TOKEN" .vercel/output/static/_next/static/chunks/ 2>/dev/null | grep -v "^Binary" || echo "0 matches"`

    Full-tree paranoid check:
    `grep -r "secret_" .vercel/output/ 2>/dev/null | grep -v "^Binary\|/functions/" | head -20 || echo "0 matches outside functions/"`

    Server-side confirmation (expected to find NOTION_TOKEN here):
    `grep -r "NOTION_TOKEN" .vercel/output/functions/ 2>/dev/null | grep -v "^Binary" | head -5 || echo "0 server refs"`

    PASS condition: 0 matches in client chunks for both terms in both trees. NOTION_TOKEN may appear only in `.vercel/output/functions/` (server-side, expected and correct).
    FAIL condition: ANY match for `secret_` or `NOTION_TOKEN` in `.next/static/chunks/` OR `.vercel/output/static/_next/static/chunks/` = blocking security failure.
  </action>
  <verify>
    <automated>grep -r "secret_" .next/static/chunks/ 2>/dev/null | grep -v "^Binary" | wc -l && grep -r "NOTION_TOKEN" .next/static/chunks/ 2>/dev/null | grep -v "^Binary" | wc -l && echo "secret_ and NOTION_TOKEN client-chunk counts (expect 0 0)"</automated>
  </verify>
  <done>Both grep commands return 0 client-side matches for both search terms in both build trees. Server-only NOTION_TOKEN refs confirmed in .vercel/output/functions/ only. Secret scan evidence captured.</done>
</task>

<task type="auto">
  <name>Task 2: D-10 theme/FOUC decision</name>
  <files>.planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-05-EVIDENCE.md (theme section)</files>
  <read_first>
    - `src/app/globals.css` — v3 Pumpkin Amber token definitions (check for dark selector presence)
    - `src/app/layout.tsx` or `src/app/providers.tsx` — check for ThemeProvider or next-themes usage
    - `.planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-CONTEXT.md` — D-10 decision logic
  </read_first>
  <action>
    Run:
    `grep -c "\.dark\|data-theme.*dark\|prefers-color-scheme.*dark" src/app/globals.css 2>/dev/null`
    `grep -rc "ThemeProvider\|next-themes\|useTheme" src/app/ 2>/dev/null | grep -v ":0" | head -5`

    The known state (from prior inspection of globals.css, line 4: "Pumpkin Amber palette — v3 single fixed theme, no dark mode"): expect 0 dark selectors and 0 ThemeProvider usage.

    Decision path:
    - If 0 dark selectors AND 0 ThemeProvider → SINGLE-MODE SHIP. Record: "v3.0 ships single-mode (Pumpkin Amber only). No dark v3 palette built. FOUC incognito test not required. Dark Pumpkin Amber variant deferred to v3.1+." D-10 PASS.
    - If dark selectors OR ThemeProvider found → Run FOUC incognito check: open browser in incognito, set system preference to light, visit homepage, confirm no flash; set system preference to dark, confirm correct render. Record results. D-10 PASS if no FOUC, FAIL if FOUC observed.
  </action>
  <verify>
    <automated>grep -c "\.dark\|data-theme.*dark" src/app/globals.css 2>/dev/null && echo "dark selector count (expect 0 for single-mode ship)"</automated>
  </verify>
  <done>Theme mode determination recorded: either single-mode ship confirmed (0 dark selectors, 0 ThemeProvider, FOUC test N/A) or multi-mode FOUC test results documented. D-10 verdict PASS or FAIL.</done>
</task>

<task type="auto">
  <name>Task 3: Write 18-05-EVIDENCE.md</name>
  <files>.planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-05-EVIDENCE.md</files>
  <read_first>
    - Task 1 secret scan outputs
    - Task 2 theme determination
    - `.planning/phases/13-v2-0-qa-go-no-go/13-05-EVIDENCE.md` (format exemplar)
    - `.planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-PATTERNS.md` — 18-05-EVIDENCE.md template
  </read_first>
  <action>
    Create `.planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-05-EVIDENCE.md` following the PATTERNS.md 18-05-EVIDENCE.md template. Two-gate structure:

    Gate 1 — D-06 (D-14 Secret Scan):
    - Requirements text: "Dual-tree grep for secret_ and NOTION_TOKEN returns 0 client-side leaks"
    - Command sequence block (the 5 commands run in Task 1)
    - Results table: Tree | Search Term | Matches | Status
      - .next/static/chunks/ | secret_ | 0 | PASS
      - .next/static/chunks/ | NOTION_TOKEN | 0 | PASS
      - .vercel/output/static/_next/static/chunks/ | secret_ | 0 | PASS
      - .vercel/output/static/_next/static/chunks/ | NOTION_TOKEN | 0 | PASS
      - .vercel/output/ (full tree) | secret_ | 0 | PASS
      - .vercel/output/functions/ | NOTION_TOKEN (expected) | N refs | PASS (server-only, expected)
    - D-06 Verdict: PASS (0 client-side leaks) or FAIL (list matching lines — blocking security issue)

    Gate 2 — D-10 (Theme / FOUC Decision):
    - grep command outputs
    - Finding: "0 .dark selectors in globals.css; 0 ThemeProvider imports in src/app/"
    - Decision: "Single-mode (Pumpkin Amber only) ship. No dark v3 palette built in Phases 14–17. FOUC incognito test not required. Dark variant deferred per CONTEXT.md Deferred Ideas."
    - D-10 Verdict: PASS (single-mode recorded, FOUC N/A)
  </action>
  <verify>
    <automated>test -f .planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-05-EVIDENCE.md && grep -q "D-06" .planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-05-EVIDENCE.md && grep -q "D-10" .planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-05-EVIDENCE.md && echo "18-05-EVIDENCE.md valid with both gates"</automated>
  </verify>
  <done>18-05-EVIDENCE.md exists with D-06 secret scan results table (both trees, both terms), D-10 theme decision (single-mode confirmed), and PASS/FAIL verdicts for both gates.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client JS bundles → browser | .next/static/chunks/ and .vercel/output/.../chunks/ are what browsers download |
| server functions → Vercel runtime | .vercel/output/functions/ is server-only; NOTION_TOKEN is expected here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-05-01 | Information Disclosure | NOTION_TOKEN leaked in client-side JS bundles | mitigate | This plan's dual-tree grep detects any leak; any match in client chunks = blocking fail requiring code fix before GO |
| T-18-05-02 | Information Disclosure | Other secret literals in client bundles | mitigate | `secret_` grep catches common env-var naming patterns; if secret leaks via a different naming convention, it would not be caught — accepted risk for presentation-layer-only phase |
| T-18-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; grep operates on existing build output |
</threat_model>

<verification>
D-06 satisfied when `18-05-EVIDENCE.md` records:
- 0 `secret_` matches in .next/static/chunks/ and .vercel/output/static/_next/static/chunks/
- 0 `NOTION_TOKEN` matches in client chunks
- NOTION_TOKEN found only in .vercel/output/functions/ (server-side, expected)

D-10 satisfied when theme decision is recorded (single-mode Pumpkin Amber ship or FOUC test result).
</verification>

<success_criteria>
`18-05-EVIDENCE.md` exists. D-06 PASS (0 client-side secret leaks). D-10 PASS (single-mode ship recorded or FOUC test clear). Both verdicts available for 18-06 GO/NO-GO compilation.
</success_criteria>

<output>
Create `.planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-05-SUMMARY.md` when done.
</output>
