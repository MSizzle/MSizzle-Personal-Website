---
phase: 18
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-01-EVIDENCE.md
autonomous: true
requirements:
  - DQ-02
must_haves:
  truths:
    - "`vercel build --prod` exits 0 at HEAD of the v3 branch"
    - "Zero TypeScript errors in build output"
    - "Zero ESLint errors in build output"
    - "Zero 429 Notion rate-limit errors in build output"
    - ".vercel/output/static/_next/static/chunks/ tree created for 18-05 secret scan"
  artifacts:
    - path: .planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-01-EVIDENCE.md
      provides: "DQ-02 gate evidence: exit code, error counts, page count, .vercel/output/ confirmation"
      contains: "DQ-02"
  key_links:
    - from: v3 branch HEAD git commit
      to: vercel build --prod
      via: local Vercel build
      pattern: "exit 0"
---

<objective>
Run `vercel build --prod` at the HEAD of the `v3` branch and record the build results.

Purpose: DQ-02 requires the production-readiness gate to pass before any alias swap. `vercel build --prod` catches env-var mismatches and TS/ESLint failures that `next build` misses (v1.0 retro lesson), and it produces `.vercel/output/` for the 18-05 secret scan.

Output: `18-01-EVIDENCE.md` — build result table (exit code, error counts, page count, .vercel/output/ confirmation) and DQ-02 PASS/FAIL verdict.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-CONTEXT.md
@.planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-PATTERNS.md

Format analog for 18-01-EVIDENCE.md: `.planning/phases/13-v2-0-qa-go-no-go/13-01-EVIDENCE.md`
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run vercel build --prod and capture gate data</name>
  <files>.planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-01-EVIDENCE.md (will be created in Task 2)</files>
  <read_first>
    - `.planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-CONTEXT.md` — D-01 build env contract, DQ-02 definition
    - `.planning/phases/13-v2-0-qa-go-no-go/13-01-EVIDENCE.md` — exact format to replicate
  </read_first>
  <action>
    Confirm you are on the v3 branch: `git branch --show-current` must print `v3`. If not, `git checkout v3`.

    Capture HEAD commit SHA: `git rev-parse --short HEAD`.

    Run `vercel build --prod 2>&1 | tee /tmp/18-01-build.log`. Allow up to 10 minutes. The command exits 0 on success, nonzero on failure.

    From `/tmp/18-01-build.log`, extract:
    - Exit code: `echo $?` immediately after the command (or read the tee'd log tail)
    - TypeScript errors: `grep -c "Type error:" /tmp/18-01-build.log` (returns 0 on no errors; `grep -c` returns exit 1 when count=0, which is the success case here)
    - ESLint errors: `grep -c "ESLint:" /tmp/18-01-build.log`
    - 429 errors: `grep -c "429" /tmp/18-01-build.log`
    - Pages generated: `grep "Generating static pages" /tmp/18-01-build.log | tail -1`
    - `.vercel/output/` creation: `test -d .vercel/output/static/_next/static/chunks && echo "YES" || echo "NO"`
    - Build timestamp: `date -u +"%Y-%m-%dT%H:%M:%SZ"`

    A blocking FAIL condition is: exit code nonzero, OR any TypeScript "Type error:", OR any ESLint error, OR any 429 that caused abort. Non-blocking warnings (e.g., Node.js deprecation notices like `[DEP0205]`) are recorded but do not fail the gate.
  </action>
  <verify>
    <automated>cat /tmp/18-01-build.log | tail -5 && test -d .vercel/output/static/_next/static/chunks && echo ".vercel/output tree confirmed"</automated>
  </verify>
  <done>Build log captured at /tmp/18-01-build.log; exit code, TS errors, ESLint errors, 429 count, page count, and .vercel/output/ status all extracted and ready for evidence file.</done>
</task>

<task type="auto">
  <name>Task 2: Write 18-01-EVIDENCE.md with DQ-02 verdict</name>
  <files>.planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-01-EVIDENCE.md</files>
  <read_first>
    - `/tmp/18-01-build.log` (produced by Task 1)
    - `.planning/phases/13-v2-0-qa-go-no-go/13-01-EVIDENCE.md` (format exemplar)
  </read_first>
  <action>
    Create `.planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-01-EVIDENCE.md` following the exact structure of `13-01-EVIDENCE.md`. Sections:

    - Header with Gate ID `DQ-02` and requirement text
    - Result table: Exit Code, Build Date (UTC ISO), Git Commit (HEAD SHA), Next.js Version (read from build log), Pages Generated, TypeScript Errors, ESLint Errors, 429 Errors, .vercel/output/ Created (YES/NO)
    - Error Summary: bullet each category with count (0 = PASS)
    - DQ-02 Verdict: PASS if exit 0 with zero TS errors, zero ESLint errors, zero blocking 429 errors; FAIL otherwise with the specific failure lines quoted.

    PASS verdict text pattern (from 13-01): "`vercel build --prod` exits 0. Zero TS errors, zero ESLint errors, zero 429 errors. N pages generated. `.vercel/output/` confirmed for secret scan (18-05)."

    FAIL verdict must include the exact error lines from the build log and a note that 18-02 through 18-07 are BLOCKED until the build is green.
  </action>
  <verify>
    <automated>test -f .planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-01-EVIDENCE.md && grep -q "DQ-02" .planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-01-EVIDENCE.md && grep -q "Verdict" .planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-01-EVIDENCE.md && echo "18-01-EVIDENCE.md exists with DQ-02 verdict"</automated>
  </verify>
  <done>18-01-EVIDENCE.md exists at the phase directory with DQ-02 gate result, full build metrics table, and PASS or FAIL verdict clearly stated. If FAIL, downstream plans are blocked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local dev → Vercel build | `vercel build --prod` contacts Vercel to pull env vars and remote config |
| build output → `.vercel/output/` | Local build artifacts that will be scanned in 18-05 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Information Disclosure | Client JS bundles in `.vercel/output/static/_next/static/chunks/` | mitigate | 18-05 runs dual-tree grep for `secret_` and `NOTION_TOKEN`; any hit in client chunks is a blocking FAIL |
| T-18-02 | Spoofing | `vercel build --prod` run on wrong branch | mitigate | Verify `git branch --show-current` == `v3` before running build; record HEAD SHA in evidence |
| T-18-SC | Tampering | npm/pip/cargo package installs | accept | No package installs in this plan; existing lockfile used by Vercel build |
</threat_model>

<verification>
DQ-02 is satisfied when `18-01-EVIDENCE.md` records:
- Exit Code: 0
- TypeScript Errors: 0
- ESLint Errors: 0
- 429 Errors: 0
- .vercel/output/ Created: YES

Any nonzero value in errors = FAIL = must resolve before proceeding to Wave 2 plans (18-02, 18-05).
</verification>

<success_criteria>
`18-01-EVIDENCE.md` exists with a PASS verdict for DQ-02. Build exits 0, zero TS/ESLint/429 errors, `.vercel/output/static/_next/static/chunks/` confirmed present for secret scan.
</success_criteria>

<output>
Create `.planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-01-SUMMARY.md` when done.
</output>
