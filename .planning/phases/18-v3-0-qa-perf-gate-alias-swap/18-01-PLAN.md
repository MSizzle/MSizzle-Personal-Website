---
phase: 18-v3-0-qa-perf-gate-alias-swap
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-01-EVIDENCE.md
autonomous: true
requirements:
  - DQ-02

must_haves:
  truths:
    - "`vercel build --prod` exits with code 0 at HEAD of the v3 branch"
    - "Zero TypeScript errors, zero ESLint errors, zero 429 Notion rate-limit errors in build output"
    - "`.vercel/output/` directory is created and available for the 18-05 secret scan"
    - "`18-01-EVIDENCE.md` records exit code, TS/ESLint/429 counts, page count, build SHA, and a PASS/FAIL verdict"
  artifacts:
    - path: ".planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-01-EVIDENCE.md"
      provides: "Build gate evidence record (exit code, TS/ESLint/429 counts, page count, SHA)"
      contains: "DQ-02"
  key_links:
    - from: ".planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-01-EVIDENCE.md"
      to: ".planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-GO-NO-GO.md"
      via: "DQ-02 verdict row"
      pattern: "DQ-02.*PASS"
---

<objective>
Run `vercel build --prod` at HEAD of the `v3` branch to verify the production-readiness gate (DQ-02). Capture build output — exit code, TypeScript error count, ESLint error count, 429 Notion rate-limit error count, pages generated, and build SHA — in a structured `18-01-EVIDENCE.md` artifact.

Purpose: The build gate is the first line of defense before any alias swap. It catches env-var mismatches, type errors, and ESLint violations that `next build` alone misses. A clean exit 0 here is a prerequisite for every downstream gate.

Output: `18-01-EVIDENCE.md` with a PASS/FAIL verdict on DQ-02; `.vercel/output/` ready for the 18-05 secret scan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-CONTEXT.md
@.planning/phases/13-v2-0-qa-go-no-go/13-01-EVIDENCE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run vercel build --prod and capture DQ-02 evidence</name>
  <files>.planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-01-EVIDENCE.md</files>

  <read_first>
    1. `.planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-CONTEXT.md` — D-01 build environment decision (vercel build --prod + preview URL for QA), D-06 secret-scan setup (confirming .vercel/output/ is the artifact of this plan)
    2. `.planning/phases/13-v2-0-qa-go-no-go/13-01-EVIDENCE.md` — exact format and field set to replicate for the 18-01 evidence doc
  </read_first>

  <action>
    Confirm the active branch is `v3`: run `git branch --show-current` and verify output is `v3`. If not, stop and report — do not proceed on the wrong branch.

    Run the build gate: `vercel build --prod 2>&1 | tee /tmp/18-01-build.log`. Let it run to completion (expect ~2 minutes with Turbopack). Do not interrupt. Capture the exit code immediately after: `echo "Exit code: $?"`.

    From `/tmp/18-01-build.log`, extract:
    - Exit code (0 = PASS; non-zero = FAIL)
    - Build date/time range (start and end timestamps from log)
    - Git HEAD SHA at build time: `git rev-parse --short HEAD`
    - Next.js version: grep `Next.js` from log
    - Pages generated: grep `Generating` or `static pages` count
    - TypeScript error count: grep `^Type error:` — count lines; expected 0
    - ESLint error count: grep `^ESLint error\|@typescript-eslint` error lines; expected 0
    - 429 error count: grep `429\|rate.limit\|NOTION.*limit` (case-insensitive); expected 0
    - `.vercel/output/` created: confirm `ls .vercel/output/static/_next/static/chunks/*.js | head -1` returns a path

    Write `18-01-EVIDENCE.md` to `.planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-01-EVIDENCE.md` using the 13-01-EVIDENCE.md format as the exact structure to replicate:
    - Frontmatter: `gate: DQ-02`, `plan: 18-01`, `date: [ISO date]`, `verdict: PASS|FAIL`
    - Header: "18-01 Evidence: vercel build --prod Production Gate"
    - Gate table: exit code, build date, git SHA, Next.js version, pages generated, TS errors, ESLint errors, 429 errors, .vercel/output created
    - Error Summary section listing actual counts (each category must appear even if 0)
    - Non-blocking warnings section (list any DEP0205 or similar non-error log lines)
    - Verdict section: "DQ-02: PASS — exit 0, N pages generated, 0 TS/ESLint/429 errors" OR explicit FAIL description

    If exit code is non-zero: do not write PASS. Write FAIL with the full error excerpt from the log. Stop the plan and report to the operator — this is a blocking failure for all downstream plans.
  </action>

  <verify>
    <automated>test -f ".planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-01-EVIDENCE.md" && grep -q "DQ-02" ".planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-01-EVIDENCE.md" && grep -q "Exit Code" ".planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-01-EVIDENCE.md" && test -d ".vercel/output"</automated>
  </verify>

  <acceptance_criteria>
    - `18-01-EVIDENCE.md` exists and contains the DQ-02 gate table with all six fields (exit code, build date, git SHA, Next.js version, pages generated, TS/ESLint/429 counts)
    - Exit code is 0 (build PASS)
    - TypeScript errors field = 0
    - ESLint errors field = 0
    - 429 errors field = 0
    - `.vercel/output/static/_next/static/chunks/` directory exists (required for 18-05 secret scan)
    - `18-01-EVIDENCE.md` verdict section contains exactly "DQ-02: PASS" or "DQ-02: FAIL" with rationale
    - If FAIL: plan stops; operator is notified before any downstream plans execute
  </acceptance_criteria>

  <done>DQ-02 build gate evaluated; `18-01-EVIDENCE.md` committed with PASS verdict and page count; `.vercel/output/` present on disk for 18-05.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| v3 branch → Vercel build | `vercel build --prod` must run at HEAD of v3 (not main); wrong branch = wrong build evidence |
| Build output → secret scan | `.vercel/output/` produced here is consumed by 18-05; if this directory is absent, the secret scan cannot run |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Tampering | Branch identity at build time | mitigate | Assert `git branch --show-current == v3` before running the build; stop and report if wrong branch |
| T-18-02 | Information Disclosure | Build log captured to /tmp/18-01-build.log | accept | Log is local-only, not committed; contains no secrets (build log may reference env-var names but not values) |
| T-18-03 | Tampering | npm/package installs during build | mitigate | `vercel build --prod` uses the locked `package-lock.json`; no new installs; slopcheck not required here |
</threat_model>

<verification>
`vercel build --prod` exits 0 on the `v3` branch; `18-01-EVIDENCE.md` written with PASS verdict; `.vercel/output/` directory exists. Run: `grep "DQ-02.*PASS" .planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-01-EVIDENCE.md && echo "GATE PASS"`.
</verification>

<success_criteria>
DQ-02 is satisfied: `vercel build --prod` exits 0 at HEAD of v3, zero TS/ESLint/429 errors, `18-01-EVIDENCE.md` records the PASS verdict with full field coverage, and `.vercel/output/` exists for the 18-05 secret scan.
</success_criteria>

<output>
Create `.planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-01-SUMMARY.md` when done.
</output>
