---
phase: 18
plan: 03
type: execute
wave: 3
depends_on:
  - 18-02
files_modified:
  - .planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-03-EVIDENCE.md
autonomous: true
requirements:
  - DQ-03
must_haves:
  truths:
    - "No WebGL/canvas/three import exists on the `/` render path (R-1 confirmed-in-code)"
    - "D-04 LCP sub-checks (mobile-poster path, deferred canvas, fetchPriority on poster) marked satisfied-by-absence"
    - "PSI mobile Performance score ≥ current production baseline (~95) on the v3 preview homepage"
    - "LCP element is the SSR'd <h1> text, not a canvas or image"
    - "DQ-03 PASS or explicit NO-GO recorded"
  artifacts:
    - path: .planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-03-EVIDENCE.md
      provides: "DQ-03 gate evidence: R-1 code inspection result, D-04 satisfied-by-absence block, PSI mobile score and CWV"
      contains: "DQ-03"
  key_links:
    - from: src/components/home/explorative-homepage.tsx
      to: R-1 confirmation (no WebGL)
      via: import analysis
      pattern: "no canvas-loader|no hero-blob|no @react-three"
    - from: v3 preview URL
      to: PSI mobile Performance ≥ 95
      via: pagespeed.web.dev or PSI API
      pattern: "score.*9[5-9]|score.*100"
---

<objective>
Verify R-1 (homepage is a static Server Component with no WebGL) in source code, then run the PSI mobile gate against the v3 preview homepage.

Purpose: DQ-03 requires mobile performance parity-or-better vs current production. The PSI baseline per R-2 (which corrects the stale D-03 figure) is current production PSI mobile ~95 — not 82. The homepage is now a pure static Server Component (Phase 17.1 removed the blob), so D-04's WebGL-LCP sub-checks are satisfied-by-absence. This plan confirms both the code state and the live PSI score.

Output: `18-03-EVIDENCE.md` with code inspection evidence, D-04 satisfied-by-absence block, PSI mobile result table with CWV, and DQ-03 PASS/FAIL verdict.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-CONTEXT.md
@.planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-RESEARCH.md
@.planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-02-SUMMARY.md

PSI baseline per R-2 (AUTHORITATIVE): current production PSI mobile ≈ 95 (PROJECT.md records "PSI mobile 95" at v2.0 ship). The floor is parity-or-better vs current production.
Format analog: `.planning/phases/13-v2-0-qa-go-no-go/13-03-EVIDENCE.md` (adapted with D-04 block)
Memory: `nextjs16-fetchpriority-quirk` — applies only to Image LCP elements; the home LCP is SSR'd text, so N/A.
</context>

<tasks>

<task type="auto">
  <name>Task 1: R-1 code inspection — confirm no WebGL on home render path</name>
  <files>.planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-03-EVIDENCE.md (R-1 section)</files>
  <read_first>
    - `src/app/page.tsx` — the home route entry point
    - `src/components/home/explorative-homepage.tsx` — the home orchestrator (Phase 17.1 rewrote this)
    - `.planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-CONTEXT.md` — R-1 reconciliation (authoritative), D-04 original text
  </read_first>
  <action>
    Read `src/app/page.tsx` and `src/components/home/explorative-homepage.tsx`. For each file, scan every import line.

    Assert the following are ALL absent from the home render path (i.e., not imported directly or via any child component imported at the top level of `explorative-homepage.tsx`):
    - `canvas-loader` (the deferred canvas mount component)
    - `hero-blob-canvas` (the Three.js canvas component)
    - `hero-blob` (the GPU morph shader component)
    - `@react-three/fiber` or `@react-three/drei` or `@react-three/postprocessing`
    - `three` (Three.js)
    - `"use client"` directive in `explorative-homepage.tsx` itself (Server Components have no client directive)

    Confirm the expected LCP element: the `<h1>` in `explorative-homepage.tsx` is SSR'd text. There are no `<Image>` components above the fold that would be the LCP element, so the `nextjs16-fetchpriority-quirk` is not applicable (text LCP has no fetchPriority requirement).

    For the D-04 4-point block, record each point as satisfied-by-absence:
    1. LCP element is text (not canvas) — confirmed: `<h1>` is the LCP; canvas is not on the page at all
    2. Mobile served poster (not WebGL) — N/A: neither WebGL nor poster is on the home path; page is fully static
    3. Canvas mounts after LCP — N/A: no canvas component exists on the home path
    4. fetchPriority="high" on LCP image — N/A: LCP element is text `<h1>`, not an image; no Image component is the LCP

    ESCALATION: If any WebGL/canvas/three import IS found, stop and report — this contradicts R-1 and may require a hot-fix before proceeding.
  </action>
  <verify>
    <automated>grep -rn "canvas-loader\|hero-blob\|@react-three\|\"three\"\|use client" src/app/page.tsx src/components/home/explorative-homepage.tsx 2>/dev/null || echo "R-1 CONFIRMED: no WebGL imports on home render path"</automated>
  </verify>
  <done>R-1 confirmed in code: zero WebGL/canvas/three imports on the `/` render path. ExplorativeHomepage is a Server Component. LCP element is SSR'd `<h1>` text. All 4 D-04 sub-checks documented as satisfied-by-absence.</done>
</task>

<task type="auto">
  <name>Task 2: PSI mobile gate — measure Performance score on v3 preview homepage</name>
  <files>.planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-03-EVIDENCE.md (PSI section)</files>
  <read_first>
    - `.planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-02-EVIDENCE.md` — extract the PREVIEW_URL (this is the only source; do not use production montysinger.com for this gate)
    - `.planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-CONTEXT.md` — D-03 methodology and R-2 floor correction
    - `.planning/PROJECT.md` — confirms v2.0 shipped PSI mobile = 95 (the parity target)
  </read_first>
  <action>
    Read the preview URL from `18-02-EVIDENCE.md`.

    PSI floor per R-2: current production PSI mobile ≈ 95. Floor = parity-or-better vs current production. A score below 95 blocks GO unless explicitly risk-accepted at the 18-06 sign-off. (Ignore the 82/77 figures from D-03 — those are superseded by R-2.)

    Attempt PSI via the Google PageSpeed Insights API (single query to avoid rate-limit risk):
    `curl -s "https://www.googleapis.com/pagespeedonline/v5/runPagespeed?url=PREVIEW_URL&strategy=mobile" -o /tmp/18-03-psi.json`

    If the preview has Deployment Protection, add the bypass token as a query parameter: `?url=PREVIEW_URL%3Fx-vercel-protection-bypass%3DBYPASS_TOKEN&strategy=mobile` (URL-encode the token into the target URL).

    If the API returns HTTP 429 (rate limit), record the 429 in the evidence file and note the manual fallback: open `https://pagespeed.web.dev/?url=PREVIEW_URL&form_factor=mobile` in a browser and capture the Performance score and Core Web Vitals screenshot. The manual reading still satisfies DQ-03.

    From `/tmp/18-03-psi.json`, extract:
    - Performance score: `lighthouseResult.categories.performance.score * 100`
    - LCP element (most important): `lighthouseResult.audits['largest-contentful-paint-element'].displayValue` or the LCP element node in the audit details — confirm it is the `<h1>` text, NOT `<canvas>`
    - FCP, LCP, TBT, CLS, Speed Index values from `lighthouseResult.audits`
    - Test conditions: emulated device, throttling profile, Lighthouse version

    DQ-03 PASS condition: Performance ≥ 95 AND LCP element is text/non-canvas.
    DQ-03 FAIL condition: Performance < 95 (blocks GO unless risk-accepted) OR LCP element is `<canvas>` (always blocking).
  </action>
  <verify>
    <automated>test -f /tmp/18-03-psi.json && python3 -c "import json; d=json.load(open('/tmp/18-03-psi.json')); score=d.get('lighthouseResult',{}).get('categories',{}).get('performance',{}).get('score',0)*100; print(f'PSI mobile Performance: {score:.0f} (floor: 95)')" 2>/dev/null || echo "PSI JSON not yet captured — manual browser run may be needed"</automated>
  </verify>
  <done>PSI mobile Performance score captured (via API or browser manual run), Core Web Vitals recorded, LCP element confirmed as text not canvas. Score compared to ~95 floor and DQ-03 verdict determined.</done>
</task>

<task type="auto">
  <name>Task 3: Write 18-03-EVIDENCE.md with DQ-03 verdict</name>
  <files>.planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-03-EVIDENCE.md</files>
  <read_first>
    - `/tmp/18-03-psi.json` or manual PSI screenshot notes (from Task 2)
    - R-1 code inspection notes (from Task 1)
    - `.planning/phases/13-v2-0-qa-go-no-go/13-03-EVIDENCE.md` (format exemplar)
    - `.planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-PATTERNS.md` — D-04 block template
  </read_first>
  <action>
    Create `.planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-03-EVIDENCE.md` with these sections:

    1. Header: Gate DQ-03, requirement text (PSI mobile parity-or-better vs current production; WebGL 3D object does not regress LCP)

    2. R-1 Code Inspection Result:
       - Finding: ExplorativeHomepage (src/components/home/explorative-homepage.tsx) is a Server Component with zero WebGL/canvas/three imports
       - All imports listed with "ABSENT" confirmation
       - LCP element: SSR'd `<h1>` "Create Order from Chaos" (no Image, no canvas)

    3. D-04 WebGL Hero LCP Verification Block (from PATTERNS.md template):
       - Point 1 (LCP element is text, not canvas): SATISFIED BY ABSENCE — no canvas on home path
       - Point 2 (Mobile served poster, not WebGL): SATISFIED BY ABSENCE — neither WebGL nor poster rendered; pure static SSR
       - Point 3 (Canvas mounts after LCP): SATISFIED BY ABSENCE — no canvas component on home path
       - Point 4 (fetchPriority="high" on LCP image): NOT APPLICABLE — LCP is text `<h1>`, not an image; memory `nextjs16-fetchpriority-quirk` does not apply to text LCP elements
       - Verdict: All 4 D-04 assertions satisfied (by absence); homepage is a plain static Server Component

    4. PSI Mobile Result:
       - Table: Metric | Desktop | Mobile | Threshold | Parity target
       - Performance: [score] | [score] | ≥ 95 mobile | ~95 (v2.0 prod per PROJECT.md)
       - CWV: FCP, LCP, TBT, CLS with traffic-light status
       - Test conditions (device emulation, throttling, Lighthouse version)

    5. DQ-03 Verdict: PASS (Performance ≥ 95 AND LCP = text not canvas) or FAIL with specific value and note that 18-06 GO requires risk-acceptance or remediation.

    PSI floor note: "Per R-2 reconciliation (2026-07-02), the authoritative baseline is current production PSI mobile ≈ 95 (PROJECT.md: 'PSI mobile 95 at v2.0 ship'). The D-03 figure of 82 in CONTEXT.md is stale and superseded."
  </action>
  <verify>
    <automated>test -f .planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-03-EVIDENCE.md && grep -q "DQ-03" .planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-03-EVIDENCE.md && grep -q "satisfied-by-absence\|SATISFIED BY ABSENCE" .planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-03-EVIDENCE.md && echo "18-03-EVIDENCE.md valid with R-1 and D-04 blocks"</automated>
  </verify>
  <done>18-03-EVIDENCE.md exists with R-1 code inspection result, D-04 4-point satisfied-by-absence block, PSI mobile Performance score vs ~95 floor, CWV metrics, and DQ-03 PASS or FAIL verdict.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source code → import analysis | Reading local files; no network boundary |
| local → PSI API (googleapis.com) | HTTPS request to Google's PSI API; unauthenticated |
| preview URL → PSI crawler | Google's servers crawl the Vercel preview |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-03-01 | Information Disclosure | PSI API call includes the preview URL (which may be behind protection) | accept | Preview URL is not sensitive; Vercel protection bypass token in URL is ephemeral and scoped to QA |
| T-18-03-02 | Spoofing | PSI measured against production montysinger.com instead of preview | mitigate | Explicitly read preview URL from 18-02-EVIDENCE.md; never substitute montysinger.com for the pre-promote gate |
| T-18-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
DQ-03 satisfied when `18-03-EVIDENCE.md` records:
- R-1 confirmed: zero WebGL/canvas/three imports on `/` render path
- D-04 4-point block: all 4 assertions satisfied-by-absence with rationale
- PSI mobile Performance ≥ 95 (current production parity)
- LCP element confirmed as `<h1>` text (not canvas)

A score below 95 is a blocking issue unless explicitly risk-accepted at 18-06.
</verification>

<success_criteria>
`18-03-EVIDENCE.md` exists. R-1 confirmed in code. D-04 block documents satisfied-by-absence for all 4 sub-checks. PSI mobile Performance score recorded against the ~95 parity floor with DQ-03 PASS or FAIL verdict.
</success_criteria>

<output>
Create `.planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-03-SUMMARY.md` when done.
</output>
