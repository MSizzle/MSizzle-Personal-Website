---
phase: 18
plan: 07
type: execute
wave: 5
depends_on:
  - 18-06
files_modified: []
autonomous: false
requirements:
  - DQ-04
must_haves:
  truths:
    - "Exact promotion command sequence handed to Monty (per R-5)"
    - "Monty has run git merge v3→main and git push origin main (or Path A fallback)"
    - "Post-promote: curl -I montysinger.com returns HTTP 200"
    - "Post-promote: montysinger.com HTML contains v3 photo-forward markers (marker-block hero text: 'Create Order' / 'from Chaos' / 'Founder of Prometheus'; Ink & Vermilion palette)"
    - "No alias drift: vercel list --prod=true shows a recent deployment within 10 min of push"
    - "/gsd-complete-milestone v3.0 invoked immediately upon confirmed GO"
  artifacts:
    - path: .planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-GO-NO-GO.md
      provides: "DQ-04 evidence updated: post-promote curl result, alias-drift confirmation, milestone close timestamp"
      contains: "verdict: GO"
  key_links:
    - from: signed 18-GO-NO-GO.md
      to: git push origin main → Vercel auto-deploy → montysinger.com
      via: Monty runs the merge and push
      pattern: "HTTP/2 200"
    - from: montysinger.com
      to: v3 photo-forward hero text markers
      via: curl -s grep
      pattern: "Create Order|from Chaos|Founder of Prometheus"
---

<objective>
Hand off the exact alias promotion commands to Monty, then run post-promotion parity verification and close the v3.0 milestone.

Purpose: R-5 requires the alias flip to be human-run (Monty), with the agent preparing the exact commands. After Monty completes the flip, the agent verifies that montysinger.com serves v3 correctly (HTTP 200, v3 markers, no alias drift), updates 18-GO-NO-GO.md with DQ-04 evidence, and immediately invokes /gsd-complete-milestone v3.0 per D-09 (run at GO verdict, not weeks later).

This is the second of exactly two `autonomous: false` plans in Phase 18.

Output: Completed DQ-04 row in 18-GO-NO-GO.md; milestone close.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-CONTEXT.md
@.planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-RESEARCH.md
@.planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-06-SUMMARY.md
@.planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-GO-NO-GO.md

R-5 (AUTHORITATIVE): Agent compiles commands; Monty runs. Agent does NOT execute git push or vercel deploy.
D-08 hard constraints: NEVER vercel deploy --prebuilt --prod; mandatory alias-drift check after promote.
D-09: Run /gsd-complete-milestone v3.0 IMMEDIATELY at GO verdict — not later.
Alias verification commands (from RESEARCH.md Path B): curl -sS -I montysinger.com; grep for v3 markers; vercel list --prod=true.
Memory: vercel-prod-deploy-gotchas — alias drift after vercel deploy --prod; verify via dashboard.
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Monty runs alias promotion (agent-prepared, human-executed)</name>
  <what-built>The agent has verified the GO verdict in 18-GO-NO-GO.md is signed. The exact promotion commands are in the Promotion Plan block of 18-GO-NO-GO.md.</what-built>
  <how-to-verify>
    Read the Promotion Plan block from `.planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-GO-NO-GO.md` (the "MONTY RUNS THESE COMMANDS" section).

    Then run these commands in your terminal (EXACTLY as written — no variations):

    ```
    git checkout v3
    git rev-parse HEAD
    # Confirm this SHA matches the build_sha in 18-GO-NO-GO.md frontmatter

    git checkout main
    git pull origin main
    git merge v3 --no-ff -m "chore(release): merge v3 for v3.0 production deployment [Phase 18 GO]"
    git push origin main
    ```

    Watch the Vercel dashboard: https://vercel.com/msizzles-projects/m-sizzle-personal-website/deployments
    Wait until the deployment status shows "Ready" (typically 2-5 minutes).

    PATH A FALLBACK: If Vercel auto-deploy from `main` is NOT configured, run instead:
    `git checkout v3 && vercel deploy --prod`
    (NEVER use --prebuilt --prod)

    After the deployment is "Ready" on the dashboard, return here and resume.
  </how-to-verify>
  <resume-signal>Type "promoted" once the Vercel deployment shows "Ready" and montysinger.com is loading (even if you haven't visually confirmed v3 yet). The agent will run the automated post-promote verification next.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Post-promote parity verification and milestone close</name>
  <files>.planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-GO-NO-GO.md (DQ-04 row update)</files>
  <read_first>
    - `.planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-GO-NO-GO.md` — read DQ-04 row (currently PENDING) to update with post-promote evidence
    - `.planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-RESEARCH.md` — Path B alias verification commands
  </read_first>
  <action>
    Wait if needed, then run alias-drift verification (per D-08 mandatory alias-drift check):

    1. HTTP smoke test: `curl -sS -o /dev/null -w "HTTP %{http_code} | size %{size_download}b\n" https://montysinger.com`
       Expected: HTTP 200, response size ~14KB+ (v3 is a richer page than v2.0)

    2. v3 marker check: `curl -s https://montysinger.com | grep -o "Create Order\|from Chaos\|Founder of Prometheus" | head -3`
       Expected: at least one v3 marker. The marker-block hero splits the headline across two spans ("Create Order" in Vermilion + "from Chaos" in ink), so grep the halves separately — NOT the joined string. The accent hex `#e5411f` is in the bundled CSS, not inline HTML, so it is not a reliable page-HTML marker.

    3. Deployment recency check: `vercel list --prod=true | head -10`
       Expected: the m-sizzle-personal-website production deployment is dated within the last 10 minutes

    4. Feed check: `curl -sS -o /dev/null -w "%{http_code}" https://montysinger.com/blog/feed.xml`
       Expected: 200

    ALIAS DRIFT DETECTION: If checks 1-3 show v2.0 content is still served 10+ minutes after the git push, the alias has drifted. Execute the rollback move from 18-GO-NO-GO.md immediately and stop. Report to Monty with the drift evidence.

    On CONFIRMED PASS (HTTP 200 + v3 markers + recent deployment):
    - Update `18-GO-NO-GO.md` DQ-04 row:
      | DQ-04 | Alias promoted to v3, post-promote verified | ✅ PASS | Post-promote: HTTP 200 + v3 markers confirmed at [timestamp]; deployment ID [ID]; curl evidence recorded |
    - Update frontmatter: status → signed (already done in 18-06), add a post-promote note with timestamp

    Invoke milestone close (per D-09 — immediately at GO verdict, not later):
    `/gsd-complete-milestone v3.0`

    Record the milestone close invocation timestamp in 18-07-SUMMARY.md.
  </action>
  <verify>
    <automated>curl -sS -o /dev/null -w "%{http_code}" https://montysinger.com 2>/dev/null && curl -s https://montysinger.com 2>/dev/null | grep -o "Create Order from Chaos\|Founder of Prometheus" | head -1 || echo "v3 marker not yet confirmed"</automated>
  </verify>
  <done>Post-promote verified: montysinger.com returns HTTP 200, v3 palette/content markers confirmed, no alias drift, deployment recency confirmed. 18-GO-NO-GO.md DQ-04 row updated to PASS with evidence. /gsd-complete-milestone v3.0 invoked. v3.0 Dark Brutalist Rebuild is SHIPPED.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Monty's terminal → git push origin main | Code change crossing the prod boundary; irreversible once deployed |
| Vercel auto-deploy → montysinger.com | Build-to-production alias binding; must not drift |
| agent curl → montysinger.com | Agent verifies the live production site post-promote |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-07-01 | Tampering | Alias drift — production alias points to old v2.0 deployment after merge push | mitigate | Mandatory post-promote drift check (HTTP 200 + v3 markers + deployment recency); rollback move in 18-GO-NO-GO.md if drift detected |
| T-18-07-02 | Tampering | Monty runs `--prebuilt --prod` variant instead of clean deploy | mitigate | Plan explicitly lists forbidden command variant; only the exact merge+push or `vercel deploy --prod` commands are permitted |
| T-18-07-03 | Elevation of Privilege | Agent executing git push without Monty's consent | mitigate | Task 1 is `checkpoint:human-action`; agent waits for "promoted" resume signal; git commands are in the "MONTY RUNS" block |
| T-18-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
DQ-04 satisfied when:
- `18-GO-NO-GO.md` DQ-04 row shows PASS with post-promote curl evidence
- `curl -I https://montysinger.com` returns HTTP 200
- `curl -s https://montysinger.com` contains a v3 content marker ("Create Order" / "from Chaos" / "Founder of Prometheus")
- `vercel list --prod=true` shows a deployment created within 10 minutes of the git push
- `/gsd-complete-milestone v3.0` invoked and confirmed
</verification>

<success_criteria>
montysinger.com serves the v3 site at HTTP 200 with v3 content markers. No alias drift. 18-GO-NO-GO.md DQ-04 row updated to PASS. /gsd-complete-milestone v3.0 invoked. v3.0 Dark Brutalist Rebuild is officially shipped.
</success_criteria>

<output>
Create `.planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-07-SUMMARY.md` when done.
</output>
