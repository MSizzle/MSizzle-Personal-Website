---
phase: 18-v3-0-qa-perf-gate-alias-swap
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-02-EVIDENCE.md
autonomous: true
requirements:
  - DQ-03

must_haves:
  truths:
    - "A v3 Vercel preview URL is deployed and reachable over HTTPS"
    - "Lighthouse desktop median-of-3 scores are recorded for all six named routes"
    - "Every route meets the threshold: Performance >= 90, Accessibility >= 95, Best Practices >= 95, SEO = 100"
    - "`18-02-EVIDENCE.md` records the preview URL, per-run raw scores, medians, and a PASS/FAIL per route"
    - "The preview URL is available for 18-03 (PSI mobile) and 18-04 (visual QA) — both Wave 2 plans"
  artifacts:
    - path: ".planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-02-EVIDENCE.md"
      provides: "Lighthouse desktop evidence + preview URL for downstream Wave-2 plans"
      contains: "preview URL"
  key_links:
    - from: ".planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-02-EVIDENCE.md"
      to: ".planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-03-PLAN.md"
      via: "Preview URL consumed by PSI mobile gate"
      pattern: "https://m-sizzle-personal-website"
    - from: ".planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-02-EVIDENCE.md"
      to: ".planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-GO-NO-GO.md"
      via: "D-02 Lighthouse verdict row"
      pattern: "D-02.*PASS"
---

<objective>
Deploy a Vercel preview of the `v3` branch and run Lighthouse desktop median-of-3 against six named routes. Capture the preview URL (a Wave-0 dependency for 18-03 and 18-04) and record raw per-run scores and medians in `18-02-EVIDENCE.md`.

Purpose: The preview URL provides an unauthenticated, production-equivalent target for Lighthouse and PSI runs — closer to real user conditions than localhost, without touching the production alias. Lighthouse desktop (median-of-3) is the authoritative desktop scoring methodology per D-02 (local mobile variance ±15pt means desktop-only Lighthouse is the gate; PSI is the separate mobile gate).

Output: `18-02-EVIDENCE.md` with preview URL + per-route median scores; preview URL surfaced for Wave-2 plans.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-CONTEXT.md
@.planning/phases/13-v2-0-qa-go-no-go/13-02-EVIDENCE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deploy v3 Vercel preview and capture preview URL</name>
  <files>.planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-02-EVIDENCE.md</files>

  <read_first>
    1. `.planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-CONTEXT.md` — D-02 Lighthouse methodology (desktop preset, median-of-3, named routes, no local mobile gate)
    2. `.planning/phases/13-v2-0-qa-go-no-go/13-02-EVIDENCE.md` — the exact table structure to replicate for per-route raw scores and medians
  </read_first>

  <action>
    Confirm active branch is `v3`: run `git branch --show-current`. Stop and report if not `v3`.

    Deploy a Vercel preview (NOT production): `vercel deploy 2>&1 | tee /tmp/18-02-deploy.log`. This deploys the v3 branch as a preview URL. Do NOT pass `--prod`. The command outputs a preview URL on the last line of stdout — extract it: `grep "https://" /tmp/18-02-deploy.log | tail -1`. It will be in the form `https://m-sizzle-personal-website-XXXXXXXX-msizzles-projects.vercel.app`. Store this URL — it is the Wave-0 dependency for 18-03 and 18-04.

    If the project has Vercel Deployment Protection (password or Vercel auth), add the bypass header to Lighthouse runs: check `.planning/phases/13-v2-0-qa-go-no-go/13-GO-NO-GO.md` for how Phase 13 handled this (BYPASS token in extra-headers). If protection is enabled, the operator must provide a BYPASS token from the Vercel project dashboard at Settings → Deployment Protection → Bypass for Automation Secret.

    Note: 18-01 and 18-02 are both Wave 1 and can run in parallel but 18-01 produces `.vercel/output/` which exists from a prior local `vercel build --prod` run. This Vercel preview deploy is a separate remote build triggered by the `vercel deploy` command — Vercel builds on its infrastructure. The preview URL and the local `.vercel/output/` from 18-01 are different artifacts; both are needed.
  </action>

  <verify>
    <automated>grep -q "https://m-sizzle-personal-website" /tmp/18-02-deploy.log && echo "Preview URL found"</automated>
  </verify>

  <acceptance_criteria>
    - `vercel deploy` exits 0 and outputs a preview URL
    - Preview URL is in the form `https://m-sizzle-personal-website-*-msizzles-projects.vercel.app`
    - `curl -o /dev/null -s -w "%{http_code}" <preview-url>` returns 200 (or 401 if protection enabled)
    - Preview URL is recorded in `18-02-EVIDENCE.md` as the first data point before Lighthouse runs
  </acceptance_criteria>

  <done>Preview URL deployed and accessible; recorded as the Wave-0 dependency for 18-03 and 18-04.</done>
</task>

<task type="auto">
  <name>Task 2: Lighthouse desktop median-of-3 on 6 named routes</name>
  <files>.planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-02-EVIDENCE.md</files>

  <read_first>
    1. `.planning/phases/13-v2-0-qa-go-no-go/13-02-EVIDENCE.md` — per-run raw score table and median calculation format; thresholds (Perf ≥ 90 / A11y ≥ 95 / BP ≥ 95 / SEO = 100)
    2. `.planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-CONTEXT.md` — D-02 named routes: `/`, `/about`, a Notion-backed index, a Notion-backed detail page, `/uses`, `/watching`
  </read_first>

  <action>
    Routes to measure (6 total, desktop preset):
    1. `/` — homepage (WebGL hero; highest perf risk)
    2. `/about` — static interior page
    3. `/writing` — Notion-backed index
    4. `/blog/[slug]` — use a real slug from the published essay list (check the writing index for a live slug); Notion-backed detail
    5. `/uses` — new page (Phase 16)
    6. `/watching` — new page (Phase 16)

    For each route, run Lighthouse 3 times in sequence (median requires 3 runs to be meaningful). Use the Lighthouse CLI installed as a dev dependency:

    `npx lighthouse <preview-url><route> --preset=desktop --output=json --output-path=/tmp/18-lh-<route>-run<N>.json --quiet --chrome-flags="--headless"`

    If Vercel Deployment Protection is active, add: `--extra-headers='{"x-vercel-protection-bypass":"<BYPASS_TOKEN>"}'`

    After all 3 runs per route, extract Performance / Accessibility / Best Practices / SEO from each JSON:
    `node -e "const r = require('/tmp/18-lh-<route>-run1.json'); console.log(r.categories.performance.score*100, r.categories.accessibility.score*100, r.categories['best-practices'].score*100, r.categories.seo.score*100)"`

    Compute median of 3 per metric per route. A median is the middle value when sorted ascending.

    Thresholds (inherited from Phase 13 D-02 pattern):
    - Performance: >= 90
    - Accessibility: >= 95
    - Best Practices: >= 95
    - SEO: = 100

    Write `18-02-EVIDENCE.md` with:
    - Preview URL at top
    - Per-route section with: raw run scores (3 runs × 4 metrics), median row, threshold row, PASS/FAIL verdict
    - Summary table: all 6 routes × 4 medians × verdict
    - Overall verdict: PASS if all routes meet all thresholds; FAIL listing specific route+metric failures
    - If any route FAILS: record the specific failure and assess whether it is blocking (Performance or Accessibility failure blocks GO; note in evidence)

    If Lighthouse produces accessibility findings below threshold, record the specific audit IDs that failed (visible in the JSON `audits` object where `score` is 0 or null) — this context is needed for the 18-GO-NO-GO.md known-limitations section if any issues are accepted.
  </action>

  <verify>
    <automated>test -f ".planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-02-EVIDENCE.md" && grep -q "Preview URL" ".planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-02-EVIDENCE.md" && grep -q "median" ".planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-02-EVIDENCE.md"</automated>
  </verify>

  <acceptance_criteria>
    - `18-02-EVIDENCE.md` exists and contains the preview URL
    - All 6 routes have 3 raw Lighthouse runs recorded with Performance / Accessibility / Best Practices / SEO per run
    - Each route has a computed median row
    - Each route has a PASS/FAIL verdict against thresholds (Perf ≥ 90 / A11y ≥ 95 / BP ≥ 95 / SEO = 100)
    - Overall verdict section present: "D-02: PASS" or "D-02: FAIL — [route/metric]"
    - Preview URL is surfaced prominently so 18-03 and 18-04 can consume it without searching the file
  </acceptance_criteria>

  <done>Lighthouse desktop median-of-3 complete for all 6 routes; `18-02-EVIDENCE.md` committed with preview URL and overall PASS/FAIL verdict; preview URL available for 18-03 PSI and 18-04 visual QA.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| v3 branch → Vercel preview | `vercel deploy` (no --prod) must target the v3 branch; wrong branch = testing the wrong build |
| Vercel preview → Lighthouse | Lighthouse must hit the actual preview URL; localhost runs miss CDN/Edge behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-04 | Tampering | Branch identity at deploy time | mitigate | Confirm `git branch --show-current == v3` before `vercel deploy`; stop if wrong |
| T-18-05 | Spoofing | Lighthouse measuring wrong URL | mitigate | Record the exact preview URL in 18-02-EVIDENCE.md; Lighthouse commands use the captured URL verbatim |
| T-18-06 | Denial of Service | Vercel Deployment Protection blocks unauthenticated Lighthouse | accept | If protection is active, use BYPASS token in --extra-headers; no fallback needed (token available in Vercel dashboard) |
</threat_model>

<verification>
Preview URL is in `18-02-EVIDENCE.md`; all 6 routes have median Lighthouse scores recorded. Run: `grep -c "PASS\|FAIL" ".planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-02-EVIDENCE.md"` — should return 7+ (one per route + overall).
</verification>

<success_criteria>
A v3 Vercel preview URL is deployed and available for downstream gates; Lighthouse desktop median-of-3 is recorded for all 6 named routes; `18-02-EVIDENCE.md` has an overall D-02 PASS/FAIL verdict with per-route breakdown.
</success_criteria>

<output>
Create `.planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-02-SUMMARY.md` when done. The preview URL MUST appear in the SUMMARY.md `provides` field so 18-03 and 18-04 can find it.
</output>
