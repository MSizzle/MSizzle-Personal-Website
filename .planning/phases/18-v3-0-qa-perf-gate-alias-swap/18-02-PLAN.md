---
phase: 18
plan: 02
type: execute
wave: 2
depends_on:
  - 18-01
files_modified:
  - .planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-02-EVIDENCE.md
autonomous: true
requirements:
  - DQ-04
must_haves:
  truths:
    - "A v3 Vercel preview URL is captured and serving the v3 branch"
    - "Lighthouse desktop median-of-3 recorded for routes: /, /about, /writing, /uses, /portfolio"
    - "All 5 routes score Perf ≥ 90 / A11y ≥ 95 / Best Practices ≥ 95 / SEO = 100"
    - "Bypass header obtained if preview has Deployment Protection enabled"
  artifacts:
    - path: .planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-02-EVIDENCE.md
      provides: "Lighthouse desktop evidence: per-route median scores and raw runs; preview URL for 18-03 and 18-04"
      contains: "preview URL"
  key_links:
    - from: v3 branch HEAD
      to: Vercel preview URL
      via: vercel deploy (no --prod)
      pattern: "https://m-sizzle-personal-website-*.vercel.app"
    - from: Vercel preview URL
      to: Lighthouse CLI desktop median-of-3
      via: npx lighthouse --preset=desktop
      pattern: "Performance.*[9][0-9]"
---

<objective>
Confirm a live v3 Vercel preview URL and run Lighthouse CLI desktop median-of-3 on 5 representative routes.

Purpose: Establishes the preview URL needed by 18-03 (PSI) and 18-04 (visual QA); Lighthouse desktop scores are supporting evidence for the GO verdict. Routes use the CURRENT v3 route set per R-3 — /watching and /newsletter are 301 redirects and must NOT be Lighthouse'd. /portfolio (Phase 17.3) IS in scope.

Output: `18-02-EVIDENCE.md` — preview URL, bypass header status, per-route median table, raw per-run scores, overall verdict.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-CONTEXT.md
@.planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-01-SUMMARY.md
@.planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-RESEARCH.md

Format analog: `.planning/phases/13-v2-0-qa-go-no-go/13-02-EVIDENCE.md`

Phase 13 Lighthouse used bypass header: `--extra-headers='{"x-vercel-protection-bypass":"TOKEN"}'`
Phase 13 preview URL format: `https://m-sizzle-personal-website-[HASH]-msizzles-projects.vercel.app`
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm or create v3 Vercel preview; capture URL and bypass token</name>
  <files>.planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-02-EVIDENCE.md (header section)</files>
  <read_first>
    - `.planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-01-EVIDENCE.md` — confirm DQ-02 PASS before proceeding (DO NOT run Lighthouse if build gate failed)
    - `.planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-CONTEXT.md` — D-01 (build environment decision), D-08 (never --prebuilt --prod)
  </read_first>
  <action>
    Verify 18-01-EVIDENCE.md shows a PASS verdict. Stop and report if DQ-02 is FAIL.

    Find the latest v3 preview using `vercel list 2>/dev/null | head -20` — look for a recent deployment on the v3 branch (Ready status, created within the last 24h). If a fresh preview exists, capture its URL.

    If no recent preview exists, create one: `git checkout v3 && vercel deploy 2>&1 | tee /tmp/18-02-deploy.log`. Never use `--prod` or `--prebuilt`. The preview URL appears in the output; extract it.

    Deployment Protection check: Run `curl -sS -o /dev/null -w "%{http_code}" PREVIEW_URL/` — if this returns 401 or shows the Vercel protection page, Deployment Protection is enabled. Obtain the bypass token from Vercel Project Settings → Deployment Protection → "Protection Bypass for Automation" (copy the secret). Store as BYPASS_TOKEN for Task 2.

    Record: preview URL, bypass token status (enabled/disabled), deploy timestamp.
  </action>
  <verify>
    <automated>curl -sS -o /dev/null -w "%{http_code}" "$PREVIEW_URL/" 2>/dev/null || echo "Set PREVIEW_URL first"</automated>
  </verify>
  <done>Preview URL captured and serving 200 (or 401 with bypass token known). URL recorded for use in Tasks 2 and as canonical reference in 18-02-EVIDENCE.md for downstream plans 18-03 and 18-04.</done>
</task>

<task type="auto">
  <name>Task 2: Lighthouse desktop median-of-3 on 5 routes</name>
  <files>.planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-02-EVIDENCE.md</files>
  <read_first>
    - `.planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-CONTEXT.md` — D-02 (desktop-only, median-of-3, no local mobile gate, thresholds ≥90/95/95/100)
    - `.planning/phases/13-v2-0-qa-go-no-go/13-02-EVIDENCE.md` — exact score table format to replicate
  </read_first>
  <action>
    Routes to test (per R-3 — corrected v3 route set): `/`, `/about`, `/writing`, `/uses`, `/portfolio`. Do NOT include `/watching`, `/newsletter`, `/events`, `/photos`, or `/links` — these are 301 redirects and Lighthouse on a redirect produces garbage scores.

    For each of the 5 routes, run 3 Lighthouse desktop runs. Command per run:
    `npx lighthouse PREVIEW_URL/ROUTE --preset=desktop --output=json --output-path=/tmp/lh-ROUTE-N.json --quiet [--extra-headers='{"x-vercel-protection-bypass":"BYPASS_TOKEN"}' if protected]`

    Where ROUTE is one of: (home = empty path, about, writing, uses, portfolio). N is 1, 2, 3.

    After each route's 3 runs, extract from the JSON:
    - Performance: `categories.performance.score * 100`
    - Accessibility: `categories.accessibility.score * 100`
    - Best Practices: `categories['best-practices'].score * 100`
    - SEO: `categories.seo.score * 100`

    Compute median of each 3-run set. Thresholds per D-02: Perf ≥ 90 / A11y ≥ 95 / BP ≥ 95 / SEO = 100.

    Build the results into a per-route table: route | Perf | A11y | BP | SEO | Verdict (PASS/FAIL). Also record raw 3-run scores per route.

    A route FAILS if any median is below threshold. Overall gate FAILS if any route fails.
  </action>
  <verify>
    <automated>ls /tmp/lh-*.json 2>/dev/null | wc -l && echo "JSON files (expect 15 = 5 routes x 3 runs)"</automated>
  </verify>
  <done>15 Lighthouse JSON files exist (/tmp/lh-*-{1,2,3}.json) for all 5 routes. Median scores computed; every route meets Perf ≥ 90 / A11y ≥ 95 / BP ≥ 95 / SEO = 100 or specific failures are documented.</done>
</task>

<task type="auto">
  <name>Task 3: Write 18-02-EVIDENCE.md</name>
  <files>.planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-02-EVIDENCE.md</files>
  <read_first>
    - `/tmp/lh-*.json` (produced by Task 2)
    - `.planning/phases/13-v2-0-qa-go-no-go/13-02-EVIDENCE.md` (format exemplar)
  </read_first>
  <action>
    Create `.planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-02-EVIDENCE.md` following `13-02-EVIDENCE.md` structure. Sections:

    - Header with gate D-02 and requirements text
    - Preview URL (bold — referenced by 18-03 and 18-04)
    - Bypass header status (enabled/disabled; if enabled, note token source)
    - Final Median Scores table: route | Perf | A11y | BP | SEO | Verdict | Delta vs initial (if polish pass needed)
    - Thresholds line: "Performance ≥ 90 / Accessibility ≥ 95 / Best Practices ≥ 95 / SEO = 100"
    - Raw Per-Run Scores table: route | Perf runs (3 values) | A11y runs | BP runs | SEO runs
    - Overall D-02 Verdict: PASS (all 5 routes clear) or FAIL (list failing routes and remediation needed)

    If any route fails, record specific metric values and note that remediation is needed before 18-06 GO sign-off; 18-03 and 18-04 can still proceed since they use the preview URL regardless.
  </action>
  <verify>
    <automated>test -f .planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-02-EVIDENCE.md && grep -q "Preview URL" .planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-02-EVIDENCE.md && grep -q "Verdict" .planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-02-EVIDENCE.md && echo "18-02-EVIDENCE.md valid"</automated>
  </verify>
  <done>18-02-EVIDENCE.md exists with: preview URL prominently recorded (for downstream use), per-route median table, raw 3-run scores, and overall D-02 PASS/FAIL verdict.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local → Vercel preview URL | Network call; preview may be behind Deployment Protection |
| Lighthouse CLI → preview | Headless Chrome crawls the preview URL (unauthenticated or bypass header) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-02-01 | Spoofing | Lighthouse running against wrong URL (e.g., production montysinger.com instead of preview) | mitigate | Always construct URL as PREVIEW_URL + ROUTE; do not substitute montysinger.com |
| T-18-02-02 | Tampering | Vercel Deployment Protection bypass token exposed in Lighthouse CLI invocation shell history | accept | Bypass token is not a secret in the security-sensitive sense; it's a temporary test token |
| T-18-SC | Tampering | npm/pip/cargo installs | accept | `npx lighthouse` uses already-installed package; no new installs |
</threat_model>

<verification>
Gate D-02 satisfied when `18-02-EVIDENCE.md` contains:
- A preview URL for the v3 branch
- Median Lighthouse desktop scores: Perf ≥ 90, A11y ≥ 95, BP ≥ 95, SEO = 100 on routes /, /about, /writing, /uses, /portfolio
- Raw 3-run data for audit

The preview URL in this file is the canonical reference for plans 18-03 and 18-04.
</verification>

<success_criteria>
`18-02-EVIDENCE.md` exists with a preview URL and either a PASS verdict (all 5 routes meet thresholds) or a clear FAIL listing specific metrics and remediation needed. Preview URL confirmed accessible.
</success_criteria>

<output>
Create `.planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-02-SUMMARY.md` when done.
</output>
