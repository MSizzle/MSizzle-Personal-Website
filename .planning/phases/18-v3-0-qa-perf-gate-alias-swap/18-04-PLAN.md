---
phase: 18
plan: 04
type: execute
wave: 3
depends_on:
  - 18-02
files_modified:
  - .planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-04-EVIDENCE.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "All live v3 routes return HTTP 200 on the preview"
    - "All 5 expected 301 redirects (/watching, /newsletter, /events, /photos, /links) resolve correctly"
    - "/portfolio shows real Notion Featured cards (not the empty-state) — R-4 content gate"
    - "Homepage 'Selected Work' section links to /portfolio"
    - "/uses page contains the Watching section (Things I Love — videos)"
    - "Phase 16 deferred 4-item checklist documented with automated evidence"
  artifacts:
    - path: .planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-04-EVIDENCE.md
      provides: "Route health table, redirect table, R-4 portfolio content gate, /uses Watching section check, Phase 16 deferred checklist"
      contains: "R-4"
  key_links:
    - from: PREVIEW_URL/portfolio
      to: Notion Featured cards rendered (not empty-state)
      via: curl HTML grep for card/featured content
      pattern: "card|featured|project"
    - from: PREVIEW_URL/uses
      to: Watching section presence
      via: curl HTML grep
      pattern: "watch|video|love"
    - from: PREVIEW_URL/ (homepage)
      to: /portfolio link in Selected Work section
      via: curl HTML grep
      pattern: "portfolio|selected"
---

<objective>
Automated route health checks, redirect verification, R-4 portfolio content gate, and Phase 16 deferred checklist assessment. Runs in parallel with 18-03 (both depend only on 18-02's preview URL).

Purpose: Completes the Phase 16 deferred visual checkpoint (per 16-09-SUMMARY.md Task 2) via automated curl/Lighthouse checks. R-4 requires /portfolio to show real Featured cards before promotion so production is not empty at launch. Monty reviews this evidence at the 18-06 GO/NO-GO sign-off before signing.

Note: /watching is NOT walked as a standalone page — it is a 301 redirect. The watching content is in the /uses "Things I Love" section (R-3). The R-4 portfolio content gate requires Monty to mark a few Notion projects `Featured: true` BEFORE the alias flip if /portfolio shows the empty-state.

Output: `18-04-EVIDENCE.md` — route health table, redirect table, R-4 content gate result, Phase 16 checklist, Lighthouse mobile screenshots path.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-CONTEXT.md
@.planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-02-SUMMARY.md
@.planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-PATTERNS.md

R-3 (AUTHORITATIVE): Live v3 routes = /, /about, /projects, /projects/[slug], /portfolio, /writing, /blog/[slug], /uses, /prometheus. Do NOT walk /watching or /newsletter as pages — they are 301 redirects. /watching → /uses (fold), /newsletter → /writing (fold).

R-4 (AUTHORITATIVE): /portfolio shows a graceful empty-state until Notion projects are marked `Featured: true`. Monty must mark proud-of projects before promotion. If /portfolio shows empty-state at this gate, record it as a BLOCKER requiring Monty action before GO.

Phase 16 deferred 4-item checklist (from 16-09-SUMMARY.md):
1. Every interior page renders in Pumpkin Amber; no v2 paper/ink visible
2. Photo-grid Writing/Works indexes show Notion covers via /api/notion-cover
3. Essay + project detail full-bleed covers load via proxy; type-only fallback when no cover
4. /watching YouTube thumbnails load and cards open YouTube in new tab → NOW: Watching section INSIDE /uses (R-3)

Format analog: `.planning/phases/13-v2-0-qa-go-no-go/13-04-EVIDENCE.md`
</context>

<tasks>

<task type="auto">
  <name>Task 1: Route health check and redirect verification via curl</name>
  <files>.planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-04-EVIDENCE.md (route table section)</files>
  <read_first>
    - `.planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-02-EVIDENCE.md` — extract PREVIEW_URL; check bypass token status
    - `.planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-CONTEXT.md` — R-3 (definitive route list), D-05 (375px visual QA scope)
  </read_first>
  <action>
    Read PREVIEW_URL from 18-02-EVIDENCE.md. If bypass token is required, include header `-H "x-vercel-protection-bypass: BYPASS_TOKEN"` in every curl call.

    For each LIVE route, run: `curl -sS -o /dev/null -w "%{http_code}" PREVIEW_URL/ROUTE`
    Live routes to check (per R-3): `/` (200 expected), `/about` (200), `/projects` (200), `/portfolio` (200), `/writing` (200), `/uses` (200), `/prometheus` (200).

    For each 301 REDIRECT route, run: `curl -sS -o /dev/null -w "%{http_code} -> %{redirect_url}" --max-redirs 0 PREVIEW_URL/ROUTE`
    Redirect routes to check (per R-3): `/watching` (301 → /uses), `/newsletter` (301 → /writing), `/events` (301), `/photos` (301), `/links` (301).

    Also check `/blog/feed.xml` returns 200 with content-type `application/xml` or `text/xml`.

    Record results in a route health table: route | expected | actual | PASS/FAIL.
    A 302 where 301 is expected = FAIL. A 404 where 200 is expected = FAIL.
  </action>
  <verify>
    <automated>curl -sS -o /dev/null -w "%{http_code}" "$PREVIEW_URL/" 2>/dev/null && curl -sS -o /dev/null -w "%{http_code}" "$PREVIEW_URL/portfolio" 2>/dev/null && curl -sS -o /dev/null -w "%{http_code}" "$PREVIEW_URL/uses" 2>/dev/null || echo "Set PREVIEW_URL first"</automated>
  </verify>
  <done>Route health table complete: all 7 live routes return 200, all 5 redirect routes resolve with 301 to correct destination. Any failures documented with actual status code.</done>
</task>

<task type="auto">
  <name>Task 2: R-4 portfolio content gate, /uses Watching section check, and Phase 16 checklist</name>
  <files>.planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-04-EVIDENCE.md (R-4 and checklist section)</files>
  <read_first>
    - `.planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-CONTEXT.md` — R-4 (portfolio content gate), R-3 (/watching in /uses)
    - `.planning/phases/17.3-portfolio-proud-of-past-work-surface-built-on-the-existing-p/17.3-01-SUMMARY.md` if exists — confirms empty-state design and Featured: true mechanism
  </read_first>
  <action>
    R-4 Portfolio Content Gate:
    Fetch `/portfolio` HTML: `curl -s PREVIEW_URL/portfolio`. Grep for content indicating real project cards vs empty-state. Presence of project card elements (look for h3/h2 with project names, or CSS class patterns like `card`, `project-title`, or anchor tags linking to `/projects/`) = CONTENT PRESENT. Presence of empty-state message (likely text like "coming soon", "no projects", "Featured: true", "mark projects as Featured") = EMPTY STATE.

    If EMPTY STATE: record as BLOCKING item for 18-06. Note the required Monty action: "In Notion, open the Projects database and set the 'Featured' property to true for at least 2-3 proud-of projects, then wait for ISR revalidation (~30 min) or manually purge the /portfolio cache before promotion."

    Also verify the homepage "Selected Work" link: `curl -s PREVIEW_URL/ | grep -i "portfolio\|selected.work"` — confirm a link to /portfolio exists in the hero or SectionWork area.

    /uses Watching Section (R-3 adaptation of Phase 16 deferred item 4):
    Fetch `/uses` HTML: `curl -s PREVIEW_URL/uses`. Grep for indicators of the Watching/Things I Love video content: `grep -i "watch\|video\|youtube\|love\|things"`. If present, PASS. If absent (empty page or no video content), FAIL with note.

    Phase 16 Deferred 4-Item Checklist (automated evidence — visual judgment at 18-06 sign-off):
    1. Pumpkin Amber palette: `curl -s PREVIEW_URL/ | grep -i "bg-amber\|pumpkin\|f5a623\|#f5a623\|bg-\[#"` — presence of amber color tokens in HTML output
    2. Notion covers via proxy: `curl -s PREVIEW_URL/writing | grep -i "notion-cover\|notion-image"` — confirm image proxy routes in the rendered HTML
    3. Essay detail cover via proxy: `curl -s PREVIEW_URL/blog/$(curl -s PREVIEW_URL/writing | grep -o '/blog/[^"]*' | head -1 | sed 's|/blog/||') | grep -i "notion-cover\|notion-image"` — attempt to verify proxy on a detail page
    4. Watching section (videos in /uses): covered above

    Lighthouse mobile screenshot capture (optional, best-effort):
    `npx lighthouse PREVIEW_URL --output=html --output-path=/tmp/18-04-lh-mobile.html --form-factor=mobile --quiet [--extra-headers if protected]` — produces a screenshot of the homepage at mobile viewport. Save any generated screenshots to `.planning/phases/18-v3-0-qa-perf-gate-alias-swap/screenshots/`.
  </action>
  <verify>
    <automated>curl -s "$PREVIEW_URL/portfolio" 2>/dev/null | grep -ic "card\|project\|featured" || echo "Portfolio content check — verify output above"; curl -s "$PREVIEW_URL/uses" 2>/dev/null | grep -ic "watch\|video\|love" || echo "Uses/Watching section check — verify output above"</automated>
  </verify>
  <done>R-4 portfolio gate result recorded (CONTENT PRESENT or EMPTY STATE + blocker note). Homepage link to /portfolio confirmed. /uses Watching section presence confirmed. Phase 16 deferred 4-item checklist completed with automated evidence, flagging any items needing visual confirmation at 18-06.</done>
</task>

<task type="auto">
  <name>Task 3: Write 18-04-EVIDENCE.md</name>
  <files>.planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-04-EVIDENCE.md</files>
  <read_first>
    - Task 1 route health data (curl outputs)
    - Task 2 R-4 and checklist data (curl grep outputs)
    - `.planning/phases/13-v2-0-qa-go-no-go/13-04-EVIDENCE.md` (format exemplar)
    - `.planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-PATTERNS.md` — 18-04-EVIDENCE.md structure template
  </read_first>
  <action>
    Create `.planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-04-EVIDENCE.md` following the PATTERNS.md 18-04-EVIDENCE.md template. Sections:

    - Header with gate D-05 and requirement text (375px visual QA + Phase 16 deferred checklist + R-4 content gate)
    - Preview URL and date
    - Route Health Table: route | expected status | actual status | PASS/FAIL
    - Redirect Resolution Table: route | expected target | actual target | PASS/FAIL
    - R-4 Portfolio Content Gate:
      - Result: CONTENT PRESENT (PASS) or EMPTY STATE (BLOCKING — requires Monty action before GO)
      - If EMPTY STATE: exact action required ("Mark N projects Featured: true in Notion, revalidate, re-check")
      - Homepage → /portfolio link: verified/not found
    - /uses Watching Section: PASS/FAIL with grep evidence
    - Phase 16 Deferred 4-Item Checklist:
      - Item 1 (Pumpkin Amber): automated evidence + "visual confirmation at 18-06"
      - Item 2 (Notion covers on indexes): curl evidence
      - Item 3 (detail page covers via proxy): curl evidence
      - Item 4 (Watching content in /uses): covered above
    - Screenshots: path(s) to .../screenshots/ if Lighthouse assets captured
    - Overall D-05 Verdict: PASS (all automated checks pass, no BLOCKING items) or FAIL (list blocking items with required actions)

    If R-4 shows EMPTY STATE, the overall verdict must be FAIL with blocker text: "BLOCKING: /portfolio shows empty-state. Monty must mark projects Featured: true in Notion BEFORE 18-06 GO sign-off."
  </action>
  <verify>
    <automated>test -f .planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-04-EVIDENCE.md && grep -q "R-4" .planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-04-EVIDENCE.md && grep -q "Route Health" .planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-04-EVIDENCE.md && echo "18-04-EVIDENCE.md valid"</automated>
  </verify>
  <done>18-04-EVIDENCE.md exists with route health table, redirect table, R-4 portfolio content gate result, /uses Watching section result, Phase 16 deferred checklist, and overall D-05 PASS or FAIL verdict. If any BLOCKING items exist, they are clearly stated for Monty to action before 18-06.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local → v3 Vercel preview | curl requests to the preview URL; unauthenticated or bypass-header protected |
| Notion API → /portfolio page | ISR-cached Notion data; if Featured projects not set, page renders empty-state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-04-01 | Tampering | /portfolio empty-state reaching production (Monty launches with no Featured projects) | mitigate | R-4 gate explicitly checks for content vs empty-state; BLOCKING verdict if empty; Monty action required before GO |
| T-18-04-02 | Information Disclosure | curl fetching preview HTML may include draft content or internal tokens | accept | Vercel preview content is the same as what Notion ISR serves; no credentials in HTML; preview is disposable |
| T-18-SC | Tampering | npm/pip/cargo installs | accept | npx lighthouse reuses cached package; no new installs |
</threat_model>

<verification>
18-04-EVIDENCE.md satisfies this plan when:
- Route health table: all 7 live routes 200, all 5 redirects 301 to correct target
- R-4: /portfolio shows real project cards OR a BLOCKING note is issued requiring Monty to mark Featured projects before 18-06
- /uses Watching section: content confirmed present
- Phase 16 deferred checklist: all 4 items documented (automated evidence + visual-at-18-06 note)
- Overall PASS or FAIL verdict clearly stated

Monty reviews the visual aspects of this evidence at the 18-06 GO/NO-GO sign-off.
</verification>

<success_criteria>
`18-04-EVIDENCE.md` exists. Route health table shows all live routes 200 and all redirects 301 correct. R-4 gate result documented (PASS = real cards, BLOCKING = empty-state with required action). Phase 16 deferred checklist completed.
</success_criteria>

<output>
Create `.planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-04-SUMMARY.md` when done.
</output>
