---
phase: 18-v3-0-qa-perf-gate-alias-swap
plan: 06
type: execute
wave: 3
depends_on:
  - 18-01
  - 18-02
  - 18-03
  - 18-04
  - 18-05
files_modified:
  - .planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-GO-NO-GO.md
autonomous: false
requirements:
  - DQ-04

must_haves:
  truths:
    - "`18-GO-NO-GO.md` is compiled with a verdict row for every gate (DQ-02, DQ-03, DQ-04, D-02, D-05, D-06, D-10)"
    - "Operator has signed the GO/NO-GO doc with verdict, name, and date"
    - "On GO: `v3` branch is merged into `main` via a no-ff merge commit; `main` is pushed to origin"
    - "On GO: Vercel auto-deploys from `main` and the production alias `montysinger.com` serves v3"
    - "Post-promote `curl -I https://montysinger.com` returns HTTP 200"
    - "Alias-drift check confirms the deployment is recent (within 10 min of the push)"
    - "`/gsd-complete-milestone v3.0` is invoked immediately at the GO verdict"
    - "On NO-GO: alias is NOT touched; remediation items are recorded in the doc"
  artifacts:
    - path: ".planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-GO-NO-GO.md"
      provides: "Signed v3.0 GO/NO-GO verdict + promotion plan + sign-off block"
      contains: "signed_by"
  key_links:
    - from: ".planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-GO-NO-GO.md"
      to: "https://montysinger.com"
      via: "alias promotion (merge v3 → main → Vercel auto-deploy)"
      pattern: "HTTP 200"
---

<objective>
Compile the signed `18-GO-NO-GO.md` from all five prior EVIDENCE.md files, conduct the human sign-off, execute the alias promotion (on GO), verify alias correctness post-promote, and immediately run `/gsd-complete-milestone v3.0`.

Purpose: This is the final gate before production. The GO doc aggregates all gate verdicts into a single signed artifact. The alias promotion is an irreversible outward-facing action that requires operator deliberation. Milestone close must happen at the GO verdict, not weeks later (v1.0 retro lesson #1).

Output: Signed `18-GO-NO-GO.md`; on GO — `montysinger.com` serves v3 with no alias drift; milestone v3.0 closed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-CONTEXT.md
@.planning/phases/13-v2-0-qa-go-no-go/13-GO-NO-GO.md
@.planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-01-EVIDENCE.md
@.planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-02-EVIDENCE.md
@.planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-03-EVIDENCE.md
@.planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-04-EVIDENCE.md
@.planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-05-EVIDENCE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compile 18-GO-NO-GO.md from evidence files</name>
  <files>.planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-GO-NO-GO.md</files>

  <read_first>
    1. `.planning/phases/13-v2-0-qa-go-no-go/13-GO-NO-GO.md` — the exact document format to replicate: frontmatter fields, Verdict Summary section, Success Criteria Status table, What's Done (autonomous) section, What's Left (manual) section, Known Limitations section, Promotion Plan section, Sign-Off block
    2. `.planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-PATTERNS.md` — the `18-GO-NO-GO.md` frontmatter structure, header fields, promotion plan commands
    3. All five EVIDENCE.md files (read each): 18-01 through 18-05 — extract each gate's PASS/FAIL verdict and key metric values to populate the Success Criteria Status table
    4. `.planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-CONTEXT.md` — D-08 promotion mechanism (merge v3 → main, Vercel auto-deploy) and D-09 milestone close timing; also read the three pre-promote confirmations flagged by RESEARCH.md
  </read_first>

  <action>
    Read each EVIDENCE.md and extract:
    - 18-01: exit code, SHA, page count, DQ-02 verdict
    - 18-02: preview URL, per-route Lighthouse medians, D-02 verdict
    - 18-03: PSI mobile score, LCP value, D-04 four assertions results, DQ-03 verdict
    - 18-04: D-05 overall verdict, any failing routes or deferred checklist items
    - 18-05: D-06 scan results (match counts), D-10 theme decision, both verdicts

    Write `18-GO-NO-GO.md` with:

    **Frontmatter** (unsigned state — operator fills before signing):
    ```yaml
    phase: 18-v3-0-qa-perf-gate-alias-swap
    type: go-no-go
    status: unsigned
    milestone: v3.0
    milestone_name: v3.0 Dark Brutalist Rebuild & WebGL Homepage
    build_sha: [HEAD SHA from 18-01-EVIDENCE.md]
    build_date: [build date from 18-01-EVIDENCE.md]
    verdict: [GO | NO-GO | GO-with-knowns]
    signed_by: [operator]
    signed_date: [date]
    ```

    **Header**: "v3.0 Dark Brutalist Rebuild — GO/NO-GO Verdict"; milestone v3.0 (Phases 14–17); build SHA; production URL https://montysinger.com; preview URL from 18-02-EVIDENCE.md.

    **Verdict Summary**: STATUS placeholder `[ GO | NO-GO | GO-with-knowns ]` with the outcome table (GO = promote + close milestone; NO-GO = remediate; GO-with-knowns = explicit risk acceptance).

    **Success Criteria Status table** (7 rows):
    | DQ-02 | `vercel build --prod` exits 0, zero TS/ESLint/429 errors | [PASS/FAIL from 18-01] | 18-01-EVIDENCE.md |
    | DQ-03 | PSI mobile ≥82; WebGL hero NOT LCP element | [PASS/FAIL from 18-03] | 18-03-EVIDENCE.md — D-04 LCP block |
    | DQ-04 | Alias promoted to v3, verified post-promote | THIS DOC | executed at sign-off |
    | D-02 | Lighthouse desktop ≥90/95/95/100 on 6 routes | [PASS/FAIL from 18-02] | 18-02-EVIDENCE.md |
    | D-05 | 375px visual QA + Phase 16 deferred 7-item checklist | [PASS/FAIL from 18-04] | 18-04-EVIDENCE.md |
    | D-06 | Secret scan: 0 client-side leaks | [PASS/FAIL from 18-05] | 18-05-EVIDENCE.md |
    | D-10 | Theme decision recorded (single-mode or FOUC clear) | [PASS/FAIL from 18-05] | 18-05-EVIDENCE.md |

    Fill in the PASS/FAIL from each evidence file (read actual verdicts, not placeholders).

    **What's Done (Autonomous)** section: summarize the 5 autonomous gates (DQ-02, D-02, DQ-03, D-06, D-10) with key metrics extracted from their EVIDENCE.md files. Match the 13-GO-NO-GO.md format for this section.

    **What's Left (Manual)** section: include only gates that require operator action: D-05 visual QA (should already be filled from 18-04, but operator must confirm) and DQ-04 (the sign-off + promote, which IS this doc).

    **Known Limitations (if any)**: if any gate is GO-with-knowns (e.g., PSI between 77-81, or a Lighthouse route with a single failing audit that was risk-accepted), list it here with explicit acceptance rationale.

    **Pre-Promote Operator Confirmations** (three checks RESEARCH.md flags for 18-06):
    Add a dedicated section before the Promotion Plan titled "Pre-Promote Operator Confirmations":
    1. Is the Vercel project `m-sizzle-personal-website` configured to auto-deploy from the `main` branch to production? (Check: Vercel dashboard → Settings → Git → Production Branch)
    2. Is `main` currently at v2.0? (Check: `git log main..v3 --oneline | wc -l` shows the commit distance — operator confirms this is the expected merge size)
    3. Has alias drift occurred in previous promotions on this project? (The v2.0 Phase 13 promotion used `--prebuilt --prod` and the memory notes drift; if yes, allow 10 min post-push before verifying alias)

    The operator must confirm each before proceeding to promote.

    **Promotion Plan** (conditional on GO — NEVER `--prebuilt --prod`):
    Exact command sequence per RESEARCH.md Path B (merge to main, Vercel auto-deploy):

    Step 1: Final branch confirmation — `git branch --show-current` (must be v3 or main at the right commit)
    Step 2: Merge v3 → main: `git checkout main && git pull origin main && git merge v3 --no-ff -m "chore(release): merge v3 for v3.0 production deployment [Phase 18 GO]"`
    Step 3: Push to trigger Vercel auto-deploy: `git push origin main`
    Step 4: Wait 5-10 min for Vercel to build and deploy (watch at https://vercel.com/msizzles-projects/m-sizzle-personal-website/deployments)
    Step 5: Alias smoke test: `curl -sS -o /dev/null -w "HTTP %{http_code} | size %{size_download}b\n" https://montysinger.com` — expected: HTTP 200
    Step 6: v3 marker check: `curl -s https://montysinger.com | grep -oi "#d93c1e\|pumpkin\|amber" | head -1` — expected: at least one v3 palette marker
    Step 7: Deployment recency check: `vercel list --prod=true | grep m-sizzle-personal-website -A1` — deployment timestamp must be within 10 min of the push
    Step 8: Browser spot-check: open https://montysinger.com — confirm Pumpkin Amber palette, WebGL hero on desktop / poster on mobile
    Step 9 (IMMEDIATELY): `/gsd-complete-milestone v3.0`

    **Rollback** (if alias drifts post-promote):
    `vercel list --prod=true` — find the previous v2.0 deployment ID
    `vercel alias set https://m-sizzle-personal-website-[OLD_ID]-msizzles-projects.vercel.app montysinger.com`
    OR: `git revert HEAD~1 --no-edit && git push origin main` (reverts the merge; Vercel auto-deploys v2.0 state)

    **Sign-Off Block** (blank — operator fills at checkpoint):
    ```
    Verdict:    [ GO | NO-GO | GO-with-knowns ]
    Signed by:  ____________________  (operator)
    Date:       __________
    Notes:      ____________________
    ```
  </action>

  <verify>
    <automated>test -f ".planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-GO-NO-GO.md" && grep -q "DQ-02" ".planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-GO-NO-GO.md" && grep -q "DQ-03" ".planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-GO-NO-GO.md" && grep -q "DQ-04" ".planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-GO-NO-GO.md" && grep -q "Promotion Plan" ".planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-GO-NO-GO.md" && grep -q "Sign-Off" ".planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-GO-NO-GO.md"</automated>
  </verify>

  <acceptance_criteria>
    - `18-GO-NO-GO.md` exists with complete frontmatter (phase, type, status=unsigned, milestone=v3.0, build_sha, build_date, verdict placeholder, signed_by placeholder)
    - Success Criteria Status table has 7 rows covering DQ-02, DQ-03, DQ-04, D-02, D-05, D-06, D-10
    - Each of the 5 autonomous gates (DQ-02, D-02, DQ-03, D-06, D-10) has its actual verdict pulled from the EVIDENCE.md files (not placeholders like "[PASS/FAIL]")
    - Pre-Promote Operator Confirmations section has the three checks from RESEARCH.md
    - Promotion Plan section contains the exact 9-step sequence with `merge v3 → main` as the promotion mechanism (NOT `--prebuilt --prod`)
    - Rollback commands are present
    - Sign-Off block is present and blank (operator fills at checkpoint)
  </acceptance_criteria>

  <done>`18-GO-NO-GO.md` compiled with all gate verdicts populated from evidence files; promotion plan with 9-step sequence included; doc ready for operator sign-off checkpoint.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Claude compiled `18-GO-NO-GO.md` with all 7 gate verdicts pulled from the 5 EVIDENCE.md files, the promotion plan (merge v3 → main, Vercel auto-deploy), three pre-promote confirmations, and the sign-off block. The doc is unsigned.
  </what-built>

  <how-to-verify>
    Review `18-GO-NO-GO.md`:
    1. Read the Success Criteria Status table — confirm every gate shows an actual PASS/FAIL verdict (not a placeholder)
    2. If any gate is FAIL: determine whether to fix and re-run OR accept as GO-with-knowns with explicit rationale

    **Complete the three pre-promote confirmations** before signing:
    - Check 1: Open https://vercel.com/msizzles-projects/m-sizzle-personal-website/settings/git — confirm `main` is set as the Production Branch
    - Check 2: Run `git log main..v3 --oneline | wc -l` — confirm the commit count is as expected (all v3 work from Phases 14-17)
    - Check 3: Recall whether alias drift occurred after the v2.0 Phase 13 promotion — if yes, allow extra time before verifying alias after this push

    **Sign the doc** — fill in the Sign-Off block:
    ```
    Verdict:    GO  (or NO-GO, or GO-with-knowns)
    Signed by:  Monty Singer
    Date:       2026-06-[XX]
    Notes:      [any risk-acceptance notes]
    ```
    Also update the frontmatter `status: signed`, `verdict: GO`, `signed_by: Monty Singer`, `signed_date: 2026-06-[XX]`.

    **If GO or GO-with-knowns — execute the promotion sequence:**

    Step 1: `git checkout v3 && git branch --show-current`  (confirm: v3)
    Step 2: `git checkout main && git pull origin main`
    Step 3: `git merge v3 --no-ff -m "chore(release): merge v3 for v3.0 production deployment [Phase 18 GO]"`
    Step 4: `git push origin main`
    Step 5: Wait 5-10 minutes. Watch Vercel deployments: https://vercel.com/msizzles-projects/m-sizzle-personal-website/deployments
    Step 6: `curl -sS -o /dev/null -w "HTTP %{http_code} | size %{size_download}b\n" https://montysinger.com`  (must return HTTP 200)
    Step 7: `curl -s https://montysinger.com | grep -oi "#d93c1e\|pumpkin\|amber" | head -1`  (must return a match)
    Step 8: `vercel list --prod=true | grep m-sizzle-personal-website -A2`  (deployment must be recent — within 10 min of push)
    Step 9: Browser spot-check at https://montysinger.com — confirm Pumpkin Amber palette and WebGL hero or poster
    Step 10 (IMMEDIATELY, do not defer): `/gsd-complete-milestone v3.0`

    **If alias is wrong after promote** (Step 6 still returns v2.0 content after 10+ min):
    Run `vercel list --prod=true` to find the old v2.0 deployment ID, then:
    `vercel alias set https://m-sizzle-personal-website-[OLD_ID]-msizzles-projects.vercel.app montysinger.com`
    Wait 2 min and re-check curl.

    Commit the signed `18-GO-NO-GO.md`: `git add ".planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-GO-NO-GO.md" && git commit -m "qa(18-06): sign v3.0 GO/NO-GO verdict [Phase 18 complete]"`
  </how-to-verify>

  <resume-signal>
    Type one of:
    - "go-verdict-signed: GO — montysinger.com confirmed v3, milestone closed" — all steps complete, production is live on v3
    - "go-verdict-signed: NO-GO — [blocking issue]" — promotion halted; describes what must be fixed before re-run
    - "go-verdict-signed: GO-with-knowns — [what was accepted]" — GO with explicit risk items documented in the GO doc
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator sign-off → production alias promotion | This is an irreversible outward-facing action; the human checkpoint MUST precede the alias push |
| v3 branch → main merge → production | Merge must use --no-ff for auditability; NEVER --prebuilt --prod; Vercel auto-deploy must complete before alias verification |
| Post-promote verify → v3 marker check | Alias drift check (Step 6-8) must run AFTER the Vercel build completes (~5-10 min), not immediately after push |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-16 | Tampering | Alias points to wrong deployment (alias drift) | mitigate | Step 8: `vercel list --prod=true` deployment timestamp must be recent (within 10 min of push); Step 6: curl HTTP 200; Step 7: v3 palette marker in curl output; Step 9: browser visual confirmation |
| T-18-17 | Elevation of Privilege | `--prebuilt --prod` used instead of `vercel deploy --prod` or merge flow | mitigate | Promotion plan explicitly uses `merge v3 → main` + `git push origin main`; `--prebuilt` flag is NEVER present in any step |
| T-18-18 | Repudiation | GO verdict not signed before promotion | mitigate | Human checkpoint gate=blocking — promotion commands are in the how-to-verify, not in an autonomous task; operator must sign first |
| T-18-19 | Denial of Service | Milestone close deferred past GO verdict | mitigate | Step 10 is `/gsd-complete-milestone v3.0` and is labeled IMMEDIATELY; the resume-signal requires confirmation that milestone close ran |
| T-18-20 | Tampering | Wrong branch merged to main (main at wrong state) | mitigate | Pre-promote confirmation #2: `git log main..v3 --oneline | wc -l` confirms expected commit count; confirmation #1 checks Vercel auto-deploy configuration |
</threat_model>

<verification>
`18-GO-NO-GO.md` frontmatter has `status: signed` and `verdict: GO` (or NO-GO); `signed_by` is non-empty; `curl -sS -o /dev/null -w "%{http_code}" https://montysinger.com` returns 200 (only on GO path); `git log main --oneline | head -1` shows the v3 merge commit.
</verification>

<success_criteria>
DQ-04 satisfied: `18-GO-NO-GO.md` is signed with a verdict; on GO — `v3` is merged into `main`, Vercel auto-deploys, `montysinger.com` returns HTTP 200 with v3 palette markers, alias drift is confirmed absent, and `/gsd-complete-milestone v3.0` has been invoked immediately. The v3.0 milestone is closed.
</success_criteria>

<output>
Create `.planning/phases/18-v3-0-qa-perf-gate-alias-swap/18-06-SUMMARY.md` when done. The SUMMARY.md must include: final GO/NO-GO verdict, PSI mobile score recorded in 18-03, post-promote curl result (HTTP status), and confirmation that `/gsd-complete-milestone v3.0` ran.
</output>
