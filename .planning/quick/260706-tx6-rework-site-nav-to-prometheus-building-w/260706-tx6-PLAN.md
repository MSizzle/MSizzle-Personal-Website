---
phase: quick-260706-tx6
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/projects/page.tsx (moved -> src/app/building/page.tsx)
  - src/app/projects/[slug]/page.tsx (moved -> src/app/building/[slug]/page.tsx)
  - src/app/projects/[slug]/opengraph-image.tsx (moved -> src/app/building/[slug]/opengraph-image.tsx)
  - src/app/about/page.tsx (deleted)
  - src/components/projects/project-card.tsx
  - src/components/home/section-work.tsx
  - src/app/sitemap.ts
  - src/components/layout/v3-footer.tsx
  - src/lib/seo/project-metadata.ts
  - next.config.ts
  - src/components/home-v2/editorial-header.tsx
  - src/components/home/sticky-nav.tsx
  - src/components/nav/navigation.tsx
  - src/components/home/section-footer.tsx
autonomous: true
requirements: [QUICK-260706-tx6]

must_haves:
  truths:
    - "User sees exactly 4 nav items, in order: Prometheus, Building, Writing, Contact -- on the global desktop header, the homepage sticky nav, and the mobile drawer"
    - "Clicking Contact from any page jumps to the footer contact block on the SAME page (no route navigation, no 404)"
    - "Visiting /building renders the former /projects works index; visiting /building/<slug> renders a project detail page"
    - "Visiting /projects or /projects/<slug> permanently redirects to /building or /building/<slug>"
    - "Visiting /about permanently redirects to /"
    - "The /uses page still loads at /uses even though no nav links to it anymore"
    - "npm run build succeeds with zero errors after the rename, deletion, and nav rework"
  artifacts:
    - path: "src/app/building/page.tsx"
      provides: "Building index route (renamed from /projects), canonical/OG pointing at /building"
    - path: "src/app/building/[slug]/page.tsx"
      provides: "Building detail route with breadcrumb href updated to /building"
    - path: "next.config.ts"
      provides: "Permanent redirects: /projects->/building, /projects/:slug->/building/:slug, /about->/, plus corrected /portfolio and /links targets"
    - path: "src/components/home-v2/editorial-header.tsx"
      provides: "Global desktop nav: Prometheus/Building/Writing/Contact"
    - path: "src/components/home/sticky-nav.tsx"
      provides: "Homepage sticky nav: Prometheus/Building/Writing/Contact + unchanged Say Hi CTA"
    - path: "src/components/nav/navigation.tsx"
      provides: "Mobile drawer: Prometheus/Building/Writing/Contact"
    - path: "src/components/home/section-footer.tsx"
      provides: "id=\"contact\" anchor target on the homepage footer"
    - path: "src/components/layout/v3-footer.tsx"
      provides: "id=\"contact\" anchor target on the global (non-home) footer"
  key_links:
    - from: "src/components/home-v2/editorial-header.tsx"
      to: "#contact"
      via: "plain anchor tag, never bolded as active"
      pattern: 'href="#contact"'
    - from: "src/components/home/section-footer.tsx"
      to: "contact anchor id"
      via: "id attribute on the footer element"
      pattern: 'id="contact"'
    - from: "next.config.ts"
      to: "/building"
      via: "redirects() permanent entry"
      pattern: "destination: '/building'"
---

<objective>
Rework site navigation to a new 4-item set (Prometheus, Building, Writing, Contact) applied consistently across the global desktop header, homepage sticky nav, and mobile drawer. This reverses locked decision D-08 per Monty's explicit new direction on 2026-07-06.

Concretely: rename the /projects route to /building (git history preserved), delete /about entirely, add permanent redirects for both, rewrite the three nav components to the new link set (Contact is an in-page anchor to the footer, not a route), and wire up the #contact anchor target on both footer surfaces.

Purpose: Monty wants a leaner, Prometheus-forward nav that funnels visitors toward Building/Writing content and a single Contact touchpoint, dropping About and Uses as primary destinations.
Output: Renamed /building route with working redirects, deleted /about with a working redirect, three nav components on the new 4-item set, and a working #contact anchor on every page.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
</context>

<interfaces>
Current LINKS arrays being replaced (D-08 4-item set: About/Projects/Writing/Uses). Fenced blocks below are read-only reference for the executor, not code to paste in verbatim -- follow the specific rewrite instructions in each task's action instead.

From src/components/home-v2/editorial-header.tsx (current):
```
type Props = { active?: "About" | "Projects" | "Writing" | "Uses" };
const LINKS = [
  { label: "About",    href: "/about"    },
  { label: "Projects", href: "/projects" },
  { label: "Writing",  href: "/writing"  },
  { label: "Uses",     href: "/uses"     },
] as const;
```

From src/components/home/sticky-nav.tsx (current):
```
const LINKS = [
  { label: "About", href: "/about" },
  { label: "Projects", href: "/projects" },
  { label: "Writing", href: "/writing" },
  { label: "Uses", href: "/uses" },
] as const;
```
The "Say Hi" mailto CTA lives below LINKS in the same component and must NOT change:
`mailto:monty@prometheus.today?subject=Saying%20hi&body=Hi%20Monty%2C%0A%0A`

From src/components/nav/navigation.tsx (current):
```
const MOBILE_LINKS = [
  { href: '/projects',   label: 'Projects'    },
  { href: '/writing',    label: 'Writing'     },
  { href: '/about',      label: 'About'       },
  { href: '/uses',       label: 'Uses'        },
]
const activeLabel: 'Projects' | 'Writing' | 'About' | 'Uses' | undefined = ...
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Rename /projects to /building, fix every internal link, add redirects</name>
  <files>src/app/projects/* (git mv to src/app/building/*), src/components/projects/project-card.tsx, src/components/home/section-work.tsx, src/app/sitemap.ts, src/components/layout/v3-footer.tsx, src/lib/seo/project-metadata.ts, next.config.ts</files>
  <action>
Run `git mv src/app/projects src/app/building` (moves page.tsx, [slug]/page.tsx, and [slug]/opengraph-image.tsx together, preserving git history). No import-path fixes are needed inside the moved files since they use `@/...` absolute aliases exclusively.

In src/app/building/page.tsx: change `alternates.canonical` from `"/projects"` to `"/building"`, and `openGraph.url` from `"/projects"` to `"/building"`. Update the doc comment header from `/projects -- Works index page (ARCH-01).` to `/building -- Works index page (ARCH-01).` and the inline comment `every /projects card is` to `every /building card is`.

In src/app/building/[slug]/page.tsx: change the Breadcrumbs `items` array entry from `{ name: "Building", href: "/projects" }` to `{ name: "Building", href: "/building" }`. Update the adjacent comment `Building points to /projects` to `Building points to /building`.

In src/components/projects/project-card.tsx: change the Link `href` template literal from `/projects/${project.slug}` to `/building/${project.slug}`.

In src/components/home/section-work.tsx: change the Link `href="/projects"` to `href="/building"` (keep the visible label text "Projects" and the "SELECTED" kicker unchanged -- this is homepage body copy, not a nav item). Update the two doc-comment references (the one mentioning "/projects link present with SELECTED kicker" and the one mentioning "consolidated from /portfolio") so no stale `/projects` string remains anywhere in the file, including comments -- rewrite them to reference `/building`.

In src/app/sitemap.ts: change the static route entry that builds `${SITE_URL}/projects` to build `${SITE_URL}/building` instead, and change the `projectRoutes` map's template literal from `${SITE_URL}/projects/${project.slug}` to `${SITE_URL}/building/${project.slug}`. Update the comment line listing static routes so it reads `/building` instead of `/projects` (the /about removal is handled in Task 2 -- for now just fix the /projects mention).

In src/components/layout/v3-footer.tsx: in the "Building" footer column, change the "Works" Link `href="/projects"` to `href="/building"`.

In src/lib/seo/project-metadata.ts: change the `canonical(...)` call argument from `/projects/${project.slug}` to `/building/${project.slug}`, and the `alternates.canonical` value from `/projects/${project.slug}` to `/building/${project.slug}`.

In next.config.ts: inside the `redirects()` return array, change the existing entry with source `/portfolio` so its destination is `/building` instead of `/projects` (it must not chain through the now-nonexistent /projects route). Then add two new entries: one with source `/projects` and destination `/building`, and one with source `/projects/:slug` and destination `/building/:slug`. All entries keep `permanent: true`.
  </action>
  <verify>
    <automated>grep -rn "/projects" --include="*.ts" --include="*.tsx" src/ | grep -v __tests__</automated>
  </verify>
  <done>Command above returns zero lines (the only remaining "/projects" strings live in next.config.ts as redirect source values, which is outside src/ and therefore not matched by this grep). `/building` and `/building/[slug]` render; `git log --follow src/app/building/page.tsx` shows history carried from src/app/projects/page.tsx.</done>
</task>

<task type="auto">
  <name>Task 2: Delete /about, add redirect, remove all references</name>
  <files>src/app/about (deleted), next.config.ts, src/components/layout/v3-footer.tsx, src/app/sitemap.ts</files>
  <action>
Run `git rm -r src/app/about` to delete the page entirely (studio/newsletter/writing/education rows and all).

In next.config.ts: add a new redirects entry with source `/about`, destination `/`, `permanent: true`. Also change the existing entry with source `/links` so its destination is `/` instead of `/about` -- it must not chain through the now-deleted /about route.

In src/app/sitemap.ts: remove the static route entry that builds `${SITE_URL}/about` entirely, and update the comment listing static routes to drop `/about` and adjust the count from 6 to 5.

In src/components/layout/v3-footer.tsx: the big "Let's be friends." signature element currently is a next/link `Link` with `href="/about"`. Change it to a plain anchor tag with `href` set to the mailto string used by the site's existing "Say Hi" contact pattern in src/components/home/sticky-nav.tsx (mailto to monty@prometheus.today, subject "Saying hi", with a greeting body -- reuse that exact mailto string verbatim). Since this becomes an external-protocol link, swap the element tag from `Link` to a plain `a` tag, keeping the same className and content. Check with grep whether `Link` (the next/link import) is still used elsewhere in this file (the Works/Building and Prometheus footer links also use it) before deciding whether to remove the import -- only remove the import if zero usages remain. Remove the entire "About" footer column (the column with the `h3` heading "About" and its single link to `/about`) since the page it points to no longer exists. Update the file's doc comment bullet that currently says the signature link points to /about (not /links) so it accurately describes the new mailto destination instead.
  </action>
  <verify>
    <automated>grep -rn "/about" --include="*.ts" --include="*.tsx" src/ | grep -v __tests__</automated>
  </verify>
  <done>Command above returns zero lines. src/app/about no longer exists (verify with a `test -d src/app/about` check -- it should fail/not-exist). Visiting /about permanently redirects to /. The v3-footer "Let's be friends." element is a plain anchor with the mailto href, not a next/link Link.</done>
</task>

<task type="auto">
  <name>Task 3: Rework nav in editorial-header, sticky-nav, and mobile drawer</name>
  <files>src/components/home-v2/editorial-header.tsx, src/components/home/sticky-nav.tsx, src/components/nav/navigation.tsx</files>
  <action>
Replace the LINKS/MOBILE_LINKS arrays and active-state types in all three files with the new 4-item set, in this exact order: Prometheus (/prometheus), Building (/building), Writing (/writing), Contact (#contact). Contact is an in-page anchor, never a route, and never receives the bold/active treatment.

In src/components/home-v2/editorial-header.tsx: replace the `LINKS` array with four entries -- Prometheus pointing to /prometheus, Building pointing to /building, Writing pointing to /writing, and Contact pointing to the anchor #contact. Change the `Props.active` union type from the current About/Projects/Writing/Uses set to just Prometheus/Building/Writing (Contact excluded -- it has no active state). In the render loop, branch on whether the link's href starts with `#`: for the Contact entry render a plain anchor tag with the same layout classes used by the other items (min-height, flex, transition-opacity, hover:opacity-60) plus the muted text color, and never apply the bold/active styling to it; for the other three entries keep the existing Link-based active-check logic unchanged. Update the file's doc comments (currently describing D-08's lean 4-item nav of About/Projects/Writing/Uses) to describe the new Prometheus/Building/Writing/Contact set and note this reverses D-08 per Monty's 2026-07-06 direction (quick task 260706-tx6).

In src/components/home/sticky-nav.tsx: replace `LINKS` with the same four entries described above. In the list render loop, branch the same way: Contact renders as a plain anchor with href #contact, the other three keep the existing Link-based rendering. Do NOT touch the "Say Hi" mailto CTA element below the list -- it stays exactly as-is, unchanged. Update the file's top comment to describe the new link set instead of the old About/Projects/Writing/Uses one.

In src/components/nav/navigation.tsx: replace `MOBILE_LINKS` with Prometheus (/prometheus), Building (/building), Writing (/writing), and Contact (#contact), in that order. Change the `activeLabel` type from the current Projects/Writing/About/Uses union to Prometheus/Building/Writing (or undefined), and its derivation logic to check pathname against /prometheus, /building, and /writing (also matching any /blog-prefixed path to Writing), falling back to undefined otherwise. In the drawer's link-rendering loop, branch on whether the href starts with `#`: for Contact render a plain anchor tag that still closes the drawer on click (same onClick handler that sets open to false), using the same classes as the other drawer items; for the other three keep the existing Link-based rendering with the same close-on-click handler. Update the file's comments (currently referencing D-08/D-10 cut routes and the old 4-item set) to describe the new set and note the D-08 reversal.
  </action>
  <verify>
    <automated>grep -c "Prometheus" src/components/home-v2/editorial-header.tsx src/components/home/sticky-nav.tsx src/components/nav/navigation.tsx</automated>
  </verify>
  <done>All three files reference Prometheus, Building, Writing, and Contact in that order; none of the three files contains "About", "Projects", or "Uses" as a nav link label or active-type member anymore (mobile drawer, desktop header, and sticky nav each render exactly 4 nav entries).</done>
</task>

<task type="auto">
  <name>Task 4: Wire the #contact anchor and run final verification</name>
  <files>src/components/home/section-footer.tsx, src/components/layout/v3-footer.tsx</files>
  <action>
In src/components/home/section-footer.tsx: add an `id="contact"` attribute to the outermost `footer` element (currently `className="site"`). This is the only footer rendered on the homepage (ConditionalFooter returns null on "/"), so `href="#contact"` from the homepage sticky nav and mobile drawer resolves here.

In src/components/layout/v3-footer.tsx: add an `id="contact"` attribute to the outermost `footer` element (the same element already touched in Task 2 for the mailto signature link -- just add the id attribute alongside the existing className). This footer renders on every non-home route via ConditionalFooter, so `href="#contact"` resolves there too.

Then run the full-project verification: execute `npm run build` from the repo root and confirm it completes with exit code 0. Re-run the two codebase-wide grep gates from Tasks 1 and 2 to confirm they still return zero lines after all edits made in Tasks 2-3 (comment and text updates must not have reintroduced a stale reference). Confirm `id="contact"` now appears in both footer files.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>`npm run build` exits 0. A combined grep for "/projects" or "/about" across src/ (excluding __tests__) returns zero lines. Both src/components/home/section-footer.tsx and src/components/layout/v3-footer.tsx contain id="contact" on their outermost footer element.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| none new | This plan only reshuffles internal routes, static nav labels, and a same-origin anchor link. No new user input, no new external service, no new data flow crosses a trust boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|--------------|------------------|
| T-260706tx6-01 | Information Disclosure | next.config.ts redirects | accept | Redirect sources/destinations are static, developer-authored strings, never derived from request input, so there is no open-redirect risk. |
| T-260706tx6-02 | Denial of Service | /projects, /about (removed routes) | accept | Old URLs now permanently redirect to valid pages instead of 404ing; no functional regression, no new attack surface introduced. |
</threat_model>

<verification>
1. `npm run build` completes with exit code 0.
2. Visit /building and /building/(an existing project slug) -- both render without a 404.
3. Visit /projects and /projects/(any slug) -- both permanently redirect to the equivalent /building URL.
4. Visit /about -- permanently redirects to /.
5. Visit /uses directly -- still renders (page kept on disk, just unlinked from nav).
6. On desktop width: header shows Prometheus, Building, Writing, Contact in that order; clicking Contact scrolls to the footer on the current page without changing the URL path.
7. On mobile width: hamburger drawer shows the same 4 items in the same order; Contact is an anchor, not a route link.
8. On the homepage, scroll past roughly 82% of the viewport height: sticky nav appears with the same 4 items plus the unchanged "Say Hi" CTA.
9. `grep -rn "/projects\|/about" --include="*.ts" --include="*.tsx" src/ | grep -v __tests__` returns zero lines.
</verification>

<success_criteria>
- /building and /building/[slug] serve the content that used to live at /projects, with git history preserved via `git mv`.
- /projects, /projects/:slug, and /about all permanently redirect to their new/home destinations; no old URL 404s.
- All three nav surfaces (editorial header, sticky nav, mobile drawer) show exactly Prometheus, Building, Writing, Contact in that order.
- Contact is a same-page anchor to the footer on every route (both footer components carry id="contact" on their outermost footer element).
- /uses remains reachable by direct URL; the homepage Loves section band is untouched.
- `npm run build` succeeds.
</success_criteria>

<output>
Create `.planning/quick/260706-tx6-rework-site-nav-to-prometheus-building-w/260706-tx6-SUMMARY.md` when done.
</output>
