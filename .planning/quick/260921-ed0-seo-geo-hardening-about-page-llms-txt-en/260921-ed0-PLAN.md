---
phase: quick-260921-ed0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/seo/site.ts
  - src/lib/seo/schemas.ts
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/app/about/page.tsx
  - src/app/llms.txt/route.ts
  - src/app/indexnow-key.txt/route.ts
  - src/lib/seo/indexnow.ts
  - src/app/api/revalidate/route.ts
  - src/app/api/notion-cover/route.ts
  - src/app/sitemap.ts
  - src/app/building/[slug]/page.tsx
  - src/lib/notion-projects.ts
  - src/components/layout/site-footer.tsx
  - next.config.ts
  - .env.example
  - src/__tests__/seo/sitemap.test.ts
  - src/__tests__/seo/schemas.test.ts
  - src/__tests__/seo/llms-txt.test.ts
autonomous: true
requirements: [SEO-GEO-HARDEN]

must_haves:
  truths:
    - "GET /about returns 200 with a biographical page (identity line, Prometheus, Georgetown University, writing, interests, contact, visible FAQ) and FAQPage + BreadcrumbList JSON-LD; /about no longer redirects"
    - "GET /llms.txt returns text/markdown following llmstxt.org shape: H1, blockquote summary, sections linking key pages, published essays and projects, and the RSS feed"
    - "Every Person reference in JSON-LD (WebSite publisher, BlogPosting author/publisher, Organization founder, project author) carries @id https://montysinger.com/#person; Person node has description, image, knowsAbout; an Organization node for Prometheus exists with founder → Person @id"
    - "One identity sentence is used verbatim in layout meta description, Person description, llms.txt and /about"
    - "sitemap.ts static routes no longer emit lastModified: new Date(); /about is listed"
    - "/building/[slug] emits BreadcrumbList + CreativeWork JSON-LD; project slugs are lowercased at extraction and a mixed-case URL 308s to the lowercase form"
    - "/api/notion-cover success responses send Cache-Control: public, max-age=86400, s-maxage=31536000, stale-while-revalidate=604800"
    - "When BING_SITE_VERIFICATION is set, <meta name=\"msvalidate.01\"> renders; when INDEXNOW_KEY is set, /indexnow-key.txt serves it and /api/revalidate pings api.indexnow.org (fire-and-forget, never fails the response)"
    - "npm test passes except the 3 known pre-existing homepage failures; npm run build passes"
  artifacts:
    - path: "src/app/about/page.tsx"
      provides: "/about route, metadata, FAQ, JSON-LD"
    - path: "src/app/llms.txt/route.ts"
      provides: "GET /llms.txt"
      exports: ["GET", "revalidate"]
    - path: "src/lib/seo/indexnow.ts"
      provides: "pingIndexNow(urls)"
      exports: ["pingIndexNow"]
    - path: "src/app/indexnow-key.txt/route.ts"
      provides: "GET key file"
      exports: ["GET"]
    - path: "src/lib/seo/schemas.ts"
      provides: "PERSON_ID, ORG_ID, buildOrganizationSchema, buildProjectSchema, @id-linked existing builders"
  key_links:
    - from: "src/app/api/revalidate/route.ts"
      to: "src/lib/seo/indexnow.ts"
      via: "pingIndexNow import"
      pattern: "pingIndexNow"
    - from: "src/app/layout.tsx"
      to: "src/lib/seo/site.ts"
      via: "IDENTITY_SENTENCE import"
      pattern: "IDENTITY_SENTENCE"
    - from: "src/app/building/[slug]/page.tsx"
      to: "src/lib/seo/schemas.ts"
      via: "buildProjectSchema"
      pattern: "buildProjectSchema"
---

<objective>
SEO/GEO hardening in one pass: a plain biographical /about page that LLMs and Google can quote, an llms.txt, entity consolidation via @id in JSON-LD, Bing/IndexNow plumbing, honest sitemap dates, project-page schema, slug-case canonical fix, longer cover-image cache, and one identity sentence used everywhere.

Purpose: Search Console (July 28 export) shows 15 indexed / 21 not indexed and single-digit daily impressions. The site has no about page, no llms.txt, three unlinked schema nodes, no Bing presence. This closes the config gaps; authority comes with time.
</objective>

<context>
Read first: ./CLAUDE.md, .planning/STATE.md (top 40 lines only), src/lib/seo/site.ts, src/lib/seo/schemas.ts, src/app/layout.tsx, src/app/sitemap.ts, src/app/contact/page.tsx (static page pattern), src/components/seo/breadcrumbs.tsx, src/app/blog/[slug]/page.tsx (how JsonLd + Breadcrumbs are used), src/app/api/revalidate/route.ts, src/app/api/notion-cover/route.ts, src/lib/notion-projects.ts, src/components/layout/site-footer.tsx, next.config.ts, src/app/blog/feed.xml/route.ts (dynamic text route pattern).

Site copy rules (PROJECT.md, non-negotiable):
- No em dashes anywhere in user-visible copy. Use a colon, comma, period, or parentheses.
- No location. Do not mention cities, states, or countries.
- Education: "Georgetown University" only. No school names beyond that.
- Sole professional identity: Founder of Prometheus.
- Voice: plain, first person where natural, anti-hype, concrete, short sentences.

Locked identity sentence (use verbatim, export as IDENTITY_SENTENCE from src/lib/seo/site.ts):
"Monty Singer is the founder of Prometheus, an applied AI company. Builder, writer, and doer."
The hero's existing line "Founder of Prometheus, an applied AI company." stays unchanged (it has a test).

Known facts allowed on the site: founder of Prometheus (https://prometheus.today, applied AI: AI integrations and education for businesses); Georgetown University; writes essays at /writing and the Monty Monthly newsletter (https://montymonthly.substack.com); interests: technology, biology, self-improvement (mirrors the hero); Things I Love lives at /#loves; contact via /contact; anonymous notes via /advice; socials: https://x.com/themontysinger, https://linkedin.com/in/monty-singer, https://github.com/MSizzle; email monty@prometheus.today. Do not invent dates, ages, employers, or numbers.

Design: bone ground, ink text, Hanken Grotesk display, hard corners, no gradients, no cards. Reuse PageHeroBand (tone="ink") and RuleStrong like /contact. Keep /about as a Server Component, no client JS.

Tests run with vitest (`npm test`). Three homepage failures are pre-existing (section-building HD-04, explorative-homepage TD-03/HD-05). Do not touch them.

Worktrees are disabled; commit directly on main. Do not commit .planning files (orchestrator does).
</context>

<tasks>

<task id="1" name="Entity consolidation and one identity sentence">
<files>src/lib/seo/site.ts, src/lib/seo/schemas.ts, src/app/layout.tsx, src/app/page.tsx, src/__tests__/seo/schemas.test.ts</files>
<action>
1. src/lib/seo/site.ts: add and export
   - `IDENTITY_SENTENCE` (verbatim above)
   - `PERSON_ID = \`${SITE_URL}/#person\``
   - `ORG_ID = 'https://prometheus.today/#organization'`
   - `PERSON_IMAGE = \`${SITE_URL}/opengraph-image\`` (the root OG image is the only stable portrait asset served by the site).
2. src/lib/seo/schemas.ts:
   - `personRef()` helper returning `{ '@type': 'Person', '@id': PERSON_ID, name: 'Monty Singer', url: SITE_URL }`.
   - buildPersonSchema: add `'@id': PERSON_ID`, `description: IDENTITY_SENTENCE`, `image: PERSON_IMAGE`, `knowsAbout: ['Artificial intelligence', 'AI integrations', 'Entrepreneurship', 'Writing', 'Biology', 'Self-improvement']`, `email: 'mailto:monty@prometheus.today'`; `worksFor` becomes `{ '@type': 'Organization', '@id': ORG_ID, name: 'Prometheus', url: 'https://prometheus.today' }`. Keep sameAs and alumniOf.
   - New `buildOrganizationSchema()`: `@type Organization`, `@id ORG_ID`, name Prometheus, url prometheus.today, `description: 'An applied AI company: AI integrations and education for businesses.'`, `founder: personRef()`.
   - buildWebSiteSchema: `publisher: personRef()`; add `'@id': \`${SITE_URL}/#website\``.
   - buildBlogPostingSchema: `author: personRef()`, `publisher: personRef()`.
   - New `buildProjectSchema(project: { title, slug, description, coverPageId?: string | null, lastEdited?: string, tags?: string[], externalUrl?: string })` → `@type CreativeWork`, name, description, url canonical(`/building/${slug}`), `author: personRef()`, optional image via `/api/notion-cover?pageId=`, optional dateModified, optional `keywords: tags.join(', ')`, optional `sameAs: externalUrl` when it is an http(s) URL.
3. src/app/layout.tsx: `SITE_DESCRIPTION = IDENTITY_SENTENCE` (import it). Emit `<JsonLd data={buildOrganizationSchema()} />` next to the WebSite node. Add Bing verification: `verification: { google: '...', other: process.env.BING_SITE_VERIFICATION ? { 'msvalidate.01': process.env.BING_SITE_VERIFICATION } : undefined }` (omit `other` entirely when unset; Next renders nothing for undefined).
4. src/app/page.tsx: unchanged except it still emits Person (already does).
5. Add src/__tests__/seo/schemas.test.ts: Person has @id PERSON_ID and description === IDENTITY_SENTENCE; WebSite.publisher['@id'] === PERSON_ID; BlogPosting author['@id'] === PERSON_ID; Organization.founder['@id'] === PERSON_ID; buildProjectSchema returns CreativeWork with author @id; no output string contains an em dash (—).
</action>
<verify>npx vitest run src/__tests__/seo</verify>
<done>Schema builders share one Person @id, layout emits Organization, meta description equals IDENTITY_SENTENCE, tests green. Commit: `feat(seo): link Person, WebSite, Organization and articles by @id; one identity sentence`</done>
</task>

<task id="2" name="About page, llms.txt, footer link, sitemap">
<files>src/app/about/page.tsx, src/app/llms.txt/route.ts, src/components/layout/site-footer.tsx, src/app/sitemap.ts, next.config.ts, src/__tests__/seo/sitemap.test.ts, src/__tests__/seo/llms-txt.test.ts</files>
<action>
1. next.config.ts: delete the `/about` → `/` redirect. Change `/links` redirect destination to `/about`. Leave everything else.
2. src/app/about/page.tsx (Server Component, `export const revalidate = 1800` not needed; fully static):
   - metadata: title "About", description = IDENTITY_SENTENCE, canonical "/about", openGraph type "profile", url "/about".
   - JSON-LD: `<Breadcrumbs items={[{name:'Home',href:'/'},{name:'About'}]} />` (sr-only, emits BreadcrumbList) and `<JsonLd data={buildFaqPageSchema(FAQ)} />`.
   - Structure: PageHeroBand tone="ink" title="About" crumb="Home / About" sub={IDENTITY_SENTENCE}. Then sections separated by RuleStrong, each an h2 in the site's mono uppercase label style plus short prose (max ~60ch measure, `px-6 md:px-40` like /contact). Sections and copy (write in Monty's voice, first person, no hype, no em dashes; you may lightly tighten but keep facts exactly):
     - "Who I am": 2 to 3 sentences. "I'm Monty Singer. I founded Prometheus, an applied AI company: we do AI integrations and education for businesses. I write essays here and a monthly letter called Monty Monthly."
     - "Prometheus": what it does in plain words, link to https://prometheus.today (rel noopener noreferrer, target _blank).
     - "Education": "Georgetown University." one line.
     - "Writing": essays at /writing; Monty Monthly at montymonthly.substack.com; RSS at /blog/feed.xml.
     - "Interests": technology, biology, self-improvement, and a link to /#loves ("Things I love outside of work").
     - "Contact": /contact, email monty@prometheus.today, /advice for anonymous notes.
     - "Questions people ask" (visible FAQ, same array feeds the schema; 4 items):
       Q "What is Prometheus?" A "Prometheus is an applied AI company I founded. We integrate AI into how businesses actually operate and teach teams to use it well. More at prometheus.today."
       Q "What does Monty Singer write about?" A "Philosophy, technology, building things, and how to live an attentive life. Essays live at montysinger.com/writing. Monty Monthly is the newsletter."
       Q "How do I get in touch with Monty Singer?" A "Email monty@prometheus.today or use montysinger.com/contact. Anonymous notes go through montysinger.com/advice."
       Q "Where did Monty Singer go to school?" A "Georgetown University."
   - Every fact must come from the allowed list. No location, no other schools, no ages, no dates.
3. src/app/llms.txt/route.ts: `export const revalidate = 1800`. GET builds markdown:
   ```
   # Monty Singer

   > {IDENTITY_SENTENCE}

   Personal site of Monty Singer. Canonical facts: founder of Prometheus (https://prometheus.today), an applied AI company doing AI integrations and education for businesses. Georgetown University. Writes essays and the Monty Monthly newsletter. Contact: monty@prometheus.today.

   ## Pages
   - [About](https://montysinger.com/about): who Monty is, FAQ
   - [Building](https://montysinger.com/building): projects
   - [Writing](https://montysinger.com/writing): essays
   - [Contact](https://montysinger.com/contact)
   - [RSS feed](https://montysinger.com/blog/feed.xml)

   ## Essays
   - [Title](url): description (one per published post; description may be empty, omit the colon then)

   ## Projects
   - [Title](url): description

   ## Elsewhere
   - Prometheus: https://prometheus.today
   - Monty Monthly: https://montymonthly.substack.com
   - X: https://x.com/themontysinger
   - LinkedIn: https://linkedin.com/in/monty-singer
   - GitHub: https://github.com/MSizzle
   ```
   Use SITE_URL, not hardcoded host. Posts via getPublishedPosts(), projects via getPublishedProjects(), each wrapped in try/catch → [] and gated on env like feed.xml. Response headers: `Content-Type: text/markdown; charset=utf-8`, `Cache-Control: public, max-age=0, s-maxage=1800`. Strip em dashes from any Notion-sourced text (`.replace(/—/g, ':')`). Verify no `<`/`>` injection concerns: it is plain text.
4. src/components/layout/site-footer.tsx: add `{ label: "About", href: "/about" }` as the first EXPLORE item. Check src/__tests__/components/footer.test.tsx still passes (update expected link counts if it asserts them).
5. src/app/sitemap.ts: replace `lastModified: new Date()` on every static route. Use a `STATIC_LAST_MODIFIED` map of ISO dates reflecting the last real content change: `/` and `/building` and `/writing` "2026-09-16", `/contact` "2026-09-20", `/advice` "2026-09-21", `/about` "2026-09-21". Add `/about` at priority 0.8 monthly. Update the comment block.
6. src/__tests__/seo/sitemap.test.ts: add a case that `/about` is present, and a case that no static entry's lastModified is within 60s of Date.now(). Add src/__tests__/seo/llms-txt.test.ts mocking @/lib/notion and @/lib/notion-projects (one post, one project) and asserting: 200, content-type text/markdown, body starts with "# Monty Singer", contains the IDENTITY_SENTENCE blockquote, the post URL, the project URL, and no "—".
</action>
<verify>npx vitest run src/__tests__/seo src/__tests__/components/footer.test.tsx</verify>
<done>/about builds, footer links it, /links redirects there, llms.txt route exists, sitemap dates honest, tests green. Commit: `feat(seo): add /about with FAQ schema, /llms.txt, honest sitemap dates`</done>
</task>

<task id="3" name="IndexNow, project schema, slug case, cover cache">
<files>src/lib/seo/indexnow.ts, src/app/indexnow-key.txt/route.ts, src/app/api/revalidate/route.ts, src/app/api/notion-cover/route.ts, src/lib/notion-projects.ts, src/app/building/[slug]/page.tsx, .env.example</files>
<action>
1. src/lib/seo/indexnow.ts:
   ```ts
   export async function pingIndexNow(paths: string[]): Promise<void>
   ```
   Reads `process.env.INDEXNOW_KEY`; returns immediately if unset. POSTs JSON `{ host, key, keyLocation: \`${SITE_URL}/indexnow-key.txt\`, urlList: paths.map(p => canonical(p)) }` to `https://api.indexnow.org/indexnow` with `Content-Type: application/json; charset=utf-8`, 5s AbortSignal.timeout, wrapped in try/catch that swallows everything (log with console.warn). Never throws.
2. src/app/indexnow-key.txt/route.ts: `export const dynamic = 'force-dynamic'`. GET returns the key as `text/plain` with 200, or 404 when INDEXNOW_KEY is unset. (Lives outside /api so robots.txt does not block it.)
3. src/app/api/revalidate/route.ts: after revalidating, `await pingIndexNow(PATHS)` inside its own try/catch; response JSON gains `indexnow: boolean` (true when key set). Extend PATHS with "/about" and "/llms.txt"? No: llms.txt is a route with its own revalidate; keep PATHS as is plus "/about" is static so skip. Leave PATHS unchanged.
4. src/app/api/notion-cover/route.ts: change the success Cache-Control to `public, max-age=86400, s-maxage=31536000, stale-while-revalidate=604800`; update the explanatory comment (browser cache one day, CDN a year, stale for a week). Do the same in src/app/api/notion-image/route.ts if it carries the identical header.
5. src/lib/notion-projects.ts: in extractProjectProperties, lowercase the slug (`.trim().toLowerCase()`). Notion's rich_text equals filter is case-insensitive (verified live: /building/gene-own and /building/Gene-own both 200), so lookups keep working.
6. src/app/building/[slug]/page.tsx: at the top of the page component, if `slug !== slug.toLowerCase()` call `permanentRedirect(\`/building/${slug.toLowerCase()}\`)` from next/navigation (before any fetch). Emit `<Breadcrumbs items={[{name:'Home',href:'/'},{name:'Building',href:'/building'},{name:project.title}]} />` and `<JsonLd data={buildProjectSchema({ title, slug, description, coverPageId: project.cover ? project.id : null, lastEdited, tags, externalUrl })} />`, mirroring the blog page. Check the /building index page and any homepage links build hrefs from project.slug (they do; now lowercase automatically).
7. .env.example: add
   ```
   # --- Search engines ---
   # Bing Webmaster Tools meta verification token (msvalidate.01). Optional.
   BING_SITE_VERIFICATION=
   # IndexNow key (any 32+ char hex string). Served at /indexnow-key.txt; /api/revalidate pings Bing/Yandex with it. Optional.
   INDEXNOW_KEY=
   ```
8. Run `npm test` (expect only the 3 known failures) and `npm run build`. Fix anything the build flags.
</action>
<verify>npm test 2>&1 | tail -15 && npm run build 2>&1 | tail -25</verify>
<done>IndexNow plumbing behind env, project pages carry schema, mixed-case slugs 308 to lowercase, covers cache a day in browser, build green. Commit: `feat(seo): IndexNow ping, project JSON-LD, lowercase project slugs, longer cover cache`</done>
</task>

</tasks>

<verification>
- `curl -s localhost:3000/about | grep -c 'ld+json'` ≥ 3 (WebSite, Organization, BreadcrumbList, FAQPage)
- `curl -s localhost:3000/llms.txt | head -3` shows H1 + blockquote
- `curl -sI localhost:3000/building/Gene-own` → 308 to lowercase
- No `—` in any new file: `grep -rn $'—' src/app/about src/app/llms.txt src/lib/seo`
</verification>

<success_criteria>
All must_haves truths hold; three atomic commits on main; SUMMARY.md written at .planning/quick/260921-ed0-seo-geo-hardening-about-page-llms-txt-en/260921-ed0-SUMMARY.md listing commits, files, and anything deferred (Bing verification token and INDEXNOW_KEY are user/env actions, not code).
</success_criteria>
