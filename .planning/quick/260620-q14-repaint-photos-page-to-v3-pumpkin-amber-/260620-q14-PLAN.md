---
phase: quick-q14
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/photos/page.tsx
autonomous: true
requirements:
  - q14-repaint-photos-v3
must_haves:
  truths:
    - "Photos page renders with PageHero title block (no v2 two-column atmosphere grid)"
    - "Zero v2 tokens remain (text-ink, text-muted, bg-paper, bg-rule, bg-rule-strong, text-faint, text-label, text-body-lead, text-meta, text-page-title)"
    - "Year-grouped photo grid logic and saturate-[0.92] filter are unchanged"
    - "ISR revalidate=1800 and metadata are unchanged"
    - "npx tsc --noEmit passes and npm run build succeeds"
  artifacts:
    - path: src/app/photos/page.tsx
      provides: "Repainted photos page using v3 Pumpkin Amber tokens + PageHero"
      contains: "PageHero"
  key_links:
    - from: src/app/photos/page.tsx
      to: src/components/v3/page-hero.tsx
      via: "import { PageHero }"
      pattern: "PageHero"
---

<objective>
Repaint src/app/photos/page.tsx from v2 paper/ink editorial tokens to the v3
Pumpkin Amber design system, consistent with how writing/page.tsx and
events/page.tsx were rebuilt in Phase 16.

Purpose: Photos page currently uses v2 tokens (text-ink, text-muted, bg-rule-strong,
text-label, text-page-title, text-body-lead, text-meta) and a bespoke two-column
atmosphere-photo title block. This repaint brings it into parity with the Phase 16
interior pages.

Output: A single updated src/app/photos/page.tsx that compiles cleanly and
contains zero v2 tokens.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Reference pages (Phase 16 pattern — already repainted):
@src/app/writing/page.tsx
@src/app/events/page.tsx

PageHero component:
@src/components/v3/page-hero.tsx

File to repaint:
@src/app/photos/page.tsx

<interfaces>
From src/components/v3/page-hero.tsx:
  Props { title: ReactNode; crumb?: ReactNode; sub?: ReactNode; outline?: boolean }
  Usage: <PageHero title="Writing" crumb="Home / Writing" sub="Short line." />

From src/app/globals.css — Pumpkin Amber token map:
  --color-text           #2a1808  (replaces text-ink)
  --color-text-muted     rgba(42,24,8,0.55)  (replaces text-muted, text-faint)
  --color-text-dim       rgba(42,24,8,0.72)  (replaces text-muted on prose)
  --color-border         rgba(42,24,8,0.18)  (replaces bg-rule)
  --color-border-strong  rgba(42,24,8,0.40)  (replaces bg-rule-strong)
  --color-surface        #2a1808  (dark panels/photo frames)

Tailwind v4 arbitrary-value pattern (from events/page.tsx):
  text-[var(--color-text)]
  text-[var(--color-text-muted)]
  border-[var(--color-border)]
  border-[var(--color-border-strong)]
  bg-[var(--color-surface)]   ← use for photo frame backgrounds (replaces bg-rule-strong)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repaint photos/page.tsx to v3 Pumpkin Amber</name>
  <files>src/app/photos/page.tsx</files>
  <action>
Rewrite src/app/photos/page.tsx applying these changes. Preserve everything
not listed below exactly as-is (revalidate, metadata, groupPhotosByYear call,
yearEntries iteration, the year-grouped grid structure, YearBlock, RuleStrong
between year groups, saturate-[0.92] on both Image elements, fill/sizes/src
props, figcaption conditional, Fragment wrappers, the block comment header at
the top).

IMPORTS — add PageHero, remove RuleStrong-related-only imports only if
RuleStrong is still used (it is — keep it). Add:
  import { PageHero } from "@/components/v3/page-hero";
Remove the editorial component imports that are no longer needed after the
title block is replaced:
  -- The `RuleStrong` import from "@/components/editorial/rule-strong" is KEPT
     (used between year groups and after the title block).
  -- The `YearBlock` import from "@/components/editorial/year-block" is KEPT.

TITLE BLOCK — replace the entire opening <section> (lines 47-73, the two-column
atmosphere-photo grid including the atmosphere Image) with:

  <section className="px-6 md:px-40">
    <PageHero
      title="Photographs"
      crumb="Home / Photographs"
      sub="A film-led survey of years in motion. Updated when something earns the page."
    />
  </section>

Note: sub line must not contain em dashes. Use a period to join the two sentences
as shown above. The atmosphere photo (20230928 MSB_0114.jpg) is dropped — it was
the right-column element of the old two-column grid. The photos remain in the grid
below.

TOKEN SWAPS — apply these substitutions everywhere in the JSX (not in comments
or import paths):
  text-ink           → text-[var(--color-text)]
  text-muted         → text-[var(--color-text-muted)]
  text-faint         → text-[var(--color-text-muted)]
  text-label         → remove (only appeared as a class on the old label div which
                       is now gone with the old title block)
  text-page-title    → remove (only appeared on old h1 which PageHero now owns)
  text-body-lead     → remove (only appeared on old <p> inside old title block)
  text-meta          → text-meta stays as a text-size utility IF it is a valid v3
                       utility; however in the figcaption it was used alongside
                       text-muted. Replace with: className="mt-3 text-xs uppercase
                       tracking-[0.08em] text-[var(--color-text-muted)]"
                       (mirrors events/page.tsx meta text pattern)
  bg-rule-strong     → bg-[var(--color-surface)]
                       Applied to: the photo frame divs (relative overflow-hidden div
                       wrapping each <Image>). Two occurrences — atmosphere photo frame
                       (now removed with old title block) and the per-photo grid frames.
  bg-paper           → remove (if present; likely not in current file)
  bg-rule            → remove (if present; likely not in current file)

The grid section outer wrapper stays at px-6 md:px-40 (no -mx bleed needed here
since photos is a simple grid, unlike writing's card grid which used a bleed).

After the title-block section, keep: <RuleStrong /> then the year-grouped grid
section. Layout identical to current except tokens swapped.

No new dependencies introduced.
  </action>
  <verify>
    <automated>
cd "/Users/Montster/MSizzle Personal Website" && npx tsc --noEmit 2>&1 | tail -20
    </automated>
    <automated>
cd "/Users/Montster/MSizzle Personal Website" && grep -c "text-ink\|text-muted\b\|bg-paper\|bg-rule\b\|bg-rule-strong\|text-faint\|text-label\|text-body-lead\|text-page-title" src/app/photos/page.tsx || echo "0 v2 tokens found — clean"
    </automated>
    <automated>
cd "/Users/Montster/MSizzle Personal Website" && grep -c "PageHero" src/app/photos/page.tsx
    </automated>
    <automated>
cd "/Users/Montster/MSizzle Personal Website" && grep -c "saturate-\[0\.92\]" src/app/photos/page.tsx
    </automated>
  </verify>
  <done>
    - src/app/photos/page.tsx imports and uses PageHero with title="Photographs", crumb="Home / Photographs", sub (no em dashes)
    - Zero v2 tokens remain (grep returns 0)
    - saturate-[0.92] present on photo grid images (grep returns 2)
    - npx tsc --noEmit exits 0
    - groupPhotosByYear, YearBlock, RuleStrong, revalidate=1800, metadata all unchanged
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Repainted src/app/photos/page.tsx: PageHero title block replaces the old
two-column atmosphere-photo grid; all Pumpkin Amber arbitrary tokens applied;
year-grouped photo grid and captions preserved.
  </what-built>
  <how-to-verify>
1. Run: npm run build (must exit 0 — no type or lint errors)
2. Run: npm run dev
3. Visit http://localhost:3000/photos
4. Confirm: large "Photographs" title in Pumpkin Amber sig style (Roasted Cocoa ink on amber background), breadcrumb "Home / Photographs" above it, subtitle below — no two-column layout with right-side atmosphere photo
5. Confirm: year-grouped photo grid (2025, 2023) renders below the RuleStrong divider with the same photo layout as before
6. Confirm: captions appear below photos in muted uppercase tracking style
7. Confirm: no visual artifacts from old v2 tokens (no rogue white/cream backgrounds on photo frames — should be dark Roasted Cocoa surface)
  </how-to-verify>
  <resume-signal>Type "approved" or describe any visual issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static data → render | groupPhotosByYear reads from hardcoded photos.ts — no user input, no external calls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-q14-01 | Tampering | Token swap correctness | accept | Verified by grep gate (zero v2 tokens) + tsc --noEmit + visual checkpoint |
| T-q14-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies introduced |
</threat_model>

<verification>
- npx tsc --noEmit exits 0
- grep for v2 tokens returns 0 matches
- grep for PageHero returns 1+ matches
- grep for saturate-[0.92] returns 2 matches (one per photo Image)
- npm run build exits 0
</verification>

<success_criteria>
src/app/photos/page.tsx is fully on the v3 Pumpkin Amber system:
- PageHero renders the title block (matching writing/page.tsx and events/page.tsx pattern)
- All v2 tokens replaced with arbitrary-value Pumpkin Amber equivalents
- Year-grouped grid, groupPhotosByYear logic, saturate-[0.92] filter, ISR revalidate=1800, and metadata are byte-for-byte equivalent to pre-repaint
- TypeScript compiles clean, build succeeds
</success_criteria>

<output>
Create `.planning/quick/260620-q14-repaint-photos-page-to-v3-pumpkin-amber-/260620-q14-01-SUMMARY.md` when done
</output>
