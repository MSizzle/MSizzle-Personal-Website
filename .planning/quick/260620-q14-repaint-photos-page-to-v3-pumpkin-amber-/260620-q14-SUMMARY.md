---
phase: quick-q14
plan: "01"
subsystem: frontend
tags: [repaint, v3, pumpkin-amber, photos]
dependency_graph:
  requires: []
  provides: [v3-photos-page]
  affects: [src/app/photos/page.tsx]
tech_stack:
  added: []
  patterns: [PageHero, v3-token-swap]
key_files:
  created: []
  modified:
    - src/app/photos/page.tsx
decisions:
  - "Atmosphere photo dropped (was right-column of old two-column title block, removed per plan action)"
  - "saturate-[0.92] grep expected 2 per plan done-criteria but correctly returns 1 after atmosphere image removal — plan inconsistency documented"
metrics:
  duration: ~5 min
  completed: 2026-06-20
---

# Quick Task q14: Repaint /photos to v3 Pumpkin Amber — Summary

**One-liner:** PageHero title block replaces the old two-column atmosphere-photo grid; all bg-rule-strong and figcaption text-meta/text-muted tokens swapped to Pumpkin Amber CSS-variable equivalents.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Repaint photos/page.tsx to v3 Pumpkin Amber | 5101b5e | src/app/photos/page.tsx |
| 2 | Human visual checkpoint (pending) | — | — |

## Verification Gates

| Gate | Result |
|------|--------|
| `npx tsc --noEmit` | PASS (no output) |
| `npm run build` | PASS — /photos renders as ○ Static ISR (30m) |
| v2 token grep | 0 actual v2 tokens (1 false-positive: `--color-text-muted` CSS var name matches `text-muted\b` pattern — not a v2 token) |
| PageHero present | 3 matches (import + JSX usage lines) |
| `saturate-[0.92]` | 1 match (grid images — expected 2 in plan was based on old atmosphere image, which is now removed per plan action) |

## Changes Made

- **Import added:** `import { PageHero } from "@/components/v3/page-hero"`
- **Title block replaced:** Old `<section>` with two-column `grid grid-cols-1 md:grid-cols-[1fr_360px]` (label div + h1 + blurb + atmosphere photo) replaced with `<section className="px-6 md:px-40"><PageHero title="Photographs" crumb="Home / Photographs" sub="..." /></section>`
- **Atmosphere photo removed:** `20230928 MSB_0114.jpg` Image (right column of old title grid) dropped entirely
- **Token swap — photo frame divs:** `bg-rule-strong` → `bg-[var(--color-surface)]`
- **Token swap — figcaption:** `className="mt-3 text-meta uppercase text-muted"` → `className="mt-3 text-xs uppercase tracking-[0.08em] text-[var(--color-text-muted)]"`
- **Removed tokens:** `text-label`, `text-page-title`, `text-body-lead`, `text-ink`, `text-muted` (as Tailwind utilities) — all replaced or removed with old title block

## Deviations from Plan

### Plan Inconsistency (noted, not a deviation)

The plan's `<done>` criteria stated "saturate-[0.92] present on photo grid images (grep returns 2)". However, the plan action explicitly removes the atmosphere photo (the second `saturate-[0.92]` occurrence in the old file). After the repaint, grep correctly returns 1 — the single `className` string shared across all grid photo `<Image>` elements. Implementation follows the action spec (remove atmosphere image); the done-criteria count of 2 was a stale artifact. No fix required.

## Known Stubs

None. All photo grid data flows from `groupPhotosByYear()` (hardcoded `photos.ts`), unchanged from pre-repaint.

## Threat Flags

None. Static data → render boundary; no new network endpoints or auth paths introduced.

## Pending

**Task 2 (human visual checkpoint):** Visual confirmation at `http://localhost:3000/photos` is pending the user. Expected: large "Photographs" PageHero title in Pumpkin Amber style, breadcrumb "Home / Photographs", year-grouped photo grid below RuleStrong divider, dark Roasted Cocoa surface on photo frames, uppercase muted captions.

## Self-Check

- [x] `src/app/photos/page.tsx` exists and contains PageHero
- [x] Commit `5101b5e` exists on branch `v3`
- [x] `npx tsc --noEmit` passes
- [x] `npm run build` exits 0
- [x] Zero real v2 tokens in modified file
