---
phase: 06-pre-launch-qa
plan: 06
type: execute
wave: 3
depends_on: [01, 02, 03, 04, 05]
files_modified:
  - .planning/phases/06-pre-launch-qa/06-GO-NO-GO.md
autonomous: false
requirements: []
decisions_addressed:
  - D-09
  - D-10
  - D-15

must_haves:
  truths:
    - "06-GO-NO-GO.md exists and aggregates evidence from 06-01 (build + security), 06-02 (Lighthouse triple-coverage: desktop CLI + mobile CLI + PSI mobile), 06-03 (UAT), 06-04 (cross-browser + FOUC + Notion long-tail), 06-05 (SEO re-audit)"
    - "Every Phase 6 ROADMAP success criterion (1-5) has an explicit pass/fail verdict in 06-GO-NO-GO.md backed by a link to prior-plan evidence"
    - "06-GO-NO-GO.md lists every open FIX sub-plan (`06-0X-FIX-*.md`) and its launch-impact verdict (blocking vs non-blocking)"
    - "The final line of 06-GO-NO-GO.md is a single unambiguous verdict: `RECOMMENDATION: GO` or `RECOMMENDATION: NO-GO`"
    - "If NO-GO: the report lists every blocker with a link to the corresponding FIX sub-plan"
    - "Per D-15, NO post-Phase-6 DNS cutover todo is created on GO — montysinger.com is already live via Namecheap→Vercel, Super is disconnected, DSGN-06 is already complete in the reconciled Traceability table"
  artifacts:
    - path: ".planning/phases/06-pre-launch-qa/06-GO-NO-GO.md"
      provides: "Final Phase 6 launch recommendation. Aggregates all evidence, makes the call."
      contains: "RECOMMENDATION:"
      min_lines: 40
  key_links:
    - from: "06-GO-NO-GO.md verdict"
      to: "user decision to launch or re-enter FIX loop"
      via: "human reads the recommendation"
      pattern: "RECOMMENDATION: (GO|NO-GO)"

threat_model:
  summary: "Pure aggregation + documentation. No code changes, no runtime surface. No secrets touched (all evidence files were already scrubbed in 06-01..06-05)."
  threats: []
---

<objective>
Produce the Phase 6 go/no-go decision.

Aggregate evidence from:
- 06-01 (build log, env-check, bundle-secret-grep across both `.next/static/chunks/` AND `.vercel/output/static/_next/static/chunks/` per D-14; reconciled REQUIREMENTS.md + ROADMAP.md Phase Overview per D-08)
- 06-02 (Lighthouse triple-coverage per D-04: desktop CLI + mobile CLI + PSI mobile, every profile gated by D-05)
- 06-03 (28-requirement UAT)
- 06-04 (Notion long-tail + dark-mode FOUC + Safari + Firefox)
- 06-05 (live SEO re-audit)

Evaluate against the 5 ROADMAP Phase 6 success criteria and the blocking-fail rubric from CONTEXT D-09. Produce `06-GO-NO-GO.md` with a single-line `RECOMMENDATION: GO` or `RECOMMENDATION: NO-GO`.

**Per D-15 (updated):** `montysinger.com` is **already fully live on Vercel via Namecheap DNS** — Super is disconnected. DSGN-06 is therefore marked `complete` in the 06-01 reconciliation, **not** `partial`. There is **no post-Phase-6 DNS cutover to queue**. On GO, this plan creates NO new todos — the phase is simply closed.

If NO-GO: the report lists every blocking FIX sub-plan with a one-line impact statement. The user decides whether to execute FIX sub-plans sequentially or reset.

Plan `autonomous: false` because the human owns the actual launch decision and because pressing GO is a call that should not be automated.

Scope bounded by D-09 (blocking vs non-blocking), D-10 (FIX sub-plans already exist by the time this plan runs), D-15 (domain cutover is ALREADY COMPLETE — no post-Phase-6 follow-up needed).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-pre-launch-qa/06-CONTEXT.md
@.planning/phases/06-pre-launch-qa/06-01-PLAN.md
@.planning/phases/06-pre-launch-qa/06-02-PLAN.md
@.planning/phases/06-pre-launch-qa/06-03-PLAN.md
@.planning/phases/06-pre-launch-qa/06-04-PLAN.md
@.planning/phases/06-pre-launch-qa/06-05-PLAN.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Aggregate evidence into 06-GO-NO-GO.md</name>
  <files>.planning/phases/06-pre-launch-qa/06-GO-NO-GO.md</files>
  <read_first>
    - .planning/phases/06-pre-launch-qa/evidence/build/vercel-build.log (from 06-01)
    - .planning/phases/06-pre-launch-qa/evidence/build/bundle-secret-grep.txt (from 06-01; verifies `.next/static/chunks/` AND `.vercel/output/static/_next/static/chunks/` are both scanned per D-14)
    - .planning/phases/06-pre-launch-qa/evidence/lighthouse/SUMMARY.md (from 06-02; three profile tables per D-04)
    - .planning/phases/06-pre-launch-qa/evidence/lighthouse/psi-mobile-scores.md (from 06-02)
    - .planning/phases/06-pre-launch-qa/06-UAT.md (from 06-03)
    - .planning/phases/06-pre-launch-qa/06-UAT-cross-browser.md (from 06-04)
    - .planning/phases/06-pre-launch-qa/06-UAT-seo.md (from 06-05)
    - .planning/ROADMAP.md §Phase 6 (the 5 success criteria) + reconciled Phase Overview (from 06-01 Task 1 Part B)
    - .planning/REQUIREMENTS.md (reconciled Traceability table from 06-01 Task 1 Part A; DSGN-06 row should read `complete` per D-15)
    - .planning/phases/06-pre-launch-qa/06-CONTEXT.md (D-09 blocking-fail rubric, D-15 domain status)
    - (Any `06-0X-FIX-*.md` sub-plans spawned during 06-02..06-05)
  </read_first>
  <action>
Write `06-GO-NO-GO.md` using this structure. Populate every field from the evidence artifacts, do not hand-wave.

```markdown
---
phase: 06-pre-launch-qa
doc_type: go-no-go
audit_url: https://montysinger.com
commit_sha: <copy from 06-02 SUMMARY.md>
evaluated: <UTC timestamp>
recommendation: <GO|NO-GO>
---

# Phase 6 Go / No-Go Report

**Commit under audit:** <sha>
**Live URL:** https://montysinger.com (Namecheap DNS → Vercel, Super disconnected per D-15)
**Audit completed:** <date>

## 1. ROADMAP Phase 6 success criteria (verdict per criterion)

| # | Criterion (from ROADMAP §Phase 6) | Evidence source | Verdict |
|---|-----------------------------------|-----------------|---------|
| 1 | Notion images in a ≥2h-built post still load | 06-UAT-cross-browser.md §1, evidence/notion-longtail/image-status.txt | <PASS/FAIL> |
| 2 | Lighthouse live-URL scores ≥ 90/95/95/100 on ALL three profiles (D-04: desktop CLI, mobile CLI, PSI mobile) | evidence/lighthouse/SUMMARY.md Profiles 1/2/3 | <PASS/FAIL> |
| 3 | `vercel build` exits clean, no 429, no TS, no ESLint errors | evidence/build/vercel-build.log | <PASS/FAIL> |
| 4 | Dark-mode first-visit FOUC passes (incognito, both themes) | 06-UAT-cross-browser.md §2, evidence/dark-mode/ | <PASS/FAIL> |
| 5 | All 28 v1 requirements confirmed on live URL | 06-UAT.md Summary | <PASS/FAIL> |

## 2. Additional blocking gates (per CONTEXT D-09)

| Gate | Evidence | Verdict |
|------|----------|---------|
| No NOTION_TOKEN / secret_ / ntn_ in client bundle, BOTH `.next/static/chunks/` AND `.vercel/output/static/_next/static/chunks/` swept (D-14) | evidence/build/bundle-secret-grep.txt | <PASS/FAIL> |
| Safari page transitions + Lenis + backdrop-filter (D-13) | 06-UAT-cross-browser.md §3 | <PASS/FAIL> |
| SEO compliance on live HTML (D-01..D-45) | 06-UAT-seo.md Summary | <PASS/FAIL> |
| REQUIREMENTS.md Traceability + ROADMAP Phase Overview reconciled (D-08) | .planning/REQUIREMENTS.md + .planning/ROADMAP.md (post-06-01) | <PASS/FAIL> |

## 3. Sub-threshold / partial items (D-09 non-blocking tier)

(List items that are non-blocking per D-09 — cosmetic nits, minor copy polish, optional animation improvements, close-but-not-below Perf scores. These go to `/gsd-add-todo` after GO, not to FIX sub-plans.)

| Item | Source | Severity | Proposed todo |
|------|--------|----------|---------------|
|      |        |          |               |

## 4. Open FIX sub-plans (blocking)

(List every `06-0X-FIX-*.md` file currently in `.planning/phases/06-pre-launch-qa/`. Any row in this table = automatic NO-GO.)

| FIX plan | Trigger | Source plan | Status |
|----------|---------|-------------|--------|
|          |         |             |        |

## 5. Deferred / out-of-scope reminders (CONTEXT §deferred)

- ~~**Domain cutover**~~ — **already complete** per D-15. `montysinger.com` is live on Vercel via Namecheap DNS; Super is disconnected. DSGN-06 is `complete` in the 06-01 reconciled Traceability table. No post-Phase-6 todo is needed.
- **Playwright E2E automation**: deferred to post-launch v1.1.
- **Bundle-size regression budget (`size-limit`)**: deferred.
- **Public analytics page (ANLY-V2-01)**: v2 backlog.

## 6. Cost / budget posture

- Vercel free tier: confirm bandwidth + build-minutes + function duration within limits per CLAUDE.md §Vercel Free Tier Fit Check. Spot-check Vercel dashboard and record here.
- Umami + Neon: confirmed live (STATE.md), no recurring cost.

## 7. Risks remaining at launch

(If any, enumerate. Each risk gets a 1-line mitigation or accept statement.)

## 8. Recommendation

<ONE OF EXACTLY:>

- If all criteria in §1 are PASS, all gates in §2 are PASS, and §4 is empty:
  **RECOMMENDATION: GO**
  Next action: proceed with launch announcement. Phase 6 is closed; DSGN-06 was already complete per D-15, so there is no post-Phase-6 DNS follow-up to queue.

- Otherwise:
  **RECOMMENDATION: NO-GO**
  Next action: execute every open FIX sub-plan in §4 via `/gsd-execute-phase 6 --gaps-only`, then re-run 06-02..06-05 as needed, then re-run 06-06 to re-evaluate.

Rationale: <1-3 sentences summarizing why the recommendation follows from the evidence>.
```

Fill every `<placeholder>` from evidence. Do NOT leave ambiguous phrasing like "mostly green" — each row is either PASS or FAIL.

**Important (D-15):** §5 must explicitly show the domain-cutover item as complete (strikethrough + "already complete" note). Do NOT write this as a pending follow-up. Do NOT write §8 in a way that creates a post-Phase-6 DNS todo on GO.
  </action>
  <verify>
    <automated>test -f .planning/phases/06-pre-launch-qa/06-GO-NO-GO.md && grep -qE "RECOMMENDATION: (GO|NO-GO)" .planning/phases/06-pre-launch-qa/06-GO-NO-GO.md && grep -q "Commit under audit:" .planning/phases/06-pre-launch-qa/06-GO-NO-GO.md && grep -q "06-UAT.md" .planning/phases/06-pre-launch-qa/06-GO-NO-GO.md && grep -q "vercel-build.log" .planning/phases/06-pre-launch-qa/06-GO-NO-GO.md && grep -q "already complete per D-15" .planning/phases/06-pre-launch-qa/06-GO-NO-GO.md && ! grep -qi "dns cutover todo" .planning/phases/06-pre-launch-qa/06-GO-NO-GO.md</automated>
  </verify>
  <acceptance_criteria>
    - 06-GO-NO-GO.md exists with commit SHA populated and frontmatter complete
    - Every row in §1 (5 success criteria) has either PASS or FAIL
    - Every row in §2 (additional blocking gates) has either PASS or FAIL
    - Recommendation line is exactly `RECOMMENDATION: GO` or `RECOMMENDATION: NO-GO` (no other phrasing)
    - §1 criterion 2 explicitly references all THREE D-04 Lighthouse profiles (desktop CLI, mobile CLI, PSI mobile)
    - §2 D-14 gate explicitly names BOTH `.next/static/chunks/` and `.vercel/output/static/_next/static/chunks/`
    - §5 lists the domain cutover as ALREADY COMPLETE per D-15 (no pending DNS todo)
    - Rationale section is present and non-empty
    - §4 lists every `06-0X-FIX-*.md` file currently in the phase dir (run `ls .planning/phases/06-pre-launch-qa/06-0*-FIX-*.md 2>/dev/null` to enumerate)
  </acceptance_criteria>
  <done>Aggregated go/no-go report produced with D-15-accurate scope (no phantom post-Phase-6 DNS todo).</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Human sign-off — approve recommendation (GO) or confirm NO-GO</name>
  <what-built>
    Task 1's 06-GO-NO-GO.md. Operator reads the evidence-backed recommendation and decides.
  </what-built>
  <decision>
Approve the launch recommendation.
  </decision>
  <context>
The Task 1 recommendation is evidence-backed but a human must own the actual launch decision.

Per D-15, `montysinger.com` is already live on Vercel (Namecheap DNS, Super disconnected). DSGN-06 is already `complete` in the reconciled Traceability table from 06-01. There is **no pending DNS cutover** — that work was done before Phase 6 started.

For GO: Phase 6 is simply closed. No new plans, no new todos, no DNS steps. Update STATE.md to mark Phase 6 complete and proceed to launch announcement.

For NO-GO: operator acknowledges, then runs `/gsd-execute-phase 6 --gaps-only` against the FIX sub-plans, then re-runs 06-02..06-05 and this plan to re-evaluate.
  </context>
  <options>
    <option id="confirm-go">
      <name>Confirm GO</name>
      <pros>Phase 6 complete. Site is already live at montysinger.com per D-15; no DNS work remains. Move directly to launch announcement.</pros>
      <cons>Closes the Phase 6 audit window. Any regression after this point needs a new phase or quick task.</cons>
      <action-on-select>
Update STATE.md: set `status:` to `Phase 6 complete`, mark Phase 6 row `complete` with today's date in the Phase Status table (STATE.md line 24-30), and remove the DSGN-06 entry from the Known Issues section (STATE.md line 77) since it's now fully complete per D-15.

Do **NOT** run `/gsd-add-todo` for a DNS cutover — per D-15 that work is already done. Do NOT create any new plans.
      </action-on-select>
    </option>
    <option id="confirm-no-go">
      <name>Confirm NO-GO</name>
      <pros>Forces every blocking FIX sub-plan to run before launch. Catches regressions.</pros>
      <cons>Delays launch. Each FIX sub-plan + re-audit costs more time than the original QA walk.</cons>
      <action-on-select>
Leave 06-GO-NO-GO.md as-is. Run `/gsd-execute-phase 06-pre-launch-qa --gaps-only` to execute the FIX sub-plans. After they pass, re-run the relevant upstream plan (02, 03, 04, or 05) and then re-run 06-06.
      </action-on-select>
    </option>
    <option id="override-go-with-caveats">
      <name>Override to GO despite minor non-blocking items</name>
      <pros>Avoids re-running a full audit pass to address items that §3 classifies as non-blocking (cosmetic nits, sub-threshold-but-close metrics, minor copy polish).</pros>
      <cons>Requires documenting the override rationale in §7 of 06-GO-NO-GO.md so future readers see the call was intentional.</cons>
      <action-on-select>
Add a paragraph to §7 (Risks remaining at launch) explaining: (a) which non-blocking items are being accepted, (b) which Phase-6 decision or CONTEXT line authorizes treating them as non-blocking, (c) any mitigation (e.g., "will monitor post-launch and file a quick task if X regresses"). Then follow the `confirm-go` action (update STATE.md, no DNS todo).
      </action-on-select>
    </option>
  </options>
  <resume-signal>
Respond with exactly one of: "confirm-go", "confirm-no-go", or "override-go-with-caveats: <1-3 sentence rationale>". On GO or override, confirm you updated STATE.md Phase 6 row to `complete` and removed DSGN-06 from Known Issues. Do NOT create a DNS cutover todo (D-15 makes it unnecessary).
  </resume-signal>
</task>

</tasks>

<verification>

Checklist for the human QA operator:

- [ ] 06-GO-NO-GO.md exists, commit SHA matches 06-02 and 06-03.
- [ ] Every success criterion (§1 rows 1-5) is explicitly PASS or FAIL.
- [ ] §1 criterion 2 references all three D-04 Lighthouse profiles (desktop CLI, mobile CLI, PSI mobile).
- [ ] §2 D-14 row explicitly names both chunk directories.
- [ ] Every blocking gate (§2 rows) is explicitly PASS or FAIL.
- [ ] §4 enumerates every FIX sub-plan (or states "None").
- [ ] §5 shows domain cutover as ALREADY COMPLETE per D-15 (no pending DNS todo).
- [ ] Recommendation is exactly `RECOMMENDATION: GO` or `RECOMMENDATION: NO-GO`.
- [ ] If GO: STATE.md Phase 6 row marked `complete` with today's date; DSGN-06 removed from Known Issues. NO new todo created.
- [ ] If NO-GO: every FIX sub-plan has a clear execution path via `/gsd-execute-phase`.

</verification>

<rollback>
No code changes to roll back. If the recommendation is wrong (e.g., the operator decides post-hoc that a blocker was missed), re-open 06-GO-NO-GO.md, downgrade the recommendation, and re-execute the relevant FIX plans.
</rollback>
