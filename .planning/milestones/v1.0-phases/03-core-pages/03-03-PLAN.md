---
phase: 03-core-pages
plan: 03
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/lib/notion-projects.ts
  - src/app/projects/page.tsx
  - src/app/projects/[slug]/page.tsx
  - src/components/projects/project-card.tsx
autonomous: true
requirements: [PORT-01, PORT-03]
user_setup:
  - service: notion
    why: "Projects database needed for project data"
    env_vars:
      - name: NOTION_PROJECTS_DATABASE_ID
        source: "Create a Notion database with properties: Name (title), Slug (rich_text), Description (rich_text), Published (checkbox), Date (date), Tags (multi_select), URL (url), Featured (checkbox). Copy the database ID from the URL."
    dashboard_config:
      - task: "Create Projects database in Notion workspace"
        location: "Notion — create new full-page database"

must_haves:
  truths:
    - "Projects listing page shows project cards in a responsive grid"
    - "Each project card displays title, description, thumbnail, and external link"
    - "Clicking a project card title leads to a case study page at /projects/[slug]"
    - "Case study pages render Notion content via NotionRenderer"
    - "Empty state displays 'Projects coming soon.' when no projects exist"
  artifacts:
    - path: "src/lib/notion-projects.ts"
      provides: "Notion data layer for projects"
      exports: ["getPublishedProjects", "getProjectBySlug", "Project"]
    - path: "src/app/projects/page.tsx"
      provides: "Projects listing page"
      contains: "revalidate"
    - path: "src/app/projects/[slug]/page.tsx"
      provides: "Project case study detail page"
      contains: "generateStaticParams"
    - path: "src/components/projects/project-card.tsx"
      provides: "Reusable project card component"
      min_lines: 20
  key_links:
    - from: "src/app/projects/page.tsx"
      to: "src/lib/notion-projects.ts"
      via: "import getPublishedProjects"
      pattern: "import.*getPublishedProjects"
    - from: "src/app/projects/[slug]/page.tsx"
      to: "src/components/notion/notion-renderer.tsx"
      via: "import NotionRenderer for case study content"
      pattern: "import.*NotionRenderer"
    - from: "src/app/projects/[slug]/page.tsx"
      to: "src/lib/notion.ts"
      via: "import getBlocks for fetching page content"
      pattern: "import.*getBlocks"
---

<objective>
Build the projects data layer and pages: Notion projects database client, project card component, listing page at `/projects`, and case study detail pages at `/projects/[slug]`.

Purpose: Portfolio is a core section — visitors see what Monty has built and can dive into case studies.
Output: Working `/projects` and `/projects/[slug]` routes powered by Notion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-core-pages/03-CONTEXT.md
@.planning/phases/03-core-pages/03-UI-SPEC.md
@.planning/phases/03-core-pages/03-RESEARCH.md
@.planning/phases/03-core-pages/03-01-SUMMARY.md

<interfaces>
<!-- Reuse existing Notion patterns from Phase 2 -->

From src/lib/notion.ts:
```typescript
// Rate limiter and retry logic — reuse same pattern
const notion = new Client({ auth: process.env.NOTION_TOKEN })
const limit = pLimit(2)
async function withRetry<T>(fn: () => Promise<T>, retries = 3): Promise<T>

// Query pattern uses dataSources.query (v5 API)
notion.dataSources.query({ data_source_id: DATABASE_ID, filter: {...}, sorts: [...] })

export interface BlogPost {
  id: string; slug: string; title: string; description: string;
  published: boolean; date: string; tags: string[]; cover: string | null; lastEdited: string;
}
```

From src/components/notion/notion-renderer.tsx:
```typescript
export function NotionRenderer({ blocks }: NotionRendererProps): JSX.Element
// Accepts BlockWithChildren[] — same type returned by getBlocks()
```

From src/lib/notion.ts:
```typescript
export async function getBlocks(blockId: string): Promise<BlockObjectResponse[]>
// Recursive — fetches nested children (toggles, synced blocks)
```

From 03-UI-SPEC.md Project Card:
- bg-secondary, border border-border rounded-xl, p-6
- Thumbnail: 16:9 aspect-ratio object-cover rounded-lg
- Title: text-xl font-semibold, mt-4
- Description: text-sm text-[var(--fg-muted)], mt-2, line-clamp-3
- Link: "Check out more information" with ExternalLink icon, accent color, mt-4
- Hover: hover:border-[var(--accent)]/50 transition-colors duration-200
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create projects Notion data layer and project card component</name>
  <files>src/lib/notion-projects.ts, src/components/projects/project-card.tsx</files>
  <read_first>
    - src/lib/notion.ts (copy patterns exactly)
    - src/components/notion/notion-renderer.tsx (for BlockWithChildren type reference)
    - .planning/phases/03-core-pages/03-UI-SPEC.md (project card spec)
    - .planning/phases/03-core-pages/03-RESEARCH.md (Pattern 7 for project data layer)
  </read_first>
  <action>
1. Create `src/lib/notion-projects.ts`:
   - Import `Client` from `@notionhq/client`, types from `@notionhq/client/build/src/api-endpoints`, `pLimit` from `p-limit`
   - Use SAME client instance pattern as notion.ts: `const notion = new Client({ auth: process.env.NOTION_TOKEN })`, same `limit = pLimit(2)`, same `withRetry` function (copy verbatim from notion.ts)
   - `const PROJECTS_DATABASE_ID = process.env.NOTION_PROJECTS_DATABASE_ID!`
   - Export interface:
     ```typescript
     export interface Project {
       id: string
       slug: string
       title: string
       description: string
       image: string | null
       externalUrl: string
       tags: string[]
       featured: boolean
       published: boolean
       lastEdited: string
     }
     ```
   - `extractProjectProperties(page: PageObjectResponse): Project` — same pattern as `extractPageProperties` in notion.ts, but:
     - `externalUrl`: read from `props["URL"]` or `props["url"]` with type `"url"`, fallback `""`
     - `featured`: read from `props["Featured"]` or `props["featured"]` with type `"checkbox"`, fallback `false`
     - `image`: read from `page.cover` (same cover extraction as BlogPost)
   - `export async function getPublishedProjects(): Promise<Project[]>` — same pagination pattern as `getPublishedPosts`, queries `PROJECTS_DATABASE_ID`, filter `Published: true`, sort by `Date` descending
   - `export async function getProjectBySlug(slug: string): Promise<Project | null>` — same pattern as `getPostBySlug`, queries `PROJECTS_DATABASE_ID`
   - `export async function getFeaturedProjects(): Promise<Project[]>` — queries with filter `Published: true AND Featured: true`, limit 3

2. Create `src/components/projects/project-card.tsx` (server component, no "use client"):
   - Import `Link` from `next/link`, `ExternalLink` icon from `lucide-react`, `Project` type from `@/lib/notion-projects`
   - Props: `{ project: Project }`
   - Outer div: `className="rounded-xl border border-[var(--border)] bg-[var(--bg-secondary)] p-6 transition-colors duration-200 hover:border-[var(--accent)]/50"`
   - Thumbnail: if `project.image`, render `<div className="aspect-video w-full overflow-hidden rounded-lg"><img src={project.image} alt={project.title} className="h-full w-full object-cover" loading="lazy" /></div>`. If no image, render `<div className="aspect-video w-full rounded-lg bg-[var(--border)]" />` as placeholder.
   - Title as link to case study: `<Link href={/projects/${project.slug}} className="mt-4 block text-xl font-semibold tracking-tight hover:underline">{project.title}</Link>`
   - Description: `<p className="mt-2 text-sm text-[var(--fg-muted)] line-clamp-3">{project.description}</p>`
   - External link (only if externalUrl exists): `<a href={project.externalUrl} target="_blank" rel="noopener noreferrer" className="mt-4 inline-flex items-center gap-1 text-sm text-[var(--accent)] hover:underline">Check out more information <ExternalLink size={16} /></a>`
   - Export: `export function ProjectCard({ project }: { project: Project })`
  </action>
  <verify>
    <automated>cd "/Users/Montster/MSizzle Personal Website" && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/notion-projects.ts contains `export interface Project`
    - src/lib/notion-projects.ts contains `NOTION_PROJECTS_DATABASE_ID`
    - src/lib/notion-projects.ts contains `export async function getPublishedProjects`
    - src/lib/notion-projects.ts contains `export async function getProjectBySlug`
    - src/lib/notion-projects.ts contains `export async function getFeaturedProjects`
    - src/lib/notion-projects.ts contains `dataSources.query` (v5 API)
    - src/lib/notion-projects.ts contains `withRetry` and `pLimit`
    - src/lib/notion-projects.ts contains `featured: boolean` in Project interface
    - src/components/projects/project-card.tsx contains `Check out more information`
    - src/components/projects/project-card.tsx contains `ExternalLink`
    - src/components/projects/project-card.tsx contains `line-clamp-3`
    - src/components/projects/project-card.tsx contains `aspect-video`
    - src/components/projects/project-card.tsx contains `hover:border-[var(--accent)]`
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>Projects data layer exports getPublishedProjects, getProjectBySlug, getFeaturedProjects with same rate-limiting pattern as blog. ProjectCard component renders per UI spec.</done>
</task>

<task type="auto">
  <name>Task 2: Create projects listing and case study pages</name>
  <files>src/app/projects/page.tsx, src/app/projects/[slug]/page.tsx</files>
  <read_first>
    - src/app/blog/page.tsx (listing page pattern to mirror)
    - src/app/blog/[slug]/page.tsx (detail page pattern to mirror)
    - src/lib/notion-projects.ts (just created — read for exports)
    - src/components/projects/project-card.tsx (just created — read for import path)
    - src/components/notion/notion-renderer.tsx (for import)
  </read_first>
  <action>
1. Create `src/app/projects/page.tsx` (server component):
   - `export const revalidate = 1800` (ISR 30 minutes — same as blog)
   - `export const metadata = { title: 'Projects — Monty Singer', description: 'Things I\'ve built and invested in.' }`
   - Import `getPublishedProjects` from `@/lib/notion-projects` and `ProjectCard` from `@/components/projects/project-card`
   - Fetch all published projects: `const projects = await getPublishedProjects()`
   - Layout: `<div className="mx-auto max-w-5xl px-6 pb-24 pt-32">`
   - Page title: `<h1 className="text-3xl font-semibold tracking-tight sm:text-5xl">Projects</h1>`
   - Subtitle: `<p className="mt-4 text-lg text-[var(--fg-muted)]">Things I&apos;ve built and invested in.</p>`
   - Empty state (when `projects.length === 0`):
     ```tsx
     <div className="mt-16 text-center">
       <h2 className="text-xl font-semibold">Projects coming soon.</h2>
       <p className="mt-2 text-[var(--fg-muted)]">Case studies and build logs being added.</p>
     </div>
     ```
   - Grid (when projects exist): `<div className="mt-12 grid gap-8 sm:grid-cols-2 lg:grid-cols-3">`
   - Map projects to `<ProjectCard key={project.id} project={project} />`

2. Create `src/app/projects/[slug]/page.tsx` (server component):
   - `export const revalidate = 1800`
   - Import `notFound` from `next/navigation`, `getPublishedProjects` and `getProjectBySlug` from `@/lib/notion-projects`, `getBlocks` from `@/lib/notion`, `NotionRenderer` from `@/components/notion/notion-renderer`, types `Metadata` and `BlockObjectResponse`
   - `interface PageProps { params: Promise<{ slug: string }> }`
   - `generateStaticParams`: fetch all published projects, return `[{ slug: project.slug }]`
   - `generateMetadata`: fetch project by slug, return `{ title: '${project.title} — Monty Singer', description: project.description, openGraph: { title: project.title, description: project.description } }`
   - Page component: fetch project by slug, if null call `notFound()`. Fetch blocks via `getBlocks(project.id)`.
   - Layout mirroring blog post page: `<article className="mx-auto max-w-3xl px-6 pb-24 pt-32">`
   - Header: project title as h1 (`text-4xl font-bold tracking-tight sm:text-5xl`), description, tags, date, external link if exists
   - Content: `<div className="prose prose-neutral dark:prose-invert mt-12 max-w-none"><NotionRenderer blocks={blocks} /></div>`
   - External link in header: if `project.externalUrl`, add `<a href={project.externalUrl} target="_blank" rel="noopener noreferrer" className="inline-flex items-center gap-1 text-sm text-[var(--accent)] hover:underline">Visit project &rarr;</a>`
  </action>
  <verify>
    <automated>cd "/Users/Montster/MSizzle Personal Website" && npx next build 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - src/app/projects/page.tsx contains `export const revalidate = 1800`
    - src/app/projects/page.tsx contains `getPublishedProjects`
    - src/app/projects/page.tsx contains `Projects coming soon.`
    - src/app/projects/page.tsx contains `grid gap-8 sm:grid-cols-2 lg:grid-cols-3`
    - src/app/projects/[slug]/page.tsx contains `export const revalidate = 1800`
    - src/app/projects/[slug]/page.tsx contains `generateStaticParams`
    - src/app/projects/[slug]/page.tsx contains `generateMetadata`
    - src/app/projects/[slug]/page.tsx contains `NotionRenderer`
    - src/app/projects/[slug]/page.tsx contains `getBlocks`
    - src/app/projects/[slug]/page.tsx contains `notFound()`
    - `npx next build` exits 0
  </acceptance_criteria>
  <done>Projects listing at /projects shows card grid (or empty state). Case study pages at /projects/[slug] render Notion content with NotionRenderer. ISR at 30 minutes. Build passes.</done>
</task>

</tasks>

<verification>
- `npx next build` completes without errors
- `/projects` route renders project grid or empty state
- `/projects/[slug]` route renders Notion content via NotionRenderer
</verification>

<success_criteria>
1. `/projects` renders a responsive grid (3 cols desktop, 1 col mobile)
2. Empty state shows "Projects coming soon." text when no data
3. `/projects/[slug]` renders full case study with NotionRenderer
4. ISR revalidation at 30 minutes on both pages
5. generateStaticParams + generateMetadata on detail page
6. Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-pages/03-03-SUMMARY.md`
</output>
