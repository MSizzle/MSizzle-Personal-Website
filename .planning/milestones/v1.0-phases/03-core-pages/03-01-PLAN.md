---
phase: 03-core-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/utils/cn.ts
  - src/components/nav/navigation.tsx
  - src/components/footer.tsx
  - src/app/layout.tsx
  - package.json
  - .env.example
  - vitest.config.ts
  - src/__tests__/setup.ts
  - src/__tests__/pages/home.test.tsx
  - src/__tests__/pages/projects.test.tsx
  - src/__tests__/pages/blog.test.tsx
  - src/__tests__/pages/about.test.tsx
  - src/__tests__/pages/links.test.tsx
  - src/__tests__/pages/og-image.test.tsx
autonomous: true
requirements: [DSGN-05, SOC-01, DSGN-02]

must_haves:
  truths:
    - "Navigation bar appears fixed at top of every page with site name, links, theme toggle"
    - "Navigation collapses to hamburger on mobile (< 768px)"
    - "Footer appears at bottom of every page with social links and copyright"
    - "Design tokens (--bg-secondary, --fg-muted, --border, --accent) defined in globals.css"
    - "cn() utility available for conditional class merging"
    - "vitest runs successfully with zero tests (infrastructure ready)"
    - "Test stub files exist for all page routes under src/__tests__/pages/"
  artifacts:
    - path: "src/utils/cn.ts"
      provides: "cn() class merging utility"
      exports: ["cn"]
    - path: "src/components/nav/navigation.tsx"
      provides: "Fixed top navigation with mobile hamburger"
      min_lines: 50
    - path: "src/components/footer.tsx"
      provides: "Shared footer with social links"
      min_lines: 20
    - path: "src/app/globals.css"
      provides: "Extended design tokens"
      contains: "--bg-secondary"
    - path: "vitest.config.ts"
      provides: "Test framework configuration"
      contains: "vitest"
    - path: "src/__tests__/pages/home.test.tsx"
      provides: "Home page test stub"
      contains: "describe"
    - path: "src/__tests__/pages/blog.test.tsx"
      provides: "Blog page test stub"
      contains: "describe"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/components/nav/navigation.tsx"
      via: "import and render inside MotionProvider"
      pattern: "import.*Navigation"
    - from: "src/app/layout.tsx"
      to: "src/components/footer.tsx"
      via: "import and render inside MotionProvider"
      pattern: "import.*Footer"
    - from: "src/components/nav/navigation.tsx"
      to: "src/components/theme-toggle.tsx"
      via: "import and render in nav bar"
      pattern: "import.*ThemeToggle"
---

<objective>
Install shared dependencies, extend design tokens, create Navigation and Footer components, wire them into the root layout, set up vitest test infrastructure, and create Wave 0 test stubs for all page routes.

Purpose: Every subsequent plan depends on the shared layout shell (nav + footer) and design tokens being in place. Test infrastructure and stubs enable verification across the phase per VALIDATION.md.
Output: Working nav/footer visible on all pages, extended color tokens, cn() utility, vitest ready, test stubs for all pages.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-core-pages/03-CONTEXT.md
@.planning/phases/03-core-pages/03-UI-SPEC.md
@.planning/phases/03-core-pages/03-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/app/layout.tsx:
```typescript
// Provider hierarchy: ThemeProvider > LenisProvider > MotionProvider > {children}
// Navigation goes INSIDE MotionProvider, BEFORE {children}
// Footer goes INSIDE MotionProvider, AFTER {children}
```

From src/components/theme-toggle.tsx:
```typescript
export function ThemeToggle(): JSX.Element
// Existing component — import and place inside navigation
```

From src/app/globals.css:
```css
@import "tailwindcss";
@theme inline {
  --color-background: var(--bg);
  --color-foreground: var(--fg);
}
:root { --bg: #fafafa; --fg: #0a0a0a; }
.dark { --bg: #0a0a0a; --fg: #fafafa; }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies, create utilities, extend design tokens, set up vitest, create Wave 0 test stubs</name>
  <files>package.json, src/utils/cn.ts, src/app/globals.css, .env.example, vitest.config.ts, src/__tests__/setup.ts, src/__tests__/pages/home.test.tsx, src/__tests__/pages/projects.test.tsx, src/__tests__/pages/blog.test.tsx, src/__tests__/pages/about.test.tsx, src/__tests__/pages/links.test.tsx, src/__tests__/pages/og-image.test.tsx</files>
  <read_first>
    - src/app/globals.css
    - package.json
    - .env.example (if exists)
    - .env.local (for reference, do not modify)
  </read_first>
  <action>
1. Install runtime deps:
   ```
   npm install clsx tailwind-merge
   ```

2. Install dev deps for vitest:
   ```
   npm install -D vitest @vitejs/plugin-react jsdom @testing-library/react @testing-library/dom @testing-library/jest-dom
   ```

3. Create `src/utils/cn.ts`:
   ```typescript
   import { clsx, type ClassValue } from 'clsx'
   import { twMerge } from 'tailwind-merge'

   export function cn(...inputs: ClassValue[]) {
     return twMerge(clsx(inputs))
   }
   ```

4. Extend `src/app/globals.css` — add these variables to `:root` and `.dark` blocks (keep existing `--bg` and `--fg` unchanged, ADD the new ones below them):

   In `:root` block, add after `--fg: #0a0a0a;`:
   ```css
   --bg-secondary: #f0f0f0;
   --fg-muted: #737373;
   --border: #e5e5e5;
   --accent: #2563eb;
   ```

   In `.dark` block, add after `--fg: #fafafa;`:
   ```css
   --bg-secondary: #171717;
   --fg-muted: #a3a3a3;
   --border: #262626;
   --accent: #3b82f6;
   ```

   Also extend the `@theme inline` block to expose new tokens:
   ```css
   @theme inline {
     --color-background: var(--bg);
     --color-foreground: var(--fg);
     --color-bg-secondary: var(--bg-secondary);
     --color-fg-muted: var(--fg-muted);
     --color-border: var(--border);
     --color-accent: var(--accent);
     --font-sans: var(--font-geist-sans);
     --font-mono: var(--font-geist-mono);
   }
   ```

   Add `overflow-x: hidden;` to the `body` rule (prevents horizontal overflow per DSGN-02 / Pitfall 8 from RESEARCH.md).

5. Create/update `.env.example` with ALL required env vars:
   ```
   NOTION_TOKEN=
   NOTION_DATABASE_ID=
   NOTION_PROJECTS_DATABASE_ID=
   NEXT_PUBLIC_SITE_URL=https://msizzle.com
   ```

6. Create `vitest.config.ts`:
   ```typescript
   import { defineConfig } from 'vitest/config'
   import react from '@vitejs/plugin-react'
   import path from 'path'

   export default defineConfig({
     plugins: [react()],
     test: {
       environment: 'jsdom',
       setupFiles: ['./src/__tests__/setup.ts'],
       include: ['src/**/*.test.{ts,tsx}'],
     },
     resolve: {
       alias: {
         '@': path.resolve(__dirname, './src'),
       },
     },
   })
   ```

7. Create `src/__tests__/setup.ts`:
   ```typescript
   import '@testing-library/jest-dom/vitest'
   ```

8. Create Wave 0 test stub files. These are minimal render-test stubs that import the page component and verify it renders without crashing. Create the `src/__tests__/pages/` directory, then create each file:

   **`src/__tests__/pages/home.test.tsx`:**
   ```typescript
   import { describe, it, expect } from 'vitest'
   import { render, screen } from '@testing-library/react'

   describe('Home Page', () => {
     it.todo('renders hero section')
     it.todo('renders scroll sections')
   })
   ```

   **`src/__tests__/pages/projects.test.tsx`:**
   ```typescript
   import { describe, it, expect } from 'vitest'
   import { render, screen } from '@testing-library/react'

   describe('Projects Page', () => {
     it.todo('renders project cards')
     it.todo('renders project detail page')
   })
   ```

   **`src/__tests__/pages/blog.test.tsx`:**
   ```typescript
   import { describe, it, expect } from 'vitest'
   import { render, screen } from '@testing-library/react'

   describe('Blog Page', () => {
     it.todo('renders blog post list')
     it.todo('renders tag filter')
     it.todo('renders reading time')
   })
   ```

   **`src/__tests__/pages/about.test.tsx`:**
   ```typescript
   import { describe, it, expect } from 'vitest'
   import { render, screen } from '@testing-library/react'

   describe('About Page', () => {
     it.todo('renders prose narrative sections')
     it.todo('renders JSON-LD structured data')
   })
   ```

   **`src/__tests__/pages/links.test.tsx`:**
   ```typescript
   import { describe, it, expect } from 'vitest'
   import { render, screen } from '@testing-library/react'

   describe('Links Page', () => {
     it.todo('renders social link cards')
     it.todo('renders all expected links')
   })
   ```

   **`src/__tests__/pages/og-image.test.tsx`:**
   ```typescript
   import { describe, it, expect } from 'vitest'

   describe('OG Image Generation', () => {
     it.todo('default OG image exports correct size')
     it.todo('blog OG image renders title and date')
     it.todo('project OG image renders title')
   })
   ```

   These are `.todo` stubs — they will be picked up by vitest and reported as pending, but will NOT cause test failures. Each plan's executor can flesh out the actual test implementations when building the corresponding page.
  </action>
  <verify>
    <automated>cd "/Users/Montster/MSizzle Personal Website" && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/utils/cn.ts exists and contains `export function cn(`
    - src/app/globals.css contains `--bg-secondary: #f0f0f0`
    - src/app/globals.css contains `--accent: #2563eb`
    - src/app/globals.css .dark block contains `--accent: #3b82f6`
    - src/app/globals.css @theme inline block contains `--color-accent: var(--accent)`
    - src/app/globals.css body rule contains `overflow-x: hidden`
    - .env.example contains `NOTION_PROJECTS_DATABASE_ID=`
    - .env.example contains `NEXT_PUBLIC_SITE_URL=`
    - vitest.config.ts exists and contains `environment: 'jsdom'`
    - src/__tests__/setup.ts exists and contains `@testing-library/jest-dom`
    - src/__tests__/pages/home.test.tsx exists and contains `describe('Home Page'`
    - src/__tests__/pages/projects.test.tsx exists and contains `describe('Projects Page'`
    - src/__tests__/pages/blog.test.tsx exists and contains `describe('Blog Page'`
    - src/__tests__/pages/about.test.tsx exists and contains `describe('About Page'`
    - src/__tests__/pages/links.test.tsx exists and contains `describe('Links Page'`
    - src/__tests__/pages/og-image.test.tsx exists and contains `describe('OG Image'`
    - `npx vitest run` exits 0 (todo tests do not fail)
    - package.json contains `clsx` and `tailwind-merge` in dependencies
  </acceptance_criteria>
  <done>clsx + tailwind-merge installed, cn() utility created, globals.css has all 4 extended tokens in both :root and .dark, @theme inline exposes them, vitest configured and passing, .env.example documents all env vars, Wave 0 test stubs created for all 6 page test files under src/__tests__/pages/</done>
</task>

<task type="auto">
  <name>Task 2: Create Navigation and Footer components, wire into root layout</name>
  <files>src/components/nav/navigation.tsx, src/components/footer.tsx, src/app/layout.tsx</files>
  <read_first>
    - src/app/layout.tsx
    - src/components/theme-toggle.tsx
    - src/app/page.tsx (to see current inline nav that will be replaced)
    - src/app/globals.css (for token names)
    - .planning/phases/03-core-pages/03-UI-SPEC.md
  </read_first>
  <action>
1. Create `src/components/nav/navigation.tsx` as a `"use client"` component:
   - Import `useState` from react, `Link` from next/link, `usePathname` from next/navigation, `ThemeToggle` from `@/components/theme-toggle`, `Menu` and `X` icons from `lucide-react` (install lucide-react if missing — check package.json first, it should be available as transitive dep or install with `npm install lucide-react`)
   - Define `NAV_LINKS` array: `[{ href: '/', label: 'Home' }, { href: '/projects', label: 'Projects' }, { href: '/blog', label: 'Blog' }, { href: '/about', label: 'About' }, { href: '/links', label: 'Links' }]`
   - `Navigation()` component with `const [open, setOpen] = useState(false)` and `const pathname = usePathname()`
   - Outer `<header>`: `className="fixed inset-x-0 top-0 z-50 h-16 border-b border-[var(--border)]/50 bg-[var(--bg)]/80 backdrop-blur-sm"`
   - Inner `<nav>`: `className="mx-auto flex h-full max-w-5xl items-center justify-between px-6"`
   - Left: `<Link href="/" className="text-base font-semibold tracking-tight">Monty Singer</Link>`
   - Center: Desktop links in `<ul className="hidden items-center gap-6 md:flex">`. Each link: `<Link>` with `className` using cn() — `text-sm transition-colors hover:text-foreground` plus conditional: if `pathname === link.href` then `text-foreground border-b-2 border-[var(--accent)] pb-0.5` else `text-[var(--fg-muted)]`
   - Right: `<div className="flex items-center gap-4">` containing `<ThemeToggle />` and hamburger button `<button className="md:hidden min-h-[44px] min-w-[44px] flex items-center justify-center" onClick={() => setOpen(!open)} aria-label={open ? 'Close navigation menu' : 'Open navigation menu'}>` with `{open ? <X size={24} /> : <Menu size={24} />}`
   - Mobile drawer: conditionally render when `open` is true. Full-screen overlay: `<div className="fixed inset-0 top-16 z-40 bg-[var(--bg)] md:hidden">` with `<nav className="flex flex-col px-6 py-4">`. Each link: `<Link href={link.href} className="flex items-center py-3 text-base min-h-[48px] border-b border-[var(--border)]" onClick={() => setOpen(false)}>{link.label}</Link>`
   - Export: `export function Navigation()`

2. Create `src/components/footer.tsx` as a server component (no "use client"):
   - Import `Mail`, `Twitter`, `Linkedin`, `Github` icons from lucide-react
   - Define social links array: `[{ href: 'mailto:monty@msizzle.com', label: 'Email', icon: Mail }, { href: 'https://twitter.com/msizzle', label: 'Twitter / X', icon: Twitter }, { href: 'https://linkedin.com/in/montysinger', label: 'LinkedIn', icon: Linkedin }, { href: 'https://github.com/montysinger', label: 'GitHub', icon: Github }]`
   - `<footer className="border-t border-[var(--border)] bg-[var(--bg-secondary)]">`
   - Inner container: `<div className="mx-auto max-w-5xl px-6 py-12">`
   - Social icons row: `<div className="flex items-center justify-center gap-6">` with each link as `<a href={link.href} target="_blank" rel="noopener noreferrer" aria-label={link.label} className="text-[var(--fg-muted)] transition-colors hover:text-foreground"><link.icon size={20} /></a>`
   - Copyright: `<p className="mt-6 text-center text-sm text-[var(--fg-muted)]">&copy; {new Date().getFullYear()} Monty Singer</p>`
   - Export: `export function Footer()`

3. Update `src/app/layout.tsx`:
   - Add imports: `import { Navigation } from "@/components/nav/navigation"` and `import { Footer } from "@/components/footer"`
   - Inside the `<MotionProvider>` wrapper, add `<Navigation />` BEFORE `{children}` and `<Footer />` AFTER `{children}`:
     ```
     <MotionProvider>
       <Navigation />
       <main>{children}</main>
       <Footer />
     </MotionProvider>
     ```
   - Wrap `{children}` in `<main>` element for semantic HTML

4. Update `src/app/page.tsx` — remove the inline `<header>` block (lines 7-12) since Navigation is now in layout. Keep the hero and scroll sections.
  </action>
  <verify>
    <automated>cd "/Users/Montster/MSizzle Personal Website" && npx next build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/nav/navigation.tsx contains `'use client'`
    - src/components/nav/navigation.tsx contains `aria-label="Open navigation menu"`
    - src/components/nav/navigation.tsx contains `aria-label="Close navigation menu"`
    - src/components/nav/navigation.tsx contains `NAV_LINKS` array with 5 entries (Home, Projects, Blog, About, Links)
    - src/components/nav/navigation.tsx contains `min-h-[44px] min-w-[44px]` (WCAG touch target)
    - src/components/nav/navigation.tsx contains `usePathname`
    - src/components/nav/navigation.tsx contains `ThemeToggle`
    - src/components/footer.tsx contains `aria-label` attributes on icon links
    - src/components/footer.tsx contains `Monty Singer` in copyright
    - src/components/footer.tsx contains social link hrefs (mailto:, twitter.com, linkedin.com, github.com)
    - src/app/layout.tsx contains `import { Navigation }` and `import { Footer }`
    - src/app/layout.tsx renders `<Navigation />` before `<main>` and `<Footer />` after `</main>`
    - src/app/page.tsx no longer contains `<header` tag (inline nav removed)
    - `npx next build` exits 0
  </acceptance_criteria>
  <done>Navigation with mobile hamburger and active route highlighting visible on all pages. Footer with social links and copyright visible on all pages. Root layout wires both. Build succeeds.</done>
</task>

</tasks>

<verification>
- `npx next build` completes without errors
- `npx vitest run` exits 0 with all todo stubs reported
- Navigation and Footer render in the build output for all existing routes
- globals.css has all extended design tokens
- All 6 test stub files exist under src/__tests__/pages/
</verification>

<success_criteria>
1. Fixed navigation bar with 5 links + theme toggle appears on every page
2. Mobile hamburger menu works at < 768px with full-screen drawer
3. Footer with social icon links and copyright appears on every page
4. Design tokens (--bg-secondary, --fg-muted, --border, --accent) available in both light and dark mode
5. cn() utility importable from @/utils/cn
6. vitest configured and ready for test files
7. Wave 0 test stubs exist for home, projects, blog, about, links, og-image
8. Build passes with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-pages/03-01-SUMMARY.md`
</output>
