---
phase: 03-core-pages
plan: 05
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/app/about/page.tsx
  - src/app/links/page.tsx
autonomous: true
requirements: [ABOUT-01, SOC-01]

must_haves:
  truths:
    - "About page shows prose narrative with Georgetown, NYC, investing content"
    - "About page has section headings for intro, education, career, skills"
    - "Links page shows vertical card list with Email, Twitter/X, LinkedIn, GitHub, and newsletter CTA"
    - "Each link card has icon, label, and external URL"
    - "Both pages are responsive at 375px"
  artifacts:
    - path: "src/app/about/page.tsx"
      provides: "About/bio page"
      contains: "Georgetown"
    - path: "src/app/links/page.tsx"
      provides: "Social links hub page"
      contains: "Find Me Online"
  key_links:
    - from: "src/app/about/page.tsx"
      to: "JSON-LD"
      via: "Person structured data embedded in page"
      pattern: "application/ld\\+json"
    - from: "src/app/links/page.tsx"
      to: "external URLs"
      via: "anchor tags to social platforms"
      pattern: "twitter.com|linkedin.com|github.com"
---

<objective>
Build the About page (hardcoded prose narrative) and Links page (social links hub with vertical card list).

Purpose: About page tells Monty's story. Links page serves as a centralized social hub (Linktree replacement).
Output: Working `/about` and `/links` routes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-core-pages/03-CONTEXT.md
@.planning/phases/03-core-pages/03-UI-SPEC.md
@.planning/phases/03-core-pages/03-01-SUMMARY.md

<interfaces>
From 03-CONTEXT.md:
- D-12: Prose narrative, sections: intro/background, education (Georgetown), career (investing, NYC), skills
- D-13: Hardcoded content — no Notion dependency
- D-10: Linktree-inspired vertical card list
- D-11: Links: Email, Twitter/X, LinkedIn, GitHub, Newsletter signup CTA

From 03-UI-SPEC.md Links Page Cards:
- Full-width, bg-secondary border border-border rounded-xl
- min-h-16, px-4
- Icon 20px left, label text-base center, ArrowUpRight right
- gap-2, border-b border-border (except last)
- hover:bg-accent/5 hover:border-accent/30 transition-colors duration-150

From 03-UI-SPEC.md Copywriting:
- About h1: "About Me"
- Links h1: "Find Me Online"

From 03-UI-SPEC.md:
- Prose: rendered inside `prose prose-neutral dark:prose-invert` from @tailwindcss/typography
- About page includes JSON-LD Person structured data per D-17
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create About page with prose narrative and JSON-LD</name>
  <files>src/app/about/page.tsx</files>
  <read_first>
    - src/app/page.tsx (for JSON-LD pattern reference — it will have Person JSON-LD after Plan 02)
    - src/app/globals.css (for token variable names)
    - .planning/phases/03-core-pages/03-UI-SPEC.md (typography and spacing)
  </read_first>
  <action>
Create `src/app/about/page.tsx` as a server component:

1. Export metadata:
   ```typescript
   export const metadata = {
     title: 'About — Monty Singer',
     description: 'Background, education, career, and interests of Monty Singer.',
   }
   ```

2. Add Person JSON-LD (same as homepage for SEO reinforcement):
   ```typescript
   const personJsonLd = {
     '@context': 'https://schema.org',
     '@type': 'Person',
     name: 'Monty Singer',
     url: 'https://msizzle.com',
     sameAs: [
       'https://twitter.com/msizzle',
       'https://linkedin.com/in/montysinger',
       'https://github.com/montysinger',
     ],
     jobTitle: 'Investor',
     alumniOf: {
       '@type': 'CollegeOrUniversity',
       name: 'Georgetown University',
     },
     description: 'Investor, builder, and lifelong learner based in NYC.',
   }
   ```

3. Page layout:
   - Outer: `<div className="mx-auto max-w-3xl px-6 pb-24 pt-32">`
   - h1: `<h1 className="text-3xl font-semibold tracking-tight sm:text-5xl">About Me</h1>`
   - JSON-LD script tag
   - Content in `<div className="prose prose-neutral dark:prose-invert mt-12 max-w-none">`:

   Sections (all as regular prose HTML within the prose wrapper):

   **Intro paragraph:**
   ```html
   <p>I'm Monty Singer — an investor, builder, and perpetual student based in New York City. I spend my time at the intersection of technology and finance, always looking for what's next and trying to build things that matter.</p>
   ```

   **Education section:**
   ```html
   <h2>Education</h2>
   <p>I studied at Georgetown University in Washington, D.C., where I developed a foundation in analytical thinking and a deep curiosity about how systems work — from markets to technology to organizations. The experience shaped how I approach problems: with rigor, but also with an openness to unconventional ideas.</p>
   ```

   **Career section:**
   ```html
   <h2>Career</h2>
   <p>My career has centered on investing — understanding businesses, evaluating opportunities, and making decisions under uncertainty. Based in NYC, I work across the investment landscape, combining quantitative analysis with a builder's mindset.</p>
   <p>More recently, I've been channeling that same energy into building. Whether it's software tools, content, or experiments with AI, I believe the best way to understand something is to create it.</p>
   ```

   **Skills & Interests section:**
   ```html
   <h2>Skills &amp; Interests</h2>
   <p>I'm drawn to the space where technology meets real-world impact. My toolkit spans financial analysis, software development (with heavy AI assistance), and a growing comfort with the full stack of modern web development.</p>
   <p>Outside of work, I'm reading constantly, exploring NYC, and finding new rabbit holes to dive into. This site is one of those projects — a place to share what I'm learning and building.</p>
   ```
  </action>
  <verify>
    <automated>cd "/Users/Montster/MSizzle Personal Website" && npx next build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/app/about/page.tsx contains `About Me` (h1 text)
    - src/app/about/page.tsx contains `Georgetown` (education content)
    - src/app/about/page.tsx contains `New York City` or `NYC` (location)
    - src/app/about/page.tsx contains `investing` or `investor` (career)
    - src/app/about/page.tsx contains `application/ld+json` (JSON-LD)
    - src/app/about/page.tsx contains `Georgetown University` in JSON-LD
    - src/app/about/page.tsx contains `prose prose-neutral dark:prose-invert`
    - src/app/about/page.tsx contains h2 headings for Education, Career, Skills
    - src/app/about/page.tsx contains `title: 'About — Monty Singer'` in metadata
    - `npx next build` exits 0
  </acceptance_criteria>
  <done>About page renders prose narrative with 4 sections (intro, education, career, skills/interests) mentioning Georgetown, NYC, investing. JSON-LD Person schema embedded. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Create Links page with vertical card list</name>
  <files>src/app/links/page.tsx</files>
  <read_first>
    - src/app/globals.css (for token variable names)
    - .planning/phases/03-core-pages/03-UI-SPEC.md (links card spec)
    - src/components/footer.tsx (for social link URLs — keep consistent)
  </read_first>
  <action>
Create `src/app/links/page.tsx` as a server component:

1. Export metadata:
   ```typescript
   export const metadata = {
     title: 'Links — Monty Singer',
     description: 'Find Monty Singer online — social links, email, and newsletter.',
   }
   ```

2. Import icons from lucide-react: `Mail`, `Twitter`, `Linkedin`, `Github`, `ArrowUpRight`, `Newspaper` (for newsletter)

3. Define links array — NOTE: Newsletter card links to `/blog` (where the newsletter CTA form lives) instead of a dead `#newsletter` anchor:
   ```typescript
   const LINKS = [
     { href: 'mailto:monty@msizzle.com', label: 'Email', icon: Mail },
     { href: 'https://twitter.com/msizzle', label: 'Twitter / X', icon: Twitter },
     { href: 'https://linkedin.com/in/montysinger', label: 'LinkedIn', icon: Linkedin },
     { href: 'https://github.com/montysinger', label: 'GitHub', icon: Github },
     { href: '/blog', label: 'Subscribe to Newsletter', icon: Newspaper },
   ]
   ```

4. Page layout:
   - Outer: `<div className="mx-auto max-w-md px-6 pb-24 pt-32">` (narrower max-width for link hub — mobile-first per D-10)
   - h1: `<h1 className="text-center text-3xl font-semibold tracking-tight sm:text-5xl">Find Me Online</h1>`
   - Subtitle: `<p className="mt-4 text-center text-[var(--fg-muted)]">All the places you can find me on the internet.</p>`
   - Links list: `<div className="mt-12 flex flex-col gap-2">`
   - Each link card:
     ```tsx
     <a
       key={link.label}
       href={link.href}
       target={link.href.startsWith('http') ? '_blank' : undefined}
       rel={link.href.startsWith('http') ? 'noopener noreferrer' : undefined}
       className="flex min-h-16 items-center rounded-xl border border-[var(--border)] bg-[var(--bg-secondary)] px-4 transition-colors duration-150 hover:border-[var(--accent)]/30 hover:bg-[var(--accent)]/5"
     >
       <link.icon size={20} className="text-[var(--fg-muted)]" aria-label={link.label} />
       <span className="flex-1 text-center text-base">{link.label}</span>
       <ArrowUpRight size={20} className="text-[var(--fg-muted)]" aria-hidden="true" />
     </a>
     ```
   - Add border-b between cards (except last): use `[&:not(:last-child)]:border-b` or just rely on the gap + individual card borders (the cards already have borders per UI-SPEC, gap-2 provides visual separation)
  </action>
  <verify>
    <automated>cd "/Users/Montster/MSizzle Personal Website" && npx next build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/app/links/page.tsx contains `Find Me Online` (h1 text)
    - src/app/links/page.tsx contains `mailto:monty@msizzle.com`
    - src/app/links/page.tsx contains `twitter.com/msizzle`
    - src/app/links/page.tsx contains `linkedin.com/in/montysinger`
    - src/app/links/page.tsx contains `github.com/montysinger`
    - src/app/links/page.tsx contains `href: '/blog'` for newsletter card (NOT '#newsletter')
    - src/app/links/page.tsx contains `aria-label` on icon elements
    - src/app/links/page.tsx contains `aria-hidden="true"` on ArrowUpRight
    - src/app/links/page.tsx contains `min-h-16` (WCAG touch target height)
    - src/app/links/page.tsx contains `hover:border-[var(--accent)]` or `hover:bg-[var(--accent)]`
    - src/app/links/page.tsx contains `title: 'Links — Monty Singer'`
    - src/app/links/page.tsx does NOT contain `#newsletter` (dead anchor removed)
    - `npx next build` exits 0
  </acceptance_criteria>
  <done>Links page at /links displays vertical card list with Email, Twitter/X, LinkedIn, GitHub, and Newsletter links. Newsletter card links to /blog (where CTA form exists) instead of dead anchor. Each card has icon, label, and arrow indicator. Mobile-first layout. Build passes.</done>
</task>

</tasks>

<verification>
- `npx next build` completes without errors
- `/about` route renders prose narrative
- `/links` route renders social link cards
- Newsletter link on /links points to /blog (not a dead anchor)
</verification>

<success_criteria>
1. About page shows 4-section prose narrative with Georgetown, NYC, investing content
2. About page has JSON-LD Person structured data
3. Links page shows 5 link cards in vertical layout
4. All links point to correct external URLs or valid internal routes
5. Newsletter card on /links points to /blog (where newsletter CTA form lives)
6. Accessible: aria-labels on icons, adequate touch targets
7. Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-pages/03-05-SUMMARY.md`
</output>
