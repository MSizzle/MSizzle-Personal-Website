---
phase: 03-core-pages
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/app/page.tsx
autonomous: true
requirements: [HOME-01, HOME-02]

must_haves:
  truths:
    - "Hero section displays 'Hey, I'm Monty.' heading, tagline, profile photo placeholder, and two CTA buttons"
    - "Four scroll-driven narrative sections exist below hero: about snapshot, featured work, writing teaser, contact CTA"
    - "Each narrative section occupies min-h-screen viewport height"
    - "Page is responsive at 375px with no horizontal overflow"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Complete homepage with hero + 4 narrative sections"
      min_lines: 80
      contains: "Hey, I"
  key_links:
    - from: "src/app/page.tsx"
      to: "/projects"
      via: "CTA link in hero and featured work section"
      pattern: "href.*projects"
    - from: "src/app/page.tsx"
      to: "/blog"
      via: "CTA link in writing teaser section"
      pattern: "href.*blog"
---

<objective>
Replace the placeholder homepage with the full scroll-driven narrative layout: hero with profile photo + CTAs, plus four full-viewport sections (about snapshot, featured work, writing teaser, contact CTA).

Purpose: The homepage is the first impression — it must communicate who Monty is and guide visitors to key content areas.
Output: Complete homepage at `/` with hero + 4 narrative sections.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-core-pages/03-CONTEXT.md
@.planning/phases/03-core-pages/03-UI-SPEC.md
@.planning/phases/03-core-pages/03-01-SUMMARY.md

<interfaces>
<!-- Navigation and Footer are already in layout.tsx from Plan 01 -->
<!-- Homepage just needs page content — nav/footer wrap automatically -->

From 03-UI-SPEC.md Copywriting Contract:
- Hero h1: "Hey, I'm Monty." (locked per D-02)
- Hero tagline: "Investor, builder, and lifelong learner based in NYC." (locked)
- Hero CTA primary: "View My Work" (links to /projects)
- Hero CTA secondary: "Read My Writing" (links to /blog)

From 03-UI-SPEC.md Typography:
- Display: text-6xl sm:text-7xl font-semibold tracking-tight (hero h1)
- Heading: text-3xl sm:text-5xl font-semibold tracking-tight (section h2s)
- Body: text-base font-normal
- Label: text-sm font-normal

From 03-UI-SPEC.md Color:
- Accent CTA button: bg-[var(--accent)] text-white
- Muted text: text-[var(--fg-muted)]
- Section dividers: border-[var(--border)]

From 03-UI-SPEC.md Spacing:
- Hero top padding: py-24 (96px, offset for fixed nav)
- Section breaks: py-16 (64px)
- Full-viewport sections: min-h-screen
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build complete homepage with hero and scroll narrative sections</name>
  <files>src/app/page.tsx</files>
  <read_first>
    - src/app/page.tsx (current placeholder)
    - src/app/layout.tsx (to confirm nav/footer are in layout)
    - src/app/globals.css (for token variable names)
    - .planning/phases/03-core-pages/03-UI-SPEC.md
  </read_first>
  <action>
Replace entire `src/app/page.tsx` content. This is a Server Component (no "use client" needed).

Remove the existing ThemeToggle import (nav is now in layout).

Add JSON-LD Person structured data per D-17/SOC-04:
```typescript
const personJsonLd = {
  '@context': 'https://schema.org',
  '@type': 'Person',
  name: 'Monty Singer',
  url: 'https://msizzle.com',
  sameAs: [
    'https://twitter.com/msizzle',
    'https://linkedin.com/in/montysinger',
    'https://github.com/montysinger',
  ],
  jobTitle: 'Investor',
  description: 'Investor, builder, and lifelong learner based in NYC.',
}
```

Structure the page as follows:

**Section 1 — Hero** (`min-h-screen` with `flex items-center justify-center pt-24 px-6`):
- Container: `max-w-2xl text-center`
- Profile photo placeholder: `<div className="mx-auto mb-8 h-32 w-32 rounded-full bg-[var(--bg-secondary)] border border-[var(--border)]" />` (placeholder — real photo added when available)
- h1: `<h1 className="text-6xl font-semibold tracking-tight sm:text-7xl">Hey, I&apos;m Monty.</h1>`
- Tagline: `<p className="mt-6 text-lg text-[var(--fg-muted)]">Investor, builder, and lifelong learner based in NYC.</p>`
- CTA row: `<div className="mt-8 flex flex-col items-center gap-4 sm:flex-row sm:justify-center">`
  - Primary: `<Link href="/projects" className="inline-flex items-center rounded-lg bg-[var(--accent)] px-6 py-3 text-sm font-semibold text-white transition-opacity hover:opacity-90">View My Work</Link>`
  - Secondary: `<Link href="/blog" className="inline-flex items-center text-sm font-semibold text-[var(--fg-muted)] transition-colors hover:text-foreground">Read My Writing<span className="ml-1">&rarr;</span></Link>`

**Section 2 — About Snapshot** (`min-h-screen flex items-center justify-center px-6 py-16 border-t border-[var(--border)]`):
- Container: `max-w-xl text-center`
- h2: `<h2 className="text-3xl font-semibold tracking-tight sm:text-5xl">About</h2>`
- Body: `<p className="mt-6 text-[var(--fg-muted)] text-lg leading-relaxed">Georgetown grad turned investor, now building at the intersection of technology and finance in New York City. Always learning, always shipping.</p>`
- Link: `<Link href="/about" className="mt-6 inline-block text-sm font-semibold text-[var(--accent)] hover:underline">More about me &rarr;</Link>`

**Section 3 — Featured Work** (same layout pattern as About Snapshot):
- h2: `<h2 className="text-3xl font-semibold tracking-tight sm:text-5xl">Projects</h2>`
- Body: `<p className="mt-6 text-[var(--fg-muted)] text-lg leading-relaxed">Things I&apos;ve built and invested in — from software products to creative experiments.</p>`
- Link: `<Link href="/projects" className="mt-6 inline-block text-sm font-semibold text-[var(--accent)] hover:underline">See all projects &rarr;</Link>`

**Section 4 — Writing Teaser** (same layout pattern):
- h2: `<h2 className="text-3xl font-semibold tracking-tight sm:text-5xl">Writing</h2>`
- Body: `<p className="mt-6 text-[var(--fg-muted)] text-lg leading-relaxed">Ideas, observations, and lessons learned. On investing, building, and everything in between.</p>`
- Link: `<Link href="/blog" className="mt-6 inline-block text-sm font-semibold text-[var(--accent)] hover:underline">Read the blog &rarr;</Link>`

**Section 5 — Contact CTA** (same layout pattern):
- h2: `<h2 className="text-3xl font-semibold tracking-tight sm:text-5xl">Let&apos;s Connect</h2>`
- Body: `<p className="mt-6 text-[var(--fg-muted)] text-lg leading-relaxed">Always open to conversations about investing, technology, or interesting ideas.</p>`
- Link: `<a href="mailto:monty@msizzle.com" className="mt-6 inline-block text-sm font-semibold text-[var(--accent)] hover:underline">Say hello &rarr;</a>`

Include the JSON-LD script tag in the return JSX:
```tsx
<script
  type="application/ld+json"
  dangerouslySetInnerHTML={{ __html: JSON.stringify(personJsonLd) }}
/>
```

Import `Link` from `next/link`. Do NOT import ThemeToggle (removed from homepage — now in nav via layout).
  </action>
  <verify>
    <automated>cd "/Users/Montster/MSizzle Personal Website" && npx next build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/app/page.tsx contains `Hey, I` (the hero h1)
    - src/app/page.tsx contains `View My Work` (primary CTA text)
    - src/app/page.tsx contains `Read My Writing` (secondary CTA text)
    - src/app/page.tsx contains `href="/projects"` (CTA link)
    - src/app/page.tsx contains `href="/blog"` (CTA link)
    - src/app/page.tsx contains `href="/about"` (about section link)
    - src/app/page.tsx contains `min-h-screen` at least 5 times (hero + 4 sections)
    - src/app/page.tsx contains `application/ld+json` (JSON-LD structured data)
    - src/app/page.tsx contains `schema.org` in JSON-LD
    - src/app/page.tsx contains `Monty Singer` in JSON-LD
    - src/app/page.tsx does NOT contain `import { ThemeToggle` (removed — now in nav)
    - src/app/page.tsx does NOT contain `<header` (removed — nav in layout)
    - `npx next build` exits 0
  </acceptance_criteria>
  <done>Homepage displays hero with profile photo placeholder, "Hey, I'm Monty." heading, tagline, two CTAs, plus 4 scroll-driven narrative sections. JSON-LD Person structured data embedded. No horizontal overflow at 375px.</done>
</task>

</tasks>

<verification>
- `npx next build` completes without errors
- Homepage route `/` renders hero + 4 narrative sections
- JSON-LD script tag present in page output
</verification>

<success_criteria>
1. Hero section shows heading, tagline, photo placeholder, and two CTA buttons
2. Four narrative sections below hero (about, projects, writing, connect) each at min-h-screen
3. All internal links point to correct routes (/projects, /blog, /about)
4. JSON-LD Person structured data present
5. Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-pages/03-02-SUMMARY.md`
</output>
