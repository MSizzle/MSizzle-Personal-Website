// Deprecated as of Phase 7 (2026-04-15).
// The horizontal career-timeline UI was retired when the About page was rewritten
// per D-21 (founder-of-Prometheus framing). Past-job history is no longer surfaced.
// File retained as a no-op export to avoid breaking any stale imports during cleanup;
// safe to delete in a future commit.

export function HorizontalTimeline() {
  return null
}
