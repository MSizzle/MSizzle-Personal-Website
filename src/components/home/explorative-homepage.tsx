"use client";
// STUB — replaced in Plan 15-02 Task 3
// Minimal export so test scaffolds resolve. Real implementation carries forward
// gate detection (WebGL2 + touch/small + reduced-motion) from deck-homepage.tsx.
export function ExplorativeHomepage() {
  return null;
}
