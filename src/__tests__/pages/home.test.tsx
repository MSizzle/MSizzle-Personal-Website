import { describe, it, expect } from 'vitest'
import { render, screen } from '@testing-library/react'

describe('Home Page', () => {
  it.todo('renders hero section')
  it.todo('renders scroll sections')
})
