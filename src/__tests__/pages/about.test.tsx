import { describe, it, expect } from 'vitest'
import { render, screen } from '@testing-library/react'

describe('About Page', () => {
  it.todo('renders prose narrative sections')
  it.todo('renders JSON-LD structured data')
})
